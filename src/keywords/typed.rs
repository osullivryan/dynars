// @generated by codegen/gen_keywords.py --typed — do not edit.
#![cfg_attr(rustfmt, rustfmt::skip)]
#![allow(non_camel_case_types, non_snake_case)]
use crate::keyword::ParsedFile;
use crate::schema::parse_schema;

pub struct AIRBAG_ADIABATIC_GAS_MODEL {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub psf: Vec<f64>,
    pub lcid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub po: Vec<f64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
}
impl AIRBAG_ADIABATIC_GAS_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_ADIABATIC_GAS_MODEL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            psf: t.take("PSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            po: t.take("PO").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_ADIABATIC_GAS_MODEL_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub psf: Vec<f64>,
    pub lcid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub po: Vec<f64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
}
impl AIRBAG_ADIABATIC_GAS_MODEL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_ADIABATIC_GAS_MODEL_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            psf: t.take("PSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            po: t.take("PO").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_ALE {
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub unused_5: Vec<i64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub tnkvol: Vec<f64>,
    pub tnkfinp: Vec<f64>,
    pub nquad: Vec<i64>,
    pub ctype: Vec<i64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub frcmin: Vec<f64>,
    pub normtyp: Vec<i64>,
    pub ileak: Vec<i64>,
    pub pleak: Vec<f64>,
    pub ivsetid: Vec<i64>,
    pub ivtype: Vec<i64>,
    pub iblock: Vec<i64>,
    pub vntcof: Vec<f64>,
    pub nx_ida: Vec<i64>,
    pub ny_idg: Vec<i64>,
    pub nz: Vec<i64>,
    pub movern: Vec<i64>,
    pub zoom: Vec<i64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub swtime: Vec<f64>,
    pub unused_6: Vec<i64>,
    pub hg: Vec<f64>,
    pub nair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub lcvel: Vec<i64>,
    pub lct: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub mwair: Vec<f64>,
    pub initm: Vec<f64>,
    pub aira: Vec<f64>,
    pub airb: Vec<f64>,
    pub airc: Vec<f64>,
    pub lcmf: Vec<i64>,
    pub unused_8: Vec<i64>,
    pub unused_2_3: Vec<i64>,
    pub mwgas: Vec<f64>,
    pub unused_3_3: Vec<i64>,
    pub gasa: Vec<f64>,
    pub gasb: Vec<f64>,
    pub gasc: Vec<f64>,
    pub nodeid: Vec<i64>,
    pub vecid: Vec<i64>,
    pub orifare: Vec<f64>,
}
impl AIRBAG_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_ALE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_5: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            tnkvol: t.take("TNKVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            tnkfinp: t.take("TNKFINP").and_then(|c| c.into_float()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            normtyp: t.take("NORMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ileak: t.take("ILEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            pleak: t.take("PLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            ivsetid: t.take("IVSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ivtype: t.take("IVTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            iblock: t.take("IBLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            vntcof: t.take("VNTCOF").and_then(|c| c.into_float()).unwrap_or_default(),
            nx_ida: t.take("NX/IDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ny_idg: t.take("NY/IDG").and_then(|c| c.into_int()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_int()).unwrap_or_default(),
            movern: t.take("MOVERN").and_then(|c| c.into_int()).unwrap_or_default(),
            zoom: t.take("ZOOM").and_then(|c| c.into_int()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            swtime: t.take("SWTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            hg: t.take("HG").and_then(|c| c.into_float()).unwrap_or_default(),
            nair: t.take("NAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvel: t.take("LCVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            mwair: t.take("MWAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            aira: t.take("AIRA").and_then(|c| c.into_float()).unwrap_or_default(),
            airb: t.take("AIRB").and_then(|c| c.into_float()).unwrap_or_default(),
            airc: t.take("AIRC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcmf: t.take("LCMF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_8: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_3: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            mwgas: t.take("MWGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            gasa: t.take("GASA").and_then(|c| c.into_float()).unwrap_or_default(),
            gasb: t.take("GASB").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            orifare: t.take("ORIFARE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_FLUID_AND_GAS {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub xwini: Vec<f64>,
    pub xwadd: Vec<f64>,
    pub xw: Vec<f64>,
    pub p: Vec<f64>,
    pub tend: Vec<f64>,
    pub rho: Vec<f64>,
    pub lcxw: Vec<i64>,
    pub lcp: Vec<i64>,
    pub gdir: Vec<f64>,
    pub nproj: Vec<i64>,
    pub idir: Vec<i64>,
    pub iidir: Vec<i64>,
    pub kappa: Vec<f64>,
    pub kbm: Vec<f64>,
}
impl AIRBAG_FLUID_AND_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_FLUID_AND_GAS").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xwini: t.take("XWINI").and_then(|c| c.into_float()).unwrap_or_default(),
            xwadd: t.take("XWADD").and_then(|c| c.into_float()).unwrap_or_default(),
            xw: t.take("XW").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxw: t.take("LCXW").and_then(|c| c.into_int()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            gdir: t.take("GDIR").and_then(|c| c.into_float()).unwrap_or_default(),
            nproj: t.take("NPROJ").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            iidir: t.take("IIDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            kbm: t.take("KBM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_FLUID_AND_GAS_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub xwini: Vec<f64>,
    pub xwadd: Vec<f64>,
    pub xw: Vec<f64>,
    pub p: Vec<f64>,
    pub tend: Vec<f64>,
    pub rho: Vec<f64>,
    pub lcxw: Vec<i64>,
    pub lcp: Vec<i64>,
    pub gdir: Vec<f64>,
    pub nproj: Vec<i64>,
    pub idir: Vec<i64>,
    pub iidir: Vec<i64>,
    pub kappa: Vec<f64>,
    pub kbm: Vec<f64>,
}
impl AIRBAG_FLUID_AND_GAS_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_FLUID_AND_GAS_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xwini: t.take("XWINI").and_then(|c| c.into_float()).unwrap_or_default(),
            xwadd: t.take("XWADD").and_then(|c| c.into_float()).unwrap_or_default(),
            xw: t.take("XW").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxw: t.take("LCXW").and_then(|c| c.into_int()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            gdir: t.take("GDIR").and_then(|c| c.into_float()).unwrap_or_default(),
            nproj: t.take("NPROJ").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            iidir: t.take("IIDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            kbm: t.take("KBM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
}
impl AIRBAG_HYBRID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_CHEMKIN {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub ngas: Vec<i64>,
    pub data: Vec<i64>,
    pub atmt: Vec<f64>,
    pub atmp: Vec<f64>,
    pub rg: Vec<f64>,
    pub hconv: Vec<f64>,
    pub c23: Vec<f64>,
    pub a23: Vec<f64>,
    pub chname: Vec<String>,
    pub mw: Vec<f64>,
    pub lcidn: Vec<i64>,
    pub fmole: Vec<f64>,
    pub fmolet: Vec<f64>,
    pub tlow: Vec<f64>,
    pub tmid: Vec<f64>,
    pub thigh: Vec<f64>,
    pub alow: Vec<f64>,
    pub blow: Vec<f64>,
    pub clow: Vec<f64>,
    pub dlow: Vec<f64>,
    pub elow: Vec<f64>,
    pub flow: Vec<f64>,
    pub glow: Vec<f64>,
    pub ahigh: Vec<f64>,
    pub bhigh: Vec<f64>,
    pub chigh: Vec<f64>,
    pub dhigh: Vec<f64>,
    pub ehigh: Vec<f64>,
    pub fhigh: Vec<f64>,
    pub ghigh: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub e: Vec<f64>,
}
impl AIRBAG_HYBRID_CHEMKIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_CHEMKIN").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_int()).unwrap_or_default(),
            atmt: t.take("ATMT").and_then(|c| c.into_float()).unwrap_or_default(),
            atmp: t.take("ATMP").and_then(|c| c.into_float()).unwrap_or_default(),
            rg: t.take("RG").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            chname: t.take("CHNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidn: t.take("LCIDN").and_then(|c| c.into_int()).unwrap_or_default(),
            fmole: t.take("FMOLE").and_then(|c| c.into_float()).unwrap_or_default(),
            fmolet: t.take("FMOLET").and_then(|c| c.into_float()).unwrap_or_default(),
            tlow: t.take("TLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_float()).unwrap_or_default(),
            thigh: t.take("THIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            alow: t.take("ALOW").and_then(|c| c.into_float()).unwrap_or_default(),
            blow: t.take("BLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            clow: t.take("CLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            dlow: t.take("DLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            elow: t.take("ELOW").and_then(|c| c.into_float()).unwrap_or_default(),
            flow: t.take("FLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            glow: t.take("GLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            ahigh: t.take("AHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            bhigh: t.take("BHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            chigh: t.take("CHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            dhigh: t.take("DHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            ehigh: t.take("EHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            fhigh: t.take("FHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            ghigh: t.take("GHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_CHEMKIN_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub ngas: Vec<i64>,
    pub data: Vec<i64>,
    pub atmt: Vec<f64>,
    pub atmp: Vec<f64>,
    pub rg: Vec<f64>,
    pub hconv: Vec<f64>,
    pub c23: Vec<f64>,
    pub a23: Vec<f64>,
    pub chname: Vec<String>,
    pub mw: Vec<f64>,
    pub lcidn: Vec<i64>,
    pub fmole: Vec<f64>,
    pub fmolet: Vec<f64>,
    pub tlow: Vec<f64>,
    pub tmid: Vec<f64>,
    pub thigh: Vec<f64>,
    pub alow: Vec<f64>,
    pub blow: Vec<f64>,
    pub clow: Vec<f64>,
    pub dlow: Vec<f64>,
    pub elow: Vec<f64>,
    pub flow: Vec<f64>,
    pub glow: Vec<f64>,
    pub ahigh: Vec<f64>,
    pub bhigh: Vec<f64>,
    pub chigh: Vec<f64>,
    pub dhigh: Vec<f64>,
    pub ehigh: Vec<f64>,
    pub fhigh: Vec<f64>,
    pub ghigh: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub e: Vec<f64>,
}
impl AIRBAG_HYBRID_CHEMKIN_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_CHEMKIN_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_int()).unwrap_or_default(),
            atmt: t.take("ATMT").and_then(|c| c.into_float()).unwrap_or_default(),
            atmp: t.take("ATMP").and_then(|c| c.into_float()).unwrap_or_default(),
            rg: t.take("RG").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            chname: t.take("CHNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidn: t.take("LCIDN").and_then(|c| c.into_int()).unwrap_or_default(),
            fmole: t.take("FMOLE").and_then(|c| c.into_float()).unwrap_or_default(),
            fmolet: t.take("FMOLET").and_then(|c| c.into_float()).unwrap_or_default(),
            tlow: t.take("TLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_float()).unwrap_or_default(),
            thigh: t.take("THIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            alow: t.take("ALOW").and_then(|c| c.into_float()).unwrap_or_default(),
            blow: t.take("BLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            clow: t.take("CLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            dlow: t.take("DLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            elow: t.take("ELOW").and_then(|c| c.into_float()).unwrap_or_default(),
            flow: t.take("FLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            glow: t.take("GLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            ahigh: t.take("AHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            bhigh: t.take("BHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            chigh: t.take("CHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            dhigh: t.take("DHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            ehigh: t.take("EHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            fhigh: t.take("FHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            ghigh: t.take("GHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
}
impl AIRBAG_HYBRID_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_JETTING {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub idum: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_HYBRID_JETTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_JETTING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idum: t.take("IDUM").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_JETTING_CM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub idum: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_HYBRID_JETTING_CM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_JETTING_CM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idum: t.take("IDUM").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_JETTING_CM_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub idum: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_HYBRID_JETTING_CM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_JETTING_CM_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idum: t.take("IDUM").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_HYBRID_JETTING_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub atmost: Vec<f64>,
    pub atmosp: Vec<f64>,
    pub atmosd: Vec<f64>,
    pub gc: Vec<f64>,
    pub cc: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lcp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub opt: Vec<i64>,
    pub pvent: Vec<f64>,
    pub ngas: Vec<i64>,
    pub lcefr: Vec<i64>,
    pub lcidm0: Vec<i64>,
    pub vntopt: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub not_used: Vec<f64>,
    pub mw: Vec<f64>,
    pub initm: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub fmass: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub idum: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_HYBRID_JETTING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_HYBRID_JETTING_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmost: t.take("ATMOST").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosp: t.take("ATMOSP").and_then(|c| c.into_float()).unwrap_or_default(),
            atmosd: t.take("ATMOSD").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp23: t.take("LCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pvent: t.take("PVENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm0: t.take("LCIDM0").and_then(|c| c.into_int()).unwrap_or_default(),
            vntopt: t.take("VNTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            initm: t.take("INITM").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmass: t.take("FMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idum: t.take("IDUM").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_INTERACTION {
    pub ab1: Vec<i64>,
    pub ab2: Vec<i64>,
    pub area: Vec<f64>,
    pub sf: Vec<f64>,
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub iflow: Vec<i64>,
}
impl AIRBAG_INTERACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_INTERACTION").unwrap());
        Self {
            ab1: t.take("AB1").and_then(|c| c.into_int()).unwrap_or_default(),
            ab2: t.take("AB2").and_then(|c| c.into_int()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            iflow: t.take("IFLOW").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_LINEAR_FLUID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub bulk: Vec<f64>,
    pub ro: Vec<f64>,
    pub lcint: Vec<i64>,
    pub lcoutt: Vec<i64>,
    pub lcoutp: Vec<i64>,
    pub lcfit: Vec<i64>,
    pub lcbulk: Vec<i64>,
    pub lcid: Vec<i64>,
    pub p_limit: Vec<f64>,
    pub p_limlc: Vec<i64>,
    pub nonull: Vec<i64>,
}
impl AIRBAG_LINEAR_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_LINEAR_FLUID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcoutt: t.take("LCOUTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcoutp: t.take("LCOUTP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfit: t.take("LCFIT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbulk: t.take("LCBULK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            p_limit: t.take("P_LIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            p_limlc: t.take("P_LIMLC").and_then(|c| c.into_int()).unwrap_or_default(),
            nonull: t.take("NONULL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_LINEAR_FLUID_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub bulk: Vec<f64>,
    pub ro: Vec<f64>,
    pub lcint: Vec<i64>,
    pub lcoutt: Vec<i64>,
    pub lcoutp: Vec<i64>,
    pub lcfit: Vec<i64>,
    pub lcbulk: Vec<i64>,
    pub lcid: Vec<i64>,
    pub p_limit: Vec<f64>,
    pub p_limlc: Vec<i64>,
    pub nonull: Vec<i64>,
}
impl AIRBAG_LINEAR_FLUID_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_LINEAR_FLUID_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcoutt: t.take("LCOUTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcoutp: t.take("LCOUTP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfit: t.take("LCFIT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbulk: t.take("LCBULK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            p_limit: t.take("P_LIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            p_limlc: t.take("P_LIMLC").and_then(|c| c.into_int()).unwrap_or_default(),
            nonull: t.take("NONULL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_LOAD_CURVE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub stime: Vec<f64>,
    pub lcid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pe: Vec<f64>,
    pub p0: Vec<f64>,
    pub t: Vec<f64>,
    pub t0: Vec<f64>,
}
impl AIRBAG_LOAD_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_LOAD_CURVE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_LOAD_CURVE_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub stime: Vec<f64>,
    pub lcid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pe: Vec<f64>,
    pub p0: Vec<f64>,
    pub t: Vec<f64>,
    pub t0: Vec<f64>,
}
impl AIRBAG_LOAD_CURVE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_LOAD_CURVE_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_INFLATION_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_JET_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_MOLEFRACTION_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_DECOMPOSITION_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_DECOMPOSITION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_DECOMPOSITION_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_INFLATION_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_INFLATION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_INFLATION_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_JET_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_JET_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_JET_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MOLEFRACTION_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MOLEFRACTION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MOLEFRACTION_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
}
impl AIRBAG_PARTICLE_MPP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_INFLATION_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_JET_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_MOLEFRACTION_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_DECOMPOSITION_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_INFLATION_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_INFLATION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_INFLATION_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_JET_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub jnode: Vec<i64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_JET_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_JET_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            jnode: t.take("JNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmass: Vec<i64>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_MOLEFRACTION_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmass: t.take("LCMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_SEGMENT {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_SEGMENT").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_SEGMENT_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_SEGMENT_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_SEGMENT_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_SEGMENT_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_SEGMENT_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_SEGMENT_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_TIME {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_TIME").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_MPP_TIME_ID {
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_MPP_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_MPP_TIME_ID").unwrap());
        Self {
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_SEGMENT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_SEGMENT_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_SEGMENT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_SEGMENT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_SEGMENT_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_SEGMENT_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_SEGMENT_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_SEGMENT_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub segsid: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_SEGMENT_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_SEGMENT_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_TIME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_TIME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_PARTICLE_TIME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub sid1: Vec<i64>,
    pub stype1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub stype2: Vec<i64>,
    pub block: Vec<i64>,
    pub npdata: Vec<i64>,
    pub fric: Vec<f64>,
    pub irdp: Vec<i64>,
    pub np: Vec<i64>,
    pub unit: Vec<i64>,
    pub visflg: Vec<i64>,
    pub tatm: Vec<f64>,
    pub patm: Vec<f64>,
    pub nvent: Vec<i64>,
    pub tend: Vec<f64>,
    pub tsw: Vec<f64>,
    pub tstop: Vec<f64>,
    pub tsmth: Vec<f64>,
    pub occup: Vec<f64>,
    pub rebl: Vec<i64>,
    pub sidsv: Vec<i64>,
    pub psid1: Vec<i64>,
    pub tsplit: Vec<f64>,
    pub sffdc: Vec<f64>,
    pub sfiair4: Vec<f64>,
    pub idfric: Vec<i64>,
    pub f0: Vec<i64>,
    pub mass: Vec<f64>,
    pub f20: Vec<i64>,
    pub time: Vec<f64>,
    pub f40: Vec<i64>,
    pub length: Vec<f64>,
    pub iair: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub chm: Vec<i64>,
    pub cd_ext: Vec<f64>,
    pub sidup: Vec<i64>,
    pub styup: Vec<i64>,
    pub pfrac: Vec<f64>,
    pub linking: Vec<i64>,
    pub sidh: Vec<i64>,
    pub stypeh: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub sid3: Vec<i64>,
    pub stype3: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub pair: Vec<f64>,
    pub tair: Vec<f64>,
    pub xmair: Vec<f64>,
    pub aair: Vec<f64>,
    pub bair: Vec<f64>,
    pub cair: Vec<f64>,
    pub npair: Vec<i64>,
    pub nprlx: Vec<String>,
    pub lcmi: Vec<i64>,
    pub lcti: Vec<i64>,
    pub xmi: Vec<f64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub infgi: Vec<i64>,
    pub nidi: Vec<i64>,
    pub ani: Vec<f64>,
    pub vdi: Vec<i64>,
    pub cai: Vec<f64>,
    pub infoi: Vec<i64>,
    pub imom: Vec<i64>,
    pub iang: Vec<i64>,
    pub chm_id: Vec<i64>,
}
impl AIRBAG_PARTICLE_TIME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_PARTICLE_TIME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            stype1: t.take("STYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            stype2: t.take("STYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            npdata: t.take("NPDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            irdp: t.take("IRDP").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            visflg: t.take("VISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tatm: t.take("TATM").and_then(|c| c.into_float()).unwrap_or_default(),
            patm: t.take("PATM").and_then(|c| c.into_float()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tsw: t.take("TSW").and_then(|c| c.into_float()).unwrap_or_default(),
            tstop: t.take("TSTOP").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmth: t.take("TSMTH").and_then(|c| c.into_float()).unwrap_or_default(),
            occup: t.take("OCCUP").and_then(|c| c.into_float()).unwrap_or_default(),
            rebl: t.take("REBL").and_then(|c| c.into_int()).unwrap_or_default(),
            sidsv: t.take("SIDSV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            tsplit: t.take("TSPLIT").and_then(|c| c.into_float()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfiair4: t.take("SFIAIR4").and_then(|c| c.into_float()).unwrap_or_default(),
            idfric: t.take("IDFRIC").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("Mass").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("Time").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("Length").and_then(|c| c.into_float()).unwrap_or_default(),
            iair: t.take("IAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            chm: t.take("CHM").and_then(|c| c.into_int()).unwrap_or_default(),
            cd_ext: t.take("CD_EXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sidup: t.take("SIDUP").and_then(|c| c.into_int()).unwrap_or_default(),
            styup: t.take("STYUP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfrac: t.take("PFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            linking: t.take("LINKING").and_then(|c| c.into_int()).unwrap_or_default(),
            sidh: t.take("SIDH").and_then(|c| c.into_int()).unwrap_or_default(),
            stypeh: t.take("STYPEH").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            stype3: t.take("STYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            pair: t.take("PAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tair: t.take("TAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            xmair: t.take("XMAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            aair: t.take("AAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            bair: t.take("BAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            cair: t.take("CAIR").and_then(|c| c.into_float()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprlx: t.take("NPRLX").and_then(|c| c.into_str()).unwrap_or_default(),
            lcmi: t.take("LCMi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcti: t.take("LCTi").and_then(|c| c.into_int()).unwrap_or_default(),
            xmi: t.take("XMi").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("Ci").and_then(|c| c.into_float()).unwrap_or_default(),
            infgi: t.take("INFGi").and_then(|c| c.into_int()).unwrap_or_default(),
            nidi: t.take("NIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            ani: t.take("ANi").and_then(|c| c.into_float()).unwrap_or_default(),
            vdi: t.take("VDi").and_then(|c| c.into_int()).unwrap_or_default(),
            cai: t.take("CAi").and_then(|c| c.into_float()).unwrap_or_default(),
            infoi: t.take("INFOi").and_then(|c| c.into_int()).unwrap_or_default(),
            imom: t.take("IMOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iang: t.take("IANG").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_BIRTH {
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_BIRTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_BIRTH").unwrap());
        Self {
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_BIRTH_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nido: Vec<i64>,
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_BIRTH_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_BIRTH_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nido: t.take("NIDO").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_BIRTH_RDT {
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_BIRTH_RDT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_BIRTH_RDT").unwrap());
        Self {
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nido: Vec<i64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nido: t.take("NIDO").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_RDT {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_RDT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_RDT").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH {
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH").unwrap());
        Self {
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nido: Vec<i64>,
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_RDT_BIRTH_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nido: t.take("NIDO").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_REFERENCE_GEOMETRY_RDT_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nido: Vec<i64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl AIRBAG_REFERENCE_GEOMETRY_RDT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_REFERENCE_GEOMETRY_RDT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nido: t.take("NIDO").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SHELL_REFERENCE_GEOMETRY {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl AIRBAG_SHELL_REFERENCE_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SHELL_REFERENCE_GEOMETRY").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SHELL_REFERENCE_GEOMETRY_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nid: Vec<i64>,
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl AIRBAG_SHELL_REFERENCE_GEOMETRY_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SHELL_REFERENCE_GEOMETRY_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT_ID {
    pub id: Vec<i64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub sz: Vec<f64>,
    pub nid: Vec<i64>,
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SHELL_REFERENCE_GEOMETRY_RDT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            sz: t.take("SZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SIMPLE_AIRBAG_MODEL {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lcid: Vec<i64>,
    pub mu: Vec<f64>,
    pub area: Vec<f64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub lou: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
}
impl AIRBAG_SIMPLE_AIRBAG_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SIMPLE_AIRBAG_MODEL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lou: t.take("LOU").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SIMPLE_AIRBAG_MODEL_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lcid: Vec<i64>,
    pub mu: Vec<f64>,
    pub area: Vec<f64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub lou: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
}
impl AIRBAG_SIMPLE_AIRBAG_MODEL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SIMPLE_AIRBAG_MODEL_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lou: t.take("LOU").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SIMPLE_PRESSURE_VOLUME {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cn: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcid: Vec<i64>,
    pub lciddr: Vec<i64>,
}
impl AIRBAG_SIMPLE_PRESSURE_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SIMPLE_PRESSURE_VOLUME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_SIMPLE_PRESSURE_VOLUME_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cn: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcid: Vec<i64>,
    pub lciddr: Vec<i64>,
}
impl AIRBAG_SIMPLE_PRESSURE_VOLUME_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_SIMPLE_PRESSURE_VOLUME_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
}
impl AIRBAG_WANG_NEFSKE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
}
impl AIRBAG_WANG_NEFSKE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_CM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_CM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_CM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_CM_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_CM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_CM_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_POP {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_POP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_POP").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_POP_CM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_POP_CM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_POP_CM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_POP_CM_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_POP_CM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_POP_CM_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_JETTING_POP_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub ca: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_JETTING_POP_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_JETTING_POP_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_CM_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
    pub nreact: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_CM_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            nreact: t.take("NREACT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
    pub xjfp: Vec<f64>,
    pub yjfp: Vec<f64>,
    pub zjfp: Vec<f64>,
    pub xjvh: Vec<f64>,
    pub yjvh: Vec<f64>,
    pub zjvh: Vec<f64>,
    pub lcjrv: Vec<i64>,
    pub beta: Vec<f64>,
    pub xsjfp: Vec<f64>,
    pub ysjfp: Vec<f64>,
    pub zsjfp: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_MULTIPLE_JETTING_POP_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            xjfp: t.take("XJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            yjfp: t.take("YJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zjfp: t.take("ZJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            xjvh: t.take("XJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            yjvh: t.take("YJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            zjvh: t.take("ZJVH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjrv: t.take("LCJRV").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsjfp: t.take("XSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            ysjfp: t.take("YSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            zsjfp: t.take("ZSJFP").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_POP {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_POP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_POP").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct AIRBAG_WANG_NEFSKE_POP_ID {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub rbid: Vec<i64>,
    pub vsca: Vec<f64>,
    pub psca: Vec<f64>,
    pub vini: Vec<f64>,
    pub mwd: Vec<f64>,
    pub spsf: Vec<f64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub t: Vec<f64>,
    pub lct: Vec<i64>,
    pub lcmt: Vec<i64>,
    pub tvol: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub iabt: Vec<f64>,
    pub c23: Vec<f64>,
    pub lcc23: Vec<i64>,
    pub a23: Vec<f64>,
    pub lca23: Vec<i64>,
    pub cp23: Vec<f64>,
    pub lccp23: Vec<i64>,
    pub ap23: Vec<f64>,
    pub lcap23: Vec<i64>,
    pub pe: Vec<f64>,
    pub ro: Vec<f64>,
    pub gc: Vec<f64>,
    pub lcefr: Vec<i64>,
    pub pover: Vec<f64>,
    pub ppop: Vec<f64>,
    pub opt: Vec<i64>,
    pub knkdn: Vec<i64>,
    pub ioc: Vec<f64>,
    pub ioa: Vec<f64>,
    pub ivol: Vec<f64>,
    pub iro: Vec<f64>,
    pub it: Vec<f64>,
    pub lcbf: Vec<i64>,
    pub text: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub mw: Vec<f64>,
    pub gasc: Vec<f64>,
    pub hconv: Vec<f64>,
    pub tdp: Vec<f64>,
    pub axp: Vec<f64>,
    pub ayp: Vec<f64>,
    pub azp: Vec<f64>,
    pub amagp: Vec<f64>,
    pub tdurp: Vec<f64>,
    pub tda: Vec<f64>,
    pub rbidp: Vec<i64>,
}
impl AIRBAG_WANG_NEFSKE_POP_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("AIRBAG_WANG_NEFSKE_POP_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid: t.take("RBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vsca: t.take("VSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            psca: t.take("PSCA").and_then(|c| c.into_float()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
            mwd: t.take("MWD").and_then(|c| c.into_float()).unwrap_or_default(),
            spsf: t.take("SPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt: t.take("LCMT").and_then(|c| c.into_int()).unwrap_or_default(),
            tvol: t.take("TVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            iabt: t.take("IABT").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc23: t.take("LCC23").and_then(|c| c.into_int()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            lca23: t.take("LCA23").and_then(|c| c.into_int()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lccp23: t.take("LCCP23").and_then(|c| c.into_int()).unwrap_or_default(),
            ap23: t.take("AP23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcap23: t.take("LCAP23").and_then(|c| c.into_int()).unwrap_or_default(),
            pe: t.take("PE").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcefr: t.take("LCEFR").and_then(|c| c.into_int()).unwrap_or_default(),
            pover: t.take("POVER").and_then(|c| c.into_float()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            knkdn: t.take("KNKDN").and_then(|c| c.into_int()).unwrap_or_default(),
            ioc: t.take("IOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ioa: t.take("IOA").and_then(|c| c.into_float()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iro: t.take("IRO").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbf: t.take("LCBF").and_then(|c| c.into_int()).unwrap_or_default(),
            text: t.take("TEXT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            mw: t.take("MW").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            tdp: t.take("TDP").and_then(|c| c.into_float()).unwrap_or_default(),
            axp: t.take("AXP").and_then(|c| c.into_float()).unwrap_or_default(),
            ayp: t.take("AYP").and_then(|c| c.into_float()).unwrap_or_default(),
            azp: t.take("AZP").and_then(|c| c.into_float()).unwrap_or_default(),
            amagp: t.take("AMAGP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdurp: t.take("TDURP").and_then(|c| c.into_float()).unwrap_or_default(),
            tda: t.take("TDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbidp: t.take("RBIDP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_AMBIENT_HYDROSTATIC {
    pub alesid: Vec<i64>,
    pub stype: Vec<i64>,
    pub vecid: Vec<i64>,
    pub grav: Vec<f64>,
    pub pbase: Vec<f64>,
    pub ramptlc: Vec<i64>,
    pub nid: Vec<i64>,
    pub mmgbl: Vec<i64>,
}
impl ALE_AMBIENT_HYDROSTATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_AMBIENT_HYDROSTATIC").unwrap());
        Self {
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            pbase: t.take("PBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            ramptlc: t.take("RAMPTLC").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgbl: t.take("MMGBL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_BURN_SWITCH_MMG {
    pub mmgfr: Vec<i64>,
    pub mmgto: Vec<i64>,
    pub nvarline: Vec<i64>,
    pub react: Vec<i64>,
    pub igni: Vec<i64>,
    pub igniv: Vec<i64>,
    pub ignivf: Vec<i64>,
    pub var: Vec<i64>,
    pub var_2: Vec<i64>,
    pub var_3: Vec<i64>,
    pub var_4: Vec<i64>,
    pub var_5: Vec<i64>,
    pub var_6: Vec<i64>,
    pub var_7: Vec<i64>,
    pub var_8: Vec<i64>,
    pub par: Vec<f64>,
    pub par_2: Vec<f64>,
    pub par_3: Vec<f64>,
    pub par_4: Vec<f64>,
    pub par_5: Vec<f64>,
    pub par_6: Vec<f64>,
    pub par_7: Vec<f64>,
    pub par_8: Vec<f64>,
}
impl ALE_BURN_SWITCH_MMG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_BURN_SWITCH_MMG").unwrap());
        Self {
            mmgfr: t.take("MMGFR").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgto: t.take("MMGTO").and_then(|c| c.into_int()).unwrap_or_default(),
            nvarline: t.take("NVARLINE").and_then(|c| c.into_int()).unwrap_or_default(),
            react: t.take("REACT").and_then(|c| c.into_int()).unwrap_or_default(),
            igni: t.take("IGNI").and_then(|c| c.into_int()).unwrap_or_default(),
            igniv: t.take("IGNIV").and_then(|c| c.into_int()).unwrap_or_default(),
            ignivf: t.take("IGNIVF").and_then(|c| c.into_int()).unwrap_or_default(),
            var: t.take("VAR").and_then(|c| c.into_int()).unwrap_or_default(),
            var_2: t.take("VAR_2").and_then(|c| c.into_int()).unwrap_or_default(),
            var_3: t.take("VAR_3").and_then(|c| c.into_int()).unwrap_or_default(),
            var_4: t.take("VAR_4").and_then(|c| c.into_int()).unwrap_or_default(),
            var_5: t.take("VAR_5").and_then(|c| c.into_int()).unwrap_or_default(),
            var_6: t.take("VAR_6").and_then(|c| c.into_int()).unwrap_or_default(),
            var_7: t.take("VAR_7").and_then(|c| c.into_int()).unwrap_or_default(),
            var_8: t.take("VAR_8").and_then(|c| c.into_int()).unwrap_or_default(),
            par: t.take("PAR").and_then(|c| c.into_float()).unwrap_or_default(),
            par_2: t.take("PAR_2").and_then(|c| c.into_float()).unwrap_or_default(),
            par_3: t.take("PAR_3").and_then(|c| c.into_float()).unwrap_or_default(),
            par_4: t.take("PAR_4").and_then(|c| c.into_float()).unwrap_or_default(),
            par_5: t.take("PAR_5").and_then(|c| c.into_float()).unwrap_or_default(),
            par_6: t.take("PAR_6").and_then(|c| c.into_float()).unwrap_or_default(),
            par_7: t.take("PAR_7").and_then(|c| c.into_float()).unwrap_or_default(),
            par_8: t.take("PAR_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_CONSTRAINT {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub ctype: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f20: Vec<i64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_CONSTRAINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_CONSTRAINT").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_CONSTRAINT_ID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub ctype: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f20: Vec<i64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_CONSTRAINT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_CONSTRAINT_ID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_CONSTRAINT_TITLE {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub ctype: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f20: Vec<i64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_CONSTRAINT_TITLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_CONSTRAINT_TITLE").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_DRAG {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub unused: Vec<i64>,
    pub fcoef: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<f64>,
    pub direcg: Vec<i64>,
    pub grav: Vec<f64>,
}
impl ALE_COUPLING_NODAL_DRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_DRAG").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            fcoef: t.take("FCOEF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("unused_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("unused_3").and_then(|c| c.into_float()).unwrap_or_default(),
            direcg: t.take("DIRECG").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_DRAG_ID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub unused: Vec<i64>,
    pub fcoef: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<f64>,
    pub direcg: Vec<i64>,
    pub grav: Vec<f64>,
}
impl ALE_COUPLING_NODAL_DRAG_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_DRAG_ID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            fcoef: t.take("FCOEF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("unused_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("unused_3").and_then(|c| c.into_float()).unwrap_or_default(),
            direcg: t.take("DIRECG").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_DRAG_TITLE {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub unused: Vec<i64>,
    pub fcoef: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<f64>,
    pub direcg: Vec<i64>,
    pub grav: Vec<f64>,
}
impl ALE_COUPLING_NODAL_DRAG_TITLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_DRAG_TITLE").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            fcoef: t.take("FCOEF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("unused_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("unused_3").and_then(|c| c.into_float()).unwrap_or_default(),
            direcg: t.take("DIRECG").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_PENALTY {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub f: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pform: Vec<i64>,
    pub pfac: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_PENALTY").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pform: t.take("PFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_PENALTY_ID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub f: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pform: Vec<i64>,
    pub pfac: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_PENALTY_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_PENALTY_ID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pform: t.take("PFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_NODAL_PENALTY_TITLE {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub strsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub strsty: Vec<i64>,
    pub alesty: Vec<i64>,
    pub f: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pform: Vec<i64>,
    pub pfac: Vec<i64>,
    pub f40: Vec<i64>,
    pub frcmin: Vec<f64>,
}
impl ALE_COUPLING_NODAL_PENALTY_TITLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_NODAL_PENALTY_TITLE").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            strsid: t.take("STRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            strsty: t.take("STRSTY").and_then(|c| c.into_int()).unwrap_or_default(),
            alesty: t.take("ALESTY").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pform: t.take("PFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_COUPLING_RIGID_BODY {
    pub pid: Vec<i64>,
    pub esid: Vec<i64>,
    pub id: Vec<i64>,
    pub idtype: Vec<i64>,
    pub ictype: Vec<i64>,
    pub iexcle: Vec<i64>,
}
impl ALE_COUPLING_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_COUPLING_RIGID_BODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            idtype: t.take("IDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ictype: t.take("ICTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            iexcle: t.take("IEXCLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_ESSENTIAL_BOUNDARY {
    pub id: Vec<i64>,
    pub idtype: Vec<i64>,
    pub ictype: Vec<i64>,
    pub iexcl: Vec<i64>,
}
impl ALE_ESSENTIAL_BOUNDARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_ESSENTIAL_BOUNDARY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            idtype: t.take("IDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ictype: t.take("ICTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            iexcl: t.take("IEXCL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FAIL_SWITCH_MMG {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub fr_mmg: Vec<i64>,
    pub to_mmg: Vec<i64>,
}
impl ALE_FAIL_SWITCH_MMG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FAIL_SWITCH_MMG").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            fr_mmg: t.take("FR_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            to_mmg: t.take("TO_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FRAGMENTATION {
    pub fr_mmg: Vec<i64>,
    pub to_mmg: Vec<i64>,
    pub fragtyp: Vec<i64>,
}
impl ALE_FRAGMENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FRAGMENTATION").unwrap());
        Self {
            fr_mmg: t.take("FR_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            to_mmg: t.take("TO_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            fragtyp: t.take("FRAGTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FSI_LOAD_TO_NODE {
    pub dt: Vec<f64>,
    pub nsid: Vec<i64>,
    pub iopt: Vec<i64>,
    pub path: Vec<String>,
}
impl ALE_FSI_LOAD_TO_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FSI_LOAD_TO_NODE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FSI_PROJECTION {
    pub lagsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub lsidtyp: Vec<i64>,
    pub asidtyp: Vec<i64>,
    pub smmgid: Vec<i64>,
    pub icorrec: Vec<i64>,
    pub inorm: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
}
impl ALE_FSI_PROJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FSI_PROJECTION").unwrap());
        Self {
            lagsid: t.take("LAGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            lsidtyp: t.take("LSIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            asidtyp: t.take("ASIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            smmgid: t.take("SMMGID").and_then(|c| c.into_int()).unwrap_or_default(),
            icorrec: t.take("ICORREC").and_then(|c| c.into_int()).unwrap_or_default(),
            inorm: t.take("INORM").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FSI_SWITCH_MMG {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub nquad: Vec<i64>,
    pub xoff: Vec<f64>,
    pub btime: Vec<f64>,
    pub dtime: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub nfold: Vec<i64>,
    pub fr_mmg: Vec<i64>,
    pub to_mmg: Vec<i64>,
    pub xclen: Vec<f64>,
}
impl ALE_FSI_SWITCH_MMG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FSI_SWITCH_MMG").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            xoff: t.take("XOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            btime: t.take("BTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            dtime: t.take("DTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            nfold: t.take("NFOLD").and_then(|c| c.into_int()).unwrap_or_default(),
            fr_mmg: t.take("FR_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            to_mmg: t.take("TO_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            xclen: t.take("XCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_FSI_TO_LOAD_NODE {
    pub dt: Vec<f64>,
    pub nsid: Vec<i64>,
    pub iopt: Vec<i64>,
    pub path: Vec<String>,
}
impl ALE_FSI_TO_LOAD_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_FSI_TO_LOAD_NODE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct ALE_INJECTION {
    pub mmgset: Vec<i64>,
    pub segset: Vec<i64>,
    pub global: Vec<i64>,
    pub lce: Vec<i64>,
    pub lcrvl: Vec<i64>,
    pub lcvt: Vec<i64>,
    pub vect: Vec<i64>,
    pub lcvr: Vec<i64>,
    pub vecr: Vec<i64>,
    pub boxv: Vec<i64>,
    pub xg: Vec<f64>,
    pub yg: Vec<f64>,
    pub zg: Vec<f64>,
    pub surfct: Vec<i64>,
    pub ndiv: Vec<i64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zd: Vec<f64>,
    pub zu: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
}
impl ALE_INJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_INJECTION").unwrap());
        Self {
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            segset: t.take("SEGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            global: t.take("GLOBAL").and_then(|c| c.into_int()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrvl: t.take("LCRVL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvt: t.take("LCVT").and_then(|c| c.into_int()).unwrap_or_default(),
            vect: t.take("VECT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvr: t.take("LCVR").and_then(|c| c.into_int()).unwrap_or_default(),
            vecr: t.take("VECR").and_then(|c| c.into_int()).unwrap_or_default(),
            boxv: t.take("BOXV").and_then(|c| c.into_int()).unwrap_or_default(),
            xg: t.take("XG").and_then(|c| c.into_float()).unwrap_or_default(),
            yg: t.take("YG").and_then(|c| c.into_float()).unwrap_or_default(),
            zg: t.take("ZG").and_then(|c| c.into_float()).unwrap_or_default(),
            surfct: t.take("SURFCT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndiv: t.take("NDIV").and_then(|c| c.into_int()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zd: t.take("ZD").and_then(|c| c.into_float()).unwrap_or_default(),
            zu: t.take("ZU").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MAPPING {
    pub ammsid: Vec<i64>,
    pub rw: Vec<i64>,
    pub ntim: Vec<i64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub vecid: Vec<i64>,
    pub angle: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nvol: Vec<i64>,
    pub voltyp: Vec<i64>,
    pub vecid1: Vec<i64>,
    pub dw1: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub dw2: Vec<f64>,
    pub dv2: Vec<f64>,
}
impl ALE_MAPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MAPPING").unwrap());
        Self {
            ammsid: t.take("AMMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rw: t.take("RW").and_then(|c| c.into_int()).unwrap_or_default(),
            ntim: t.take("NTIM").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nvol: t.take("NVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            voltyp: t.take("VOLTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid1: t.take("VECID1").and_then(|c| c.into_int()).unwrap_or_default(),
            dw1: t.take("DW1").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            dw2: t.take("DW2").and_then(|c| c.into_float()).unwrap_or_default(),
            dv2: t.take("DV2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MAPPING_FROM_LAGRANGIAN {
    pub lagpid: Vec<i64>,
    pub lagpty: Vec<i64>,
    pub nx: Vec<i64>,
    pub ny: Vec<i64>,
    pub nx_2: Vec<i64>,
    pub npx: Vec<i64>,
    pub npy: Vec<i64>,
    pub npz: Vec<i64>,
    pub aleid: Vec<i64>,
    pub method: Vec<i64>,
    pub div: Vec<i64>,
}
impl ALE_MAPPING_FROM_LAGRANGIAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MAPPING_FROM_LAGRANGIAN").unwrap());
        Self {
            lagpid: t.take("LAGPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lagpty: t.take("LAGPTY").and_then(|c| c.into_int()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_int()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_int()).unwrap_or_default(),
            nx_2: t.take("NX_2").and_then(|c| c.into_int()).unwrap_or_default(),
            npx: t.take("NPX").and_then(|c| c.into_int()).unwrap_or_default(),
            npy: t.take("NPY").and_then(|c| c.into_int()).unwrap_or_default(),
            npz: t.take("NPZ").and_then(|c| c.into_int()).unwrap_or_default(),
            aleid: t.take("ALEID").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            div: t.take("DIV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MESH_INTERFACE {
    pub mmgset: Vec<i64>,
    pub nowrt: Vec<i64>,
    pub volrat: Vec<f64>,
    pub interp: Vec<i64>,
    pub edgmin: Vec<f64>,
    pub edgmax: Vec<f64>,
}
impl ALE_MESH_INTERFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MESH_INTERFACE").unwrap());
        Self {
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nowrt: t.take("NOWRT").and_then(|c| c.into_int()).unwrap_or_default(),
            volrat: t.take("VOLRAT").and_then(|c| c.into_float()).unwrap_or_default(),
            interp: t.take("INTERP").and_then(|c| c.into_int()).unwrap_or_default(),
            edgmin: t.take("EDGMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            edgmax: t.take("EDGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MULTI_MATERIAL_GROUP {
    pub sid: Vec<i64>,
    pub idtype: Vec<i64>,
}
impl ALE_MULTI_MATERIAL_GROUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MULTI-MATERIAL_GROUP").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            idtype: t.take("IDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MULTI_MATERIAL_GROUP_PART {
    pub pid: Vec<i64>,
}
impl ALE_MULTI_MATERIAL_GROUP_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MULTI-MATERIAL_GROUP_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_MULTI_MATERIAL_GROUP_SET {
    pub psid: Vec<i64>,
}
impl ALE_MULTI_MATERIAL_GROUP_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_MULTI-MATERIAL_GROUP_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_PRESCRIBED_MOTION {
    pub mmsid: Vec<i64>,
    pub inside: Vec<i64>,
    pub sidr: Vec<i64>,
    pub lcvtx: Vec<i64>,
    pub lcvty: Vec<i64>,
    pub lcvtz: Vec<i64>,
    pub lcvrx: Vec<i64>,
    pub lcvry: Vec<i64>,
    pub lcvrz: Vec<i64>,
    pub xg: Vec<f64>,
    pub yg: Vec<f64>,
    pub zg: Vec<f64>,
}
impl ALE_PRESCRIBED_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_PRESCRIBED_MOTION").unwrap());
        Self {
            mmsid: t.take("MMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            inside: t.take("INSIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvtx: t.take("LCVTX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvty: t.take("LCVTY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvtz: t.take("LCVTZ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvrx: t.take("LCVRX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvry: t.take("LCVRY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvrz: t.take("LCVRZ").and_then(|c| c.into_int()).unwrap_or_default(),
            xg: t.take("XG").and_then(|c| c.into_float()).unwrap_or_default(),
            yg: t.take("YG").and_then(|c| c.into_float()).unwrap_or_default(),
            zg: t.take("ZG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_REFERENCE_SYSTEM_CURVE {
    pub id: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub lc5: Vec<i64>,
    pub lc6: Vec<i64>,
    pub lc7: Vec<i64>,
    pub lc8: Vec<i64>,
    pub lc9: Vec<i64>,
    pub lc10: Vec<i64>,
    pub lc11: Vec<i64>,
    pub lc12: Vec<i64>,
}
impl ALE_REFERENCE_SYSTEM_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_REFERENCE_SYSTEM_CURVE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            lc5: t.take("LC5").and_then(|c| c.into_int()).unwrap_or_default(),
            lc6: t.take("LC6").and_then(|c| c.into_int()).unwrap_or_default(),
            lc7: t.take("LC7").and_then(|c| c.into_int()).unwrap_or_default(),
            lc8: t.take("LC8").and_then(|c| c.into_int()).unwrap_or_default(),
            lc9: t.take("LC9").and_then(|c| c.into_int()).unwrap_or_default(),
            lc10: t.take("LC10").and_then(|c| c.into_int()).unwrap_or_default(),
            lc11: t.take("LC11").and_then(|c| c.into_int()).unwrap_or_default(),
            lc12: t.take("LC12").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_REFERENCE_SYSTEM_GROUP {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub prtype: Vec<i64>,
    pub prid: Vec<i64>,
    pub bctran: Vec<i64>,
    pub bcexp: Vec<i64>,
    pub bcrot: Vec<i64>,
    pub icoord: Vec<i64>,
    pub sid_2: Vec<i64>,
    pub stype_2: Vec<i64>,
    pub prtype_2: Vec<i64>,
    pub prid_2: Vec<i64>,
    pub bctran_2: Vec<i64>,
    pub bcexp_2: Vec<i64>,
    pub bcrot_2: Vec<i64>,
    pub icoord_2: Vec<i64>,
    pub sid_3: Vec<i64>,
    pub stype_3: Vec<i64>,
    pub prtype_3: Vec<i64>,
    pub prid_3: Vec<i64>,
    pub bctran_3: Vec<i64>,
    pub bcexp_3: Vec<i64>,
    pub bcrot_3: Vec<i64>,
    pub icoord_3: Vec<i64>,
}
impl ALE_REFERENCE_SYSTEM_GROUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_REFERENCE_SYSTEM_GROUP").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prtype: t.take("PRTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prid: t.take("PRID").and_then(|c| c.into_int()).unwrap_or_default(),
            bctran: t.take("BCTRAN").and_then(|c| c.into_int()).unwrap_or_default(),
            bcexp: t.take("BCEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            bcrot: t.take("BCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
            sid_2: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype_2: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prtype_2: t.take("PRTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prid_2: t.take("PRID").and_then(|c| c.into_int()).unwrap_or_default(),
            bctran_2: t.take("BCTRAN").and_then(|c| c.into_int()).unwrap_or_default(),
            bcexp_2: t.take("BCEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            bcrot_2: t.take("BCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord_2: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
            sid_3: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype_3: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prtype_3: t.take("PRTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prid_3: t.take("PRID").and_then(|c| c.into_int()).unwrap_or_default(),
            bctran_3: t.take("BCTRAN").and_then(|c| c.into_int()).unwrap_or_default(),
            bcexp_3: t.take("BCEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            bcrot_3: t.take("BCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord_3: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_REFERENCE_SYSTEM_NODE {
    pub id: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
    pub nid9: Vec<i64>,
    pub nid10: Vec<i64>,
    pub nid11: Vec<i64>,
    pub nid12: Vec<i64>,
}
impl ALE_REFERENCE_SYSTEM_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_REFERENCE_SYSTEM_NODE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
            nid9: t.take("NID9").and_then(|c| c.into_int()).unwrap_or_default(),
            nid10: t.take("NID10").and_then(|c| c.into_int()).unwrap_or_default(),
            nid11: t.take("NID11").and_then(|c| c.into_int()).unwrap_or_default(),
            nid12: t.take("NID12").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_REFERENCE_SYSTEM_SWITCH {
    pub id: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub type1: Vec<i64>,
    pub type2: Vec<i64>,
    pub type3: Vec<i64>,
    pub type4: Vec<i64>,
    pub type5: Vec<i64>,
    pub type6: Vec<i64>,
    pub type7: Vec<i64>,
    pub type8: Vec<i64>,
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl ALE_REFERENCE_SYSTEM_SWITCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_REFERENCE_SYSTEM_SWITCH").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            type1: t.take("TYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            type2: t.take("TYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            type3: t.take("TYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            type4: t.take("TYPE4").and_then(|c| c.into_int()).unwrap_or_default(),
            type5: t.take("TYPE5").and_then(|c| c.into_int()).unwrap_or_default(),
            type6: t.take("TYPE6").and_then(|c| c.into_int()).unwrap_or_default(),
            type7: t.take("TYPE7").and_then(|c| c.into_int()).unwrap_or_default(),
            type8: t.take("TYPE8").and_then(|c| c.into_int()).unwrap_or_default(),
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_REFINE {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nlvl: Vec<i64>,
    pub mmsid: Vec<i64>,
    pub ntotrf: Vec<i64>,
    pub ncycrf: Vec<i64>,
    pub critrf: Vec<i64>,
    pub valrf: Vec<f64>,
    pub begrf: Vec<f64>,
    pub endrf: Vec<f64>,
    pub layrf: Vec<i64>,
    pub maxrm: Vec<i64>,
    pub ncycrm: Vec<i64>,
    pub critrm: Vec<i64>,
    pub valrm: Vec<f64>,
    pub begrm: Vec<f64>,
    pub endrm: Vec<f64>,
}
impl ALE_REFINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_REFINE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nlvl: t.take("NLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            mmsid: t.take("MMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntotrf: t.take("NTOTRF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrf: t.take("NCYCRF").and_then(|c| c.into_int()).unwrap_or_default(),
            critrf: t.take("CRITRF").and_then(|c| c.into_int()).unwrap_or_default(),
            valrf: t.take("VALRF").and_then(|c| c.into_float()).unwrap_or_default(),
            begrf: t.take("BEGRF").and_then(|c| c.into_float()).unwrap_or_default(),
            endrf: t.take("ENDRF").and_then(|c| c.into_float()).unwrap_or_default(),
            layrf: t.take("LAYRF").and_then(|c| c.into_int()).unwrap_or_default(),
            maxrm: t.take("MAXRM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrm: t.take("NCYCRM").and_then(|c| c.into_int()).unwrap_or_default(),
            critrm: t.take("CRITRM").and_then(|c| c.into_int()).unwrap_or_default(),
            valrm: t.take("VALRM").and_then(|c| c.into_float()).unwrap_or_default(),
            begrm: t.take("BEGRM").and_then(|c| c.into_float()).unwrap_or_default(),
            endrm: t.take("ENDRM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_SMOOTHING {
    pub dnid: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub ipre: Vec<i64>,
    pub xco: Vec<f64>,
    pub yco: Vec<f64>,
    pub zco: Vec<f64>,
}
impl ALE_SMOOTHING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_SMOOTHING").unwrap());
        Self {
            dnid: t.take("DNID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ipre: t.take("IPRE").and_then(|c| c.into_int()).unwrap_or_default(),
            xco: t.take("XCO").and_then(|c| c.into_float()).unwrap_or_default(),
            yco: t.take("YCO").and_then(|c| c.into_float()).unwrap_or_default(),
            zco: t.take("ZCO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_FSI {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub lstrsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub lstrstyp: Vec<i64>,
    pub alestyp: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub f_4: Vec<i64>,
    pub flip: Vec<i64>,
}
impl ALE_STRUCTURED_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_FSI").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            lstrsid: t.take("LSTRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            lstrstyp: t.take("LSTRSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            alestyp: t.take("ALESTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            flip: t.take("FLIP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH {
    pub mshid: Vec<i64>,
    pub dpid: Vec<i64>,
    pub nbid: Vec<i64>,
    pub ebid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub tdeath: Vec<f64>,
    pub cpidx: Vec<i64>,
    pub cpidy: Vec<i64>,
    pub cpidz: Vec<i64>,
    pub nid0: Vec<i64>,
    pub lcsid: Vec<i64>,
}
impl ALE_STRUCTURED_MESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH").unwrap());
        Self {
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            dpid: t.take("DPID").and_then(|c| c.into_int()).unwrap_or_default(),
            nbid: t.take("NBID").and_then(|c| c.into_int()).unwrap_or_default(),
            ebid: t.take("EBID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            cpidx: t.take("CPIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            cpidy: t.take("CPIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            cpidz: t.take("CPIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            nid0: t.take("NID0").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsid: t.take("LCSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH_CONTROL_POINTS {
    pub cpid: Vec<i64>,
    pub unused: Vec<i64>,
    pub icase: Vec<i64>,
    pub sfo: Vec<f64>,
    pub unused_2: Vec<i64>,
    pub offo: Vec<f64>,
    pub n: Vec<i64>,
    pub x: Vec<f64>,
    pub ratio: Vec<f64>,
}
impl ALE_STRUCTURED_MESH_CONTROL_POINTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH_CONTROL_POINTS").unwrap());
        Self {
            cpid: t.take("CPID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            icase: t.take("ICASE").and_then(|c| c.into_int()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            ratio: t.take("RATIO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH_MOTION {
    pub mshid: Vec<i64>,
    pub option: Vec<String>,
    pub ammgsid: Vec<i64>,
    pub explim: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub symcod: Vec<i64>,
}
impl ALE_STRUCTURED_MESH_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH_MOTION").unwrap());
        Self {
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgsid: t.take("AMMGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            explim: t.take("EXPLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            symcod: t.take("SYMCOD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH_REFINE {
    pub mshid: Vec<i64>,
    pub ifx: Vec<i64>,
    pub ify: Vec<i64>,
    pub ifz: Vec<i64>,
}
impl ALE_STRUCTURED_MESH_REFINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH_REFINE").unwrap());
        Self {
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifx: t.take("IFX,").and_then(|c| c.into_int()).unwrap_or_default(),
            ify: t.take("IFY,").and_then(|c| c.into_int()).unwrap_or_default(),
            ifz: t.take("IFZ,").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH_TRIM {
    pub mshid: Vec<i64>,
    pub option: Vec<String>,
    pub oper: Vec<i64>,
    pub ioutin: Vec<i64>,
    pub psid: Vec<i64>,
    pub dist: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
}
impl ALE_STRUCTURED_MESH_TRIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH_TRIM").unwrap());
        Self {
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            oper: t.take("OPER").and_then(|c| c.into_int()).unwrap_or_default(),
            ioutin: t.take("IOUTIN").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MESH_VOLUME_FILLING {
    pub mshid: Vec<i64>,
    pub unused: Vec<i64>,
    pub ammgto: Vec<String>,
    pub unused_2: Vec<i64>,
    pub nsample: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub vid: Vec<i64>,
    pub geom: Vec<String>,
    pub in_out: Vec<i64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
}
impl ALE_STRUCTURED_MESH_VOLUME_FILLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MESH_VOLUME_FILLING").unwrap());
        Self {
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgto: t.take("AMMGTO").and_then(|c| c.into_str()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            nsample: t.take("NSAMPLE").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED-").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            geom: t.take("GEOM").and_then(|c| c.into_str()).unwrap_or_default(),
            in_out: t.take("IN/OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MULTI_MATERIAL_GROUP {
    pub ammg_name: Vec<String>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<f64>,
    pub pref: Vec<f64>,
}
impl ALE_STRUCTURED_MULTI_MATERIAL_GROUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MULTI-MATERIAL_GROUP").unwrap());
        Self {
            ammg_name: t.take("AMMG_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MULTI_MATERIAL_GROUP_AXISYM {
    pub ammg_name: Vec<String>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<f64>,
    pub pref: Vec<f64>,
}
impl ALE_STRUCTURED_MULTI_MATERIAL_GROUP_AXISYM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MULTI-MATERIAL_GROUP_AXISYM").unwrap());
        Self {
            ammg_name: t.take("AMMG_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MULTI_MATERIAL_GROUP_PLNEPS {
    pub ammg_name: Vec<String>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<f64>,
    pub pref: Vec<f64>,
}
impl ALE_STRUCTURED_MULTI_MATERIAL_GROUP_PLNEPS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MULTI-MATERIAL_GROUP_PLNEPS").unwrap());
        Self {
            ammg_name: t.take("AMMG_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_STRUCTURED_MULTI_MATERIAL_GROUP_2 {
    pub ammg_name: Vec<String>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<f64>,
    pub pref: Vec<f64>,
}
impl ALE_STRUCTURED_MULTI_MATERIAL_GROUP_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_STRUCTURED_MULTI_MATERIAL_GROUP").unwrap());
        Self {
            ammg_name: t.take("AMMG_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ALE_SWITCH_MMG {
    pub fr_mmg: Vec<i64>,
    pub to_mmg: Vec<i64>,
    pub idfunc: Vec<i64>,
    pub idsegset: Vec<i64>,
    pub idsldset: Vec<i64>,
    pub ncycseg: Vec<i64>,
    pub ncycsld: Vec<i64>,
    pub var1: Vec<i64>,
    pub var2: Vec<i64>,
    pub var3: Vec<i64>,
    pub var4: Vec<i64>,
    pub var5: Vec<i64>,
    pub var6: Vec<i64>,
    pub var7: Vec<i64>,
    pub var8: Vec<i64>,
}
impl ALE_SWITCH_MMG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_SWITCH_MMG").unwrap());
        Self {
            fr_mmg: t.take("FR_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            to_mmg: t.take("TO_MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            idfunc: t.take("IDFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            idsegset: t.take("IDSEGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            idsldset: t.take("IDSLDSET").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycseg: t.take("NCYCSEG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycsld: t.take("NCYCSLD").and_then(|c| c.into_int()).unwrap_or_default(),
            var1: t.take("VAR1").and_then(|c| c.into_int()).unwrap_or_default(),
            var2: t.take("VAR2").and_then(|c| c.into_int()).unwrap_or_default(),
            var3: t.take("VAR3").and_then(|c| c.into_int()).unwrap_or_default(),
            var4: t.take("VAR4").and_then(|c| c.into_int()).unwrap_or_default(),
            var5: t.take("VAR5").and_then(|c| c.into_int()).unwrap_or_default(),
            var6: t.take("VAR6").and_then(|c| c.into_int()).unwrap_or_default(),
            var7: t.take("VAR7").and_then(|c| c.into_int()).unwrap_or_default(),
            var8: t.take("VAR8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_TANK_TEST {
    pub mdotlc: Vec<i64>,
    pub tankvol: Vec<f64>,
    pub pamb: Vec<f64>,
    pub pfinal: Vec<f64>,
    pub machlim: Vec<f64>,
    pub velmax: Vec<f64>,
    pub aorif: Vec<f64>,
    pub ammgidg: Vec<i64>,
    pub ammgida: Vec<i64>,
    pub numpnt: Vec<i64>,
}
impl ALE_TANK_TEST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_TANK_TEST").unwrap());
        Self {
            mdotlc: t.take("MDOTLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tankvol: t.take("TANKVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            pamb: t.take("PAMB").and_then(|c| c.into_float()).unwrap_or_default(),
            pfinal: t.take("PFINAL").and_then(|c| c.into_float()).unwrap_or_default(),
            machlim: t.take("MACHLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            velmax: t.take("VELMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            aorif: t.take("AORIF").and_then(|c| c.into_float()).unwrap_or_default(),
            ammgidg: t.take("AMMGIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgida: t.take("AMMGIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            numpnt: t.take("NUMPNT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ALE_UP_SWITCH {
    pub upid: Vec<i64>,
    pub swtime: Vec<f64>,
    pub fsi_id1: Vec<i64>,
    pub fsi_id2: Vec<i64>,
    pub fsi_id3: Vec<i64>,
    pub fsi_id4: Vec<i64>,
    pub fsi_id5: Vec<i64>,
    pub fsi_id6: Vec<i64>,
    pub fsi_id7: Vec<i64>,
    pub fsi_id8: Vec<i64>,
    pub sid: Vec<i64>,
    pub sidtype: Vec<i64>,
    pub mmgair: Vec<i64>,
    pub mmggas: Vec<i64>,
}
impl ALE_UP_SWITCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ALE_UP_SWITCH").unwrap());
        Self {
            upid: t.take("UPID").and_then(|c| c.into_int()).unwrap_or_default(),
            swtime: t.take("SWTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            fsi_id1: t.take("FSI_ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id2: t.take("FSI_ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id3: t.take("FSI_ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id4: t.take("FSI_ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id5: t.take("FSI_ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id6: t.take("FSI_ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id7: t.take("FSI_ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            fsi_id8: t.take("FSI_ID8").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtype: t.take("SIDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgair: t.take("MMGAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mmggas: t.take("MMGGAS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_BA_ECHEM_CONTROL_SOLVER {
    pub imodel: Vec<i64>,
    pub igeom: Vec<i64>,
    pub ncycle: Vec<i64>,
    pub aging: Vec<i64>,
    pub tra: Vec<i64>,
    pub gas: Vec<i64>,
    pub esolid: Vec<i64>,
    pub f: Vec<i64>,
    pub irun: Vec<i64>,
    pub lcur: Vec<i64>,
    pub curv: Vec<f64>,
    pub ctime: Vec<f64>,
    pub vcut: Vec<f64>,
}
impl BATTERY_BA_ECHEM_CONTROL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_BA_ECHEM_CONTROL_SOLVER").unwrap());
        Self {
            imodel: t.take("IMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            igeom: t.take("IGEOM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            aging: t.take("AGING").and_then(|c| c.into_int()).unwrap_or_default(),
            tra: t.take("TRA").and_then(|c| c.into_int()).unwrap_or_default(),
            gas: t.take("GAS").and_then(|c| c.into_int()).unwrap_or_default(),
            esolid: t.take("ESOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            irun: t.take("IRUN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            curv: t.take("CURV").and_then(|c| c.into_float()).unwrap_or_default(),
            ctime: t.take("CTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            vcut: t.take("VCUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_CELL_GEOMETRY {
    pub imodel: Vec<i64>,
    pub alen: Vec<f64>,
    pub slen: Vec<f64>,
    pub clen: Vec<f64>,
    pub acclen: Vec<f64>,
    pub cclen: Vec<f64>,
    pub amesh: Vec<i64>,
    pub smesh: Vec<i64>,
    pub cmesh: Vec<i64>,
    pub accmesh: Vec<i64>,
    pub cccmesh: Vec<i64>,
}
impl BATTERY_ECHEM_CELL_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_CELL_GEOMETRY").unwrap());
        Self {
            imodel: t.take("IMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            alen: t.take("ALEN").and_then(|c| c.into_float()).unwrap_or_default(),
            slen: t.take("SLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            clen: t.take("CLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            acclen: t.take("ACCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            cclen: t.take("CCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            amesh: t.take("AMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            smesh: t.take("SMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            cmesh: t.take("CMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            accmesh: t.take("ACCMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            cccmesh: t.take("CCCMESH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_CONTROL_SOLVER {
    pub imodel: Vec<i64>,
    pub igeom: Vec<i64>,
    pub ncycle: Vec<i64>,
    pub aging: Vec<i64>,
    pub tra: Vec<i64>,
    pub gas: Vec<i64>,
    pub esolid: Vec<i64>,
    pub f: Vec<i64>,
    pub irun: Vec<i64>,
    pub lcur: Vec<i64>,
    pub curv: Vec<f64>,
    pub ctime: Vec<f64>,
    pub vcut: Vec<f64>,
    pub mws: Vec<f64>,
    pub dens: Vec<f64>,
    pub brugs: Vec<f64>,
    pub epss: Vec<f64>,
    pub cseio: Vec<f64>,
    pub tseio: Vec<f64>,
    pub ecdo: Vec<f64>,
    pub kfs: Vec<f64>,
    pub ceco: Vec<f64>,
    pub ecdf: Vec<f64>,
    pub hofeln: Vec<i64>,
    pub hofli: Vec<i64>,
    pub hofsei: Vec<f64>,
    pub hofc2h4: Vec<f64>,
    pub afi: Vec<f64>,
    pub eat: Vec<f64>,
    pub hoflc: Vec<f64>,
    pub hofco2: Vec<f64>,
    pub hofo2: Vec<f64>,
    pub file1: Vec<String>,
    pub file2: Vec<String>,
}
impl BATTERY_ECHEM_CONTROL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_CONTROL_SOLVER").unwrap());
        Self {
            imodel: t.take("IMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            igeom: t.take("IGEOM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            aging: t.take("AGING").and_then(|c| c.into_int()).unwrap_or_default(),
            tra: t.take("TRA").and_then(|c| c.into_int()).unwrap_or_default(),
            gas: t.take("GAS").and_then(|c| c.into_int()).unwrap_or_default(),
            esolid: t.take("ESOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            irun: t.take("IRUN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            curv: t.take("CURV").and_then(|c| c.into_float()).unwrap_or_default(),
            ctime: t.take("CTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            vcut: t.take("VCUT").and_then(|c| c.into_float()).unwrap_or_default(),
            mws: t.take("MWS").and_then(|c| c.into_float()).unwrap_or_default(),
            dens: t.take("DENS").and_then(|c| c.into_float()).unwrap_or_default(),
            brugs: t.take("BRUGS").and_then(|c| c.into_float()).unwrap_or_default(),
            epss: t.take("EPSS").and_then(|c| c.into_float()).unwrap_or_default(),
            cseio: t.take("CSEIO").and_then(|c| c.into_float()).unwrap_or_default(),
            tseio: t.take("TSEIO").and_then(|c| c.into_float()).unwrap_or_default(),
            ecdo: t.take("ECDO").and_then(|c| c.into_float()).unwrap_or_default(),
            kfs: t.take("KFS").and_then(|c| c.into_float()).unwrap_or_default(),
            ceco: t.take("CECO").and_then(|c| c.into_float()).unwrap_or_default(),
            ecdf: t.take("ECDF").and_then(|c| c.into_float()).unwrap_or_default(),
            hofeln: t.take("HOFELN").and_then(|c| c.into_int()).unwrap_or_default(),
            hofli: t.take("HOFLI").and_then(|c| c.into_int()).unwrap_or_default(),
            hofsei: t.take("HOFSEI").and_then(|c| c.into_float()).unwrap_or_default(),
            hofc2h4: t.take("HOFC2H4").and_then(|c| c.into_float()).unwrap_or_default(),
            afi: t.take("AFI").and_then(|c| c.into_float()).unwrap_or_default(),
            eat: t.take("EAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hoflc: t.take("HOFLC").and_then(|c| c.into_float()).unwrap_or_default(),
            hofco2: t.take("HOFCO2").and_then(|c| c.into_float()).unwrap_or_default(),
            hofo2: t.take("HOFO2").and_then(|c| c.into_float()).unwrap_or_default(),
            file1: t.take("FILE1").and_then(|c| c.into_str()).unwrap_or_default(),
            file2: t.take("FILE2").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_INITIAL {
    pub echemid: Vec<i64>,
    pub mid: Vec<i64>,
    pub lic: Vec<f64>,
    pub lisic: Vec<f64>,
    pub phi2ic: Vec<f64>,
    pub phi1ic: Vec<f64>,
    pub curic: Vec<f64>,
    pub fluxic: Vec<f64>,
}
impl BATTERY_ECHEM_INITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_INITIAL").unwrap());
        Self {
            echemid: t.take("ECHEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lic: t.take("LIC").and_then(|c| c.into_float()).unwrap_or_default(),
            lisic: t.take("LISIC").and_then(|c| c.into_float()).unwrap_or_default(),
            phi2ic: t.take("PHI2IC").and_then(|c| c.into_float()).unwrap_or_default(),
            phi1ic: t.take("PHI1IC").and_then(|c| c.into_float()).unwrap_or_default(),
            curic: t.take("CURIC").and_then(|c| c.into_float()).unwrap_or_default(),
            fluxic: t.take("FLUXIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_MAT_ANODE {
    pub pid: Vec<i64>,
    pub iocpa: Vec<i64>,
    pub capta: Vec<f64>,
    pub s_xa: Vec<f64>,
    pub rada: Vec<f64>,
    pub ratea: Vec<f64>,
    pub ranode: Vec<f64>,
    pub rhoea: Vec<f64>,
    pub rhofa: Vec<f64>,
    pub rhocca: Vec<f64>,
    pub diffa: Vec<f64>,
    pub conda: Vec<f64>,
    pub vfea: Vec<f64>,
    pub vfpa: Vec<f64>,
    pub vffa: Vec<f64>,
    pub vfga: Vec<f64>,
}
impl BATTERY_ECHEM_MAT_ANODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_MAT_ANODE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            iocpa: t.take("IOCPA").and_then(|c| c.into_int()).unwrap_or_default(),
            capta: t.take("CAPTA").and_then(|c| c.into_float()).unwrap_or_default(),
            s_xa: t.take("S_XA").and_then(|c| c.into_float()).unwrap_or_default(),
            rada: t.take("RADA").and_then(|c| c.into_float()).unwrap_or_default(),
            ratea: t.take("RATEA").and_then(|c| c.into_float()).unwrap_or_default(),
            ranode: t.take("RANODE").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoea: t.take("RHOEA").and_then(|c| c.into_float()).unwrap_or_default(),
            rhofa: t.take("RHOFA").and_then(|c| c.into_float()).unwrap_or_default(),
            rhocca: t.take("RHOCCA").and_then(|c| c.into_float()).unwrap_or_default(),
            diffa: t.take("DIFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            conda: t.take("CONDA").and_then(|c| c.into_float()).unwrap_or_default(),
            vfea: t.take("VFEA").and_then(|c| c.into_float()).unwrap_or_default(),
            vfpa: t.take("VFPA").and_then(|c| c.into_float()).unwrap_or_default(),
            vffa: t.take("VFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            vfga: t.take("VFGA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_MAT_CATHODE {
    pub pid: Vec<i64>,
    pub iocpa: Vec<i64>,
    pub capta: Vec<f64>,
    pub s_yc: Vec<f64>,
    pub s_rad: Vec<f64>,
    pub ratec: Vec<f64>,
    pub rcath: Vec<f64>,
    pub rhoec: Vec<f64>,
    pub rhofc: Vec<f64>,
    pub rhoccc: Vec<f64>,
    pub diffc: Vec<f64>,
    pub condc: Vec<f64>,
    pub vfec: Vec<f64>,
    pub vfpc: Vec<f64>,
    pub vffc: Vec<f64>,
    pub vfgc: Vec<f64>,
}
impl BATTERY_ECHEM_MAT_CATHODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_MAT_CATHODE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            iocpa: t.take("IOCPA").and_then(|c| c.into_int()).unwrap_or_default(),
            capta: t.take("CAPTA").and_then(|c| c.into_float()).unwrap_or_default(),
            s_yc: t.take("S_YC").and_then(|c| c.into_float()).unwrap_or_default(),
            s_rad: t.take("S_RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            ratec: t.take("RATEC").and_then(|c| c.into_float()).unwrap_or_default(),
            rcath: t.take("RCATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoec: t.take("RHOEC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhofc: t.take("RHOFC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoccc: t.take("RHOCCC").and_then(|c| c.into_float()).unwrap_or_default(),
            diffc: t.take("DIFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            condc: t.take("CONDC").and_then(|c| c.into_float()).unwrap_or_default(),
            vfec: t.take("VFEC").and_then(|c| c.into_float()).unwrap_or_default(),
            vfpc: t.take("VFPC").and_then(|c| c.into_float()).unwrap_or_default(),
            vffc: t.take("VFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            vfgc: t.take("VFGC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_MAT_ELECTROLYTE {
    pub pid: Vec<i64>,
    pub ielyte: Vec<i64>,
    pub etype: Vec<i64>,
    pub rhoe: Vec<f64>,
    pub rhop: Vec<f64>,
    pub rhos: Vec<f64>,
    pub calmax: Vec<f64>,
    pub vfes: Vec<f64>,
    pub vfps: Vec<f64>,
    pub vfgs: Vec<f64>,
}
impl BATTERY_ECHEM_MAT_ELECTROLYTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_MAT_ELECTROLYTE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ielyte: t.take("IELYTE").and_then(|c| c.into_int()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rhoe: t.take("RHOE").and_then(|c| c.into_float()).unwrap_or_default(),
            rhop: t.take("RHOP").and_then(|c| c.into_float()).unwrap_or_default(),
            rhos: t.take("RHOS").and_then(|c| c.into_float()).unwrap_or_default(),
            calmax: t.take("CALMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vfes: t.take("VFES").and_then(|c| c.into_float()).unwrap_or_default(),
            vfps: t.take("VFPS").and_then(|c| c.into_float()).unwrap_or_default(),
            vfgs: t.take("VFGS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_PART {
    pub pid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
}
impl BATTERY_ECHEM_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BATTERY_ECHEM_THERMAL {
    pub thame: Vec<String>,
    pub tid: Vec<i64>,
    pub iprt: Vec<i64>,
    pub cp: Vec<f64>,
    pub hconv: Vec<f64>,
    pub temp: Vec<f64>,
    pub dudt: Vec<f64>,
    pub filename: Vec<String>,
}
impl BATTERY_ECHEM_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BATTERY_ECHEM_THERMAL").unwrap());
        Self {
            thame: t.take("THAME").and_then(|c| c.into_str()).unwrap_or_default(),
            tid: t.take("TID").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_COUPLING {
    pub ssid: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_COUPLING").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_COUPLING_MISMATCH {
    pub ssid: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_COUPLING_MISMATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_COUPLING_MISMATCH").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_COUPLING_SPECTRAL {
    pub ssids: Vec<i64>,
    pub ssidf: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_COUPLING_SPECTRAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_COUPLING_SPECTRAL").unwrap());
        Self {
            ssids: t.take("SSIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            ssidf: t.take("SSIDF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_FREE_SURFACE {
    pub ssids: Vec<i64>,
    pub ssidf: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_FREE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_FREE_SURFACE").unwrap());
        Self {
            ssids: t.take("SSIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            ssidf: t.take("SSIDF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_IMPEDANCE {
    pub ssid: Vec<i64>,
    pub zee: Vec<f64>,
}
impl BOUNDARY_ACOUSTIC_IMPEDANCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_IMPEDANCE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            zee: t.take("ZEE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_IMPEDANCE_COMPLEX {
    pub ssid: Vec<i64>,
    pub zr: Vec<f64>,
    pub zi: Vec<f64>,
    pub lcidr: Vec<i64>,
    pub lcidi: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_IMPEDANCE_COMPLEX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_IMPEDANCE_COMPLEX").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            zr: t.take("ZR").and_then(|c| c.into_float()).unwrap_or_default(),
            zi: t.take("ZI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidi: t.take("LCIDI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_IMPEDANCE_MECHANICAL {
    pub ssid: Vec<i64>,
    pub mparea: Vec<f64>,
    pub cparea: Vec<f64>,
    pub kparea: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_IMPEDANCE_MECHANICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_IMPEDANCE_MECHANICAL").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mparea: t.take("MPAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            cparea: t.take("CPAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            kparea: t.take("KPAREA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_INTERFACE {
    pub ssid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_INTERFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_INTERFACE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_MAPPING {
    pub ssid: Vec<i64>,
    pub styp: Vec<i64>,
}
impl BOUNDARY_ACOUSTIC_MAPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_MAPPING").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_NON_REFLECTING {
    pub ssid: Vec<i64>,
    pub nrbtyp: Vec<i64>,
    pub crvopt: Vec<i64>,
    pub data1: Vec<f64>,
    pub data2: Vec<f64>,
    pub data3: Vec<f64>,
}
impl BOUNDARY_ACOUSTIC_NON_REFLECTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_NON_REFLECTING").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nrbtyp: t.take("NRBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            crvopt: t.take("CRVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            data1: t.take("DATA1").and_then(|c| c.into_float()).unwrap_or_default(),
            data2: t.take("DATA2").and_then(|c| c.into_float()).unwrap_or_default(),
            data3: t.take("DATA3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_PRESCRIBED_MOTION {
    pub ssid: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl BOUNDARY_ACOUSTIC_PRESCRIBED_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_PRESCRIBED_MOTION").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ACOUSTIC_PRESSURE_SPECTRAL {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub tdeath: Vec<f64>,
}
impl BOUNDARY_ACOUSTIC_PRESSURE_SPECTRAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ACOUSTIC_PRESSURE_SPECTRAL").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ALE_MAPPING {
    pub id: Vec<i64>,
    pub typ: Vec<i64>,
    pub ammsid: Vec<i64>,
    pub ivoltyp: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtout: Vec<f64>,
    pub ini: Vec<i64>,
    pub thick: Vec<f64>,
    pub radius: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub vecid: Vec<i64>,
}
impl BOUNDARY_ALE_MAPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ALE_MAPPING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            typ: t.take("TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ammsid: t.take("AMMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ivoltyp: t.take("IVOLTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            ini: t.take("INI").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_AMBIENT {
    pub setid: Vec<i64>,
    pub mmg: Vec<i64>,
    pub ambtyp: Vec<i64>,
    pub sidr: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
}
impl BOUNDARY_AMBIENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_AMBIENT").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            mmg: t.take("MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            ambtyp: t.take("AMBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_AMBIENT_EOS {
    pub pid: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
}
impl BOUNDARY_AMBIENT_EOS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_AMBIENT_EOS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_CONVECTION_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub hmult: Vec<f64>,
    pub tlcid: Vec<i64>,
    pub tmult: Vec<f64>,
    pub loc: Vec<i64>,
}
impl BOUNDARY_CONVECTION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_CONVECTION_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hmult: t.take("HMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlcid: t.take("TLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tmult: t.take("TMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_CONVECTION_SET {
    pub ssid: Vec<i64>,
    pub pserod: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub hmult: Vec<f64>,
    pub tlcid: Vec<i64>,
    pub tmult: Vec<f64>,
    pub loc: Vec<i64>,
}
impl BOUNDARY_CONVECTION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_CONVECTION_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pserod: t.take("PSEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hmult: t.take("HMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlcid: t.take("TLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tmult: t.take("TMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_COUPLED {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub set: Vec<i64>,
    pub type_: Vec<i64>,
    pub prog: Vec<i64>,
}
impl BOUNDARY_COUPLED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_COUPLED").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            set: t.take("SET").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prog: t.take("PROG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_CYCLIC {
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub nsid1: Vec<i64>,
    pub nsid2: Vec<i64>,
    pub iglobal: Vec<i64>,
    pub isort: Vec<i64>,
}
impl BOUNDARY_CYCLIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_CYCLIC").unwrap());
        Self {
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            iglobal: t.take("IGLOBAL").and_then(|c| c.into_int()).unwrap_or_default(),
            isort: t.take("ISORT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_DE_NON_REFLECTING {
    pub nsid: Vec<i64>,
}
impl BOUNDARY_DE_NON_REFLECTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_DE_NON_REFLECTING").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_ACOUSTIC {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dt_out: Vec<i64>,
    pub t_start: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsid_ext: Vec<i64>,
    pub type_ext: Vec<i64>,
    pub nsid_int: Vec<i64>,
    pub type_int: Vec<i64>,
    pub fft_win: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub res: Vec<f64>,
    pub ndd: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bem_type: Vec<i64>,
    pub restart: Vec<i64>,
}
impl BOUNDARY_ELEMENT_METHOD_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_ACOUSTIC").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dt_out: t.take("DT_OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            t_start: t.take("T_START").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid_ext: t.take("NSID_EXT").and_then(|c| c.into_int()).unwrap_or_default(),
            type_ext: t.take("TYPE_EXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid_int: t.take("NSID_INT").and_then(|c| c.into_int()).unwrap_or_default(),
            type_int: t.take("TYPE_INT").and_then(|c| c.into_int()).unwrap_or_default(),
            fft_win: t.take("FFT_WIN").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            res: t.take("RES").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bem_type: t.take("BEM_TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            restart: t.take("RESTART").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_CONTROL {
    pub lwake: Vec<i64>,
    pub dtbem: Vec<f64>,
    pub iupbem: Vec<i64>,
    pub farbem: Vec<f64>,
}
impl BOUNDARY_ELEMENT_METHOD_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_CONTROL").unwrap());
        Self {
            lwake: t.take("LWAKE").and_then(|c| c.into_int()).unwrap_or_default(),
            dtbem: t.take("DTBEM").and_then(|c| c.into_float()).unwrap_or_default(),
            iupbem: t.take("IUPBEM").and_then(|c| c.into_int()).unwrap_or_default(),
            farbem: t.take("FARBEM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_FLOW {
    pub ssid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub ro: Vec<f64>,
    pub pstatic: Vec<f64>,
    pub mach: Vec<f64>,
}
impl BOUNDARY_ELEMENT_METHOD_FLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_FLOW").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pstatic: t.take("PSTATIC").and_then(|c| c.into_float()).unwrap_or_default(),
            mach: t.take("MACH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_NEIGHBOR {
    pub nelem: Vec<i64>,
    pub nabor1: Vec<i64>,
    pub nabor2: Vec<i64>,
    pub nabor3: Vec<i64>,
    pub nabor4: Vec<i64>,
}
impl BOUNDARY_ELEMENT_METHOD_NEIGHBOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_NEIGHBOR").unwrap());
        Self {
            nelem: t.take("NELEM").and_then(|c| c.into_int()).unwrap_or_default(),
            nabor1: t.take("NABOR1").and_then(|c| c.into_int()).unwrap_or_default(),
            nabor2: t.take("NABOR2").and_then(|c| c.into_int()).unwrap_or_default(),
            nabor3: t.take("NABOR3").and_then(|c| c.into_int()).unwrap_or_default(),
            nabor4: t.take("NABOR4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_SYMMETRY {
    pub bemsym: Vec<i64>,
}
impl BOUNDARY_ELEMENT_METHOD_SYMMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_SYMMETRY").unwrap());
        Self {
            bemsym: t.take("BEMSYM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_ELEMENT_METHOD_WAKE {
    pub nelem: Vec<i64>,
    pub nside: Vec<i64>,
}
impl BOUNDARY_ELEMENT_METHOD_WAKE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_ELEMENT_METHOD_WAKE").unwrap());
        Self {
            nelem: t.take("NELEM").and_then(|c| c.into_int()).unwrap_or_default(),
            nside: t.take("NSIDE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FLUX_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mlc1: Vec<f64>,
    pub mlc2: Vec<f64>,
    pub mlc3: Vec<f64>,
    pub mlc4: Vec<f64>,
    pub loc: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub unused: Vec<i64>,
    pub nhisv1: Vec<f64>,
    pub nhisv2: Vec<f64>,
    pub nhisv3: Vec<f64>,
    pub nhisv4: Vec<f64>,
    pub nhisv5: Vec<f64>,
    pub nhisv6: Vec<f64>,
    pub nhisv7: Vec<f64>,
    pub nhisv8: Vec<f64>,
}
impl BOUNDARY_FLUX_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FLUX_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mlc1: t.take("MLC1").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc2: t.take("MLC2").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc3: t.take("MLC3").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc4: t.take("MLC4").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv1: t.take("NHISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv2: t.take("NHISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv3: t.take("NHISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv4: t.take("NHISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv5: t.take("NHISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv6: t.take("NHISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv7: t.take("NHISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv8: t.take("NHISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FLUX_SET {
    pub ssid: Vec<i64>,
    pub pserod: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mlc1: Vec<f64>,
    pub mlc2: Vec<f64>,
    pub mlc3: Vec<f64>,
    pub mlc4: Vec<f64>,
    pub loc: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub f: Vec<i64>,
    pub nhisv1: Vec<f64>,
    pub nhisv2: Vec<f64>,
    pub nhisv3: Vec<f64>,
    pub nhisv4: Vec<f64>,
    pub nhisv5: Vec<f64>,
    pub nhisv6: Vec<f64>,
    pub nhisv7: Vec<f64>,
    pub nhisv8: Vec<f64>,
}
impl BOUNDARY_FLUX_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FLUX_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pserod: t.take("PSEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mlc1: t.take("MLC1").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc2: t.take("MLC2").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc3: t.take("MLC3").and_then(|c| c.into_float()).unwrap_or_default(),
            mlc4: t.take("MLC4").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv1: t.take("NHISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv2: t.take("NHISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv3: t.take("NHISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv4: t.take("NHISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv5: t.take("NHISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv6: t.take("NHISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv7: t.take("NHISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            nhisv8: t.take("NHISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FLUX_TRAJECTORY {
    pub ssid: Vec<i64>,
    pub pserod: Vec<i64>,
    pub nsid1: Vec<i64>,
    pub spd1: Vec<f64>,
    pub nsid2: Vec<i64>,
    pub spd2: Vec<f64>,
    pub relvel: Vec<i64>,
    pub erod: Vec<i64>,
    pub loc: Vec<i64>,
    pub lcrot: Vec<i64>,
    pub lclat: Vec<i64>,
    pub iform: Vec<i64>,
    pub lctim: Vec<i64>,
    pub q: Vec<f64>,
    pub lcinc: Vec<i64>,
    pub enfor: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub tx: Vec<f64>,
    pub ty: Vec<f64>,
    pub tz: Vec<f64>,
}
impl BOUNDARY_FLUX_TRAJECTORY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FLUX_TRAJECTORY").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pserod: t.take("PSEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            spd1: t.take("SPD1").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            spd2: t.take("SPD2").and_then(|c| c.into_float()).unwrap_or_default(),
            relvel: t.take("RELVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            erod: t.take("EROD").and_then(|c| c.into_int()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrot: t.take("LCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            lclat: t.take("LCLAT").and_then(|c| c.into_int()).unwrap_or_default(),
            iform: t.take("IFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctim: t.take("LCTIM").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            lcinc: t.take("LCINC").and_then(|c| c.into_int()).unwrap_or_default(),
            enfor: t.take("ENFOR").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            tx: t.take("TX").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            tz: t.take("TZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FREE_FIELD_GROUND_MOTION_NODE {
    pub ssid: Vec<i64>,
    pub typeid: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl BOUNDARY_FREE_FIELD_GROUND_MOTION_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FREE_FIELD_GROUND_MOTION_NODE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FREE_FIELD_GROUND_MOTION_POINT {
    pub ssid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl BOUNDARY_FREE_FIELD_GROUND_MOTION_POINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FREE_FIELD_GROUND_MOTION_POINT").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_FREE_FIELD_GROUND_MOTION_SET {
    pub ssid: Vec<i64>,
    pub typeid: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl BOUNDARY_FREE_FIELD_GROUND_MOTION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_FREE_FIELD_GROUND_MOTION_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_MCOL {
    pub nmcol: Vec<i64>,
    pub mxstep: Vec<i64>,
    pub endtmcol: Vec<f64>,
    pub tsubc: Vec<f64>,
    pub prtmcol: Vec<f64>,
    pub rbmcol: Vec<i64>,
    pub mcolfile: Vec<String>,
}
impl BOUNDARY_MCOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_MCOL").unwrap());
        Self {
            nmcol: t.take("NMCOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mxstep: t.take("MXSTEP").and_then(|c| c.into_int()).unwrap_or_default(),
            endtmcol: t.take("ENDTMCOL").and_then(|c| c.into_float()).unwrap_or_default(),
            tsubc: t.take("TSUBC").and_then(|c| c.into_float()).unwrap_or_default(),
            prtmcol: t.take("PRTMCOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rbmcol: t.take("RBMCOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mcolfile: t.take("MCOLFILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_NON_REFLECTING {
    pub ssid: Vec<i64>,
    pub ad: Vec<f64>,
    pub as_: Vec<f64>,
}
impl BOUNDARY_NON_REFLECTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_NON_REFLECTING").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ad: t.take("AD").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_NON_REFLECTING_2D {
    pub nsid: Vec<i64>,
    pub ad: Vec<i64>,
    pub as_: Vec<i64>,
}
impl BOUNDARY_NON_REFLECTING_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_NON_REFLECTING_2D").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ad: t.take("AD").and_then(|c| c.into_int()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PAP {
    pub segid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub cvmass: Vec<f64>,
    pub block: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub cvrper: Vec<f64>,
}
impl BOUNDARY_PAP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PAP").unwrap());
        Self {
            segid: t.take("SEGID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass: t.take("CVMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            cvrper: t.take("CVRPER").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PORE_FLUID_PART {
    pub pid: Vec<i64>,
    pub wtable: Vec<f64>,
    pub pf_rho: Vec<f64>,
    pub atype: Vec<i64>,
    pub pf_bulk: Vec<f64>,
    pub acurve: Vec<i64>,
    pub wtcur: Vec<i64>,
    pub suclim: Vec<f64>,
}
impl BOUNDARY_PORE_FLUID_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PORE_FLUID_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            wtable: t.take("WTABLE").and_then(|c| c.into_float()).unwrap_or_default(),
            pf_rho: t.take("PF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pf_bulk: t.take("PF_BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            acurve: t.take("ACURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            wtcur: t.take("WTCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            suclim: t.take("SUCLIM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PORE_FLUID_SET {
    pub psid: Vec<i64>,
    pub wtable: Vec<f64>,
    pub pf_rho: Vec<f64>,
    pub atype: Vec<i64>,
    pub pf_bulk: Vec<f64>,
    pub acurve: Vec<i64>,
    pub wtcur: Vec<i64>,
    pub suclim: Vec<f64>,
}
impl BOUNDARY_PORE_FLUID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PORE_FLUID_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            wtable: t.take("WTABLE").and_then(|c| c.into_float()).unwrap_or_default(),
            pf_rho: t.take("PF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pf_bulk: t.take("PF_BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            acurve: t.take("ACURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            wtcur: t.take("WTCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            suclim: t.take("SUCLIM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRECRACK {
    pub pid: Vec<i64>,
    pub ctype: Vec<i64>,
    pub np: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl BOUNDARY_PRECRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRECRACK").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_ACCELEROMETER_RIGID {
    pub pid: Vec<i64>,
    pub nid: Vec<i64>,
    pub cid: Vec<i64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_ACCELEROMETER_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_ACCELEROMETER_RIGID").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_FINAL_GEOMETRY {
    pub bpfgid: Vec<i64>,
    pub lcidf: Vec<i64>,
    pub deathd: Vec<f64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub lcid: Vec<i64>,
    pub death: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_FINAL_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_FINAL_GEOMETRY").unwrap());
        Self {
            bpfgid: t.take("BPFGID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidf: t.take("LCIDF").and_then(|c| c.into_int()).unwrap_or_default(),
            deathd: t.take("DEATHD").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION {
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_EDGE_UVW {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_EDGE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_EDGE_UVW").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_FACE_XYZ {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_FACE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_FACE_XYZ").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_NODE {
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_POINT_UVW {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_POINT_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_POINT_UVW").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_RIGID {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_RIGID").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_RIGID_BNDOUT2DYNAIN {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub prmr: Vec<String>,
}
impl BOUNDARY_PRESCRIBED_MOTION_RIGID_BNDOUT2DYNAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_RIGID_BNDOUT2DYNAIN").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL_BNDOUT2DYNAIN {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub prmr: Vec<String>,
}
impl BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL_BNDOUT2DYNAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_RIGID_LOCAL_BNDOUT2DYNAIN").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET {
    pub nsid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_BOX {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub boxid: Vec<i64>,
    pub toffset: Vec<i64>,
    pub lcbchk: Vec<i64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_BOX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_BOX").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            toffset: t.take("TOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbchk: t.take("LCBCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_EDGE_UVW {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_EDGE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_EDGE_UVW").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_FACE_XYZ {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_FACE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_FACE_XYZ").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_LINE {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub nbeg: Vec<i64>,
    pub nend: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_LINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_LINE").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            nbeg: t.take("NBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            nend: t.take("NEND").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_POINT_UVW {
    pub typeid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub offset1: Vec<f64>,
    pub offset2: Vec<f64>,
    pub lrb: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_POINT_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_POINT_UVW").unwrap());
        Self {
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            offset1: t.take("OFFSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            offset2: t.take("OFFSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_MOTION_SET_SEGMENT {
    pub sid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_MOTION_SET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_MOTION_SET_SEGMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_ANGLES {
    pub pidb: Vec<i64>,
    pub pida: Vec<i64>,
    pub intrp: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub toffset: Vec<i64>,
    pub lcidq1: Vec<i64>,
    pub lcidq2: Vec<i64>,
    pub lcidq3: Vec<i64>,
    pub iseq: Vec<i64>,
    pub ishft: Vec<i64>,
    pub body: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_ANGLES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_ANGLES").unwrap());
        Self {
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            intrp: t.take("INTRP").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            toffset: t.take("TOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidq1: t.take("LCIDQ1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidq2: t.take("LCIDQ2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidq3: t.take("LCIDQ3").and_then(|c| c.into_int()).unwrap_or_default(),
            iseq: t.take("ISEQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ishft: t.take("ISHFT").and_then(|c| c.into_int()).unwrap_or_default(),
            body: t.take("BODY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_DIRCOS {
    pub pidb: Vec<i64>,
    pub pida: Vec<i64>,
    pub intrp: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub toffset: Vec<i64>,
    pub lcidc11: Vec<i64>,
    pub lcidc12: Vec<i64>,
    pub lcidc13: Vec<i64>,
    pub lcidc21: Vec<i64>,
    pub lcidc22: Vec<i64>,
    pub lcidc23: Vec<i64>,
    pub lcidc31: Vec<i64>,
    pub lcidc32: Vec<i64>,
    pub lcidc33: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_DIRCOS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_DIRCOS").unwrap());
        Self {
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            intrp: t.take("INTRP").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            toffset: t.take("TOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc11: t.take("LCIDC11").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc12: t.take("LCIDC12").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc13: t.take("LCIDC13").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc21: t.take("LCIDC21").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc22: t.take("LCIDC22").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc23: t.take("LCIDC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc31: t.take("LCIDC31").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc32: t.take("LCIDC32").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidc33: t.take("LCIDC33").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_EULERP {
    pub pidb: Vec<i64>,
    pub pida: Vec<i64>,
    pub intrp: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub toffset: Vec<i64>,
    pub lcide1: Vec<i64>,
    pub lcide2: Vec<i64>,
    pub lcide3: Vec<i64>,
    pub lcide4: Vec<i64>,
}
impl BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_EULERP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_EULERP").unwrap());
        Self {
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            intrp: t.take("INTRP").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            toffset: t.take("TOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcide1: t.take("LCIDE1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcide2: t.take("LCIDE2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcide3: t.take("LCIDE3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcide4: t.take("LCIDE4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_VECTOR {
    pub pidb: Vec<i64>,
    pub pida: Vec<i64>,
    pub intrp: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub toffset: Vec<i64>,
    pub lcidv1: Vec<i64>,
    pub lcidv2: Vec<i64>,
    pub lcidv3: Vec<i64>,
    pub lcids: Vec<i64>,
    pub valspin: Vec<f64>,
}
impl BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESCRIBED_ORIENTATION_RIGID_VECTOR").unwrap());
        Self {
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            intrp: t.take("INTRP").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            toffset: t.take("TOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv1: t.take("LCIDV1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv2: t.take("LCIDV2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv3: t.take("LCIDV3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            valspin: t.take("VALSPIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESSURE_OUTFLOW_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl BOUNDARY_PRESSURE_OUTFLOW_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESSURE_OUTFLOW_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PRESSURE_OUTFLOW_SET {
    pub ssid: Vec<i64>,
}
impl BOUNDARY_PRESSURE_OUTFLOW_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PRESSURE_OUTFLOW_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PWP_NODE {
    pub nid: Vec<i64>,
    pub lc: Vec<f64>,
    pub cmult: Vec<f64>,
    pub lcdr: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub iphre: Vec<i64>,
    pub itotex: Vec<i64>,
    pub idrflag: Vec<i64>,
    pub f: Vec<i64>,
    pub lcleak: Vec<i64>,
    pub cleak: Vec<f64>,
    pub lcpum: Vec<i64>,
}
impl BOUNDARY_PWP_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PWP_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_float()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            iphre: t.take("IPHRE").and_then(|c| c.into_int()).unwrap_or_default(),
            itotex: t.take("ITOTEX").and_then(|c| c.into_int()).unwrap_or_default(),
            idrflag: t.take("IDRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcleak: t.take("LCLEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            cleak: t.take("CLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpum: t.take("LCPUM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PWP_SET {
    pub sid: Vec<i64>,
    pub lc: Vec<i64>,
    pub cmult: Vec<f64>,
    pub lcdr: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub iphre: Vec<i64>,
    pub itotex: Vec<i64>,
    pub idrflag: Vec<i64>,
    pub f: Vec<i64>,
    pub lcleak: Vec<i64>,
    pub cleak: Vec<f64>,
    pub lcpum: Vec<i64>,
}
impl BOUNDARY_PWP_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PWP_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            iphre: t.take("IPHRE").and_then(|c| c.into_int()).unwrap_or_default(),
            itotex: t.take("ITOTEX").and_then(|c| c.into_int()).unwrap_or_default(),
            idrflag: t.take("IDRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcleak: t.take("LCLEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            cleak: t.take("CLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpum: t.take("LCPUM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PWP_TABLE {
    pub pid: Vec<i64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub f_4: Vec<f64>,
    pub itotex: Vec<i64>,
    pub f_2_2: Vec<f64>,
    pub table: Vec<i64>,
}
impl BOUNDARY_PWP_TABLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PWP_TABLE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            itotex: t.take("ITOTEX").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            table: t.take("TABLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PWP_TABLE_SET {
    pub pid: Vec<i64>,
    pub lc: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub f_3: Vec<f64>,
    pub itotex: Vec<i64>,
    pub f_2_2: Vec<f64>,
    pub table: Vec<i64>,
}
impl BOUNDARY_PWP_TABLE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PWP_TABLE_SET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            itotex: t.take("ITOTEX").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            table: t.take("TABLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_PZEPOT {
    pub id: Vec<i64>,
    pub nsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<i64>,
}
impl BOUNDARY_PZEPOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_PZEPOT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_ENCLOSURE {
    pub brencid: Vec<i64>,
    pub encname: Vec<String>,
    pub calopt: Vec<i64>,
    pub outopt: Vec<i64>,
    pub conopt: Vec<i64>,
    pub encname_2: Vec<String>,
    pub smflag: Vec<i64>,
    pub smmaxi: Vec<i64>,
    pub smabst: Vec<f64>,
    pub smrelt: Vec<f64>,
    pub stype: Vec<i64>,
    pub slmaxi: Vec<i64>,
    pub slabst: Vec<f64>,
    pub slrelt: Vec<f64>,
    pub slmlev: Vec<i64>,
    pub ssid: Vec<i64>,
    pub nint: Vec<i64>,
    pub block: Vec<i64>,
    pub selcid: Vec<i64>,
    pub semult: Vec<f64>,
    pub loc: Vec<i64>,
}
impl BOUNDARY_RADIATION_ENCLOSURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_ENCLOSURE").unwrap());
        Self {
            brencid: t.take("BRENCID").and_then(|c| c.into_int()).unwrap_or_default(),
            encname: t.take("ENCNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            calopt: t.take("CALOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            outopt: t.take("OUTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            conopt: t.take("CONOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            encname_2: t.take("ENCNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            smflag: t.take("SMFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            smmaxi: t.take("SMMAXI").and_then(|c| c.into_int()).unwrap_or_default(),
            smabst: t.take("SMABST").and_then(|c| c.into_float()).unwrap_or_default(),
            smrelt: t.take("SMRELT").and_then(|c| c.into_float()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            slmaxi: t.take("SLMAXI").and_then(|c| c.into_int()).unwrap_or_default(),
            slabst: t.take("SLABST").and_then(|c| c.into_float()).unwrap_or_default(),
            slrelt: t.take("SLRELT").and_then(|c| c.into_float()).unwrap_or_default(),
            slmlev: t.take("SLMLEV").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            selcid: t.take("SELCID").and_then(|c| c.into_int()).unwrap_or_default(),
            semult: t.take("SEMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub type_: Vec<i64>,
    pub rflcid: Vec<i64>,
    pub rfmult: Vec<f64>,
    pub tilcid: Vec<i64>,
    pub timult: Vec<f64>,
    pub loc: Vec<i64>,
}
impl BOUNDARY_RADIATION_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rflcid: t.take("RFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            rfmult: t.take("RFMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tilcid: t.take("TILCID").and_then(|c| c.into_int()).unwrap_or_default(),
            timult: t.take("TIMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SEGMENT_VF_CALCULATE {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub type_: Vec<i64>,
    pub block: Vec<i64>,
    pub nint: Vec<i64>,
    pub selcid: Vec<i64>,
    pub semult: Vec<f64>,
}
impl BOUNDARY_RADIATION_SEGMENT_VF_CALCULATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SEGMENT_VF_CALCULATE").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            selcid: t.take("SELCID").and_then(|c| c.into_int()).unwrap_or_default(),
            semult: t.take("SEMULT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SEGMENT_VF_READ {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub type_: Vec<i64>,
    pub block: Vec<i64>,
    pub nint: Vec<i64>,
    pub selcid: Vec<i64>,
    pub semult: Vec<f64>,
}
impl BOUNDARY_RADIATION_SEGMENT_VF_READ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SEGMENT_VF_READ").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            selcid: t.take("SELCID").and_then(|c| c.into_int()).unwrap_or_default(),
            semult: t.take("SEMULT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SET {
    pub ssid: Vec<i64>,
    pub type_: Vec<i64>,
    pub f20: Vec<i64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub pserod: Vec<i64>,
    pub rflcid: Vec<i64>,
    pub rfmult: Vec<f64>,
    pub tilcid: Vec<i64>,
    pub timult: Vec<f64>,
    pub loc: Vec<i64>,
}
impl BOUNDARY_RADIATION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            pserod: t.take("PSEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            rflcid: t.take("RFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            rfmult: t.take("RFMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tilcid: t.take("TILCID").and_then(|c| c.into_int()).unwrap_or_default(),
            timult: t.take("TIMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SET_EF_CALCULATE {
    pub ssid: Vec<i64>,
    pub nmat: Vec<i64>,
    pub npht: Vec<i64>,
    pub errmax: Vec<i64>,
}
impl BOUNDARY_RADIATION_SET_EF_CALCULATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SET_EF_CALCULATE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmat: t.take("NMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            npht: t.take("NPHT").and_then(|c| c.into_int()).unwrap_or_default(),
            errmax: t.take("ERRMAX").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SET_EF_READ {
    pub ssid: Vec<i64>,
    pub nmat: Vec<i64>,
    pub npht: Vec<i64>,
    pub errmax: Vec<i64>,
}
impl BOUNDARY_RADIATION_SET_EF_READ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SET_EF_READ").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmat: t.take("NMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            npht: t.take("NPHT").and_then(|c| c.into_int()).unwrap_or_default(),
            errmax: t.take("ERRMAX").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SET_VF_CALCULATE {
    pub ssid: Vec<i64>,
    pub type_: Vec<i64>,
    pub rad_grp: Vec<i64>,
    pub file_no: Vec<i64>,
    pub block: Vec<i64>,
    pub nint: Vec<i64>,
    pub selcid: Vec<i64>,
    pub semult: Vec<f64>,
}
impl BOUNDARY_RADIATION_SET_VF_CALCULATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SET_VF_CALCULATE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rad_grp: t.take("RAD_GRP").and_then(|c| c.into_int()).unwrap_or_default(),
            file_no: t.take("FILE_NO").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            selcid: t.take("SELCID").and_then(|c| c.into_int()).unwrap_or_default(),
            semult: t.take("SEMULT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_RADIATION_SET_VF_READ {
    pub ssid: Vec<i64>,
    pub type_: Vec<i64>,
    pub rad_grp: Vec<i64>,
    pub file_no: Vec<i64>,
    pub block: Vec<i64>,
    pub nint: Vec<i64>,
    pub selcid: Vec<i64>,
    pub semult: Vec<f64>,
}
impl BOUNDARY_RADIATION_SET_VF_READ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_RADIATION_SET_VF_READ").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rad_grp: t.take("RAD_GRP").and_then(|c| c.into_int()).unwrap_or_default(),
            file_no: t.take("FILE_NO").and_then(|c| c.into_int()).unwrap_or_default(),
            block: t.take("BLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            selcid: t.take("SELCID").and_then(|c| c.into_int()).unwrap_or_default(),
            semult: t.take("SEMULT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SALE_MESH_FACE {
    pub option: Vec<String>,
    pub mshid: Vec<i64>,
    pub x: Vec<i64>,
    pub x_2: Vec<i64>,
    pub y: Vec<i64>,
    pub y_2: Vec<i64>,
    pub z: Vec<i64>,
    pub z_2: Vec<i64>,
}
impl BOUNDARY_SALE_MESH_FACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SALE_MESH_FACE").unwrap());
        Self {
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            mshid: t.take("MSHID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("-X").and_then(|c| c.into_int()).unwrap_or_default(),
            x_2: t.take("+X").and_then(|c| c.into_int()).unwrap_or_default(),
            y: t.take("-Y").and_then(|c| c.into_int()).unwrap_or_default(),
            y_2: t.take("+Y").and_then(|c| c.into_int()).unwrap_or_default(),
            z: t.take("-Z").and_then(|c| c.into_int()).unwrap_or_default(),
            z_2: t.take("-Z_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SLIDING_PLANE {
    pub nsid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub copt: Vec<i64>,
}
impl BOUNDARY_SLIDING_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SLIDING_PLANE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            copt: t.take("COPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC {
    pub nid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
    pub dofrx: Vec<i64>,
    pub dofry: Vec<i64>,
    pub dofrz: Vec<i64>,
}
impl BOUNDARY_SPC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("DOFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("DOFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("DOFZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrx: t.take("DOFRX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofry: t.take("DOFRY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrz: t.take("DOFRZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_NODE {
    pub nid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
    pub dofrx: Vec<i64>,
    pub dofry: Vec<i64>,
    pub dofrz: Vec<i64>,
}
impl BOUNDARY_SPC_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("DOFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("DOFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("DOFZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrx: t.take("DOFRX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofry: t.take("DOFRY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrz: t.take("DOFRZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_NODE_BIRTH_DEATH {
    pub nid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
    pub dofrx: Vec<i64>,
    pub dofry: Vec<i64>,
    pub dofrz: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
}
impl BOUNDARY_SPC_NODE_BIRTH_DEATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_NODE_BIRTH_DEATH").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("DOFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("DOFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("DOFZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrx: t.take("DOFRX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofry: t.take("DOFRY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrz: t.take("DOFRZ").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_SET {
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
    pub dofrx: Vec<i64>,
    pub dofry: Vec<i64>,
    pub dofrz: Vec<i64>,
}
impl BOUNDARY_SPC_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("DOFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("DOFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("DOFZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrx: t.take("DOFRX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofry: t.take("DOFRY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrz: t.take("DOFRZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_SET_BIRTH_DEATH {
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
    pub dofrx: Vec<i64>,
    pub dofry: Vec<i64>,
    pub dofrz: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
}
impl BOUNDARY_SPC_SET_BIRTH_DEATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_SET_BIRTH_DEATH").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("DOFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("DOFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("DOFZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrx: t.take("DOFRX").and_then(|c| c.into_int()).unwrap_or_default(),
            dofry: t.take("DOFRY").and_then(|c| c.into_int()).unwrap_or_default(),
            dofrz: t.take("DOFRZ").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_SYMMETRY_PLANE {
    pub idsp: Vec<i64>,
    pub pid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tol: Vec<f64>,
}
impl BOUNDARY_SPC_SYMMETRY_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_SYMMETRY_PLANE").unwrap());
        Self {
            idsp: t.take("IDSP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPC_SYMMETRY_PLANE_SET {
    pub idsp: Vec<i64>,
    pub psid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tol: Vec<f64>,
}
impl BOUNDARY_SPC_SYMMETRY_PLANE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPC_SYMMETRY_PLANE_SET").unwrap());
        Self {
            idsp: t.take("IDSP").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPH_FLOW {
    pub id: Vec<i64>,
    pub styp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
    pub nid: Vec<i64>,
    pub vid: Vec<i64>,
}
impl BOUNDARY_SPH_FLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPH_FLOW").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPH_NON_REFLECTING {
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vhx: Vec<f64>,
    pub vhy: Vec<f64>,
    pub vhz: Vec<f64>,
}
impl BOUNDARY_SPH_NON_REFLECTING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPH_NON_REFLECTING").unwrap());
        Self {
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vhx: t.take("VHX").and_then(|c| c.into_float()).unwrap_or_default(),
            vhy: t.take("VHY").and_then(|c| c.into_float()).unwrap_or_default(),
            vhz: t.take("VHZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SPH_SYMMETRY_PLANE {
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vhx: Vec<f64>,
    pub vhy: Vec<f64>,
    pub vhz: Vec<f64>,
}
impl BOUNDARY_SPH_SYMMETRY_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SPH_SYMMETRY_PLANE").unwrap());
        Self {
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vhx: t.take("VHX").and_then(|c| c.into_float()).unwrap_or_default(),
            vhy: t.take("VHY").and_then(|c| c.into_float()).unwrap_or_default(),
            vhz: t.take("VHZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_SYMMETRY_FAILURE {
    pub ssid: Vec<i64>,
    pub fs: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vhx: Vec<f64>,
    pub vhy: Vec<f64>,
    pub vhz: Vec<f64>,
}
impl BOUNDARY_SYMMETRY_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_SYMMETRY_FAILURE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vhx: t.take("VHX").and_then(|c| c.into_float()).unwrap_or_default(),
            vhy: t.take("VHY").and_then(|c| c.into_float()).unwrap_or_default(),
            vhz: t.take("VHZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_TEMPERATURE_NODE {
    pub nid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub loc: Vec<i64>,
    pub tdeath: Vec<f64>,
    pub tbirth: Vec<f64>,
}
impl BOUNDARY_TEMPERATURE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_TEMPERATURE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_TEMPERATURE_PERIODIC_SET {
    pub ssid1: Vec<i64>,
    pub ptype: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub tdlcid: Vec<i64>,
    pub axe: Vec<i64>,
    pub nid: Vec<i64>,
    pub angle: Vec<f64>,
}
impl BOUNDARY_TEMPERATURE_PERIODIC_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_TEMPERATURE_PERIODIC_SET").unwrap());
        Self {
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            tdlcid: t.take("TDLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            axe: t.take("AXE").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_TEMPERATURE_RSW {
    pub sid: Vec<i64>,
    pub option: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub tdeath: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub loc: Vec<i64>,
    pub dist: Vec<f64>,
    pub h1: Vec<f64>,
    pub h2: Vec<f64>,
    pub r: Vec<f64>,
    pub tempc: Vec<f64>,
    pub tempb: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub hz1: Vec<f64>,
    pub hz2: Vec<f64>,
    pub rz: Vec<f64>,
    pub tempzb: Vec<f64>,
}
impl BOUNDARY_TEMPERATURE_RSW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_TEMPERATURE_RSW").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            h1: t.take("H1").and_then(|c| c.into_float()).unwrap_or_default(),
            h2: t.take("H2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            tempc: t.take("TEMPC").and_then(|c| c.into_float()).unwrap_or_default(),
            tempb: t.take("TEMPB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            hz1: t.take("HZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            hz2: t.take("HZ2").and_then(|c| c.into_float()).unwrap_or_default(),
            rz: t.take("RZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tempzb: t.take("TEMPZB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_TEMPERATURE_SET {
    pub nsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub loc: Vec<i64>,
    pub tdeath: Vec<f64>,
    pub tbirth: Vec<f64>,
}
impl BOUNDARY_TEMPERATURE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_TEMPERATURE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_TEMPERATURE_TRAJECTORY {
    pub pid: Vec<i64>,
    pub pype: Vec<i64>,
    pub nsid1: Vec<i64>,
    pub spd1: Vec<f64>,
    pub nsid2: Vec<i64>,
    pub spd2: Vec<f64>,
    pub f: Vec<i64>,
    pub relvel: Vec<i64>,
    pub iform: Vec<i64>,
    pub lcid: Vec<i64>,
    pub tmult: Vec<f64>,
    pub lcrot: Vec<i64>,
    pub lcmov: Vec<i64>,
    pub lclat: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub tx: Vec<f64>,
    pub ty: Vec<f64>,
    pub tz: Vec<f64>,
}
impl BOUNDARY_TEMPERATURE_TRAJECTORY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_TEMPERATURE_TRAJECTORY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            pype: t.take("PYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            spd1: t.take("SPD1").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            spd2: t.take("SPD2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            relvel: t.take("RELVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            iform: t.take("IFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tmult: t.take("TMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrot: t.take("LCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmov: t.take("LCMOV").and_then(|c| c.into_int()).unwrap_or_default(),
            lclat: t.take("LCLAT").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            tx: t.take("TX").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            tz: t.take("TZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_BULKFLOW_ELEMENT {
    pub eid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mdot: Vec<f64>,
}
impl BOUNDARY_THERMAL_BULKFLOW_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_BULKFLOW_ELEMENT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mdot: t.take("MDOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_BULKFLOW_SET {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mdot: Vec<f64>,
}
impl BOUNDARY_THERMAL_BULKFLOW_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_BULKFLOW_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mdot: t.take("MDOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_BULKFLOW_SET_UPWIND {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mdot: Vec<f64>,
}
impl BOUNDARY_THERMAL_BULKFLOW_SET_UPWIND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_BULKFLOW_SET_UPWIND").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mdot: t.take("MDOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_BULKNODE {
    pub nid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nbnseg: Vec<i64>,
    pub vol: Vec<f64>,
    pub lcid: Vec<i64>,
    pub h: Vec<f64>,
    pub aexp: Vec<f64>,
    pub bexp: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl BOUNDARY_THERMAL_BULKNODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_BULKNODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nbnseg: t.take("NBNSEG").and_then(|c| c.into_int()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            aexp: t.take("AEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            bexp: t.take("BEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_WELD {
    pub pid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub nid: Vec<i64>,
    pub nflag: Vec<i64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub n2id: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub cf: Vec<f64>,
    pub cr: Vec<f64>,
    pub lcid: Vec<i64>,
    pub q: Vec<f64>,
    pub ff: Vec<f64>,
    pub fr: Vec<f64>,
    pub tx: Vec<f64>,
    pub ty: Vec<f64>,
    pub tz: Vec<f64>,
}
impl BOUNDARY_THERMAL_WELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_WELD").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nflag: t.take("NFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            n2id: t.take("N2ID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("CR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            ff: t.take("FF").and_then(|c| c.into_float()).unwrap_or_default(),
            fr: t.take("FR").and_then(|c| c.into_float()).unwrap_or_default(),
            tx: t.take("TX").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            tz: t.take("TZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_THERMAL_WELD_TRAJECTORY {
    pub pid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub nsid1: Vec<i64>,
    pub spd1: Vec<f64>,
    pub nsid2: Vec<i64>,
    pub spd2: Vec<f64>,
    pub ncyc: Vec<i64>,
    pub relvel: Vec<i64>,
    pub iform: Vec<i64>,
    pub lcid: Vec<i64>,
    pub q: Vec<f64>,
    pub lcrot: Vec<i64>,
    pub lcmov: Vec<i64>,
    pub lclat: Vec<i64>,
    pub disc: Vec<f64>,
    pub enfor: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub tx: Vec<f64>,
    pub ty: Vec<f64>,
    pub tz: Vec<f64>,
}
impl BOUNDARY_THERMAL_WELD_TRAJECTORY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_THERMAL_WELD_TRAJECTORY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            spd1: t.take("SPD1").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            spd2: t.take("SPD2").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            relvel: t.take("RELVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            iform: t.take("IFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrot: t.take("LCROT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmov: t.take("LCMOV").and_then(|c| c.into_int()).unwrap_or_default(),
            lclat: t.take("LCLAT").and_then(|c| c.into_int()).unwrap_or_default(),
            disc: t.take("DISC").and_then(|c| c.into_float()).unwrap_or_default(),
            enfor: t.take("ENFOR").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            tx: t.take("TX").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            tz: t.take("TZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct BOUNDARY_USA_SURFACE {
    pub ssid: Vec<i64>,
    pub wetdry: Vec<i64>,
    pub nbeam: Vec<i64>,
}
impl BOUNDARY_USA_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("BOUNDARY_USA_SURFACE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            wetdry: t.take("WETDRY").and_then(|c| c.into_int()).unwrap_or_default(),
            nbeam: t.take("NBEAM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CASE_BEGIN {
}
impl CASE_BEGIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CASE_BEGIN").unwrap());
        Self {
        }
    }
}

pub struct CASE_CASE {
    pub caseid: Vec<i64>,
    pub jobid: Vec<String>,
    pub commands: Vec<String>,
    pub scid1: Vec<i64>,
    pub scid2: Vec<i64>,
    pub scid3: Vec<i64>,
    pub scid4: Vec<i64>,
    pub scid5: Vec<i64>,
    pub scid6: Vec<i64>,
    pub scid7: Vec<i64>,
    pub scid8: Vec<i64>,
}
impl CASE_CASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CASE_CASE").unwrap());
        Self {
            caseid: t.take("CASEID").and_then(|c| c.into_int()).unwrap_or_default(),
            jobid: t.take("JOBID").and_then(|c| c.into_str()).unwrap_or_default(),
            commands: t.take("COMMANDS").and_then(|c| c.into_str()).unwrap_or_default(),
            scid1: t.take("SCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            scid2: t.take("SCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            scid3: t.take("SCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            scid4: t.take("SCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            scid5: t.take("SCID5").and_then(|c| c.into_int()).unwrap_or_default(),
            scid6: t.take("SCID6").and_then(|c| c.into_int()).unwrap_or_default(),
            scid7: t.take("SCID7").and_then(|c| c.into_int()).unwrap_or_default(),
            scid8: t.take("SCID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CASE_END {
}
impl CASE_END {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CASE_END").unwrap());
        Self {
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_MSURF {
    pub msurfid: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_MSURF").unwrap());
        Self {
            msurfid: t.take("MSURFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_MSURF_SET {
    pub msurf_s: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_MSURF_SET").unwrap());
        Self {
            msurf_s: t.take("MSURF_S").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_PART {
    pub msurfid: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_PART").unwrap());
        Self {
            msurfid: t.take("MSURFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_PART_SET {
    pub msurf_s: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_PART_SET").unwrap());
        Self {
            msurf_s: t.take("MSURF_S").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXISYMMETRIC_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_AXISYMMETRIC_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXISYMMETRIC_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXIS_SYMMETRIC_PART {
    pub msurfid: Vec<i64>,
}
impl CESE_BOUNDARY_AXIS_SYMMETRIC_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXIS_SYMMETRIC_PART").unwrap());
        Self {
            msurfid: t.take("MSURFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXIS_SYMMETRIC_PART_SET {
    pub msurf_s: Vec<i64>,
}
impl CESE_BOUNDARY_AXIS_SYMMETRIC_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXIS_SYMMETRIC_PART_SET").unwrap());
        Self {
            msurf_s: t.take("MSURF_S").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXIS_SYMMETRIC_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_AXIS_SYMMETRIC_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXIS_SYMMETRIC_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_AXIS_SYMMETRIC_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_AXIS_SYMMETRIC_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_AXIS_SYMMETRIC_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_BLAST_LOAD_MSURF {
    pub bid: Vec<i64>,
    pub msurfid: Vec<i64>,
}
impl CESE_BOUNDARY_BLAST_LOAD_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_BLAST_LOAD_MSURF").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            msurfid: t.take("MSURFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_BLAST_LOAD_MSURF_SET {
    pub bid: Vec<i64>,
    pub msurf_s: Vec<i64>,
}
impl CESE_BOUNDARY_BLAST_LOAD_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_BLAST_LOAD_MSURF_SET").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            msurf_s: t.take("MSURF_S").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_BLAST_LOAD_SEGMENT {
    pub bid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_BLAST_LOAD_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_BLAST_LOAD_SEGMENT").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_BLAST_LOAD_SET {
    pub bid: Vec<i64>,
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_BLAST_LOAD_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_BLAST_LOAD_SET").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CONJ_HEAT_MSURF {
    pub msurfid: Vec<i64>,
}
impl CESE_BOUNDARY_CONJ_HEAT_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CONJ_HEAT_MSURF").unwrap());
        Self {
            msurfid: t.take("MSURFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CONJ_HEAT_MSURF_SET {
    pub msurf_s: Vec<i64>,
}
impl CESE_BOUNDARY_CONJ_HEAT_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CONJ_HEAT_MSURF_SET").unwrap());
        Self {
            msurf_s: t.take("MSURF_S").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CONJ_HEAT_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_CONJ_HEAT_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CONJ_HEAT_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CONJ_HEAT_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_CONJ_HEAT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CONJ_HEAT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_MSURF {
    pub srfprt1: Vec<i64>,
    pub srfprt2: Vec<i64>,
    pub cyctyp: Vec<i64>,
    pub axisx1: Vec<f64>,
    pub axisy1: Vec<f64>,
    pub axisz1: Vec<f64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub rotang: Vec<f64>,
    pub transx: Vec<f64>,
    pub transy: Vec<f64>,
    pub transz: Vec<f64>,
}
impl CESE_BOUNDARY_CYCLIC_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_MSURF").unwrap());
        Self {
            srfprt1: t.take("SRFPRT1").and_then(|c| c.into_int()).unwrap_or_default(),
            srfprt2: t.take("SRFPRT2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            axisx1: t.take("AXISX1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisy1: t.take("AXISY1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisz1: t.take("AXISZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rotang: t.take("ROTANG").and_then(|c| c.into_float()).unwrap_or_default(),
            transx: t.take("TRANSX").and_then(|c| c.into_float()).unwrap_or_default(),
            transy: t.take("TRANSY").and_then(|c| c.into_float()).unwrap_or_default(),
            transz: t.take("TRANSZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_MSURF_SET {
    pub surfsid1: Vec<i64>,
    pub surfsid2: Vec<i64>,
    pub cyctyp: Vec<i64>,
    pub axisx1: Vec<f64>,
    pub axisy1: Vec<f64>,
    pub axisz1: Vec<f64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub rotang: Vec<f64>,
    pub transx: Vec<f64>,
    pub transy: Vec<f64>,
    pub transz: Vec<f64>,
}
impl CESE_BOUNDARY_CYCLIC_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_MSURF_SET").unwrap());
        Self {
            surfsid1: t.take("SURFSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            surfsid2: t.take("SURFSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            axisx1: t.take("AXISX1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisy1: t.take("AXISY1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisz1: t.take("AXISZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rotang: t.take("ROTANG").and_then(|c| c.into_float()).unwrap_or_default(),
            transx: t.take("TRANSX").and_then(|c| c.into_float()).unwrap_or_default(),
            transy: t.take("TRANSY").and_then(|c| c.into_float()).unwrap_or_default(),
            transz: t.take("TRANSZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_PART {
    pub srfprt1: Vec<i64>,
    pub srfprt2: Vec<i64>,
    pub cyctyp: Vec<i64>,
    pub axisx1: Vec<f64>,
    pub axisy1: Vec<f64>,
    pub axisz1: Vec<f64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub rotang: Vec<f64>,
    pub transx: Vec<f64>,
    pub transy: Vec<f64>,
    pub transz: Vec<f64>,
}
impl CESE_BOUNDARY_CYCLIC_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_PART").unwrap());
        Self {
            srfprt1: t.take("SRFPRT1").and_then(|c| c.into_int()).unwrap_or_default(),
            srfprt2: t.take("SRFPRT2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            axisx1: t.take("AXISX1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisy1: t.take("AXISY1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisz1: t.take("AXISZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rotang: t.take("ROTANG").and_then(|c| c.into_float()).unwrap_or_default(),
            transx: t.take("TRANSX").and_then(|c| c.into_float()).unwrap_or_default(),
            transy: t.take("TRANSY").and_then(|c| c.into_float()).unwrap_or_default(),
            transz: t.take("TRANSZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_PART_SET {
    pub surfsid1: Vec<i64>,
    pub surfsid2: Vec<i64>,
    pub cyctyp: Vec<i64>,
    pub axisx1: Vec<f64>,
    pub axisy1: Vec<f64>,
    pub axisz1: Vec<f64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub rotang: Vec<f64>,
    pub transx: Vec<f64>,
    pub transy: Vec<f64>,
    pub transz: Vec<f64>,
}
impl CESE_BOUNDARY_CYCLIC_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_PART_SET").unwrap());
        Self {
            surfsid1: t.take("SURFSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            surfsid2: t.take("SURFSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            axisx1: t.take("AXISX1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisy1: t.take("AXISY1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisz1: t.take("AXISZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rotang: t.take("ROTANG").and_then(|c| c.into_float()).unwrap_or_default(),
            transx: t.take("TRANSX").and_then(|c| c.into_float()).unwrap_or_default(),
            transy: t.take("TRANSY").and_then(|c| c.into_float()).unwrap_or_default(),
            transz: t.take("TRANSZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_SEGMENT {
    pub nd1: Vec<i64>,
    pub nd2: Vec<i64>,
    pub nd3: Vec<i64>,
    pub nd4: Vec<i64>,
    pub np1: Vec<i64>,
    pub np2: Vec<i64>,
    pub np3: Vec<i64>,
    pub np4: Vec<i64>,
}
impl CESE_BOUNDARY_CYCLIC_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_SEGMENT").unwrap());
        Self {
            nd1: t.take("ND1").and_then(|c| c.into_int()).unwrap_or_default(),
            nd2: t.take("ND2").and_then(|c| c.into_int()).unwrap_or_default(),
            nd3: t.take("ND3").and_then(|c| c.into_int()).unwrap_or_default(),
            nd4: t.take("ND4").and_then(|c| c.into_int()).unwrap_or_default(),
            np1: t.take("NP1").and_then(|c| c.into_int()).unwrap_or_default(),
            np2: t.take("NP2").and_then(|c| c.into_int()).unwrap_or_default(),
            np3: t.take("NP3").and_then(|c| c.into_int()).unwrap_or_default(),
            np4: t.take("NP4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_CYCLIC_SET {
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub cyctyp: Vec<i64>,
    pub axisx1: Vec<f64>,
    pub axisy1: Vec<f64>,
    pub axisz1: Vec<f64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub rotang: Vec<f64>,
    pub transx: Vec<f64>,
    pub transy: Vec<f64>,
    pub transz: Vec<f64>,
}
impl CESE_BOUNDARY_CYCLIC_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_CYCLIC_SET").unwrap());
        Self {
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            axisx1: t.take("AXISX1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisy1: t.take("AXISY1").and_then(|c| c.into_float()).unwrap_or_default(),
            axisz1: t.take("AXISZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rotang: t.take("ROTANG").and_then(|c| c.into_float()).unwrap_or_default(),
            transx: t.take("TRANSX").and_then(|c| c.into_float()).unwrap_or_default(),
            transy: t.take("TRANSY").and_then(|c| c.into_float()).unwrap_or_default(),
            transz: t.take("TRANSZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_FSI_PART {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_FSI_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_FSI_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_FSI_PART_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_FSI_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_FSI_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_FSI_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_FSI_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_FSI_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_FSI_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_FSI_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_FSI_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_MSURF {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_MSURF").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_MSURF_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_MSURF_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_PART {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_PART_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_NON_REFLECTIVE_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_NON_REFLECTIVE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_NON_REFLECTIVE_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_MSURF {
    pub surfprt: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_MSURF").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_MSURF_SET {
    pub surfsid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_MSURF_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_PART {
    pub surfprt: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_PART_SET {
    pub surfsid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_PRESCRIBED_SET {
    pub ssid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl CESE_BOUNDARY_PRESCRIBED_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_PRESCRIBED_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_MSURF {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_MSURF").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_MSURF_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_MSURF_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_PART {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_PART_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_REFLECTIVE_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_REFLECTIVE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_REFLECTIVE_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl CESE_BOUNDARY_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SET {
    pub ssid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl CESE_BOUNDARY_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_MSURF {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_MSURF").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_MSURF_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_MSURF_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_PART {
    pub surfprt: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_PART_SET {
    pub surfsid: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SLIDING_SET {
    pub ssid: Vec<i64>,
}
impl CESE_BOUNDARY_SLIDING_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SLIDING_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_MSURF {
    pub surfprt: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_MSURF").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE {
    pub surfprt: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_MSURF_SET {
    pub surfsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_MSURF_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_MSURF_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_MSURF_SET_ROTATE {
    pub surfsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_MSURF_SET_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_MSURF_SET_ROTATE").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_PART {
    pub surfprt: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_PART").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_PART_ROTATE {
    pub surfprt: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_PART_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_PART_ROTATE").unwrap());
        Self {
            surfprt: t.take("SURFPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_PART_SET {
    pub surfsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_PART_SET").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_PART_SET_ROTATE {
    pub surfsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_PART_SET_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_PART_SET_ROTATE").unwrap());
        Self {
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_SEGMENT {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_SEGMENT").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_SEGMENT_ROTATE {
    pub n1: Vec<i64>,
    pub n1_2: Vec<i64>,
    pub n1_3: Vec<i64>,
    pub n1_4: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_SEGMENT_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_SEGMENT_ROTATE").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n1_2: t.take("N1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            n1_3: t.take("N1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            n1_4: t.take("N1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_SET {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_BOUNDARY_SOLID_WALL_SET_ROTATE {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl CESE_BOUNDARY_SOLID_WALL_SET_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_BOUNDARY_SOLID_WALL_SET_ROTATE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_CHEMISTRY_D3PLOT {
    pub modelid: Vec<i64>,
    pub species: Vec<String>,
}
impl CESE_CHEMISTRY_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_CHEMISTRY_D3PLOT").unwrap());
        Self {
            modelid: t.take("MODELID").and_then(|c| c.into_int()).unwrap_or_default(),
            species: t.take("SPECIES").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CESE_CONTROL_LIMITER {
    pub idlmt: Vec<i64>,
    pub alfa: Vec<f64>,
    pub beta: Vec<f64>,
    pub epsr: Vec<f64>,
}
impl CESE_CONTROL_LIMITER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_CONTROL_LIMITER").unwrap());
        Self {
            idlmt: t.take("IDLMT").and_then(|c| c.into_int()).unwrap_or_default(),
            alfa: t.take("ALFA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_CONTROL_MESH_MOV {
    pub mmsh: Vec<i64>,
    pub lim_iter: Vec<i64>,
    pub reltol: Vec<f64>,
    pub abstol: Vec<f64>,
}
impl CESE_CONTROL_MESH_MOV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_CONTROL_MESH_MOV").unwrap());
        Self {
            mmsh: t.take("MMSH").and_then(|c| c.into_int()).unwrap_or_default(),
            lim_iter: t.take("LIM_ITER").and_then(|c| c.into_int()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_CONTROL_SOLVER {
    pub icese: Vec<i64>,
    pub iflow: Vec<i64>,
    pub igeom: Vec<i64>,
    pub iframe: Vec<i64>,
}
impl CESE_CONTROL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_CONTROL_SOLVER").unwrap());
        Self {
            icese: t.take("ICESE").and_then(|c| c.into_int()).unwrap_or_default(),
            iflow: t.take("IFLOW").and_then(|c| c.into_int()).unwrap_or_default(),
            igeom: t.take("IGEOM").and_then(|c| c.into_int()).unwrap_or_default(),
            iframe: t.take("IFRAME").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_CONTROL_TIMESTEP {
    pub iddt: Vec<i64>,
    pub cfl: Vec<f64>,
    pub dtint: Vec<f64>,
}
impl CESE_CONTROL_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_CONTROL_TIMESTEP").unwrap());
        Self {
            iddt: t.take("IDDT").and_then(|c| c.into_int()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtint: t.take("DTINT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DATABASE_ELOUT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub elsid: Vec<i64>,
}
impl CESE_DATABASE_ELOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DATABASE_ELOUT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            elsid: t.take("ELSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DATABASE_FLUXAVG {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub elsid: Vec<i64>,
}
impl CESE_DATABASE_FLUXAVG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DATABASE_FLUXAVG").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            elsid: t.take("ELSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DATABASE_FSIDRAG {
    pub outlv: Vec<i64>,
}
impl CESE_DATABASE_FSIDRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DATABASE_FSIDRAG").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DATABASE_POINTOUT {
    pub psid: Vec<i64>,
    pub dtout: Vec<f64>,
    pub pstype: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl CESE_DATABASE_POINTOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DATABASE_POINTOUT").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            pstype: t.take("PSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DATABASE_SSETDRAG {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub elsid: Vec<i64>,
}
impl CESE_DATABASE_SSETDRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DATABASE_SSETDRAG").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            elsid: t.take("ELSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DEFINE_NONINERTIAL {
    pub freq: Vec<f64>,
    pub lcid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
    pub l: Vec<f64>,
    pub r: Vec<f64>,
    pub relv: Vec<i64>,
}
impl CESE_DEFINE_NONINERTIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DEFINE_NONINERTIAL").unwrap());
        Self {
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nx: t.take("Nx").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("Ny").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("Nz").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            relv: t.take("RELV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DEFINE_POINT {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl CESE_DEFINE_POINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DEFINE_POINT").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_DRAG {
    pub press: Vec<f64>,
}
impl CESE_DRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_DRAG").unwrap());
        Self {
            press: t.take("PRESS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_EOS_CAV_HOMOG_EQUILIB {
    pub eosid: Vec<i64>,
    pub p_vap: Vec<f64>,
    pub p_liq: Vec<f64>,
    pub a_vap: Vec<f64>,
    pub a_liq: Vec<f64>,
    pub u_vap: Vec<f64>,
    pub u_liq: Vec<f64>,
    pub p_sat_vap: Vec<f64>,
}
impl CESE_EOS_CAV_HOMOG_EQUILIB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_EOS_CAV_HOMOG_EQUILIB").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            p_vap: t.take("p_vap").and_then(|c| c.into_float()).unwrap_or_default(),
            p_liq: t.take("p_liq").and_then(|c| c.into_float()).unwrap_or_default(),
            a_vap: t.take("a_vap").and_then(|c| c.into_float()).unwrap_or_default(),
            a_liq: t.take("a_liq").and_then(|c| c.into_float()).unwrap_or_default(),
            u_vap: t.take("u_vap").and_then(|c| c.into_float()).unwrap_or_default(),
            u_liq: t.take("u_liq").and_then(|c| c.into_float()).unwrap_or_default(),
            p_sat_vap: t.take("P_sat_vap").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_EOS_IDEAL_GAS {
    pub eosid: Vec<i64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
}
impl CESE_EOS_IDEAL_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_EOS_IDEAL_GAS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cv: t.take("Cv").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("Cp").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_EOS_INFLATOR1 {
    pub eosid: Vec<i64>,
    pub cp0: Vec<f64>,
    pub cp1: Vec<f64>,
    pub cp2: Vec<f64>,
    pub cp3: Vec<f64>,
    pub cp4: Vec<f64>,
    pub cv0: Vec<f64>,
    pub cv1: Vec<f64>,
    pub cv2: Vec<f64>,
    pub cv3: Vec<f64>,
    pub cv4: Vec<f64>,
}
impl CESE_EOS_INFLATOR1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_EOS_INFLATOR1").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cp0: t.take("Cp0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1: t.take("Cp1").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2: t.take("Cp2").and_then(|c| c.into_float()).unwrap_or_default(),
            cp3: t.take("Cp3").and_then(|c| c.into_float()).unwrap_or_default(),
            cp4: t.take("Cp4").and_then(|c| c.into_float()).unwrap_or_default(),
            cv0: t.take("Cv0").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1: t.take("Cv1").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2: t.take("Cv2").and_then(|c| c.into_float()).unwrap_or_default(),
            cv3: t.take("Cv3").and_then(|c| c.into_float()).unwrap_or_default(),
            cv4: t.take("Cv4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_EOS_INFLATOR2 {
    pub eosid: Vec<i64>,
    pub cp1_0: Vec<f64>,
    pub cp1_1: Vec<f64>,
    pub cp1_2: Vec<f64>,
    pub cp1_3: Vec<f64>,
    pub cp1_4: Vec<f64>,
    pub cp2_0: Vec<f64>,
    pub cp2_1: Vec<f64>,
    pub cp2_2: Vec<f64>,
    pub cp2_3: Vec<f64>,
    pub cp2_4: Vec<f64>,
    pub cv1_0: Vec<f64>,
    pub cv1_1: Vec<f64>,
    pub cv1_2: Vec<f64>,
    pub cv1_3: Vec<f64>,
    pub cv1_4: Vec<f64>,
    pub cv2_0: Vec<f64>,
    pub cv2_1: Vec<f64>,
    pub cv2_2: Vec<f64>,
    pub cv2_3: Vec<f64>,
    pub cv2_4: Vec<f64>,
}
impl CESE_EOS_INFLATOR2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_EOS_INFLATOR2").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cp1_0: t.take("Cp1_0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1_1: t.take("Cp1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1_2: t.take("Cp1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1_3: t.take("Cp1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1_4: t.take("Cp1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2_0: t.take("Cp2_0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2_1: t.take("Cp2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2_2: t.take("Cp2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2_3: t.take("Cp2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2_4: t.take("Cp2_4").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1_0: t.take("Cv1_0").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1_1: t.take("Cv1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1_2: t.take("Cv1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1_3: t.take("Cv1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1_4: t.take("Cv1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2_0: t.take("Cv2_0").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2_1: t.take("Cv2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2_2: t.take("Cv2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2_3: t.take("Cv2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2_4: t.take("Cv2_4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_FSI_EXCLUDE {
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl CESE_FSI_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_FSI_EXCLUDE").unwrap());
        Self {
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL {
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub rho: Vec<f64>,
    pub p: Vec<f64>,
    pub t: Vec<f64>,
}
impl CESE_INITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL").unwrap());
        Self {
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_CHEMISTRY {
    pub chemid: Vec<i64>,
    pub compid: Vec<i64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rhoic: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub hic: Vec<f64>,
}
impl CESE_INITIAL_CHEMISTRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_CHEMISTRY").unwrap());
        Self {
            chemid: t.take("CHEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoic: t.take("RHOIC").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            hic: t.take("HIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_CHEMISTRY_ELEMENT {
    pub chemid: Vec<i64>,
    pub compid: Vec<i64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rhoic: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub hic: Vec<f64>,
    pub ele1: Vec<i64>,
    pub ele2: Vec<i64>,
    pub ele3: Vec<i64>,
    pub ele4: Vec<i64>,
    pub ele5: Vec<i64>,
    pub ele6: Vec<i64>,
    pub ele7: Vec<i64>,
    pub ele8: Vec<i64>,
}
impl CESE_INITIAL_CHEMISTRY_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_CHEMISTRY_ELEMENT").unwrap());
        Self {
            chemid: t.take("CHEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoic: t.take("RHOIC").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            hic: t.take("HIC").and_then(|c| c.into_float()).unwrap_or_default(),
            ele1: t.take("ELE1").and_then(|c| c.into_int()).unwrap_or_default(),
            ele2: t.take("ELE2").and_then(|c| c.into_int()).unwrap_or_default(),
            ele3: t.take("ELE3").and_then(|c| c.into_int()).unwrap_or_default(),
            ele4: t.take("ELE4").and_then(|c| c.into_int()).unwrap_or_default(),
            ele5: t.take("ELE5").and_then(|c| c.into_int()).unwrap_or_default(),
            ele6: t.take("ELE6").and_then(|c| c.into_int()).unwrap_or_default(),
            ele7: t.take("ELE7").and_then(|c| c.into_int()).unwrap_or_default(),
            ele8: t.take("ELE8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_CHEMISTRY_PART {
    pub partid: Vec<i64>,
    pub chemid: Vec<i64>,
    pub compid: Vec<i64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rhoic: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub hic: Vec<f64>,
}
impl CESE_INITIAL_CHEMISTRY_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_CHEMISTRY_PART").unwrap());
        Self {
            partid: t.take("PARTID").and_then(|c| c.into_int()).unwrap_or_default(),
            chemid: t.take("CHEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoic: t.take("RHOIC").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            hic: t.take("HIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_CHEMISTRY_SET {
    pub setid: Vec<i64>,
    pub chemid: Vec<i64>,
    pub compid: Vec<i64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rhoic: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub hic: Vec<f64>,
}
impl CESE_INITIAL_CHEMISTRY_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_CHEMISTRY_SET").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            chemid: t.take("CHEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoic: t.take("RHOIC").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            hic: t.take("HIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_ELEMENT {
    pub eid: Vec<i64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub rho: Vec<f64>,
    pub p: Vec<f64>,
    pub t: Vec<f64>,
}
impl CESE_INITIAL_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_ELEMENT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_INITIAL_SET {
    pub esid: Vec<i64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub rho: Vec<f64>,
    pub p: Vec<f64>,
    pub t: Vec<f64>,
}
impl CESE_INITIAL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_INITIAL_SET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_MAT_001 {
    pub mid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub pr: Vec<f64>,
}
impl CESE_MAT_001 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_MAT_001").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("Pr").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_MAT_002 {
    pub mid: Vec<i64>,
    pub mu: Vec<f64>,
    pub prnd: Vec<f64>,
}
impl CESE_MAT_002 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_MAT_002").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            prnd: t.take("PRND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_MAT_GAS {
    pub mid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub pr: Vec<f64>,
}
impl CESE_MAT_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_MAT_GAS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("Pr").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CESE_PART {
    pub pid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
}
impl CESE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CESE_SURFACE_MECHSSID_D3PLOT {
    pub ssid: Vec<i64>,
    pub dir: Vec<String>,
}
impl CESE_SURFACE_MECHSSID_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_SURFACE_MECHSSID_D3PLOT").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CESE_SURFACE_MECHVARS_D3PLOT {
    pub output_quantity: Vec<String>,
}
impl CESE_SURFACE_MECHVARS_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CESE_SURFACE_MECHVARS_D3PLOT").unwrap());
        Self {
            output_quantity: t.take("Output_Quantity").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_BOUNDARY_CONDITION {
    pub nid: Vec<i64>,
    pub bcc: Vec<i64>,
}
impl CHANGE_BOUNDARY_CONDITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_BOUNDARY_CONDITION").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            bcc: t.take("BCC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_CONTACT_SMALL_PENETRATION {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl CHANGE_CONTACT_SMALL_PENETRATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_CONTACT_SMALL_PENETRATION").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_CURVE_DEFINITION {
    pub lcid: Vec<i64>,
}
impl CHANGE_CURVE_DEFINITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_CURVE_DEFINITION").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_OUTPUT {
    pub iascii: Vec<i64>,
}
impl CHANGE_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_OUTPUT").unwrap());
        Self {
            iascii: t.take("IASCII").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_RIGIDWALL_GEOMETRIC {
}
impl CHANGE_RIGIDWALL_GEOMETRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_RIGIDWALL_GEOMETRIC").unwrap());
        Self {
        }
    }
}

pub struct CHANGE_RIGIDWALL_PLANAR {
}
impl CHANGE_RIGIDWALL_PLANAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_RIGIDWALL_PLANAR").unwrap());
        Self {
        }
    }
}

pub struct CHANGE_RIGID_BODY_CONSTRAINT {
    pub pid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl CHANGE_RIGID_BODY_CONSTRAINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_RIGID_BODY_CONSTRAINT").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_RIGID_BODY_INERTIA {
    pub id: Vec<i64>,
    pub pid: Vec<i64>,
    pub tm: Vec<f64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
}
impl CHANGE_RIGID_BODY_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_RIGID_BODY_INERTIA").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_RIGID_BODY_STOPPER {
    pub pid: Vec<i64>,
    pub lcmax: Vec<i64>,
    pub lcmin: Vec<i64>,
    pub psidmx: Vec<i64>,
    pub psidmn: Vec<i64>,
    pub lcvmnx: Vec<i64>,
    pub dir: Vec<i64>,
    pub vid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
}
impl CHANGE_RIGID_BODY_STOPPER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_RIGID_BODY_STOPPER").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmax: t.take("LCMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmin: t.take("LCMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            psidmx: t.take("PSIDMX").and_then(|c| c.into_int()).unwrap_or_default(),
            psidmn: t.take("PSIDMN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvmnx: t.take("LCVMNX").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_STATUS_REPORT_FREQUENCY {
    pub ikedit: Vec<i64>,
}
impl CHANGE_STATUS_REPORT_FREQUENCY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_STATUS_REPORT_FREQUENCY").unwrap());
        Self {
            ikedit: t.take("IKEDIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_THERMAL_PARAMETERS {
    pub ts: Vec<i64>,
    pub dt: Vec<f64>,
    pub tmin: Vec<f64>,
    pub tmax: Vec<f64>,
    pub dtemp: Vec<f64>,
    pub tscp: Vec<f64>,
    pub refmax: Vec<i64>,
    pub tol: Vec<f64>,
}
impl CHANGE_THERMAL_PARAMETERS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_THERMAL_PARAMETERS").unwrap());
        Self {
            ts: t.take("TS").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tmin: t.take("TMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtemp: t.take("DTEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tscp: t.take("TSCP").and_then(|c| c.into_float()).unwrap_or_default(),
            refmax: t.take("REFMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY {
    pub nsid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
}
impl CHANGE_VELOCITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY_GENERATION {
    pub nsid_pid: Vec<i64>,
    pub styp: Vec<i64>,
    pub omega: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub ivatn: Vec<i64>,
    pub icid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
    pub phase: Vec<i64>,
    pub irigid: Vec<i64>,
}
impl CHANGE_VELOCITY_GENERATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY_GENERATION").unwrap());
        Self {
            nsid_pid: t.take("NSID/PID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ivatn: t.take("IVATN").and_then(|c| c.into_int()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            irigid: t.take("IRIGID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY_NODE {
    pub nid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
}
impl CHANGE_VELOCITY_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY_NODE_ONLY {
    pub nid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
}
impl CHANGE_VELOCITY_NODE_ONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY_NODE_ONLY").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY_RIGID_BODY {
    pub pid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
}
impl CHANGE_VELOCITY_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY_RIGID_BODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHANGE_VELOCITY_ZERO {
}
impl CHANGE_VELOCITY_ZERO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHANGE_VELOCITY_ZERO").unwrap());
        Self {
        }
    }
}

pub struct CHEMISTRY_COMPOSITION {
    pub id: Vec<i64>,
    pub modelid: Vec<i64>,
    pub molfr: Vec<f64>,
    pub species: Vec<String>,
}
impl CHEMISTRY_COMPOSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_COMPOSITION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            modelid: t.take("MODELID").and_then(|c| c.into_int()).unwrap_or_default(),
            molfr: t.take("MOLFR").and_then(|c| c.into_float()).unwrap_or_default(),
            species: t.take("SPECIES").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_0D {
    pub id: Vec<i64>,
    pub compid: Vec<i64>,
    pub soltyp: Vec<i64>,
    pub plotdt: Vec<f64>,
    pub csp_sel: Vec<i64>,
    pub dt: Vec<f64>,
    pub tlimit: Vec<f64>,
    pub tic: Vec<f64>,
    pub pic: Vec<f64>,
    pub ric: Vec<f64>,
    pub eic: Vec<f64>,
    pub ampl: Vec<f64>,
    pub ycut: Vec<f64>,
}
impl CHEMISTRY_CONTROL_0D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_0D").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            soltyp: t.take("SOLTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            plotdt: t.take("PLOTDT").and_then(|c| c.into_float()).unwrap_or_default(),
            csp_sel: t.take("CSP_SEL").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlimit: t.take("TLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            ric: t.take("RIC").and_then(|c| c.into_float()).unwrap_or_default(),
            eic: t.take("EIC").and_then(|c| c.into_float()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            ycut: t.take("YCUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_1D {
    pub id: Vec<i64>,
    pub xyzd: Vec<f64>,
    pub detdir: Vec<i64>,
    pub csp_sel: Vec<i64>,
    pub file: Vec<String>,
    pub ampl: Vec<f64>,
    pub ycut: Vec<f64>,
}
impl CHEMISTRY_CONTROL_1D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_1D").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            xyzd: t.take("XYZD").and_then(|c| c.into_float()).unwrap_or_default(),
            detdir: t.take("DETDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            csp_sel: t.take("CSP_SEL").and_then(|c| c.into_int()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            ycut: t.take("YCUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_BLAST1D {
    pub blastid: Vec<i64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub file: Vec<String>,
}
impl CHEMISTRY_CONTROL_BLAST1D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_BLAST1D").unwrap());
        Self {
            blastid: t.take("BLASTID").and_then(|c| c.into_int()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_CSP {
    pub id: Vec<i64>,
    pub ierropt: Vec<i64>,
    pub ampl: Vec<f64>,
    pub ycut: Vec<f64>,
}
impl CHEMISTRY_CONTROL_CSP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_CSP").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            ierropt: t.take("IERROPT").and_then(|c| c.into_int()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            ycut: t.take("YCUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_FULL {
    pub id: Vec<i64>,
    pub errlim: Vec<f64>,
    pub rhomin: Vec<f64>,
    pub tmin: Vec<f64>,
}
impl CHEMISTRY_CONTROL_FULL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_FULL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            errlim: t.take("ERRLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            rhomin: t.take("RHOMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tmin: t.take("TMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_HGI_PART {
    pub id: Vec<i64>,
    pub compid: Vec<i64>,
    pub exit_bc: Vec<i64>,
    pub file: Vec<String>,
}
impl CHEMISTRY_CONTROL_HGI_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_HGI_PART").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            exit_bc: t.take("EXIT_BC").and_then(|c| c.into_int()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_HGI_SET {
    pub id: Vec<i64>,
    pub compid: Vec<i64>,
    pub exit_bc: Vec<i64>,
    pub file: Vec<String>,
}
impl CHEMISTRY_CONTROL_HGI_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_HGI_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            exit_bc: t.take("EXIT_BC").and_then(|c| c.into_int()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_INFLATOR {
    pub model: Vec<i64>,
    pub out_type: Vec<i64>,
    pub truntim: Vec<f64>,
    pub delt: Vec<f64>,
    pub ptime: Vec<f64>,
    pub file: Vec<String>,
    pub density: Vec<String>,
    pub species_name: Vec<String>,
}
impl CHEMISTRY_CONTROL_INFLATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_INFLATOR").unwrap());
        Self {
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            out_type: t.take("OUT_TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            truntim: t.take("TRUNTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            delt: t.take("DELT").and_then(|c| c.into_float()).unwrap_or_default(),
            ptime: t.take("PTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_str()).unwrap_or_default(),
            species_name: t.take("Species_Name").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_PYRPTECHNIC {
    pub comp1id: Vec<i64>,
    pub vol1: Vec<f64>,
    pub area1: Vec<f64>,
    pub cd1: Vec<f64>,
    pub p1: Vec<f64>,
    pub t1: Vec<f64>,
    pub delp1: Vec<f64>,
    pub tflame: Vec<f64>,
    pub comp2id: Vec<i64>,
    pub vol2: Vec<f64>,
    pub area2: Vec<f64>,
    pub cd2: Vec<f64>,
    pub p2: Vec<f64>,
    pub t2: Vec<f64>,
    pub delp2: Vec<f64>,
    pub truntime: Vec<f64>,
    pub comp3id: Vec<i64>,
    pub vol3: Vec<f64>,
    pub p3: Vec<f64>,
    pub t3: Vec<f64>,
    pub ptime: Vec<f64>,
    pub file: Vec<String>,
}
impl CHEMISTRY_CONTROL_PYRPTECHNIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_PYRPTECHNIC").unwrap());
        Self {
            comp1id: t.take("COMP1ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol1: t.take("VOL1").and_then(|c| c.into_float()).unwrap_or_default(),
            area1: t.take("AREA1").and_then(|c| c.into_float()).unwrap_or_default(),
            cd1: t.take("CD1").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            delp1: t.take("DELP1").and_then(|c| c.into_float()).unwrap_or_default(),
            tflame: t.take("TFLAME").and_then(|c| c.into_float()).unwrap_or_default(),
            comp2id: t.take("COMP2ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol2: t.take("VOL2").and_then(|c| c.into_float()).unwrap_or_default(),
            area2: t.take("AREA2").and_then(|c| c.into_float()).unwrap_or_default(),
            cd2: t.take("CD2").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            delp2: t.take("DELP2").and_then(|c| c.into_float()).unwrap_or_default(),
            truntime: t.take("TRUNTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            comp3id: t.take("COMP3ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol3: t.take("VOL3").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            ptime: t.take("PTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_TBX {
    pub idchem: Vec<i64>,
    pub usepar: Vec<i64>,
}
impl CHEMISTRY_CONTROL_TBX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_TBX").unwrap());
        Self {
            idchem: t.take("IDCHEM").and_then(|c| c.into_int()).unwrap_or_default(),
            usepar: t.take("USEPAR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_CONTROL_ZND {
    pub id: Vec<i64>,
    pub f: Vec<f64>,
    pub eplus: Vec<f64>,
    pub q0: Vec<f64>,
    pub gam: Vec<f64>,
    pub xyzd: Vec<f64>,
    pub detdir: Vec<i64>,
}
impl CHEMISTRY_CONTROL_ZND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_CONTROL_ZND").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            eplus: t.take("EPLUS").and_then(|c| c.into_float()).unwrap_or_default(),
            q0: t.take("Q0").and_then(|c| c.into_float()).unwrap_or_default(),
            gam: t.take("GAM").and_then(|c| c.into_float()).unwrap_or_default(),
            xyzd: t.take("XYZD").and_then(|c| c.into_float()).unwrap_or_default(),
            detdir: t.take("DETDIR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_DET_INITIATION {
    pub id: Vec<i64>,
    pub compid: Vec<i64>,
    pub nmesh: Vec<i64>,
    pub dlen: Vec<f64>,
    pub cfl: Vec<f64>,
    pub tlimit: Vec<f64>,
    pub xyzd: Vec<f64>,
    pub detdir: Vec<i64>,
    pub file: Vec<String>,
}
impl CHEMISTRY_DET_INITIATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_DET_INITIATION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            compid: t.take("COMPID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmesh: t.take("NMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            dlen: t.take("DLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            tlimit: t.take("TLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            xyzd: t.take("XYZD").and_then(|c| c.into_float()).unwrap_or_default(),
            detdir: t.take("DETDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            file: t.take("FILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_INFLATOR_PROPERTIES {
    pub comp_id: Vec<i64>,
    pub pdia: Vec<f64>,
    pub pheight: Vec<f64>,
    pub pmass: Vec<f64>,
    pub totmass: Vec<f64>,
    pub tflame: Vec<f64>,
    pub pindex: Vec<f64>,
    pub a0: Vec<f64>,
    pub tdelay: Vec<f64>,
    pub risetime: Vec<f64>,
    pub comp1id: Vec<i64>,
    pub vol1: Vec<f64>,
    pub area1: Vec<f64>,
    pub cd1: Vec<f64>,
    pub p1: Vec<f64>,
    pub t1: Vec<f64>,
    pub delp1: Vec<f64>,
    pub delt1: Vec<f64>,
    pub comp2id: Vec<i64>,
    pub vol2: Vec<f64>,
    pub area2: Vec<f64>,
    pub cd2: Vec<f64>,
    pub p2: Vec<f64>,
    pub t2: Vec<f64>,
    pub delp2: Vec<f64>,
    pub delt2: Vec<f64>,
    pub comp3id: Vec<i64>,
    pub vol3: Vec<f64>,
    pub p3: Vec<f64>,
    pub t3: Vec<f64>,
    pub com42id: Vec<i64>,
    pub vol4: Vec<f64>,
    pub area4: Vec<f64>,
    pub cd4: Vec<f64>,
    pub p4: Vec<f64>,
    pub t4: Vec<f64>,
    pub delp4: Vec<f64>,
    pub delt4: Vec<f64>,
    pub comp5id: Vec<i64>,
    pub vol5: Vec<f64>,
    pub area5: Vec<f64>,
    pub cd5: Vec<f64>,
    pub p5: Vec<f64>,
    pub t5: Vec<f64>,
    pub delp5: Vec<f64>,
    pub delt5: Vec<f64>,
}
impl CHEMISTRY_INFLATOR_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_INFLATOR_PROPERTIES").unwrap());
        Self {
            comp_id: t.take("COMP_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pdia: t.take("PDIA").and_then(|c| c.into_float()).unwrap_or_default(),
            pheight: t.take("PHEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            pmass: t.take("PMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            totmass: t.take("TOTMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            tflame: t.take("TFLAME").and_then(|c| c.into_float()).unwrap_or_default(),
            pindex: t.take("PINDEX").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            tdelay: t.take("TDELAY").and_then(|c| c.into_float()).unwrap_or_default(),
            risetime: t.take("RISETIME").and_then(|c| c.into_float()).unwrap_or_default(),
            comp1id: t.take("COMP1ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol1: t.take("VOL1").and_then(|c| c.into_float()).unwrap_or_default(),
            area1: t.take("AREA1").and_then(|c| c.into_float()).unwrap_or_default(),
            cd1: t.take("CD1").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            delp1: t.take("DELP1").and_then(|c| c.into_float()).unwrap_or_default(),
            delt1: t.take("DELT1").and_then(|c| c.into_float()).unwrap_or_default(),
            comp2id: t.take("COMP2ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol2: t.take("VOL2").and_then(|c| c.into_float()).unwrap_or_default(),
            area2: t.take("AREA2").and_then(|c| c.into_float()).unwrap_or_default(),
            cd2: t.take("CD2").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            delp2: t.take("DELP2").and_then(|c| c.into_float()).unwrap_or_default(),
            delt2: t.take("DELT2").and_then(|c| c.into_float()).unwrap_or_default(),
            comp3id: t.take("COMP3ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol3: t.take("VOL3").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            com42id: t.take("COM42ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol4: t.take("VOL4").and_then(|c| c.into_float()).unwrap_or_default(),
            area4: t.take("AREA4").and_then(|c| c.into_float()).unwrap_or_default(),
            cd4: t.take("CD4").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            delp4: t.take("DELP4").and_then(|c| c.into_float()).unwrap_or_default(),
            delt4: t.take("DELT4").and_then(|c| c.into_float()).unwrap_or_default(),
            comp5id: t.take("COMP5ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vol5: t.take("VOL5").and_then(|c| c.into_float()).unwrap_or_default(),
            area5: t.take("AREA5").and_then(|c| c.into_float()).unwrap_or_default(),
            cd5: t.take("CD5").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            delp5: t.take("DELP5").and_then(|c| c.into_float()).unwrap_or_default(),
            delt5: t.take("DELT5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_MODEL {
    pub modelid: Vec<i64>,
    pub jacsel: Vec<i64>,
    pub errlim: Vec<f64>,
    pub file1: Vec<String>,
    pub file2: Vec<String>,
    pub file3: Vec<String>,
}
impl CHEMISTRY_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_MODEL").unwrap());
        Self {
            modelid: t.take("MODELID").and_then(|c| c.into_int()).unwrap_or_default(),
            jacsel: t.take("JACSEL").and_then(|c| c.into_int()).unwrap_or_default(),
            errlim: t.take("ERRLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            file1: t.take("FILE1").and_then(|c| c.into_str()).unwrap_or_default(),
            file2: t.take("FILE2").and_then(|c| c.into_str()).unwrap_or_default(),
            file3: t.take("FILE3").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CHEMISTRY_PATH {
    pub dir: Vec<String>,
}
impl CHEMISTRY_PATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CHEMISTRY_PATH").unwrap());
        Self {
            dir: t.take("DIR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct COMMENT_COMMENT {
    pub comment: Vec<String>,
}
impl COMMENT_COMMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMMENT_COMMENT").unwrap());
        Self {
            comment: t.take("COMMENT").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_CHILD {
    pub did: Vec<i64>,
    pub units: Vec<i64>,
    pub size: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub gx: Vec<f64>,
    pub gy: Vec<f64>,
    pub gz: Vec<f64>,
}
impl COMPONENT_GEBOD_CHILD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_CHILD").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            size: t.take("SIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gx: t.take("GX").and_then(|c| c.into_float()).unwrap_or_default(),
            gy: t.take("GY").and_then(|c| c.into_float()).unwrap_or_default(),
            gz: t.take("GZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_FEMALE {
    pub did: Vec<i64>,
    pub units: Vec<i64>,
    pub size: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub gx: Vec<f64>,
    pub gy: Vec<f64>,
    pub gz: Vec<f64>,
}
impl COMPONENT_GEBOD_FEMALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_FEMALE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            size: t.take("SIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gx: t.take("GX").and_then(|c| c.into_float()).unwrap_or_default(),
            gy: t.take("GY").and_then(|c| c.into_float()).unwrap_or_default(),
            gz: t.take("GZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LEFT_ANKLE {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LEFT_ANKLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LEFT_ANKLE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LEFT_ELBOW {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LEFT_ELBOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LEFT_ELBOW").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LEFT_HIP {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LEFT_HIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LEFT_HIP").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LEFT_KNEE {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LEFT_KNEE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LEFT_KNEE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LEFT_SHOULDER {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LEFT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LEFT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_LOWER_NECK {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_LOWER_NECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_LOWER_NECK").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_PELVIS {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_PELVIS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_PELVIS").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_RIGHT_ANKLE {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_RIGHT_ANKLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_RIGHT_ANKLE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_RIGHT_ELBOW {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_RIGHT_ELBOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_RIGHT_ELBOW").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_RIGHT_HIP {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_RIGHT_HIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_RIGHT_HIP").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_RIGHT_KNEE {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_RIGHT_KNEE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_RIGHT_KNEE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_RIGHT_SHOULDER {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_RIGHT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_RIGHT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_UPPER_NECK {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_UPPER_NECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_UPPER_NECK").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_JOINT_WAIST {
    pub did: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub scf1: Vec<f64>,
    pub scf2: Vec<f64>,
    pub scf3: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub neut1: Vec<f64>,
    pub neut2: Vec<f64>,
    pub neut3: Vec<f64>,
    pub losa1: Vec<f64>,
    pub hisa1: Vec<f64>,
    pub losa2: Vec<f64>,
    pub hisa2: Vec<f64>,
    pub losa3: Vec<f64>,
    pub hisa3: Vec<f64>,
    pub unk1: Vec<f64>,
    pub unk2: Vec<f64>,
    pub unk3: Vec<f64>,
}
impl COMPONENT_GEBOD_JOINT_WAIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_JOINT_WAIST").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            scf1: t.take("SCF1").and_then(|c| c.into_float()).unwrap_or_default(),
            scf2: t.take("SCF2").and_then(|c| c.into_float()).unwrap_or_default(),
            scf3: t.take("SCF3").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            neut1: t.take("NEUT1").and_then(|c| c.into_float()).unwrap_or_default(),
            neut2: t.take("NEUT2").and_then(|c| c.into_float()).unwrap_or_default(),
            neut3: t.take("NEUT3").and_then(|c| c.into_float()).unwrap_or_default(),
            losa1: t.take("LOSA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa1: t.take("HISA1").and_then(|c| c.into_float()).unwrap_or_default(),
            losa2: t.take("LOSA2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa2: t.take("HISA2").and_then(|c| c.into_float()).unwrap_or_default(),
            losa3: t.take("LOSA3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisa3: t.take("HISA3").and_then(|c| c.into_float()).unwrap_or_default(),
            unk1: t.take("UNK1").and_then(|c| c.into_float()).unwrap_or_default(),
            unk2: t.take("UNK2").and_then(|c| c.into_float()).unwrap_or_default(),
            unk3: t.take("UNK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_GEBOD_MALE {
    pub did: Vec<i64>,
    pub units: Vec<i64>,
    pub size: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub gx: Vec<f64>,
    pub gy: Vec<f64>,
    pub gz: Vec<f64>,
}
impl COMPONENT_GEBOD_MALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_GEBOD_MALE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            size: t.take("SIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gx: t.take("GX").and_then(|c| c.into_float()).unwrap_or_default(),
            gy: t.take("GY").and_then(|c| c.into_float()).unwrap_or_default(),
            gz: t.take("GZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII {
    pub did: Vec<i64>,
    pub size: Vec<i64>,
    pub units: Vec<i64>,
    pub defrm: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub hx: Vec<f64>,
    pub hy: Vec<f64>,
    pub hz: Vec<f64>,
    pub rx: Vec<f64>,
    pub ry: Vec<f64>,
    pub rz: Vec<f64>,
}
impl COMPONENT_HYBRIDIII {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            size: t.take("SIZE").and_then(|c| c.into_int()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            defrm: t.take("DEFRM").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            hx: t.take("HX").and_then(|c| c.into_float()).unwrap_or_default(),
            hy: t.take("HY").and_then(|c| c.into_float()).unwrap_or_default(),
            hz: t.take("HZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rx: t.take("RX").and_then(|c| c.into_float()).unwrap_or_default(),
            ry: t.take("RY").and_then(|c| c.into_float()).unwrap_or_default(),
            rz: t.take("RZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_ANKLE {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_ANKLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_ANKLE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_ELBOW {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_ELBOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_ELBOW").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_HIP {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_HIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_HIP").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_KNEE {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_KNEE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_KNEE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_SHOULDER {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LEFT_WRIST {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LEFT_WRIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LEFT_WRIST").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LOWER_NECK {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LOWER_NECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LOWER_NECK").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_LUMBAR {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_LUMBAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_LUMBAR").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIBCAGE {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIBCAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIBCAGE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_ANKLE {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_ANKLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_ANKLE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_ELBOW {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_ELBOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_ELBOW").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_HIP {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_HIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_HIP").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_KNEE {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_KNEE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_KNEE").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_SHOULDER {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_RIGHT_WRIST {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_RIGHT_WRIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_RIGHT_WRIST").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct COMPONENT_HYBRIDIII_JOINT_UPPER_NECK {
    pub did: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub fric: Vec<f64>,
    pub c1: Vec<f64>,
    pub alo1: Vec<f64>,
    pub blo1: Vec<f64>,
    pub ahi1: Vec<f64>,
    pub bhi1: Vec<f64>,
    pub qlo1: Vec<f64>,
    pub qhi1: Vec<f64>,
    pub sclk1: Vec<f64>,
    pub c2: Vec<f64>,
    pub alo2: Vec<f64>,
    pub blo2: Vec<f64>,
    pub ahi2: Vec<f64>,
    pub bhi2: Vec<f64>,
    pub qlo2: Vec<f64>,
    pub qhi2: Vec<f64>,
    pub sclk2: Vec<f64>,
    pub c3: Vec<f64>,
    pub alo3: Vec<f64>,
    pub blo3: Vec<f64>,
    pub ahi3: Vec<f64>,
    pub bhi3: Vec<f64>,
    pub qlo3: Vec<f64>,
    pub qhi3: Vec<f64>,
    pub sclk3: Vec<f64>,
}
impl COMPONENT_HYBRIDIII_JOINT_UPPER_NECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COMPONENT_HYBRIDIII_JOINT_UPPER_NECK").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            alo1: t.take("ALO1").and_then(|c| c.into_float()).unwrap_or_default(),
            blo1: t.take("BLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi1: t.take("AHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi1: t.take("BHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo1: t.take("QLO1").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi1: t.take("QHI1").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk1: t.take("SCLK1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            alo2: t.take("ALO2").and_then(|c| c.into_float()).unwrap_or_default(),
            blo2: t.take("BLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi2: t.take("AHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi2: t.take("BHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo2: t.take("QLO2").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi2: t.take("QHI2").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk2: t.take("SCLK2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            alo3: t.take("ALO3").and_then(|c| c.into_float()).unwrap_or_default(),
            blo3: t.take("BLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ahi3: t.take("AHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            bhi3: t.take("BHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            qlo3: t.take("QLO3").and_then(|c| c.into_float()).unwrap_or_default(),
            qhi3: t.take("QHI3").and_then(|c| c.into_float()).unwrap_or_default(),
            sclk3: t.take("SCLK3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_ADAPTIVITY {
    pub dnid: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
}
impl CONSTRAINED_ADAPTIVITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_ADAPTIVITY").unwrap());
        Self {
            dnid: t.take("DNID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_BEAM_IN_SOLID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub bside: Vec<i64>,
    pub ssid: Vec<i64>,
    pub bstyp: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub ncoup: Vec<i64>,
    pub cdir: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f20: Vec<i64>,
    pub axfor: Vec<i64>,
    pub f40_2: Vec<i64>,
    pub pssf: Vec<f64>,
    pub f60: Vec<i64>,
    pub xint: Vec<i64>,
}
impl CONSTRAINED_BEAM_IN_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_BEAM_IN_SOLID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            bside: t.take("BSIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            bstyp: t.take("BSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            ncoup: t.take("NCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            cdir: t.take("CDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            axfor: t.take("AXFOR").and_then(|c| c.into_int()).unwrap_or_default(),
            f40_2: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            xint: t.take("XINT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_BEAM_IN_SOLID_PENALTY {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub bside: Vec<i64>,
    pub ssid: Vec<i64>,
    pub bstyp: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub ncoup: Vec<i64>,
    pub cdir: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f20: Vec<i64>,
    pub axfor: Vec<i64>,
    pub f40_2: Vec<i64>,
    pub pssf: Vec<f64>,
    pub f60: Vec<i64>,
    pub xint: Vec<i64>,
}
impl CONSTRAINED_BEAM_IN_SOLID_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_BEAM_IN_SOLID_PENALTY").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            bside: t.take("BSIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            bstyp: t.take("BSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            ncoup: t.take("NCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            cdir: t.take("CDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            axfor: t.take("AXFOR").and_then(|c| c.into_int()).unwrap_or_default(),
            f40_2: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            xint: t.take("XINT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_BUTT_WELD {
    pub nsid1: Vec<i64>,
    pub nsid2: Vec<i64>,
    pub eppf: Vec<f64>,
    pub sigf: Vec<f64>,
    pub beta: Vec<f64>,
}
impl CONSTRAINED_BUTT_WELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_BUTT_WELD").unwrap());
        Self {
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_COORDINATE {
    pub id: Vec<i64>,
    pub pid: Vec<i64>,
    pub idir: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl CONSTRAINED_COORDINATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_COORDINATE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_COORDINATE_LOCAL {
    pub id: Vec<i64>,
    pub pid: Vec<i64>,
    pub idir: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub cid: Vec<i64>,
}
impl CONSTRAINED_COORDINATE_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_COORDINATE_LOCAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_EULER_IN_EULER {
    pub psid1: Vec<i64>,
    pub psid2: Vec<i64>,
    pub pfac: Vec<f64>,
}
impl CONSTRAINED_EULER_IN_EULER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_EULER_IN_EULER").unwrap());
        Self {
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            psid2: t.take("PSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_EXTRA_NODES_NODE {
    pub pid: Vec<i64>,
    pub nid: Vec<i64>,
    pub iflag: Vec<i64>,
}
impl CONSTRAINED_EXTRA_NODES_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_EXTRA_NODES_NODE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_EXTRA_NODES_SET {
    pub pid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub iflag: Vec<i64>,
}
impl CONSTRAINED_EXTRA_NODES_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_EXTRA_NODES_SET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_FEM_PERI_TIE {
    pub cid: Vec<i64>,
    pub msid: Vec<i64>,
    pub ssid: Vec<i64>,
}
impl CONSTRAINED_FEM_PERI_TIE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_FEM_PERI_TIE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_FEM_PERI_TIE_BREAK {
    pub cid: Vec<i64>,
    pub msid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub ft: Vec<i64>,
    pub fs: Vec<i64>,
}
impl CONSTRAINED_FEM_PERI_TIE_BREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_FEM_PERI_TIE_BREAK").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GENERALIZED_WELD_BUTT {
    pub wid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub filter: Vec<i64>,
    pub window: Vec<i64>,
    pub npr: Vec<i64>,
    pub nprt: Vec<i64>,
    pub tfail: Vec<f64>,
    pub epsf: Vec<f64>,
    pub sigy: Vec<f64>,
    pub beta: Vec<f64>,
    pub l: Vec<f64>,
    pub d: Vec<f64>,
    pub lt: Vec<f64>,
}
impl CONSTRAINED_GENERALIZED_WELD_BUTT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GENERALIZED_WELD_BUTT").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            filter: t.take("FILTER").and_then(|c| c.into_int()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprt: t.take("NPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            lt: t.take("LT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GENERALIZED_WELD_COMBINED {
    pub wid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub filter: Vec<i64>,
    pub window: Vec<f64>,
    pub npr: Vec<i64>,
    pub nprt: Vec<i64>,
}
impl CONSTRAINED_GENERALIZED_WELD_COMBINED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GENERALIZED_WELD_COMBINED").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            filter: t.take("FILTER").and_then(|c| c.into_int()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprt: t.take("NPRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GENERALIZED_WELD_CROSS_FILLET {
    pub wid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub filter: Vec<i64>,
    pub window: Vec<f64>,
    pub npr: Vec<i64>,
    pub nprt: Vec<i64>,
    pub tfail: Vec<f64>,
    pub epsf: Vec<f64>,
    pub sigy: Vec<f64>,
    pub beta: Vec<f64>,
    pub l: Vec<f64>,
    pub w: Vec<f64>,
    pub a: Vec<f64>,
    pub alpha: Vec<f64>,
    pub nodea: Vec<i64>,
    pub nodeb: Vec<i64>,
    pub ncid: Vec<i64>,
}
impl CONSTRAINED_GENERALIZED_WELD_CROSS_FILLET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GENERALIZED_WELD_CROSS_FILLET").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            filter: t.take("FILTER").and_then(|c| c.into_int()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprt: t.take("NPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            nodea: t.take("NODEA").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeb: t.take("NODEB").and_then(|c| c.into_int()).unwrap_or_default(),
            ncid: t.take("NCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GENERALIZED_WELD_FILLET {
    pub wid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub filter: Vec<i64>,
    pub window: Vec<f64>,
    pub npr: Vec<i64>,
    pub nprt: Vec<i64>,
    pub tfail: Vec<f64>,
    pub epsf: Vec<f64>,
    pub sigy: Vec<f64>,
    pub beta: Vec<f64>,
    pub l: Vec<f64>,
    pub w: Vec<f64>,
    pub a: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl CONSTRAINED_GENERALIZED_WELD_FILLET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GENERALIZED_WELD_FILLET").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            filter: t.take("FILTER").and_then(|c| c.into_int()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprt: t.take("NPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GENERALIZED_WELD_SPOT {
    pub wid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub filter: Vec<i64>,
    pub window: Vec<f64>,
    pub npr: Vec<i64>,
    pub nprt: Vec<i64>,
    pub tfail: Vec<f64>,
    pub epsf: Vec<f64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
}
impl CONSTRAINED_GENERALIZED_WELD_SPOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GENERALIZED_WELD_SPOT").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            filter: t.take("FILTER").and_then(|c| c.into_int()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            nprt: t.take("NPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_GLOBAL {
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
    pub dir: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub tol: Vec<f64>,
}
impl CONSTRAINED_GLOBAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_GLOBAL").unwrap());
        Self {
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_IMMERSED_IN_SPG {
    pub spgpid: Vec<i64>,
    pub ipid1: Vec<i64>,
    pub ipid2: Vec<i64>,
    pub ipid3: Vec<i64>,
    pub ipid4: Vec<i64>,
    pub ipid5: Vec<i64>,
    pub ipid6: Vec<i64>,
    pub ipid7: Vec<i64>,
    pub ipid8: Vec<i64>,
}
impl CONSTRAINED_IMMERSED_IN_SPG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_IMMERSED_IN_SPG").unwrap());
        Self {
            spgpid: t.take("SPGPID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid1: t.take("IPID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid2: t.take("IPID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid3: t.take("IPID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid4: t.take("IPID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid5: t.take("IPID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid6: t.take("IPID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid7: t.take("IPID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ipid8: t.take("IPID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_INTERPOLATION {
    pub icid: Vec<i64>,
    pub dnid: Vec<i64>,
    pub ddof: Vec<i64>,
    pub cidd: Vec<i64>,
    pub ityp: Vec<i64>,
    pub idnsw: Vec<i64>,
    pub fgm: Vec<i64>,
    pub inid: Vec<i64>,
    pub idof: Vec<i64>,
    pub twghtx: Vec<f64>,
    pub twghty: Vec<f64>,
    pub twghtz: Vec<f64>,
    pub rwghtx: Vec<f64>,
    pub rwghty: Vec<f64>,
    pub rwghtz: Vec<f64>,
}
impl CONSTRAINED_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_INTERPOLATION").unwrap());
        Self {
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            dnid: t.take("DNID").and_then(|c| c.into_int()).unwrap_or_default(),
            ddof: t.take("DDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            cidd: t.take("CIDD").and_then(|c| c.into_int()).unwrap_or_default(),
            ityp: t.take("ITYP").and_then(|c| c.into_int()).unwrap_or_default(),
            idnsw: t.take("IDNSW").and_then(|c| c.into_int()).unwrap_or_default(),
            fgm: t.take("FGM").and_then(|c| c.into_int()).unwrap_or_default(),
            inid: t.take("INID").and_then(|c| c.into_int()).unwrap_or_default(),
            idof: t.take("IDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            twghtx: t.take("TWGHTX").and_then(|c| c.into_float()).unwrap_or_default(),
            twghty: t.take("TWGHTY").and_then(|c| c.into_float()).unwrap_or_default(),
            twghtz: t.take("TWGHTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghtx: t.take("RWGHTX").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghty: t.take("RWGHTY").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghtz: t.take("RWGHTZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_INTERPOLATION_LOCAL {
    pub icid: Vec<i64>,
    pub dnid: Vec<i64>,
    pub ddof: Vec<i64>,
    pub cidd: Vec<i64>,
    pub ityp: Vec<i64>,
    pub idnsw: Vec<i64>,
    pub fgm: Vec<i64>,
    pub inid: Vec<i64>,
    pub idof: Vec<i64>,
    pub twghtx: Vec<f64>,
    pub twghty: Vec<f64>,
    pub twghtz: Vec<f64>,
    pub rwghtx: Vec<f64>,
    pub rwghty: Vec<f64>,
    pub rwghtz: Vec<f64>,
    pub cidi: Vec<i64>,
}
impl CONSTRAINED_INTERPOLATION_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_INTERPOLATION_LOCAL").unwrap());
        Self {
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            dnid: t.take("DNID").and_then(|c| c.into_int()).unwrap_or_default(),
            ddof: t.take("DDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            cidd: t.take("CIDD").and_then(|c| c.into_int()).unwrap_or_default(),
            ityp: t.take("ITYP").and_then(|c| c.into_int()).unwrap_or_default(),
            idnsw: t.take("IDNSW").and_then(|c| c.into_int()).unwrap_or_default(),
            fgm: t.take("FGM").and_then(|c| c.into_int()).unwrap_or_default(),
            inid: t.take("INID").and_then(|c| c.into_int()).unwrap_or_default(),
            idof: t.take("IDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            twghtx: t.take("TWGHTX").and_then(|c| c.into_float()).unwrap_or_default(),
            twghty: t.take("TWGHTY").and_then(|c| c.into_float()).unwrap_or_default(),
            twghtz: t.take("TWGHTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghtx: t.take("RWGHTX").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghty: t.take("RWGHTY").and_then(|c| c.into_float()).unwrap_or_default(),
            rwghtz: t.take("RWGHTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            cidi: t.take("CIDI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_INTERPOLATION_SPOTWELD {
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub nsid: Vec<i64>,
    pub thick: Vec<f64>,
    pub r: Vec<f64>,
    pub stiff: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub model: Vec<i64>,
    pub rn: Vec<f64>,
    pub rs: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcf: Vec<i64>,
    pub lcupf: Vec<i64>,
    pub lcupr: Vec<i64>,
    pub dens: Vec<f64>,
    pub intp: Vec<i64>,
    pub upfn: Vec<f64>,
    pub upfs: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub uprn: Vec<f64>,
    pub uprs: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub beta3: Vec<f64>,
    pub mrn: Vec<f64>,
    pub mrs: Vec<f64>,
}
impl CONSTRAINED_INTERPOLATION_SPOTWELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_INTERPOLATION_SPOTWELD").unwrap());
        Self {
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupf: t.take("LCUPF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupr: t.take("LCUPR").and_then(|c| c.into_int()).unwrap_or_default(),
            dens: t.take("DENS").and_then(|c| c.into_float()).unwrap_or_default(),
            intp: t.take("INTP").and_then(|c| c.into_int()).unwrap_or_default(),
            upfn: t.take("UPFN").and_then(|c| c.into_float()).unwrap_or_default(),
            upfs: t.take("UPFS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            uprn: t.take("UPRN").and_then(|c| c.into_float()).unwrap_or_default(),
            uprs: t.take("UPRS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta3: t.take("BETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            mrn: t.take("MRN").and_then(|c| c.into_float()).unwrap_or_default(),
            mrs: t.take("MRS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_CONSTANT_VELOCITY {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_CONSTANT_VELOCITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_CONSTANT_VELOCITY").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_CONSTANT_VELOCITY {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_CONSTANT_VELOCITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_CONSTANT_VELOCITY").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_CYLINDRICAL {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_CYLINDRICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_CYLINDRICAL").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_GEARS {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_GEARS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_GEARS").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_LOCKING {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_LOCKING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_LOCKING").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_PLANAR {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_PLANAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_PLANAR").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_PULLEY {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_PULLEY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_PULLEY").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_RACK_AND_PINION {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_RACK_AND_PINION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_RACK_AND_PINION").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_REVOLUTE {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_REVOLUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_REVOLUTE").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_ROTATIONAL_MOTOR {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_ROTATIONAL_MOTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_ROTATIONAL_MOTOR").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_SCREW {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_SCREW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_SCREW").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_SPHERICAL {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_SPHERICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_SPHERICAL").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_TRANSLATIONAL {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_TRANSLATIONAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_TRANSLATIONAL").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_TRANSLATIONAL_MOTOR {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_TRANSLATIONAL_MOTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_TRANSLATIONAL_MOTOR").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_COOR_UNIVERSAL {
    pub rbid_a: Vec<i64>,
    pub rbid_b: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub tmass: Vec<f64>,
    pub rmass: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
    pub x4: Vec<f64>,
    pub y4: Vec<f64>,
    pub z4: Vec<f64>,
    pub x5: Vec<f64>,
    pub y5: Vec<f64>,
    pub z5: Vec<f64>,
    pub x6: Vec<f64>,
    pub y6: Vec<f64>,
    pub z6: Vec<f64>,
}
impl CONSTRAINED_JOINT_COOR_UNIVERSAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_COOR_UNIVERSAL").unwrap());
        Self {
            rbid_a: t.take("RBID_A").and_then(|c| c.into_int()).unwrap_or_default(),
            rbid_b: t.take("RBID_B").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tmass: t.take("TMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            z4: t.take("Z4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            z5: t.take("Z5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            y6: t.take("Y6").and_then(|c| c.into_float()).unwrap_or_default(),
            z6: t.take("Z6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_CYLINDRICAL {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_CYLINDRICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_CYLINDRICAL").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_GEARS {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
    pub h_angle: Vec<f64>,
}
impl CONSTRAINED_JOINT_GEARS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_GEARS").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            h_angle: t.take("H_ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_LOCKING {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_LOCKING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_LOCKING").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_PLANAR {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_PLANAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_PLANAR").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_PULLEY {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_PULLEY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_PULLEY").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_RACK_AND_PINION {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_RACK_AND_PINION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_RACK_AND_PINION").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_REVOLUTE {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_REVOLUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_REVOLUTE").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_ROTATIONAL_MOTOR {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_ROTATIONAL_MOTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_ROTATIONAL_MOTOR").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_SCREW {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_SCREW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_SCREW").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_SPHERICAL {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_SPHERICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_SPHERICAL").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_STIFFNESS_CYLINDRICAL {
    pub jsid: Vec<i64>,
    pub pida: Vec<i64>,
    pub pidb: Vec<i64>,
    pub cida: Vec<i64>,
    pub cidb: Vec<i64>,
    pub jid: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub f: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub dlcidr: Vec<i64>,
    pub dlcidp: Vec<i64>,
    pub dlcidz: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub dlcidt: Vec<i64>,
    pub esr: Vec<f64>,
    pub ffr: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub esz: Vec<f64>,
    pub ffz: Vec<f64>,
    pub rad1: Vec<f64>,
    pub rad2: Vec<f64>,
    pub f_3: Vec<i64>,
    pub psdr: Vec<f64>,
    pub f_2_3: Vec<i64>,
    pub f_3_2: Vec<i64>,
    pub nsdz: Vec<f64>,
    pub psdz: Vec<f64>,
}
impl CONSTRAINED_JOINT_STIFFNESS_CYLINDRICAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_STIFFNESS_CYLINDRICAL").unwrap());
        Self {
            jsid: t.take("JSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cida: t.take("CIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            cidb: t.take("CIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidr: t.take("DLCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidp: t.take("DLCIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidz: t.take("DLCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidt: t.take("DLCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            esr: t.take("ESR").and_then(|c| c.into_float()).unwrap_or_default(),
            ffr: t.take("FFR").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            esz: t.take("ESZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ffz: t.take("FFZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rad1: t.take("RAD1").and_then(|c| c.into_float()).unwrap_or_default(),
            rad2: t.take("RAD2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            psdr: t.take("PSDR").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            nsdz: t.take("NSDZ").and_then(|c| c.into_float()).unwrap_or_default(),
            psdz: t.take("PSDZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_STIFFNESS_FLEXION_TORSION {
    pub jsid: Vec<i64>,
    pub pida: Vec<i64>,
    pub pidb: Vec<i64>,
    pub cida: Vec<i64>,
    pub cidb: Vec<i64>,
    pub jid: Vec<i64>,
    pub lcidal: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcidbt: Vec<i64>,
    pub dlcidal: Vec<i64>,
    pub dlcidg: Vec<i64>,
    pub dlcidbt: Vec<i64>,
    pub esal: Vec<f64>,
    pub fmal: Vec<f64>,
    pub esbt: Vec<f64>,
    pub fmbt: Vec<f64>,
    pub saal: Vec<f64>,
    pub nsabt: Vec<f64>,
    pub psabt: Vec<f64>,
}
impl CONSTRAINED_JOINT_STIFFNESS_FLEXION_TORSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_STIFFNESS_FLEXION-TORSION").unwrap());
        Self {
            jsid: t.take("JSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cida: t.take("CIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            cidb: t.take("CIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidal: t.take("LCIDAL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidbt: t.take("LCIDBT").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidal: t.take("DLCIDAL").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidg: t.take("DLCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidbt: t.take("DLCIDBT").and_then(|c| c.into_int()).unwrap_or_default(),
            esal: t.take("ESAL").and_then(|c| c.into_float()).unwrap_or_default(),
            fmal: t.take("FMAL").and_then(|c| c.into_float()).unwrap_or_default(),
            esbt: t.take("ESBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fmbt: t.take("FMBT").and_then(|c| c.into_float()).unwrap_or_default(),
            saal: t.take("SAAL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsabt: t.take("NSABT").and_then(|c| c.into_float()).unwrap_or_default(),
            psabt: t.take("PSABT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_STIFFNESS_GENERAL {
    pub jsid: Vec<i64>,
    pub pida: Vec<i64>,
    pub pidb: Vec<i64>,
    pub cida: Vec<i64>,
    pub cidb: Vec<i64>,
    pub jid: Vec<i64>,
    pub lcidph: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcidps: Vec<i64>,
    pub dlcidph: Vec<i64>,
    pub dlcidt: Vec<i64>,
    pub dlcidps: Vec<i64>,
    pub esph: Vec<f64>,
    pub fmph: Vec<f64>,
    pub est: Vec<f64>,
    pub fmt: Vec<f64>,
    pub esps: Vec<f64>,
    pub fmps: Vec<f64>,
    pub nsaph: Vec<f64>,
    pub psaph: Vec<f64>,
    pub nsat: Vec<f64>,
    pub psat: Vec<f64>,
    pub nsaps: Vec<f64>,
    pub psaps: Vec<f64>,
}
impl CONSTRAINED_JOINT_STIFFNESS_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_STIFFNESS_GENERAL").unwrap());
        Self {
            jsid: t.take("JSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cida: t.take("CIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            cidb: t.take("CIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidph: t.take("LCIDPH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidps: t.take("LCIDPS").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidph: t.take("DLCIDPH").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidt: t.take("DLCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidps: t.take("DLCIDPS").and_then(|c| c.into_int()).unwrap_or_default(),
            esph: t.take("ESPH").and_then(|c| c.into_float()).unwrap_or_default(),
            fmph: t.take("FMPH").and_then(|c| c.into_float()).unwrap_or_default(),
            est: t.take("EST").and_then(|c| c.into_float()).unwrap_or_default(),
            fmt: t.take("FMT").and_then(|c| c.into_float()).unwrap_or_default(),
            esps: t.take("ESPS").and_then(|c| c.into_float()).unwrap_or_default(),
            fmps: t.take("FMPS").and_then(|c| c.into_float()).unwrap_or_default(),
            nsaph: t.take("NSAPH").and_then(|c| c.into_float()).unwrap_or_default(),
            psaph: t.take("PSAPH").and_then(|c| c.into_float()).unwrap_or_default(),
            nsat: t.take("NSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            psat: t.take("PSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsaps: t.take("NSAPS").and_then(|c| c.into_float()).unwrap_or_default(),
            psaps: t.take("PSAPS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_STIFFNESS_GENERALIZED {
    pub jsid: Vec<i64>,
    pub pida: Vec<i64>,
    pub pidb: Vec<i64>,
    pub cida: Vec<i64>,
    pub cidb: Vec<i64>,
    pub jid: Vec<i64>,
    pub lcidph: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcidps: Vec<i64>,
    pub dlcidph: Vec<i64>,
    pub dlcidt: Vec<i64>,
    pub dlcidps: Vec<i64>,
    pub esph: Vec<f64>,
    pub fmph: Vec<f64>,
    pub est: Vec<f64>,
    pub fmt: Vec<f64>,
    pub esps: Vec<f64>,
    pub fmps: Vec<f64>,
    pub nsaph: Vec<f64>,
    pub psaph: Vec<f64>,
    pub nsat: Vec<f64>,
    pub psat: Vec<f64>,
    pub nsaps: Vec<f64>,
    pub psaps: Vec<f64>,
}
impl CONSTRAINED_JOINT_STIFFNESS_GENERALIZED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_STIFFNESS_GENERALIZED").unwrap());
        Self {
            jsid: t.take("JSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cida: t.take("CIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            cidb: t.take("CIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidph: t.take("LCIDPH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidps: t.take("LCIDPS").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidph: t.take("DLCIDPH").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidt: t.take("DLCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidps: t.take("DLCIDPS").and_then(|c| c.into_int()).unwrap_or_default(),
            esph: t.take("ESPH").and_then(|c| c.into_float()).unwrap_or_default(),
            fmph: t.take("FMPH").and_then(|c| c.into_float()).unwrap_or_default(),
            est: t.take("EST").and_then(|c| c.into_float()).unwrap_or_default(),
            fmt: t.take("FMT").and_then(|c| c.into_float()).unwrap_or_default(),
            esps: t.take("ESPS").and_then(|c| c.into_float()).unwrap_or_default(),
            fmps: t.take("FMPS").and_then(|c| c.into_float()).unwrap_or_default(),
            nsaph: t.take("NSAPH").and_then(|c| c.into_float()).unwrap_or_default(),
            psaph: t.take("PSAPH").and_then(|c| c.into_float()).unwrap_or_default(),
            nsat: t.take("NSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            psat: t.take("PSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsaps: t.take("NSAPS").and_then(|c| c.into_float()).unwrap_or_default(),
            psaps: t.take("PSAPS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_STIFFNESS_TRANSLATIONAL {
    pub jsid: Vec<i64>,
    pub pida: Vec<i64>,
    pub pidb: Vec<i64>,
    pub cida: Vec<i64>,
    pub cidb: Vec<i64>,
    pub jid: Vec<i64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub dlcidx: Vec<i64>,
    pub dlcidy: Vec<i64>,
    pub dlcidz: Vec<i64>,
    pub esx: Vec<f64>,
    pub ffx: Vec<f64>,
    pub esy: Vec<f64>,
    pub ffy: Vec<f64>,
    pub esz: Vec<f64>,
    pub ffz: Vec<f64>,
    pub nsdx: Vec<f64>,
    pub psdx: Vec<f64>,
    pub nsdy: Vec<f64>,
    pub psdy: Vec<f64>,
    pub nsdz: Vec<f64>,
    pub psdz: Vec<f64>,
}
impl CONSTRAINED_JOINT_STIFFNESS_TRANSLATIONAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_STIFFNESS_TRANSLATIONAL").unwrap());
        Self {
            jsid: t.take("JSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pida: t.take("PIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cida: t.take("CIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            cidb: t.take("CIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidx: t.take("DLCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidy: t.take("DLCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidz: t.take("DLCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            esx: t.take("ESX").and_then(|c| c.into_float()).unwrap_or_default(),
            ffx: t.take("FFX").and_then(|c| c.into_float()).unwrap_or_default(),
            esy: t.take("ESY").and_then(|c| c.into_float()).unwrap_or_default(),
            ffy: t.take("FFY").and_then(|c| c.into_float()).unwrap_or_default(),
            esz: t.take("ESZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ffz: t.take("FFZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nsdx: t.take("NSDX").and_then(|c| c.into_float()).unwrap_or_default(),
            psdx: t.take("PSDX").and_then(|c| c.into_float()).unwrap_or_default(),
            nsdy: t.take("NSDY").and_then(|c| c.into_float()).unwrap_or_default(),
            psdy: t.take("PSDY").and_then(|c| c.into_float()).unwrap_or_default(),
            nsdz: t.take("NSDZ").and_then(|c| c.into_float()).unwrap_or_default(),
            psdz: t.take("PSDZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_TRANSLATIONAL {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_TRANSLATIONAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_TRANSLATIONAL").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_TRANSLATIONAL_MOTOR {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
    pub parm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub type_: Vec<i64>,
    pub r1: Vec<f64>,
}
impl CONSTRAINED_JOINT_TRANSLATIONAL_MOTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_TRANSLATIONAL_MOTOR").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_UNIVERSAL {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub rps: Vec<f64>,
    pub damp: Vec<f64>,
}
impl CONSTRAINED_JOINT_UNIVERSAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_UNIVERSAL").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            rps: t.take("RPS").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_JOINT_USER_FORCE {
    pub fid: Vec<i64>,
    pub jid: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub const1: Vec<i64>,
    pub const2: Vec<i64>,
    pub const3: Vec<i64>,
    pub const4: Vec<i64>,
    pub const5: Vec<i64>,
    pub const6: Vec<i64>,
    pub const7: Vec<i64>,
    pub const8: Vec<i64>,
}
impl CONSTRAINED_JOINT_USER_FORCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_JOINT_USER_FORCE").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            jid: t.take("JID").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            const1: t.take("CONST1").and_then(|c| c.into_int()).unwrap_or_default(),
            const2: t.take("CONST2").and_then(|c| c.into_int()).unwrap_or_default(),
            const3: t.take("CONST3").and_then(|c| c.into_int()).unwrap_or_default(),
            const4: t.take("CONST4").and_then(|c| c.into_int()).unwrap_or_default(),
            const5: t.take("CONST5").and_then(|c| c.into_int()).unwrap_or_default(),
            const6: t.take("CONST6").and_then(|c| c.into_int()).unwrap_or_default(),
            const7: t.take("CONST7").and_then(|c| c.into_int()).unwrap_or_default(),
            const8: t.take("CONST8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LAGRANGE_IN_SOLID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub lstrsid: Vec<i64>,
    pub alesid: Vec<i64>,
    pub lstrstyp: Vec<i64>,
    pub alestyp: Vec<i64>,
    pub nquad: Vec<i64>,
    pub ctype: Vec<i64>,
    pub direc: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub frcmin: Vec<f64>,
    pub norm: Vec<i64>,
    pub normtyp: Vec<i64>,
    pub damp: Vec<f64>,
    pub k: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub ileak: Vec<i64>,
    pub pleak: Vec<f64>,
    pub lcidpor: Vec<i64>,
    pub nvent: Vec<i64>,
    pub blockage: Vec<i64>,
    pub iboxid: Vec<i64>,
    pub ipenchk: Vec<i64>,
    pub intforc: Vec<i64>,
    pub ialesof: Vec<i64>,
    pub lagmul: Vec<f64>,
    pub pfacmm: Vec<i64>,
    pub thkf: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub a2: Vec<f64>,
    pub b2: Vec<f64>,
    pub a3: Vec<f64>,
    pub b3: Vec<f64>,
    pub f60: Vec<i64>,
    pub poreini: Vec<f64>,
    pub ventsid: Vec<i64>,
    pub ventyp: Vec<i64>,
    pub vtcoef: Vec<i64>,
    pub poppres: Vec<f64>,
    pub coeflc: Vec<i64>,
}
impl CONSTRAINED_LAGRANGE_IN_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LAGRANGE_IN_SOLID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            lstrsid: t.take("LSTRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            lstrstyp: t.take("LSTRSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            alestyp: t.take("ALESTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            direc: t.take("DIREC").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            normtyp: t.take("NORMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ileak: t.take("ILEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            pleak: t.take("PLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidpor: t.take("LCIDPOR").and_then(|c| c.into_int()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            blockage: t.take("BLOCKAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            iboxid: t.take("IBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipenchk: t.take("IPENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            intforc: t.take("INTFORC").and_then(|c| c.into_int()).unwrap_or_default(),
            ialesof: t.take("IALESOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lagmul: t.take("LAGMUL").and_then(|c| c.into_float()).unwrap_or_default(),
            pfacmm: t.take("PFACMM").and_then(|c| c.into_int()).unwrap_or_default(),
            thkf: t.take("THKF").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            poreini: t.take("POREINI").and_then(|c| c.into_float()).unwrap_or_default(),
            ventsid: t.take("VENTSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ventyp: t.take("VENTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vtcoef: t.take("VTCOEF").and_then(|c| c.into_int()).unwrap_or_default(),
            poppres: t.take("POPPRES").and_then(|c| c.into_float()).unwrap_or_default(),
            coeflc: t.take("COEFLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LAGRANGE_IN_SOLID_EDGE {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub nquad: Vec<i64>,
    pub ctype: Vec<i64>,
    pub direc: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub frcmin: Vec<f64>,
    pub norm: Vec<i64>,
    pub normtyp: Vec<i64>,
    pub damp: Vec<f64>,
    pub cq: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub ileak: Vec<i64>,
    pub pleak: Vec<f64>,
    pub lcidpor: Vec<i64>,
    pub nvent: Vec<i64>,
    pub blockage: Vec<i64>,
    pub iboxid: Vec<i64>,
    pub ipenchk: Vec<i64>,
    pub intforc: Vec<i64>,
    pub ialesof: Vec<i64>,
    pub lagmul: Vec<f64>,
    pub pfacmm: Vec<i64>,
    pub thkf: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub a2: Vec<f64>,
    pub b2: Vec<f64>,
    pub a3: Vec<f64>,
    pub b3: Vec<f64>,
    pub ventsid: Vec<i64>,
    pub ventyp: Vec<i64>,
    pub vtcoef: Vec<i64>,
    pub poppres: Vec<f64>,
    pub coeflc: Vec<i64>,
}
impl CONSTRAINED_LAGRANGE_IN_SOLID_EDGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LAGRANGE_IN_SOLID_EDGE").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            direc: t.take("DIREC").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            normtyp: t.take("NORMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ileak: t.take("ILEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            pleak: t.take("PLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidpor: t.take("LCIDPOR").and_then(|c| c.into_int()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            blockage: t.take("BLOCKAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            iboxid: t.take("IBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipenchk: t.take("IPENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            intforc: t.take("INTFORC").and_then(|c| c.into_int()).unwrap_or_default(),
            ialesof: t.take("IALESOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lagmul: t.take("LAGMUL").and_then(|c| c.into_float()).unwrap_or_default(),
            pfacmm: t.take("PFACMM").and_then(|c| c.into_int()).unwrap_or_default(),
            thkf: t.take("THKF").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            ventsid: t.take("VENTSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ventyp: t.take("VENTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vtcoef: t.take("VTCOEF").and_then(|c| c.into_int()).unwrap_or_default(),
            poppres: t.take("POPPRES").and_then(|c| c.into_float()).unwrap_or_default(),
            coeflc: t.take("COEFLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LAGRANGE_IN_SOLID_EDGES {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub nquad: Vec<i64>,
    pub ctype: Vec<i64>,
    pub direc: Vec<i64>,
    pub mcoup: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub frcmin: Vec<f64>,
    pub norm: Vec<i64>,
    pub normtyp: Vec<i64>,
    pub damp: Vec<f64>,
    pub cq: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub ileak: Vec<i64>,
    pub pleak: Vec<f64>,
    pub lcidpor: Vec<i64>,
    pub nvent: Vec<i64>,
    pub blockage: Vec<i64>,
    pub iboxid: Vec<i64>,
    pub ipenchk: Vec<i64>,
    pub intforc: Vec<i64>,
    pub ialesof: Vec<i64>,
    pub lagmul: Vec<f64>,
    pub pfacmm: Vec<i64>,
    pub thkf: Vec<f64>,
}
impl CONSTRAINED_LAGRANGE_IN_SOLID_EDGES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LAGRANGE_IN_SOLID_EDGES").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            direc: t.take("DIREC").and_then(|c| c.into_int()).unwrap_or_default(),
            mcoup: t.take("MCOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            normtyp: t.take("NORMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ileak: t.take("ILEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            pleak: t.take("PLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidpor: t.take("LCIDPOR").and_then(|c| c.into_int()).unwrap_or_default(),
            nvent: t.take("NVENT").and_then(|c| c.into_int()).unwrap_or_default(),
            blockage: t.take("BLOCKAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            iboxid: t.take("IBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipenchk: t.take("IPENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            intforc: t.take("INTFORC").and_then(|c| c.into_int()).unwrap_or_default(),
            ialesof: t.take("IALESOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lagmul: t.take("LAGMUL").and_then(|c| c.into_float()).unwrap_or_default(),
            pfacmm: t.take("PFACMM").and_then(|c| c.into_int()).unwrap_or_default(),
            thkf: t.take("THKF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LINEAR_GLOBAL {
    pub licd: Vec<i64>,
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub coef: Vec<f64>,
}
impl CONSTRAINED_LINEAR_GLOBAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LINEAR_GLOBAL").unwrap());
        Self {
            licd: t.take("LICD").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            coef: t.take("COEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LINEAR_LOCAL {
    pub id: Vec<i64>,
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub cid: Vec<i64>,
    pub coef: Vec<f64>,
}
impl CONSTRAINED_LINEAR_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LINEAR_LOCAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            coef: t.take("COEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_LOCAL {
    pub id: Vec<i64>,
    pub heading: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
    pub dir: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub cid: Vec<i64>,
    pub tol: Vec<f64>,
}
impl CONSTRAINED_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_LOCAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_MULTIPLE_GLOBAL {
    pub id: Vec<i64>,
    pub nmp: Vec<i64>,
    pub nid: Vec<i64>,
    pub dir: Vec<i64>,
    pub coef: Vec<f64>,
}
impl CONSTRAINED_MULTIPLE_GLOBAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_MULTIPLE_GLOBAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmp: t.take("NMP").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            coef: t.take("COEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGIDBODY {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGIDBODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGIDBODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_INERTIA {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_INERTIA").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_INERTIA_OVERRIDE {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
    pub icnt: Vec<i64>,
    pub ibag: Vec<i64>,
    pub ipsm: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_INERTIA_OVERRIDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_INERTIA_OVERRIDE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
            icnt: t.take("ICNT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibag: t.take("IBAG").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsm: t.take("IPSM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_INERTIA_SPC {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_INERTIA_SPC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_INERTIA_SPC").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_OVERRIDE {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub icnt: Vec<i64>,
    pub ibag: Vec<i64>,
    pub ipsm: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_OVERRIDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_OVERRIDE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            icnt: t.take("ICNT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibag: t.take("IBAG").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsm: t.take("IPSM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_SPC {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_SPC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_SPC").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_MASTER {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_MASTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_MASTER").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_OVERRIDE {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid2: Vec<i64>,
    pub icnt: Vec<i64>,
    pub ibag: Vec<i64>,
    pub ipsm: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_OVERRIDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_SPC_INERTIA_OVERRIDE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
            icnt: t.take("ICNT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibag: t.take("IBAG").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsm: t.take("IPSM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODAL_RIGID_BODY_SPC_OVERRIDE {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub pnode: Vec<i64>,
    pub iprt: Vec<i64>,
    pub drflag: Vec<i64>,
    pub rrflag: Vec<i64>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub icnt: Vec<i64>,
    pub ibag: Vec<i64>,
    pub ipsm: Vec<i64>,
}
impl CONSTRAINED_NODAL_RIGID_BODY_SPC_OVERRIDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODAL_RIGID_BODY_SPC_OVERRIDE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnode: t.take("PNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            iprt: t.take("IPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            drflag: t.take("DRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rrflag: t.take("RRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            icnt: t.take("ICNT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibag: t.take("IBAG").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsm: t.take("IPSM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODE_INTERPOLATION {
    pub nid: Vec<i64>,
    pub numcn: Vec<i64>,
    pub cn1: Vec<i64>,
    pub w1: Vec<f64>,
    pub cn2: Vec<i64>,
    pub w2: Vec<f64>,
    pub cn3: Vec<i64>,
    pub w3: Vec<f64>,
    pub cn4: Vec<i64>,
    pub w4: Vec<f64>,
}
impl CONSTRAINED_NODE_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODE_INTERPOLATION").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            numcn: t.take("NUMCN").and_then(|c| c.into_int()).unwrap_or_default(),
            cn1: t.take("CN1").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            cn2: t.take("CN2").and_then(|c| c.into_int()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            cn3: t.take("CN3").and_then(|c| c.into_int()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            cn4: t.take("CN4").and_then(|c| c.into_int()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODE_SET {
    pub cnsid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub dof: Vec<i64>,
    pub tf: Vec<f64>,
}
impl CONSTRAINED_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODE_SET").unwrap());
        Self {
            cnsid: t.take("CNSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODE_TO_NURBS_PATCH {
    pub patchid: Vec<i64>,
    pub nid: Vec<i64>,
    pub con: Vec<String>,
    pub cid: Vec<i64>,
    pub sf: Vec<f64>,
    pub dbflg: Vec<i64>,
}
impl CONSTRAINED_NODE_TO_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODE_TO_NURBS_PATCH").unwrap());
        Self {
            patchid: t.take("PATCHID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            con: t.take("CON").and_then(|c| c.into_str()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            dbflg: t.take("DBFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_NODE_TO_NURBS_PATCH_SET {
    pub patchid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub con: Vec<String>,
    pub cid: Vec<i64>,
    pub sf: Vec<f64>,
    pub dbflg: Vec<i64>,
}
impl CONSTRAINED_NODE_TO_NURBS_PATCH_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_NODE_TO_NURBS_PATCH_SET").unwrap());
        Self {
            patchid: t.take("PATCHID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            con: t.take("CON").and_then(|c| c.into_str()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            dbflg: t.take("DBFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_POINTS {
    pub cid: Vec<i64>,
    pub eid1: Vec<i64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub eid2: Vec<i64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub psf: Vec<f64>,
    pub faila: Vec<f64>,
    pub fails: Vec<f64>,
    pub failm: Vec<f64>,
}
impl CONSTRAINED_POINTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_POINTS").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            psf: t.take("PSF").and_then(|c| c.into_float()).unwrap_or_default(),
            faila: t.take("FAILA").and_then(|c| c.into_float()).unwrap_or_default(),
            fails: t.take("FAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            failm: t.take("FAILM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_RIGID_BODIES {
    pub pidl: Vec<i64>,
    pub pidc: Vec<i64>,
    pub iflag: Vec<i64>,
}
impl CONSTRAINED_RIGID_BODIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_RIGID_BODIES").unwrap());
        Self {
            pidl: t.take("PIDL").and_then(|c| c.into_int()).unwrap_or_default(),
            pidc: t.take("PIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_RIGID_BODIES_SET {
    pub pidl: Vec<i64>,
    pub pidc: Vec<i64>,
    pub iflag: Vec<i64>,
}
impl CONSTRAINED_RIGID_BODIES_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_RIGID_BODIES_SET").unwrap());
        Self {
            pidl: t.take("PIDL").and_then(|c| c.into_int()).unwrap_or_default(),
            pidc: t.take("PIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_RIGID_BODY_INSERT {
    pub id: Vec<i64>,
    pub pidl: Vec<i64>,
    pub pidc: Vec<i64>,
    pub coordid: Vec<i64>,
    pub idir: Vec<i64>,
    pub mflag: Vec<i64>,
    pub mcid: Vec<i64>,
    pub deathm: Vec<f64>,
    pub partb: Vec<i64>,
    pub deathb: Vec<f64>,
}
impl CONSTRAINED_RIGID_BODY_INSERT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_RIGID_BODY_INSERT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pidl: t.take("PIDL").and_then(|c| c.into_int()).unwrap_or_default(),
            pidc: t.take("PIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            coordid: t.take("COORDID").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mflag: t.take("MFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            deathm: t.take("DEATHM").and_then(|c| c.into_float()).unwrap_or_default(),
            partb: t.take("PARTB").and_then(|c| c.into_int()).unwrap_or_default(),
            deathb: t.take("DEATHB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_RIGID_BODY_STOPPERS {
    pub pid: Vec<i64>,
    pub lcmax: Vec<i64>,
    pub lcmin: Vec<i64>,
    pub psidmx: Vec<i64>,
    pub psidmn: Vec<i64>,
    pub lcvmnx: Vec<i64>,
    pub dir: Vec<i64>,
    pub vid: Vec<i64>,
    pub tb: Vec<f64>,
    pub td: Vec<f64>,
    pub unused: Vec<f64>,
    pub stiff: Vec<f64>,
}
impl CONSTRAINED_RIGID_BODY_STOPPERS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_RIGID_BODY_STOPPERS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmax: t.take("LCMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmin: t.take("LCMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            psidmx: t.take("PSIDMX").and_then(|c| c.into_int()).unwrap_or_default(),
            psidmn: t.take("PSIDMN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvmnx: t.take("LCVMNX").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            td: t.take("TD").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_RIVET {
    pub rid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub tf: Vec<f64>,
}
impl CONSTRAINED_RIVET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_RIVET").unwrap());
        Self {
            rid: t.take("RID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SHELL_IN_SOLID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub shsid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub shstyp: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub pssf: Vec<f64>,
}
impl CONSTRAINED_SHELL_IN_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SHELL_IN_SOLID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            shsid: t.take("SHSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            shstyp: t.take("SHSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SHELL_IN_SOLID_PENALTY {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub shsid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub shstyp: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub pssf: Vec<f64>,
}
impl CONSTRAINED_SHELL_IN_SOLID_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SHELL_IN_SOLID_PENALTY").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            shsid: t.take("SHSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            shstyp: t.take("SHSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SHELL_TO_SOLID {
    pub nid: Vec<i64>,
    pub nsid: Vec<i64>,
}
impl CONSTRAINED_SHELL_TO_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SHELL_TO_SOLID").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub pid: Vec<i64>,
    pub acu: Vec<f64>,
    pub bcu: Vec<f64>,
    pub lccu: Vec<i64>,
    pub astiffs: Vec<f64>,
    pub bstiffs: Vec<f64>,
    pub astiffb: Vec<f64>,
    pub zref: Vec<f64>,
    pub kbcon: Vec<f64>,
    pub kbcu: Vec<f64>,
    pub kbsx: Vec<f64>,
    pub kbsy: Vec<f64>,
    pub kbsz: Vec<f64>,
    pub bstfac: Vec<f64>,
    pub bhyper: Vec<f64>,
    pub blc: Vec<i64>,
    pub kvcon: Vec<f64>,
    pub kvcu: Vec<f64>,
    pub kvsx: Vec<f64>,
    pub kvsy: Vec<f64>,
    pub kvsz: Vec<f64>,
    pub vstfac: Vec<f64>,
    pub vhyper: Vec<f64>,
    pub vlc: Vec<i64>,
    pub khcon: Vec<f64>,
    pub khcu: Vec<f64>,
    pub khsx: Vec<f64>,
    pub khsy: Vec<f64>,
    pub khsz: Vec<f64>,
    pub hstfac: Vec<f64>,
    pub hhyper: Vec<f64>,
    pub hlc: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            acu: t.take("ACU").and_then(|c| c.into_float()).unwrap_or_default(),
            bcu: t.take("BCU").and_then(|c| c.into_float()).unwrap_or_default(),
            lccu: t.take("LCCU").and_then(|c| c.into_int()).unwrap_or_default(),
            astiffs: t.take("ASTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            bstiffs: t.take("BSTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            astiffb: t.take("ASTIFFB").and_then(|c| c.into_float()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcon: t.take("KBCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcu: t.take("KBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsx: t.take("KBSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsy: t.take("KBSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsz: t.take("KBSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bstfac: t.take("BSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bhyper: t.take("BHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            kvcon: t.take("KVCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kvcu: t.take("KVCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsx: t.take("KVSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsy: t.take("KVSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsz: t.take("KVSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vstfac: t.take("VSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            vhyper: t.take("VHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            khcon: t.take("KHCON").and_then(|c| c.into_float()).unwrap_or_default(),
            khcu: t.take("KHCU").and_then(|c| c.into_float()).unwrap_or_default(),
            khsx: t.take("KHSX").and_then(|c| c.into_float()).unwrap_or_default(),
            khsy: t.take("KHSY").and_then(|c| c.into_float()).unwrap_or_default(),
            khsz: t.take("KHSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            hstfac: t.take("HSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            hhyper: t.take("HHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE_CONSTANTS {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub damp: Vec<f64>,
    pub local: Vec<i64>,
    pub pid: Vec<i64>,
    pub acu: Vec<f64>,
    pub bcu: Vec<f64>,
    pub lccu: Vec<i64>,
    pub astiffs: Vec<f64>,
    pub bstiffs: Vec<f64>,
    pub astiffb: Vec<f64>,
    pub zref: Vec<f64>,
    pub kbcon: Vec<f64>,
    pub kbcu: Vec<f64>,
    pub kbsx: Vec<f64>,
    pub kbsy: Vec<f64>,
    pub kbsz: Vec<f64>,
    pub bstfac: Vec<f64>,
    pub bhyper: Vec<f64>,
    pub blc: Vec<i64>,
    pub kvcon: Vec<f64>,
    pub kvcu: Vec<f64>,
    pub kvsx: Vec<f64>,
    pub kvsy: Vec<f64>,
    pub kvsz: Vec<f64>,
    pub vstfac: Vec<f64>,
    pub vhyper: Vec<f64>,
    pub vlc: Vec<i64>,
    pub khcon: Vec<f64>,
    pub khcu: Vec<f64>,
    pub khsx: Vec<f64>,
    pub khsy: Vec<f64>,
    pub khsz: Vec<f64>,
    pub hstfac: Vec<f64>,
    pub hhyper: Vec<f64>,
    pub hlc: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE_CONSTANTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE_CONSTANTS").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            acu: t.take("ACU").and_then(|c| c.into_float()).unwrap_or_default(),
            bcu: t.take("BCU").and_then(|c| c.into_float()).unwrap_or_default(),
            lccu: t.take("LCCU").and_then(|c| c.into_int()).unwrap_or_default(),
            astiffs: t.take("ASTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            bstiffs: t.take("BSTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            astiffb: t.take("ASTIFFB").and_then(|c| c.into_float()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcon: t.take("KBCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcu: t.take("KBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsx: t.take("KBSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsy: t.take("KBSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsz: t.take("KBSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bstfac: t.take("BSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bhyper: t.take("BHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            kvcon: t.take("KVCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kvcu: t.take("KVCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsx: t.take("KVSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsy: t.take("KVSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsz: t.take("KVSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vstfac: t.take("VSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            vhyper: t.take("VHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            khcon: t.take("KHCON").and_then(|c| c.into_float()).unwrap_or_default(),
            khcu: t.take("KHCU").and_then(|c| c.into_float()).unwrap_or_default(),
            khsx: t.take("KHSX").and_then(|c| c.into_float()).unwrap_or_default(),
            khsy: t.take("KHSY").and_then(|c| c.into_float()).unwrap_or_default(),
            khsz: t.take("KHSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            hstfac: t.take("HSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            hhyper: t.take("HHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE_CONSTANTS_SET {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub damp: Vec<f64>,
    pub local: Vec<i64>,
    pub pid: Vec<i64>,
    pub acu: Vec<f64>,
    pub bcu: Vec<f64>,
    pub lccu: Vec<i64>,
    pub astiffs: Vec<f64>,
    pub bstiffs: Vec<f64>,
    pub astiffb: Vec<f64>,
    pub zref: Vec<f64>,
    pub kbcon: Vec<f64>,
    pub kbcu: Vec<f64>,
    pub kbsx: Vec<f64>,
    pub kbsy: Vec<f64>,
    pub kbsz: Vec<f64>,
    pub bstfac: Vec<f64>,
    pub bhyper: Vec<f64>,
    pub blc: Vec<i64>,
    pub kvcon: Vec<f64>,
    pub kvcu: Vec<f64>,
    pub kvsx: Vec<f64>,
    pub kvsy: Vec<f64>,
    pub kvsz: Vec<f64>,
    pub vstfac: Vec<f64>,
    pub vhyper: Vec<f64>,
    pub vlc: Vec<i64>,
    pub khcon: Vec<f64>,
    pub khcu: Vec<f64>,
    pub khsx: Vec<f64>,
    pub khsy: Vec<f64>,
    pub khsz: Vec<f64>,
    pub hstfac: Vec<f64>,
    pub hhyper: Vec<f64>,
    pub hlc: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE_CONSTANTS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE_CONSTANTS_SET").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            acu: t.take("ACU").and_then(|c| c.into_float()).unwrap_or_default(),
            bcu: t.take("BCU").and_then(|c| c.into_float()).unwrap_or_default(),
            lccu: t.take("LCCU").and_then(|c| c.into_int()).unwrap_or_default(),
            astiffs: t.take("ASTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            bstiffs: t.take("BSTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            astiffb: t.take("ASTIFFB").and_then(|c| c.into_float()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcon: t.take("KBCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcu: t.take("KBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsx: t.take("KBSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsy: t.take("KBSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsz: t.take("KBSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bstfac: t.take("BSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bhyper: t.take("BHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            kvcon: t.take("KVCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kvcu: t.take("KVCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsx: t.take("KVSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsy: t.take("KVSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsz: t.take("KVSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vstfac: t.take("VSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            vhyper: t.take("VHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            khcon: t.take("KHCON").and_then(|c| c.into_float()).unwrap_or_default(),
            khcu: t.take("KHCU").and_then(|c| c.into_float()).unwrap_or_default(),
            khsx: t.take("KHSX").and_then(|c| c.into_float()).unwrap_or_default(),
            khsy: t.take("KHSY").and_then(|c| c.into_float()).unwrap_or_default(),
            khsz: t.take("KHSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            hstfac: t.take("HSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            hhyper: t.take("HHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE_CURVES {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub damp: Vec<f64>,
    pub local: Vec<i64>,
    pub pid: Vec<i64>,
    pub zref: Vec<f64>,
    pub blcz: Vec<i64>,
    pub blc: Vec<i64>,
    pub blcsh: Vec<i64>,
    pub blcsv: Vec<i64>,
    pub vlcz: Vec<i64>,
    pub vlc: Vec<i64>,
    pub vlcsh: Vec<i64>,
    pub vlcsv: Vec<i64>,
    pub hlcz: Vec<i64>,
    pub hlc: Vec<i64>,
    pub hlcsh: Vec<i64>,
    pub hlcsv: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE_CURVES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE_CURVES").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            blcz: t.take("BLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            blcsh: t.take("BLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            blcsv: t.take("BLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcz: t.take("VLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcsh: t.take("VLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcsv: t.take("VLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcz: t.take("HLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcsh: t.take("HLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcsv: t.take("HLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE_CURVES_SET {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub damp: Vec<f64>,
    pub local: Vec<i64>,
    pub psid: Vec<i64>,
    pub zref: Vec<f64>,
    pub blcz: Vec<i64>,
    pub blc: Vec<i64>,
    pub blcsh: Vec<i64>,
    pub blcsv: Vec<i64>,
    pub vlcz: Vec<i64>,
    pub vlc: Vec<i64>,
    pub vlcsh: Vec<i64>,
    pub vlcsv: Vec<i64>,
    pub hlcz: Vec<i64>,
    pub hlc: Vec<i64>,
    pub hlcsh: Vec<i64>,
    pub hlcsv: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE_CURVES_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE_CURVES_SET").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            blcz: t.take("BLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            blcsh: t.take("BLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            blcsv: t.take("BLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcz: t.take("VLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcsh: t.take("VLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcsv: t.take("VLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcz: t.take("HLCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcsh: t.take("HLCSH").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcsv: t.take("HLCSV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOIL_PILE_SET {
    pub pbsid: Vec<i64>,
    pub diam: Vec<f64>,
    pub f: Vec<i64>,
    pub pidns: Vec<i64>,
    pub pidnb: Vec<i64>,
    pub error: Vec<i64>,
    pub nring: Vec<i64>,
    pub nringb: Vec<i64>,
    pub pid: Vec<i64>,
    pub acu: Vec<f64>,
    pub bcu: Vec<f64>,
    pub lccu: Vec<i64>,
    pub astiffs: Vec<f64>,
    pub bstiffs: Vec<f64>,
    pub astiffb: Vec<f64>,
    pub zref: Vec<f64>,
    pub kbcon: Vec<f64>,
    pub kbcu: Vec<f64>,
    pub kbsx: Vec<f64>,
    pub kbsy: Vec<f64>,
    pub kbsz: Vec<f64>,
    pub bstfac: Vec<f64>,
    pub bhyper: Vec<f64>,
    pub blc: Vec<i64>,
    pub kvcon: Vec<f64>,
    pub kvcu: Vec<f64>,
    pub kvsx: Vec<f64>,
    pub kvsy: Vec<f64>,
    pub kvsz: Vec<f64>,
    pub vstfac: Vec<f64>,
    pub vhyper: Vec<f64>,
    pub vlc: Vec<i64>,
    pub khcon: Vec<f64>,
    pub khcu: Vec<f64>,
    pub khsx: Vec<f64>,
    pub khsy: Vec<f64>,
    pub khsz: Vec<f64>,
    pub hstfac: Vec<f64>,
    pub hhyper: Vec<f64>,
    pub hlc: Vec<i64>,
}
impl CONSTRAINED_SOIL_PILE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOIL_PILE_SET").unwrap());
        Self {
            pbsid: t.take("PBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            diam: t.take("DIAM").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pidns: t.take("PIDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            pidnb: t.take("PIDNB").and_then(|c| c.into_int()).unwrap_or_default(),
            error: t.take("ERROR").and_then(|c| c.into_int()).unwrap_or_default(),
            nring: t.take("NRING").and_then(|c| c.into_int()).unwrap_or_default(),
            nringb: t.take("NRINGB").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            acu: t.take("ACU").and_then(|c| c.into_float()).unwrap_or_default(),
            bcu: t.take("BCU").and_then(|c| c.into_float()).unwrap_or_default(),
            lccu: t.take("LCCU").and_then(|c| c.into_int()).unwrap_or_default(),
            astiffs: t.take("ASTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            bstiffs: t.take("BSTIFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            astiffb: t.take("ASTIFFB").and_then(|c| c.into_float()).unwrap_or_default(),
            zref: t.take("ZREF").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcon: t.take("KBCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kbcu: t.take("KBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsx: t.take("KBSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsy: t.take("KBSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kbsz: t.take("KBSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bstfac: t.take("BSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bhyper: t.take("BHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            blc: t.take("BLC").and_then(|c| c.into_int()).unwrap_or_default(),
            kvcon: t.take("KVCON").and_then(|c| c.into_float()).unwrap_or_default(),
            kvcu: t.take("KVCU").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsx: t.take("KVSX").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsy: t.take("KVSY").and_then(|c| c.into_float()).unwrap_or_default(),
            kvsz: t.take("KVSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vstfac: t.take("VSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            vhyper: t.take("VHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            vlc: t.take("VLC").and_then(|c| c.into_int()).unwrap_or_default(),
            khcon: t.take("KHCON").and_then(|c| c.into_float()).unwrap_or_default(),
            khcu: t.take("KHCU").and_then(|c| c.into_float()).unwrap_or_default(),
            khsx: t.take("KHSX").and_then(|c| c.into_float()).unwrap_or_default(),
            khsy: t.take("KHSY").and_then(|c| c.into_float()).unwrap_or_default(),
            khsz: t.take("KHSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            hstfac: t.take("HSTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            hhyper: t.take("HHYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            hlc: t.take("HLC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOLID_IN_SOLID {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub ssidb: Vec<i64>,
    pub ssida: Vec<i64>,
    pub bstypb: Vec<i64>,
    pub sstypa: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub pssf: Vec<f64>,
}
impl CONSTRAINED_SOLID_IN_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOLID_IN_SOLID").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            ssidb: t.take("SSIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            ssida: t.take("SSIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            bstypb: t.take("BSTYPB").and_then(|c| c.into_int()).unwrap_or_default(),
            sstypa: t.take("SSTYPA").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SOLID_IN_SOLID_PENALTY {
    pub coupid: Vec<i64>,
    pub title: Vec<String>,
    pub ssidb: Vec<i64>,
    pub ssida: Vec<i64>,
    pub bstypb: Vec<i64>,
    pub sstypa: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub pssf: Vec<f64>,
}
impl CONSTRAINED_SOLID_IN_SOLID_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SOLID_IN_SOLID_PENALTY").unwrap());
        Self {
            coupid: t.take("COUPID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            ssidb: t.take("SSIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            ssida: t.take("SSIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            bstypb: t.take("BSTYPB").and_then(|c| c.into_int()).unwrap_or_default(),
            sstypa: t.take("SSTYPA").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            pssf: t.take("PSSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SPLINE {
    pub splid: Vec<i64>,
    pub dlratio: Vec<f64>,
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
}
impl CONSTRAINED_SPLINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SPLINE").unwrap());
        Self {
            splid: t.take("SPLID").and_then(|c| c.into_int()).unwrap_or_default(),
            dlratio: t.take("DLRATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SPOTWELD {
    pub wid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub tf: Vec<f64>,
    pub ep: Vec<f64>,
}
impl CONSTRAINED_SPOTWELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SPOTWELD").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SPOTWELD_FILTERED_FORCE {
    pub wid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub tf: Vec<f64>,
    pub ep: Vec<f64>,
    pub nf: Vec<i64>,
    pub tw: Vec<f64>,
}
impl CONSTRAINED_SPOTWELD_FILTERED_FORCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SPOTWELD_FILTERED_FORCE").unwrap());
        Self {
            wid: t.take("WID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_int()).unwrap_or_default(),
            tw: t.take("TW").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_SPR2 {
    pub upid: Vec<i64>,
    pub lpid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub thick: Vec<f64>,
    pub d: Vec<f64>,
    pub fn_: Vec<f64>,
    pub ft: Vec<f64>,
    pub dn: Vec<f64>,
    pub dt: Vec<f64>,
    pub xin: Vec<f64>,
    pub xit: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub dens: Vec<f64>,
    pub intp: Vec<i64>,
    pub expn: Vec<f64>,
    pub expt: Vec<f64>,
    pub pidvb: Vec<i64>,
    pub xpid1: Vec<i64>,
    pub xpid2: Vec<i64>,
    pub xpid3: Vec<i64>,
    pub xpid4: Vec<i64>,
}
impl CONSTRAINED_SPR2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_SPR2").unwrap());
        Self {
            upid: t.take("UPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lpid: t.take("LPID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            dn: t.take("DN").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            xin: t.take("XIN").and_then(|c| c.into_float()).unwrap_or_default(),
            xit: t.take("XIT").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            dens: t.take("DENS").and_then(|c| c.into_float()).unwrap_or_default(),
            intp: t.take("INTP").and_then(|c| c.into_int()).unwrap_or_default(),
            expn: t.take("EXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            expt: t.take("EXPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pidvb: t.take("PIDVB").and_then(|c| c.into_int()).unwrap_or_default(),
            xpid1: t.take("XPID1").and_then(|c| c.into_int()).unwrap_or_default(),
            xpid2: t.take("XPID2").and_then(|c| c.into_int()).unwrap_or_default(),
            xpid3: t.take("XPID3").and_then(|c| c.into_int()).unwrap_or_default(),
            xpid4: t.take("XPID4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_TIE_BREAK {
    pub nsid1: Vec<i64>,
    pub nsid2: Vec<i64>,
    pub eppf: Vec<f64>,
}
impl CONSTRAINED_TIE_BREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_TIE-BREAK").unwrap());
        Self {
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_TIED_NODES {
    pub nsid: Vec<i64>,
    pub eppf: Vec<f64>,
    pub etype: Vec<i64>,
}
impl CONSTRAINED_TIED_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_TIED_NODES").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONSTRAINED_TIED_NODES_FAILURE {
    pub nsid: Vec<i64>,
    pub eppf: Vec<f64>,
    pub etype: Vec<i64>,
}
impl CONSTRAINED_TIED_NODES_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONSTRAINED_TIED_NODES_FAILURE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_1D {
    pub nsidr: Vec<i64>,
    pub nsidc: Vec<i64>,
    pub err: Vec<f64>,
    pub sigc: Vec<f64>,
    pub gb: Vec<f64>,
    pub smax: Vec<f64>,
    pub exp: Vec<f64>,
}
impl CONTACT_1D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_1D").unwrap());
        Self {
            nsidr: t.take("NSIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidc: t.take("NSIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            err: t.take("ERR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigc: t.take("SIGC").and_then(|c| c.into_float()).unwrap_or_default(),
            gb: t.take("GB").and_then(|c| c.into_float()).unwrap_or_default(),
            smax: t.take("SMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            exp: t.take("EXP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_NODE_TO_SURFACE_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SINGLE_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SINGLE_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_MORTAR_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SINGLE_SURFACE_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_IN_CONTINUUM_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_SURFACE_TO_SURFACE_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_TIED {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_TIED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_TIED").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_TIED_ONE_WAY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_TIED_ONE_WAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_TIED_ONE_WAY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_TIED_ONE_WAY_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_TIED_ONE_WAY_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_TIED_ONE_WAY_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_AUTOMATIC_TIED_THERMAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
    pub k: Vec<f64>,
    pub rad: Vec<f64>,
    pub h: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub chlm: Vec<f64>,
    pub bc_flag: Vec<i64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub ipf: Vec<i64>,
    pub slide: Vec<i64>,
    pub istiff: Vec<i64>,
    pub tiedgap: Vec<f64>,
    pub igapcl: Vec<i64>,
    pub tietyp: Vec<i64>,
    pub sldsoa: Vec<f64>,
    pub sldsob: Vec<f64>,
    pub tdpen: Vec<f64>,
}
impl CONTACT_2D_AUTOMATIC_TIED_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_AUTOMATIC_TIED_THERMAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            chlm: t.take("CHLM").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flag: t.take("BC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            ipf: t.take("IPF").and_then(|c| c.into_int()).unwrap_or_default(),
            slide: t.take("SLIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedgap: t.take("TIEDGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            igapcl: t.take("IGAPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            tietyp: t.take("TIETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sldsoa: t.take("SLDSOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sldsob: t.take("SLDSOB").and_then(|c| c.into_float()).unwrap_or_default(),
            tdpen: t.take("TDPEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_FORCE_TRANSDUCER {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub sfact: Vec<f64>,
    pub freq: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub f: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub soa: Vec<f64>,
    pub sob: Vec<f64>,
    pub nda: Vec<i64>,
    pub ndb: Vec<i64>,
    pub cof: Vec<i64>,
    pub init: Vec<i64>,
}
impl CONTACT_2D_FORCE_TRANSDUCER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_FORCE_TRANSDUCER").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            sfact: t.take("SFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            soa: t.take("SOA").and_then(|c| c.into_float()).unwrap_or_default(),
            sob: t.take("SOB").and_then(|c| c.into_float()).unwrap_or_default(),
            nda: t.take("NDA").and_then(|c| c.into_int()).unwrap_or_default(),
            ndb: t.take("NDB").and_then(|c| c.into_int()).unwrap_or_default(),
            cof: t.take("COF").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_NODE_TO_SOLID {
    pub sph: Vec<i64>,
    pub solid: Vec<i64>,
    pub tbirth: Vec<i64>,
    pub tdeath: Vec<i64>,
    pub soft: Vec<i64>,
    pub unused: Vec<i64>,
    pub vc: Vec<f64>,
    pub offd: Vec<f64>,
    pub pen: Vec<f64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
}
impl CONTACT_2D_NODE_TO_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_NODE_TO_SOLID").unwrap());
        Self {
            sph: t.take("SPH").and_then(|c| c.into_int()).unwrap_or_default(),
            solid: t.take("SOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("tbirth").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("tdeath").and_then(|c| c.into_int()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            offd: t.take("OFFD").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_NODE_TO_SOLID_TIED {
    pub sph: Vec<i64>,
    pub solid: Vec<i64>,
    pub tbirth: Vec<i64>,
    pub tdeath: Vec<i64>,
    pub soft: Vec<i64>,
    pub unused: Vec<i64>,
    pub vc: Vec<f64>,
    pub offd: Vec<f64>,
    pub pen: Vec<f64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
}
impl CONTACT_2D_NODE_TO_SOLID_TIED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_NODE_TO_SOLID_TIED").unwrap());
        Self {
            sph: t.take("SPH").and_then(|c| c.into_int()).unwrap_or_default(),
            solid: t.take("SOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("tbirth").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("tdeath").and_then(|c| c.into_int()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            offd: t.take("OFFD").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_PENALTY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub ext_pas: Vec<i64>,
    pub theta1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub tol_ig: Vec<f64>,
    pub pen: Vec<f64>,
    pub toloff: Vec<f64>,
    pub frcscl: Vec<f64>,
    pub oneway: Vec<f64>,
}
impl CONTACT_2D_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_PENALTY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            ext_pas: t.take("EXT_PAS").and_then(|c| c.into_int()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol_ig: t.take("TOL_IG").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            toloff: t.take("TOLOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            frcscl: t.take("FRCSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_PENALTY_FRICTION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub ext_pas: Vec<i64>,
    pub theta1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub tol_ig: Vec<f64>,
    pub pen: Vec<f64>,
    pub toloff: Vec<f64>,
    pub frcscl: Vec<f64>,
    pub oneway: Vec<f64>,
    pub fric: Vec<f64>,
    pub fric_l: Vec<f64>,
    pub fric_h: Vec<f64>,
    pub fric_s: Vec<f64>,
}
impl CONTACT_2D_PENALTY_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_PENALTY_FRICTION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            ext_pas: t.take("EXT_PAS").and_then(|c| c.into_int()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol_ig: t.take("TOL_IG").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            toloff: t.take("TOLOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            frcscl: t.take("FRCSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric_l: t.take("FRIC_L").and_then(|c| c.into_float()).unwrap_or_default(),
            fric_h: t.take("FRIC_H").and_then(|c| c.into_float()).unwrap_or_default(),
            fric_s: t.take("FRIC_S").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_SLIDING_ONLY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub ext_pas: Vec<i64>,
    pub theta1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub tol_ig: Vec<f64>,
    pub pen: Vec<f64>,
    pub toloff: Vec<f64>,
    pub frcscl: Vec<f64>,
    pub oneway: Vec<f64>,
}
impl CONTACT_2D_SLIDING_ONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_SLIDING_ONLY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            ext_pas: t.take("EXT_PAS").and_then(|c| c.into_int()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol_ig: t.take("TOL_IG").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            toloff: t.take("TOLOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            frcscl: t.take("FRCSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_SLIDING_VOIDS {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub ext_pas: Vec<i64>,
    pub theta1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub tol_ig: Vec<f64>,
    pub pen: Vec<f64>,
    pub toloff: Vec<f64>,
    pub frcscl: Vec<f64>,
    pub oneway: Vec<f64>,
}
impl CONTACT_2D_SLIDING_VOIDS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_SLIDING_VOIDS").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            ext_pas: t.take("EXT_PAS").and_then(|c| c.into_int()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol_ig: t.take("TOL_IG").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            toloff: t.take("TOLOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            frcscl: t.take("FRCSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_2D_TIED_SLIDING {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub ext_pas: Vec<i64>,
    pub theta1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub tol_ig: Vec<f64>,
    pub pen: Vec<f64>,
    pub toloff: Vec<f64>,
    pub frcscl: Vec<f64>,
    pub oneway: Vec<f64>,
}
impl CONTACT_2D_TIED_SLIDING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_2D_TIED_SLIDING").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            ext_pas: t.take("EXT_PAS").and_then(|c| c.into_int()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol_ig: t.take("TOL_IG").and_then(|c| c.into_float()).unwrap_or_default(),
            pen: t.take("PEN").and_then(|c| c.into_float()).unwrap_or_default(),
            toloff: t.take("TOLOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            frcscl: t.take("FRCSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ADD_WEAR {
    pub cid: Vec<i64>,
    pub wtype: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
}
impl CONTACT_ADD_WEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ADD_WEAR").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            wtype: t.take("WTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AIRBAG_SINGLE_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AIRBAG_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AIRBAG_SINGLE_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_BEAMS_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_BEAMS_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_BEAMS_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_GENERAL {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_GENERAL").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_GENERAL_EDGEONLY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_GENERAL_EDGEONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_GENERAL_EDGEONLY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_GENERAL_INTERIOR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_GENERAL_INTERIOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_GENERAL_INTERIOR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_GENERAL_TIEBREAK {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_GENERAL_TIEBREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_GENERAL_TIEBREAK").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_GENERAL_TIEBREAK_BEAM_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_GENERAL_TIEBREAK_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_GENERAL_TIEBREAK_BEAM_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_NODES_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_NODES_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_NODES_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub fs1_sa: Vec<f64>,
    pub fd1_sa: Vec<f64>,
    pub dc1_sa: Vec<f64>,
    pub vc1_sa: Vec<f64>,
    pub lc1_sa: Vec<i64>,
    pub oacs_sa: Vec<i64>,
    pub lcfsa: Vec<i64>,
    pub lcpsa: Vec<i64>,
    pub fs2_sa: Vec<f64>,
    pub fd2_sa: Vec<f64>,
    pub dc2_sa: Vec<f64>,
    pub vc2_sa: Vec<f64>,
    pub lc2_sa: Vec<i64>,
    pub fs1_sb: Vec<f64>,
    pub fd1_sb: Vec<f64>,
    pub dc1_sb: Vec<f64>,
    pub vc1_sb: Vec<f64>,
    pub lc1_sb: Vec<i64>,
    pub oacs_sb: Vec<i64>,
    pub lcfsb: Vec<i64>,
    pub lcpsb: Vec<i64>,
    pub fs2_sb: Vec<f64>,
    pub fd2_sb: Vec<f64>,
    pub dc2_sb: Vec<f64>,
    pub vc2_sb: Vec<f64>,
    pub lc2_sb: Vec<i64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            fs1_sa: t.take("FS1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sa: t.take("FD1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sa: t.take("DC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sa: t.take("VC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sa: t.take("LC1_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sa: t.take("OACS_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsa: t.take("LCFSA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsa: t.take("LCPSA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sa: t.take("FS2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sa: t.take("FD2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sa: t.take("DC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sa: t.take("VC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sa: t.take("LC2_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs1_sb: t.take("FS1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sb: t.take("FD1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sb: t.take("DC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sb: t.take("VC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sb: t.take("LC1_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sb: t.take("OACS_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsb: t.take("LCFSB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsb: t.take("LCPSB").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sb: t.take("FS2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sb: t.take("FD2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sb: t.take("DC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sb: t.take("VC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sb: t.take("LC2_SB").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_DAMPING {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub dmp_1: Vec<f64>,
    pub dmp_2: Vec<f64>,
    pub dmp_3: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_DAMPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_DAMPING").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_1: t.take("DMP_1").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_2: t.take("DMP_2").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_3: t.take("DMP_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_USER {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nhv: Vec<i64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub offset: Vec<i64>,
    pub nhmat: Vec<i64>,
    pub nhwld: Vec<i64>,
    pub up1: Vec<f64>,
    pub up2: Vec<f64>,
    pub up3: Vec<f64>,
    pub up4: Vec<f64>,
    pub up5: Vec<f64>,
    pub up6: Vec<f64>,
    pub up7: Vec<f64>,
    pub up8: Vec<f64>,
    pub up9: Vec<f64>,
    pub up10: Vec<f64>,
    pub up11: Vec<f64>,
    pub up12: Vec<f64>,
    pub up13: Vec<f64>,
    pub up14: Vec<f64>,
    pub up15: Vec<f64>,
    pub up16: Vec<f64>,
}
impl CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_USER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE_TIEBREAK_USER").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nhmat: t.take("NHMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhwld: t.take("NHWLD").and_then(|c| c.into_int()).unwrap_or_default(),
            up1: t.take("UP1").and_then(|c| c.into_float()).unwrap_or_default(),
            up2: t.take("UP2").and_then(|c| c.into_float()).unwrap_or_default(),
            up3: t.take("UP3").and_then(|c| c.into_float()).unwrap_or_default(),
            up4: t.take("UP4").and_then(|c| c.into_float()).unwrap_or_default(),
            up5: t.take("UP5").and_then(|c| c.into_float()).unwrap_or_default(),
            up6: t.take("UP6").and_then(|c| c.into_float()).unwrap_or_default(),
            up7: t.take("UP7").and_then(|c| c.into_float()).unwrap_or_default(),
            up8: t.take("UP8").and_then(|c| c.into_float()).unwrap_or_default(),
            up9: t.take("UP9").and_then(|c| c.into_float()).unwrap_or_default(),
            up10: t.take("UP10").and_then(|c| c.into_float()).unwrap_or_default(),
            up11: t.take("UP11").and_then(|c| c.into_float()).unwrap_or_default(),
            up12: t.take("UP12").and_then(|c| c.into_float()).unwrap_or_default(),
            up13: t.take("UP13").and_then(|c| c.into_float()).unwrap_or_default(),
            up14: t.take("UP14").and_then(|c| c.into_float()).unwrap_or_default(),
            up15: t.take("UP15").and_then(|c| c.into_float()).unwrap_or_default(),
            up16: t.take("UP16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK_BEAM_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE_TIEBREAK_BEAM_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SINGLE_SURFACE_TIED {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub close: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SINGLE_SURFACE_TIED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SINGLE_SURFACE_TIED").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            close: t.take("CLOSE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_COMPOSITE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub tfail: Vec<f64>,
    pub model: Vec<i64>,
    pub cidmu: Vec<i64>,
    pub cideta: Vec<i64>,
    pub d: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_COMPOSITE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            cidmu: t.take("CIDMU").and_then(|c| c.into_int()).unwrap_or_default(),
            cideta: t.take("CIDETA").and_then(|c| c.into_int()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED_WELD {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub temp: Vec<f64>,
    pub close: Vec<f64>,
    pub hclose: Vec<f64>,
    pub ntprm: Vec<i64>,
    pub nmhis: Vec<i64>,
    pub nstwh: Vec<i64>,
    pub nmtwh: Vec<i64>,
    pub tprm1: Vec<f64>,
    pub tprm2: Vec<f64>,
    pub tprm3: Vec<f64>,
    pub tprm4: Vec<f64>,
    pub tprmi: Vec<f64>,
    pub tprmi_2: Vec<f64>,
    pub tprmi_3: Vec<f64>,
    pub tprmi_4: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED_WELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR_TIED_WELD").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            close: t.take("CLOSE").and_then(|c| c.into_float()).unwrap_or_default(),
            hclose: t.take("HCLOSE").and_then(|c| c.into_float()).unwrap_or_default(),
            ntprm: t.take("NTPRM").and_then(|c| c.into_int()).unwrap_or_default(),
            nmhis: t.take("NMHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            nstwh: t.take("NSTWH").and_then(|c| c.into_int()).unwrap_or_default(),
            nmtwh: t.take("NMTWH").and_then(|c| c.into_int()).unwrap_or_default(),
            tprm1: t.take("TPRM1").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm2: t.take("TPRM2").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm3: t.take("TPRM3").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm4: t.take("TPRM4").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi: t.take("TPRMi").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_2: t.take("TPRMi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_3: t.take("TPRMi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_4: t.take("TPRMi_4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub fs1_sa: Vec<f64>,
    pub fd1_sa: Vec<f64>,
    pub dc1_sa: Vec<f64>,
    pub vc1_sa: Vec<f64>,
    pub lc1_sa: Vec<i64>,
    pub oacs_sa: Vec<i64>,
    pub lcfsa: Vec<i64>,
    pub lcpsa: Vec<i64>,
    pub fs2_sa: Vec<f64>,
    pub fd2_sa: Vec<f64>,
    pub dc2_sa: Vec<f64>,
    pub vc2_sa: Vec<f64>,
    pub lc2_sa: Vec<i64>,
    pub fs1_sb: Vec<f64>,
    pub fd1_sb: Vec<f64>,
    pub dc1_sb: Vec<f64>,
    pub vc1_sb: Vec<f64>,
    pub lc1_sb: Vec<i64>,
    pub oacs_sb: Vec<i64>,
    pub lcfsb: Vec<i64>,
    pub lcpsb: Vec<i64>,
    pub fs2_sb: Vec<f64>,
    pub fd2_sb: Vec<f64>,
    pub dc2_sb: Vec<f64>,
    pub vc2_sb: Vec<f64>,
    pub lc2_sb: Vec<i64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_ORTHO_FRICTION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            fs1_sa: t.take("FS1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sa: t.take("FD1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sa: t.take("DC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sa: t.take("VC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sa: t.take("LC1_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sa: t.take("OACS_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsa: t.take("LCFSA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsa: t.take("LCPSA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sa: t.take("FS2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sa: t.take("FD2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sa: t.take("DC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sa: t.take("VC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sa: t.take("LC2_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs1_sb: t.take("FS1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sb: t.take("FD1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sb: t.take("DC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sb: t.take("VC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sb: t.take("LC1_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sb: t.take("OACS_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsb: t.take("LCFSB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsb: t.take("LCPSB").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sb: t.take("FS2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sb: t.take("FD2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sb: t.take("DC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sb: t.take("VC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sb: t.take("LC2_SB").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_DAMPING {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub dmp_1: Vec<f64>,
    pub dmp_2: Vec<f64>,
    pub dmp_3: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<f64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_DAMPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_DAMPING").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_1: t.take("DMP_1").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_2: t.take("DMP_2").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp_3: t.take("DMP_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub param: Vec<f64>,
    pub eraten: Vec<f64>,
    pub erates: Vec<f64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            param: t.take("PARAM").and_then(|c| c.into_float()).unwrap_or_default(),
            eraten: t.take("ERATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            erates: t.take("ERATES").and_then(|c| c.into_float()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nhv: Vec<i64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub offset: Vec<i64>,
    pub nhmat: Vec<i64>,
    pub nhwld: Vec<i64>,
    pub up1: Vec<f64>,
    pub up2: Vec<f64>,
    pub up3: Vec<f64>,
    pub up4: Vec<f64>,
    pub up5: Vec<f64>,
    pub up6: Vec<f64>,
    pub up7: Vec<f64>,
    pub up8: Vec<f64>,
    pub up9: Vec<f64>,
    pub up10: Vec<f64>,
    pub up11: Vec<f64>,
    pub up12: Vec<f64>,
    pub up13: Vec<f64>,
    pub up14: Vec<f64>,
    pub up15: Vec<f64>,
    pub up16: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nhmat: t.take("NHMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhwld: t.take("NHWLD").and_then(|c| c.into_int()).unwrap_or_default(),
            up1: t.take("UP1").and_then(|c| c.into_float()).unwrap_or_default(),
            up2: t.take("UP2").and_then(|c| c.into_float()).unwrap_or_default(),
            up3: t.take("UP3").and_then(|c| c.into_float()).unwrap_or_default(),
            up4: t.take("UP4").and_then(|c| c.into_float()).unwrap_or_default(),
            up5: t.take("UP5").and_then(|c| c.into_float()).unwrap_or_default(),
            up6: t.take("UP6").and_then(|c| c.into_float()).unwrap_or_default(),
            up7: t.take("UP7").and_then(|c| c.into_float()).unwrap_or_default(),
            up8: t.take("UP8").and_then(|c| c.into_float()).unwrap_or_default(),
            up9: t.take("UP9").and_then(|c| c.into_float()).unwrap_or_default(),
            up10: t.take("UP10").and_then(|c| c.into_float()).unwrap_or_default(),
            up11: t.take("UP11").and_then(|c| c.into_float()).unwrap_or_default(),
            up12: t.take("UP12").and_then(|c| c.into_float()).unwrap_or_default(),
            up13: t.take("UP13").and_then(|c| c.into_float()).unwrap_or_default(),
            up14: t.take("UP14").and_then(|c| c.into_float()).unwrap_or_default(),
            up15: t.take("UP15").and_then(|c| c.into_float()).unwrap_or_default(),
            up16: t.take("UP16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub option: Vec<i64>,
    pub nhv: Vec<i64>,
    pub ct2cn: Vec<f64>,
    pub cn: Vec<f64>,
    pub offset: Vec<i64>,
    pub nhmat: Vec<i64>,
    pub nhwld: Vec<i64>,
    pub up1: Vec<f64>,
    pub up2: Vec<f64>,
    pub up3: Vec<f64>,
    pub up4: Vec<f64>,
    pub up5: Vec<f64>,
    pub up6: Vec<f64>,
    pub up7: Vec<f64>,
    pub up8: Vec<f64>,
    pub up9: Vec<f64>,
    pub up10: Vec<f64>,
    pub up11: Vec<f64>,
    pub up12: Vec<f64>,
    pub up13: Vec<f64>,
    pub up14: Vec<f64>,
    pub up15: Vec<f64>,
    pub up16: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIEBREAK_USER_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ct2cn: t.take("CT2CN").and_then(|c| c.into_float()).unwrap_or_default(),
            cn: t.take("CN").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nhmat: t.take("NHMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhwld: t.take("NHWLD").and_then(|c| c.into_int()).unwrap_or_default(),
            up1: t.take("UP1").and_then(|c| c.into_float()).unwrap_or_default(),
            up2: t.take("UP2").and_then(|c| c.into_float()).unwrap_or_default(),
            up3: t.take("UP3").and_then(|c| c.into_float()).unwrap_or_default(),
            up4: t.take("UP4").and_then(|c| c.into_float()).unwrap_or_default(),
            up5: t.take("UP5").and_then(|c| c.into_float()).unwrap_or_default(),
            up6: t.take("UP6").and_then(|c| c.into_float()).unwrap_or_default(),
            up7: t.take("UP7").and_then(|c| c.into_float()).unwrap_or_default(),
            up8: t.take("UP8").and_then(|c| c.into_float()).unwrap_or_default(),
            up9: t.take("UP9").and_then(|c| c.into_float()).unwrap_or_default(),
            up10: t.take("UP10").and_then(|c| c.into_float()).unwrap_or_default(),
            up11: t.take("UP11").and_then(|c| c.into_float()).unwrap_or_default(),
            up12: t.take("UP12").and_then(|c| c.into_float()).unwrap_or_default(),
            up13: t.take("UP13").and_then(|c| c.into_float()).unwrap_or_default(),
            up14: t.take("UP14").and_then(|c| c.into_float()).unwrap_or_default(),
            up15: t.take("UP15").and_then(|c| c.into_float()).unwrap_or_default(),
            up16: t.take("UP16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIED_WELD {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub temp: Vec<f64>,
    pub close: Vec<f64>,
    pub hclose: Vec<f64>,
    pub ntprm: Vec<i64>,
    pub nmhis: Vec<i64>,
    pub nstwh: Vec<i64>,
    pub nmtwh: Vec<i64>,
    pub tprm1: Vec<f64>,
    pub tprm2: Vec<f64>,
    pub tprm3: Vec<f64>,
    pub tprm4: Vec<f64>,
    pub tprmi: Vec<f64>,
    pub tprmi_2: Vec<f64>,
    pub tprmi_3: Vec<f64>,
    pub tprmi_4: Vec<f64>,
}
impl CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIED_WELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTOMATIC_SURFACE_TO_SURFACE_TIED_WELD").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            close: t.take("CLOSE").and_then(|c| c.into_float()).unwrap_or_default(),
            hclose: t.take("HCLOSE").and_then(|c| c.into_float()).unwrap_or_default(),
            ntprm: t.take("NTPRM").and_then(|c| c.into_int()).unwrap_or_default(),
            nmhis: t.take("NMHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            nstwh: t.take("NSTWH").and_then(|c| c.into_int()).unwrap_or_default(),
            nmtwh: t.take("NMTWH").and_then(|c| c.into_int()).unwrap_or_default(),
            tprm1: t.take("TPRM1").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm2: t.take("TPRM2").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm3: t.take("TPRM3").and_then(|c| c.into_float()).unwrap_or_default(),
            tprm4: t.take("TPRM4").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi: t.take("TPRMi").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_2: t.take("TPRMi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_3: t.take("TPRMi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            tprmi_4: t.take("TPRMi_4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_AUTO_MOVE {
    pub id: Vec<i64>,
    pub cid: Vec<i64>,
    pub vid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub atime: Vec<f64>,
    pub offset: Vec<f64>,
}
impl CONTACT_AUTO_MOVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_AUTO_MOVE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            atime: t.take("ATIME").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_CONSTRAINT_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub kpf: Vec<f64>,
}
impl CONTACT_CONSTRAINT_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_CONSTRAINT_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            kpf: t.take("KPF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_CONSTRAINT_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub kpf: Vec<f64>,
}
impl CONTACT_CONSTRAINT_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_CONSTRAINT_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            kpf: t.take("KPF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_COUPLING {
    pub id: Vec<i64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
}
impl CONTACT_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_COUPLING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_DRAWBEAD {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcidrf: Vec<i64>,
    pub lcidnf: Vec<i64>,
    pub dbdth: Vec<f64>,
    pub dfscl: Vec<f64>,
    pub numint: Vec<i64>,
    pub dbpid: Vec<i64>,
    pub eloff: Vec<i64>,
    pub nbead: Vec<i64>,
    pub point1: Vec<i64>,
    pub point2: Vec<i64>,
    pub width: Vec<f64>,
    pub effhgt: Vec<f64>,
}
impl CONTACT_DRAWBEAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_DRAWBEAD").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidrf: t.take("LCIDRF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidnf: t.take("LCIDNF").and_then(|c| c.into_int()).unwrap_or_default(),
            dbdth: t.take("DBDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfscl: t.take("DFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            dbpid: t.take("DBPID").and_then(|c| c.into_int()).unwrap_or_default(),
            eloff: t.take("ELOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            nbead: t.take("NBEAD").and_then(|c| c.into_int()).unwrap_or_default(),
            point1: t.take("POINT1").and_then(|c| c.into_int()).unwrap_or_default(),
            point2: t.take("POINT2").and_then(|c| c.into_int()).unwrap_or_default(),
            width: t.take("WIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            effhgt: t.take("EFFHGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_DRAWBEAD_BENDING {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcidrf: Vec<i64>,
    pub lcidnf: Vec<i64>,
    pub dbdth: Vec<f64>,
    pub dfscl: Vec<f64>,
    pub numint: Vec<i64>,
    pub dbpid: Vec<i64>,
    pub eloff: Vec<i64>,
    pub nbead: Vec<i64>,
    pub point1: Vec<i64>,
    pub point2: Vec<i64>,
    pub width: Vec<f64>,
    pub effhgt: Vec<f64>,
    pub epm: Vec<f64>,
    pub epscal: Vec<f64>,
    pub ending: Vec<f64>,
    pub f: Vec<i64>,
}
impl CONTACT_DRAWBEAD_BENDING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_DRAWBEAD_BENDING").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidrf: t.take("LCIDRF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidnf: t.take("LCIDNF").and_then(|c| c.into_int()).unwrap_or_default(),
            dbdth: t.take("DBDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfscl: t.take("DFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            dbpid: t.take("DBPID").and_then(|c| c.into_int()).unwrap_or_default(),
            eloff: t.take("ELOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            nbead: t.take("NBEAD").and_then(|c| c.into_int()).unwrap_or_default(),
            point1: t.take("POINT1").and_then(|c| c.into_int()).unwrap_or_default(),
            point2: t.take("POINT2").and_then(|c| c.into_int()).unwrap_or_default(),
            width: t.take("WIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            effhgt: t.take("EFFHGT").and_then(|c| c.into_float()).unwrap_or_default(),
            epm: t.take("EPM").and_then(|c| c.into_float()).unwrap_or_default(),
            epscal: t.take("EPSCAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ending: t.take("ENDING").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_DRAWBEAD_INITIALIZE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcidrf: Vec<i64>,
    pub lcidnf: Vec<i64>,
    pub dbdth: Vec<f64>,
    pub dfscl: Vec<f64>,
    pub numint: Vec<i64>,
    pub dbpid: Vec<i64>,
    pub eloff: Vec<i64>,
    pub nbead: Vec<i64>,
    pub point1: Vec<i64>,
    pub point2: Vec<i64>,
    pub width: Vec<f64>,
    pub effhgt: Vec<f64>,
    pub lceps: Vec<i64>,
    pub tscale: Vec<f64>,
    pub lceps2: Vec<i64>,
    pub offset: Vec<i64>,
}
impl CONTACT_DRAWBEAD_INITIALIZE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_DRAWBEAD_INITIALIZE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidrf: t.take("LCIDRF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidnf: t.take("LCIDNF").and_then(|c| c.into_int()).unwrap_or_default(),
            dbdth: t.take("DBDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfscl: t.take("DFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            dbpid: t.take("DBPID").and_then(|c| c.into_int()).unwrap_or_default(),
            eloff: t.take("ELOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            nbead: t.take("NBEAD").and_then(|c| c.into_int()).unwrap_or_default(),
            point1: t.take("POINT1").and_then(|c| c.into_int()).unwrap_or_default(),
            point2: t.take("POINT2").and_then(|c| c.into_int()).unwrap_or_default(),
            width: t.take("WIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            effhgt: t.take("EFFHGT").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps: t.take("LCEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps2: t.take("LCEPS2").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ENTITY {
    pub pid: Vec<i64>,
    pub geotyp: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
    pub go: Vec<i64>,
    pub ithk: Vec<i64>,
    pub spr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub inout: Vec<i64>,
    pub g1: Vec<f64>,
    pub g2: Vec<f64>,
    pub g3: Vec<f64>,
    pub g4: Vec<f64>,
    pub g5: Vec<f64>,
    pub g6: Vec<f64>,
    pub g7: Vec<f64>,
}
impl CONTACT_ENTITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ENTITY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            geotyp: t.take("GEOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
            go: t.take("GO").and_then(|c| c.into_int()).unwrap_or_default(),
            ithk: t.take("ITHK").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            g5: t.take("G5").and_then(|c| c.into_float()).unwrap_or_default(),
            g6: t.take("G6").and_then(|c| c.into_float()).unwrap_or_default(),
            g7: t.take("G7").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ERODING_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub isym: Vec<i64>,
    pub erosop: Vec<i64>,
    pub iadj: Vec<i64>,
}
impl CONTACT_ERODING_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ERODING_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            erosop: t.take("EROSOP").and_then(|c| c.into_int()).unwrap_or_default(),
            iadj: t.take("IADJ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ERODING_SINGLE_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub isym: Vec<i64>,
    pub erosop: Vec<i64>,
    pub iadj: Vec<i64>,
}
impl CONTACT_ERODING_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ERODING_SINGLE_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            erosop: t.take("EROSOP").and_then(|c| c.into_int()).unwrap_or_default(),
            iadj: t.take("IADJ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ERODING_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub isym: Vec<i64>,
    pub erosop: Vec<i64>,
    pub iadj: Vec<i64>,
}
impl CONTACT_ERODING_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ERODING_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            erosop: t.take("EROSOP").and_then(|c| c.into_int()).unwrap_or_default(),
            iadj: t.take("IADJ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_EXCLUDE_INTERACTION {
    pub ceid: Vec<i64>,
    pub cid: Vec<i64>,
    pub sid2: Vec<i64>,
    pub sid1: Vec<i64>,
    pub type2: Vec<i64>,
    pub type1: Vec<i64>,
}
impl CONTACT_EXCLUDE_INTERACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_EXCLUDE_INTERACTION").unwrap());
        Self {
            ceid: t.take("CEID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            type2: t.take("TYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            type1: t.take("TYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORCE_TRANSDUCER {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORCE_TRANSDUCER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORCE_TRANSDUCER").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORCE_TRANSDUCER_CONSTRAINT {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORCE_TRANSDUCER_CONSTRAINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORCE_TRANSDUCER_CONSTRAINT").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORCE_TRANSDUCER_PENALTY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORCE_TRANSDUCER_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORCE_TRANSDUCER_PENALTY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_NODES_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_NODES_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_NODES_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub fs1_sa: Vec<f64>,
    pub fd1_sa: Vec<f64>,
    pub dc1_sa: Vec<f64>,
    pub vc1_sa: Vec<f64>,
    pub lc1_sa: Vec<i64>,
    pub oacs_sa: Vec<i64>,
    pub lcfsa: Vec<i64>,
    pub lcpsa: Vec<i64>,
    pub fs2_sa: Vec<f64>,
    pub fd2_sa: Vec<f64>,
    pub dc2_sa: Vec<f64>,
    pub vc2_sa: Vec<f64>,
    pub lc2_sa: Vec<i64>,
    pub fs1_sb: Vec<f64>,
    pub fd1_sb: Vec<f64>,
    pub dc1_sb: Vec<f64>,
    pub vc1_sb: Vec<f64>,
    pub lc1_sb: Vec<i64>,
    pub oacs_sb: Vec<i64>,
    pub lcfsb: Vec<i64>,
    pub lcpsb: Vec<i64>,
    pub fs2_sb: Vec<f64>,
    pub fd2_sb: Vec<f64>,
    pub dc2_sb: Vec<f64>,
    pub vc2_sb: Vec<f64>,
    pub lc2_sb: Vec<i64>,
}
impl CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_ORTHO_FRICTION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            fs1_sa: t.take("FS1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sa: t.take("FD1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sa: t.take("DC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sa: t.take("VC1_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sa: t.take("LC1_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sa: t.take("OACS_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsa: t.take("LCFSA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsa: t.take("LCPSA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sa: t.take("FS2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sa: t.take("FD2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sa: t.take("DC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sa: t.take("VC2_SA").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sa: t.take("LC2_SA").and_then(|c| c.into_int()).unwrap_or_default(),
            fs1_sb: t.take("FS1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd1_sb: t.take("FD1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc1_sb: t.take("DC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc1_sb: t.take("VC1_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_sb: t.take("LC1_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            oacs_sb: t.take("OACS_SB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsb: t.take("LCFSB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsb: t.take("LCPSB").and_then(|c| c.into_int()).unwrap_or_default(),
            fs2_sb: t.take("FS2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            fd2_sb: t.take("FD2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2_sb: t.take("DC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            vc2_sb: t.take("VC2_SB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2_sb: t.take("LC2_SB").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_SURFACE_TO_SURFACE_MORTAR {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_SURFACE_TO_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_SURFACE_TO_SURFACE_MORTAR").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_FORMING_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_FORMING_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_FORMING_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_HEAD {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_HEAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_HEAD").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_FOOT {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_FOOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_FOOT").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_HAND {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_HAND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_HAND").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_LOWER_ARM {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_LOWER_ARM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_LOWER_ARM").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_LOWER_LEG {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_LOWER_LEG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_LOWER_LEG").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_SHOULDER {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_UPPER_ARM {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_UPPER_ARM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_UPPER_ARM").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LEFT_UPPER_LEG {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LEFT_UPPER_LEG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LEFT_UPPER_LEG").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_LOWER_TORSO {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_LOWER_TORSO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_LOWER_TORSO").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_MIDDLE_TORSO {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_MIDDLE_TORSO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_MIDDLE_TORSO").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_NECK {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_NECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_NECK").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_FOOT {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_FOOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_FOOT").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_HAND {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_HAND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_HAND").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_LOWER_ARM {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_LOWER_ARM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_LOWER_ARM").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_LOWER_LEG {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_LOWER_LEG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_LOWER_LEG").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_SHOULDER {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_SHOULDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_SHOULDER").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_UPPER_ARM {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_UPPER_ARM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_UPPER_ARM").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_RIGHT_UPPER_LEG {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_RIGHT_UPPER_LEG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_RIGHT_UPPER_LEG").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GEBOD_UPPER_TORSO {
    pub did: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub sf: Vec<f64>,
    pub df: Vec<f64>,
    pub cf: Vec<f64>,
    pub intord: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub so: Vec<i64>,
}
impl CONTACT_GEBOD_UPPER_TORSO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GEBOD_UPPER_TORSO").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            intord: t.take("INTORD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GUIDED_CABLE {
    pub cid: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub pid: Vec<i64>,
    pub soft: Vec<i64>,
    pub ssfac: Vec<f64>,
    pub fric: Vec<f64>,
}
impl CONTACT_GUIDED_CABLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GUIDED_CABLE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssfac: t.take("SSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_GUIDED_CABLE_SET {
    pub cid: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub psid: Vec<i64>,
    pub soft: Vec<i64>,
    pub ssfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub endtol: Vec<f64>,
}
impl CONTACT_GUIDED_CABLE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_GUIDED_CABLE_SET").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssfac: t.take("SSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            endtol: t.take("ENDTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_INTERIOR {
    pub psid: Vec<i64>,
}
impl CONTACT_INTERIOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_INTERIOR").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NODES_TO_SURFACE_INTERFERENCE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
}
impl CONTACT_NODES_TO_SURFACE_INTERFERENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NODES_TO_SURFACE_INTERFERENCE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NODES_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_NODES_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NODES_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NURBS_TIED_EDGE_TO_EDGE {
    pub cid: Vec<i64>,
    pub title: Vec<String>,
    pub ssid: Vec<i64>,
    pub msid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub cform: Vec<i64>,
}
impl CONTACT_NURBS_TIED_EDGE_TO_EDGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NURBS_TIED_EDGE_TO_EDGE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            cform: t.take("CFORM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NURBS_TIED_EDGE_TO_SURFACE {
    pub cid: Vec<i64>,
    pub title: Vec<String>,
    pub ssid: Vec<i64>,
    pub msid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub cform: Vec<i64>,
}
impl CONTACT_NURBS_TIED_EDGE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NURBS_TIED_EDGE_TO_SURFACE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            cform: t.take("CFORM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_NURBS_TIED_NODES_TO_SURFACE {
    pub cid: Vec<i64>,
    pub title: Vec<String>,
    pub ssid: Vec<i64>,
    pub msid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub cform: Vec<i64>,
}
impl CONTACT_NURBS_TIED_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_NURBS_TIED_NODES_TO_SURFACE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            cform: t.take("CFORM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ONE_WAY_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_ONE_WAY_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ONE_WAY_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ONE_WAY_SURFACE_TO_SURFACE_INTERFERENCE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
}
impl CONTACT_ONE_WAY_SURFACE_TO_SURFACE_INTERFERENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ONE_WAY_SURFACE_TO_SURFACE_INTERFERENCE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_ONE_WAY_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_RIGID_BODY_ONE_WAY_TO_RIGID_BODY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fcm: Vec<i64>,
    pub us: Vec<f64>,
    pub f30: Vec<i64>,
    pub lcdc: Vec<i64>,
    pub dsf: Vec<f64>,
    pub unlcid: Vec<i64>,
}
impl CONTACT_RIGID_BODY_ONE_WAY_TO_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_RIGID_BODY_ONE_WAY_TO_RIGID_BODY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_int()).unwrap_or_default(),
            us: t.take("US").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdc: t.take("LCDC").and_then(|c| c.into_int()).unwrap_or_default(),
            dsf: t.take("DSF").and_then(|c| c.into_float()).unwrap_or_default(),
            unlcid: t.take("UNLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_RIGID_BODY_TWO_WAY_TO_RIGID_BODY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fcm: Vec<i64>,
    pub us: Vec<f64>,
    pub f30: Vec<i64>,
    pub lcdc: Vec<i64>,
    pub dsf: Vec<f64>,
    pub unlcid: Vec<i64>,
}
impl CONTACT_RIGID_BODY_TWO_WAY_TO_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_RIGID_BODY_TWO_WAY_TO_RIGID_BODY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_int()).unwrap_or_default(),
            us: t.take("US").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdc: t.take("LCDC").and_then(|c| c.into_int()).unwrap_or_default(),
            dsf: t.take("DSF").and_then(|c| c.into_float()).unwrap_or_default(),
            unlcid: t.take("UNLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_RIGID_NODES_TO_RIGID_BODY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fcm: Vec<i64>,
    pub us: Vec<f64>,
    pub f30: Vec<i64>,
    pub lcdc: Vec<i64>,
    pub dsf: Vec<f64>,
    pub unlcid: Vec<i64>,
}
impl CONTACT_RIGID_NODES_TO_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_RIGID_NODES_TO_RIGID_BODY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_int()).unwrap_or_default(),
            us: t.take("US").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdc: t.take("LCDC").and_then(|c| c.into_int()).unwrap_or_default(),
            dsf: t.take("DSF").and_then(|c| c.into_float()).unwrap_or_default(),
            unlcid: t.take("UNLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_RIGID_SURFACE {
    pub cid: Vec<i64>,
    pub psid: Vec<i64>,
    pub boxid: Vec<i64>,
    pub segid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub fslcid: Vec<i64>,
    pub fdlcid: Vec<i64>,
    pub sfs: Vec<f64>,
    pub stthk: Vec<f64>,
    pub sfthk: Vec<f64>,
    pub xpene: Vec<f64>,
    pub bsort: Vec<f64>,
    pub ctype: Vec<i64>,
}
impl CONTACT_RIGID_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_RIGID_SURFACE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            segid: t.take("SEGID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            fslcid: t.take("FSLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fdlcid: t.take("FDLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            stthk: t.take("STTHK").and_then(|c| c.into_float()).unwrap_or_default(),
            sfthk: t.take("SFTHK").and_then(|c| c.into_float()).unwrap_or_default(),
            xpene: t.take("XPENE").and_then(|c| c.into_float()).unwrap_or_default(),
            bsort: t.take("BSORT").and_then(|c| c.into_float()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SINGLE_EDGE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SINGLE_EDGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SINGLE_EDGE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SINGLE_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SINGLE_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SLIDING_ONLY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SLIDING_ONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SLIDING_ONLY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SLIDING_ONLY_PENALTY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SLIDING_ONLY_PENALTY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SLIDING_ONLY_PENALTY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SPG {
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
    pub iself1: Vec<i64>,
    pub iself2: Vec<i64>,
    pub iself3: Vec<i64>,
    pub iself4: Vec<i64>,
    pub iself5: Vec<i64>,
    pub iself6: Vec<i64>,
    pub iself7: Vec<i64>,
    pub iself8: Vec<i64>,
    pub pfac1: Vec<f64>,
    pub pfac2: Vec<f64>,
    pub pfac3: Vec<f64>,
    pub pfac4: Vec<f64>,
    pub pfac5: Vec<f64>,
    pub pfac6: Vec<f64>,
    pub pfac7: Vec<f64>,
    pub pfac8: Vec<f64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub nfreq: Vec<f64>,
}
impl CONTACT_SPG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SPG").unwrap());
        Self {
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
            iself1: t.take("ISELF1").and_then(|c| c.into_int()).unwrap_or_default(),
            iself2: t.take("ISELF2").and_then(|c| c.into_int()).unwrap_or_default(),
            iself3: t.take("ISELF3").and_then(|c| c.into_int()).unwrap_or_default(),
            iself4: t.take("ISELF4").and_then(|c| c.into_int()).unwrap_or_default(),
            iself5: t.take("ISELF5").and_then(|c| c.into_int()).unwrap_or_default(),
            iself6: t.take("ISELF6").and_then(|c| c.into_int()).unwrap_or_default(),
            iself7: t.take("ISELF7").and_then(|c| c.into_int()).unwrap_or_default(),
            iself8: t.take("ISELF8").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac1: t.take("PFAC1").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac2: t.take("PFAC2").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac3: t.take("PFAC3").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac4: t.take("PFAC4").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac5: t.take("PFAC5").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac6: t.take("PFAC6").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac7: t.take("PFAC7").and_then(|c| c.into_float()).unwrap_or_default(),
            pfac8: t.take("PFAC8").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SPOTWELD {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SPOTWELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SPOTWELD").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SPOTWELD_BEAM_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SPOTWELD_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SPOTWELD_BEAM_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SPOTWELD_WITH_TORSION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SPOTWELD_WITH_TORSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SPOTWELD_WITH_TORSION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SURFACE_TO_SURFACE_CONTRACTION_JOINT {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub mtcj: Vec<i64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub tsvx: Vec<f64>,
    pub tsvy: Vec<f64>,
    pub tsvz: Vec<f64>,
}
impl CONTACT_SURFACE_TO_SURFACE_CONTRACTION_JOINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SURFACE_TO_SURFACE_CONTRACTION_JOINT").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            mtcj: t.take("MTCJ").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tsvx: t.take("TSVX").and_then(|c| c.into_float()).unwrap_or_default(),
            tsvy: t.take("TSVY").and_then(|c| c.into_float()).unwrap_or_default(),
            tsvz: t.take("TSVZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SURFACE_TO_SURFACE_INTERFERENCE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
}
impl CONTACT_SURFACE_TO_SURFACE_INTERFERENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SURFACE_TO_SURFACE_INTERFERENCE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SURFACE_TO_SURFACE_SMOOTH {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_SURFACE_TO_SURFACE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SURFACE_TO_SURFACE_SMOOTH").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_SURFACE_TO_SURFACE_THERMAL_FRICTION {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub k: Vec<f64>,
    pub frad: Vec<f64>,
    pub h0: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub ftosa: Vec<f64>,
    pub bc_flg: Vec<i64>,
    pub algo: Vec<i64>,
    pub lcfst: Vec<i64>,
    pub lcfdt: Vec<i64>,
    pub formula: Vec<i64>,
    pub a: Vec<i64>,
    pub b: Vec<i64>,
    pub c: Vec<i64>,
    pub d: Vec<i64>,
    pub lch: Vec<i64>,
}
impl CONTACT_SURFACE_TO_SURFACE_THERMAL_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_SURFACE_TO_SURFACE_THERMAL_FRICTION").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            frad: t.take("FRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ftosa: t.take("FTOSA").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flg: t.take("BC_FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            algo: t.take("ALGO").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfst: t.take("LCFST").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfdt: t.take("LCFDT").and_then(|c| c.into_int()).unwrap_or_default(),
            formula: t.take("FORMULA").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("a").and_then(|c| c.into_int()).unwrap_or_default(),
            b: t.take("b").and_then(|c| c.into_int()).unwrap_or_default(),
            c: t.take("c").and_then(|c| c.into_int()).unwrap_or_default(),
            d: t.take("d").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIEBREAK_NODES_ONLY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub nflf: Vec<f64>,
    pub sflf: Vec<f64>,
    pub nen: Vec<f64>,
    pub mes: Vec<f64>,
}
impl CONTACT_TIEBREAK_NODES_ONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIEBREAK_NODES_ONLY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            nflf: t.take("NFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            sflf: t.take("SFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            nen: t.take("NEN").and_then(|c| c.into_float()).unwrap_or_default(),
            mes: t.take("MES").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIEBREAK_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub nflf: Vec<f64>,
    pub sflf: Vec<f64>,
    pub nen: Vec<f64>,
    pub mes: Vec<f64>,
}
impl CONTACT_TIEBREAK_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIEBREAK_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            nflf: t.take("NFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            sflf: t.take("SFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            nen: t.take("NEN").and_then(|c| c.into_float()).unwrap_or_default(),
            mes: t.take("MES").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIEBREAK_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub tblcid: Vec<i64>,
    pub thkoff: Vec<i64>,
}
impl CONTACT_TIEBREAK_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIEBREAK_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thkoff: t.take("THKOFF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIEBREAK_SURFACE_TO_SURFACE_ONLY {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
    pub nfls: Vec<f64>,
    pub sfls: Vec<f64>,
    pub tblcid: Vec<i64>,
    pub thkoff: Vec<i64>,
}
impl CONTACT_TIEBREAK_SURFACE_TO_SURFACE_ONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIEBREAK_SURFACE_TO_SURFACE_ONLY").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
            nfls: t.take("NFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            sfls: t.take("SFLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thkoff: t.take("THKOFF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_NODES_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_NODES_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_NODES_TO_SURFACE_CONSTRAINED_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_NODES_TO_SURFACE_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_NODES_TO_SURFACE_CONSTRAINED_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_NODES_TO_SURFACE_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_NODES_TO_SURFACE_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_NODES_TO_SURFACE_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SOLID {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SOLID").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SOLID_BEAM_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SOLID_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SOLID_BEAM_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SOLID_CONSTRAINED_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SOLID_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SOLID_CONSTRAINED_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SOLID_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SOLID_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SOLID_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SURFACE_BEAM_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SURFACE_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SURFACE_BEAM_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SURFACE_CONSTRAINED_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SURFACE_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SURFACE_CONSTRAINED_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SHELL_EDGE_TO_SURFACE_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SHELL_EDGE_TO_SURFACE_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SHELL_EDGE_TO_SURFACE_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SURFACE_TO_SURFACE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SURFACE_TO_SURFACE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SURFACE_TO_SURFACE_CONSTRAINED_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SURFACE_TO_SURFACE_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SURFACE_TO_SURFACE_CONSTRAINED_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SURFACE_TO_SURFACE_FAILURE {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SURFACE_TO_SURFACE_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SURFACE_TO_SURFACE_FAILURE").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTACT_TIED_SURFACE_TO_SURFACE_OFFSET {
    pub surfa: Vec<i64>,
    pub surfb: Vec<i64>,
    pub surfatyp: Vec<i64>,
    pub surfbtyp: Vec<i64>,
    pub saboxid: Vec<i64>,
    pub sbboxid: Vec<i64>,
    pub sapr: Vec<i64>,
    pub sbpr: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub vdc: Vec<f64>,
    pub penchk: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sfsa: Vec<f64>,
    pub sfsb: Vec<f64>,
    pub sast: Vec<f64>,
    pub sbst: Vec<f64>,
    pub sfsat: Vec<f64>,
    pub sfsbt: Vec<f64>,
    pub fsf: Vec<f64>,
    pub vsf: Vec<f64>,
}
impl CONTACT_TIED_SURFACE_TO_SURFACE_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTACT_TIED_SURFACE_TO_SURFACE_OFFSET").unwrap());
        Self {
            surfa: t.take("SURFA").and_then(|c| c.into_int()).unwrap_or_default(),
            surfb: t.take("SURFB").and_then(|c| c.into_int()).unwrap_or_default(),
            surfatyp: t.take("SURFATYP").and_then(|c| c.into_int()).unwrap_or_default(),
            surfbtyp: t.take("SURFBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            saboxid: t.take("SABOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbboxid: t.take("SBBOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            sapr: t.take("SAPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sbpr: t.take("SBPR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsa: t.take("SFSA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsb: t.take("SFSB").and_then(|c| c.into_float()).unwrap_or_default(),
            sast: t.take("SAST").and_then(|c| c.into_float()).unwrap_or_default(),
            sbst: t.take("SBST").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsat: t.take("SFSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsbt: t.take("SFSBT").and_then(|c| c.into_float()).unwrap_or_default(),
            fsf: t.take("FSF").and_then(|c| c.into_float()).unwrap_or_default(),
            vsf: t.take("VSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROLLER_PLANT {
    pub plntid: Vec<i64>,
    pub nin: Vec<i64>,
    pub nout: Vec<i64>,
    pub nmode: Vec<i64>,
    pub mtxq: Vec<i64>,
    pub mtxr: Vec<i64>,
    pub mopt: Vec<i64>,
    pub f: Vec<i64>,
    pub fscilab: Vec<String>,
    pub flsdyna: Vec<String>,
    pub fmatlab: Vec<String>,
    pub f60: Vec<String>,
    pub nodi1: Vec<i64>,
    pub dofi1: Vec<i64>,
    pub nodi2: Vec<i64>,
    pub dofi2: Vec<i64>,
    pub nodi3: Vec<i64>,
    pub dofi3: Vec<i64>,
    pub nodi4: Vec<i64>,
    pub dofi4: Vec<i64>,
    pub nodo1: Vec<i64>,
    pub dofo1: Vec<i64>,
    pub nodo2: Vec<i64>,
    pub dofo2: Vec<i64>,
    pub nodo3: Vec<i64>,
    pub dofo3: Vec<i64>,
    pub nodo4: Vec<i64>,
    pub dofo4: Vec<i64>,
    pub nfeq: Vec<i64>,
    pub deftol: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_6: Vec<i64>,
    pub mod1: Vec<i64>,
    pub mod2: Vec<i64>,
    pub mod3: Vec<i64>,
    pub mod4: Vec<i64>,
    pub mod5: Vec<i64>,
    pub mod6: Vec<i64>,
    pub mod7: Vec<i64>,
    pub mod8: Vec<i64>,
}
impl CONTROLLER_PLANT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROLLER_PLANT").unwrap());
        Self {
            plntid: t.take("PLNTID").and_then(|c| c.into_int()).unwrap_or_default(),
            nin: t.take("NIN").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            nmode: t.take("NMODE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxq: t.take("MTXQ").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxr: t.take("MTXR").and_then(|c| c.into_int()).unwrap_or_default(),
            mopt: t.take("MOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            fscilab: t.take("FSCILAB").and_then(|c| c.into_str()).unwrap_or_default(),
            flsdyna: t.take("FLSDYNA").and_then(|c| c.into_str()).unwrap_or_default(),
            fmatlab: t.take("FMATLAB").and_then(|c| c.into_str()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_str()).unwrap_or_default(),
            nodi1: t.take("NODI1").and_then(|c| c.into_int()).unwrap_or_default(),
            dofi1: t.take("DOFI1").and_then(|c| c.into_int()).unwrap_or_default(),
            nodi2: t.take("NODI2").and_then(|c| c.into_int()).unwrap_or_default(),
            dofi2: t.take("DOFI2").and_then(|c| c.into_int()).unwrap_or_default(),
            nodi3: t.take("NODI3").and_then(|c| c.into_int()).unwrap_or_default(),
            dofi3: t.take("DOFI3").and_then(|c| c.into_int()).unwrap_or_default(),
            nodi4: t.take("NODI4").and_then(|c| c.into_int()).unwrap_or_default(),
            dofi4: t.take("DOFI4").and_then(|c| c.into_int()).unwrap_or_default(),
            nodo1: t.take("NODO1").and_then(|c| c.into_int()).unwrap_or_default(),
            dofo1: t.take("DOFO1").and_then(|c| c.into_int()).unwrap_or_default(),
            nodo2: t.take("NODO2").and_then(|c| c.into_int()).unwrap_or_default(),
            dofo2: t.take("DOFO2").and_then(|c| c.into_int()).unwrap_or_default(),
            nodo3: t.take("NODO3").and_then(|c| c.into_int()).unwrap_or_default(),
            dofo3: t.take("DOFO3").and_then(|c| c.into_int()).unwrap_or_default(),
            nodo4: t.take("NODO4").and_then(|c| c.into_int()).unwrap_or_default(),
            dofo4: t.take("DOFO4").and_then(|c| c.into_int()).unwrap_or_default(),
            nfeq: t.take("NFEQ").and_then(|c| c.into_int()).unwrap_or_default(),
            deftol: t.take("DEFTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_int()).unwrap_or_default(),
            mod1: t.take("MOD1").and_then(|c| c.into_int()).unwrap_or_default(),
            mod2: t.take("MOD2").and_then(|c| c.into_int()).unwrap_or_default(),
            mod3: t.take("MOD3").and_then(|c| c.into_int()).unwrap_or_default(),
            mod4: t.take("MOD4").and_then(|c| c.into_int()).unwrap_or_default(),
            mod5: t.take("MOD5").and_then(|c| c.into_int()).unwrap_or_default(),
            mod6: t.take("MOD6").and_then(|c| c.into_int()).unwrap_or_default(),
            mod7: t.take("MOD7").and_then(|c| c.into_int()).unwrap_or_default(),
            mod8: t.take("MOD8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_2D_REMESHING_REGION {
    pub ltyp: Vec<i64>,
    pub par1: Vec<f64>,
    pub par2: Vec<f64>,
    pub par3: Vec<f64>,
    pub par4: Vec<f64>,
    pub par5: Vec<f64>,
    pub par6: Vec<f64>,
    pub par7: Vec<f64>,
}
impl CONTROL_2D_REMESHING_REGION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_2D_REMESHING_REGION").unwrap());
        Self {
            ltyp: t.take("LTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            par1: t.take("PAR1").and_then(|c| c.into_float()).unwrap_or_default(),
            par2: t.take("PAR2").and_then(|c| c.into_float()).unwrap_or_default(),
            par3: t.take("PAR3").and_then(|c| c.into_float()).unwrap_or_default(),
            par4: t.take("PAR4").and_then(|c| c.into_float()).unwrap_or_default(),
            par5: t.take("PAR5").and_then(|c| c.into_float()).unwrap_or_default(),
            par6: t.take("PAR6").and_then(|c| c.into_float()).unwrap_or_default(),
            par7: t.take("PAR7").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ACCURACY {
    pub osu: Vec<i64>,
    pub inn: Vec<i64>,
    pub pidosu: Vec<i64>,
    pub iacc: Vec<i64>,
    pub exacc: Vec<f64>,
}
impl CONTROL_ACCURACY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ACCURACY").unwrap());
        Self {
            osu: t.take("OSU").and_then(|c| c.into_int()).unwrap_or_default(),
            inn: t.take("INN").and_then(|c| c.into_int()).unwrap_or_default(),
            pidosu: t.take("PIDOSU").and_then(|c| c.into_int()).unwrap_or_default(),
            iacc: t.take("IACC").and_then(|c| c.into_int()).unwrap_or_default(),
            exacc: t.take("EXACC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ACOUSTIC {
    pub macdvp: Vec<i64>,
}
impl CONTROL_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ACOUSTIC").unwrap());
        Self {
            macdvp: t.take("MACDVP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ACOUSTIC_COUPLING {
    pub maccpl: Vec<i64>,
    pub acecf1: Vec<f64>,
    pub acecf2: Vec<f64>,
    pub acecf3: Vec<f64>,
    pub acecf4: Vec<f64>,
}
impl CONTROL_ACOUSTIC_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ACOUSTIC_COUPLING").unwrap());
        Self {
            maccpl: t.take("MACCPL").and_then(|c| c.into_int()).unwrap_or_default(),
            acecf1: t.take("ACECF1").and_then(|c| c.into_float()).unwrap_or_default(),
            acecf2: t.take("ACECF2").and_then(|c| c.into_float()).unwrap_or_default(),
            acecf3: t.take("ACECF3").and_then(|c| c.into_float()).unwrap_or_default(),
            acecf4: t.take("ACECF4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ACOUSTIC_SPECTRAL {
    pub maseord: Vec<i64>,
    pub masehrf: Vec<i64>,
    pub masekfl: Vec<i64>,
    pub maseigx: Vec<i64>,
}
impl CONTROL_ACOUSTIC_SPECTRAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ACOUSTIC_SPECTRAL").unwrap());
        Self {
            maseord: t.take("MASEORD").and_then(|c| c.into_int()).unwrap_or_default(),
            masehrf: t.take("MASEHRF").and_then(|c| c.into_int()).unwrap_or_default(),
            masekfl: t.take("MASEKFL").and_then(|c| c.into_int()).unwrap_or_default(),
            maseigx: t.take("MASEIGX").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ADAPSTEP {
    pub factin: Vec<f64>,
    pub dfactr: Vec<f64>,
}
impl CONTROL_ADAPSTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ADAPSTEP").unwrap());
        Self {
            factin: t.take("FACTIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dfactr: t.take("DFACTR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ADAPT {
    pub adpfreq: Vec<f64>,
    pub adptol: Vec<f64>,
    pub adptyp: Vec<i64>,
    pub maxlvl: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub lcadp: Vec<i64>,
    pub ioflag: Vec<i64>,
    pub adpsize: Vec<f64>,
    pub adpass: Vec<i64>,
    pub ireflg: Vec<i64>,
    pub adpene: Vec<f64>,
    pub adpth: Vec<f64>,
    pub memory: Vec<i64>,
    pub orient: Vec<i64>,
    pub maxel: Vec<i64>,
}
impl CONTROL_ADAPT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ADAPT").unwrap());
        Self {
            adpfreq: t.take("ADPFREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            adptol: t.take("ADPTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            adptyp: t.take("ADPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            maxlvl: t.take("MAXLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcadp: t.take("LCADP").and_then(|c| c.into_int()).unwrap_or_default(),
            ioflag: t.take("IOFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            adpsize: t.take("ADPSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            adpass: t.take("ADPASS").and_then(|c| c.into_int()).unwrap_or_default(),
            ireflg: t.take("IREFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            adpene: t.take("ADPENE").and_then(|c| c.into_float()).unwrap_or_default(),
            adpth: t.take("ADPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            orient: t.take("ORIENT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxel: t.take("MAXEL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ADAPTIVE {
    pub adpfreq: Vec<f64>,
    pub adptol: Vec<f64>,
    pub adptyp: Vec<i64>,
    pub maxlvl: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub lcadp: Vec<i64>,
    pub ioflag: Vec<i64>,
    pub adpsize: Vec<f64>,
    pub adpass: Vec<i64>,
    pub ireflg: Vec<i64>,
    pub adpene: Vec<f64>,
    pub adpth: Vec<f64>,
    pub memory: Vec<i64>,
    pub orient: Vec<i64>,
    pub maxel: Vec<i64>,
    pub ladpn90: Vec<i64>,
    pub ladpgh: Vec<i64>,
    pub ncfred: Vec<i64>,
    pub ladpcl: Vec<i64>,
    pub adpctl: Vec<f64>,
    pub cbirth: Vec<f64>,
    pub cdeath: Vec<f64>,
    pub lclvl: Vec<i64>,
    pub cnla: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub mmm2d: Vec<i64>,
    pub adperr: Vec<String>,
    pub d3trace: Vec<i64>,
    pub iadpcf: Vec<i64>,
    pub ifsand: Vec<i64>,
}
impl CONTROL_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ADAPTIVE").unwrap());
        Self {
            adpfreq: t.take("ADPFREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            adptol: t.take("ADPTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            adptyp: t.take("ADPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            maxlvl: t.take("MAXLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcadp: t.take("LCADP").and_then(|c| c.into_int()).unwrap_or_default(),
            ioflag: t.take("IOFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            adpsize: t.take("ADPSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            adpass: t.take("ADPASS").and_then(|c| c.into_int()).unwrap_or_default(),
            ireflg: t.take("IREFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            adpene: t.take("ADPENE").and_then(|c| c.into_float()).unwrap_or_default(),
            adpth: t.take("ADPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            orient: t.take("ORIENT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxel: t.take("MAXEL").and_then(|c| c.into_int()).unwrap_or_default(),
            ladpn90: t.take("LADPN90").and_then(|c| c.into_int()).unwrap_or_default(),
            ladpgh: t.take("LADPGH").and_then(|c| c.into_int()).unwrap_or_default(),
            ncfred: t.take("NCFRED").and_then(|c| c.into_int()).unwrap_or_default(),
            ladpcl: t.take("LADPCL").and_then(|c| c.into_int()).unwrap_or_default(),
            adpctl: t.take("ADPCTL").and_then(|c| c.into_float()).unwrap_or_default(),
            cbirth: t.take("CBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            cdeath: t.take("CDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            lclvl: t.take("LCLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            cnla: t.take("CNLA").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            mmm2d: t.take("MMM2D").and_then(|c| c.into_int()).unwrap_or_default(),
            adperr: t.take("ADPERR").and_then(|c| c.into_str()).unwrap_or_default(),
            d3trace: t.take("D3TRACE").and_then(|c| c.into_int()).unwrap_or_default(),
            iadpcf: t.take("IADPCF").and_then(|c| c.into_int()).unwrap_or_default(),
            ifsand: t.take("IFSAND").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ADAPTIVE_CURVE {
    pub idset: Vec<i64>,
    pub itype: Vec<i64>,
    pub n: Vec<i64>,
    pub smin: Vec<f64>,
    pub itriop: Vec<i64>,
}
impl CONTROL_ADAPTIVE_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ADAPTIVE_CURVE").unwrap());
        Self {
            idset: t.take("IDSET").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            smin: t.take("SMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            itriop: t.take("ITRIOP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_AIRBAG {
    pub ckerr: Vec<i64>,
}
impl CONTROL_AIRBAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_AIRBAG").unwrap());
        Self {
            ckerr: t.take("CKERR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ALE {
    pub dct: Vec<i64>,
    pub nadv: Vec<i64>,
    pub meth: Vec<i64>,
    pub afac: Vec<f64>,
    pub bfac: Vec<f64>,
    pub cfac: Vec<f64>,
    pub dfac: Vec<f64>,
    pub efac: Vec<f64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
    pub aafac: Vec<f64>,
    pub vfact: Vec<f64>,
    pub prit: Vec<i64>,
    pub ebc: Vec<i64>,
    pub pref: Vec<f64>,
    pub nsidebc: Vec<i64>,
    pub ncpl: Vec<i64>,
    pub nbkt: Vec<i64>,
    pub imascl: Vec<i64>,
    pub checkr: Vec<f64>,
    pub beamin: Vec<f64>,
    pub mmgpref: Vec<i64>,
    pub pdifmx: Vec<f64>,
    pub dtmufac: Vec<f64>,
    pub optimpp: Vec<i64>,
    pub ialedr: Vec<i64>,
    pub bndflx: Vec<i64>,
    pub minmas: Vec<f64>,
}
impl CONTROL_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ALE").unwrap());
        Self {
            dct: t.take("DCT").and_then(|c| c.into_int()).unwrap_or_default(),
            nadv: t.take("NADV").and_then(|c| c.into_int()).unwrap_or_default(),
            meth: t.take("METH").and_then(|c| c.into_int()).unwrap_or_default(),
            afac: t.take("AFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bfac: t.take("BFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            cfac: t.take("CFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dfac: t.take("DFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            efac: t.take("EFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
            aafac: t.take("AAFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            vfact: t.take("VFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            prit: t.take("PRIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ebc: t.take("EBC").and_then(|c| c.into_int()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidebc: t.take("NSIDEBC").and_then(|c| c.into_int()).unwrap_or_default(),
            ncpl: t.take("NCPL").and_then(|c| c.into_int()).unwrap_or_default(),
            nbkt: t.take("NBKT").and_then(|c| c.into_int()).unwrap_or_default(),
            imascl: t.take("IMASCL").and_then(|c| c.into_int()).unwrap_or_default(),
            checkr: t.take("CHECKR").and_then(|c| c.into_float()).unwrap_or_default(),
            beamin: t.take("BEAMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            mmgpref: t.take("MMGPREF").and_then(|c| c.into_int()).unwrap_or_default(),
            pdifmx: t.take("PDIFMX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmufac: t.take("DTMUFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            optimpp: t.take("OPTIMPP").and_then(|c| c.into_int()).unwrap_or_default(),
            ialedr: t.take("IALEDR").and_then(|c| c.into_int()).unwrap_or_default(),
            bndflx: t.take("BNDFLX").and_then(|c| c.into_int()).unwrap_or_default(),
            minmas: t.take("MINMAS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_BULK_VISCOSITY {
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub type_: Vec<i64>,
    pub btype: Vec<i64>,
    pub tstype: Vec<i64>,
}
impl CONTROL_BULK_VISCOSITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_BULK_VISCOSITY").unwrap());
        Self {
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            btype: t.take("BTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            tstype: t.take("TSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CHECK {
    pub pid: Vec<i64>,
    pub ifauto: Vec<i64>,
    pub convex: Vec<i64>,
    pub adpt: Vec<i64>,
    pub aratio: Vec<f64>,
    pub angke: Vec<f64>,
    pub smin: Vec<f64>,
}
impl CONTROL_CHECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CHECK").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifauto: t.take("IFAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            convex: t.take("CONVEX").and_then(|c| c.into_int()).unwrap_or_default(),
            adpt: t.take("ADPT").and_then(|c| c.into_int()).unwrap_or_default(),
            aratio: t.take("ARATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            angke: t.take("ANGKE").and_then(|c| c.into_float()).unwrap_or_default(),
            smin: t.take("SMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CHECK_SHELL {
    pub pid: Vec<i64>,
    pub ifauto: Vec<i64>,
    pub convex: Vec<i64>,
    pub adpt: Vec<i64>,
    pub aratio: Vec<f64>,
    pub angke: Vec<f64>,
    pub smin: Vec<f64>,
}
impl CONTROL_CHECK_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CHECK_SHELL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifauto: t.take("IFAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            convex: t.take("CONVEX").and_then(|c| c.into_int()).unwrap_or_default(),
            adpt: t.take("ADPT").and_then(|c| c.into_int()).unwrap_or_default(),
            aratio: t.take("ARATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            angke: t.take("ANGKE").and_then(|c| c.into_float()).unwrap_or_default(),
            smin: t.take("SMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_COARSEN {
    pub icoarse: Vec<i64>,
    pub angle: Vec<f64>,
    pub nseed: Vec<i64>,
    pub psid: Vec<i64>,
    pub smax: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl CONTROL_COARSEN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_COARSEN").unwrap());
        Self {
            icoarse: t.take("ICOARSE").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nseed: t.take("NSEED").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            smax: t.take("SMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CONSTRANINED {
    pub sprchk: Vec<i64>,
}
impl CONTROL_CONSTRANINED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CONSTRANINED").unwrap());
        Self {
            sprchk: t.take("SPRCHK").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CONTACT {
    pub slsfac: Vec<f64>,
    pub rwpnal: Vec<f64>,
    pub islchk: Vec<i64>,
    pub shlthk: Vec<i64>,
    pub penopt: Vec<i64>,
    pub thkchg: Vec<i64>,
    pub orien: Vec<i64>,
    pub enmass: Vec<i64>,
    pub usrstr: Vec<i64>,
    pub usrfrc: Vec<i64>,
    pub nsbcs: Vec<i64>,
    pub interm: Vec<i64>,
    pub xpene: Vec<f64>,
    pub ssthk: Vec<i64>,
    pub ecdt: Vec<i64>,
    pub tiedprj: Vec<i64>,
    pub sfric: Vec<f64>,
    pub dfric: Vec<f64>,
    pub edc: Vec<f64>,
    pub vfc: Vec<f64>,
    pub th: Vec<f64>,
    pub th_sf: Vec<f64>,
    pub pen_sf: Vec<f64>,
    pub ignore: Vec<i64>,
    pub frceng: Vec<i64>,
    pub skiprwg: Vec<i64>,
    pub outseg: Vec<i64>,
    pub spotstp: Vec<i64>,
    pub spotdel: Vec<i64>,
    pub spothin: Vec<f64>,
    pub isym: Vec<i64>,
    pub nserod: Vec<i64>,
    pub rwgaps: Vec<i64>,
    pub rwgdth: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub icov: Vec<i64>,
    pub swradf: Vec<f64>,
    pub ithoff: Vec<i64>,
    pub shledg: Vec<i64>,
    pub pstiff: Vec<i64>,
    pub ithcnt: Vec<i64>,
    pub tdcnof: Vec<i64>,
    pub ftall: Vec<i64>,
    pub unused: Vec<i64>,
    pub shltrw: Vec<f64>,
    pub igactc: Vec<i64>,
}
impl CONTROL_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CONTACT").unwrap());
        Self {
            slsfac: t.take("SLSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            rwpnal: t.take("RWPNAL").and_then(|c| c.into_float()).unwrap_or_default(),
            islchk: t.take("ISLCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            shlthk: t.take("SHLTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            penopt: t.take("PENOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            thkchg: t.take("THKCHG").and_then(|c| c.into_int()).unwrap_or_default(),
            orien: t.take("ORIEN").and_then(|c| c.into_int()).unwrap_or_default(),
            enmass: t.take("ENMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            usrstr: t.take("USRSTR").and_then(|c| c.into_int()).unwrap_or_default(),
            usrfrc: t.take("USRFRC").and_then(|c| c.into_int()).unwrap_or_default(),
            nsbcs: t.take("NSBCS").and_then(|c| c.into_int()).unwrap_or_default(),
            interm: t.take("INTERM").and_then(|c| c.into_int()).unwrap_or_default(),
            xpene: t.take("XPENE").and_then(|c| c.into_float()).unwrap_or_default(),
            ssthk: t.take("SSTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            ecdt: t.take("ECDT").and_then(|c| c.into_int()).unwrap_or_default(),
            tiedprj: t.take("TIEDPRJ").and_then(|c| c.into_int()).unwrap_or_default(),
            sfric: t.take("SFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            dfric: t.take("DFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            edc: t.take("EDC").and_then(|c| c.into_float()).unwrap_or_default(),
            vfc: t.take("VFC").and_then(|c| c.into_float()).unwrap_or_default(),
            th: t.take("TH").and_then(|c| c.into_float()).unwrap_or_default(),
            th_sf: t.take("TH_SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pen_sf: t.take("PEN_SF").and_then(|c| c.into_float()).unwrap_or_default(),
            ignore: t.take("IGNORE").and_then(|c| c.into_int()).unwrap_or_default(),
            frceng: t.take("FRCENG").and_then(|c| c.into_int()).unwrap_or_default(),
            skiprwg: t.take("SKIPRWG").and_then(|c| c.into_int()).unwrap_or_default(),
            outseg: t.take("OUTSEG").and_then(|c| c.into_int()).unwrap_or_default(),
            spotstp: t.take("SPOTSTP").and_then(|c| c.into_int()).unwrap_or_default(),
            spotdel: t.take("SPOTDEL").and_then(|c| c.into_int()).unwrap_or_default(),
            spothin: t.take("SPOTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            nserod: t.take("NSEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            rwgaps: t.take("RWGAPS").and_then(|c| c.into_int()).unwrap_or_default(),
            rwgdth: t.take("RWGDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            icov: t.take("ICOV").and_then(|c| c.into_int()).unwrap_or_default(),
            swradf: t.take("SWRADF").and_then(|c| c.into_float()).unwrap_or_default(),
            ithoff: t.take("ITHOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            shledg: t.take("SHLEDG").and_then(|c| c.into_int()).unwrap_or_default(),
            pstiff: t.take("PSTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ithcnt: t.take("ITHCNT").and_then(|c| c.into_int()).unwrap_or_default(),
            tdcnof: t.take("TDCNOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ftall: t.take("FTALL").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            shltrw: t.take("SHLTRW").and_then(|c| c.into_float()).unwrap_or_default(),
            igactc: t.take("IGACTC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_COUPLING {
    pub unleng: Vec<f64>,
    pub untime: Vec<f64>,
    pub unforc: Vec<f64>,
    pub timidl: Vec<f64>,
    pub flipx: Vec<i64>,
    pub flipy: Vec<i64>,
    pub flipz: Vec<i64>,
    pub subcyl: Vec<i64>,
}
impl CONTROL_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_COUPLING").unwrap());
        Self {
            unleng: t.take("UNLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            untime: t.take("UNTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            unforc: t.take("UNFORC").and_then(|c| c.into_float()).unwrap_or_default(),
            timidl: t.take("TIMIDL").and_then(|c| c.into_float()).unwrap_or_default(),
            flipx: t.take("FLIPX").and_then(|c| c.into_int()).unwrap_or_default(),
            flipy: t.take("FLIPY").and_then(|c| c.into_int()).unwrap_or_default(),
            flipz: t.take("FLIPZ").and_then(|c| c.into_int()).unwrap_or_default(),
            subcyl: t.take("SUBCYL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CPM {
    pub cpmout: Vec<i64>,
    pub np2p: Vec<i64>,
    pub ncpmts: Vec<i64>,
    pub cpmerr: Vec<i64>,
    pub sffdc: Vec<f64>,
    pub f: Vec<i64>,
    pub cpmmf: Vec<i64>,
}
impl CONTROL_CPM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CPM").unwrap());
        Self {
            cpmout: t.take("CPMOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            np2p: t.take("NP2P").and_then(|c| c.into_int()).unwrap_or_default(),
            ncpmts: t.take("NCPMTS").and_then(|c| c.into_int()).unwrap_or_default(),
            cpmerr: t.take("CPMERR").and_then(|c| c.into_int()).unwrap_or_default(),
            sffdc: t.take("SFFDC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            cpmmf: t.take("CPMMF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CPU {
    pub cputim: Vec<f64>,
    pub iglst: Vec<i64>,
}
impl CONTROL_CPU {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CPU").unwrap());
        Self {
            cputim: t.take("CPUTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            iglst: t.take("IGLST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CR_MPP_DECOMPOSITION_DEFORMED_GEOMETRY {
    pub sph: Vec<i64>,
}
impl CONTROL_CR_MPP_DECOMPOSITION_DEFORMED_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CR_MPP_DECOMPOSITION_DEFORMED_GEOMETRY").unwrap());
        Self {
            sph: t.take("SPH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_CR_MPP_DECOMPOSITION_FLAG_STRESS_STRAIN_CURVE {
}
impl CONTROL_CR_MPP_DECOMPOSITION_FLAG_STRESS_STRAIN_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CR_MPP_DECOMPOSITION_FLAG_STRESS_STRAIN_CURVE").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_CR_MPP_MATERIAL_MODEL_DRIVER {
}
impl CONTROL_CR_MPP_MATERIAL_MODEL_DRIVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_CR_MPP_MATERIAL_MODEL_DRIVER").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_DAMPING {
    pub nrcyck: Vec<i64>,
    pub drtol: Vec<f64>,
    pub drfctr: Vec<f64>,
    pub drterm: Vec<f64>,
    pub tssfdr: Vec<f64>,
    pub irelal: Vec<i64>,
    pub edttl: Vec<f64>,
    pub idrflg: Vec<i64>,
    pub drpset: Vec<i64>,
}
impl CONTROL_DAMPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_DAMPING").unwrap());
        Self {
            nrcyck: t.take("NRCYCK").and_then(|c| c.into_int()).unwrap_or_default(),
            drtol: t.take("DRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            drfctr: t.take("DRFCTR").and_then(|c| c.into_float()).unwrap_or_default(),
            drterm: t.take("DRTERM").and_then(|c| c.into_float()).unwrap_or_default(),
            tssfdr: t.take("TSSFDR").and_then(|c| c.into_float()).unwrap_or_default(),
            irelal: t.take("IRELAL").and_then(|c| c.into_int()).unwrap_or_default(),
            edttl: t.take("EDTTL").and_then(|c| c.into_float()).unwrap_or_default(),
            idrflg: t.take("IDRFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            drpset: t.take("DRPSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_DEBUG {
}
impl CONTROL_DEBUG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_DEBUG").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_DISCRETE_ELEMENT {
    pub ndamp: Vec<f64>,
    pub tdamp: Vec<f64>,
    pub frics: Vec<f64>,
    pub fricr: Vec<f64>,
    pub normk: Vec<f64>,
    pub sheark: Vec<f64>,
    pub cap: Vec<i64>,
    pub vtk: Vec<i64>,
    pub gamma: Vec<f64>,
    pub vol: Vec<f64>,
    pub ang: Vec<f64>,
    pub gap: Vec<f64>,
    pub unused: Vec<i64>,
    pub ignore: Vec<i64>,
    pub nbuf: Vec<i64>,
    pub parallel: Vec<i64>,
    pub lnorm: Vec<i64>,
    pub lshear: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub fricd: Vec<f64>,
    pub dc: Vec<f64>,
    pub ncrb: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub cp: Vec<f64>,
    pub tc: Vec<f64>,
    pub tfac: Vec<f64>,
    pub idesoft: Vec<i64>,
    pub sofscl: Vec<f64>,
    pub unused_3: Vec<i64>,
    pub iskip: Vec<i64>,
    pub maxnei: Vec<i64>,
}
impl CONTROL_DISCRETE_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_DISCRETE_ELEMENT").unwrap());
        Self {
            ndamp: t.take("NDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdamp: t.take("TDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            frics: t.take("FRICS").and_then(|c| c.into_float()).unwrap_or_default(),
            fricr: t.take("FRICR").and_then(|c| c.into_float()).unwrap_or_default(),
            normk: t.take("NORMK").and_then(|c| c.into_float()).unwrap_or_default(),
            sheark: t.take("SHEARK").and_then(|c| c.into_float()).unwrap_or_default(),
            cap: t.take("CAP").and_then(|c| c.into_int()).unwrap_or_default(),
            vtk: t.take("VTK").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ignore: t.take("IGNORE").and_then(|c| c.into_int()).unwrap_or_default(),
            nbuf: t.take("NBUF").and_then(|c| c.into_int()).unwrap_or_default(),
            parallel: t.take("PARALLEL").and_then(|c| c.into_int()).unwrap_or_default(),
            lnorm: t.take("LNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lshear: t.take("LSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            fricd: t.take("FRICD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            ncrb: t.take("NCRB").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            tfac: t.take("TFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            idesoft: t.take("IDESOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            sofscl: t.take("SOFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            iskip: t.take("ISKIP").and_then(|c| c.into_int()).unwrap_or_default(),
            maxnei: t.take("MAXNEI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_DYNAMIC_RELAXATION {
    pub nrcyck: Vec<i64>,
    pub drtol: Vec<f64>,
    pub drfctr: Vec<f64>,
    pub drterm: Vec<f64>,
    pub tssfdr: Vec<f64>,
    pub irelal: Vec<i64>,
    pub edttl: Vec<f64>,
    pub idrflg: Vec<i64>,
    pub drpset: Vec<i64>,
    pub nc: Vec<i64>,
    pub np: Vec<i64>,
    pub psid: Vec<i64>,
    pub vecid: Vec<i64>,
}
impl CONTROL_DYNAMIC_RELAXATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_DYNAMIC_RELAXATION").unwrap());
        Self {
            nrcyck: t.take("NRCYCK").and_then(|c| c.into_int()).unwrap_or_default(),
            drtol: t.take("DRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            drfctr: t.take("DRFCTR").and_then(|c| c.into_float()).unwrap_or_default(),
            drterm: t.take("DRTERM").and_then(|c| c.into_float()).unwrap_or_default(),
            tssfdr: t.take("TSSFDR").and_then(|c| c.into_float()).unwrap_or_default(),
            irelal: t.take("IRELAL").and_then(|c| c.into_int()).unwrap_or_default(),
            edttl: t.take("EDTTL").and_then(|c| c.into_float()).unwrap_or_default(),
            idrflg: t.take("IDRFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            drpset: t.take("DRPSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nc: t.take("NC").and_then(|c| c.into_int()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EFG {
    pub isplane: Vec<i64>,
    pub idila: Vec<i64>,
    pub inint: Vec<i64>,
    pub imlm: Vec<i64>,
    pub etol: Vec<f64>,
}
impl CONTROL_EFG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EFG").unwrap());
        Self {
            isplane: t.take("ISPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            idila: t.take("IDILA").and_then(|c| c.into_int()).unwrap_or_default(),
            inint: t.take("ININT").and_then(|c| c.into_int()).unwrap_or_default(),
            imlm: t.take("IMLM").and_then(|c| c.into_int()).unwrap_or_default(),
            etol: t.take("ETOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_ENERGY {
    pub hgen: Vec<i64>,
    pub rwen: Vec<i64>,
    pub slnten: Vec<i64>,
    pub rylen: Vec<i64>,
    pub irgen: Vec<i64>,
    pub maten: Vec<i64>,
    pub drlen: Vec<i64>,
    pub disen: Vec<i64>,
}
impl CONTROL_ENERGY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_ENERGY").unwrap());
        Self {
            hgen: t.take("HGEN").and_then(|c| c.into_int()).unwrap_or_default(),
            rwen: t.take("RWEN").and_then(|c| c.into_int()).unwrap_or_default(),
            slnten: t.take("SLNTEN").and_then(|c| c.into_int()).unwrap_or_default(),
            rylen: t.take("RYLEN").and_then(|c| c.into_int()).unwrap_or_default(),
            irgen: t.take("IRGEN").and_then(|c| c.into_int()).unwrap_or_default(),
            maten: t.take("MATEN").and_then(|c| c.into_int()).unwrap_or_default(),
            drlen: t.take("DRLEN").and_then(|c| c.into_int()).unwrap_or_default(),
            disen: t.take("DISEN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EOS_USER_LIBRARY {
    pub path: Vec<String>,
    pub conm: Vec<f64>,
    pub conl: Vec<f64>,
    pub cont: Vec<f64>,
    pub conp: Vec<f64>,
}
impl CONTROL_EOS_USER_LIBRARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EOS_USER_LIBRARY").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
            conm: t.take("CONM").and_then(|c| c.into_float()).unwrap_or_default(),
            conl: t.take("CONL").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_float()).unwrap_or_default(),
            conp: t.take("CONP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_ALE_COUPLING {
    pub partset: Vec<i64>,
    pub mmgset: Vec<i64>,
}
impl CONTROL_EXPLICIT_THERMAL_ALE_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_ALE_COUPLING").unwrap());
        Self {
            partset: t.take("PARTSET").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_BOUNDARY {
    pub partset: Vec<i64>,
    pub mmgset: Vec<i64>,
}
impl CONTROL_EXPLICIT_THERMAL_BOUNDARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_BOUNDARY").unwrap());
        Self {
            partset: t.take("PARTSET").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_CONTACT {
    pub partset: Vec<i64>,
    pub ncycle: Vec<i64>,
}
impl CONTROL_EXPLICIT_THERMAL_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_CONTACT").unwrap());
        Self {
            partset: t.take("PARTSET").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_INITIAL {
    pub id: Vec<i64>,
    pub idtyp: Vec<i64>,
    pub tempini: Vec<f64>,
}
impl CONTROL_EXPLICIT_THERMAL_INITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_INITIAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            idtyp: t.take("IDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            tempini: t.take("TEMPINI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_OUTPUT {
    pub dtout: Vec<f64>,
    pub dtoutyp: Vec<i64>,
    pub setid: Vec<i64>,
    pub setyp: Vec<i64>,
}
impl CONTROL_EXPLICIT_THERMAL_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_OUTPUT").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            dtoutyp: t.take("DTOUTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_PROPERTIES {
    pub partset: Vec<i64>,
    pub cp: Vec<f64>,
    pub cptyp: Vec<i64>,
    pub vecid1: Vec<i64>,
    pub vecid2: Vec<i64>,
    pub local: Vec<i64>,
    pub kxx: Vec<f64>,
    pub kxy: Vec<f64>,
    pub kxz: Vec<f64>,
    pub kxxtyp: Vec<i64>,
    pub kxytyp: Vec<i64>,
    pub kxztyp: Vec<i64>,
    pub kyx: Vec<f64>,
    pub kyy: Vec<f64>,
    pub kyz: Vec<f64>,
    pub kyxtyp: Vec<i64>,
    pub kyytyp: Vec<i64>,
    pub kyztyp: Vec<i64>,
    pub kzx: Vec<f64>,
    pub kzy: Vec<f64>,
    pub kzz: Vec<f64>,
    pub kzxtyp: Vec<i64>,
    pub kzytyp: Vec<i64>,
    pub kzztyp: Vec<i64>,
}
impl CONTROL_EXPLICIT_THERMAL_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_PROPERTIES").unwrap());
        Self {
            partset: t.take("PARTSET").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            cptyp: t.take("CPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid1: t.take("VECID1").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid2: t.take("VECID2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            kxx: t.take("Kxx").and_then(|c| c.into_float()).unwrap_or_default(),
            kxy: t.take("Kxy").and_then(|c| c.into_float()).unwrap_or_default(),
            kxz: t.take("Kxz").and_then(|c| c.into_float()).unwrap_or_default(),
            kxxtyp: t.take("KxxTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kxytyp: t.take("KxyTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kxztyp: t.take("KxzTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kyx: t.take("Kyx").and_then(|c| c.into_float()).unwrap_or_default(),
            kyy: t.take("Kyy").and_then(|c| c.into_float()).unwrap_or_default(),
            kyz: t.take("Kyz").and_then(|c| c.into_float()).unwrap_or_default(),
            kyxtyp: t.take("KyxTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kyytyp: t.take("KyyTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kyztyp: t.take("KyzTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kzx: t.take("Kzx").and_then(|c| c.into_float()).unwrap_or_default(),
            kzy: t.take("Kzy").and_then(|c| c.into_float()).unwrap_or_default(),
            kzz: t.take("Kzz").and_then(|c| c.into_float()).unwrap_or_default(),
            kzxtyp: t.take("KzxTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kzytyp: t.take("KzyTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            kzztyp: t.take("KzzTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLICIT_THERMAL_SOLVER {
    pub partset: Vec<i64>,
    pub dtfac: Vec<f64>,
}
impl CONTROL_EXPLICIT_THERMAL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLICIT_THERMAL_SOLVER").unwrap());
        Self {
            partset: t.take("PARTSET").and_then(|c| c.into_int()).unwrap_or_default(),
            dtfac: t.take("DTFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_EXPLOSIVE_SHADOW {
}
impl CONTROL_EXPLOSIVE_SHADOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_EXPLOSIVE_SHADOW").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_FORMING_AUTOCHECK {
    pub icheck: Vec<i64>,
    pub igd: Vec<i64>,
    pub ioffset: Vec<i64>,
    pub ioutputp: Vec<i64>,
}
impl CONTROL_FORMING_AUTOCHECK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTOCHECK").unwrap());
        Self {
            icheck: t.take("ICHECK").and_then(|c| c.into_int()).unwrap_or_default(),
            igd: t.take("IGD").and_then(|c| c.into_int()).unwrap_or_default(),
            ioffset: t.take("IOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            ioutputp: t.take("IOUTPUTP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_AUTOPOSITION_PARAMETER {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dir: Vec<i64>,
    pub mpid: Vec<i64>,
    pub position: Vec<i64>,
    pub premove: Vec<f64>,
    pub thick: Vec<f64>,
    pub porder: Vec<String>,
}
impl CONTROL_FORMING_AUTOPOSITION_PARAMETER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTOPOSITION_PARAMETER").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpid: t.take("MPID").and_then(|c| c.into_int()).unwrap_or_default(),
            position: t.take("POSITION").and_then(|c| c.into_int()).unwrap_or_default(),
            premove: t.take("PREMOVE").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            porder: t.take("PORDER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_AUTOPOSITION_PARAMETER_POSITIVE {
    pub pid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dir: Vec<i64>,
    pub mpid: Vec<i64>,
    pub position: Vec<i64>,
    pub premove: Vec<f64>,
    pub thick: Vec<f64>,
    pub porder: Vec<String>,
}
impl CONTROL_FORMING_AUTOPOSITION_PARAMETER_POSITIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTOPOSITION_PARAMETER_POSITIVE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpid: t.take("MPID").and_then(|c| c.into_int()).unwrap_or_default(),
            position: t.take("POSITION").and_then(|c| c.into_int()).unwrap_or_default(),
            premove: t.take("PREMOVE").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            porder: t.take("PORDER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET {
    pub psid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dir: Vec<i64>,
    pub mpsid: Vec<i64>,
    pub position: Vec<i64>,
    pub premove: Vec<f64>,
    pub thick: Vec<f64>,
    pub porder: Vec<String>,
}
impl CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpsid: t.take("MPSID").and_then(|c| c.into_int()).unwrap_or_default(),
            position: t.take("POSITION").and_then(|c| c.into_int()).unwrap_or_default(),
            premove: t.take("PREMOVE").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            porder: t.take("PORDER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET_POSITIVE {
    pub psid: Vec<i64>,
    pub cid: Vec<i64>,
    pub dir: Vec<i64>,
    pub mpsid: Vec<i64>,
    pub position: Vec<i64>,
    pub premove: Vec<f64>,
    pub thick: Vec<f64>,
    pub porder: Vec<String>,
}
impl CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET_POSITIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTOPOSITION_PARAMETER_SET_POSITIVE").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpsid: t.take("MPSID").and_then(|c| c.into_int()).unwrap_or_default(),
            position: t.take("POSITION").and_then(|c| c.into_int()).unwrap_or_default(),
            premove: t.take("PREMOVE").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            porder: t.take("PORDER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_AUTO_NET {
    pub idnet: Vec<i64>,
    pub itype: Vec<String>,
    pub idv: Vec<i64>,
    pub idp: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub sx: Vec<f64>,
    pub sy: Vec<f64>,
    pub offset: Vec<f64>,
}
impl CONTROL_FORMING_AUTO_NET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_AUTO_NET").unwrap());
        Self {
            idnet: t.take("IDNET").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            idv: t.take("IDV").and_then(|c| c.into_int()).unwrap_or_default(),
            idp: t.take("IDP").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            sx: t.take("SX").and_then(|c| c.into_float()).unwrap_or_default(),
            sy: t.take("SY").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_BESTFIT {
    pub ifit: Vec<i64>,
    pub nskip: Vec<i64>,
    pub gaponly: Vec<i64>,
    pub ifast: Vec<i64>,
    pub ifset: Vec<i64>,
    pub nsets: Vec<i64>,
    pub nsett: Vec<i64>,
    pub filename: Vec<String>,
}
impl CONTROL_FORMING_BESTFIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_BESTFIT").unwrap());
        Self {
            ifit: t.take("IFIT").and_then(|c| c.into_int()).unwrap_or_default(),
            nskip: t.take("NSKIP").and_then(|c| c.into_int()).unwrap_or_default(),
            gaponly: t.take("GAPONLY").and_then(|c| c.into_int()).unwrap_or_default(),
            ifast: t.take("IFAST").and_then(|c| c.into_int()).unwrap_or_default(),
            ifset: t.take("IFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nsets: t.take("NSETS").and_then(|c| c.into_int()).unwrap_or_default(),
            nsett: t.take("NSETT").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_BESTFIT_VECTOR {
    pub ifit: Vec<i64>,
    pub nskip: Vec<i64>,
    pub gaponly: Vec<i64>,
    pub ifast: Vec<i64>,
    pub ifset: Vec<i64>,
    pub nsets: Vec<i64>,
    pub nsett: Vec<i64>,
    pub filename: Vec<String>,
}
impl CONTROL_FORMING_BESTFIT_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_BESTFIT_VECTOR").unwrap());
        Self {
            ifit: t.take("IFIT").and_then(|c| c.into_int()).unwrap_or_default(),
            nskip: t.take("NSKIP").and_then(|c| c.into_int()).unwrap_or_default(),
            gaponly: t.take("GAPONLY").and_then(|c| c.into_int()).unwrap_or_default(),
            ifast: t.take("IFAST").and_then(|c| c.into_int()).unwrap_or_default(),
            ifset: t.take("IFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nsets: t.take("NSETS").and_then(|c| c.into_int()).unwrap_or_default(),
            nsett: t.take("NSETT").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_BLANKMESH {
    pub idmesh: Vec<i64>,
    pub eleng: Vec<f64>,
    pub xleng: Vec<f64>,
    pub yleng: Vec<f64>,
    pub angelx: Vec<f64>,
    pub nplane: Vec<i64>,
    pub cid: Vec<i64>,
    pub pidbk: Vec<i64>,
    pub nid: Vec<i64>,
    pub eid: Vec<i64>,
    pub xcent: Vec<f64>,
    pub ycent: Vec<f64>,
    pub zcent: Vec<f64>,
    pub xshift: Vec<f64>,
    pub yshift: Vec<f64>,
}
impl CONTROL_FORMING_BLANKMESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_BLANKMESH").unwrap());
        Self {
            idmesh: t.take("IDMESH").and_then(|c| c.into_int()).unwrap_or_default(),
            eleng: t.take("ELENG").and_then(|c| c.into_float()).unwrap_or_default(),
            xleng: t.take("XLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            yleng: t.take("YLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            angelx: t.take("ANGELX").and_then(|c| c.into_float()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            pidbk: t.take("PIDBK").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            xcent: t.take("XCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycent: t.take("YCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            zcent: t.take("ZCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            xshift: t.take("XSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
            yshift: t.take("YSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_GENERATE_BLANKMESH {
    pub gentyp: Vec<i64>,
    pub eleng: Vec<f64>,
    pub center: Vec<i64>,
    pub xleng: Vec<f64>,
    pub yleng: Vec<f64>,
    pub align: Vec<f64>,
    pub plane: Vec<i64>,
    pub cid: Vec<i64>,
    pub bpid: Vec<i64>,
    pub snid: Vec<i64>,
    pub seid: Vec<i64>,
    pub xcent: Vec<f64>,
    pub ycent: Vec<f64>,
    pub zcent: Vec<f64>,
    pub xshift: Vec<f64>,
    pub yshift: Vec<f64>,
    pub filename: Vec<String>,
}
impl CONTROL_FORMING_GENERATE_BLANKMESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_GENERATE_BLANKMESH").unwrap());
        Self {
            gentyp: t.take("GENTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            eleng: t.take("ELENG").and_then(|c| c.into_float()).unwrap_or_default(),
            center: t.take("CENTER").and_then(|c| c.into_int()).unwrap_or_default(),
            xleng: t.take("XLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            yleng: t.take("YLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            align: t.take("ALIGN").and_then(|c| c.into_float()).unwrap_or_default(),
            plane: t.take("PLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            bpid: t.take("BPID").and_then(|c| c.into_int()).unwrap_or_default(),
            snid: t.take("SNID").and_then(|c| c.into_int()).unwrap_or_default(),
            seid: t.take("SEID").and_then(|c| c.into_int()).unwrap_or_default(),
            xcent: t.take("XCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycent: t.take("YCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            zcent: t.take("ZCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            xshift: t.take("XSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
            yshift: t.take("YSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_HOME_GAP {
    pub psidu: Vec<i64>,
    pub psidl: Vec<i64>,
    pub gap: Vec<f64>,
    pub mvinc: Vec<f64>,
    pub istop: Vec<i64>,
}
impl CONTROL_FORMING_HOME_GAP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_HOME_GAP").unwrap());
        Self {
            psidu: t.take("PSIDU").and_then(|c| c.into_int()).unwrap_or_default(),
            psidl: t.take("PSIDL").and_then(|c| c.into_int()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            mvinc: t.take("MVINC").and_then(|c| c.into_float()).unwrap_or_default(),
            istop: t.take("ISTOP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_INITIAL_THICKNESS {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0x: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl CONTROL_FORMING_INITIAL_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_INITIAL_THICKNESS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0x: t.take("Z0X").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_MAXID {
    pub pid: Vec<i64>,
    pub maxidn: Vec<i64>,
    pub maxide: Vec<i64>,
    pub f: Vec<i64>,
    pub i2dynain: Vec<i64>,
}
impl CONTROL_FORMING_MAXID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_MAXID").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            maxidn: t.take("MAXIDN").and_then(|c| c.into_int()).unwrap_or_default(),
            maxide: t.take("MAXIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            i2dynain: t.take("I2DYNAIN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP {
    pub option: Vec<i64>,
    pub tsclmax: Vec<f64>,
    pub autobd: Vec<f64>,
    pub tsclmin: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub unused: Vec<i64>,
    pub lcsdg: Vec<i64>,
    pub dmgexp: Vec<f64>,
    pub flatname: Vec<String>,
}
impl CONTROL_FORMING_ONESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP").unwrap());
        Self {
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            tsclmax: t.take("TSCLMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            autobd: t.take("AUTOBD").and_then(|c| c.into_float()).unwrap_or_default(),
            tsclmin: t.take("TSCLMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flatname: t.take("FLATNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_AUTO_CONSTRAINT {
    pub icon: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub node3: Vec<i64>,
}
impl CONTROL_FORMING_ONESTEP_AUTO_CONSTRAINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_AUTO_CONSTRAINT").unwrap());
        Self {
            icon: t.take("ICON").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            node3: t.take("NODE3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_DRAWBEAD {
    pub ndset: Vec<i64>,
    pub lcid: Vec<i64>,
    pub th: Vec<f64>,
    pub percnt: Vec<f64>,
}
impl CONTROL_FORMING_ONESTEP_DRAWBEAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_DRAWBEAD").unwrap());
        Self {
            ndset: t.take("NDSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            th: t.take("TH").and_then(|c| c.into_float()).unwrap_or_default(),
            percnt: t.take("PERCNT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_FRICTION {
    pub ndset: Vec<i64>,
    pub bdton: Vec<f64>,
    pub frict: Vec<f64>,
}
impl CONTROL_FORMING_ONESTEP_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_FRICTION").unwrap());
        Self {
            ndset: t.take("NDSET").and_then(|c| c.into_int()).unwrap_or_default(),
            bdton: t.take("BDTON").and_then(|c| c.into_float()).unwrap_or_default(),
            frict: t.take("FRICT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_ORTHO {
    pub pid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
}
impl CONTROL_FORMING_ONESTEP_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_ORTHO").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_QUAD {
    pub option: Vec<i64>,
    pub tsclmax: Vec<f64>,
    pub autobd: Vec<f64>,
    pub tsclmin: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub unused: Vec<i64>,
    pub lcsdg: Vec<i64>,
    pub dmgexp: Vec<f64>,
    pub flatname: Vec<String>,
}
impl CONTROL_FORMING_ONESTEP_QUAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_QUAD").unwrap());
        Self {
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            tsclmax: t.take("TSCLMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            autobd: t.take("AUTOBD").and_then(|c| c.into_float()).unwrap_or_default(),
            tsclmin: t.take("TSCLMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flatname: t.take("FLATNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_QUAD2 {
    pub option: Vec<i64>,
    pub tsclmax: Vec<f64>,
    pub autobd: Vec<f64>,
    pub tsclmin: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub unused: Vec<i64>,
    pub lcsdg: Vec<i64>,
    pub dmgexp: Vec<f64>,
    pub flatname: Vec<String>,
}
impl CONTROL_FORMING_ONESTEP_QUAD2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_QUAD2").unwrap());
        Self {
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            tsclmax: t.take("TSCLMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            autobd: t.take("AUTOBD").and_then(|c| c.into_float()).unwrap_or_default(),
            tsclmin: t.take("TSCLMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flatname: t.take("FLATNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_ONESTEP_TRIA {
    pub option: Vec<i64>,
    pub tsclmax: Vec<f64>,
    pub autobd: Vec<f64>,
    pub tsclmin: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub unused: Vec<i64>,
    pub lcsdg: Vec<i64>,
    pub dmgexp: Vec<f64>,
    pub flatname: Vec<String>,
}
impl CONTROL_FORMING_ONESTEP_TRIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_ONESTEP_TRIA").unwrap());
        Self {
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            tsclmax: t.take("TSCLMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            autobd: t.take("AUTOBD").and_then(|c| c.into_float()).unwrap_or_default(),
            tsclmin: t.take("TSCLMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("unused").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flatname: t.take("FLATNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_OUTPUT {
    pub cid: Vec<i64>,
    pub nout: Vec<i64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub y1_lcid: Vec<f64>,
    pub y2_lcid: Vec<f64>,
    pub y3: Vec<f64>,
    pub y4: Vec<f64>,
}
impl CONTROL_FORMING_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_OUTPUT").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            y1_lcid: t.take("Y1/LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            y2_lcid: t.take("Y2/LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_OUTPUT_INTFOR {
    pub cid: Vec<i64>,
    pub nout: Vec<i64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub y1_lcid: Vec<f64>,
    pub y2_lcid: Vec<f64>,
    pub y3: Vec<f64>,
    pub y4: Vec<f64>,
}
impl CONTROL_FORMING_OUTPUT_INTFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_OUTPUT_INTFOR").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            y1_lcid: t.take("Y1/LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            y2_lcid: t.take("Y2/LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_PARAMETER_READ {
    pub filename: Vec<String>,
    pub paraname: Vec<String>,
    pub imethod: Vec<i64>,
    pub line: Vec<i64>,
    pub nbegpa: Vec<i64>,
    pub nendpa: Vec<i64>,
    pub value: Vec<f64>,
}
impl CONTROL_FORMING_PARAMETER_READ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_PARAMETER_READ").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            paraname: t.take("PARANAME").and_then(|c| c.into_str()).unwrap_or_default(),
            imethod: t.take("IMETHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            line: t.take("LINE").and_then(|c| c.into_int()).unwrap_or_default(),
            nbegpa: t.take("NBEGPA").and_then(|c| c.into_int()).unwrap_or_default(),
            nendpa: t.take("NENDPA").and_then(|c| c.into_int()).unwrap_or_default(),
            value: t.take("VALUE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_POSITION {
    pub pid: Vec<i64>,
    pub premove: Vec<f64>,
    pub target: Vec<i64>,
}
impl CONTROL_FORMING_POSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_POSITION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            premove: t.take("PREMOVE").and_then(|c| c.into_float()).unwrap_or_default(),
            target: t.take("TARGET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_PRE_BENDING {
    pub pset: Vec<i64>,
    pub radius: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
}
impl CONTROL_FORMING_PRE_BENDING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_PRE_BENDING").unwrap());
        Self {
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_PRE_BENDING_LOCAL {
    pub pset: Vec<i64>,
    pub radius: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl CONTROL_FORMING_PRE_BENDING_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_PRE_BENDING_LOCAL").unwrap());
        Self {
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_PROJECTION {
    pub pidb: Vec<i64>,
    pub pidt: Vec<i64>,
    pub gap: Vec<f64>,
    pub nrbst: Vec<i64>,
    pub nrtst: Vec<i64>,
}
impl CONTROL_FORMING_PROJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_PROJECTION").unwrap());
        Self {
            pidb: t.take("PIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            pidt: t.take("PIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            nrbst: t.take("NRBST").and_then(|c| c.into_int()).unwrap_or_default(),
            nrtst: t.take("NRTST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_REMOVE_ADAPTIVE_CONSTRAINTS {
    pub pid: Vec<i64>,
}
impl CONTROL_FORMING_REMOVE_ADAPTIVE_CONSTRAINTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_REMOVE_ADAPTIVE_CONSTRAINTS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_SCRAP_FALL {
    pub pid: Vec<i64>,
    pub vectid: Vec<i64>,
    pub ndset: Vec<i64>,
    pub lcid: Vec<i64>,
    pub depth: Vec<f64>,
    pub dist: Vec<f64>,
    pub idrgd: Vec<i64>,
    pub ifseed: Vec<i64>,
    pub nobead: Vec<i64>,
    pub seedx: Vec<f64>,
    pub seedy: Vec<f64>,
    pub seedz: Vec<f64>,
    pub effset: Vec<f64>,
    pub gap: Vec<f64>,
    pub ipset: Vec<i64>,
    pub extend: Vec<i64>,
    pub newid: Vec<i64>,
}
impl CONTROL_FORMING_SCRAP_FALL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_SCRAP_FALL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vectid: t.take("VECTID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndset: t.take("NDSET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            depth: t.take("DEPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            idrgd: t.take("IDRGD").and_then(|c| c.into_int()).unwrap_or_default(),
            ifseed: t.take("IFSEED").and_then(|c| c.into_int()).unwrap_or_default(),
            nobead: t.take("NOBEAD").and_then(|c| c.into_int()).unwrap_or_default(),
            seedx: t.take("SEEDX").and_then(|c| c.into_float()).unwrap_or_default(),
            seedy: t.take("SEEDY").and_then(|c| c.into_float()).unwrap_or_default(),
            seedz: t.take("SEEDZ").and_then(|c| c.into_float()).unwrap_or_default(),
            effset: t.take("EFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            ipset: t.take("IPSET").and_then(|c| c.into_int()).unwrap_or_default(),
            extend: t.take("EXTEND").and_then(|c| c.into_int()).unwrap_or_default(),
            newid: t.take("NEWID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_SHELL_TO_TSHELL {
    pub pid: Vec<i64>,
    pub thick: Vec<f64>,
    pub midsf: Vec<f64>,
    pub idsegb: Vec<f64>,
    pub idsegt: Vec<f64>,
}
impl CONTROL_FORMING_SHELL_TO_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_SHELL_TO_TSHELL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            midsf: t.take("MIDSF").and_then(|c| c.into_float()).unwrap_or_default(),
            idsegb: t.take("IDSEGB").and_then(|c| c.into_float()).unwrap_or_default(),
            idsegt: t.take("IDSEGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_STONING {
    pub istone: Vec<i64>,
    pub length: Vec<f64>,
    pub width: Vec<i64>,
    pub step: Vec<f64>,
    pub direction: Vec<i64>,
    pub reverse: Vec<i64>,
    pub method: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub setid: Vec<i64>,
    pub itype: Vec<i64>,
}
impl CONTROL_FORMING_STONING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_STONING").unwrap());
        Self {
            istone: t.take("ISTONE").and_then(|c| c.into_int()).unwrap_or_default(),
            length: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            width: t.take("WIDTH").and_then(|c| c.into_int()).unwrap_or_default(),
            step: t.take("STEP").and_then(|c| c.into_float()).unwrap_or_default(),
            direction: t.take("DIRECTION").and_then(|c| c.into_int()).unwrap_or_default(),
            reverse: t.take("REVERSE").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_STRAIN_RATIO_SMOOTH {
    pub dt_cycle: Vec<f64>,
    pub weight: Vec<f64>,
}
impl CONTROL_FORMING_STRAIN_RATIO_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_STRAIN_RATIO_SMOOTH").unwrap());
        Self {
            dt_cycle: t.take("DT/CYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
            weight: t.take("WEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TEMPLATE {
    pub idtemp: Vec<i64>,
    pub blkid: Vec<i64>,
    pub dieid: Vec<i64>,
    pub pnch: Vec<i64>,
    pub bndu: Vec<i64>,
    pub bndl: Vec<i64>,
    pub type_: Vec<i64>,
    pub prebd: Vec<f64>,
    pub lcss: Vec<i64>,
    pub al_fe: Vec<String>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub e: Vec<f64>,
    pub density: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub mtype: Vec<i64>,
    pub unit: Vec<i64>,
    pub thick: Vec<f64>,
    pub gap: Vec<f64>,
    pub fs: Vec<f64>,
    pub patern: Vec<i64>,
    pub vmax: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vid: Vec<i64>,
    pub amax: Vec<f64>,
    pub lvlada: Vec<i64>,
    pub sizeada: Vec<f64>,
    pub timsada: Vec<i64>,
    pub d3plt: Vec<i64>,
}
impl CONTROL_FORMING_TEMPLATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TEMPLATE").unwrap());
        Self {
            idtemp: t.take("IDTEMP").and_then(|c| c.into_int()).unwrap_or_default(),
            blkid: t.take("BLKID").and_then(|c| c.into_int()).unwrap_or_default(),
            dieid: t.take("DIEID").and_then(|c| c.into_int()).unwrap_or_default(),
            pnch: t.take("PNCH").and_then(|c| c.into_int()).unwrap_or_default(),
            bndu: t.take("BNDU").and_then(|c| c.into_int()).unwrap_or_default(),
            bndl: t.take("BNDL").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            prebd: t.take("PREBD").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            al_fe: t.take("AL/FE").and_then(|c| c.into_str()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            patern: t.take("PATERN").and_then(|c| c.into_int()).unwrap_or_default(),
            vmax: t.take("VMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            amax: t.take("AMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            lvlada: t.take("LVLADA").and_then(|c| c.into_int()).unwrap_or_default(),
            sizeada: t.take("SIZEADA").and_then(|c| c.into_float()).unwrap_or_default(),
            timsada: t.take("TIMSADA").and_then(|c| c.into_int()).unwrap_or_default(),
            d3plt: t.take("D3PLT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TIPPING {
    pub pid: Vec<i64>,
    pub itype: Vec<i64>,
    pub ifstrn: Vec<i64>,
    pub ifstrs: Vec<i64>,
    pub nmove: Vec<i64>,
    pub rot_tran: Vec<i64>,
    pub v11: Vec<f64>,
    pub v12: Vec<f64>,
    pub v13: Vec<f64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub dista1: Vec<f64>,
    pub rot_tran_2: Vec<i64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
}
impl CONTROL_FORMING_TIPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TIPPING").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ifstrn: t.take("IFSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            ifstrs: t.take("IFSTRS").and_then(|c| c.into_int()).unwrap_or_default(),
            nmove: t.take("NMOVE").and_then(|c| c.into_int()).unwrap_or_default(),
            rot_tran: t.take("ROT/TRAN").and_then(|c| c.into_int()).unwrap_or_default(),
            v11: t.take("V11").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v13: t.take("V13").and_then(|c| c.into_float()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            dista1: t.take("DISTA1").and_then(|c| c.into_float()).unwrap_or_default(),
            rot_tran_2: t.take("ROT/TRAN").and_then(|c| c.into_int()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TOLERANC {
    pub dt_cycle: Vec<f64>,
    pub weight: Vec<f64>,
}
impl CONTROL_FORMING_TOLERANC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TOLERANC").unwrap());
        Self {
            dt_cycle: t.take("DT/CYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
            weight: t.take("WEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TRAVEL {
    pub pid: Vec<i64>,
    pub vid: Vec<i64>,
    pub travel: Vec<f64>,
    pub target: Vec<i64>,
    pub gap: Vec<f64>,
    pub phase: Vec<i64>,
    pub follow: Vec<i64>,
}
impl CONTROL_FORMING_TRAVEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TRAVEL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            travel: t.take("TRAVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            target: t.take("TARGET").and_then(|c| c.into_int()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            follow: t.take("FOLLOW").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TRIMMING {
    pub psid: Vec<i64>,
    pub f: Vec<i64>,
    pub ityp: Vec<i64>,
}
impl CONTROL_FORMING_TRIMMING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TRIMMING").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ityp: t.take("ITYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TRIM_MERGE {
    pub imerge: Vec<i64>,
    pub gapm: Vec<f64>,
}
impl CONTROL_FORMING_TRIM_MERGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TRIM_MERGE").unwrap());
        Self {
            imerge: t.take("IMERGE").and_then(|c| c.into_int()).unwrap_or_default(),
            gapm: t.take("GAPM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_TRIM_SOLID_REFINEMENT {
    pub irefine: Vec<i64>,
    pub ilevel: Vec<i64>,
}
impl CONTROL_FORMING_TRIM_SOLID_REFINEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_TRIM_SOLID_REFINEMENT").unwrap());
        Self {
            irefine: t.take("IREFINE").and_then(|c| c.into_int()).unwrap_or_default(),
            ilevel: t.take("ILEVEL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_UNFLANGING {
    pub noption: Vec<i64>,
    pub dvid: Vec<i64>,
    pub nunbend: Vec<i64>,
    pub stfbend: Vec<f64>,
    pub stfcnt: Vec<f64>,
    pub iflimit: Vec<i64>,
    pub dist: Vec<f64>,
    pub ilinear: Vec<i64>,
    pub nb1: Vec<i64>,
    pub nb2: Vec<i64>,
    pub nb3: Vec<i64>,
    pub charlen: Vec<f64>,
    pub ndouter: Vec<i64>,
}
impl CONTROL_FORMING_UNFLANGING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_UNFLANGING").unwrap());
        Self {
            noption: t.take("NOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            dvid: t.take("DVID").and_then(|c| c.into_int()).unwrap_or_default(),
            nunbend: t.take("NUNBEND").and_then(|c| c.into_int()).unwrap_or_default(),
            stfbend: t.take("STFBEND").and_then(|c| c.into_float()).unwrap_or_default(),
            stfcnt: t.take("STFCNT").and_then(|c| c.into_float()).unwrap_or_default(),
            iflimit: t.take("IFLIMIT").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            ilinear: t.take("ILINEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            nb1: t.take("NB1").and_then(|c| c.into_int()).unwrap_or_default(),
            nb2: t.take("NB2").and_then(|c| c.into_int()).unwrap_or_default(),
            nb3: t.take("NB3").and_then(|c| c.into_int()).unwrap_or_default(),
            charlen: t.take("CHARLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            ndouter: t.take("NDOUTER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_UNFLANGING_OUTPUT {
    pub thmx: Vec<f64>,
    pub thmn: Vec<f64>,
    pub epsmx: Vec<f64>,
}
impl CONTROL_FORMING_UNFLANGING_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_UNFLANGING_OUTPUT").unwrap());
        Self {
            thmx: t.take("THMX").and_then(|c| c.into_float()).unwrap_or_default(),
            thmn: t.take("THMN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmx: t.take("EPSMX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FORMING_USER {
    pub blank: Vec<i64>,
    pub type_: Vec<i64>,
    pub thick: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub al_fe: Vec<String>,
    pub unit: Vec<i64>,
    pub lcss: Vec<i64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub e: Vec<f64>,
    pub density: Vec<f64>,
    pub pr: Vec<f64>,
    pub fs: Vec<f64>,
    pub mtype: Vec<i64>,
    pub patern: Vec<i64>,
    pub vmax: Vec<f64>,
    pub amax: Vec<f64>,
    pub lvlada: Vec<i64>,
    pub sizeada: Vec<f64>,
    pub adatims: Vec<i64>,
    pub d3plot: Vec<i64>,
    pub gap: Vec<f64>,
}
impl CONTROL_FORMING_USER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FORMING_USER").unwrap());
        Self {
            blank: t.take("BLANK").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            al_fe: t.take("AL/FE").and_then(|c| c.into_str()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            patern: t.take("PATERN").and_then(|c| c.into_int()).unwrap_or_default(),
            vmax: t.take("VMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            amax: t.take("AMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            lvlada: t.take("LVLADA").and_then(|c| c.into_int()).unwrap_or_default(),
            sizeada: t.take("SIZEADA").and_then(|c| c.into_float()).unwrap_or_default(),
            adatims: t.take("ADATIMS").and_then(|c| c.into_int()).unwrap_or_default(),
            d3plot: t.take("D3PLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FREQUENCY_DOMAIN {
    pub refgeo: Vec<i64>,
    pub mpn: Vec<f64>,
}
impl CONTROL_FREQUENCY_DOMAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FREQUENCY_DOMAIN").unwrap());
        Self {
            refgeo: t.take("REFGEO").and_then(|c| c.into_int()).unwrap_or_default(),
            mpn: t.take("MPN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_FREQUENCY_RESPONSE_FUNCTION {
    pub n1: Vec<i64>,
    pub n1typ: Vec<i64>,
    pub dof1: Vec<i64>,
    pub vad1: Vec<i64>,
    pub vid: Vec<i64>,
    pub fnmax: Vec<f64>,
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub n2: Vec<i64>,
    pub n2typ: Vec<i64>,
    pub dof2: Vec<i64>,
    pub vad2: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
    pub restrt: Vec<i64>,
}
impl CONTROL_FREQUENCY_RESPONSE_FUNCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_FREQUENCY_RESPONSE_FUNCTION").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n1typ: t.take("N1TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof1: t.take("DOF1").and_then(|c| c.into_int()).unwrap_or_default(),
            vad1: t.take("VAD1").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n2typ: t.take("N2TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof2: t.take("DOF2").and_then(|c| c.into_int()).unwrap_or_default(),
            vad2: t.take("VAD2").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_HOURGLASS {
    pub ihq: Vec<i64>,
    pub qh: Vec<f64>,
}
impl CONTROL_HOURGLASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_HOURGLASS").unwrap());
        Self {
            ihq: t.take("IHQ").and_then(|c| c.into_int()).unwrap_or_default(),
            qh: t.take("QH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_HOURGLASS_936 {
    pub ihq: Vec<i64>,
    pub qh: Vec<f64>,
}
impl CONTROL_HOURGLASS_936 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_HOURGLASS_936").unwrap());
        Self {
            ihq: t.take("IHQ").and_then(|c| c.into_int()).unwrap_or_default(),
            qh: t.take("QH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_AUTO {
    pub iauto: Vec<i64>,
    pub iteopt: Vec<i64>,
    pub itewin: Vec<i64>,
    pub dtmin: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub dtexp: Vec<f64>,
    pub kfail: Vec<i64>,
    pub kcycle: Vec<i64>,
    pub hcmin: Vec<f64>,
    pub hcmax: Vec<f64>,
    pub hmmin: Vec<f64>,
    pub hmmax: Vec<f64>,
    pub hntmax: Vec<f64>,
    pub hnrmax: Vec<f64>,
    pub hrtmax: Vec<f64>,
    pub hrrmax: Vec<f64>,
}
impl CONTROL_IMPLICIT_AUTO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_AUTO").unwrap());
        Self {
            iauto: t.take("IAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            iteopt: t.take("ITEOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            itewin: t.take("ITEWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtexp: t.take("DTEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            kfail: t.take("KFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            kcycle: t.take("KCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            hcmin: t.take("HCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hcmax: t.take("HCMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmin: t.take("HMMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmax: t.take("HMMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hntmax: t.take("HNTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hnrmax: t.take("HNRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrtmax: t.take("HRTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrrmax: t.take("HRRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_AUTO_DYN {
    pub iauto: Vec<i64>,
    pub iteopt: Vec<i64>,
    pub itewin: Vec<i64>,
    pub dtmin: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub dtexp: Vec<f64>,
    pub kfail: Vec<i64>,
    pub kcycle: Vec<i64>,
    pub hcmin: Vec<f64>,
    pub hcmax: Vec<f64>,
    pub hmmin: Vec<f64>,
    pub hmmax: Vec<f64>,
    pub hntmax: Vec<f64>,
    pub hnrmax: Vec<f64>,
    pub hrtmax: Vec<f64>,
    pub hrrmax: Vec<f64>,
}
impl CONTROL_IMPLICIT_AUTO_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_AUTO_DYN").unwrap());
        Self {
            iauto: t.take("IAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            iteopt: t.take("ITEOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            itewin: t.take("ITEWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtexp: t.take("DTEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            kfail: t.take("KFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            kcycle: t.take("KCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            hcmin: t.take("HCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hcmax: t.take("HCMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmin: t.take("HMMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmax: t.take("HMMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hntmax: t.take("HNTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hnrmax: t.take("HNRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrtmax: t.take("HRTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrrmax: t.take("HRRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_AUTO_SPR {
    pub iauto: Vec<i64>,
    pub iteopt: Vec<i64>,
    pub itewin: Vec<i64>,
    pub dtmin: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub dtexp: Vec<f64>,
    pub kfail: Vec<i64>,
    pub kcycle: Vec<i64>,
    pub hcmin: Vec<f64>,
    pub hcmax: Vec<f64>,
    pub hmmin: Vec<f64>,
    pub hmmax: Vec<f64>,
    pub hntmax: Vec<f64>,
    pub hnrmax: Vec<f64>,
    pub hrtmax: Vec<f64>,
    pub hrrmax: Vec<f64>,
}
impl CONTROL_IMPLICIT_AUTO_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_AUTO_SPR").unwrap());
        Self {
            iauto: t.take("IAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            iteopt: t.take("ITEOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            itewin: t.take("ITEWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtexp: t.take("DTEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            kfail: t.take("KFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            kcycle: t.take("KCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            hcmin: t.take("HCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hcmax: t.take("HCMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmin: t.take("HMMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmmax: t.take("HMMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hntmax: t.take("HNTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hnrmax: t.take("HNRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrtmax: t.take("HRTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hrrmax: t.take("HRRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_BUCKLE {
    pub nmode: Vec<i64>,
    pub bckmth: Vec<i64>,
}
impl CONTROL_IMPLICIT_BUCKLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_BUCKLE").unwrap());
        Self {
            nmode: t.take("NMODE").and_then(|c| c.into_int()).unwrap_or_default(),
            bckmth: t.take("BCKMTH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_CONSISTENT_MASS {
    pub iflag: Vec<i64>,
}
impl CONTROL_IMPLICIT_CONSISTENT_MASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_CONSISTENT_MASS").unwrap());
        Self {
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_DYNAMIC {
    pub imass: Vec<i64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub tdybir: Vec<f64>,
    pub tdydth: Vec<f64>,
    pub tdybur: Vec<f64>,
    pub irate: Vec<i64>,
    pub alpha: Vec<f64>,
}
impl CONTROL_IMPLICIT_DYNAMIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_DYNAMIC").unwrap());
        Self {
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybir: t.take("TDYBIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tdydth: t.take("TDYDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybur: t.take("TDYBUR").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_DYNAMICS {
    pub imass: Vec<i64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub tdybir: Vec<f64>,
    pub tdydth: Vec<f64>,
    pub tdybur: Vec<f64>,
    pub irate: Vec<i64>,
    pub alpha: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
}
impl CONTROL_IMPLICIT_DYNAMICS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_DYNAMICS").unwrap());
        Self {
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybir: t.take("TDYBIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tdydth: t.take("TDYDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybur: t.take("TDYBUR").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_DYNAMICS_DYN {
    pub imass: Vec<i64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub tdybir: Vec<f64>,
    pub tdydth: Vec<f64>,
    pub tdybur: Vec<f64>,
    pub irate: Vec<i64>,
    pub alpha: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
}
impl CONTROL_IMPLICIT_DYNAMICS_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_DYNAMICS_DYN").unwrap());
        Self {
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybir: t.take("TDYBIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tdydth: t.take("TDYDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybur: t.take("TDYBUR").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_DYNAMICS_SPR {
    pub imass: Vec<i64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub tdybir: Vec<f64>,
    pub tdydth: Vec<f64>,
    pub tdybur: Vec<f64>,
    pub irate: Vec<i64>,
    pub alpha: Vec<f64>,
    pub psid: Vec<i64>,
    pub angle: Vec<f64>,
}
impl CONTROL_IMPLICIT_DYNAMICS_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_DYNAMICS_SPR").unwrap());
        Self {
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybir: t.take("TDYBIR").and_then(|c| c.into_float()).unwrap_or_default(),
            tdydth: t.take("TDYDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdybur: t.take("TDYBUR").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_EIGENVALUE {
    pub neig: Vec<i64>,
    pub center: Vec<f64>,
    pub lflag: Vec<i64>,
    pub lftend: Vec<f64>,
    pub rflag: Vec<i64>,
    pub rhtend: Vec<f64>,
    pub eigmth: Vec<i64>,
    pub shfscl: Vec<f64>,
    pub isolid: Vec<i64>,
    pub ibeam: Vec<i64>,
    pub ishell: Vec<i64>,
    pub itshell: Vec<i64>,
    pub mstres: Vec<i64>,
    pub evdump: Vec<i64>,
    pub mstrscl: Vec<f64>,
    pub iparm1: Vec<i64>,
    pub iparm2: Vec<i64>,
    pub iparm3: Vec<i64>,
    pub iparm4: Vec<i64>,
    pub rparm1: Vec<f64>,
    pub iparm1_2: Vec<i64>,
    pub iparm2_2: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub rparm1_2: Vec<i64>,
    pub rparm2: Vec<i64>,
    pub iparm1_3: Vec<i64>,
    pub iparm2_3: Vec<i64>,
    pub iparm3_2: Vec<i64>,
    pub iparm4_2: Vec<i64>,
    pub rparm5: Vec<i64>,
    pub rparm6: Vec<i64>,
}
impl CONTROL_IMPLICIT_EIGENVALUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_EIGENVALUE").unwrap());
        Self {
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
            center: t.take("CENTER").and_then(|c| c.into_float()).unwrap_or_default(),
            lflag: t.take("LFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lftend: t.take("LFTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rhtend: t.take("RHTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            eigmth: t.take("EIGMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            shfscl: t.take("SHFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            isolid: t.take("ISOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibeam: t.take("IBEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            ishell: t.take("ISHELL").and_then(|c| c.into_int()).unwrap_or_default(),
            itshell: t.take("ITSHELL").and_then(|c| c.into_int()).unwrap_or_default(),
            mstres: t.take("MSTRES").and_then(|c| c.into_int()).unwrap_or_default(),
            evdump: t.take("EVDUMP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstrscl: t.take("MSTRSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            iparm1: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm2: t.take("IPARM2").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm3: t.take("IPARM3").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm4: t.take("IPARM4").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1: t.take("RPARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            iparm1_2: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm2_2: t.take("IPARM2").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1_2: t.take("RPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm2: t.take("RPARM2").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm1_3: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm2_3: t.take("IPARM2").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm3_2: t.take("IPARM3").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm4_2: t.take("IPARM4").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm5: t.take("RPARM5").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm6: t.take("RPARM6").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_EIGENVALUES {
    pub neig: Vec<i64>,
    pub center: Vec<f64>,
    pub lflag: Vec<i64>,
    pub lftend: Vec<f64>,
    pub rflag: Vec<i64>,
    pub rhtend: Vec<f64>,
    pub eigmth: Vec<i64>,
    pub shfscl: Vec<f64>,
    pub isolid: Vec<i64>,
    pub ibeam: Vec<i64>,
    pub ishell: Vec<i64>,
    pub itshell: Vec<i64>,
    pub mstres: Vec<i64>,
    pub evdump: Vec<i64>,
    pub mstrscl: Vec<f64>,
}
impl CONTROL_IMPLICIT_EIGENVALUES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_EIGENVALUES").unwrap());
        Self {
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
            center: t.take("CENTER").and_then(|c| c.into_float()).unwrap_or_default(),
            lflag: t.take("LFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lftend: t.take("LFTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            rhtend: t.take("RHTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            eigmth: t.take("EIGMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            shfscl: t.take("SHFSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            isolid: t.take("ISOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibeam: t.take("IBEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            ishell: t.take("ISHELL").and_then(|c| c.into_int()).unwrap_or_default(),
            itshell: t.take("ITSHELL").and_then(|c| c.into_int()).unwrap_or_default(),
            mstres: t.take("MSTRES").and_then(|c| c.into_int()).unwrap_or_default(),
            evdump: t.take("EVDUMP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstrscl: t.take("MSTRSCL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_FORMING {
    pub ioption: Vec<i64>,
    pub nsmin: Vec<i64>,
    pub nsmax: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub penchk: Vec<f64>,
    pub dt0: Vec<f64>,
}
impl CONTROL_IMPLICIT_FORMING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_FORMING").unwrap());
        Self {
            ioption: t.take("IOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmin: t.take("NSMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmax: t.take("NSMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_float()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_FORMING_DYN {
    pub ioption: Vec<i64>,
    pub nsmin: Vec<i64>,
    pub nsmax: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub penchk: Vec<f64>,
    pub dt0: Vec<f64>,
}
impl CONTROL_IMPLICIT_FORMING_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_FORMING_DYN").unwrap());
        Self {
            ioption: t.take("IOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmin: t.take("NSMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmax: t.take("NSMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_float()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_FORMING_SPR {
    pub ioption: Vec<i64>,
    pub nsmin: Vec<i64>,
    pub nsmax: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub penchk: Vec<f64>,
    pub dt0: Vec<f64>,
}
impl CONTROL_IMPLICIT_FORMING_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_FORMING_SPR").unwrap());
        Self {
            ioption: t.take("IOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmin: t.take("NSMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmax: t.take("NSMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            penchk: t.take("PENCHK").and_then(|c| c.into_float()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_GENERAL {
    pub imflag: Vec<i64>,
    pub dt0: Vec<f64>,
    pub imform: Vec<i64>,
    pub nsbs: Vec<i64>,
    pub igs: Vec<i64>,
    pub cnstn: Vec<i64>,
    pub form: Vec<i64>,
    pub zero_v: Vec<i64>,
}
impl CONTROL_IMPLICIT_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_GENERAL").unwrap());
        Self {
            imflag: t.take("IMFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
            imform: t.take("IMFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nsbs: t.take("NSBS").and_then(|c| c.into_int()).unwrap_or_default(),
            igs: t.take("IGS").and_then(|c| c.into_int()).unwrap_or_default(),
            cnstn: t.take("CNSTN").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            zero_v: t.take("ZERO_V").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_GENERAL_DYN {
    pub imflag: Vec<i64>,
    pub dt0: Vec<f64>,
    pub imform: Vec<i64>,
    pub nsbs: Vec<i64>,
    pub igs: Vec<i64>,
    pub cnstn: Vec<i64>,
    pub form: Vec<i64>,
    pub zero_v: Vec<i64>,
}
impl CONTROL_IMPLICIT_GENERAL_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_GENERAL_DYN").unwrap());
        Self {
            imflag: t.take("IMFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
            imform: t.take("IMFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nsbs: t.take("NSBS").and_then(|c| c.into_int()).unwrap_or_default(),
            igs: t.take("IGS").and_then(|c| c.into_int()).unwrap_or_default(),
            cnstn: t.take("CNSTN").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            zero_v: t.take("ZERO_V").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_GENERAL_SPR {
    pub imflag: Vec<i64>,
    pub dt0: Vec<f64>,
    pub imform: Vec<i64>,
    pub nsbs: Vec<i64>,
    pub igs: Vec<i64>,
    pub cnstn: Vec<i64>,
    pub form: Vec<i64>,
    pub zero_v: Vec<i64>,
}
impl CONTROL_IMPLICIT_GENERAL_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_GENERAL_SPR").unwrap());
        Self {
            imflag: t.take("IMFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dt0: t.take("DT0").and_then(|c| c.into_float()).unwrap_or_default(),
            imform: t.take("IMFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nsbs: t.take("NSBS").and_then(|c| c.into_int()).unwrap_or_default(),
            igs: t.take("IGS").and_then(|c| c.into_int()).unwrap_or_default(),
            cnstn: t.take("CNSTN").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            zero_v: t.take("ZERO_V").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_INERTIA_RELIEF {
    pub irflag: Vec<i64>,
    pub thresh: Vec<f64>,
    pub ircnt: Vec<i64>,
    pub mode1: Vec<i64>,
    pub mode2: Vec<i64>,
    pub mode3: Vec<i64>,
    pub mode1_2: Vec<i64>,
    pub mode1_3: Vec<i64>,
    pub mode1_4: Vec<i64>,
    pub mode1_5: Vec<i64>,
    pub mode1_6: Vec<i64>,
}
impl CONTROL_IMPLICIT_INERTIA_RELIEF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_INERTIA_RELIEF").unwrap());
        Self {
            irflag: t.take("IRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            thresh: t.take("THRESH").and_then(|c| c.into_float()).unwrap_or_default(),
            ircnt: t.take("IRCNT").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1: t.take("MODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            mode2: t.take("MODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            mode3: t.take("MODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1_2: t.take("MODE1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1_3: t.take("MODE1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1_4: t.take("MODE1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1_5: t.take("MODE1_5").and_then(|c| c.into_int()).unwrap_or_default(),
            mode1_6: t.take("MODE1_6").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_JOINTS {
    pub ispher: Vec<i64>,
    pub irevol: Vec<i64>,
    pub icylin: Vec<i64>,
}
impl CONTROL_IMPLICIT_JOINTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_JOINTS").unwrap());
        Self {
            ispher: t.take("ISPHER").and_then(|c| c.into_int()).unwrap_or_default(),
            irevol: t.take("IREVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            icylin: t.take("ICYLIN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_LINEAR {
    pub lsolvr: Vec<i64>,
    pub lprint: Vec<i64>,
    pub negev: Vec<i64>,
    pub order: Vec<i64>,
    pub drcm: Vec<i64>,
    pub drcprm: Vec<f64>,
    pub autospc: Vec<i64>,
    pub autotol: Vec<f64>,
    pub lcpack: Vec<i64>,
    pub mtxdmp: Vec<i64>,
    pub iparm1: Vec<i64>,
    pub rparm1: Vec<f64>,
    pub rparm2: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub rparm5: Vec<f64>,
}
impl CONTROL_IMPLICIT_LINEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_LINEAR").unwrap());
        Self {
            lsolvr: t.take("LSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            lprint: t.take("LPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            negev: t.take("NEGEV").and_then(|c| c.into_int()).unwrap_or_default(),
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
            drcm: t.take("DRCM").and_then(|c| c.into_int()).unwrap_or_default(),
            drcprm: t.take("DRCPRM").and_then(|c| c.into_float()).unwrap_or_default(),
            autospc: t.take("AUTOSPC").and_then(|c| c.into_int()).unwrap_or_default(),
            autotol: t.take("AUTOTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpack: t.take("LCPACK").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxdmp: t.take("MTXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm1: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1: t.take("RPARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            rparm2: t.take("RPARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm5: t.take("RPARM5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC {
    pub mdflag: Vec<i64>,
    pub zeta: Vec<f64>,
    pub md_strs: Vec<i64>,
    pub dtout: Vec<f64>,
    pub integ: Vec<i64>,
    pub nsid: Vec<i64>,
    pub filename: Vec<String>,
    pub filename2: Vec<String>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC").unwrap());
        Self {
            mdflag: t.take("MDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            zeta: t.take("ZETA").and_then(|c| c.into_float()).unwrap_or_default(),
            md_strs: t.take("MD_STRS").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            integ: t.take("INTEG").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            filename2: t.take("FILENAME2").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING {
    pub zeta1: Vec<f64>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING").unwrap());
        Self {
            zeta1: t.take("ZETA1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_FREQUENCY_RANGE {
    pub freq1: Vec<f64>,
    pub zeta1: Vec<f64>,
    pub freq2: Vec<f64>,
    pub zeta2: Vec<f64>,
    pub freq3: Vec<f64>,
    pub zeta3: Vec<f64>,
    pub freq4: Vec<f64>,
    pub zeta4: Vec<f64>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_FREQUENCY_RANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_FREQUENCY_RANGE").unwrap());
        Self {
            freq1: t.take("FREQ1").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta1: t.take("ZETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            freq2: t.take("FREQ2").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta2: t.take("ZETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            freq3: t.take("FREQ3").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta3: t.take("ZETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            freq4: t.take("FREQ4").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta4: t.take("ZETA4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_SPECIFIC {
    pub mid1: Vec<i64>,
    pub zeta1: Vec<f64>,
    pub mid2: Vec<i64>,
    pub zeta2: Vec<f64>,
    pub mid3: Vec<i64>,
    pub zeta3: Vec<f64>,
    pub mid4: Vec<i64>,
    pub zeta4: Vec<f64>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_SPECIFIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC_DAMPING_SPECIFIC").unwrap());
        Self {
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            zeta1: t.take("ZETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            zeta2: t.take("ZETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            zeta3: t.take("ZETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            zeta4: t.take("ZETA4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_GENERATE {
    pub m1beg: Vec<i64>,
    pub m1end: Vec<i64>,
    pub m2beg: Vec<i64>,
    pub m2end: Vec<i64>,
    pub m3beg: Vec<i64>,
    pub m3end: Vec<i64>,
    pub m4beg: Vec<i64>,
    pub m4end: Vec<i64>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_GENERATE").unwrap());
        Self {
            m1beg: t.take("M1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m1end: t.take("M1END").and_then(|c| c.into_int()).unwrap_or_default(),
            m2beg: t.take("M2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m2end: t.take("M2END").and_then(|c| c.into_int()).unwrap_or_default(),
            m3beg: t.take("M3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m3end: t.take("M3END").and_then(|c| c.into_int()).unwrap_or_default(),
            m4beg: t.take("M4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m4end: t.take("M4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_LIST {
    pub mid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub mid3: Vec<i64>,
    pub mid4: Vec<i64>,
    pub mid5: Vec<i64>,
    pub mid6: Vec<i64>,
    pub mid7: Vec<i64>,
    pub mid8: Vec<i64>,
}
impl CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODAL_DYNAMIC_MODE_LIST").unwrap());
        Self {
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            mid5: t.take("MID5").and_then(|c| c.into_int()).unwrap_or_default(),
            mid6: t.take("MID6").and_then(|c| c.into_int()).unwrap_or_default(),
            mid7: t.take("MID7").and_then(|c| c.into_int()).unwrap_or_default(),
            mid8: t.take("MID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODES {
    pub nsidc: Vec<i64>,
    pub nsida: Vec<i64>,
    pub neig: Vec<i64>,
    pub ibase: Vec<i64>,
    pub se_mass: Vec<String>,
    pub se_damp: Vec<String>,
    pub se_stiff: Vec<String>,
    pub se_inert: Vec<String>,
    pub filename: Vec<String>,
}
impl CONTROL_IMPLICIT_MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODES").unwrap());
        Self {
            nsidc: t.take("NSIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            nsida: t.take("NSIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
            ibase: t.take("IBASE").and_then(|c| c.into_int()).unwrap_or_default(),
            se_mass: t.take("SE_MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            se_damp: t.take("SE_DAMP").and_then(|c| c.into_str()).unwrap_or_default(),
            se_stiff: t.take("SE_STIFF").and_then(|c| c.into_str()).unwrap_or_default(),
            se_inert: t.take("SE_INERT").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_MODES_BINARY {
    pub nsidc: Vec<i64>,
    pub nsida: Vec<i64>,
    pub neig: Vec<i64>,
    pub ibase: Vec<i64>,
    pub se_mass: Vec<String>,
    pub se_damp: Vec<String>,
    pub se_stiff: Vec<String>,
    pub se_inert: Vec<String>,
    pub filename: Vec<String>,
}
impl CONTROL_IMPLICIT_MODES_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_MODES_BINARY").unwrap());
        Self {
            nsidc: t.take("NSIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            nsida: t.take("NSIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
            ibase: t.take("IBASE").and_then(|c| c.into_int()).unwrap_or_default(),
            se_mass: t.take("SE_MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            se_damp: t.take("SE_DAMP").and_then(|c| c.into_str()).unwrap_or_default(),
            se_stiff: t.take("SE_STIFF").and_then(|c| c.into_str()).unwrap_or_default(),
            se_inert: t.take("SE_INERT").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_NONLINEAR {
    pub nsolvr: Vec<i64>,
    pub ilimit: Vec<i64>,
    pub maxref: Vec<i64>,
    pub dctol: Vec<f64>,
    pub ectol: Vec<f64>,
    pub rctol: Vec<f64>,
    pub lstol: Vec<f64>,
    pub abstol: Vec<f64>,
    pub dnorm: Vec<i64>,
    pub diverg: Vec<i64>,
    pub istif: Vec<i64>,
    pub nlprint: Vec<i64>,
    pub nlnorm: Vec<f64>,
    pub d3itctl: Vec<i64>,
    pub cpchk: Vec<i64>,
    pub dmtol: Vec<f64>,
    pub emtol: Vec<f64>,
    pub rmtol: Vec<f64>,
    pub f: Vec<String>,
    pub nttol: Vec<f64>,
    pub nrtol: Vec<f64>,
    pub rttol: Vec<f64>,
    pub rrtol: Vec<f64>,
    pub arcctl: Vec<i64>,
    pub arcdir: Vec<i64>,
    pub arclen: Vec<f64>,
    pub arcmth: Vec<i64>,
    pub arcdmp: Vec<i64>,
    pub arcpsi: Vec<f64>,
    pub arcalf: Vec<f64>,
    pub arctim: Vec<f64>,
    pub lsmtd: Vec<i64>,
    pub lsdir: Vec<i64>,
    pub irad: Vec<f64>,
    pub srad: Vec<f64>,
    pub awgt: Vec<f64>,
    pub sred: Vec<f64>,
}
impl CONTROL_IMPLICIT_NONLINEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_NONLINEAR").unwrap());
        Self {
            nsolvr: t.take("NSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            ilimit: t.take("ILIMIT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxref: t.take("MAXREF").and_then(|c| c.into_int()).unwrap_or_default(),
            dctol: t.take("DCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ectol: t.take("ECTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rctol: t.take("RCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lstol: t.take("LSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dnorm: t.take("DNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            diverg: t.take("DIVERG").and_then(|c| c.into_int()).unwrap_or_default(),
            istif: t.take("ISTIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nlprint: t.take("NLPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nlnorm: t.take("NLNORM").and_then(|c| c.into_float()).unwrap_or_default(),
            d3itctl: t.take("D3ITCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            cpchk: t.take("CPCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            dmtol: t.take("DMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            emtol: t.take("EMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rmtol: t.take("RMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nttol: t.take("NTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrtol: t.take("NRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rttol: t.take("RTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rrtol: t.take("RRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            arcctl: t.take("ARCCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdir: t.take("ARCDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            arclen: t.take("ARCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            arcmth: t.take("ARCMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdmp: t.take("ARCDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            arcpsi: t.take("ARCPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            arcalf: t.take("ARCALF").and_then(|c| c.into_float()).unwrap_or_default(),
            arctim: t.take("ARCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            lsmtd: t.take("LSMTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lsdir: t.take("LSDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            irad: t.take("IRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            awgt: t.take("AWGT").and_then(|c| c.into_float()).unwrap_or_default(),
            sred: t.take("SRED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_ORDERING {
    pub order: Vec<i64>,
    pub nmetis: Vec<i64>,
}
impl CONTROL_IMPLICIT_ORDERING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_ORDERING").unwrap());
        Self {
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
            nmetis: t.take("NMETIS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_RESIDUAL_VECTOR {
    pub iresvec: Vec<i64>,
    pub neig: Vec<i64>,
    pub iformat: Vec<i64>,
    pub rv_filenam: Vec<String>,
}
impl CONTROL_IMPLICIT_RESIDUAL_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_RESIDUAL_VECTOR").unwrap());
        Self {
            iresvec: t.take("IRESVEC").and_then(|c| c.into_int()).unwrap_or_default(),
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
            iformat: t.take("IFORMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            rv_filenam: t.take("RV_FILENAM").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_ROTATIONAL_DYNAMICS {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub omega: Vec<f64>,
    pub vid: Vec<i64>,
    pub nomeg: Vec<i64>,
    pub iref: Vec<i64>,
    pub omegadr: Vec<f64>,
    pub omeg1: Vec<f64>,
    pub omeg2: Vec<f64>,
    pub omeg3: Vec<f64>,
    pub omeg4: Vec<f64>,
    pub omeg5: Vec<f64>,
    pub omeg6: Vec<f64>,
    pub omeg7: Vec<f64>,
    pub omeg8: Vec<f64>,
}
impl CONTROL_IMPLICIT_ROTATIONAL_DYNAMICS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_ROTATIONAL_DYNAMICS").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            nomeg: t.take("NOMEG").and_then(|c| c.into_int()).unwrap_or_default(),
            iref: t.take("IREF").and_then(|c| c.into_int()).unwrap_or_default(),
            omegadr: t.take("OMEGADR").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg1: t.take("OMEG1").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg2: t.take("OMEG2").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg3: t.take("OMEG3").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg4: t.take("OMEG4").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg5: t.take("OMEG5").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg6: t.take("OMEG6").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg7: t.take("OMEG7").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg8: t.take("OMEG8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLUTION {
    pub nsolvr: Vec<i64>,
    pub ilimit: Vec<i64>,
    pub maxref: Vec<i64>,
    pub dctol: Vec<f64>,
    pub ectol: Vec<f64>,
    pub rctol: Vec<f64>,
    pub lstol: Vec<f64>,
    pub abstol: Vec<f64>,
    pub dnorm: Vec<i64>,
    pub diverg: Vec<i64>,
    pub istif: Vec<i64>,
    pub nlprint: Vec<i64>,
    pub nlnorm: Vec<f64>,
    pub d3itctl: Vec<i64>,
    pub cpchk: Vec<i64>,
    pub dmtol: Vec<f64>,
    pub emtol: Vec<f64>,
    pub rmtol: Vec<f64>,
    pub f: Vec<String>,
    pub nttol: Vec<f64>,
    pub nrtol: Vec<f64>,
    pub rttol: Vec<f64>,
    pub rrtol: Vec<f64>,
    pub arcctl: Vec<i64>,
    pub arcdir: Vec<i64>,
    pub arclen: Vec<f64>,
    pub arcmth: Vec<i64>,
    pub arcdmp: Vec<i64>,
    pub arcpsi: Vec<f64>,
    pub arcalf: Vec<f64>,
    pub arctim: Vec<f64>,
    pub lsmtd: Vec<i64>,
    pub lsdir: Vec<i64>,
    pub irad: Vec<f64>,
    pub srad: Vec<f64>,
    pub awgt: Vec<f64>,
    pub sred: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLUTION").unwrap());
        Self {
            nsolvr: t.take("NSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            ilimit: t.take("ILIMIT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxref: t.take("MAXREF").and_then(|c| c.into_int()).unwrap_or_default(),
            dctol: t.take("DCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ectol: t.take("ECTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rctol: t.take("RCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lstol: t.take("LSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dnorm: t.take("DNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            diverg: t.take("DIVERG").and_then(|c| c.into_int()).unwrap_or_default(),
            istif: t.take("ISTIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nlprint: t.take("NLPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nlnorm: t.take("NLNORM").and_then(|c| c.into_float()).unwrap_or_default(),
            d3itctl: t.take("D3ITCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            cpchk: t.take("CPCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            dmtol: t.take("DMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            emtol: t.take("EMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rmtol: t.take("RMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nttol: t.take("NTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrtol: t.take("NRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rttol: t.take("RTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rrtol: t.take("RRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            arcctl: t.take("ARCCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdir: t.take("ARCDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            arclen: t.take("ARCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            arcmth: t.take("ARCMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdmp: t.take("ARCDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            arcpsi: t.take("ARCPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            arcalf: t.take("ARCALF").and_then(|c| c.into_float()).unwrap_or_default(),
            arctim: t.take("ARCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            lsmtd: t.take("LSMTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lsdir: t.take("LSDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            irad: t.take("IRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            awgt: t.take("AWGT").and_then(|c| c.into_float()).unwrap_or_default(),
            sred: t.take("SRED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLUTION_DYN {
    pub nsolvr: Vec<i64>,
    pub ilimit: Vec<i64>,
    pub maxref: Vec<i64>,
    pub dctol: Vec<f64>,
    pub ectol: Vec<f64>,
    pub rctol: Vec<f64>,
    pub lstol: Vec<f64>,
    pub abstol: Vec<f64>,
    pub dnorm: Vec<i64>,
    pub diverg: Vec<i64>,
    pub istif: Vec<i64>,
    pub nlprint: Vec<i64>,
    pub nlnorm: Vec<f64>,
    pub d3itctl: Vec<i64>,
    pub cpchk: Vec<i64>,
    pub dmtol: Vec<f64>,
    pub emtol: Vec<f64>,
    pub rmtol: Vec<f64>,
    pub f: Vec<String>,
    pub nttol: Vec<f64>,
    pub nrtol: Vec<f64>,
    pub rttol: Vec<f64>,
    pub rrtol: Vec<f64>,
    pub arcctl: Vec<i64>,
    pub arcdir: Vec<i64>,
    pub arclen: Vec<f64>,
    pub arcmth: Vec<i64>,
    pub arcdmp: Vec<i64>,
    pub arcpsi: Vec<f64>,
    pub arcalf: Vec<f64>,
    pub arctim: Vec<f64>,
    pub lsmtd: Vec<i64>,
    pub lsdir: Vec<i64>,
    pub irad: Vec<f64>,
    pub srad: Vec<f64>,
    pub awgt: Vec<f64>,
    pub sred: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLUTION_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLUTION_DYN").unwrap());
        Self {
            nsolvr: t.take("NSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            ilimit: t.take("ILIMIT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxref: t.take("MAXREF").and_then(|c| c.into_int()).unwrap_or_default(),
            dctol: t.take("DCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ectol: t.take("ECTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rctol: t.take("RCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lstol: t.take("LSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dnorm: t.take("DNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            diverg: t.take("DIVERG").and_then(|c| c.into_int()).unwrap_or_default(),
            istif: t.take("ISTIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nlprint: t.take("NLPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nlnorm: t.take("NLNORM").and_then(|c| c.into_float()).unwrap_or_default(),
            d3itctl: t.take("D3ITCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            cpchk: t.take("CPCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            dmtol: t.take("DMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            emtol: t.take("EMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rmtol: t.take("RMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nttol: t.take("NTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrtol: t.take("NRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rttol: t.take("RTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rrtol: t.take("RRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            arcctl: t.take("ARCCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdir: t.take("ARCDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            arclen: t.take("ARCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            arcmth: t.take("ARCMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdmp: t.take("ARCDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            arcpsi: t.take("ARCPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            arcalf: t.take("ARCALF").and_then(|c| c.into_float()).unwrap_or_default(),
            arctim: t.take("ARCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            lsmtd: t.take("LSMTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lsdir: t.take("LSDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            irad: t.take("IRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            awgt: t.take("AWGT").and_then(|c| c.into_float()).unwrap_or_default(),
            sred: t.take("SRED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLUTION_SPR {
    pub nsolvr: Vec<i64>,
    pub ilimit: Vec<i64>,
    pub maxref: Vec<i64>,
    pub dctol: Vec<f64>,
    pub ectol: Vec<f64>,
    pub rctol: Vec<f64>,
    pub lstol: Vec<f64>,
    pub abstol: Vec<f64>,
    pub dnorm: Vec<i64>,
    pub diverg: Vec<i64>,
    pub istif: Vec<i64>,
    pub nlprint: Vec<i64>,
    pub nlnorm: Vec<f64>,
    pub d3itctl: Vec<i64>,
    pub cpchk: Vec<i64>,
    pub dmtol: Vec<f64>,
    pub emtol: Vec<f64>,
    pub rmtol: Vec<f64>,
    pub f: Vec<String>,
    pub nttol: Vec<f64>,
    pub nrtol: Vec<f64>,
    pub rttol: Vec<f64>,
    pub rrtol: Vec<f64>,
    pub arcctl: Vec<i64>,
    pub arcdir: Vec<i64>,
    pub arclen: Vec<f64>,
    pub arcmth: Vec<i64>,
    pub arcdmp: Vec<i64>,
    pub arcpsi: Vec<f64>,
    pub arcalf: Vec<f64>,
    pub arctim: Vec<f64>,
    pub lsmtd: Vec<i64>,
    pub lsdir: Vec<i64>,
    pub irad: Vec<f64>,
    pub srad: Vec<f64>,
    pub awgt: Vec<f64>,
    pub sred: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLUTION_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLUTION_SPR").unwrap());
        Self {
            nsolvr: t.take("NSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            ilimit: t.take("ILIMIT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxref: t.take("MAXREF").and_then(|c| c.into_int()).unwrap_or_default(),
            dctol: t.take("DCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ectol: t.take("ECTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rctol: t.take("RCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lstol: t.take("LSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dnorm: t.take("DNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            diverg: t.take("DIVERG").and_then(|c| c.into_int()).unwrap_or_default(),
            istif: t.take("ISTIF").and_then(|c| c.into_int()).unwrap_or_default(),
            nlprint: t.take("NLPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nlnorm: t.take("NLNORM").and_then(|c| c.into_float()).unwrap_or_default(),
            d3itctl: t.take("D3ITCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            cpchk: t.take("CPCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            dmtol: t.take("DMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            emtol: t.take("EMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rmtol: t.take("RMTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nttol: t.take("NTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrtol: t.take("NRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rttol: t.take("RTTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rrtol: t.take("RRTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            arcctl: t.take("ARCCTL").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdir: t.take("ARCDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            arclen: t.take("ARCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            arcmth: t.take("ARCMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            arcdmp: t.take("ARCDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            arcpsi: t.take("ARCPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            arcalf: t.take("ARCALF").and_then(|c| c.into_float()).unwrap_or_default(),
            arctim: t.take("ARCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            lsmtd: t.take("LSMTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lsdir: t.take("LSDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            irad: t.take("IRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            awgt: t.take("AWGT").and_then(|c| c.into_float()).unwrap_or_default(),
            sred: t.take("SRED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLVER {
    pub lsolvr: Vec<i64>,
    pub lprint: Vec<i64>,
    pub negev: Vec<i64>,
    pub order: Vec<i64>,
    pub drcm: Vec<i64>,
    pub drcprm: Vec<f64>,
    pub autospc: Vec<i64>,
    pub autotol: Vec<f64>,
    pub lcpack: Vec<i64>,
    pub mtxdmp: Vec<i64>,
    pub iparm1: Vec<i64>,
    pub rparm1: Vec<f64>,
    pub rparm2: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub rparm5: Vec<f64>,
    pub emxdmp: Vec<i64>,
    pub rdcmem: Vec<f64>,
    pub absmem: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLVER").unwrap());
        Self {
            lsolvr: t.take("LSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            lprint: t.take("LPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            negev: t.take("NEGEV").and_then(|c| c.into_int()).unwrap_or_default(),
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
            drcm: t.take("DRCM").and_then(|c| c.into_int()).unwrap_or_default(),
            drcprm: t.take("DRCPRM").and_then(|c| c.into_float()).unwrap_or_default(),
            autospc: t.take("AUTOSPC").and_then(|c| c.into_int()).unwrap_or_default(),
            autotol: t.take("AUTOTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpack: t.take("LCPACK").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxdmp: t.take("MTXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm1: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1: t.take("RPARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            rparm2: t.take("RPARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm5: t.take("RPARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            emxdmp: t.take("EMXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            rdcmem: t.take("RDCMEM").and_then(|c| c.into_float()).unwrap_or_default(),
            absmem: t.take("ABSMEM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLVER_DYN {
    pub lsolvr: Vec<i64>,
    pub lprint: Vec<i64>,
    pub negev: Vec<i64>,
    pub order: Vec<i64>,
    pub drcm: Vec<i64>,
    pub drcprm: Vec<f64>,
    pub autospc: Vec<i64>,
    pub autotol: Vec<f64>,
    pub lcpack: Vec<i64>,
    pub mtxdmp: Vec<i64>,
    pub iparm1: Vec<i64>,
    pub rparm1: Vec<f64>,
    pub rparm2: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub rparm5: Vec<f64>,
    pub emxdmp: Vec<i64>,
    pub rdcmem: Vec<f64>,
    pub absmem: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLVER_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLVER_DYN").unwrap());
        Self {
            lsolvr: t.take("LSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            lprint: t.take("LPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            negev: t.take("NEGEV").and_then(|c| c.into_int()).unwrap_or_default(),
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
            drcm: t.take("DRCM").and_then(|c| c.into_int()).unwrap_or_default(),
            drcprm: t.take("DRCPRM").and_then(|c| c.into_float()).unwrap_or_default(),
            autospc: t.take("AUTOSPC").and_then(|c| c.into_int()).unwrap_or_default(),
            autotol: t.take("AUTOTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpack: t.take("LCPACK").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxdmp: t.take("MTXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm1: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1: t.take("RPARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            rparm2: t.take("RPARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm5: t.take("RPARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            emxdmp: t.take("EMXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            rdcmem: t.take("RDCMEM").and_then(|c| c.into_float()).unwrap_or_default(),
            absmem: t.take("ABSMEM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SOLVER_SPR {
    pub lsolvr: Vec<i64>,
    pub lprint: Vec<i64>,
    pub negev: Vec<i64>,
    pub order: Vec<i64>,
    pub drcm: Vec<i64>,
    pub drcprm: Vec<f64>,
    pub autospc: Vec<i64>,
    pub autotol: Vec<f64>,
    pub lcpack: Vec<i64>,
    pub mtxdmp: Vec<i64>,
    pub iparm1: Vec<i64>,
    pub rparm1: Vec<f64>,
    pub rparm2: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub rparm5: Vec<f64>,
    pub emxdmp: Vec<i64>,
    pub rdcmem: Vec<f64>,
    pub absmem: Vec<f64>,
}
impl CONTROL_IMPLICIT_SOLVER_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SOLVER_SPR").unwrap());
        Self {
            lsolvr: t.take("LSOLVR").and_then(|c| c.into_int()).unwrap_or_default(),
            lprint: t.take("LPRINT").and_then(|c| c.into_int()).unwrap_or_default(),
            negev: t.take("NEGEV").and_then(|c| c.into_int()).unwrap_or_default(),
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
            drcm: t.take("DRCM").and_then(|c| c.into_int()).unwrap_or_default(),
            drcprm: t.take("DRCPRM").and_then(|c| c.into_float()).unwrap_or_default(),
            autospc: t.take("AUTOSPC").and_then(|c| c.into_int()).unwrap_or_default(),
            autotol: t.take("AUTOTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpack: t.take("LCPACK").and_then(|c| c.into_int()).unwrap_or_default(),
            mtxdmp: t.take("MTXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            iparm1: t.take("IPARM1").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm1: t.take("RPARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            rparm2: t.take("RPARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rparm5: t.take("RPARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            emxdmp: t.take("EMXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            rdcmem: t.take("RDCMEM").and_then(|c| c.into_float()).unwrap_or_default(),
            absmem: t.take("ABSMEM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_SSD_DIRECT {
    pub issflg: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub loss: Vec<f64>,
    pub fspace: Vec<f64>,
    pub fractn: Vec<i64>,
}
impl CONTROL_IMPLICIT_SSD_DIRECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_SSD_DIRECT").unwrap());
        Self {
            issflg: t.take("ISSFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            loss: t.take("LOSS").and_then(|c| c.into_float()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_float()).unwrap_or_default(),
            fractn: t.take("FRACTN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STABILISATION {
    pub ias: Vec<i64>,
    pub scale: Vec<f64>,
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
}
impl CONTROL_IMPLICIT_STABILISATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STABILISATION").unwrap());
        Self {
            ias: t.take("IAS").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STABILIZATION {
    pub ias: Vec<i64>,
    pub scale: Vec<f64>,
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
}
impl CONTROL_IMPLICIT_STABILIZATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STABILIZATION").unwrap());
        Self {
            ias: t.take("IAS").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STABILIZATION_DYN {
    pub ias: Vec<i64>,
    pub scale: Vec<f64>,
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
}
impl CONTROL_IMPLICIT_STABILIZATION_DYN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STABILIZATION_DYN").unwrap());
        Self {
            ias: t.take("IAS").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STABILIZATION_SPR {
    pub ias: Vec<i64>,
    pub scale: Vec<f64>,
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
}
impl CONTROL_IMPLICIT_STABILIZATION_SPR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STABILIZATION_SPR").unwrap());
        Self {
            ias: t.take("IAS").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STATIC_CONDENSATION {
    pub sc_flag: Vec<i64>,
    pub sc_nsid: Vec<i64>,
    pub sc_psid: Vec<i64>,
    pub se_mass: Vec<String>,
    pub se_stiff: Vec<String>,
    pub se_inert: Vec<String>,
    pub filename: Vec<String>,
}
impl CONTROL_IMPLICIT_STATIC_CONDENSATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STATIC_CONDENSATION").unwrap());
        Self {
            sc_flag: t.take("SC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sc_nsid: t.take("SC_NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sc_psid: t.take("SC_PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            se_mass: t.take("SE_MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            se_stiff: t.take("SE_STIFF").and_then(|c| c.into_str()).unwrap_or_default(),
            se_inert: t.take("SE_INERT").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_STATIC_CONDENSATION_BINARY {
    pub sc_flag: Vec<i64>,
    pub sc_nsid: Vec<i64>,
    pub sc_psid: Vec<i64>,
    pub se_mass: Vec<String>,
    pub se_stiff: Vec<String>,
    pub se_inert: Vec<String>,
    pub filename: Vec<String>,
}
impl CONTROL_IMPLICIT_STATIC_CONDENSATION_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_STATIC_CONDENSATION_BINARY").unwrap());
        Self {
            sc_flag: t.take("SC_FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sc_nsid: t.take("SC_NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sc_psid: t.take("SC_PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            se_mass: t.take("SE_MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            se_stiff: t.take("SE_STIFF").and_then(|c| c.into_str()).unwrap_or_default(),
            se_inert: t.take("SE_INERT").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_IMPLICIT_TERMINATION {
    pub deltau: Vec<f64>,
    pub delta1: Vec<f64>,
    pub ketol: Vec<f64>,
    pub ietol: Vec<f64>,
    pub tetol: Vec<f64>,
    pub nstep: Vec<i64>,
}
impl CONTROL_IMPLICIT_TERMINATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_IMPLICIT_TERMINATION").unwrap());
        Self {
            deltau: t.take("DELTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            delta1: t.take("DELTA1").and_then(|c| c.into_float()).unwrap_or_default(),
            ketol: t.take("KETOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ietol: t.take("IETOL").and_then(|c| c.into_float()).unwrap_or_default(),
            tetol: t.take("TETOL").and_then(|c| c.into_float()).unwrap_or_default(),
            nstep: t.take("NSTEP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_LSDA {
    pub npexcl: Vec<i64>,
    pub p1: Vec<i64>,
    pub p2: Vec<i64>,
    pub p3: Vec<i64>,
    pub p4: Vec<i64>,
    pub p5: Vec<i64>,
    pub p6: Vec<i64>,
    pub p7: Vec<i64>,
    pub p8: Vec<i64>,
}
impl CONTROL_LSDA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_LSDA").unwrap());
        Self {
            npexcl: t.take("NPEXCL").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_int()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_int()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_int()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_int()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_int()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_int()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_int()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MAT {
    pub maef: Vec<i64>,
    pub f: Vec<i64>,
    pub umchk: Vec<i64>,
}
impl CONTROL_MAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MAT").unwrap());
        Self {
            maef: t.take("MAEF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            umchk: t.take("UMCHK").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_CONTACT_GROUPABLE {
    pub grp: Vec<i64>,
}
impl CONTROL_MPP_CONTACT_GROUPABLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_CONTACT_GROUPABLE").unwrap());
        Self {
            grp: t.take("GRP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_ADAPTIVE {
    pub freq: Vec<f64>,
    pub defgeo: Vec<i64>,
    pub cweight: Vec<f64>,
    pub f: Vec<f64>,
    pub stime: Vec<f64>,
}
impl CONTROL_MPP_DECOMPOSITION_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_ADAPTIVE").unwrap());
        Self {
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            defgeo: t.take("DEFGEO").and_then(|c| c.into_int()).unwrap_or_default(),
            cweight: t.take("CWEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_ARRANGE_PARTS {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nproc: Vec<i64>,
    pub frstp: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_ARRANGE_PARTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_ARRANGE_PARTS").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nproc: t.take("NPROC").and_then(|c| c.into_int()).unwrap_or_default(),
            frstp: t.take("FRSTP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_AUTOMATIC {
}
impl CONTROL_MPP_DECOMPOSITION_AUTOMATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_AUTOMATIC").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_BAGREF {
}
impl CONTROL_MPP_DECOMPOSITION_BAGREF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_BAGREF").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_CHECK_SPEED {
}
impl CONTROL_MPP_DECOMPOSITION_CHECK_SPEED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_CHECK_SPEED").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_CONTACT_DISTRIBUTE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_CONTACT_DISTRIBUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_CONTACT_DISTRIBUTE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_CONTACT_ISOLATE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_CONTACT_ISOLATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_CONTACT_ISOLATE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_DISABLE_UNREF_CURVES {
}
impl CONTROL_MPP_DECOMPOSITION_DISABLE_UNREF_CURVES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_DISABLE_UNREF_CURVES").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENT {
}
impl CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENT").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENTS {
}
impl CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_ALE_ELEMENTS").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENT {
}
impl CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENT").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENTS {
}
impl CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_DISTRIBUTE_SPH_ELEMENTS").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_ELCOST {
    pub itype: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_ELCOST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_ELCOST").unwrap());
        Self {
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_FILE {
    pub filename: Vec<String>,
}
impl CONTROL_MPP_DECOMPOSITION_FILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_FILE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_FILE_READ {
    pub filename: Vec<String>,
}
impl CONTROL_MPP_DECOMPOSITION_FILE_READ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_FILE_READ").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_FILE_WRITE {
    pub filename: Vec<String>,
}
impl CONTROL_MPP_DECOMPOSITION_FILE_WRITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_FILE_WRITE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_METHOD {
    pub name: Vec<String>,
}
impl CONTROL_MPP_DECOMPOSITION_METHOD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_METHOD").unwrap());
        Self {
            name: t.take("Name").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_NODISTRIBUTE_DES_ELEMENTS {
}
impl CONTROL_MPP_DECOMPOSITION_NODISTRIBUTE_DES_ELEMENTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_NODISTRIBUTE_DES_ELEMENTS").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_NUMPROC {
    pub i: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_NUMPROC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_NUMPROC").unwrap());
        Self {
            i: t.take("I").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_OUTDECOMP {
    pub itype: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_OUTDECOMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_OUTDECOMP").unwrap());
        Self {
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_PARTSET_DISTRIBUTE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_PARTSET_DISTRIBUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_PARTSET_DISTRIBUTE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_PARTS_DISTRIBUTE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
    pub id9: Vec<i64>,
    pub id10: Vec<i64>,
    pub id11: Vec<i64>,
    pub id12: Vec<i64>,
    pub id13: Vec<i64>,
    pub id14: Vec<i64>,
    pub id15: Vec<i64>,
    pub id16: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_PARTS_DISTRIBUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_PARTS_DISTRIBUTE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
            id9: t.take("ID9").and_then(|c| c.into_int()).unwrap_or_default(),
            id10: t.take("ID10").and_then(|c| c.into_int()).unwrap_or_default(),
            id11: t.take("ID11").and_then(|c| c.into_int()).unwrap_or_default(),
            id12: t.take("ID12").and_then(|c| c.into_int()).unwrap_or_default(),
            id13: t.take("ID13").and_then(|c| c.into_int()).unwrap_or_default(),
            id14: t.take("ID14").and_then(|c| c.into_int()).unwrap_or_default(),
            id15: t.take("ID15").and_then(|c| c.into_int()).unwrap_or_default(),
            id16: t.take("ID16").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_RCBLOG {
    pub name: Vec<String>,
}
impl CONTROL_MPP_DECOMPOSITION_RCBLOG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_RCBLOG").unwrap());
        Self {
            name: t.take("Name").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION {
    pub freq: Vec<f64>,
    pub defgeo: Vec<i64>,
    pub weight: Vec<f64>,
    pub remsph: Vec<i64>,
    pub stime: Vec<f64>,
    pub sampt: Vec<f64>,
}
impl CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION").unwrap());
        Self {
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            defgeo: t.take("DEFGEO").and_then(|c| c.into_int()).unwrap_or_default(),
            weight: t.take("WEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            remsph: t.take("REMSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
            sampt: t.take("SAMPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION_ONCE {
    pub freq: Vec<f64>,
    pub defgeo: Vec<i64>,
    pub weight: Vec<f64>,
    pub remsph: Vec<i64>,
    pub stime: Vec<f64>,
    pub sampt: Vec<f64>,
}
impl CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION_ONCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_REDECOMPOSITION_ONCE").unwrap());
        Self {
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            defgeo: t.take("DEFGEO").and_then(|c| c.into_int()).unwrap_or_default(),
            weight: t.take("WEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            remsph: t.take("REMSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
            sampt: t.take("SAMPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_SCALE_CONTACT_COST {
    pub sf: Vec<f64>,
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
    pub id9: Vec<i64>,
    pub id10: Vec<i64>,
    pub id11: Vec<i64>,
    pub id12: Vec<i64>,
    pub id13: Vec<i64>,
    pub id14: Vec<i64>,
    pub id15: Vec<i64>,
}
impl CONTROL_MPP_DECOMPOSITION_SCALE_CONTACT_COST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_SCALE_CONTACT_COST").unwrap());
        Self {
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
            id9: t.take("ID9").and_then(|c| c.into_int()).unwrap_or_default(),
            id10: t.take("ID10").and_then(|c| c.into_int()).unwrap_or_default(),
            id11: t.take("ID11").and_then(|c| c.into_int()).unwrap_or_default(),
            id12: t.take("ID12").and_then(|c| c.into_int()).unwrap_or_default(),
            id13: t.take("ID13").and_then(|c| c.into_int()).unwrap_or_default(),
            id14: t.take("ID14").and_then(|c| c.into_int()).unwrap_or_default(),
            id15: t.take("ID15").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_SCALE_FACTOR_SPH {
    pub sf: Vec<f64>,
}
impl CONTROL_MPP_DECOMPOSITION_SCALE_FACTOR_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_SCALE_FACTOR_SPH").unwrap());
        Self {
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_SHOW {
}
impl CONTROL_MPP_DECOMPOSITION_SHOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_SHOW").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_DECOMPOSITION_TRANSFORMATION {
    pub type_: Vec<String>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub v4: Vec<f64>,
    pub v5: Vec<f64>,
    pub v6: Vec<f64>,
    pub v7: Vec<f64>,
    pub v8: Vec<f64>,
    pub v9: Vec<f64>,
}
impl CONTROL_MPP_DECOMPOSITION_TRANSFORMATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_DECOMPOSITION_TRANSFORMATION").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            v4: t.take("V4").and_then(|c| c.into_float()).unwrap_or_default(),
            v5: t.take("V5").and_then(|c| c.into_float()).unwrap_or_default(),
            v6: t.take("V6").and_then(|c| c.into_float()).unwrap_or_default(),
            v7: t.take("V7").and_then(|c| c.into_float()).unwrap_or_default(),
            v8: t.take("V8").and_then(|c| c.into_float()).unwrap_or_default(),
            v9: t.take("V9").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_IO_BINOUTONLY {
}
impl CONTROL_MPP_IO_BINOUTONLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_BINOUTONLY").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_LSTC_REDUCE {
}
impl CONTROL_MPP_IO_LSTC_REDUCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_LSTC_REDUCE").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_NOBEAMOUT {
}
impl CONTROL_MPP_IO_NOBEAMOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_NOBEAMOUT").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_NOD3DUMP {
}
impl CONTROL_MPP_IO_NOD3DUMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_NOD3DUMP").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_NODUMP {
}
impl CONTROL_MPP_IO_NODUMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_NODUMP").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_NOFULL {
}
impl CONTROL_MPP_IO_NOFULL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_NOFULL").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_IO_SWAPBYTES {
}
impl CONTROL_MPP_IO_SWAPBYTES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_IO_SWAPBYTES").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_MPP_PFILE {
    pub info: Vec<String>,
}
impl CONTROL_MPP_PFILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_PFILE").unwrap());
        Self {
            info: t.take("INFO").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_MPP_REBALANCE {
    pub ncycle: Vec<i64>,
    pub icoor: Vec<i64>,
    pub icost: Vec<i64>,
    pub thres: Vec<f64>,
}
impl CONTROL_MPP_REBALANCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_MPP_REBALANCE").unwrap());
        Self {
            ncycle: t.take("NCYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            icoor: t.take("ICOOR").and_then(|c| c.into_int()).unwrap_or_default(),
            icost: t.take("ICOST").and_then(|c| c.into_int()).unwrap_or_default(),
            thres: t.take("THRES").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_NONLOCAL {
    pub mem: Vec<i64>,
}
impl CONTROL_NONLOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_NONLOCAL").unwrap());
        Self {
            mem: t.take("MEM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_OUTPUT {
    pub npopt: Vec<i64>,
    pub neecho: Vec<i64>,
    pub nrefup: Vec<i64>,
    pub iaccop: Vec<i64>,
    pub opifs: Vec<f64>,
    pub ipnint: Vec<i64>,
    pub ikedit: Vec<i64>,
    pub iflush: Vec<i64>,
    pub iprtf: Vec<i64>,
    pub ierode: Vec<i64>,
    pub tet10s8: Vec<i64>,
    pub msgmax: Vec<i64>,
    pub ipcurv: Vec<i64>,
    pub gmdt: Vec<f64>,
    pub ip1dblt: Vec<i64>,
    pub eocs: Vec<i64>,
    pub tolev: Vec<i64>,
    pub newleg: Vec<i64>,
    pub frfreq: Vec<i64>,
    pub minfo: Vec<i64>,
    pub solsig: Vec<i64>,
    pub msgflg: Vec<i64>,
    pub cdetol: Vec<f64>,
    pub phschng: Vec<i64>,
    pub demden: Vec<i64>,
    pub icrfile: Vec<i64>,
    pub spc2bnd: Vec<i64>,
    pub penout: Vec<i64>,
    pub shlsig: Vec<i64>,
    pub hisnout: Vec<i64>,
    pub engout: Vec<i64>,
    pub insf: Vec<i64>,
    pub isolsf: Vec<i64>,
    pub ibsf: Vec<i64>,
    pub issf: Vec<i64>,
    pub mlkbag: Vec<i64>,
}
impl CONTROL_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_OUTPUT").unwrap());
        Self {
            npopt: t.take("NPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            neecho: t.take("NEECHO").and_then(|c| c.into_int()).unwrap_or_default(),
            nrefup: t.take("NREFUP").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccop: t.take("IACCOP").and_then(|c| c.into_int()).unwrap_or_default(),
            opifs: t.take("OPIFS").and_then(|c| c.into_float()).unwrap_or_default(),
            ipnint: t.take("IPNINT").and_then(|c| c.into_int()).unwrap_or_default(),
            ikedit: t.take("IKEDIT").and_then(|c| c.into_int()).unwrap_or_default(),
            iflush: t.take("IFLUSH").and_then(|c| c.into_int()).unwrap_or_default(),
            iprtf: t.take("IPRTF").and_then(|c| c.into_int()).unwrap_or_default(),
            ierode: t.take("IERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            tet10s8: t.take("TET10S8").and_then(|c| c.into_int()).unwrap_or_default(),
            msgmax: t.take("MSGMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            ipcurv: t.take("IPCURV").and_then(|c| c.into_int()).unwrap_or_default(),
            gmdt: t.take("GMDT").and_then(|c| c.into_float()).unwrap_or_default(),
            ip1dblt: t.take("IP1DBLT").and_then(|c| c.into_int()).unwrap_or_default(),
            eocs: t.take("EOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tolev: t.take("TOLEV").and_then(|c| c.into_int()).unwrap_or_default(),
            newleg: t.take("NEWLEG").and_then(|c| c.into_int()).unwrap_or_default(),
            frfreq: t.take("FRFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            minfo: t.take("MINFO").and_then(|c| c.into_int()).unwrap_or_default(),
            solsig: t.take("SOLSIG").and_then(|c| c.into_int()).unwrap_or_default(),
            msgflg: t.take("MSGFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            cdetol: t.take("CDETOL").and_then(|c| c.into_float()).unwrap_or_default(),
            phschng: t.take("PHSCHNG").and_then(|c| c.into_int()).unwrap_or_default(),
            demden: t.take("DEMDEN").and_then(|c| c.into_int()).unwrap_or_default(),
            icrfile: t.take("ICRFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            spc2bnd: t.take("SPC2BND").and_then(|c| c.into_int()).unwrap_or_default(),
            penout: t.take("PENOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            shlsig: t.take("SHLSIG").and_then(|c| c.into_int()).unwrap_or_default(),
            hisnout: t.take("HISNOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            engout: t.take("ENGOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            insf: t.take("INSF").and_then(|c| c.into_int()).unwrap_or_default(),
            isolsf: t.take("ISOLSF").and_then(|c| c.into_int()).unwrap_or_default(),
            ibsf: t.take("IBSF").and_then(|c| c.into_int()).unwrap_or_default(),
            issf: t.take("ISSF").and_then(|c| c.into_int()).unwrap_or_default(),
            mlkbag: t.take("MLKBAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_PARALLEL {
    pub ncpu: Vec<i64>,
    pub numrhs: Vec<i64>,
    pub const_: Vec<i64>,
    pub para: Vec<i64>,
}
impl CONTROL_PARALLEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_PARALLEL").unwrap());
        Self {
            ncpu: t.take("NCPU").and_then(|c| c.into_int()).unwrap_or_default(),
            numrhs: t.take("NUMRHS").and_then(|c| c.into_int()).unwrap_or_default(),
            const_: t.take("CONST").and_then(|c| c.into_int()).unwrap_or_default(),
            para: t.take("PARA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_PORE_AIR {
    pub pa_rho: Vec<f64>,
    pub air_p: Vec<f64>,
    pub eterm: Vec<f64>,
    pub anamsg: Vec<i64>,
}
impl CONTROL_PORE_AIR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_PORE_AIR").unwrap());
        Self {
            pa_rho: t.take("PA_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            air_p: t.take("AIR_P").and_then(|c| c.into_float()).unwrap_or_default(),
            eterm: t.take("ETERM").and_then(|c| c.into_float()).unwrap_or_default(),
            anamsg: t.take("ANAMSG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_PORE_FLUID {
    pub atype: Vec<i64>,
    pub f10: Vec<f64>,
    pub wtable: Vec<f64>,
    pub pf_rho: Vec<f64>,
    pub grav: Vec<f64>,
    pub pf_bulk: Vec<f64>,
    pub output: Vec<i64>,
    pub tmf: Vec<f64>,
    pub targ: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub ftied: Vec<f64>,
    pub conv: Vec<f64>,
    pub conmax: Vec<f64>,
    pub eterm: Vec<f64>,
    pub therm: Vec<f64>,
    pub etfag: Vec<i64>,
}
impl CONTROL_PORE_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_PORE_FLUID").unwrap());
        Self {
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            wtable: t.take("WTABLE").and_then(|c| c.into_float()).unwrap_or_default(),
            pf_rho: t.take("PF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            pf_bulk: t.take("PF_BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmf: t.take("TMF").and_then(|c| c.into_float()).unwrap_or_default(),
            targ: t.take("TARG").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ftied: t.take("FTIED").and_then(|c| c.into_float()).unwrap_or_default(),
            conv: t.take("CONV").and_then(|c| c.into_float()).unwrap_or_default(),
            conmax: t.take("CONMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            eterm: t.take("ETERM").and_then(|c| c.into_float()).unwrap_or_default(),
            therm: t.take("THERM").and_then(|c| c.into_float()).unwrap_or_default(),
            etfag: t.take("ETFAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_PZELECTRIC {
    pub solver: Vec<i64>,
    pub msgitr: Vec<i64>,
    pub maxitr: Vec<i64>,
    pub abstol: Vec<f64>,
    pub reltol: Vec<i64>,
    pub ndtrfk: Vec<i64>,
    pub epzmsg: Vec<i64>,
}
impl CONTROL_PZELECTRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_PZELECTRIC").unwrap());
        Self {
            solver: t.take("SOLVER").and_then(|c| c.into_int()).unwrap_or_default(),
            msgitr: t.take("MSGITR").and_then(|c| c.into_int()).unwrap_or_default(),
            maxitr: t.take("MAXITR").and_then(|c| c.into_int()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_int()).unwrap_or_default(),
            ndtrfk: t.take("NDTRFK").and_then(|c| c.into_int()).unwrap_or_default(),
            epzmsg: t.take("EPZMSG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFERENCE_CONFIGURAION {
    pub maxiter: Vec<i64>,
    pub target: Vec<String>,
    pub method: Vec<i64>,
    pub step: Vec<f64>,
    pub tol: Vec<f64>,
}
impl CONTROL_REFERENCE_CONFIGURAION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFERENCE_CONFIGURAION").unwrap());
        Self {
            maxiter: t.take("MAXITER").and_then(|c| c.into_int()).unwrap_or_default(),
            target: t.take("TARGET").and_then(|c| c.into_str()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            step: t.take("STEP").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFERENCE_CONFIGURAION_ITER {
    pub maxiter: Vec<i64>,
    pub target: Vec<String>,
    pub method: Vec<i64>,
    pub step: Vec<f64>,
    pub tol: Vec<f64>,
    pub iter: Vec<String>,
}
impl CONTROL_REFERENCE_CONFIGURAION_ITER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFERENCE_CONFIGURAION_ITER").unwrap());
        Self {
            maxiter: t.take("MAXITER").and_then(|c| c.into_int()).unwrap_or_default(),
            target: t.take("TARGET").and_then(|c| c.into_str()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            step: t.take("STEP").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFERENCE_CONTROL_VOLUME_COORDINATES {
    pub filename: Vec<String>,
    pub opt: Vec<i64>,
    pub psid: Vec<i64>,
}
impl CONTROL_REFERENCE_CONTROL_VOLUME_COORDINATES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFERENCE_CONTROL_VOLUME_COORDINATES").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFINE_ALE {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nlvl: Vec<i64>,
    pub mmsid: Vec<i64>,
    pub ibox: Vec<i64>,
    pub ielout: Vec<i64>,
    pub ntotrf: Vec<i64>,
    pub ncycrf: Vec<f64>,
    pub critrf: Vec<i64>,
    pub valrf: Vec<f64>,
    pub begrf: Vec<f64>,
    pub endrf: Vec<f64>,
    pub layrf: Vec<i64>,
    pub delayrf: Vec<f64>,
    pub maxrm: Vec<i64>,
    pub ncycrm: Vec<f64>,
    pub critrm: Vec<i64>,
    pub valrm: Vec<f64>,
    pub begrm: Vec<f64>,
    pub endrm: Vec<f64>,
    pub mmsrm: Vec<i64>,
    pub delayrm: Vec<f64>,
}
impl CONTROL_REFINE_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFINE_ALE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nlvl: t.take("NLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            mmsid: t.take("MMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox: t.take("IBOX").and_then(|c| c.into_int()).unwrap_or_default(),
            ielout: t.take("IELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            ntotrf: t.take("NTOTRF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrf: t.take("NCYCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            critrf: t.take("CRITRF").and_then(|c| c.into_int()).unwrap_or_default(),
            valrf: t.take("VALRF").and_then(|c| c.into_float()).unwrap_or_default(),
            begrf: t.take("BEGRF").and_then(|c| c.into_float()).unwrap_or_default(),
            endrf: t.take("ENDRF").and_then(|c| c.into_float()).unwrap_or_default(),
            layrf: t.take("LAYRF").and_then(|c| c.into_int()).unwrap_or_default(),
            delayrf: t.take("DELAYRF").and_then(|c| c.into_float()).unwrap_or_default(),
            maxrm: t.take("MAXRM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrm: t.take("NCYCRM").and_then(|c| c.into_float()).unwrap_or_default(),
            critrm: t.take("CRITRM").and_then(|c| c.into_int()).unwrap_or_default(),
            valrm: t.take("VALRM").and_then(|c| c.into_float()).unwrap_or_default(),
            begrm: t.take("BEGRM").and_then(|c| c.into_float()).unwrap_or_default(),
            endrm: t.take("ENDRM").and_then(|c| c.into_float()).unwrap_or_default(),
            mmsrm: t.take("MMSRM").and_then(|c| c.into_int()).unwrap_or_default(),
            delayrm: t.take("DELAYRM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFINE_ALE2D {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nlvl: Vec<i64>,
    pub mmsid: Vec<i64>,
    pub ibox: Vec<i64>,
    pub ielout: Vec<i64>,
    pub ntotrf: Vec<i64>,
    pub ncycrf: Vec<f64>,
    pub critrf: Vec<i64>,
    pub valrf: Vec<f64>,
    pub begrf: Vec<f64>,
    pub endrf: Vec<f64>,
    pub layrf: Vec<i64>,
    pub maxrm: Vec<i64>,
    pub ncycrm: Vec<f64>,
    pub critrm: Vec<i64>,
    pub valrm: Vec<f64>,
    pub begrm: Vec<f64>,
    pub endrm: Vec<f64>,
    pub mmsrm: Vec<i64>,
}
impl CONTROL_REFINE_ALE2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFINE_ALE2D").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nlvl: t.take("NLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            mmsid: t.take("MMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox: t.take("IBOX").and_then(|c| c.into_int()).unwrap_or_default(),
            ielout: t.take("IELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            ntotrf: t.take("NTOTRF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrf: t.take("NCYCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            critrf: t.take("CRITRF").and_then(|c| c.into_int()).unwrap_or_default(),
            valrf: t.take("VALRF").and_then(|c| c.into_float()).unwrap_or_default(),
            begrf: t.take("BEGRF").and_then(|c| c.into_float()).unwrap_or_default(),
            endrf: t.take("ENDRF").and_then(|c| c.into_float()).unwrap_or_default(),
            layrf: t.take("LAYRF").and_then(|c| c.into_int()).unwrap_or_default(),
            maxrm: t.take("MAXRM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrm: t.take("NCYCRM").and_then(|c| c.into_float()).unwrap_or_default(),
            critrm: t.take("CRITRM").and_then(|c| c.into_int()).unwrap_or_default(),
            valrm: t.take("VALRM").and_then(|c| c.into_float()).unwrap_or_default(),
            begrm: t.take("BEGRM").and_then(|c| c.into_float()).unwrap_or_default(),
            endrm: t.take("ENDRM").and_then(|c| c.into_float()).unwrap_or_default(),
            mmsrm: t.take("MMSRM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFINE_MPP_DISTRIBUTION {
    pub id: Vec<i64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub ex: Vec<f64>,
    pub ey: Vec<f64>,
    pub ez: Vec<f64>,
}
impl CONTROL_REFINE_MPP_DISTRIBUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFINE_MPP_DISTRIBUTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ex: t.take("EX").and_then(|c| c.into_float()).unwrap_or_default(),
            ey: t.take("EY").and_then(|c| c.into_float()).unwrap_or_default(),
            ez: t.take("EZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFINE_SHELL {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nlvl: Vec<i64>,
    pub ibox: Vec<i64>,
    pub ntotrf: Vec<i64>,
    pub ncycrf: Vec<f64>,
    pub critrf: Vec<i64>,
    pub valrf: Vec<f64>,
    pub begrf: Vec<f64>,
    pub endrf: Vec<f64>,
    pub layrf: Vec<i64>,
    pub maxrm: Vec<i64>,
    pub ncycrm: Vec<f64>,
    pub critrm: Vec<i64>,
    pub valrm: Vec<f64>,
    pub begrm: Vec<f64>,
    pub endrm: Vec<f64>,
}
impl CONTROL_REFINE_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFINE_SHELL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nlvl: t.take("NLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox: t.take("IBOX").and_then(|c| c.into_int()).unwrap_or_default(),
            ntotrf: t.take("NTOTRF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrf: t.take("NCYCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            critrf: t.take("CRITRF").and_then(|c| c.into_int()).unwrap_or_default(),
            valrf: t.take("VALRF").and_then(|c| c.into_float()).unwrap_or_default(),
            begrf: t.take("BEGRF").and_then(|c| c.into_float()).unwrap_or_default(),
            endrf: t.take("ENDRF").and_then(|c| c.into_float()).unwrap_or_default(),
            layrf: t.take("LAYRF").and_then(|c| c.into_int()).unwrap_or_default(),
            maxrm: t.take("MAXRM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrm: t.take("NCYCRM").and_then(|c| c.into_float()).unwrap_or_default(),
            critrm: t.take("CRITRM").and_then(|c| c.into_int()).unwrap_or_default(),
            valrm: t.take("VALRM").and_then(|c| c.into_float()).unwrap_or_default(),
            begrm: t.take("BEGRM").and_then(|c| c.into_float()).unwrap_or_default(),
            endrm: t.take("ENDRM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REFINE_SOLID {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub nlvl: Vec<i64>,
    pub ibox: Vec<i64>,
    pub ielout: Vec<i64>,
    pub ntotrf: Vec<i64>,
    pub ncycrf: Vec<f64>,
    pub critrf: Vec<i64>,
    pub valrf: Vec<f64>,
    pub begrf: Vec<f64>,
    pub endrf: Vec<f64>,
    pub layrf: Vec<i64>,
    pub maxrm: Vec<i64>,
    pub ncycrm: Vec<f64>,
    pub critrm: Vec<i64>,
    pub valrm: Vec<f64>,
    pub begrm: Vec<f64>,
    pub endrm: Vec<f64>,
}
impl CONTROL_REFINE_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REFINE_SOLID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nlvl: t.take("NLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox: t.take("IBOX").and_then(|c| c.into_int()).unwrap_or_default(),
            ielout: t.take("IELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            ntotrf: t.take("NTOTRF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrf: t.take("NCYCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            critrf: t.take("CRITRF").and_then(|c| c.into_int()).unwrap_or_default(),
            valrf: t.take("VALRF").and_then(|c| c.into_float()).unwrap_or_default(),
            begrf: t.take("BEGRF").and_then(|c| c.into_float()).unwrap_or_default(),
            endrf: t.take("ENDRF").and_then(|c| c.into_float()).unwrap_or_default(),
            layrf: t.take("LAYRF").and_then(|c| c.into_int()).unwrap_or_default(),
            maxrm: t.take("MAXRM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycrm: t.take("NCYCRM").and_then(|c| c.into_float()).unwrap_or_default(),
            critrm: t.take("CRITRM").and_then(|c| c.into_int()).unwrap_or_default(),
            valrm: t.take("VALRM").and_then(|c| c.into_float()).unwrap_or_default(),
            begrm: t.take("BEGRM").and_then(|c| c.into_float()).unwrap_or_default(),
            endrm: t.take("ENDRM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REMESHING {
    pub rmin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub vf_loss: Vec<f64>,
    pub mfrac: Vec<f64>,
    pub dt_min: Vec<f64>,
    pub icurv: Vec<i64>,
    pub iadp10: Vec<i64>,
    pub sefang: Vec<f64>,
}
impl CONTROL_REMESHING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REMESHING").unwrap());
        Self {
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_loss: t.take("VF_LOSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfrac: t.take("MFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dt_min: t.take("DT_MIN").and_then(|c| c.into_float()).unwrap_or_default(),
            icurv: t.take("ICURV").and_then(|c| c.into_int()).unwrap_or_default(),
            iadp10: t.take("IADP10").and_then(|c| c.into_int()).unwrap_or_default(),
            sefang: t.take("SEFANG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REMESHING_EFG {
    pub rmin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub vf_loss: Vec<f64>,
    pub mfrac: Vec<f64>,
    pub dt_min: Vec<f64>,
    pub icurv: Vec<i64>,
    pub iadp10: Vec<i64>,
    pub sefang: Vec<f64>,
    pub ivt: Vec<i64>,
    pub iat: Vec<i64>,
    pub iaat: Vec<i64>,
    pub f: Vec<i64>,
    pub mm: Vec<i64>,
    pub iat1: Vec<f64>,
    pub iat2: Vec<f64>,
    pub iat3: Vec<f64>,
}
impl CONTROL_REMESHING_EFG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REMESHING_EFG").unwrap());
        Self {
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_loss: t.take("VF_LOSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfrac: t.take("MFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dt_min: t.take("DT_MIN").and_then(|c| c.into_float()).unwrap_or_default(),
            icurv: t.take("ICURV").and_then(|c| c.into_int()).unwrap_or_default(),
            iadp10: t.take("IADP10").and_then(|c| c.into_int()).unwrap_or_default(),
            sefang: t.take("SEFANG").and_then(|c| c.into_float()).unwrap_or_default(),
            ivt: t.take("IVT").and_then(|c| c.into_int()).unwrap_or_default(),
            iat: t.take("IAT").and_then(|c| c.into_int()).unwrap_or_default(),
            iaat: t.take("IAAT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mm: t.take("MM").and_then(|c| c.into_int()).unwrap_or_default(),
            iat1: t.take("IAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            iat2: t.take("IAT2").and_then(|c| c.into_float()).unwrap_or_default(),
            iat3: t.take("IAT3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_REQUIRE_REVISION {
    pub release: Vec<String>,
    pub svnrev: Vec<i64>,
    pub gitrev: Vec<i64>,
}
impl CONTROL_REQUIRE_REVISION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_REQUIRE_REVISION").unwrap());
        Self {
            release: t.take("RELEASE").and_then(|c| c.into_str()).unwrap_or_default(),
            svnrev: t.take("SVNREV").and_then(|c| c.into_int()).unwrap_or_default(),
            gitrev: t.take("GITREV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_RIGID {
    pub lmf: Vec<i64>,
    pub jntf: Vec<i64>,
    pub orthmd: Vec<i64>,
    pub partm: Vec<i64>,
    pub sparse: Vec<i64>,
    pub metalf: Vec<i64>,
    pub plotel: Vec<i64>,
    pub rbsms: Vec<i64>,
    pub norbic: Vec<i64>,
    pub gjadstf: Vec<f64>,
    pub gjadvsc: Vec<f64>,
    pub tjastf: Vec<f64>,
    pub tjadvsc: Vec<f64>,
}
impl CONTROL_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_RIGID").unwrap());
        Self {
            lmf: t.take("LMF").and_then(|c| c.into_int()).unwrap_or_default(),
            jntf: t.take("JNTF").and_then(|c| c.into_int()).unwrap_or_default(),
            orthmd: t.take("ORTHMD").and_then(|c| c.into_int()).unwrap_or_default(),
            partm: t.take("PARTM").and_then(|c| c.into_int()).unwrap_or_default(),
            sparse: t.take("SPARSE").and_then(|c| c.into_int()).unwrap_or_default(),
            metalf: t.take("METALF").and_then(|c| c.into_int()).unwrap_or_default(),
            plotel: t.take("PLOTEL").and_then(|c| c.into_int()).unwrap_or_default(),
            rbsms: t.take("RBSMS").and_then(|c| c.into_int()).unwrap_or_default(),
            norbic: t.take("NORBIC").and_then(|c| c.into_int()).unwrap_or_default(),
            gjadstf: t.take("GJADSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            gjadvsc: t.take("GJADVSC").and_then(|c| c.into_float()).unwrap_or_default(),
            tjastf: t.take("TJASTF").and_then(|c| c.into_float()).unwrap_or_default(),
            tjadvsc: t.take("TJADVSC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SEGMENTS_IN_ALE_COUPLING {
    pub rankey: Vec<i64>,
    pub segset: Vec<i64>,
    pub ncychk: Vec<i64>,
    pub sym: Vec<i64>,
    pub ninthk: Vec<i64>,
    pub conthk: Vec<f64>,
}
impl CONTROL_SEGMENTS_IN_ALE_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SEGMENTS_IN_ALE_COUPLING").unwrap());
        Self {
            rankey: t.take("RANKEY").and_then(|c| c.into_int()).unwrap_or_default(),
            segset: t.take("SEGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            ncychk: t.take("NCYCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            sym: t.take("SYM").and_then(|c| c.into_int()).unwrap_or_default(),
            ninthk: t.take("NINTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            conthk: t.take("CONTHK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SHELL {
    pub wrpang: Vec<f64>,
    pub esort: Vec<i64>,
    pub irnxx: Vec<i64>,
    pub istupd: Vec<i64>,
    pub theory: Vec<i64>,
    pub bwc: Vec<i64>,
    pub miter: Vec<i64>,
    pub proj: Vec<i64>,
    pub rotascl: Vec<f64>,
    pub intgrd: Vec<i64>,
    pub lamsht: Vec<i64>,
    pub cstyp6: Vec<i64>,
    pub thshel: Vec<i64>,
    pub psstupd: Vec<i64>,
    pub sidt4tu: Vec<i64>,
    pub cntco: Vec<i64>,
    pub itsflg: Vec<i64>,
    pub irquad: Vec<i64>,
    pub w_mode: Vec<f64>,
    pub stretch: Vec<f64>,
    pub icrq: Vec<i64>,
    pub nfail1: Vec<i64>,
    pub nfail4: Vec<i64>,
    pub psnfail: Vec<i64>,
    pub keepcs: Vec<i64>,
    pub delfr: Vec<i64>,
    pub drcpsid: Vec<i64>,
    pub drcprm: Vec<f64>,
    pub intperr: Vec<i64>,
    pub drcmth: Vec<i64>,
    pub lispsid: Vec<i64>,
    pub nlocdt: Vec<i64>,
}
impl CONTROL_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SHELL").unwrap());
        Self {
            wrpang: t.take("WRPANG").and_then(|c| c.into_float()).unwrap_or_default(),
            esort: t.take("ESORT").and_then(|c| c.into_int()).unwrap_or_default(),
            irnxx: t.take("IRNXX").and_then(|c| c.into_int()).unwrap_or_default(),
            istupd: t.take("ISTUPD").and_then(|c| c.into_int()).unwrap_or_default(),
            theory: t.take("THEORY").and_then(|c| c.into_int()).unwrap_or_default(),
            bwc: t.take("BWC").and_then(|c| c.into_int()).unwrap_or_default(),
            miter: t.take("MITER").and_then(|c| c.into_int()).unwrap_or_default(),
            proj: t.take("PROJ").and_then(|c| c.into_int()).unwrap_or_default(),
            rotascl: t.take("ROTASCL").and_then(|c| c.into_float()).unwrap_or_default(),
            intgrd: t.take("INTGRD").and_then(|c| c.into_int()).unwrap_or_default(),
            lamsht: t.take("LAMSHT").and_then(|c| c.into_int()).unwrap_or_default(),
            cstyp6: t.take("CSTYP6").and_then(|c| c.into_int()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            psstupd: t.take("PSSTUPD").and_then(|c| c.into_int()).unwrap_or_default(),
            sidt4tu: t.take("SIDT4TU").and_then(|c| c.into_int()).unwrap_or_default(),
            cntco: t.take("CNTCO").and_then(|c| c.into_int()).unwrap_or_default(),
            itsflg: t.take("ITSFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            irquad: t.take("IRQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            w_mode: t.take("W-MODE").and_then(|c| c.into_float()).unwrap_or_default(),
            stretch: t.take("STRETCH").and_then(|c| c.into_float()).unwrap_or_default(),
            icrq: t.take("ICRQ").and_then(|c| c.into_int()).unwrap_or_default(),
            nfail1: t.take("NFAIL1").and_then(|c| c.into_int()).unwrap_or_default(),
            nfail4: t.take("NFAIL4").and_then(|c| c.into_int()).unwrap_or_default(),
            psnfail: t.take("PSNFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            keepcs: t.take("KEEPCS").and_then(|c| c.into_int()).unwrap_or_default(),
            delfr: t.take("DELFR").and_then(|c| c.into_int()).unwrap_or_default(),
            drcpsid: t.take("DRCPSID").and_then(|c| c.into_int()).unwrap_or_default(),
            drcprm: t.take("DRCPRM").and_then(|c| c.into_float()).unwrap_or_default(),
            intperr: t.take("INTPERR").and_then(|c| c.into_int()).unwrap_or_default(),
            drcmth: t.take("DRCMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            lispsid: t.take("LISPSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nlocdt: t.take("NLOCDT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SOLID {
    pub esort: Vec<i64>,
    pub fmatrix: Vec<i64>,
    pub niptets: Vec<i64>,
    pub swlocl: Vec<i64>,
    pub psfail: Vec<i64>,
    pub t10jtol: Vec<f64>,
    pub icoh: Vec<i64>,
    pub tet13k: Vec<i64>,
    pub pm1: Vec<i64>,
    pub pm2: Vec<i64>,
    pub pm3: Vec<i64>,
    pub pm4: Vec<i64>,
    pub pm5: Vec<i64>,
    pub pm6: Vec<i64>,
    pub pm7: Vec<i64>,
    pub pm8: Vec<i64>,
    pub pm9: Vec<i64>,
    pub pm10: Vec<i64>,
    pub tet13v: Vec<i64>,
}
impl CONTROL_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SOLID").unwrap());
        Self {
            esort: t.take("ESORT").and_then(|c| c.into_int()).unwrap_or_default(),
            fmatrix: t.take("FMATRIX").and_then(|c| c.into_int()).unwrap_or_default(),
            niptets: t.take("NIPTETS").and_then(|c| c.into_int()).unwrap_or_default(),
            swlocl: t.take("SWLOCL").and_then(|c| c.into_int()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            t10jtol: t.take("T10JTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            icoh: t.take("ICOH").and_then(|c| c.into_int()).unwrap_or_default(),
            tet13k: t.take("TET13K").and_then(|c| c.into_int()).unwrap_or_default(),
            pm1: t.take("PM1").and_then(|c| c.into_int()).unwrap_or_default(),
            pm2: t.take("PM2").and_then(|c| c.into_int()).unwrap_or_default(),
            pm3: t.take("PM3").and_then(|c| c.into_int()).unwrap_or_default(),
            pm4: t.take("PM4").and_then(|c| c.into_int()).unwrap_or_default(),
            pm5: t.take("PM5").and_then(|c| c.into_int()).unwrap_or_default(),
            pm6: t.take("PM6").and_then(|c| c.into_int()).unwrap_or_default(),
            pm7: t.take("PM7").and_then(|c| c.into_int()).unwrap_or_default(),
            pm8: t.take("PM8").and_then(|c| c.into_int()).unwrap_or_default(),
            pm9: t.take("PM9").and_then(|c| c.into_int()).unwrap_or_default(),
            pm10: t.take("PM10").and_then(|c| c.into_int()).unwrap_or_default(),
            tet13v: t.take("TET13V").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SOLUTION {
    pub soln: Vec<i64>,
    pub nlq: Vec<i64>,
    pub isnan: Vec<i64>,
    pub lcint: Vec<i64>,
    pub lcacc: Vec<i64>,
    pub ncdcf: Vec<i64>,
    pub nocop: Vec<i64>,
}
impl CONTROL_SOLUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SOLUTION").unwrap());
        Self {
            soln: t.take("SOLN").and_then(|c| c.into_int()).unwrap_or_default(),
            nlq: t.take("NLQ").and_then(|c| c.into_int()).unwrap_or_default(),
            isnan: t.take("ISNAN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcacc: t.take("LCACC").and_then(|c| c.into_int()).unwrap_or_default(),
            ncdcf: t.take("NCDCF").and_then(|c| c.into_int()).unwrap_or_default(),
            nocop: t.take("NOCOP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SPH {
    pub ncbs: Vec<i64>,
    pub boxid: Vec<i64>,
    pub dt: Vec<f64>,
    pub idim: Vec<i64>,
    pub nmneigh: Vec<i64>,
    pub form: Vec<i64>,
    pub start: Vec<f64>,
    pub maxv: Vec<f64>,
    pub cont: Vec<i64>,
    pub deriv: Vec<i64>,
    pub ini: Vec<i64>,
    pub ishow: Vec<i64>,
    pub ierod: Vec<i64>,
    pub icont: Vec<i64>,
    pub iavis: Vec<i64>,
    pub isymp: Vec<i64>,
    pub ithk: Vec<i64>,
    pub istab: Vec<i64>,
    pub ql: Vec<f64>,
    pub f: Vec<i64>,
    pub sphsort: Vec<i64>,
    pub ishift: Vec<i64>,
}
impl CONTROL_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SPH").unwrap());
        Self {
            ncbs: t.take("NCBS").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            idim: t.take("IDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            nmneigh: t.take("NMNEIGH").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            maxv: t.take("MAXV").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_int()).unwrap_or_default(),
            deriv: t.take("DERIV").and_then(|c| c.into_int()).unwrap_or_default(),
            ini: t.take("INI").and_then(|c| c.into_int()).unwrap_or_default(),
            ishow: t.take("ISHOW").and_then(|c| c.into_int()).unwrap_or_default(),
            ierod: t.take("IEROD").and_then(|c| c.into_int()).unwrap_or_default(),
            icont: t.take("ICONT").and_then(|c| c.into_int()).unwrap_or_default(),
            iavis: t.take("IAVIS").and_then(|c| c.into_int()).unwrap_or_default(),
            isymp: t.take("ISYMP").and_then(|c| c.into_int()).unwrap_or_default(),
            ithk: t.take("ITHK").and_then(|c| c.into_int()).unwrap_or_default(),
            istab: t.take("ISTAB").and_then(|c| c.into_int()).unwrap_or_default(),
            ql: t.take("QL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            sphsort: t.take("SPHSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            ishift: t.take("ISHIFT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SPH_INCOMPRESSIBLE {
    pub ibndp: Vec<i64>,
    pub tavg: Vec<f64>,
    pub tmax: Vec<f64>,
    pub rol: Vec<f64>,
    pub ihtc: Vec<i64>,
    pub imat: Vec<i64>,
}
impl CONTROL_SPH_INCOMPRESSIBLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SPH_INCOMPRESSIBLE").unwrap());
        Self {
            ibndp: t.take("IBNDP").and_then(|c| c.into_int()).unwrap_or_default(),
            tavg: t.take("TAVG").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            rol: t.take("ROL").and_then(|c| c.into_float()).unwrap_or_default(),
            ihtc: t.take("IHTC").and_then(|c| c.into_int()).unwrap_or_default(),
            imat: t.take("IMAT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SPOTWELD_BEAM {
    pub lct: Vec<i64>,
    pub lcs: Vec<i64>,
    pub t_ort: Vec<i64>,
    pub prtflg: Vec<i64>,
    pub t_ors: Vec<i64>,
    pub rpbhx: Vec<i64>,
    pub bmsid: Vec<i64>,
    pub id_off: Vec<i64>,
}
impl CONTROL_SPOTWELD_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SPOTWELD_BEAM").unwrap());
        Self {
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcs: t.take("LCS").and_then(|c| c.into_int()).unwrap_or_default(),
            t_ort: t.take("T_ORT").and_then(|c| c.into_int()).unwrap_or_default(),
            prtflg: t.take("PRTFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            t_ors: t.take("T_ORS").and_then(|c| c.into_int()).unwrap_or_default(),
            rpbhx: t.take("RPBHX").and_then(|c| c.into_int()).unwrap_or_default(),
            bmsid: t.take("BMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            id_off: t.take("ID_OFF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_STAGED_CONSTRUCTION {
    pub tstart: Vec<f64>,
    pub stgs: Vec<i64>,
    pub stge: Vec<i64>,
    pub accel: Vec<f64>,
    pub fact: Vec<f64>,
    pub f: Vec<i64>,
    pub dordel: Vec<i64>,
    pub nopdel: Vec<i64>,
    pub itime: Vec<i64>,
    pub f_2: Vec<i64>,
    pub idynain: Vec<i64>,
}
impl CONTROL_STAGED_CONSTRUCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_STAGED_CONSTRUCTION").unwrap());
        Self {
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            stgs: t.take("STGS").and_then(|c| c.into_int()).unwrap_or_default(),
            stge: t.take("STGE").and_then(|c| c.into_int()).unwrap_or_default(),
            accel: t.take("ACCEL").and_then(|c| c.into_float()).unwrap_or_default(),
            fact: t.take("FACT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            dordel: t.take("DORDEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nopdel: t.take("NOPDEL").and_then(|c| c.into_int()).unwrap_or_default(),
            itime: t.take("ITIME").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            idynain: t.take("IDYNAIN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_START {
    pub begtim: Vec<f64>,
}
impl CONTROL_START {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_START").unwrap());
        Self {
            begtim: t.take("BEGTIM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_STEADY_STATE_ROLLING {
    pub imass: Vec<i64>,
    pub lcdmu: Vec<i64>,
    pub lcdmur: Vec<i64>,
    pub ivel: Vec<i64>,
    pub scl_k: Vec<i64>,
}
impl CONTROL_STEADY_STATE_ROLLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_STEADY_STATE_ROLLING").unwrap());
        Self {
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdmu: t.take("LCDMU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdmur: t.take("LCDMUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ivel: t.take("IVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            scl_k: t.take("SCL_K").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_STRUCTURED {
}
impl CONTROL_STRUCTURED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_STRUCTURED").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_STRUCTURED_TERM {
}
impl CONTROL_STRUCTURED_TERM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_STRUCTURED_TERM").unwrap());
        Self {
        }
    }
}

pub struct CONTROL_SUBCYCLE {
    pub k: Vec<i64>,
    pub l: Vec<i64>,
}
impl CONTROL_SUBCYCLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SUBCYCLE").unwrap());
        Self {
            k: t.take("K").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SUBCYCLE_MASS_SCALED_PART {
    pub pid: Vec<i64>,
    pub ts: Vec<f64>,
}
impl CONTROL_SUBCYCLE_MASS_SCALED_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SUBCYCLE_MASS_SCALED_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_SUBCYCLE_MASS_SCALED_PART_SET {
    pub psid: Vec<i64>,
    pub ts: Vec<f64>,
}
impl CONTROL_SUBCYCLE_MASS_SCALED_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_SUBCYCLE_MASS_SCALED_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_TERMINATION {
    pub endtim: Vec<f64>,
    pub endcyc: Vec<i64>,
    pub dtmin: Vec<f64>,
    pub endeng: Vec<f64>,
    pub endmas: Vec<f64>,
    pub nosol: Vec<i64>,
}
impl CONTROL_TERMINATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_TERMINATION").unwrap());
        Self {
            endtim: t.take("ENDTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            endcyc: t.take("ENDCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            endeng: t.take("ENDENG").and_then(|c| c.into_float()).unwrap_or_default(),
            endmas: t.take("ENDMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            nosol: t.take("NOSOL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_THERMAL_EIGENVALUE {
    pub neig: Vec<i64>,
}
impl CONTROL_THERMAL_EIGENVALUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_THERMAL_EIGENVALUE").unwrap());
        Self {
            neig: t.take("NEIG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_THERMAL_FORMING {
    pub its: Vec<f64>,
    pub ptype: Vec<i64>,
    pub tsf: Vec<f64>,
    pub thshel: Vec<i64>,
    pub ithoff: Vec<i64>,
    pub solver: Vec<i64>,
    pub fwork: Vec<f64>,
    pub k: Vec<f64>,
    pub frad: Vec<f64>,
    pub h0: Vec<f64>,
    pub lmin: Vec<f64>,
    pub lmax: Vec<f64>,
    pub ftoslv: Vec<f64>,
    pub bc_flg: Vec<i64>,
    pub algo: Vec<i64>,
    pub lcfst: Vec<i64>,
    pub lcfdt: Vec<i64>,
    pub formula: Vec<i64>,
    pub a: Vec<i64>,
    pub b: Vec<i64>,
    pub c: Vec<i64>,
    pub d: Vec<i64>,
    pub lch: Vec<i64>,
}
impl CONTROL_THERMAL_FORMING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_THERMAL_FORMING").unwrap());
        Self {
            its: t.take("ITS").and_then(|c| c.into_float()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            tsf: t.take("TSF").and_then(|c| c.into_float()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            ithoff: t.take("ITHOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_int()).unwrap_or_default(),
            fwork: t.take("FWORK").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            frad: t.take("FRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ftoslv: t.take("FTOSLV").and_then(|c| c.into_float()).unwrap_or_default(),
            bc_flg: t.take("BC_FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            algo: t.take("ALGO").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfst: t.take("LCFST").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfdt: t.take("LCFDT").and_then(|c| c.into_int()).unwrap_or_default(),
            formula: t.take("FORMULA").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_int()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_int()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_int()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_THERMAL_NONLINEAR {
    pub refmax: Vec<i64>,
    pub tol: Vec<f64>,
    pub dcp: Vec<f64>,
    pub lumpbc: Vec<i64>,
    pub thlstl: Vec<f64>,
    pub nlthpr: Vec<i64>,
    pub phchpn: Vec<f64>,
}
impl CONTROL_THERMAL_NONLINEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_THERMAL_NONLINEAR").unwrap());
        Self {
            refmax: t.take("REFMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dcp: t.take("DCP").and_then(|c| c.into_float()).unwrap_or_default(),
            lumpbc: t.take("LUMPBC").and_then(|c| c.into_int()).unwrap_or_default(),
            thlstl: t.take("THLSTL").and_then(|c| c.into_float()).unwrap_or_default(),
            nlthpr: t.take("NLTHPR").and_then(|c| c.into_int()).unwrap_or_default(),
            phchpn: t.take("PHCHPN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_THERMAL_SOLVER {
    pub atype: Vec<i64>,
    pub ptype: Vec<i64>,
    pub solver: Vec<i64>,
    pub f: Vec<i64>,
    pub gpt: Vec<i64>,
    pub eqheat: Vec<f64>,
    pub fwork: Vec<f64>,
    pub sbc: Vec<f64>,
    pub msglvl: Vec<i64>,
    pub maxitr: Vec<i64>,
    pub abstol: Vec<f64>,
    pub reltol: Vec<f64>,
    pub omega: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub tsf: Vec<f64>,
    pub mxdmp: Vec<i64>,
    pub dtvf: Vec<f64>,
    pub varden: Vec<i64>,
    pub f_2: Vec<i64>,
    pub ncycl: Vec<i64>,
}
impl CONTROL_THERMAL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_THERMAL_SOLVER").unwrap());
        Self {
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            gpt: t.take("GPT").and_then(|c| c.into_int()).unwrap_or_default(),
            eqheat: t.take("EQHEAT").and_then(|c| c.into_float()).unwrap_or_default(),
            fwork: t.take("FWORK").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            msglvl: t.take("MSGLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            maxitr: t.take("MAXITR").and_then(|c| c.into_int()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            tsf: t.take("TSF").and_then(|c| c.into_float()).unwrap_or_default(),
            mxdmp: t.take("MXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dtvf: t.take("DTVF").and_then(|c| c.into_float()).unwrap_or_default(),
            varden: t.take("VARDEN").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycl: t.take("NCYCL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_THERMAL_TIMESTEP {
    pub ts: Vec<i64>,
    pub tip: Vec<f64>,
    pub its: Vec<f64>,
    pub tmin: Vec<f64>,
    pub tmax: Vec<f64>,
    pub dtemp: Vec<f64>,
    pub tscp: Vec<f64>,
    pub lcts: Vec<i64>,
}
impl CONTROL_THERMAL_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_THERMAL_TIMESTEP").unwrap());
        Self {
            ts: t.take("TS").and_then(|c| c.into_int()).unwrap_or_default(),
            tip: t.take("TIP").and_then(|c| c.into_float()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_float()).unwrap_or_default(),
            tmin: t.take("TMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtemp: t.take("DTEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tscp: t.take("TSCP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcts: t.take("LCTS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_TIMESTEP {
    pub dtinit: Vec<f64>,
    pub tssfac: Vec<f64>,
    pub isdo: Vec<i64>,
    pub tslimt: Vec<f64>,
    pub dt2ms: Vec<f64>,
    pub lctm: Vec<i64>,
    pub erode: Vec<i64>,
    pub ms1st: Vec<i64>,
    pub dt2msf: Vec<f64>,
    pub dt2mslc: Vec<i64>,
    pub imscl: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub rmscl: Vec<f64>,
    pub emscl: Vec<f64>,
    pub ihdo: Vec<i64>,
    pub f: Vec<i64>,
    pub igado: Vec<i64>,
}
impl CONTROL_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_TIMESTEP").unwrap());
        Self {
            dtinit: t.take("DTINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tssfac: t.take("TSSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            isdo: t.take("ISDO").and_then(|c| c.into_int()).unwrap_or_default(),
            tslimt: t.take("TSLIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt2ms: t.take("DT2MS").and_then(|c| c.into_float()).unwrap_or_default(),
            lctm: t.take("LCTM").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            ms1st: t.take("MS1ST").and_then(|c| c.into_int()).unwrap_or_default(),
            dt2msf: t.take("DT2MSF").and_then(|c| c.into_float()).unwrap_or_default(),
            dt2mslc: t.take("DT2MSLC").and_then(|c| c.into_int()).unwrap_or_default(),
            imscl: t.take("IMSCL").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rmscl: t.take("RMSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            emscl: t.take("EMSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            ihdo: t.take("IHDO").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            igado: t.take("IGADO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_TIME_STEP {
    pub dtinit: Vec<f64>,
    pub tssfac: Vec<f64>,
    pub isdo: Vec<i64>,
    pub tslimt: Vec<f64>,
    pub dt2ms: Vec<f64>,
    pub lctm: Vec<i64>,
    pub erode: Vec<i64>,
    pub ms1st: Vec<i64>,
    pub dt2msf: Vec<f64>,
    pub dt2mslc: Vec<i64>,
    pub imscl: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub rmscl: Vec<f64>,
    pub emscl: Vec<f64>,
    pub ihdo: Vec<i64>,
}
impl CONTROL_TIME_STEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_TIME_STEP").unwrap());
        Self {
            dtinit: t.take("DTINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tssfac: t.take("TSSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            isdo: t.take("ISDO").and_then(|c| c.into_int()).unwrap_or_default(),
            tslimt: t.take("TSLIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt2ms: t.take("DT2MS").and_then(|c| c.into_float()).unwrap_or_default(),
            lctm: t.take("LCTM").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            ms1st: t.take("MS1ST").and_then(|c| c.into_int()).unwrap_or_default(),
            dt2msf: t.take("DT2MSF").and_then(|c| c.into_float()).unwrap_or_default(),
            dt2mslc: t.take("DT2MSLC").and_then(|c| c.into_int()).unwrap_or_default(),
            imscl: t.take("IMSCL").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            rmscl: t.take("RMSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            emscl: t.take("EMSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            ihdo: t.take("IHDO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_UNITS {
    pub length: Vec<String>,
    pub time: Vec<String>,
    pub mass: Vec<String>,
    pub temp: Vec<String>,
}
impl CONTROL_UNITS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_UNITS").unwrap());
        Self {
            length: t.take("LENGTH").and_then(|c| c.into_str()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_str()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct CONTROL_VIBRO_ACOUSTIC {
    pub vaflag: Vec<i64>,
    pub vaprld: Vec<i64>,
    pub vastrs: Vec<i64>,
    pub vapsd: Vec<i64>,
    pub varms: Vec<i64>,
    pub vaplot: Vec<i64>,
    pub ipanelu: Vec<i64>,
    pub ipanelv: Vec<i64>,
    pub restart: Vec<i64>,
    pub nmodstr: Vec<i64>,
}
impl CONTROL_VIBRO_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("CONTROL_VIBRO_ACOUSTIC").unwrap());
        Self {
            vaflag: t.take("VAFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vaprld: t.take("VAPRLD").and_then(|c| c.into_int()).unwrap_or_default(),
            vastrs: t.take("VASTRS").and_then(|c| c.into_int()).unwrap_or_default(),
            vapsd: t.take("VAPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            varms: t.take("VARMS").and_then(|c| c.into_int()).unwrap_or_default(),
            vaplot: t.take("VAPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelu: t.take("IPANELU").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelv: t.take("IPANELV").and_then(|c| c.into_int()).unwrap_or_default(),
            restart: t.take("RESTART").and_then(|c| c.into_int()).unwrap_or_default(),
            nmodstr: t.take("NMODSTR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct COSIM_FMI_CONTROL {
    pub appid: Vec<String>,
    pub opt: Vec<String>,
    pub mode: Vec<String>,
    pub fmi: Vec<i64>,
    pub setting: Vec<String>,
}
impl COSIM_FMI_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COSIM_FMI_CONTROL").unwrap());
        Self {
            appid: t.take("APPID").and_then(|c| c.into_str()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_str()).unwrap_or_default(),
            mode: t.take("MODE").and_then(|c| c.into_str()).unwrap_or_default(),
            fmi: t.take("FMI").and_then(|c| c.into_int()).unwrap_or_default(),
            setting: t.take("SETTING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct COSIM_FMI_INTERFACE {
    pub appid: Vec<String>,
    pub impexp: Vec<String>,
    pub regtyp: Vec<String>,
    pub regid: Vec<i64>,
    pub field: Vec<String>,
    pub winit: Vec<f64>,
    pub ratio: Vec<f64>,
    pub cid: Vec<i64>,
    pub ref_: Vec<i64>,
}
impl COSIM_FMI_INTERFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("COSIM_FMI_INTERFACE").unwrap());
        Self {
            appid: t.take("APPID").and_then(|c| c.into_str()).unwrap_or_default(),
            impexp: t.take("IMPEXP").and_then(|c| c.into_str()).unwrap_or_default(),
            regtyp: t.take("REGTYP").and_then(|c| c.into_str()).unwrap_or_default(),
            regid: t.take("REGID").and_then(|c| c.into_int()).unwrap_or_default(),
            field: t.take("FIELD").and_then(|c| c.into_str()).unwrap_or_default(),
            winit: t.take("WINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ratio: t.take("RATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_FREQUENCY_RANGE {
    pub cdamp: Vec<f64>,
    pub flow: Vec<f64>,
    pub fhigh: Vec<f64>,
    pub psid: Vec<i64>,
    pub blank: Vec<i64>,
    pub pidrel: Vec<i64>,
    pub iflg: Vec<i64>,
}
impl DAMPING_FREQUENCY_RANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_FREQUENCY_RANGE").unwrap());
        Self {
            cdamp: t.take("CDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            flow: t.take("FLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            fhigh: t.take("FHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            blank: t.take("Blank").and_then(|c| c.into_int()).unwrap_or_default(),
            pidrel: t.take("PIDREL").and_then(|c| c.into_int()).unwrap_or_default(),
            iflg: t.take("IFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_FREQUENCY_RANGE_DEFORM {
    pub cdamp: Vec<f64>,
    pub flow: Vec<f64>,
    pub fhigh: Vec<f64>,
    pub psid: Vec<i64>,
    pub blank: Vec<i64>,
    pub pidrel: Vec<i64>,
    pub iflg: Vec<i64>,
}
impl DAMPING_FREQUENCY_RANGE_DEFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_FREQUENCY_RANGE_DEFORM").unwrap());
        Self {
            cdamp: t.take("CDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            flow: t.take("FLOW").and_then(|c| c.into_float()).unwrap_or_default(),
            fhigh: t.take("FHIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            blank: t.take("Blank").and_then(|c| c.into_int()).unwrap_or_default(),
            pidrel: t.take("PIDREL").and_then(|c| c.into_int()).unwrap_or_default(),
            iflg: t.take("IFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_GLOBAL {
    pub lcid: Vec<i64>,
    pub valdmp: Vec<f64>,
    pub stx: Vec<f64>,
    pub sty: Vec<f64>,
    pub stz: Vec<f64>,
    pub srx: Vec<f64>,
    pub sry: Vec<f64>,
    pub srz: Vec<f64>,
}
impl DAMPING_GLOBAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_GLOBAL").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            valdmp: t.take("VALDMP").and_then(|c| c.into_float()).unwrap_or_default(),
            stx: t.take("STX").and_then(|c| c.into_float()).unwrap_or_default(),
            sty: t.take("STY").and_then(|c| c.into_float()).unwrap_or_default(),
            stz: t.take("STZ").and_then(|c| c.into_float()).unwrap_or_default(),
            srx: t.take("SRX").and_then(|c| c.into_float()).unwrap_or_default(),
            sry: t.take("SRY").and_then(|c| c.into_float()).unwrap_or_default(),
            srz: t.take("SRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_PART_MASS {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub flag: Vec<i64>,
    pub stx: Vec<f64>,
    pub sty: Vec<f64>,
    pub stz: Vec<f64>,
    pub srx: Vec<f64>,
    pub sry: Vec<f64>,
    pub srz: Vec<f64>,
}
impl DAMPING_PART_MASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_PART_MASS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            stx: t.take("STX").and_then(|c| c.into_float()).unwrap_or_default(),
            sty: t.take("STY").and_then(|c| c.into_float()).unwrap_or_default(),
            stz: t.take("STZ").and_then(|c| c.into_float()).unwrap_or_default(),
            srx: t.take("SRX").and_then(|c| c.into_float()).unwrap_or_default(),
            sry: t.take("SRY").and_then(|c| c.into_float()).unwrap_or_default(),
            srz: t.take("SRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_PART_MASS_SET {
    pub psid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub flag: Vec<i64>,
    pub stx: Vec<f64>,
    pub sty: Vec<f64>,
    pub stz: Vec<f64>,
    pub srx: Vec<f64>,
    pub sry: Vec<f64>,
    pub srz: Vec<f64>,
}
impl DAMPING_PART_MASS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_PART_MASS_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            stx: t.take("STX").and_then(|c| c.into_float()).unwrap_or_default(),
            sty: t.take("STY").and_then(|c| c.into_float()).unwrap_or_default(),
            stz: t.take("STZ").and_then(|c| c.into_float()).unwrap_or_default(),
            srx: t.take("SRX").and_then(|c| c.into_float()).unwrap_or_default(),
            sry: t.take("SRY").and_then(|c| c.into_float()).unwrap_or_default(),
            srz: t.take("SRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_PART_STIFFNESS {
    pub pid: Vec<i64>,
    pub coef: Vec<f64>,
}
impl DAMPING_PART_STIFFNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_PART_STIFFNESS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            coef: t.take("COEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_PART_STIFFNESS_SET {
    pub psid: Vec<i64>,
    pub coef: Vec<f64>,
}
impl DAMPING_PART_STIFFNESS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_PART_STIFFNESS_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            coef: t.take("COEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_POROSITY {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub flag: Vec<i64>,
    pub stx: Vec<f64>,
    pub sty: Vec<f64>,
    pub stz: Vec<f64>,
    pub srx: Vec<f64>,
    pub sry: Vec<f64>,
    pub srz: Vec<f64>,
}
impl DAMPING_POROSITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_POROSITY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            stx: t.take("STX").and_then(|c| c.into_float()).unwrap_or_default(),
            sty: t.take("STY").and_then(|c| c.into_float()).unwrap_or_default(),
            stz: t.take("STZ").and_then(|c| c.into_float()).unwrap_or_default(),
            srx: t.take("SRX").and_then(|c| c.into_float()).unwrap_or_default(),
            sry: t.take("SRY").and_then(|c| c.into_float()).unwrap_or_default(),
            srz: t.take("SRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DAMPING_RELATIVE {
    pub cdamp: Vec<f64>,
    pub freq: Vec<f64>,
    pub pidrb: Vec<i64>,
    pub psid: Vec<i64>,
    pub dv2: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl DAMPING_RELATIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DAMPING_RELATIVE").unwrap());
        Self {
            cdamp: t.take("CDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            pidrb: t.take("PIDRB").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dv2: t.take("DV2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ABSTAT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_ABSTAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ABSTAT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ABSTAT_CPM {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_ABSTAT_CPM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ABSTAT_CPM").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ACEOUT {
    pub dt: Vec<f64>,
}
impl DATABASE_ACEOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ACEOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ALE {
    pub dtout: Vec<f64>,
    pub setid: Vec<i64>,
    pub var1: Vec<i64>,
    pub var2: Vec<i64>,
    pub var3: Vec<i64>,
    pub var4: Vec<i64>,
    pub var5: Vec<i64>,
    pub var6: Vec<i64>,
    pub var7: Vec<i64>,
    pub var8: Vec<i64>,
}
impl DATABASE_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ALE").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            var1: t.take("VAR1").and_then(|c| c.into_int()).unwrap_or_default(),
            var2: t.take("VAR2").and_then(|c| c.into_int()).unwrap_or_default(),
            var3: t.take("VAR3").and_then(|c| c.into_int()).unwrap_or_default(),
            var4: t.take("VAR4").and_then(|c| c.into_int()).unwrap_or_default(),
            var5: t.take("VAR5").and_then(|c| c.into_int()).unwrap_or_default(),
            var6: t.take("VAR6").and_then(|c| c.into_int()).unwrap_or_default(),
            var7: t.take("VAR7").and_then(|c| c.into_int()).unwrap_or_default(),
            var8: t.take("VAR8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ALE_MAT {
    pub dtout: Vec<f64>,
    pub boxlow: Vec<i64>,
    pub boxup: Vec<i64>,
    pub dtxy: Vec<f64>,
}
impl DATABASE_ALE_MAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ALE_MAT").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            boxlow: t.take("BOXLOW").and_then(|c| c.into_int()).unwrap_or_default(),
            boxup: t.take("BOXUP").and_then(|c| c.into_int()).unwrap_or_default(),
            dtxy: t.take("DTXY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ALE_OPERATION {
    pub fct: Vec<i64>,
    pub hisvn: Vec<i64>,
    pub wrt: Vec<i64>,
    pub dt: Vec<i64>,
    pub setid: Vec<i64>,
    pub var: Vec<i64>,
    pub var_2: Vec<i64>,
    pub var_3: Vec<i64>,
    pub var_4: Vec<i64>,
    pub var_5: Vec<i64>,
    pub var_6: Vec<i64>,
    pub var_7: Vec<i64>,
    pub var_8: Vec<i64>,
}
impl DATABASE_ALE_OPERATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ALE_OPERATION").unwrap());
        Self {
            fct: t.take("FCT").and_then(|c| c.into_int()).unwrap_or_default(),
            hisvn: t.take("HISVN").and_then(|c| c.into_int()).unwrap_or_default(),
            wrt: t.take("WRT").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            var: t.take("VAR").and_then(|c| c.into_int()).unwrap_or_default(),
            var_2: t.take("VAR_2").and_then(|c| c.into_int()).unwrap_or_default(),
            var_3: t.take("VAR_3").and_then(|c| c.into_int()).unwrap_or_default(),
            var_4: t.take("VAR_4").and_then(|c| c.into_int()).unwrap_or_default(),
            var_5: t.take("VAR_5").and_then(|c| c.into_int()).unwrap_or_default(),
            var_6: t.take("VAR_6").and_then(|c| c.into_int()).unwrap_or_default(),
            var_7: t.take("VAR_7").and_then(|c| c.into_int()).unwrap_or_default(),
            var_8: t.take("VAR_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ATDOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_ATDOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ATDOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_AVSFLT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_AVSFLT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_AVSFLT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BEARING {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_BEARING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BEARING").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_BLSTFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_BLSTFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_BLSTFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_CPMFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_CPMFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_CPMFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3CRACK {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_D3CRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3CRACK").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3DRLF {
    pub cycl: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_D3DRLF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3DRLF").unwrap());
        Self {
            cycl: t.take("CYCL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3DUMP {
    pub cycl: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_BINARY_D3DUMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3DUMP").unwrap());
        Self {
            cycl: t.take("CYCL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3PART {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub hsetid: Vec<i64>,
    pub bsetid: Vec<i64>,
    pub ssetid: Vec<i64>,
    pub tsetid: Vec<i64>,
}
impl DATABASE_BINARY_D3PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3PART").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            hsetid: t.take("HSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsetid: t.take("BSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssetid: t.take("SSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsetid: t.take("TSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3PLOT {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub ioopt: Vec<i64>,
    pub rate: Vec<f64>,
    pub cutoff: Vec<f64>,
    pub window: Vec<f64>,
    pub type_: Vec<i64>,
    pub pset: Vec<i64>,
}
impl DATABASE_BINARY_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3PLOT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            rate: t.take("RATE").and_then(|c| c.into_float()).unwrap_or_default(),
            cutoff: t.take("CUTOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3PROP {
    pub ifile: Vec<i64>,
    pub imatl: Vec<i64>,
    pub iwall: Vec<i64>,
}
impl DATABASE_BINARY_D3PROP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3PROP").unwrap());
        Self {
            ifile: t.take("IFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            imatl: t.take("IMATL").and_then(|c| c.into_int()).unwrap_or_default(),
            iwall: t.take("IWALL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_D3THDT {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_D3THDT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_D3THDT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_DEMFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_DEMFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_DEMFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_FSIFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub cid: Vec<i64>,
}
impl DATABASE_BINARY_FSIFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_FSIFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_FSILNK {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub cid: Vec<i64>,
}
impl DATABASE_BINARY_FSILNK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_FSILNK").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_INTFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_BINARY_INTFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_INTFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_INTFOR_FILE {
    pub filename: Vec<String>,
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_BINARY_INTFOR_FILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_INTFOR_FILE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_ISPHFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_ISPHFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_ISPHFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_PBMFOR {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_PBMFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_PBMFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_RUNRSF {
    pub cycl: Vec<f64>,
    pub nr: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_RUNRSF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_RUNRSF").unwrap());
        Self {
            cycl: t.take("CYCL").and_then(|c| c.into_float()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BINARY_XTFILE {
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub beam: Vec<i64>,
    pub npltc: Vec<i64>,
    pub psetid: Vec<i64>,
}
impl DATABASE_BINARY_XTFILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BINARY_XTFILE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            beam: t.take("BEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            psetid: t.take("PSETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_BNDOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_BNDOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_BNDOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_CPM_SENSOR {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub segsid: Vec<i64>,
    pub offset: Vec<f64>,
    pub r_lx: Vec<f64>,
    pub len_ly: Vec<f64>,
    pub lz: Vec<f64>,
}
impl DATABASE_CPM_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_CPM_SENSOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            segsid: t.take("SEGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            r_lx: t.take("R/LX").and_then(|c| c.into_float()).unwrap_or_default(),
            len_ly: t.take("LEN/LY").and_then(|c| c.into_float()).unwrap_or_default(),
            lz: t.take("LZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_CROSS_SECTION_PLANE {
    pub csid: Vec<i64>,
    pub title: Vec<String>,
    pub psid: Vec<i64>,
    pub xct: Vec<f64>,
    pub yct: Vec<f64>,
    pub zct: Vec<f64>,
    pub xch: Vec<f64>,
    pub ych: Vec<f64>,
    pub zch: Vec<f64>,
    pub radius: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub id: Vec<i64>,
    pub itype: Vec<i64>,
}
impl DATABASE_CROSS_SECTION_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_CROSS_SECTION_PLANE").unwrap());
        Self {
            csid: t.take("CSID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xct: t.take("XCT").and_then(|c| c.into_float()).unwrap_or_default(),
            yct: t.take("YCT").and_then(|c| c.into_float()).unwrap_or_default(),
            zct: t.take("ZCT").and_then(|c| c.into_float()).unwrap_or_default(),
            xch: t.take("XCH").and_then(|c| c.into_float()).unwrap_or_default(),
            ych: t.take("YCH").and_then(|c| c.into_float()).unwrap_or_default(),
            zch: t.take("ZCH").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_CROSS_SECTION_SET {
    pub csid: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub hsid: Vec<i64>,
    pub bsid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub tsid: Vec<i64>,
    pub dsid: Vec<i64>,
    pub id: Vec<i64>,
    pub itype: Vec<i64>,
}
impl DATABASE_CROSS_SECTION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_CROSS_SECTION_SET").unwrap());
        Self {
            csid: t.take("CSID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hsid: t.take("HSID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid: t.take("BSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsid: t.take("TSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid: t.take("DSID").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_CURVOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_CURVOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_CURVOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_D3FTG {
    pub binary: Vec<i64>,
    pub dt: Vec<f64>,
}
impl DATABASE_D3FTG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_D3FTG").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_D3MAX {
    pub dtcheck: Vec<f64>,
    pub me: Vec<i64>,
    pub pstrs: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub ifilt: Vec<i64>,
    pub output: Vec<i64>,
    pub fcutout: Vec<f64>,
}
impl DATABASE_D3MAX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_D3MAX").unwrap());
        Self {
            dtcheck: t.take("DTCHECK").and_then(|c| c.into_float()).unwrap_or_default(),
            me: t.take("ME").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrs: t.take("PSTRS").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            ifilt: t.take("IFILT").and_then(|c| c.into_int()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
            fcutout: t.take("FCUTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_DCFAIL {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_DCFAIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_DCFAIL").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_DEFGEO {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_DEFGEO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_DEFGEO").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_DEFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_DEFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_DEFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_DEMASSFLOW {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_DEMASSFLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_DEMASSFLOW").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_DISBOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_DISBOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_DISBOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ELOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
    pub option1: Vec<i64>,
    pub option2: Vec<i64>,
    pub option3: Vec<i64>,
    pub option4: Vec<i64>,
}
impl DATABASE_ELOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ELOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            option1: t.take("OPTION1").and_then(|c| c.into_int()).unwrap_or_default(),
            option2: t.take("OPTION2").and_then(|c| c.into_int()).unwrap_or_default(),
            option3: t.take("OPTION3").and_then(|c| c.into_int()).unwrap_or_default(),
            option4: t.take("OPTION4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_AVS {
    pub vtype: Vec<i64>,
    pub comp: Vec<i64>,
}
impl DATABASE_EXTENT_AVS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_AVS").unwrap());
        Self {
            vtype: t.take("VTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_BINARY {
    pub neiph: Vec<i64>,
    pub neips: Vec<i64>,
    pub maxint: Vec<i64>,
    pub strflg: Vec<i64>,
    pub sigflg: Vec<i64>,
    pub epsflg: Vec<i64>,
    pub rltflg: Vec<i64>,
    pub engflg: Vec<i64>,
    pub cmpflg: Vec<i64>,
    pub ieverp: Vec<i64>,
    pub beamip: Vec<i64>,
    pub dcomp: Vec<i64>,
    pub shge: Vec<i64>,
    pub stssz: Vec<i64>,
    pub n3thdt: Vec<i64>,
    pub ialemat: Vec<i64>,
    pub nintsld: Vec<i64>,
    pub pkp_sen: Vec<i64>,
    pub sclp: Vec<f64>,
    pub hydro: Vec<i64>,
    pub msscl: Vec<i64>,
    pub therm: Vec<i64>,
    pub intout: Vec<String>,
    pub nodout: Vec<String>,
    pub dtdt: Vec<i64>,
    pub resplt: Vec<i64>,
    pub neipb: Vec<i64>,
    pub quadsld: Vec<i64>,
    pub cubsld: Vec<i64>,
    pub deleres: Vec<i64>,
}
impl DATABASE_EXTENT_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_BINARY").unwrap());
        Self {
            neiph: t.take("NEIPH").and_then(|c| c.into_int()).unwrap_or_default(),
            neips: t.take("NEIPS").and_then(|c| c.into_int()).unwrap_or_default(),
            maxint: t.take("MAXINT").and_then(|c| c.into_int()).unwrap_or_default(),
            strflg: t.take("STRFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            sigflg: t.take("SIGFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            epsflg: t.take("EPSFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            rltflg: t.take("RLTFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            engflg: t.take("ENGFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            cmpflg: t.take("CMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ieverp: t.take("IEVERP").and_then(|c| c.into_int()).unwrap_or_default(),
            beamip: t.take("BEAMIP").and_then(|c| c.into_int()).unwrap_or_default(),
            dcomp: t.take("DCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            shge: t.take("SHGE").and_then(|c| c.into_int()).unwrap_or_default(),
            stssz: t.take("STSSZ").and_then(|c| c.into_int()).unwrap_or_default(),
            n3thdt: t.take("N3THDT").and_then(|c| c.into_int()).unwrap_or_default(),
            ialemat: t.take("IALEMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            nintsld: t.take("NINTSLD").and_then(|c| c.into_int()).unwrap_or_default(),
            pkp_sen: t.take("PKP_SEN").and_then(|c| c.into_int()).unwrap_or_default(),
            sclp: t.take("SCLP").and_then(|c| c.into_float()).unwrap_or_default(),
            hydro: t.take("HYDRO").and_then(|c| c.into_int()).unwrap_or_default(),
            msscl: t.take("MSSCL").and_then(|c| c.into_int()).unwrap_or_default(),
            therm: t.take("THERM").and_then(|c| c.into_int()).unwrap_or_default(),
            intout: t.take("INTOUT").and_then(|c| c.into_str()).unwrap_or_default(),
            nodout: t.take("NODOUT").and_then(|c| c.into_str()).unwrap_or_default(),
            dtdt: t.take("DTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            resplt: t.take("RESPLT").and_then(|c| c.into_int()).unwrap_or_default(),
            neipb: t.take("NEIPB").and_then(|c| c.into_int()).unwrap_or_default(),
            quadsld: t.take("QUADSLD").and_then(|c| c.into_int()).unwrap_or_default(),
            cubsld: t.take("CUBSLD").and_then(|c| c.into_int()).unwrap_or_default(),
            deleres: t.take("DELERES").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_BINARY_COMP {
    pub iglb: Vec<i64>,
    pub ixyz: Vec<i64>,
    pub ivel: Vec<i64>,
    pub iacc: Vec<i64>,
    pub istrs: Vec<i64>,
    pub istra: Vec<i64>,
    pub ised: Vec<i64>,
}
impl DATABASE_EXTENT_BINARY_COMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_BINARY_COMP").unwrap());
        Self {
            iglb: t.take("IGLB").and_then(|c| c.into_int()).unwrap_or_default(),
            ixyz: t.take("IXYZ").and_then(|c| c.into_int()).unwrap_or_default(),
            ivel: t.take("IVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            iacc: t.take("IACC").and_then(|c| c.into_int()).unwrap_or_default(),
            istrs: t.take("ISTRS").and_then(|c| c.into_int()).unwrap_or_default(),
            istra: t.take("ISTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            ised: t.take("ISED").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_D3PART {
    pub neiph: Vec<i64>,
    pub neips: Vec<i64>,
    pub maxint: Vec<i64>,
    pub strflg: Vec<i64>,
    pub sigflg: Vec<i64>,
    pub epsflg: Vec<i64>,
    pub rltflg: Vec<i64>,
    pub engflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub ieverp: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub shge: Vec<i64>,
    pub stssz: Vec<i64>,
    pub nintsld: Vec<i64>,
}
impl DATABASE_EXTENT_D3PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_D3PART").unwrap());
        Self {
            neiph: t.take("NEIPH").and_then(|c| c.into_int()).unwrap_or_default(),
            neips: t.take("NEIPS").and_then(|c| c.into_int()).unwrap_or_default(),
            maxint: t.take("MAXINT").and_then(|c| c.into_int()).unwrap_or_default(),
            strflg: t.take("STRFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            sigflg: t.take("SIGFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            epsflg: t.take("EPSFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            rltflg: t.take("RLTFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            engflg: t.take("ENGFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ieverp: t.take("IEVERP").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            shge: t.take("SHGE").and_then(|c| c.into_int()).unwrap_or_default(),
            stssz: t.take("STSSZ").and_then(|c| c.into_int()).unwrap_or_default(),
            nintsld: t.take("NINTSLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_INTFOR {
    pub nglbv: Vec<i64>,
    pub nvelo: Vec<i64>,
    pub npresu: Vec<i64>,
    pub nshear: Vec<i64>,
    pub nforce: Vec<i64>,
    pub ngapc: Vec<i64>,
    pub nfail: Vec<i64>,
    pub ieverf: Vec<i64>,
    pub nwear: Vec<i64>,
    pub nwusr: Vec<i64>,
    pub nhuf: Vec<i64>,
    pub ntied: Vec<i64>,
    pub neng: Vec<i64>,
    pub npen: Vec<i64>,
}
impl DATABASE_EXTENT_INTFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_INTFOR").unwrap());
        Self {
            nglbv: t.take("NGLBV").and_then(|c| c.into_int()).unwrap_or_default(),
            nvelo: t.take("NVELO").and_then(|c| c.into_int()).unwrap_or_default(),
            npresu: t.take("NPRESU").and_then(|c| c.into_int()).unwrap_or_default(),
            nshear: t.take("NSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            nforce: t.take("NFORCE").and_then(|c| c.into_int()).unwrap_or_default(),
            ngapc: t.take("NGAPC").and_then(|c| c.into_int()).unwrap_or_default(),
            nfail: t.take("NFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ieverf: t.take("IEVERF").and_then(|c| c.into_int()).unwrap_or_default(),
            nwear: t.take("NWEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            nwusr: t.take("NWUSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nhuf: t.take("NHUF").and_then(|c| c.into_int()).unwrap_or_default(),
            ntied: t.take("NTIED").and_then(|c| c.into_int()).unwrap_or_default(),
            neng: t.take("NENG").and_then(|c| c.into_int()).unwrap_or_default(),
            npen: t.take("NPEN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_MOVIE {
    pub vtype: Vec<i64>,
    pub comp: Vec<i64>,
}
impl DATABASE_EXTENT_MOVIE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_MOVIE").unwrap());
        Self {
            vtype: t.take("VTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_MPGS {
    pub vtype: Vec<i64>,
    pub comp: Vec<i64>,
}
impl DATABASE_EXTENT_MPGS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_MPGS").unwrap());
        Self {
            vtype: t.take("VTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_SSSTAT {
    pub psid1: Vec<i64>,
    pub psid2: Vec<i64>,
    pub psid3: Vec<i64>,
    pub psid4: Vec<i64>,
    pub psid5: Vec<i64>,
    pub psid6: Vec<i64>,
    pub psid7: Vec<i64>,
    pub psid8: Vec<i64>,
}
impl DATABASE_EXTENT_SSSTAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_SSSTAT").unwrap());
        Self {
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            psid2: t.take("PSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            psid3: t.take("PSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            psid4: t.take("PSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            psid5: t.take("PSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            psid6: t.take("PSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            psid7: t.take("PSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            psid8: t.take("PSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_EXTENT_SSSTAT_ID {
    pub psidn: Vec<i64>,
    pub headingn: Vec<String>,
}
impl DATABASE_EXTENT_SSSTAT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_EXTENT_SSSTAT_ID").unwrap());
        Self {
            psidn: t.take("PSIDn").and_then(|c| c.into_int()).unwrap_or_default(),
            headingn: t.take("HEADINGn").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FATXML {
    pub xml: Vec<String>,
}
impl DATABASE_FATXML {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FATXML").unwrap());
        Self {
            xml: t.take("XML").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FORMAT {
    pub iform: Vec<i64>,
    pub ibinary: Vec<i64>,
}
impl DATABASE_FORMAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FORMAT").unwrap());
        Self {
            iform: t.take("IFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            ibinary: t.take("IBINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_ELOUT_PSD {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_ELOUT_PSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_ELOUT_PSD").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_ELOUT_SSD {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_ELOUT_SSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_ELOUT_SSD").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_ELOUT_SSD_MODAL_CONTRIBUTION {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_ELOUT_SSD_MODAL_CONTRIBUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_ELOUT_SSD_MODAL_CONTRIBUTION").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_ELOUT_SSD_SUBCASE {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_ELOUT_SSD_SUBCASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_ELOUT_SSD_SUBCASE").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODFOR_SSD {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODFOR_SSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODFOR_SSD").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODFOR_SSD_SUBCASE {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODFOR_SSD_SUBCASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODFOR_SSD_SUBCASE").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODOUT_PSD {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODOUT_PSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODOUT_PSD").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODOUT_SSD {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODOUT_SSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODOUT_SSD").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODOUT_SSD_MODAL_CONTRIBUTION {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODOUT_SSD_MODAL_CONTRIBUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODOUT_SSD_MODAL_CONTRIBUTION").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_ASCII_NODOUT_SSD_SUBCASE {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_ASCII_NODOUT_SSD_SUBCASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_ASCII_NODOUT_SSD_SUBCASE").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3ACC {
    pub binary: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3ACC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3ACC").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3ACS {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3ACS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3ACS").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3ATV {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3ATV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3ATV").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3ERP {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3ERP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3ERP").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3FTG {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3FTG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3FTG").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3PSD {
    pub binary: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3PSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3PSD").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3PSD_SUMMATION {
    pub filename: Vec<String>,
}
impl DATABASE_FREQUENCY_BINARY_D3PSD_SUMMATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3PSD_SUMMATION").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3RMS {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3RMS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3RMS").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3RMS_SUMMATION {
    pub filename: Vec<String>,
}
impl DATABASE_FREQUENCY_BINARY_D3RMS_SUMMATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3RMS_SUMMATION").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3SPCM {
    pub binary: Vec<i64>,
    pub istate: Vec<i64>,
    pub filename: Vec<String>,
}
impl DATABASE_FREQUENCY_BINARY_D3SPCM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3SPCM").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            istate: t.take("ISTATE").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3SSD {
    pub binary: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3SSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3SSD").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3SSD_SUBCASE {
    pub binary: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3SSD_SUBCASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3SSD_SUBCASE").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FREQUENCY_BINARY_D3ZCF {
    pub binary: Vec<i64>,
}
impl DATABASE_FREQUENCY_BINARY_D3ZCF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FREQUENCY_BINARY_D3ZCF").unwrap());
        Self {
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FSI {
    pub dtout: Vec<f64>,
    pub binary: Vec<i64>,
    pub dbsfi_id: Vec<i64>,
    pub sid: Vec<i64>,
    pub stdype: Vec<i64>,
    pub swid: Vec<i64>,
    pub convid: Vec<i64>,
    pub ndsetid: Vec<i64>,
    pub cid: Vec<i64>,
}
impl DATABASE_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FSI").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            dbsfi_id: t.take("DBSFI_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stdype: t.take("STDYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            swid: t.take("SWID").and_then(|c| c.into_int()).unwrap_or_default(),
            convid: t.take("CONVID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndsetid: t.take("NDSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_FSI_SENSOR {
    pub dtout: Vec<f64>,
    pub binary: Vec<i64>,
    pub dbfsi_id: Vec<i64>,
    pub nid: Vec<i64>,
    pub segmid: Vec<i64>,
    pub offset: Vec<f64>,
    pub nd1: Vec<i64>,
    pub nd2: Vec<i64>,
    pub nd3: Vec<i64>,
}
impl DATABASE_FSI_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_FSI_SENSOR").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            dbfsi_id: t.take("DBFSI_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            segmid: t.take("SEGMID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            nd1: t.take("ND1").and_then(|c| c.into_int()).unwrap_or_default(),
            nd2: t.take("ND2").and_then(|c| c.into_int()).unwrap_or_default(),
            nd3: t.take("ND3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_GCEOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_GCEOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_GCEOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_GLSTAT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_GLSTAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_GLSTAT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_GLSTAT_MASS_PROPERTIES {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_GLSTAT_MASS_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_GLSTAT_MASS_PROPERTIES").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_ACOUSTIC {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_ACOUSTIC").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_BEAM {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_BEAM").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_BEAM_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_BEAM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_BEAM_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_BEAM_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_BEAM_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_BEAM_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_DISCRETE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_DISCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_DISCRETE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_DISCRETE_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_DISCRETE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_DISCRETE_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_DISCRETE_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_DISCRETE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_DISCRETE_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_NODE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE_LOCAL {
    pub id: Vec<i64>,
    pub cid: Vec<i64>,
    pub ref_: Vec<i64>,
    pub hfo: Vec<i64>,
}
impl DATABASE_HISTORY_NODE_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE_LOCAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            hfo: t.take("HFO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE_LOCAL_ID {
    pub id: Vec<i64>,
    pub cid: Vec<i64>,
    pub ref_: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_NODE_LOCAL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE_LOCAL_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_NODE_SET_LOCAL {
    pub id: Vec<i64>,
    pub cid: Vec<i64>,
    pub ref_: Vec<i64>,
    pub hfo: Vec<i64>,
}
impl DATABASE_HISTORY_NODE_SET_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_NODE_SET_LOCAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            hfo: t.take("HFO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SEATBELT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_SEATBELT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT_RETRACTOR {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SEATBELT_RETRACTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT_RETRACTOR").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT_RETRACTOR_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_SEATBELT_RETRACTOR_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT_RETRACTOR_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT_SLIPRING {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SEATBELT_SLIPRING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT_SLIPRING").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SEATBELT_SLIPRING_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_SEATBELT_SLIPRING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SEATBELT_SLIPRING_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SHELL {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SHELL").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SHELL_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_SHELL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SHELL_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SHELL_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SHELL_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SOLID {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SOLID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SOLID_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_SOLID_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SOLID_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SOLID_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SOLID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SOLID_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SPH {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SPH").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_SPH_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_SPH_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_SPH_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_TSHELL {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_TSHELL").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_TSHELL_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_HISTORY_TSHELL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_TSHELL_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_HISTORY_TSHELL_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_HISTORY_TSHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_HISTORY_TSHELL_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_ICVOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_ICVOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_ICVOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_JNTFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_JNTFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_JNTFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MASSOUT {
    pub setid: Vec<i64>,
    pub ndflg: Vec<i64>,
    pub rbflg: Vec<i64>,
}
impl DATABASE_MASSOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MASSOUT").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflg: t.take("NDFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            rbflg: t.take("RBFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MATSUM {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_MATSUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MATSUM").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_BEAM {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_BEAM").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_BEAM_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_MAX_BEAM_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_BEAM_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_BEAM_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_BEAM_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_BEAM_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SHELL {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SHELL").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SHELL_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_MAX_SHELL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SHELL_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SHELL_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SHELL_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SOLID {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SOLID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SOLID_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_MAX_SOLID_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SOLID_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_SOLID_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_SOLID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_SOLID_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_TSHELL {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_TSHELL").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_TSHELL_ID {
    pub id1: Vec<i64>,
    pub heading: Vec<String>,
}
impl DATABASE_MAX_TSHELL_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_TSHELL_ID").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MAX_TSHELL_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DATABASE_MAX_TSHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MAX_TSHELL_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MOVIE {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_MOVIE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MOVIE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_MPGS {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_MPGS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_MPGS").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_NCFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_NCFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_NCFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_NCFORC_FILTER {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
    pub rate: Vec<f64>,
    pub cutoff: Vec<f64>,
    pub window: Vec<f64>,
    pub type_: Vec<i64>,
}
impl DATABASE_NCFORC_FILTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_NCFORC_FILTER").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            rate: t.take("RATE").and_then(|c| c.into_float()).unwrap_or_default(),
            cutoff: t.take("CUTOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_NODAL_FORCE_GROUP {
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
}
impl DATABASE_NODAL_FORCE_GROUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_NODAL_FORCE_GROUP").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_NODFOR {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_NODFOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_NODFOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_NODOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
    pub option1: Vec<f64>,
    pub option2: Vec<i64>,
}
impl DATABASE_NODOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_NODOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            option1: t.take("OPTION1").and_then(|c| c.into_float()).unwrap_or_default(),
            option2: t.take("OPTION2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PAP_OUTPUT {
    pub ivel: Vec<i64>,
    pub iaccx: Vec<i64>,
    pub iaccy: Vec<i64>,
    pub iaccz: Vec<i64>,
    pub ncyout: Vec<i64>,
}
impl DATABASE_PAP_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PAP_OUTPUT").unwrap());
        Self {
            ivel: t.take("IVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccx: t.take("IACCX").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccy: t.take("IACCY").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccz: t.take("IACCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyout: t.take("NCYOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PBLAST_SENSOR {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub id: Vec<i64>,
    pub itype: Vec<i64>,
    pub offset: Vec<f64>,
    pub radius: Vec<f64>,
}
impl DATABASE_PBLAST_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PBLAST_SENSOR").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PBSTAT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_PBSTAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PBSTAT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PLLYOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_PLLYOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PLLYOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_POWER_SPECTRAL_DENSITY {
    pub fbeg: Vec<f64>,
    pub fend: Vec<f64>,
    pub fintval: Vec<f64>,
    pub ftrunk: Vec<f64>,
}
impl DATABASE_POWER_SPECTRAL_DENSITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_POWER_SPECTRAL_DENSITY").unwrap());
        Self {
            fbeg: t.take("FBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            fend: t.take("FEND").and_then(|c| c.into_float()).unwrap_or_default(),
            fintval: t.take("FINTVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ftrunk: t.take("FTRUNK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_POWER_SPECTRAL_DENSITY_FREQUENCY {
    pub fbeg: Vec<f64>,
    pub fend: Vec<f64>,
    pub fintval: Vec<f64>,
    pub ftrunk: Vec<f64>,
}
impl DATABASE_POWER_SPECTRAL_DENSITY_FREQUENCY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_POWER_SPECTRAL_DENSITY_FREQUENCY").unwrap());
        Self {
            fbeg: t.take("FBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            fend: t.take("FEND").and_then(|c| c.into_float()).unwrap_or_default(),
            fintval: t.take("FINTVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ftrunk: t.take("FTRUNK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PROFILE {
    pub dt: Vec<i64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub data: Vec<i64>,
    pub dir: Vec<i64>,
    pub updloc: Vec<i64>,
    pub mmg: Vec<i64>,
}
impl DATABASE_PROFILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PROFILE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            updloc: t.take("UPDLOC").and_then(|c| c.into_int()).unwrap_or_default(),
            mmg: t.take("MMG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PRTUBE {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_PRTUBE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PRTUBE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PWP_FLOW {
    pub nsid: Vec<i64>,
}
impl DATABASE_PWP_FLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PWP_FLOW").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PWP_OUTPUT {
    pub ivel: Vec<i64>,
    pub iaccx: Vec<i64>,
    pub iaccy: Vec<i64>,
    pub iaccz: Vec<i64>,
    pub ncyout: Vec<i64>,
}
impl DATABASE_PWP_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PWP_OUTPUT").unwrap());
        Self {
            ivel: t.take("IVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccx: t.take("IACCX").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccy: t.take("IACCY").and_then(|c| c.into_int()).unwrap_or_default(),
            iaccz: t.take("IACCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyout: t.take("NCYOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_PYRO {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_PYRO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_PYRO").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RBDOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_RBDOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RBDOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RCFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_RCFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RCFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RCFORC_MOMENT {
    pub cid: Vec<i64>,
    pub nodes: Vec<i64>,
    pub nodem: Vec<i64>,
}
impl DATABASE_RCFORC_MOMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RCFORC_MOMENT").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nodes: t.take("NODES").and_then(|c| c.into_int()).unwrap_or_default(),
            nodem: t.take("NODEM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RECOVER_NODE {
    pub psid: Vec<i64>,
    pub iax: Vec<String>,
    pub iay: Vec<String>,
    pub iaz: Vec<String>,
    pub method: Vec<i64>,
    pub ivx: Vec<String>,
    pub ivy: Vec<String>,
    pub ivz: Vec<String>,
}
impl DATABASE_RECOVER_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RECOVER_NODE").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            iax: t.take("IAX").and_then(|c| c.into_str()).unwrap_or_default(),
            iay: t.take("IAY").and_then(|c| c.into_str()).unwrap_or_default(),
            iaz: t.take("IAZ").and_then(|c| c.into_str()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            ivx: t.take("IVX").and_then(|c| c.into_str()).unwrap_or_default(),
            ivy: t.take("IVY").and_then(|c| c.into_str()).unwrap_or_default(),
            ivz: t.take("IVZ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RVE {
    pub dt: Vec<f64>,
    pub bina: Vec<i64>,
}
impl DATABASE_RVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RVE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            bina: t.take("BINA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_RWFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_RWFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_RWFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SALE {
    pub on_off: Vec<i64>,
}
impl DATABASE_SALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SALE").unwrap());
        Self {
            on_off: t.take("ON/OFF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SBTOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SBTOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SBTOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SECFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SECFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SECFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SECFORC_FILTER {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
    pub rate: Vec<f64>,
    pub cutoff: Vec<f64>,
    pub window: Vec<f64>,
    pub type_: Vec<i64>,
}
impl DATABASE_SECFORC_FILTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SECFORC_FILTER").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            rate: t.take("RATE").and_then(|c| c.into_float()).unwrap_or_default(),
            cutoff: t.take("CUTOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            window: t.take("WINDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SLEOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SLEOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SLEOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SPCFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SPCFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SPCFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SPHMASSFLOW {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SPHMASSFLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SPHMASSFLOW").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SPHOUT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SPHOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SPHOUT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SPRING_FORWARD {
    pub iflag: Vec<i64>,
}
impl DATABASE_SPRING_FORWARD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SPRING_FORWARD").unwrap());
        Self {
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SSSTAT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SSSTAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SSSTAT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SSSTAT_MASS_PROPERTIES {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SSSTAT_MASS_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SSSTAT_MASS_PROPERTIES").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SUPERPLASTIC_FORMING {
    pub dtout: Vec<f64>,
}
impl DATABASE_SUPERPLASTIC_FORMING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SUPERPLASTIC_FORMING").unwrap());
        Self {
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_SWFORC {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_SWFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_SWFORC").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TPRINT {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_TPRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TPRINT").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRACER {
    pub time: Vec<f64>,
    pub track: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub ammgid: Vec<i64>,
    pub nid: Vec<i64>,
    pub radius: Vec<f64>,
}
impl DATABASE_TRACER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRACER").unwrap());
        Self {
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            track: t.take("TRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            ammgid: t.take("AMMGID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRACER_ALE {
    pub nid: Vec<i64>,
    pub track: Vec<i64>,
    pub ammgid: Vec<i64>,
    pub hvbeg: Vec<i64>,
    pub hvend: Vec<i64>,
    pub time: Vec<f64>,
}
impl DATABASE_TRACER_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRACER_ALE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            track: t.take("TRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid: t.take("AMMGID").and_then(|c| c.into_int()).unwrap_or_default(),
            hvbeg: t.take("HVBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            hvend: t.take("HVEND").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRACER_DE {
    pub time: Vec<f64>,
    pub track: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub ammgid: Vec<i64>,
    pub nid: Vec<i64>,
    pub radius: Vec<f64>,
}
impl DATABASE_TRACER_DE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRACER_DE").unwrap());
        Self {
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            track: t.take("TRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            ammgid: t.take("AMMGID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRACER_GENERAL {
    pub node: Vec<i64>,
    pub elem: Vec<i64>,
    pub typm: Vec<i64>,
    pub move_: Vec<i64>,
    pub set: Vec<i64>,
    pub typs: Vec<i64>,
    pub dt: Vec<f64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub fid: Vec<i64>,
    pub varloc: Vec<i64>,
    pub varepl: Vec<i64>,
}
impl DATABASE_TRACER_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRACER_GENERAL").unwrap());
        Self {
            node: t.take("NODE").and_then(|c| c.into_int()).unwrap_or_default(),
            elem: t.take("ELEM").and_then(|c| c.into_int()).unwrap_or_default(),
            typm: t.take("TYPM").and_then(|c| c.into_int()).unwrap_or_default(),
            move_: t.take("MOVE").and_then(|c| c.into_int()).unwrap_or_default(),
            set: t.take("SET").and_then(|c| c.into_int()).unwrap_or_default(),
            typs: t.take("TYPS").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            varloc: t.take("VARLOC").and_then(|c| c.into_int()).unwrap_or_default(),
            varepl: t.take("VAREPL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRACER_GENERATE {
    pub dt: Vec<f64>,
    pub valow: Vec<f64>,
    pub valup: Vec<f64>,
    pub valtype1: Vec<i64>,
    pub set: Vec<i64>,
    pub setype: Vec<i64>,
    pub mmgset: Vec<i64>,
    pub updt: Vec<f64>,
    pub varloc: Vec<i64>,
    pub valtype2: Vec<i64>,
    pub mmgset_2: Vec<i64>,
}
impl DATABASE_TRACER_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRACER_GENERATE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            valow: t.take("VALOW").and_then(|c| c.into_float()).unwrap_or_default(),
            valup: t.take("VALUP").and_then(|c| c.into_float()).unwrap_or_default(),
            valtype1: t.take("VALTYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            set: t.take("SET").and_then(|c| c.into_int()).unwrap_or_default(),
            setype: t.take("SETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            updt: t.take("UPDT").and_then(|c| c.into_float()).unwrap_or_default(),
            varloc: t.take("VARLOC").and_then(|c| c.into_int()).unwrap_or_default(),
            valtype2: t.take("VALTYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgset_2: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DATABASE_TRHIST {
    pub dt: Vec<f64>,
    pub binary: Vec<i64>,
    pub lcur: Vec<i64>,
    pub ioopt: Vec<i64>,
}
impl DATABASE_TRHIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DATABASE_TRHIST").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            binary: t.take("BINARY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            ioopt: t.take("IOOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ADAPTIVE_SOLID_TO_DES {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub ipid: Vec<i64>,
    pub itype: Vec<i64>,
    pub nq: Vec<i64>,
    pub ipdes: Vec<i64>,
    pub isdes: Vec<i64>,
    pub rsf: Vec<f64>,
    pub outdes: Vec<i64>,
    pub ibond: Vec<i64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl DEFINE_ADAPTIVE_SOLID_TO_DES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ADAPTIVE_SOLID_TO_DES").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            ipid: t.take("IPID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nq: t.take("NQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ipdes: t.take("IPDES").and_then(|c| c.into_int()).unwrap_or_default(),
            isdes: t.take("ISDES").and_then(|c| c.into_int()).unwrap_or_default(),
            rsf: t.take("RSF").and_then(|c| c.into_float()).unwrap_or_default(),
            outdes: t.take("OUTDES").and_then(|c| c.into_int()).unwrap_or_default(),
            ibond: t.take("IBOND").and_then(|c| c.into_int()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ADAPTIVE_SOLID_TO_DES_ID {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub ipid: Vec<i64>,
    pub itype: Vec<i64>,
    pub nq: Vec<i64>,
    pub ipdes: Vec<i64>,
    pub isdes: Vec<i64>,
    pub rsf: Vec<f64>,
    pub outdes: Vec<i64>,
    pub ibond: Vec<i64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl DEFINE_ADAPTIVE_SOLID_TO_DES_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ADAPTIVE_SOLID_TO_DES_ID").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            ipid: t.take("IPID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nq: t.take("NQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ipdes: t.take("IPDES").and_then(|c| c.into_int()).unwrap_or_default(),
            isdes: t.take("ISDES").and_then(|c| c.into_int()).unwrap_or_default(),
            rsf: t.take("RSF").and_then(|c| c.into_float()).unwrap_or_default(),
            outdes: t.take("OUTDES").and_then(|c| c.into_int()).unwrap_or_default(),
            ibond: t.take("IBOND").and_then(|c| c.into_int()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ADAPTIVE_SOLID_TO_SPH {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub ipid: Vec<i64>,
    pub itype: Vec<i64>,
    pub nq: Vec<i64>,
    pub ipsph: Vec<i64>,
    pub issph: Vec<i64>,
    pub icpl: Vec<i64>,
    pub iopt: Vec<i64>,
    pub cpcd: Vec<f64>,
}
impl DEFINE_ADAPTIVE_SOLID_TO_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ADAPTIVE_SOLID_TO_SPH").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            ipid: t.take("IPID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nq: t.take("NQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsph: t.take("IPSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            issph: t.take("ISSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            icpl: t.take("ICPL").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cpcd: t.take("CPCD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ADAPTIVE_SOLID_TO_SPH_ID {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub ipid: Vec<i64>,
    pub itype: Vec<i64>,
    pub nq: Vec<i64>,
    pub ipsph: Vec<i64>,
    pub issph: Vec<i64>,
    pub icpl: Vec<i64>,
    pub iopt: Vec<i64>,
    pub cpcd: Vec<f64>,
}
impl DEFINE_ADAPTIVE_SOLID_TO_SPH_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ADAPTIVE_SOLID_TO_SPH_ID").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            ipid: t.take("IPID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nq: t.take("NQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ipsph: t.take("IPSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            issph: t.take("ISSPH").and_then(|c| c.into_int()).unwrap_or_default(),
            icpl: t.take("ICPL").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cpcd: t.take("CPCD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ALEBAG_BAG {
    pub bagid: Vec<i64>,
    pub sid: Vec<i64>,
    pub sidtype: Vec<i64>,
    pub cvbag: Vec<i64>,
    pub iblock: Vec<i64>,
    pub vcof: Vec<f64>,
    pub vset: Vec<i64>,
    pub vtype: Vec<i64>,
    pub nquad: Vec<i64>,
    pub ctype: Vec<i64>,
    pub pfac: Vec<f64>,
    pub fric: Vec<f64>,
    pub frcmin: Vec<f64>,
    pub normtyp: Vec<i64>,
    pub ileak: Vec<i64>,
    pub pleak: Vec<f64>,
    pub norm: Vec<i64>,
    pub start: Vec<f64>,
    pub end: Vec<f64>,
}
impl DEFINE_ALEBAG_BAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ALEBAG_BAG").unwrap());
        Self {
            bagid: t.take("BAGID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtype: t.take("SIDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cvbag: t.take("CVBAG").and_then(|c| c.into_int()).unwrap_or_default(),
            iblock: t.take("IBLOCK").and_then(|c| c.into_int()).unwrap_or_default(),
            vcof: t.take("VCOF").and_then(|c| c.into_float()).unwrap_or_default(),
            vset: t.take("VSET").and_then(|c| c.into_int()).unwrap_or_default(),
            vtype: t.take("VTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pfac: t.take("PFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            frcmin: t.take("FRCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            normtyp: t.take("NORMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ileak: t.take("ILEAK").and_then(|c| c.into_int()).unwrap_or_default(),
            pleak: t.take("PLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            end: t.take("END").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ALEBAG_HOLE {
    pub holeid: Vec<i64>,
    pub sid: Vec<i64>,
    pub sidtype: Vec<i64>,
    pub nquad: Vec<i64>,
    pub xoff: Vec<f64>,
    pub nfold: Vec<i64>,
    pub xclen: Vec<f64>,
    pub int_ext: Vec<i64>,
}
impl DEFINE_ALEBAG_HOLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ALEBAG_HOLE").unwrap());
        Self {
            holeid: t.take("HOLEID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtype: t.take("SIDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            xoff: t.take("XOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            nfold: t.take("NFOLD").and_then(|c| c.into_int()).unwrap_or_default(),
            xclen: t.take("XCLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            int_ext: t.take("INT/EXT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ALEBAG_INFLATOR {
    pub infid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub ngas: Vec<i64>,
    pub norif: Vec<i64>,
    pub lcvel: Vec<i64>,
    pub lct: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub mwgas: Vec<f64>,
    pub unused_3_2: Vec<i64>,
    pub gasa: Vec<f64>,
    pub gasb: Vec<f64>,
    pub gasc: Vec<f64>,
    pub nodeid: Vec<i64>,
    pub vecid: Vec<i64>,
    pub orifare: Vec<f64>,
}
impl DEFINE_ALEBAG_INFLATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ALEBAG_INFLATOR").unwrap());
        Self {
            infid: t.take("INFID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            ngas: t.take("NGAS").and_then(|c| c.into_int()).unwrap_or_default(),
            norif: t.take("NORIF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvel: t.take("LCVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            mwgas: t.take("MWGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            gasa: t.take("GASA").and_then(|c| c.into_float()).unwrap_or_default(),
            gasb: t.take("GASB").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            orifare: t.take("ORIFARE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BEAM_SOLID_COUPLING {
    pub lstrid: Vec<i64>,
    pub msolidm: Vec<i64>,
    pub lstrtype: Vec<i64>,
    pub soltype: Vec<i64>,
    pub form: Vec<i64>,
    pub psf: Vec<f64>,
}
impl DEFINE_BEAM_SOLID_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BEAM_SOLID_COUPLING").unwrap());
        Self {
            lstrid: t.take("LSTRID").and_then(|c| c.into_int()).unwrap_or_default(),
            msolidm: t.take("MSOLIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            lstrtype: t.take("LSTRTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            soltype: t.take("SOLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            psf: t.take("PSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
}
impl DEFINE_BOX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_ADAPTIVE {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub pid: Vec<i64>,
    pub level: Vec<i64>,
    pub lidx_ndid: Vec<i64>,
    pub lidy: Vec<i64>,
    pub lidz: Vec<i64>,
    pub brmin: Vec<f64>,
    pub brmax: Vec<f64>,
}
impl DEFINE_BOX_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_ADAPTIVE").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            level: t.take("LEVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            lidx_ndid: t.take("LIDX/NDID").and_then(|c| c.into_int()).unwrap_or_default(),
            lidy: t.take("LIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lidz: t.take("LIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            brmin: t.take("BRMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            brmax: t.take("BRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_ADAPTIVE_LOCAL {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub pid: Vec<i64>,
    pub level: Vec<i64>,
    pub lidx_ndid: Vec<i64>,
    pub lidy: Vec<i64>,
    pub lidz: Vec<i64>,
    pub brmin: Vec<f64>,
    pub brmax: Vec<f64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
}
impl DEFINE_BOX_ADAPTIVE_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_ADAPTIVE_LOCAL").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            level: t.take("LEVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            lidx_ndid: t.take("LIDX/NDID").and_then(|c| c.into_int()).unwrap_or_default(),
            lidy: t.take("LIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lidz: t.take("LIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            brmin: t.take("BRMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            brmax: t.take("BRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_COARSEN {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub iflag: Vec<i64>,
}
impl DEFINE_BOX_COARSEN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_COARSEN").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_COARSEN_LOCAL {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub iflag: Vec<i64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
}
impl DEFINE_BOX_COARSEN_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_COARSEN_LOCAL").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_DRAWBEAD {
    pub boxid: Vec<i64>,
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub idir: Vec<i64>,
    pub stype: Vec<i64>,
    pub radius: Vec<f64>,
    pub cid: Vec<i64>,
}
impl DEFINE_BOX_DRAWBEAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_DRAWBEAD").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_DRAWBEAD_LOCAL {
    pub boxid: Vec<i64>,
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub idir: Vec<i64>,
    pub stype: Vec<i64>,
    pub radius: Vec<f64>,
    pub cid: Vec<i64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
}
impl DEFINE_BOX_DRAWBEAD_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_DRAWBEAD_LOCAL").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_LOCAL {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
}
impl DEFINE_BOX_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_LOCAL").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_NODES_ADAPTIVE {
    pub boxid: Vec<i64>,
    pub node: Vec<i64>,
    pub lcx: Vec<i64>,
    pub lcy: Vec<i64>,
    pub lcz: Vec<i64>,
    pub itype: Vec<i64>,
    pub radius: Vec<f64>,
    pub npiece: Vec<i64>,
    pub pid: Vec<i64>,
    pub level: Vec<i64>,
}
impl DEFINE_BOX_NODES_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_NODES_ADAPTIVE").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            node: t.take("NODE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy: t.take("LCY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcz: t.take("LCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            npiece: t.take("NPIECE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            level: t.take("LEVEL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_SPH {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub vid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vd: Vec<i64>,
    pub nid: Vec<i64>,
    pub ireact: Vec<i64>,
    pub ibuff: Vec<i64>,
    pub ishow: Vec<i64>,
    pub pid: Vec<i64>,
}
impl DEFINE_BOX_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_SPH").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vd: t.take("VD").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ireact: t.take("IREACT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibuff: t.take("IBUFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ishow: t.take("ISHOW").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_BOX_SPH_LOCAL {
    pub boxid: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub vid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vd: Vec<i64>,
    pub nid: Vec<i64>,
    pub ireact: Vec<i64>,
    pub ibuff: Vec<i64>,
    pub ishow: Vec<i64>,
    pub pid: Vec<i64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
}
impl DEFINE_BOX_SPH_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_BOX_SPH_LOCAL").unwrap());
        Self {
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vd: t.take("VD").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ireact: t.take("IREACT").and_then(|c| c.into_int()).unwrap_or_default(),
            ibuff: t.take("IBUFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ishow: t.take("ISHOW").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONNECTION_PROPERTIES {
    pub con_id: Vec<i64>,
    pub proprul: Vec<i64>,
    pub areaeq: Vec<i64>,
    pub f30: Vec<i64>,
    pub dgtyp: Vec<i64>,
    pub moarfl: Vec<i64>,
    pub f0: Vec<i64>,
    pub dsigy: Vec<f64>,
    pub detan: Vec<f64>,
    pub ddg_pr: Vec<f64>,
    pub drank: Vec<f64>,
    pub dsn: Vec<f64>,
    pub dsb: Vec<f64>,
    pub dss: Vec<f64>,
    pub dexsn: Vec<f64>,
    pub dexsb: Vec<f64>,
    pub dexss: Vec<f64>,
    pub dcsn: Vec<i64>,
    pub dlcsb: Vec<i64>,
    pub dlcss: Vec<i64>,
    pub dgfad: Vec<i64>,
    pub dsclmrr: Vec<f64>,
}
impl DEFINE_CONNECTION_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONNECTION_PROPERTIES").unwrap());
        Self {
            con_id: t.take("CON_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            proprul: t.take("PROPRUL").and_then(|c| c.into_int()).unwrap_or_default(),
            areaeq: t.take("AREAEQ").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            dgtyp: t.take("DGTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            moarfl: t.take("MOARFL").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            dsigy: t.take("DSIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            detan: t.take("DETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            ddg_pr: t.take("DDG_PR").and_then(|c| c.into_float()).unwrap_or_default(),
            drank: t.take("DRANK").and_then(|c| c.into_float()).unwrap_or_default(),
            dsn: t.take("DSN").and_then(|c| c.into_float()).unwrap_or_default(),
            dsb: t.take("DSB").and_then(|c| c.into_float()).unwrap_or_default(),
            dss: t.take("DSS").and_then(|c| c.into_float()).unwrap_or_default(),
            dexsn: t.take("DEXSN").and_then(|c| c.into_float()).unwrap_or_default(),
            dexsb: t.take("DEXSB").and_then(|c| c.into_float()).unwrap_or_default(),
            dexss: t.take("DEXSS").and_then(|c| c.into_float()).unwrap_or_default(),
            dcsn: t.take("DCSN").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcsb: t.take("DLCSB").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcss: t.take("DLCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            dgfad: t.take("DGFAD").and_then(|c| c.into_int()).unwrap_or_default(),
            dsclmrr: t.take("DSCLMRR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONNECTION_PROPERTIES_ADD {
    pub con_id: Vec<i64>,
    pub proprul: Vec<i64>,
    pub areaeq: Vec<i64>,
    pub f30: Vec<i64>,
    pub dg_typ: Vec<i64>,
    pub moarfl: Vec<i64>,
    pub mid: Vec<i64>,
    pub sgiy: Vec<f64>,
    pub etan: Vec<f64>,
    pub dgpr: Vec<f64>,
    pub rank: Vec<f64>,
    pub sn: Vec<f64>,
    pub sb: Vec<f64>,
    pub ss: Vec<f64>,
    pub exsn: Vec<f64>,
    pub exsb: Vec<f64>,
    pub exss: Vec<f64>,
    pub lcsn: Vec<i64>,
    pub lcsb: Vec<i64>,
    pub lcss: Vec<i64>,
    pub gfad: Vec<i64>,
    pub sclmrr: Vec<f64>,
}
impl DEFINE_CONNECTION_PROPERTIES_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONNECTION_PROPERTIES_ADD").unwrap());
        Self {
            con_id: t.take("CON_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            proprul: t.take("PROPRUL").and_then(|c| c.into_int()).unwrap_or_default(),
            areaeq: t.take("AREAEQ").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            dg_typ: t.take("DG_TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            moarfl: t.take("MOARFL").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            sgiy: t.take("SGIY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            dgpr: t.take("DGPR").and_then(|c| c.into_float()).unwrap_or_default(),
            rank: t.take("RANK").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            exsn: t.take("EXSN").and_then(|c| c.into_float()).unwrap_or_default(),
            exsb: t.take("EXSB").and_then(|c| c.into_float()).unwrap_or_default(),
            exss: t.take("EXSS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsn: t.take("LCSN").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsb: t.take("LCSB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            gfad: t.take("GFAD").and_then(|c| c.into_int()).unwrap_or_default(),
            sclmrr: t.take("SCLMRR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONSTRUCTION_STAGES {
    pub istage: Vec<i64>,
    pub ats: Vec<f64>,
    pub ate: Vec<f64>,
    pub atr: Vec<f64>,
    pub rts: Vec<f64>,
    pub rte: Vec<f64>,
    pub f: Vec<f64>,
    pub idynain: Vec<i64>,
}
impl DEFINE_CONSTRUCTION_STAGES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONSTRUCTION_STAGES").unwrap());
        Self {
            istage: t.take("ISTAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            ats: t.take("ATS").and_then(|c| c.into_float()).unwrap_or_default(),
            ate: t.take("ATE").and_then(|c| c.into_float()).unwrap_or_default(),
            atr: t.take("ATR").and_then(|c| c.into_float()).unwrap_or_default(),
            rts: t.take("RTS").and_then(|c| c.into_float()).unwrap_or_default(),
            rte: t.take("RTE").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            idynain: t.take("IDYNAIN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONTACT_EXCLUSION {
    pub eid: Vec<i64>,
    pub title: Vec<String>,
    pub target: Vec<i64>,
    pub c1: Vec<i64>,
    pub c2: Vec<i64>,
    pub c3: Vec<i64>,
    pub c4: Vec<i64>,
    pub c5: Vec<i64>,
    pub c6: Vec<i64>,
    pub c7: Vec<i64>,
    pub c8: Vec<i64>,
    pub c9: Vec<i64>,
    pub c10: Vec<i64>,
    pub c11: Vec<i64>,
    pub c12: Vec<i64>,
    pub c13: Vec<i64>,
    pub c14: Vec<i64>,
    pub c15: Vec<i64>,
}
impl DEFINE_CONTACT_EXCLUSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONTACT_EXCLUSION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("Title").and_then(|c| c.into_str()).unwrap_or_default(),
            target: t.take("Target").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_int()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_int()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_int()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_int()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_int()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_int()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_int()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_int()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_int()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_int()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_int()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_int()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_int()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_int()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONTACT_VOLUME {
    pub cvid: Vec<i64>,
    pub cid: Vec<i64>,
    pub type_: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
    pub length: Vec<f64>,
    pub rinner: Vec<f64>,
    pub router: Vec<f64>,
    pub d_angc: Vec<f64>,
    pub rinner_2: Vec<f64>,
    pub router_2: Vec<f64>,
    pub d_angs: Vec<f64>,
}
impl DEFINE_CONTACT_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONTACT_VOLUME").unwrap());
        Self {
            cvid: t.take("CVID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
            length: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            rinner: t.take("RINNER").and_then(|c| c.into_float()).unwrap_or_default(),
            router: t.take("ROUTER").and_then(|c| c.into_float()).unwrap_or_default(),
            d_angc: t.take("D_ANGC").and_then(|c| c.into_float()).unwrap_or_default(),
            rinner_2: t.take("RINNER").and_then(|c| c.into_float()).unwrap_or_default(),
            router_2: t.take("ROUTER").and_then(|c| c.into_float()).unwrap_or_default(),
            d_angs: t.take("D_ANGS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONTROL_VOLUME {
    pub id: Vec<i64>,
    pub sid: Vec<i64>,
    pub p0: Vec<f64>,
}
impl DEFINE_CONTROL_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONTROL_VOLUME").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONTROL_VOLUME_FLOW_AREA {
    pub id: Vec<i64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub pid: Vec<i64>,
    pub area: Vec<f64>,
    pub cviid: Vec<f64>,
}
impl DEFINE_CONTROL_VOLUME_FLOW_AREA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONTROL_VOLUME_FLOW_AREA").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            area: t.take("Area").and_then(|c| c.into_float()).unwrap_or_default(),
            cviid: t.take("CVIID").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CONTROL_VOLUME_INTERACTION {
    pub id: Vec<i64>,
    pub cvid1: Vec<i64>,
    pub cvid2: Vec<i64>,
    pub lcid: Vec<i64>,
    pub area: Vec<f64>,
}
impl DEFINE_CONTROL_VOLUME_INTERACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CONTROL_VOLUME_INTERACTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cvid1: t.take("CVID1").and_then(|c| c.into_int()).unwrap_or_default(),
            cvid2: t.take("CVID2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            area: t.take("Area").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_COORDINATE_NODE {
    pub cid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub flag: Vec<i64>,
    pub dir: Vec<String>,
}
impl DEFINE_COORDINATE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_COORDINATE_NODE").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_COORDINATE_NODES {
    pub cid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub flag: Vec<i64>,
    pub dir: Vec<String>,
}
impl DEFINE_COORDINATE_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_COORDINATE_NODES").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_COORDINATE_SYSTEM {
    pub cid: Vec<i64>,
    pub xo: Vec<f64>,
    pub yo: Vec<f64>,
    pub zo: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub cidl: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
}
impl DEFINE_COORDINATE_SYSTEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_COORDINATE_SYSTEM").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            yo: t.take("YO").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("ZO").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            cidl: t.take("CIDL").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_COORDINATE_VECTOR {
    pub cid: Vec<i64>,
    pub xx: Vec<f64>,
    pub yx: Vec<f64>,
    pub zx: Vec<f64>,
    pub xv: Vec<f64>,
    pub yv: Vec<f64>,
    pub zv: Vec<f64>,
    pub nid: Vec<i64>,
}
impl DEFINE_COORDINATE_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_COORDINATE_VECTOR").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            yx: t.take("YX").and_then(|c| c.into_float()).unwrap_or_default(),
            zx: t.take("ZX").and_then(|c| c.into_float()).unwrap_or_default(),
            xv: t.take("XV").and_then(|c| c.into_float()).unwrap_or_default(),
            yv: t.take("YV").and_then(|c| c.into_float()).unwrap_or_default(),
            zv: t.take("ZV").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_BAG_INTERACTION {
    pub bagid1: Vec<i64>,
    pub bagid2: Vec<i64>,
    pub nspec: Vec<i64>,
}
impl DEFINE_CPM_BAG_INTERACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_BAG_INTERACTION").unwrap());
        Self {
            bagid1: t.take("BAGID1").and_then(|c| c.into_int()).unwrap_or_default(),
            bagid2: t.take("BAGID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nspec: t.take("NSPEC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_CHAMBER {
    pub id: Vec<i64>,
    pub nchm: Vec<i64>,
    pub sid1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub ninter: Vec<i64>,
    pub chm_id: Vec<i64>,
    pub sid3: Vec<i64>,
    pub itype3: Vec<i64>,
    pub tochm: Vec<i64>,
}
impl DEFINE_CPM_CHAMBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_CHAMBER").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchm: t.take("NCHM").and_then(|c| c.into_int()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ninter: t.take("NINTER").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            itype3: t.take("ITYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            tochm: t.take("TOCHM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_CHAMBERS {
    pub id: Vec<i64>,
    pub nchm: Vec<i64>,
    pub sid1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub ninter: Vec<i64>,
    pub chm_id: Vec<i64>,
    pub sid3: Vec<i64>,
    pub itype3: Vec<i64>,
    pub tochm: Vec<i64>,
}
impl DEFINE_CPM_CHAMBERS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_CHAMBERS").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchm: t.take("NCHM").and_then(|c| c.into_int()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ninter: t.take("NINTER").and_then(|c| c.into_int()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            itype3: t.take("ITYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            tochm: t.take("TOCHM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_GAS_PROPERTIES {
    pub id: Vec<i64>,
    pub xmm: Vec<f64>,
    pub cp0: Vec<f64>,
    pub cp1: Vec<f64>,
    pub cp2: Vec<f64>,
    pub cp3: Vec<f64>,
    pub cp4: Vec<f64>,
    pub mut0: Vec<f64>,
    pub mut1: Vec<f64>,
    pub mut2: Vec<f64>,
    pub mut3: Vec<f64>,
    pub mut4: Vec<f64>,
    pub chm_id: Vec<i64>,
    pub vini: Vec<f64>,
}
impl DEFINE_CPM_GAS_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_GAS_PROPERTIES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmm: t.take("Xmm").and_then(|c| c.into_float()).unwrap_or_default(),
            cp0: t.take("Cp0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1: t.take("Cp1").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2: t.take("Cp2").and_then(|c| c.into_float()).unwrap_or_default(),
            cp3: t.take("Cp3").and_then(|c| c.into_float()).unwrap_or_default(),
            cp4: t.take("Cp4").and_then(|c| c.into_float()).unwrap_or_default(),
            mut0: t.take("MUt0").and_then(|c| c.into_float()).unwrap_or_default(),
            mut1: t.take("MUt1").and_then(|c| c.into_float()).unwrap_or_default(),
            mut2: t.take("MUt2").and_then(|c| c.into_float()).unwrap_or_default(),
            mut3: t.take("MUt3").and_then(|c| c.into_float()).unwrap_or_default(),
            mut4: t.take("MUt4").and_then(|c| c.into_float()).unwrap_or_default(),
            chm_id: t.take("CHM_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            vini: t.take("VINI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_NPDATA {
    pub id: Vec<i64>,
    pub hconv: Vec<f64>,
    pub pfric: Vec<f64>,
    pub sdfblk: Vec<f64>,
    pub kp: Vec<f64>,
    pub inip: Vec<i64>,
    pub cp: Vec<f64>,
    pub psfdcf: Vec<f64>,
    pub id_2: Vec<i64>,
}
impl DEFINE_CPM_NPDATA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_NPDATA").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            hconv: t.take("HCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            pfric: t.take("PFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfblk: t.take("SDFBLK").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            inip: t.take("INIP").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            psfdcf: t.take("PSFDCF").and_then(|c| c.into_float()).unwrap_or_default(),
            id_2: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CPM_VENT {
    pub id: Vec<i64>,
    pub c23: Vec<f64>,
    pub lctc23: Vec<i64>,
    pub lcpc23: Vec<i64>,
    pub enh_v: Vec<i64>,
    pub ppop: Vec<f64>,
    pub c23up: Vec<f64>,
    pub iopt: Vec<i64>,
    pub jt: Vec<i64>,
    pub ids1: Vec<i64>,
    pub ids2: Vec<i64>,
    pub iopt1: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub vang: Vec<f64>,
    pub lcred: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid2_2: Vec<i64>,
    pub lcac23: Vec<i64>,
    pub psetpv: Vec<i64>,
    pub sfpv: Vec<i64>,
    pub lpatm: Vec<i64>,
    pub jtnd: Vec<i64>,
}
impl DEFINE_CPM_VENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CPM_VENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            lctc23: t.take("LCTC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpc23: t.take("LCPC23").and_then(|c| c.into_int()).unwrap_or_default(),
            enh_v: t.take("ENH_V").and_then(|c| c.into_int()).unwrap_or_default(),
            ppop: t.take("PPOP").and_then(|c| c.into_float()).unwrap_or_default(),
            c23up: t.take("C23UP").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            jt: t.take("JT").and_then(|c| c.into_int()).unwrap_or_default(),
            ids1: t.take("IDS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ids2: t.take("IDS2").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt1: t.take("IOPT1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            vang: t.take("VANG").and_then(|c| c.into_float()).unwrap_or_default(),
            lcred: t.take("LCRED").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2_2: t.take("NID2_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcac23: t.take("LCAC23").and_then(|c| c.into_int()).unwrap_or_default(),
            psetpv: t.take("PSETPV").and_then(|c| c.into_int()).unwrap_or_default(),
            sfpv: t.take("SFPV").and_then(|c| c.into_int()).unwrap_or_default(),
            lpatm: t.take("LPATM").and_then(|c| c.into_int()).unwrap_or_default(),
            jtnd: t.take("JTND").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CRASHFRONT {
    pub sid: Vec<i64>,
    pub type_: Vec<i64>,
}
impl DEFINE_CRASHFRONT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CRASHFRONT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE {
    pub lcid: Vec<i64>,
    pub sidr: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub dattyp: Vec<i64>,
    pub lcint: Vec<i64>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            dattyp: t.take("DATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_3858 {
    pub lcid: Vec<i64>,
    pub sidr: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub dattyp: Vec<i64>,
    pub lcint: Vec<i64>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_CURVE_3858 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_3858").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            dattyp: t.take("DATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_5434A {
    pub lcid: Vec<i64>,
    pub sidr: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub dattyp: Vec<i64>,
    pub lcint: Vec<i64>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_CURVE_5434A {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_5434A").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            dattyp: t.take("DATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_BOX_ADAPTIVITY {
    pub id: Vec<i64>,
    pub pid: Vec<i64>,
    pub level: Vec<i64>,
    pub dist1: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl DEFINE_CURVE_BOX_ADAPTIVITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_BOX_ADAPTIVITY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            level: t.take("LEVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            dist1: t.take("DIST1").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_COMPENSATION_CONSTRAINT_BEGIN {
    pub crvid: Vec<i64>,
    pub in_out: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl DEFINE_CURVE_COMPENSATION_CONSTRAINT_BEGIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_COMPENSATION_CONSTRAINT_BEGIN").unwrap());
        Self {
            crvid: t.take("CRVID").and_then(|c| c.into_int()).unwrap_or_default(),
            in_out: t.take("IN/OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_COMPENSATION_CONSTRAINT_END {
    pub crvid: Vec<i64>,
    pub in_out: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl DEFINE_CURVE_COMPENSATION_CONSTRAINT_END {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_COMPENSATION_CONSTRAINT_END").unwrap());
        Self {
            crvid: t.take("CRVID").and_then(|c| c.into_int()).unwrap_or_default(),
            in_out: t.take("IN/OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_DRAWBEAD {
    pub cid: Vec<i64>,
    pub tcype: Vec<i64>,
    pub vid: Vec<i64>,
    pub pid: Vec<i64>,
    pub blkid: Vec<i64>,
    pub perct: Vec<i64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub filename: Vec<String>,
}
impl DEFINE_CURVE_DRAWBEAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_DRAWBEAD").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            tcype: t.take("TCYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            blkid: t.take("BLKID").and_then(|c| c.into_int()).unwrap_or_default(),
            perct: t.take("PERCT").and_then(|c| c.into_int()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_DUPLICATE {
    pub lcid: Vec<i64>,
    pub rlcid: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub dattyp: Vec<i64>,
}
impl DEFINE_CURVE_DUPLICATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_DUPLICATE").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            rlcid: t.take("RLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            dattyp: t.take("DATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_ENTITY {
    pub lcid: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub sfr: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub offr: Vec<f64>,
    pub ai: Vec<f64>,
    pub oi: Vec<f64>,
    pub ri: Vec<f64>,
    pub iflag: Vec<i64>,
}
impl DEFINE_CURVE_ENTITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_ENTITY").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offr: t.take("OFFR").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            oi: t.take("Oi").and_then(|c| c.into_float()).unwrap_or_default(),
            ri: t.take("Ri").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_FEEDBACK {
    pub lcid: Vec<i64>,
    pub pid: Vec<i64>,
    pub boxid: Vec<i64>,
    pub fldid: Vec<i64>,
    pub fsl: Vec<f64>,
    pub tsl: Vec<f64>,
    pub sff: Vec<f64>,
    pub sft: Vec<f64>,
    pub bias: Vec<f64>,
}
impl DEFINE_CURVE_FEEDBACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_FEEDBACK").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            fldid: t.take("FLDID").and_then(|c| c.into_int()).unwrap_or_default(),
            fsl: t.take("FSL").and_then(|c| c.into_float()).unwrap_or_default(),
            tsl: t.take("TSL").and_then(|c| c.into_float()).unwrap_or_default(),
            sff: t.take("SFF").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            bias: t.take("BIAS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_FLC {
    pub lcid: Vec<i64>,
    pub th: Vec<f64>,
    pub n: Vec<f64>,
}
impl DEFINE_CURVE_FLC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_FLC").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            th: t.take("TH").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_FLD_FROM_TRIAXIAL_LIMIT {
    pub lcid: Vec<i64>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_CURVE_FLD_FROM_TRIAXIAL_LIMIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_FLD_FROM_TRIAXIAL_LIMIT").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_FUNCTION {
    pub lcid: Vec<i64>,
    pub sidr: Vec<i64>,
    pub sfa: Vec<f64>,
    pub sfo: Vec<f64>,
    pub offa: Vec<f64>,
    pub offo: Vec<f64>,
    pub dattyp: Vec<i64>,
    pub function: Vec<String>,
}
impl DEFINE_CURVE_FUNCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_FUNCTION").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfo: t.take("SFO").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offo: t.take("OFFO").and_then(|c| c.into_float()).unwrap_or_default(),
            dattyp: t.take("DATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            function: t.take("FUNCTION").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_SMOOTH {
    pub lcid: Vec<i64>,
    pub sidr: Vec<i64>,
    pub dist: Vec<f64>,
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
    pub trise: Vec<f64>,
    pub vmax: Vec<f64>,
}
impl DEFINE_CURVE_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_SMOOTH").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidr: t.take("SIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            trise: t.take("TRISE").and_then(|c| c.into_float()).unwrap_or_default(),
            vmax: t.take("VMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_STRESS {
    pub lcid: Vec<i64>,
    pub itype: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub lcid_2: Vec<i64>,
    pub itype_2: Vec<i64>,
    pub p1_2: Vec<f64>,
    pub p2_2: Vec<f64>,
    pub p3_2: Vec<f64>,
    pub p4_2: Vec<f64>,
    pub p5_2: Vec<f64>,
    pub p6_2: Vec<f64>,
}
impl DEFINE_CURVE_STRESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_STRESS").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_2: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype_2: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            p1_2: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2_2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3_2: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4_2: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5_2: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6_2: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_TRIAXIAL_LIMIT_FROM_FLD {
    pub lcid: Vec<i64>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_CURVE_TRIAXIAL_LIMIT_FROM_FLD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_TRIAXIAL_LIMIT_FROM_FLD").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_TRIM {
    pub tcid: Vec<i64>,
    pub tctype: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub tctol: Vec<f64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub filename: Vec<String>,
}
impl DEFINE_CURVE_TRIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_TRIM").unwrap());
        Self {
            tcid: t.take("TCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tctype: t.take("TCTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            tctol: t.take("TCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_TRIM_2D {
    pub tcid: Vec<i64>,
    pub tctype: Vec<i64>,
    pub tflg: Vec<i64>,
    pub tdir: Vec<i64>,
    pub tctol: Vec<f64>,
    pub depth: Vec<f64>,
    pub nseed1: Vec<i64>,
    pub nseed2: Vec<i64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub filename: Vec<String>,
}
impl DEFINE_CURVE_TRIM_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_TRIM_2D").unwrap());
        Self {
            tcid: t.take("TCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tctype: t.take("TCTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            tflg: t.take("TFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tdir: t.take("TDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            tctol: t.take("TCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            depth: t.take("DEPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nseed1: t.take("NSEED1").and_then(|c| c.into_int()).unwrap_or_default(),
            nseed2: t.take("NSEED2").and_then(|c| c.into_int()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_TRIM_3D {
    pub tcid: Vec<i64>,
    pub tctype: Vec<i64>,
    pub f: Vec<i64>,
    pub tdir: Vec<i64>,
    pub tctol: Vec<f64>,
    pub toln: Vec<f64>,
    pub nseed1: Vec<i64>,
    pub nseed2: Vec<i64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub cz: Vec<f64>,
    pub filename: Vec<String>,
}
impl DEFINE_CURVE_TRIM_3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_TRIM_3D").unwrap());
        Self {
            tcid: t.take("TCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tctype: t.take("TCTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            tdir: t.take("TDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            tctol: t.take("TCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            toln: t.take("TOLN").and_then(|c| c.into_float()).unwrap_or_default(),
            nseed1: t.take("NSEED1").and_then(|c| c.into_int()).unwrap_or_default(),
            nseed2: t.take("NSEED2").and_then(|c| c.into_int()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_CURVE_TRIM_NEW {
    pub tcid: Vec<i64>,
    pub tctype: Vec<i64>,
    pub tflg: Vec<i64>,
    pub tdir: Vec<i64>,
    pub tctol: Vec<f64>,
    pub depth: Vec<f64>,
    pub nseed1: Vec<i64>,
    pub nseed2: Vec<i64>,
    pub cx: Vec<f64>,
    pub cy: Vec<f64>,
    pub filename: Vec<String>,
}
impl DEFINE_CURVE_TRIM_NEW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_CURVE_TRIM_NEW").unwrap());
        Self {
            tcid: t.take("TCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tctype: t.take("TCTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            tflg: t.take("TFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            tdir: t.take("TDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            tctol: t.take("TCTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            depth: t.take("DEPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nseed1: t.take("NSEED1").and_then(|c| c.into_int()).unwrap_or_default(),
            nseed2: t.take("NSEED2").and_then(|c| c.into_int()).unwrap_or_default(),
            cx: t.take("CX").and_then(|c| c.into_float()).unwrap_or_default(),
            cy: t.take("CY").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DEATH_TIMES_NODES {
    pub geo: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub x_t: Vec<f64>,
    pub y_t: Vec<f64>,
    pub z_t: Vec<f64>,
    pub x_h: Vec<f64>,
    pub y_h: Vec<f64>,
    pub x_h_2: Vec<f64>,
    pub r: Vec<f64>,
    pub flag: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl DEFINE_DEATH_TIMES_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DEATH_TIMES_NODES").unwrap());
        Self {
            geo: t.take("GEO").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            x_t: t.take("X_T").and_then(|c| c.into_float()).unwrap_or_default(),
            y_t: t.take("Y_T").and_then(|c| c.into_float()).unwrap_or_default(),
            z_t: t.take("Z_T").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h: t.take("X_H").and_then(|c| c.into_float()).unwrap_or_default(),
            y_h: t.take("Y_H").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h_2: t.take("X_H_2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DEATH_TIMES_RIGID {
    pub geo: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub x_t: Vec<f64>,
    pub y_t: Vec<f64>,
    pub z_t: Vec<f64>,
    pub x_h: Vec<f64>,
    pub y_h: Vec<f64>,
    pub x_h_2: Vec<f64>,
    pub r: Vec<f64>,
    pub flag: Vec<i64>,
}
impl DEFINE_DEATH_TIMES_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DEATH_TIMES_RIGID").unwrap());
        Self {
            geo: t.take("GEO").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            x_t: t.take("X_T").and_then(|c| c.into_float()).unwrap_or_default(),
            y_t: t.take("Y_T").and_then(|c| c.into_float()).unwrap_or_default(),
            z_t: t.take("Z_T").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h: t.take("X_H").and_then(|c| c.into_float()).unwrap_or_default(),
            y_h: t.take("Y_H").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h_2: t.take("X_H_2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DEATH_TIMES_SET {
    pub geo: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub x_t: Vec<f64>,
    pub y_t: Vec<f64>,
    pub z_t: Vec<f64>,
    pub x_h: Vec<f64>,
    pub y_h: Vec<f64>,
    pub x_h_2: Vec<f64>,
    pub r: Vec<f64>,
    pub flag: Vec<i64>,
    pub sid1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub sid3: Vec<i64>,
    pub sid4: Vec<i64>,
    pub sid5: Vec<i64>,
    pub sid6: Vec<i64>,
    pub sid7: Vec<i64>,
    pub sid8: Vec<i64>,
}
impl DEFINE_DEATH_TIMES_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DEATH_TIMES_SET").unwrap());
        Self {
            geo: t.take("GEO").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            x_t: t.take("X_T").and_then(|c| c.into_float()).unwrap_or_default(),
            y_t: t.take("Y_T").and_then(|c| c.into_float()).unwrap_or_default(),
            z_t: t.take("Z_T").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h: t.take("X_H").and_then(|c| c.into_float()).unwrap_or_default(),
            y_h: t.take("Y_H").and_then(|c| c.into_float()).unwrap_or_default(),
            x_h_2: t.take("X_H_2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            sid4: t.take("SID4").and_then(|c| c.into_int()).unwrap_or_default(),
            sid5: t.take("SID5").and_then(|c| c.into_int()).unwrap_or_default(),
            sid6: t.take("SID6").and_then(|c| c.into_int()).unwrap_or_default(),
            sid7: t.take("SID7").and_then(|c| c.into_int()).unwrap_or_default(),
            sid8: t.take("SID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_ACTIVE_REGION {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub xm_r: Vec<f64>,
    pub ym: Vec<f64>,
    pub zm: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub nfreq: Vec<i64>,
}
impl DEFINE_DE_ACTIVE_REGION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_ACTIVE_REGION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            xm_r: t.take("XM/R").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            zm: t.take("ZM").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_BOND {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub bdform: Vec<i64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
    pub f: Vec<f64>,
    pub maxgap: Vec<f64>,
}
impl DEFINE_DE_BOND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_BOND").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            bdform: t.take("BDFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_BOND_BY_PART {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl DEFINE_DE_BOND_BY_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_BOND_BY_PART").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_BOND_OVERRIDE {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl DEFINE_DE_BOND_OVERRIDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_BOND_OVERRIDE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_BY_PART {
    pub pid: Vec<i64>,
    pub ndamp: Vec<f64>,
    pub tdamp: Vec<f64>,
    pub fric: Vec<f64>,
    pub fricr: Vec<f64>,
    pub normk: Vec<f64>,
    pub sheark: Vec<f64>,
    pub gamma: Vec<f64>,
    pub vol: Vec<f64>,
    pub ang: Vec<f64>,
    pub lnorm: Vec<i64>,
    pub lshear: Vec<i64>,
    pub f: Vec<i64>,
    pub fricd: Vec<f64>,
    pub dc: Vec<f64>,
}
impl DEFINE_DE_BY_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_BY_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndamp: t.take("NDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tdamp: t.take("TDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            fricr: t.take("FRICR").and_then(|c| c.into_float()).unwrap_or_default(),
            normk: t.take("NORMK").and_then(|c| c.into_float()).unwrap_or_default(),
            sheark: t.take("SHEARK").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            lnorm: t.take("LNORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lshear: t.take("LSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            fricd: t.take("FRICD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_COHESIVE {
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub gamma: Vec<f64>,
    pub vol: Vec<f64>,
    pub ang: Vec<f64>,
    pub gap: Vec<f64>,
}
impl DEFINE_DE_COHESIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_COHESIVE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_FLOW_DRAG {
    pub cd: Vec<f64>,
    pub rho: Vec<f64>,
    pub mu: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub vs: Vec<f64>,
    pub dflag: Vec<i64>,
    pub sfn: Vec<f64>,
    pub sfs: Vec<f64>,
}
impl DEFINE_DE_FLOW_DRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_FLOW_DRAG").unwrap());
        Self {
            cd: t.take("CD").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            vs: t.take("VS").and_then(|c| c.into_float()).unwrap_or_default(),
            dflag: t.take("DFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfn: t.take("SFN").and_then(|c| c.into_float()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_HBOND {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub hbdfm: Vec<i64>,
    pub idim: Vec<i64>,
    pub pbk_sf: Vec<f64>,
    pub pbs_sf: Vec<f64>,
    pub frgk: Vec<f64>,
    pub frgs: Vec<f64>,
    pub bondr: Vec<f64>,
    pub alpha: Vec<f64>,
    pub dmg: Vec<f64>,
    pub frmdl: Vec<i64>,
    pub precrk: Vec<i64>,
    pub cktype: Vec<i64>,
    pub f: Vec<i64>,
    pub itfid: Vec<i64>,
}
impl DEFINE_DE_HBOND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_HBOND").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            hbdfm: t.take("HBDFM").and_then(|c| c.into_int()).unwrap_or_default(),
            idim: t.take("IDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            pbk_sf: t.take("PBK_SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_sf: t.take("PBS_SF").and_then(|c| c.into_float()).unwrap_or_default(),
            frgk: t.take("FRGK").and_then(|c| c.into_float()).unwrap_or_default(),
            frgs: t.take("FRGS").and_then(|c| c.into_float()).unwrap_or_default(),
            bondr: t.take("BONDR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            dmg: t.take("DMG").and_then(|c| c.into_float()).unwrap_or_default(),
            frmdl: t.take("FRMDL").and_then(|c| c.into_int()).unwrap_or_default(),
            precrk: t.take("PRECRK").and_then(|c| c.into_int()).unwrap_or_default(),
            cktype: t.take("CKTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            itfid: t.take("ITFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INJECTION {
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub cid: Vec<i64>,
    pub rmass: Vec<f64>,
    pub rmin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
}
impl DEFINE_DE_INJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INJECTION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INJECTION_ELLIPSE {
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub cid: Vec<i64>,
    pub rmass: Vec<f64>,
    pub rmin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub ifunc: Vec<i64>,
    pub nid: Vec<i64>,
    pub imulti: Vec<i64>,
    pub lcvx: Vec<i64>,
    pub lcvy: Vec<i64>,
    pub lcvz: Vec<i64>,
    pub r1: Vec<f64>,
    pub p1: Vec<f64>,
    pub r2: Vec<f64>,
    pub p2: Vec<f64>,
    pub r3: Vec<f64>,
    pub p3: Vec<f64>,
    pub r4: Vec<f64>,
    pub p4: Vec<f64>,
}
impl DEFINE_DE_INJECTION_ELLIPSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INJECTION_ELLIPSE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            imulti: t.take("IMULTI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvx: t.take("LCVX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvy: t.take("LCVY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvz: t.take("LCVZ").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INJECT_BONDED {
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub cid: Vec<i64>,
    pub rmass: Vec<f64>,
    pub vx: Vec<f64>,
    pub xy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
    pub maxgap: Vec<f64>,
    pub nshape: Vec<i64>,
    pub ishape: Vec<i64>,
    pub ishape_2: Vec<i64>,
    pub ishape_3: Vec<i64>,
    pub ishape_4: Vec<i64>,
    pub ishape_5: Vec<i64>,
    pub ishape_6: Vec<i64>,
    pub ishape_7: Vec<i64>,
    pub ishape_8: Vec<i64>,
}
impl DEFINE_DE_INJECT_BONDED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INJECT_BONDED").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            xy: t.take("XY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            nshape: t.take("NSHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape: t.take("ISHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_2: t.take("ISHAPE_2").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_3: t.take("ISHAPE_3").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_4: t.take("ISHAPE_4").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_5: t.take("ISHAPE_5").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_6: t.take("ISHAPE_6").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_7: t.take("ISHAPE_7").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_8: t.take("ISHAPE_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INJECT_BONDED_ELLIPSE {
    pub pid: Vec<i64>,
    pub sid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub cid: Vec<i64>,
    pub rmass: Vec<f64>,
    pub vx: Vec<f64>,
    pub xy: Vec<f64>,
    pub vz: Vec<f64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub pbn: Vec<f64>,
    pub pbs: Vec<f64>,
    pub pbn_s: Vec<f64>,
    pub pbs_s: Vec<f64>,
    pub sfa: Vec<f64>,
    pub alpha: Vec<f64>,
    pub maxgap: Vec<f64>,
    pub nshape: Vec<i64>,
    pub ishape: Vec<i64>,
    pub ishape_2: Vec<i64>,
    pub ishape_3: Vec<i64>,
    pub ishape_4: Vec<i64>,
    pub ishape_5: Vec<i64>,
    pub ishape_6: Vec<i64>,
    pub ishape_7: Vec<i64>,
    pub ishape_8: Vec<i64>,
}
impl DEFINE_DE_INJECT_BONDED_ELLIPSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INJECT_BONDED_ELLIPSE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            rmass: t.take("RMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            xy: t.take("XY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn: t.take("PBN").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs: t.take("PBS").and_then(|c| c.into_float()).unwrap_or_default(),
            pbn_s: t.take("PBN_S").and_then(|c| c.into_float()).unwrap_or_default(),
            pbs_s: t.take("PBS_S").and_then(|c| c.into_float()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            nshape: t.take("NSHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape: t.take("ISHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_2: t.take("ISHAPE_2").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_3: t.take("ISHAPE_3").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_4: t.take("ISHAPE_4").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_5: t.take("ISHAPE_5").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_6: t.take("ISHAPE_6").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_7: t.take("ISHAPE_7").and_then(|c| c.into_int()).unwrap_or_default(),
            ishape_8: t.take("ISHAPE_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INJECT_SHAPE {
    pub id: Vec<i64>,
    pub nde: Vec<i64>,
    pub iauto: Vec<i64>,
    pub itype: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub r: Vec<f64>,
}
impl DEFINE_DE_INJECT_SHAPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INJECT_SHAPE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nde: t.take("NDE").and_then(|c| c.into_int()).unwrap_or_default(),
            iauto: t.take("IAUTO").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_INTERNAL_SKIP {
    pub pid: Vec<i64>,
    pub type_: Vec<i64>,
}
impl DEFINE_DE_INTERNAL_SKIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_INTERNAL_SKIP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_MASSFLOW_PLANE {
    pub prtclsid: Vec<i64>,
    pub surfsid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub stype: Vec<i64>,
}
impl DEFINE_DE_MASSFLOW_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_MASSFLOW_PLANE").unwrap());
        Self {
            prtclsid: t.take("PRTCLSID").and_then(|c| c.into_int()).unwrap_or_default(),
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_MESH_BEAM {
    pub sid: Vec<i64>,
    pub type_: Vec<i64>,
    pub nquad: Vec<i64>,
    pub despid: Vec<i64>,
    pub desxid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub rsf: Vec<f64>,
    pub iactive: Vec<i64>,
    pub mass: Vec<f64>,
    pub inertia: Vec<f64>,
    pub radius: Vec<f64>,
}
impl DEFINE_DE_MESH_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_MESH_BEAM").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            despid: t.take("DESPID").and_then(|c| c.into_int()).unwrap_or_default(),
            desxid: t.take("DESXID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rsf: t.take("RSF").and_then(|c| c.into_float()).unwrap_or_default(),
            iactive: t.take("IACTIVE").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            inertia: t.take("INERTIA").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_MESH_SURFACE {
    pub sid: Vec<i64>,
    pub type_: Vec<i64>,
    pub nquad: Vec<i64>,
    pub despid: Vec<i64>,
    pub descid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub rsf: Vec<f64>,
    pub iactive: Vec<i64>,
}
impl DEFINE_DE_MESH_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_MESH_SURFACE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nquad: t.take("NQUAD").and_then(|c| c.into_int()).unwrap_or_default(),
            despid: t.take("DESPID").and_then(|c| c.into_int()).unwrap_or_default(),
            descid: t.take("DESCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rsf: t.take("RSF").and_then(|c| c.into_float()).unwrap_or_default(),
            iactive: t.take("IACTIVE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_PATTERN_OUTPUT {
    pub pid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub xo: Vec<f64>,
    pub yo: Vec<f64>,
    pub zo: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub nset: Vec<i64>,
    pub dist1: Vec<f64>,
    pub dist2: Vec<f64>,
    pub dist3: Vec<f64>,
    pub dist4: Vec<f64>,
    pub dist5: Vec<f64>,
    pub dist6: Vec<f64>,
    pub dist7: Vec<f64>,
    pub dist8: Vec<f64>,
}
impl DEFINE_DE_PATTERN_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_PATTERN_OUTPUT").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            yo: t.take("YO").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("ZO").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            nset: t.take("NSET").and_then(|c| c.into_int()).unwrap_or_default(),
            dist1: t.take("DIST1").and_then(|c| c.into_float()).unwrap_or_default(),
            dist2: t.take("DIST2").and_then(|c| c.into_float()).unwrap_or_default(),
            dist3: t.take("DIST3").and_then(|c| c.into_float()).unwrap_or_default(),
            dist4: t.take("DIST4").and_then(|c| c.into_float()).unwrap_or_default(),
            dist5: t.take("DIST5").and_then(|c| c.into_float()).unwrap_or_default(),
            dist6: t.take("DIST6").and_then(|c| c.into_float()).unwrap_or_default(),
            dist7: t.take("DIST7").and_then(|c| c.into_float()).unwrap_or_default(),
            dist8: t.take("DIST8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_TO_BEAM_COUPLING {
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub stype: Vec<i64>,
    pub mtype: Vec<i64>,
    pub frics: Vec<f64>,
    pub fricd: Vec<f64>,
    pub damp: Vec<f64>,
    pub bsort: Vec<i64>,
}
impl DEFINE_DE_TO_BEAM_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_TO_BEAM_COUPLING").unwrap());
        Self {
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            frics: t.take("FricS").and_then(|c| c.into_float()).unwrap_or_default(),
            fricd: t.take("FricD").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            bsort: t.take("BSORT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_TO_SURFACE_COUPLING {
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub stype: Vec<i64>,
    pub mtype: Vec<i64>,
    pub frics: Vec<f64>,
    pub fricd: Vec<f64>,
    pub damp: Vec<f64>,
    pub bsort: Vec<i64>,
    pub lcvx: Vec<i64>,
    pub lcvy: Vec<i64>,
    pub lcvz: Vec<i64>,
    pub wearc: Vec<f64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub sfp: Vec<f64>,
    pub sft: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub cid_rcf: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
}
impl DEFINE_DE_TO_SURFACE_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_TO_SURFACE_COUPLING").unwrap());
        Self {
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            frics: t.take("FricS").and_then(|c| c.into_float()).unwrap_or_default(),
            fricd: t.take("FricD").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            bsort: t.take("BSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvx: t.take("LCVx").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvy: t.take("LCVy").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvz: t.take("LCVz").and_then(|c| c.into_int()).unwrap_or_default(),
            wearc: t.take("WEARC").and_then(|c| c.into_float()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            sfp: t.take("SFP").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cid_rcf: t.take("CID_RCF").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_TO_SURFACE_COUPLING_TRANSDUCER {
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub stype: Vec<i64>,
    pub mtype: Vec<i64>,
    pub frics: Vec<f64>,
    pub fricd: Vec<f64>,
    pub damp: Vec<f64>,
    pub bsort: Vec<i64>,
    pub lcvx: Vec<i64>,
    pub lcvy: Vec<i64>,
    pub lcvz: Vec<i64>,
    pub wearc: Vec<f64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub sfp: Vec<f64>,
    pub sft: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub cid_rcf: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
}
impl DEFINE_DE_TO_SURFACE_COUPLING_TRANSDUCER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_TO_SURFACE_COUPLING_TRANSDUCER").unwrap());
        Self {
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            frics: t.take("FricS").and_then(|c| c.into_float()).unwrap_or_default(),
            fricd: t.take("FricD").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            bsort: t.take("BSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvx: t.take("LCVx").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvy: t.take("LCVy").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvz: t.take("LCVz").and_then(|c| c.into_int()).unwrap_or_default(),
            wearc: t.take("WEARC").and_then(|c| c.into_float()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            sfp: t.take("SFP").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cid_rcf: t.take("CID_RCF").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_DE_TO_SURFACE_TIED {
    pub slave: Vec<i64>,
    pub master: Vec<i64>,
    pub stype: Vec<i64>,
    pub mtype: Vec<i64>,
    pub nflf: Vec<f64>,
    pub sflf: Vec<f64>,
    pub nen: Vec<f64>,
    pub mes: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nsort: Vec<i64>,
    pub maxgap: Vec<f64>,
}
impl DEFINE_DE_TO_SURFACE_TIED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_DE_TO_SURFACE_TIED").unwrap());
        Self {
            slave: t.take("SLAVE").and_then(|c| c.into_int()).unwrap_or_default(),
            master: t.take("MASTER").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nflf: t.take("NFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            sflf: t.take("SFLF").and_then(|c| c.into_float()).unwrap_or_default(),
            nen: t.take("NEN").and_then(|c| c.into_float()).unwrap_or_default(),
            mes: t.take("MES").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsort: t.take("NSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_BEAM {
    pub eid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_BEAM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_BEAM_SET {
    pub sid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_BEAM_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_BEAM_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_SHELL {
    pub eid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_SHELL_SET {
    pub sid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_SHELL_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_SOLID {
    pub eid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_SOLID_SET {
    pub sid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_SOLID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_SOLID_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_THICK_SHELL {
    pub eid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_THICK_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_THICK_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_DEATH_THICK_SHELL_SET {
    pub sid: Vec<i64>,
    pub time: Vec<f64>,
    pub boxid: Vec<i64>,
    pub inout: Vec<i64>,
    pub idgrp: Vec<i64>,
    pub cid: Vec<i64>,
    pub percent: Vec<f64>,
}
impl DEFINE_ELEMENT_DEATH_THICK_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_DEATH_THICK_SHELL_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            idgrp: t.take("IDGRP").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            percent: t.take("PERCENT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_EROSION_IGA {
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub numfip: Vec<f64>,
    pub nifp: Vec<i64>,
}
impl DEFINE_ELEMENT_EROSION_IGA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_EROSION_IGA").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            nifp: t.take("NIFP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_EROSION_SHELL {
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub numfip: Vec<f64>,
    pub nifp: Vec<i64>,
}
impl DEFINE_ELEMENT_EROSION_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_EROSION_SHELL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            nifp: t.take("NIFP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_EROSION_TSHELL {
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub numfip: Vec<f64>,
    pub nifp: Vec<i64>,
}
impl DEFINE_ELEMENT_EROSION_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_EROSION_TSHELL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            nifp: t.take("NIFP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_GENERALIZED_SHELL {
    pub elform: Vec<i64>,
    pub nip: Vec<i64>,
    pub nmnp: Vec<i64>,
    pub imass: Vec<i64>,
    pub form: Vec<i64>,
    pub wi: Vec<f64>,
    pub nki: Vec<f64>,
    pub dnkidr: Vec<f64>,
    pub dnkids: Vec<f64>,
    pub dnkldr: Vec<f64>,
    pub dnklds: Vec<f64>,
    pub d2nkidr2: Vec<f64>,
    pub d2nkidrds: Vec<f64>,
    pub d2nkids2: Vec<f64>,
}
impl DEFINE_ELEMENT_GENERALIZED_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_GENERALIZED_SHELL").unwrap());
        Self {
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nmnp: t.take("NMNP").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            wi: t.take("WI").and_then(|c| c.into_float()).unwrap_or_default(),
            nki: t.take("NKI").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkidr: t.take("DNKIDR").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkids: t.take("DNKIDS").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkldr: t.take("DNKLDR").and_then(|c| c.into_float()).unwrap_or_default(),
            dnklds: t.take("DNKLDs").and_then(|c| c.into_float()).unwrap_or_default(),
            d2nkidr2: t.take("D2NKIDR2").and_then(|c| c.into_float()).unwrap_or_default(),
            d2nkidrds: t.take("D2NKIDRDS").and_then(|c| c.into_float()).unwrap_or_default(),
            d2nkids2: t.take("D2NKIDS2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_ELEMENT_GENERALIZED_SOLID {
    pub elform: Vec<i64>,
    pub nip: Vec<i64>,
    pub nmnp: Vec<i64>,
    pub imass: Vec<i64>,
    pub wi: Vec<f64>,
    pub nki: Vec<f64>,
    pub dnkidr: Vec<f64>,
    pub dnkids: Vec<f64>,
    pub dnkidt: Vec<f64>,
}
impl DEFINE_ELEMENT_GENERALIZED_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_ELEMENT_GENERALIZED_SOLID").unwrap());
        Self {
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nmnp: t.take("NMNP").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            wi: t.take("WI").and_then(|c| c.into_float()).unwrap_or_default(),
            nki: t.take("NKI").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkidr: t.take("DNKIDR").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkids: t.take("DNKIDS").and_then(|c| c.into_float()).unwrap_or_default(),
            dnkidt: t.take("DNKIDT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FABRIC_ASSEMBLIES {
    pub spid1: Vec<i64>,
    pub spid2: Vec<i64>,
    pub spid3: Vec<i64>,
    pub spid4: Vec<i64>,
    pub spid5: Vec<i64>,
    pub spid6: Vec<i64>,
    pub spid7: Vec<i64>,
    pub spid8: Vec<i64>,
}
impl DEFINE_FABRIC_ASSEMBLIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FABRIC_ASSEMBLIES").unwrap());
        Self {
            spid1: t.take("SPID1").and_then(|c| c.into_int()).unwrap_or_default(),
            spid2: t.take("SPID2").and_then(|c| c.into_int()).unwrap_or_default(),
            spid3: t.take("SPID3").and_then(|c| c.into_int()).unwrap_or_default(),
            spid4: t.take("SPID4").and_then(|c| c.into_int()).unwrap_or_default(),
            spid5: t.take("SPID5").and_then(|c| c.into_int()).unwrap_or_default(),
            spid6: t.take("SPID6").and_then(|c| c.into_int()).unwrap_or_default(),
            spid7: t.take("SPID7").and_then(|c| c.into_int()).unwrap_or_default(),
            spid8: t.take("SPID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FIBERS {
    pub idf: Vec<i64>,
    pub idp: Vec<i64>,
    pub numf: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub efb: Vec<f64>,
    pub shr: Vec<f64>,
    pub hrgls: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
}
impl DEFINE_FIBERS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FIBERS").unwrap());
        Self {
            idf: t.take("IDF").and_then(|c| c.into_int()).unwrap_or_default(),
            idp: t.take("IDP").and_then(|c| c.into_int()).unwrap_or_default(),
            numf: t.take("NUMF").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            efb: t.take("EFB").and_then(|c| c.into_float()).unwrap_or_default(),
            shr: t.take("SHR").and_then(|c| c.into_float()).unwrap_or_default(),
            hrgls: t.take("HRGLS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FILTER {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub type_: Vec<String>,
    pub data1: Vec<String>,
    pub data2: Vec<String>,
    pub data3: Vec<String>,
    pub data4: Vec<String>,
    pub data5: Vec<String>,
    pub data6: Vec<String>,
    pub data7: Vec<String>,
}
impl DEFINE_FILTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FILTER").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("Title").and_then(|c| c.into_str()).unwrap_or_default(),
            type_: t.take("Type").and_then(|c| c.into_str()).unwrap_or_default(),
            data1: t.take("Data1").and_then(|c| c.into_str()).unwrap_or_default(),
            data2: t.take("Data2").and_then(|c| c.into_str()).unwrap_or_default(),
            data3: t.take("Data3").and_then(|c| c.into_str()).unwrap_or_default(),
            data4: t.take("Data4").and_then(|c| c.into_str()).unwrap_or_default(),
            data5: t.take("Data5").and_then(|c| c.into_str()).unwrap_or_default(),
            data6: t.take("Data6").and_then(|c| c.into_str()).unwrap_or_default(),
            data7: t.take("Data7").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_BLANKMESH {
    pub idmsh: Vec<i64>,
    pub eleng: Vec<f64>,
    pub xleng: Vec<f64>,
    pub yleng: Vec<f64>,
    pub anglex: Vec<f64>,
    pub nplane: Vec<i64>,
    pub cid: Vec<i64>,
    pub pidbk: Vec<i64>,
    pub nid: Vec<i64>,
    pub eid: Vec<i64>,
    pub xcent: Vec<f64>,
    pub ycent: Vec<f64>,
    pub zcent: Vec<f64>,
    pub xshift: Vec<f64>,
    pub yshift: Vec<f64>,
}
impl DEFINE_FORMING_BLANKMESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_BLANKMESH").unwrap());
        Self {
            idmsh: t.take("IDMSH").and_then(|c| c.into_int()).unwrap_or_default(),
            eleng: t.take("ELENG").and_then(|c| c.into_float()).unwrap_or_default(),
            xleng: t.take("XLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            yleng: t.take("YLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            anglex: t.take("ANGLEX").and_then(|c| c.into_float()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            pidbk: t.take("PIDBK").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            xcent: t.take("XCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycent: t.take("YCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            zcent: t.take("ZCENT").and_then(|c| c.into_float()).unwrap_or_default(),
            xshift: t.take("XSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
            yshift: t.take("YSHIFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_CLAMP {
    pub clp1: Vec<i64>,
    pub clp2: Vec<i64>,
    pub vid: Vec<i64>,
    pub gap: Vec<f64>,
    pub at: Vec<f64>,
    pub dt: Vec<f64>,
}
impl DEFINE_FORMING_CLAMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_CLAMP").unwrap());
        Self {
            clp1: t.take("CLP1").and_then(|c| c.into_int()).unwrap_or_default(),
            clp2: t.take("CLP2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            gap: t.take("GAP").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_CONTACT {
    pub ips: Vec<i64>,
    pub ipm: Vec<i64>,
    pub fs: Vec<f64>,
    pub oneway: Vec<i64>,
}
impl DEFINE_FORMING_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_CONTACT").unwrap());
        Self {
            ips: t.take("IPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ipm: t.take("IPM").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            oneway: t.take("ONEWAY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_ONESTEP_MASTER {
    pub slpid: Vec<i64>,
}
impl DEFINE_FORMING_ONESTEP_MASTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_ONESTEP_MASTER").unwrap());
        Self {
            slpid: t.take("SLPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_ONESTEP_PRIMARY {
    pub slpid: Vec<i64>,
}
impl DEFINE_FORMING_ONESTEP_PRIMARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_ONESTEP_PRIMARY").unwrap());
        Self {
            slpid: t.take("SLPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FORMING_SOLID_SURFACE {
    pub spid: Vec<i64>,
    pub ssetlow: Vec<i64>,
    pub ssetupp: Vec<i64>,
}
impl DEFINE_FORMING_SOLID_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FORMING_SOLID_SURFACE").unwrap());
        Self {
            spid: t.take("SPID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssetlow: t.take("SSETLOW").and_then(|c| c.into_int()).unwrap_or_default(),
            ssetupp: t.take("SSETUPP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FP_TO_SURFACE_COUPLING {
    pub fp: Vec<i64>,
    pub surf: Vec<i64>,
    pub fptype: Vec<i64>,
    pub surftype: Vec<i64>,
    pub sbc: Vec<i64>,
    pub sca: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub sfp: Vec<i64>,
}
impl DEFINE_FP_TO_SURFACE_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FP_TO_SURFACE_COUPLING").unwrap());
        Self {
            fp: t.take("FP").and_then(|c| c.into_int()).unwrap_or_default(),
            surf: t.take("SURF").and_then(|c| c.into_int()).unwrap_or_default(),
            fptype: t.take("FPTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            surftype: t.take("SURFTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_int()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            sfp: t.take("SFP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AIRBAG_SINGLE_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AIRBAG_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AIRBAG_SINGLE_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_GENERAL {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_GENERAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_NODES_TO_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_NODES_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_NODES_TO_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_ONE_WAY_SURFACE_TO_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_SINGLE_SURFACE_MORTAR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_AUTOMATIC_SURFACE_TO_SURFACE_MORTAR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_ERODING_SINGLE_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_ERODING_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_ERODING_SINGLE_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_ORIENTATION {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub lcidp: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl DEFINE_FRICTION_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_ORIENTATION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidp: t.take("LCIDP").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_SCALING {
    pub fsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub psid: Vec<i64>,
    pub scale1: Vec<f64>,
    pub scaleo: Vec<f64>,
}
impl DEFINE_FRICTION_SCALING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_SCALING").unwrap());
        Self {
            fsid: t.take("FSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            scale1: t.take("SCALE1").and_then(|c| c.into_float()).unwrap_or_default(),
            scaleo: t.take("SCALEO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FRICTION_SINGLE_SURFACE {
    pub id: Vec<i64>,
    pub fs_d: Vec<f64>,
    pub fd_d: Vec<f64>,
    pub dc_d: Vec<f64>,
    pub vc_d: Vec<f64>,
    pub icnep: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub fs_ij: Vec<f64>,
    pub fd_ij: Vec<f64>,
    pub dc_ij: Vec<f64>,
    pub vc_ij: Vec<f64>,
    pub ptypei: Vec<String>,
    pub ptypej: Vec<String>,
}
impl DEFINE_FRICTION_SINGLE_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FRICTION_SINGLE_SURFACE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_d: t.take("FS_D").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_d: t.take("FD_D").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_d: t.take("DC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_d: t.take("VC_D").and_then(|c| c.into_float()).unwrap_or_default(),
            icnep: t.take("ICNEP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            fs_ij: t.take("FS_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            fd_ij: t.take("FD_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_ij: t.take("DC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vc_ij: t.take("VC_IJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ptypei: t.take("PTYPEI").and_then(|c| c.into_str()).unwrap_or_default(),
            ptypej: t.take("PTYPEJ").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FUNCTION {
    pub fid: Vec<i64>,
    pub heading: Vec<String>,
    pub function: Vec<String>,
}
impl DEFINE_FUNCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FUNCTION").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            function: t.take("FUNCTION").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_FUNCTION_TABULATED {
    pub fid: Vec<i64>,
    pub heading: Vec<String>,
    pub function: Vec<String>,
    pub a1: Vec<f64>,
    pub o1: Vec<f64>,
}
impl DEFINE_FUNCTION_TABULATED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_FUNCTION_TABULATED").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            function: t.take("FUNCTION").and_then(|c| c.into_str()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_GROUND_MOTION {
    pub gmid: Vec<i64>,
    pub alcid: Vec<i64>,
    pub vlcid: Vec<i64>,
}
impl DEFINE_GROUND_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_GROUND_MOTION").unwrap());
        Self {
            gmid: t.take("GMID").and_then(|c| c.into_int()).unwrap_or_default(),
            alcid: t.take("ALCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HAZ_PROPERTIES {
    pub id_haz: Vec<i64>,
    pub iop: Vec<i64>,
    pub pid: Vec<i64>,
    pub pid_typ: Vec<i64>,
    pub iss: Vec<i64>,
    pub ifs: Vec<i64>,
    pub isb: Vec<i64>,
    pub ifb: Vec<i64>,
    pub isc: Vec<i64>,
    pub ifc: Vec<i64>,
    pub isw: Vec<i64>,
    pub ifw: Vec<i64>,
}
impl DEFINE_HAZ_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HAZ_PROPERTIES").unwrap());
        Self {
            id_haz: t.take("ID_HAZ").and_then(|c| c.into_int()).unwrap_or_default(),
            iop: t.take("IOP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_typ: t.take("PID_TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            iss: t.take("ISS").and_then(|c| c.into_int()).unwrap_or_default(),
            ifs: t.take("IFS").and_then(|c| c.into_int()).unwrap_or_default(),
            isb: t.take("ISB").and_then(|c| c.into_int()).unwrap_or_default(),
            ifb: t.take("IFB").and_then(|c| c.into_int()).unwrap_or_default(),
            isc: t.take("ISC").and_then(|c| c.into_int()).unwrap_or_default(),
            ifc: t.take("IFC").and_then(|c| c.into_int()).unwrap_or_default(),
            isw: t.take("ISW").and_then(|c| c.into_int()).unwrap_or_default(),
            ifw: t.take("IFW").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HAZ_TAILOR_WELDED_BLANK {
    pub idtwb: Vec<i64>,
    pub idns: Vec<i64>,
    pub idp: Vec<i64>,
    pub ipflag: Vec<i64>,
    pub imonflag: Vec<i64>,
}
impl DEFINE_HAZ_TAILOR_WELDED_BLANK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HAZ_TAILOR_WELDED_BLANK").unwrap());
        Self {
            idtwb: t.take("IDTWB").and_then(|c| c.into_int()).unwrap_or_default(),
            idns: t.take("IDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            idp: t.take("IDP").and_then(|c| c.into_int()).unwrap_or_default(),
            ipflag: t.take("IPFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            imonflag: t.take("IMONFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HEX_SPOTWELD_ASSEMBLY {
    pub id_sw: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl DEFINE_HEX_SPOTWELD_ASSEMBLY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HEX_SPOTWELD_ASSEMBLY").unwrap());
        Self {
            id_sw: t.take("ID_SW").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HEX_SPOTWELD_ASSEMBLY_16 {
    pub id_sw: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
    pub eid9: Vec<i64>,
    pub eid10: Vec<i64>,
    pub eid11: Vec<i64>,
    pub eid12: Vec<i64>,
    pub eid13: Vec<i64>,
    pub eid14: Vec<i64>,
    pub eid15: Vec<i64>,
    pub eid16: Vec<i64>,
}
impl DEFINE_HEX_SPOTWELD_ASSEMBLY_16 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HEX_SPOTWELD_ASSEMBLY_16").unwrap());
        Self {
            id_sw: t.take("ID_SW").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
            eid9: t.take("EID9").and_then(|c| c.into_int()).unwrap_or_default(),
            eid10: t.take("EID10").and_then(|c| c.into_int()).unwrap_or_default(),
            eid11: t.take("EID11").and_then(|c| c.into_int()).unwrap_or_default(),
            eid12: t.take("EID12").and_then(|c| c.into_int()).unwrap_or_default(),
            eid13: t.take("EID13").and_then(|c| c.into_int()).unwrap_or_default(),
            eid14: t.take("EID14").and_then(|c| c.into_int()).unwrap_or_default(),
            eid15: t.take("EID15").and_then(|c| c.into_int()).unwrap_or_default(),
            eid16: t.take("EID16").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HEX_SPOTWELD_ASSEMBLY_4 {
    pub id_sw: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl DEFINE_HEX_SPOTWELD_ASSEMBLY_4 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HEX_SPOTWELD_ASSEMBLY_4").unwrap());
        Self {
            id_sw: t.take("ID_SW").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_HEX_SPOTWELD_ASSEMBLY_8 {
    pub id_sw: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl DEFINE_HEX_SPOTWELD_ASSEMBLY_8 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_HEX_SPOTWELD_ASSEMBLY_8").unwrap());
        Self {
            id_sw: t.take("ID_SW").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_LANCE_SEED_POINT_COORDINATES {
    pub nseed: Vec<i64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
}
impl DEFINE_LANCE_SEED_POINT_COORDINATES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_LANCE_SEED_POINT_COORDINATES").unwrap());
        Self {
            nseed: t.take("NSEED").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_MATERIAL_HISTORIES {
    pub label: Vec<String>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl DEFINE_MATERIAL_HISTORIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_MATERIAL_HISTORIES").unwrap());
        Self {
            label: t.take("LABEL").and_then(|c| c.into_str()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_MATERIAL_HISTORIES_NAMES {
    pub label: Vec<String>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub name: Vec<String>,
}
impl DEFINE_MATERIAL_HISTORIES_NAMES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_MATERIAL_HISTORIES_NAMES").unwrap());
        Self {
            label: t.take("LABEL").and_then(|c| c.into_str()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            name: t.take("NAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_MULTISCALE {
    pub id: Vec<i64>,
    pub bset: Vec<i64>,
    pub id_2: Vec<i64>,
    pub bset_2: Vec<i64>,
    pub id_3: Vec<i64>,
    pub bset_3: Vec<i64>,
    pub id_4: Vec<i64>,
    pub bset_4: Vec<i64>,
}
impl DEFINE_MULTISCALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_MULTISCALE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            bset: t.take("BSET").and_then(|c| c.into_int()).unwrap_or_default(),
            id_2: t.take("ID_2").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_2: t.take("BSET_2").and_then(|c| c.into_int()).unwrap_or_default(),
            id_3: t.take("ID_3").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_3: t.take("BSET_3").and_then(|c| c.into_int()).unwrap_or_default(),
            id_4: t.take("ID_4").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_4: t.take("BSET_4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_MULTI_DRAWBEADS_IGES {
    pub filename: Vec<String>,
    pub dbid: Vec<i64>,
    pub vid: Vec<i64>,
    pub pid: Vec<i64>,
    pub blkid: Vec<i64>,
    pub ncur: Vec<i64>,
    pub crvid: Vec<i64>,
    pub bforce: Vec<f64>,
}
impl DEFINE_MULTI_DRAWBEADS_IGES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_MULTI_DRAWBEADS_IGES").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            dbid: t.take("DBID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            blkid: t.take("BLKID").and_then(|c| c.into_int()).unwrap_or_default(),
            ncur: t.take("NCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            crvid: t.take("CRVID").and_then(|c| c.into_int()).unwrap_or_default(),
            bforce: t.take("BFORCE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_NURBS_CURVE {
    pub id: Vec<i64>,
    pub n: Vec<i64>,
    pub p: Vec<i64>,
    pub f30: Vec<i64>,
    pub type_: Vec<i64>,
    pub wfl: Vec<i64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub w: Vec<f64>,
}
impl DEFINE_NURBS_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_NURBS_CURVE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PARTICLE_BLAST {
    pub lagsid: Vec<i64>,
    pub lagstype: Vec<i64>,
    pub nodid: Vec<i64>,
    pub nodtype: Vec<i64>,
    pub hecid: Vec<i64>,
    pub hectype: Vec<i64>,
    pub aircid: Vec<i64>,
    pub nphe: Vec<i64>,
    pub npair: Vec<i64>,
    pub iunit: Vec<i64>,
    pub ihetype: Vec<i64>,
    pub density: Vec<f64>,
    pub energy: Vec<f64>,
    pub gamma: Vec<f64>,
    pub covol: Vec<f64>,
    pub deto_v: Vec<f64>,
    pub detx: Vec<f64>,
    pub dety: Vec<f64>,
    pub detz: Vec<f64>,
    pub tdet: Vec<f64>,
    pub btend: Vec<f64>,
    pub nid: Vec<i64>,
    pub bcx0: Vec<f64>,
    pub bcx1: Vec<f64>,
    pub bcy0: Vec<f64>,
    pub bcy1: Vec<f64>,
    pub bcz0: Vec<f64>,
    pub bcz1: Vec<f64>,
    pub ibcx0: Vec<i64>,
    pub ibcx1: Vec<i64>,
    pub ibcy0: Vec<i64>,
    pub ibcy1: Vec<i64>,
    pub ibcz0: Vec<i64>,
    pub ibcz1: Vec<i64>,
    pub bc_p: Vec<i64>,
}
impl DEFINE_PARTICLE_BLAST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PARTICLE_BLAST").unwrap());
        Self {
            lagsid: t.take("LAGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lagstype: t.take("LAGSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nodid: t.take("NODID").and_then(|c| c.into_int()).unwrap_or_default(),
            nodtype: t.take("NODTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            hecid: t.take("HECID").and_then(|c| c.into_int()).unwrap_or_default(),
            hectype: t.take("HECTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            aircid: t.take("AIRCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nphe: t.take("NPHE").and_then(|c| c.into_int()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            iunit: t.take("IUNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ihetype: t.take("IHETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_float()).unwrap_or_default(),
            energy: t.take("ENERGY").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            covol: t.take("COVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            deto_v: t.take("DETO_V").and_then(|c| c.into_float()).unwrap_or_default(),
            detx: t.take("DETX").and_then(|c| c.into_float()).unwrap_or_default(),
            dety: t.take("DETY").and_then(|c| c.into_float()).unwrap_or_default(),
            detz: t.take("DETZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tdet: t.take("TDET").and_then(|c| c.into_float()).unwrap_or_default(),
            btend: t.take("BTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            bcx0: t.take("BCX0").and_then(|c| c.into_float()).unwrap_or_default(),
            bcx1: t.take("BCX1").and_then(|c| c.into_float()).unwrap_or_default(),
            bcy0: t.take("BCY0").and_then(|c| c.into_float()).unwrap_or_default(),
            bcy1: t.take("BCY1").and_then(|c| c.into_float()).unwrap_or_default(),
            bcz0: t.take("BCZ0").and_then(|c| c.into_float()).unwrap_or_default(),
            bcz1: t.take("BCZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            ibcx0: t.take("IBCX0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcx1: t.take("IBCX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcy0: t.take("IBCY0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcy1: t.take("IBCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcz0: t.take("IBCZ0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcz1: t.take("IBCZ1").and_then(|c| c.into_int()).unwrap_or_default(),
            bc_p: t.take("BC_p").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PART_FROM_LAYER {
    pub pid: Vec<i64>,
    pub layer: Vec<i64>,
    pub pidsrc: Vec<i64>,
    pub layold: Vec<i64>,
    pub mid: Vec<i64>,
    pub thick: Vec<f64>,
}
impl DEFINE_PART_FROM_LAYER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PART_FROM_LAYER").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            layer: t.take("LAYER").and_then(|c| c.into_int()).unwrap_or_default(),
            pidsrc: t.take("PIDSRC").and_then(|c| c.into_int()).unwrap_or_default(),
            layold: t.take("LAYOLD").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PBLAST_AIRGEO {
    pub gid: Vec<i64>,
    pub gtype1: Vec<i64>,
    pub gtype2: Vec<i64>,
    pub xa: Vec<f64>,
    pub ya: Vec<f64>,
    pub za: Vec<f64>,
    pub xb: Vec<f64>,
    pub yb: Vec<f64>,
    pub zb: Vec<f64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub g1: Vec<f64>,
    pub g2: Vec<f64>,
    pub g3: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub g4: Vec<f64>,
    pub g5: Vec<f64>,
    pub g6: Vec<f64>,
}
impl DEFINE_PBLAST_AIRGEO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PBLAST_AIRGEO").unwrap());
        Self {
            gid: t.take("GID").and_then(|c| c.into_int()).unwrap_or_default(),
            gtype1: t.take("GTYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            gtype2: t.take("GTYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            xa: t.take("XA").and_then(|c| c.into_float()).unwrap_or_default(),
            ya: t.take("YA").and_then(|c| c.into_float()).unwrap_or_default(),
            za: t.take("ZA").and_then(|c| c.into_float()).unwrap_or_default(),
            xb: t.take("XB").and_then(|c| c.into_float()).unwrap_or_default(),
            yb: t.take("YB").and_then(|c| c.into_float()).unwrap_or_default(),
            zb: t.take("ZB").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            g5: t.take("G5").and_then(|c| c.into_float()).unwrap_or_default(),
            g6: t.take("G6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PBLAST_GEOMETRY {
    pub gid: Vec<i64>,
    pub gtype1: Vec<i64>,
    pub xa: Vec<f64>,
    pub ya: Vec<f64>,
    pub za: Vec<f64>,
    pub xb: Vec<f64>,
    pub yb: Vec<f64>,
    pub zb: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub g1: Vec<f64>,
    pub g2: Vec<f64>,
    pub g3: Vec<f64>,
}
impl DEFINE_PBLAST_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PBLAST_GEOMETRY").unwrap());
        Self {
            gid: t.take("GID").and_then(|c| c.into_int()).unwrap_or_default(),
            gtype1: t.take("GTYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            xa: t.take("XA").and_then(|c| c.into_float()).unwrap_or_default(),
            ya: t.take("YA").and_then(|c| c.into_float()).unwrap_or_default(),
            za: t.take("ZA").and_then(|c| c.into_float()).unwrap_or_default(),
            xb: t.take("XB").and_then(|c| c.into_float()).unwrap_or_default(),
            yb: t.take("YB").and_then(|c| c.into_float()).unwrap_or_default(),
            zb: t.take("ZB").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PLANE {
    pub pid: Vec<i64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub cid: Vec<i64>,
    pub x3: Vec<f64>,
    pub y3: Vec<f64>,
    pub z3: Vec<f64>,
}
impl DEFINE_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PLANE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            z3: t.take("Z3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_POROUS_ALE {
    pub eidbeg: Vec<i64>,
    pub eidend: Vec<i64>,
    pub local: Vec<i64>,
    pub vecid1: Vec<i64>,
    pub vecid2: Vec<i64>,
    pub userdef: Vec<i64>,
    pub axx: Vec<f64>,
    pub axy: Vec<f64>,
    pub axz: Vec<f64>,
    pub bxx: Vec<f64>,
    pub bxy: Vec<f64>,
    pub bxz: Vec<f64>,
    pub ayx: Vec<f64>,
    pub ayy: Vec<f64>,
    pub ayz: Vec<f64>,
    pub byx: Vec<f64>,
    pub byy: Vec<f64>,
    pub byz: Vec<f64>,
    pub azx: Vec<f64>,
    pub azy: Vec<f64>,
    pub azz: Vec<f64>,
    pub bzx: Vec<f64>,
    pub bzy: Vec<f64>,
    pub bzz: Vec<f64>,
}
impl DEFINE_POROUS_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_POROUS_ALE").unwrap());
        Self {
            eidbeg: t.take("EIDBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            eidend: t.take("EIDEND").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid1: t.take("VECID1").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid2: t.take("VECID2").and_then(|c| c.into_int()).unwrap_or_default(),
            userdef: t.take("USERDEF").and_then(|c| c.into_int()).unwrap_or_default(),
            axx: t.take("Axx").and_then(|c| c.into_float()).unwrap_or_default(),
            axy: t.take("Axy").and_then(|c| c.into_float()).unwrap_or_default(),
            axz: t.take("Axz").and_then(|c| c.into_float()).unwrap_or_default(),
            bxx: t.take("Bxx").and_then(|c| c.into_float()).unwrap_or_default(),
            bxy: t.take("Bxy").and_then(|c| c.into_float()).unwrap_or_default(),
            bxz: t.take("Bxz").and_then(|c| c.into_float()).unwrap_or_default(),
            ayx: t.take("Ayx").and_then(|c| c.into_float()).unwrap_or_default(),
            ayy: t.take("Ayy").and_then(|c| c.into_float()).unwrap_or_default(),
            ayz: t.take("Ayz").and_then(|c| c.into_float()).unwrap_or_default(),
            byx: t.take("Byx").and_then(|c| c.into_float()).unwrap_or_default(),
            byy: t.take("Byy").and_then(|c| c.into_float()).unwrap_or_default(),
            byz: t.take("Byz").and_then(|c| c.into_float()).unwrap_or_default(),
            azx: t.take("Azx").and_then(|c| c.into_float()).unwrap_or_default(),
            azy: t.take("Azy").and_then(|c| c.into_float()).unwrap_or_default(),
            azz: t.take("Azz").and_then(|c| c.into_float()).unwrap_or_default(),
            bzx: t.take("Bzx").and_then(|c| c.into_float()).unwrap_or_default(),
            bzy: t.take("Bzy").and_then(|c| c.into_float()).unwrap_or_default(),
            bzz: t.take("Bzz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_POROUS_LAGRANGIAN {
    pub eidbeg: Vec<i64>,
    pub eidend: Vec<i64>,
    pub local: Vec<i64>,
    pub vecid1: Vec<i64>,
    pub vecid2: Vec<i64>,
    pub userdef: Vec<i64>,
    pub axx: Vec<f64>,
    pub axy: Vec<f64>,
    pub axz: Vec<f64>,
    pub bxx: Vec<f64>,
    pub bxy: Vec<f64>,
    pub bxz: Vec<f64>,
    pub ayx: Vec<f64>,
    pub ayy: Vec<f64>,
    pub ayz: Vec<f64>,
    pub byx: Vec<f64>,
    pub byy: Vec<f64>,
    pub byz: Vec<f64>,
    pub azx: Vec<f64>,
    pub azy: Vec<f64>,
    pub azz: Vec<f64>,
    pub bzx: Vec<f64>,
    pub bzy: Vec<f64>,
    pub bzz: Vec<f64>,
}
impl DEFINE_POROUS_LAGRANGIAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_POROUS_LAGRANGIAN").unwrap());
        Self {
            eidbeg: t.take("EIDBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            eidend: t.take("EIDEND").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid1: t.take("VECID1").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid2: t.take("VECID2").and_then(|c| c.into_int()).unwrap_or_default(),
            userdef: t.take("USERDEF").and_then(|c| c.into_int()).unwrap_or_default(),
            axx: t.take("Axx").and_then(|c| c.into_float()).unwrap_or_default(),
            axy: t.take("Axy").and_then(|c| c.into_float()).unwrap_or_default(),
            axz: t.take("Axz").and_then(|c| c.into_float()).unwrap_or_default(),
            bxx: t.take("Bxx").and_then(|c| c.into_float()).unwrap_or_default(),
            bxy: t.take("Bxy").and_then(|c| c.into_float()).unwrap_or_default(),
            bxz: t.take("Bxz").and_then(|c| c.into_float()).unwrap_or_default(),
            ayx: t.take("Ayx").and_then(|c| c.into_float()).unwrap_or_default(),
            ayy: t.take("Ayy").and_then(|c| c.into_float()).unwrap_or_default(),
            ayz: t.take("Ayz").and_then(|c| c.into_float()).unwrap_or_default(),
            byx: t.take("Byx").and_then(|c| c.into_float()).unwrap_or_default(),
            byy: t.take("Byy").and_then(|c| c.into_float()).unwrap_or_default(),
            byz: t.take("Byz").and_then(|c| c.into_float()).unwrap_or_default(),
            azx: t.take("Azx").and_then(|c| c.into_float()).unwrap_or_default(),
            azy: t.take("Azy").and_then(|c| c.into_float()).unwrap_or_default(),
            azz: t.take("Azz").and_then(|c| c.into_float()).unwrap_or_default(),
            bzx: t.take("Bzx").and_then(|c| c.into_float()).unwrap_or_default(),
            bzy: t.take("Bzy").and_then(|c| c.into_float()).unwrap_or_default(),
            bzz: t.take("Bzz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_PRESSURE_TUBE {
    pub pid: Vec<i64>,
    pub ws: Vec<f64>,
    pub pr: Vec<f64>,
    pub mtd: Vec<i64>,
    pub type_: Vec<i64>,
    pub visc: Vec<f64>,
    pub cfl: Vec<f64>,
    pub damp: Vec<f64>,
    pub bndl: Vec<f64>,
    pub bndr: Vec<f64>,
    pub cavl: Vec<f64>,
    pub cavr: Vec<f64>,
    pub snode: Vec<i64>,
    pub nshl: Vec<i64>,
    pub elform: Vec<i64>,
    pub nip: Vec<i64>,
    pub shrf: Vec<f64>,
    pub bpid: Vec<i64>,
    pub nsld: Vec<i64>,
    pub elform_2: Vec<i64>,
    pub nthk: Vec<i64>,
    pub f: Vec<i64>,
    pub bpid_2: Vec<i64>,
}
impl DEFINE_PRESSURE_TUBE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_PRESSURE_TUBE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ws: t.take("WS").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            mtd: t.take("MTD").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            visc: t.take("VISC").and_then(|c| c.into_float()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            bndl: t.take("BNDL").and_then(|c| c.into_float()).unwrap_or_default(),
            bndr: t.take("BNDR").and_then(|c| c.into_float()).unwrap_or_default(),
            cavl: t.take("CAVL").and_then(|c| c.into_float()).unwrap_or_default(),
            cavr: t.take("CAVR").and_then(|c| c.into_float()).unwrap_or_default(),
            snode: t.take("SNODE").and_then(|c| c.into_int()).unwrap_or_default(),
            nshl: t.take("NSHL").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            bpid: t.take("BPID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsld: t.take("NSLD").and_then(|c| c.into_int()).unwrap_or_default(),
            elform_2: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            nthk: t.take("NTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            bpid_2: t.take("BPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_QUASAR_COUPLING {
    pub node: Vec<i64>,
    pub type_: Vec<i64>,
    pub romid: Vec<i64>,
    pub pid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub iopt: Vec<i64>,
    pub cid: Vec<i64>,
    pub ex_id: Vec<i64>,
    pub frcfrq: Vec<i64>,
    pub encname1: Vec<String>,
    pub encname2: Vec<String>,
    pub var1: Vec<f64>,
    pub var2: Vec<f64>,
    pub var3: Vec<f64>,
    pub var4: Vec<f64>,
    pub var5: Vec<f64>,
    pub var6: Vec<f64>,
    pub var7: Vec<f64>,
    pub var8: Vec<f64>,
}
impl DEFINE_QUASAR_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_QUASAR_COUPLING").unwrap());
        Self {
            node: t.take("NODE").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            romid: t.take("ROMID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ex_id: t.take("EX_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            frcfrq: t.take("FRCFRQ").and_then(|c| c.into_int()).unwrap_or_default(),
            encname1: t.take("ENCNAME1").and_then(|c| c.into_str()).unwrap_or_default(),
            encname2: t.take("ENCNAME2").and_then(|c| c.into_str()).unwrap_or_default(),
            var1: t.take("VAR1").and_then(|c| c.into_float()).unwrap_or_default(),
            var2: t.take("VAR2").and_then(|c| c.into_float()).unwrap_or_default(),
            var3: t.take("VAR3").and_then(|c| c.into_float()).unwrap_or_default(),
            var4: t.take("VAR4").and_then(|c| c.into_float()).unwrap_or_default(),
            var5: t.take("VAR5").and_then(|c| c.into_float()).unwrap_or_default(),
            var6: t.take("VAR6").and_then(|c| c.into_float()).unwrap_or_default(),
            var7: t.take("VAR7").and_then(|c| c.into_float()).unwrap_or_default(),
            var8: t.take("VAR8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_REGION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub type_: Vec<i64>,
    pub cid: Vec<i64>,
    pub move_: Vec<i64>,
    pub xmn: Vec<f64>,
    pub xmx: Vec<f64>,
    pub ymn: Vec<f64>,
    pub ymx: Vec<f64>,
    pub zmn: Vec<f64>,
    pub zmx: Vec<f64>,
}
impl DEFINE_REGION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_REGION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            move_: t.take("MOVE").and_then(|c| c.into_int()).unwrap_or_default(),
            xmn: t.take("XMN").and_then(|c| c.into_float()).unwrap_or_default(),
            xmx: t.take("XMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ymn: t.take("YMN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymx: t.take("YMX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmn: t.take("ZMN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmx: t.take("ZMX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SD_ORIENTATION {
    pub vid: Vec<i64>,
    pub iop: Vec<i64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
}
impl DEFINE_SD_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SD_ORIENTATION").unwrap());
        Self {
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            iop: t.take("IOP").and_then(|c| c.into_int()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SET_ADAPTIVE {
    pub setid: Vec<i64>,
    pub stype: Vec<i64>,
    pub adplvl: Vec<i64>,
    pub adpsize: Vec<f64>,
}
impl DEFINE_SET_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SET_ADAPTIVE").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            adplvl: t.take("ADPLVL").and_then(|c| c.into_int()).unwrap_or_default(),
            adpsize: t.take("ADPSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_ACTIVE_REGION {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub stype: Vec<i64>,
    pub cycle: Vec<i64>,
    pub nid: Vec<i64>,
    pub icid: Vec<i64>,
    pub ibuff: Vec<i64>,
    pub ximin: Vec<f64>,
    pub yimin: Vec<f64>,
    pub zimin: Vec<f64>,
    pub ximax: Vec<f64>,
    pub yimax: Vec<f64>,
    pub zimax: Vec<f64>,
    pub xomin: Vec<f64>,
    pub yomin: Vec<f64>,
    pub zomin: Vec<f64>,
    pub xomax: Vec<f64>,
    pub yomax: Vec<f64>,
    pub zomax: Vec<f64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub rmin: Vec<f64>,
    pub zmin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub zmax: Vec<f64>,
    pub x0_2: Vec<f64>,
    pub y0_2: Vec<f64>,
    pub z0_2: Vec<f64>,
    pub rmin_2: Vec<f64>,
    pub rmax_2: Vec<f64>,
}
impl DEFINE_SPH_ACTIVE_REGION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_ACTIVE_REGION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cycle: t.take("CYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibuff: t.take("IBUFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ximin: t.take("XIMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            yimin: t.take("YIMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            zimin: t.take("ZIMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ximax: t.take("XIMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            yimax: t.take("YIMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            zimax: t.take("ZIMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            xomin: t.take("XOMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            yomin: t.take("YOMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            zomin: t.take("ZOMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            xomax: t.take("XOMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            yomax: t.take("YOMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            zomax: t.take("ZOMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmin: t.take("ZMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            zmax: t.take("ZMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            x0_2: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0_2: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0_2: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            rmin_2: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax_2: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_AMBIENT_DRAG {
    pub icid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub rhoa: Vec<f64>,
    pub mua: Vec<f64>,
    pub sftens: Vec<f64>,
}
impl DEFINE_SPH_AMBIENT_DRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_AMBIENT_DRAG").unwrap());
        Self {
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoa: t.take("RHOA").and_then(|c| c.into_float()).unwrap_or_default(),
            mua: t.take("MUA").and_then(|c| c.into_float()).unwrap_or_default(),
            sftens: t.take("SFTENS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_DE_COUPLING {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub sphid: Vec<i64>,
    pub desid: Vec<i64>,
    pub sphtyp: Vec<i64>,
    pub destyp: Vec<i64>,
    pub pfact: Vec<f64>,
    pub dfact: Vec<f64>,
    pub sphbox: Vec<i64>,
}
impl DEFINE_SPH_DE_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_DE_COUPLING").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            sphid: t.take("SPHID").and_then(|c| c.into_int()).unwrap_or_default(),
            desid: t.take("DESID").and_then(|c| c.into_int()).unwrap_or_default(),
            sphtyp: t.take("SPHTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            destyp: t.take("DESTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfact: t.take("PFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfact: t.take("DFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            sphbox: t.take("SPHBOX").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_DE_COUPLING_ID {
    pub did: Vec<i64>,
    pub heading: Vec<String>,
    pub sphid: Vec<i64>,
    pub desid: Vec<i64>,
    pub sphtyp: Vec<i64>,
    pub destyp: Vec<i64>,
    pub pfact: Vec<f64>,
    pub dfact: Vec<f64>,
    pub sphbox: Vec<i64>,
}
impl DEFINE_SPH_DE_COUPLING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_DE_COUPLING_ID").unwrap());
        Self {
            did: t.take("DID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            sphid: t.take("SPHID").and_then(|c| c.into_int()).unwrap_or_default(),
            desid: t.take("DESID").and_then(|c| c.into_int()).unwrap_or_default(),
            sphtyp: t.take("SPHTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            destyp: t.take("DESTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            pfact: t.take("PFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfact: t.take("DFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            sphbox: t.take("SPHBOX").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_INJECTION {
    pub pid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub area: Vec<f64>,
    pub vmag: Vec<i64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub nid: Vec<i64>,
}
impl DEFINE_SPH_INJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_INJECTION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            vmag: t.take("VMAG").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_MASSFLOW_PLANE {
    pub prtclsid: Vec<i64>,
    pub surfsid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub stype: Vec<i64>,
}
impl DEFINE_SPH_MASSFLOW_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_MASSFLOW_PLANE").unwrap());
        Self {
            prtclsid: t.take("PRTCLSID").and_then(|c| c.into_int()).unwrap_or_default(),
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_MESH_BOX {
    pub xmin: Vec<f64>,
    pub ymin: Vec<f64>,
    pub zmin: Vec<f64>,
    pub xlen: Vec<f64>,
    pub ylen: Vec<f64>,
    pub zlen: Vec<f64>,
    pub ipid: Vec<i64>,
    pub nx: Vec<i64>,
    pub ny: Vec<i64>,
    pub nz: Vec<i64>,
    pub f40: Vec<i64>,
    pub idseg: Vec<i64>,
    pub sfsp: Vec<f64>,
}
impl DEFINE_SPH_MESH_BOX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_MESH_BOX").unwrap());
        Self {
            xmin: t.take("XMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ymin: t.take("YMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            zmin: t.take("ZMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            xlen: t.take("XLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            ylen: t.take("YLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            zlen: t.take("ZLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            ipid: t.take("IPID").and_then(|c| c.into_int()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_int()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_int()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            idseg: t.take("IDSEG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfsp: t.take("SFSP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_MESH_SURFACE {
    pub sid: Vec<i64>,
    pub type_: Vec<i64>,
    pub sphpid: Vec<i64>,
    pub sphxid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub space: Vec<f64>,
    pub iout: Vec<i64>,
}
impl DEFINE_SPH_MESH_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_MESH_SURFACE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sphpid: t.take("SPHPID").and_then(|c| c.into_int()).unwrap_or_default(),
            sphxid: t.take("SPHXID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            space: t.take("SPACE").and_then(|c| c.into_float()).unwrap_or_default(),
            iout: t.take("IOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_TO_SPH_COUPLING {
    pub ssid: Vec<i64>,
    pub msid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub ibox1: Vec<i64>,
    pub ibox2: Vec<i64>,
    pub pfact: Vec<f64>,
    pub srad: Vec<f64>,
    pub dfact: Vec<f64>,
    pub isoft: Vec<i64>,
}
impl DEFINE_SPH_TO_SPH_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_TO_SPH_COUPLING").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox1: t.take("IBOX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox2: t.take("IBOX2").and_then(|c| c.into_int()).unwrap_or_default(),
            pfact: t.take("PFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            dfact: t.take("DFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            isoft: t.take("ISOFT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_TO_SPH_COUPLING_ID {
    pub ssid: Vec<i64>,
    pub msid: Vec<i64>,
    pub sstyp: Vec<i64>,
    pub mstyp: Vec<i64>,
    pub ibox1: Vec<i64>,
    pub ibox2: Vec<i64>,
    pub pfact: Vec<f64>,
    pub srad: Vec<f64>,
    pub dfact: Vec<f64>,
    pub isoft: Vec<i64>,
}
impl DEFINE_SPH_TO_SPH_COUPLING_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_TO_SPH_COUPLING_ID").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            msid: t.take("MSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstyp: t.take("SSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mstyp: t.take("MSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox1: t.take("IBOX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibox2: t.take("IBOX2").and_then(|c| c.into_int()).unwrap_or_default(),
            pfact: t.take("PFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            srad: t.take("SRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            dfact: t.take("DFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            isoft: t.take("ISOFT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPH_VICINITY_SENSOR {
    pub prtclsid: Vec<i64>,
    pub surfsid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub stype: Vec<i64>,
    pub dist: Vec<i64>,
}
impl DEFINE_SPH_VICINITY_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPH_VICINITY_SENSOR").unwrap());
        Self {
            prtclsid: t.take("PRTCLSID").and_then(|c| c.into_int()).unwrap_or_default(),
            surfsid: t.take("SURFSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_FAILURE {
    pub id: Vec<i64>,
    pub tflag: Vec<i64>,
    pub dc1: Vec<f64>,
    pub dc2: Vec<f64>,
    pub dc3: Vec<f64>,
    pub dc4: Vec<f64>,
    pub exn: Vec<f64>,
    pub exs: Vec<f64>,
    pub navg: Vec<i64>,
    pub d_sn: Vec<f64>,
    pub d_ss: Vec<f64>,
    pub r_sult: Vec<f64>,
    pub tscale: Vec<f64>,
    pub mid: Vec<i64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
}
impl DEFINE_SPOTWELD_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_FAILURE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dc1: t.take("DC1").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2: t.take("DC2").and_then(|c| c.into_float()).unwrap_or_default(),
            dc3: t.take("DC3").and_then(|c| c.into_float()).unwrap_or_default(),
            dc4: t.take("DC4").and_then(|c| c.into_float()).unwrap_or_default(),
            exn: t.take("EXN").and_then(|c| c.into_float()).unwrap_or_default(),
            exs: t.take("EXS").and_then(|c| c.into_float()).unwrap_or_default(),
            navg: t.take("NAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            d_sn: t.take("D_SN").and_then(|c| c.into_float()).unwrap_or_default(),
            d_ss: t.take("D_SS").and_then(|c| c.into_float()).unwrap_or_default(),
            r_sult: t.take("R_SULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_FAILURE_ADD {
    pub id: Vec<i64>,
    pub tflag: Vec<i64>,
    pub dc1: Vec<f64>,
    pub dc2: Vec<f64>,
    pub dc3: Vec<f64>,
    pub dc4: Vec<f64>,
    pub exn: Vec<f64>,
    pub exs: Vec<f64>,
    pub mid: Vec<i64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
}
impl DEFINE_SPOTWELD_FAILURE_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_FAILURE_ADD").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dc1: t.take("DC1").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2: t.take("DC2").and_then(|c| c.into_float()).unwrap_or_default(),
            dc3: t.take("DC3").and_then(|c| c.into_float()).unwrap_or_default(),
            dc4: t.take("DC4").and_then(|c| c.into_float()).unwrap_or_default(),
            exn: t.take("EXN").and_then(|c| c.into_float()).unwrap_or_default(),
            exs: t.take("EXS").and_then(|c| c.into_float()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_FAILURE_PID {
    pub id: Vec<i64>,
    pub tflag: Vec<i64>,
    pub dc1: Vec<f64>,
    pub dc2: Vec<f64>,
    pub dc3: Vec<f64>,
    pub dc4: Vec<f64>,
    pub exn: Vec<f64>,
    pub exs: Vec<f64>,
    pub navg: Vec<i64>,
    pub d_sn: Vec<f64>,
    pub d_ss: Vec<f64>,
    pub r_sult: Vec<f64>,
    pub tscale: Vec<f64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub sn: Vec<f64>,
    pub ss: Vec<f64>,
}
impl DEFINE_SPOTWELD_FAILURE_PID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_FAILURE_PID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dc1: t.take("DC1").and_then(|c| c.into_float()).unwrap_or_default(),
            dc2: t.take("DC2").and_then(|c| c.into_float()).unwrap_or_default(),
            dc3: t.take("DC3").and_then(|c| c.into_float()).unwrap_or_default(),
            dc4: t.take("DC4").and_then(|c| c.into_float()).unwrap_or_default(),
            exn: t.take("EXN").and_then(|c| c.into_float()).unwrap_or_default(),
            exs: t.take("EXS").and_then(|c| c.into_float()).unwrap_or_default(),
            navg: t.take("NAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            d_sn: t.take("D_SN").and_then(|c| c.into_float()).unwrap_or_default(),
            d_ss: t.take("D_SS").and_then(|c| c.into_float()).unwrap_or_default(),
            r_sult: t.take("R_SULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_FAILURE_RESULTANTS {
    pub id: Vec<i64>,
    pub dsn: Vec<f64>,
    pub dss: Vec<f64>,
    pub dlcidsn: Vec<i64>,
    pub dlcidss: Vec<i64>,
    pub pid_i: Vec<i64>,
    pub pid_j: Vec<i64>,
    pub snij: Vec<f64>,
    pub ssij: Vec<f64>,
    pub lcidsnij: Vec<i64>,
    pub lcidssij: Vec<i64>,
}
impl DEFINE_SPOTWELD_FAILURE_RESULTANTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_FAILURE_RESULTANTS").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            dsn: t.take("DSN").and_then(|c| c.into_float()).unwrap_or_default(),
            dss: t.take("DSS").and_then(|c| c.into_float()).unwrap_or_default(),
            dlcidsn: t.take("DLCIDSN").and_then(|c| c.into_int()).unwrap_or_default(),
            dlcidss: t.take("DLCIDSS").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_i: t.take("PID_I").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_j: t.take("PID_J").and_then(|c| c.into_int()).unwrap_or_default(),
            snij: t.take("SNIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            ssij: t.take("SSIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidsnij: t.take("LCIDSNIJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidssij: t.take("LCIDSSIJ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_MULTISCALE {
    pub type_: Vec<i64>,
    pub bset: Vec<i64>,
    pub type_2: Vec<i64>,
    pub bset_2: Vec<i64>,
    pub type_3: Vec<i64>,
    pub bset_3: Vec<i64>,
    pub type_4: Vec<i64>,
    pub bset_4: Vec<i64>,
}
impl DEFINE_SPOTWELD_MULTISCALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_MULTISCALE").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            bset: t.take("BSET").and_then(|c| c.into_int()).unwrap_or_default(),
            type_2: t.take("TYPE_2").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_2: t.take("BSET_2").and_then(|c| c.into_int()).unwrap_or_default(),
            type_3: t.take("TYPE_3").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_3: t.take("BSET_3").and_then(|c| c.into_int()).unwrap_or_default(),
            type_4: t.take("TYPE_4").and_then(|c| c.into_int()).unwrap_or_default(),
            bset_4: t.take("BSET_4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_RUPTURE_PARAMETER {
    pub pid: Vec<i64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub n11: Vec<f64>,
    pub n12: Vec<f64>,
    pub n13: Vec<f64>,
    pub f: Vec<f64>,
    pub sig_pf: Vec<f64>,
    pub c21: Vec<f64>,
    pub c22: Vec<f64>,
    pub c23: Vec<f64>,
    pub n2: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub sig_nf: Vec<f64>,
    pub lcdpa: Vec<i64>,
    pub lcdpm: Vec<i64>,
    pub lcdps: Vec<i64>,
    pub lcdna: Vec<i64>,
    pub lcdnm: Vec<i64>,
    pub lcdns: Vec<i64>,
    pub f_4: Vec<i64>,
    pub nsmt: Vec<i64>,
}
impl DEFINE_SPOTWELD_RUPTURE_PARAMETER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_RUPTURE_PARAMETER").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_float()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_float()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_pf: t.take("SIG_PF").and_then(|c| c.into_float()).unwrap_or_default(),
            c21: t.take("C21").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_nf: t.take("SIG_NF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdpa: t.take("LCDPA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdpm: t.take("LCDPM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdps: t.take("LCDPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdna: t.take("LCDNA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdnm: t.take("LCDNM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdns: t.take("LCDNS").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            nsmt: t.take("NSMT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_SPOTWELD_RUPTURE_STRESS {
    pub pid: Vec<i64>,
    pub srsig: Vec<f64>,
    pub sigtau: Vec<f64>,
}
impl DEFINE_SPOTWELD_RUPTURE_STRESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_SPOTWELD_RUPTURE_STRESS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            srsig: t.take("SRSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            sigtau: t.take("SIGTAU").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STAGED_CONSTRUCTION_PART {
    pub pid: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl DEFINE_STAGED_CONSTRUCTION_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STAGED_CONSTRUCTION_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STAGED_CONSTRUCTION_PART_SET {
    pub psid: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl DEFINE_STAGED_CONSTRUCTION_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STAGED_CONSTRUCTION_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STOCHASTIC_ELEMENT_SHELL_VARIATON {
    pub ide: Vec<i64>,
    pub varsy: Vec<f64>,
    pub varf: Vec<f64>,
    pub varro: Vec<f64>,
    pub vare: Vec<f64>,
}
impl DEFINE_STOCHASTIC_ELEMENT_SHELL_VARIATON {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STOCHASTIC_ELEMENT_SHELL_VARIATON").unwrap());
        Self {
            ide: t.take("IDE").and_then(|c| c.into_int()).unwrap_or_default(),
            varsy: t.take("VARSY").and_then(|c| c.into_float()).unwrap_or_default(),
            varf: t.take("VARF").and_then(|c| c.into_float()).unwrap_or_default(),
            varro: t.take("VARRO").and_then(|c| c.into_float()).unwrap_or_default(),
            vare: t.take("VARE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STOCHASTIC_ELEMENT_SOLID_VARIATON {
    pub ide: Vec<i64>,
    pub varsy: Vec<f64>,
    pub varf: Vec<f64>,
    pub varro: Vec<f64>,
    pub vare: Vec<f64>,
}
impl DEFINE_STOCHASTIC_ELEMENT_SOLID_VARIATON {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STOCHASTIC_ELEMENT_SOLID_VARIATON").unwrap());
        Self {
            ide: t.take("IDE").and_then(|c| c.into_int()).unwrap_or_default(),
            varsy: t.take("VARSY").and_then(|c| c.into_float()).unwrap_or_default(),
            varf: t.take("VARF").and_then(|c| c.into_float()).unwrap_or_default(),
            varro: t.take("VARRO").and_then(|c| c.into_float()).unwrap_or_default(),
            vare: t.take("VARE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STOCHASTIC_VARIATION {
    pub id_sv: Vec<i64>,
    pub pid: Vec<i64>,
    pub pid_typ: Vec<i64>,
    pub icor: Vec<i64>,
    pub var_s: Vec<i64>,
    pub var_f: Vec<i64>,
    pub irng: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub lcid: Vec<i64>,
    pub r1_2: Vec<f64>,
    pub r2_2: Vec<f64>,
    pub r3_2: Vec<f64>,
    pub lcid_2: Vec<i64>,
}
impl DEFINE_STOCHASTIC_VARIATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STOCHASTIC_VARIATION").unwrap());
        Self {
            id_sv: t.take("ID_SV").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_typ: t.take("PID_TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            icor: t.take("ICOR").and_then(|c| c.into_int()).unwrap_or_default(),
            var_s: t.take("VAR_S").and_then(|c| c.into_int()).unwrap_or_default(),
            var_f: t.take("VAR_F").and_then(|c| c.into_int()).unwrap_or_default(),
            irng: t.take("IRNG").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            r1_2: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2_2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3_2: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_2: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_STOCHASTIC_VARIATION_PROPERTIES {
    pub id_sv: Vec<i64>,
    pub mtype: Vec<i64>,
    pub pid: Vec<i64>,
    pub pid_typ: Vec<i64>,
    pub irng: Vec<i64>,
    pub numv: Vec<i64>,
    pub num_beg: Vec<i64>,
    pub f: Vec<i64>,
    pub vartyp: Vec<i64>,
    pub corlgr: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
}
impl DEFINE_STOCHASTIC_VARIATION_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_STOCHASTIC_VARIATION_PROPERTIES").unwrap());
        Self {
            id_sv: t.take("ID_SV").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid_typ: t.take("PID_TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            irng: t.take("IRNG").and_then(|c| c.into_int()).unwrap_or_default(),
            numv: t.take("NUMV").and_then(|c| c.into_int()).unwrap_or_default(),
            num_beg: t.take("NUM_BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            vartyp: t.take("VARTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            corlgr: t.take("CORLGR").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TABLE {
    pub tbid: Vec<i64>,
    pub sfa: Vec<f64>,
    pub offa: Vec<f64>,
    pub value: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl DEFINE_TABLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TABLE").unwrap());
        Self {
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            value: t.take("VALUE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TABLE_2D {
    pub tbid: Vec<i64>,
    pub sfa: Vec<f64>,
    pub offa: Vec<f64>,
    pub value: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl DEFINE_TABLE_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TABLE_2D").unwrap());
        Self {
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            value: t.take("VALUE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TABLE_3D {
    pub tbid: Vec<i64>,
    pub sfa: Vec<f64>,
    pub offa: Vec<f64>,
    pub value: Vec<f64>,
    pub tbid_2: Vec<i64>,
}
impl DEFINE_TABLE_3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TABLE_3D").unwrap());
        Self {
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfa: t.take("SFA").and_then(|c| c.into_float()).unwrap_or_default(),
            offa: t.take("OFFA").and_then(|c| c.into_float()).unwrap_or_default(),
            value: t.take("VALUE").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid_2: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TABLE_COMPACT {
    pub tbid: Vec<i64>,
    pub nvar: Vec<i64>,
    pub lcint: Vec<i64>,
    pub mathis: Vec<i64>,
    pub inexect: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub his1: Vec<i64>,
    pub his2: Vec<i64>,
    pub his3: Vec<i64>,
    pub his4: Vec<i64>,
    pub his5: Vec<i64>,
    pub his6: Vec<i64>,
    pub his7: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub his8: Vec<i64>,
    pub his9: Vec<i64>,
    pub unused_6: Vec<i64>,
    pub ixe1: Vec<i64>,
    pub ixe2: Vec<i64>,
    pub ixe3: Vec<i64>,
    pub ixe4: Vec<i64>,
    pub ixe5: Vec<i64>,
    pub ixe6: Vec<i64>,
    pub ixe7: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub ixe8: Vec<i64>,
    pub ixe9: Vec<i64>,
    pub o1: Vec<f64>,
    pub a1_1: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub a1_3: Vec<f64>,
    pub a1_4: Vec<f64>,
    pub a1_5: Vec<f64>,
    pub a1_6: Vec<f64>,
    pub a1_7: Vec<f64>,
    pub unused_8: Vec<i64>,
    pub a1_8: Vec<f64>,
    pub a1_9: Vec<f64>,
}
impl DEFINE_TABLE_COMPACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TABLE_COMPACT").unwrap());
        Self {
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            nvar: t.take("NVAR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcint: t.take("LCINT").and_then(|c| c.into_int()).unwrap_or_default(),
            mathis: t.take("MATHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            inexect: t.take("INEXECT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            his1: t.take("HIS1").and_then(|c| c.into_int()).unwrap_or_default(),
            his2: t.take("HIS2").and_then(|c| c.into_int()).unwrap_or_default(),
            his3: t.take("HIS3").and_then(|c| c.into_int()).unwrap_or_default(),
            his4: t.take("HIS4").and_then(|c| c.into_int()).unwrap_or_default(),
            his5: t.take("HIS5").and_then(|c| c.into_int()).unwrap_or_default(),
            his6: t.take("HIS6").and_then(|c| c.into_int()).unwrap_or_default(),
            his7: t.take("HIS7").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            his8: t.take("HIS8").and_then(|c| c.into_int()).unwrap_or_default(),
            his9: t.take("HIS9").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe1: t.take("IXE1").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe2: t.take("IXE2").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe3: t.take("IXE3").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe4: t.take("IXE4").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe5: t.take("IXE5").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe6: t.take("IXE6").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe7: t.take("IXE7").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe8: t.take("IXE8").and_then(|c| c.into_int()).unwrap_or_default(),
            ixe9: t.take("IXE9").and_then(|c| c.into_int()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_1: t.take("A1.1").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1.2").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_3: t.take("A1.3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_4: t.take("A1.4").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_5: t.take("A1.5").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_6: t.take("A1.6").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_7: t.take("A1.7").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_8: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            a1_8: t.take("A1.8").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_9: t.take("A1.9").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TABLE_MATRIX {
    pub tbid: Vec<i64>,
    pub filename: Vec<String>,
    pub nrow: Vec<i64>,
    pub ncol: Vec<i64>,
    pub srow: Vec<f64>,
    pub scol: Vec<f64>,
    pub sval: Vec<f64>,
    pub orow: Vec<f64>,
    pub ocol: Vec<f64>,
    pub oval: Vec<f64>,
}
impl DEFINE_TABLE_MATRIX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TABLE_MATRIX").unwrap());
        Self {
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            nrow: t.take("NROW").and_then(|c| c.into_int()).unwrap_or_default(),
            ncol: t.take("NCOL").and_then(|c| c.into_int()).unwrap_or_default(),
            srow: t.take("SROW").and_then(|c| c.into_float()).unwrap_or_default(),
            scol: t.take("SCOL").and_then(|c| c.into_float()).unwrap_or_default(),
            sval: t.take("SVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            orow: t.take("OROW").and_then(|c| c.into_float()).unwrap_or_default(),
            ocol: t.take("OCOL").and_then(|c| c.into_float()).unwrap_or_default(),
            oval: t.take("OVAL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TARGET_BOUNDARY {
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl DEFINE_TARGET_BOUNDARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TARGET_BOUNDARY").unwrap());
        Self {
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TRACER_PARTICLES_2D {
    pub nset: Vec<i64>,
    pub pset: Vec<i64>,
}
impl DEFINE_TRACER_PARTICLES_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TRACER_PARTICLES_2D").unwrap());
        Self {
            nset: t.take("NSET").and_then(|c| c.into_int()).unwrap_or_default(),
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TRANSFORM {
    pub tranid: Vec<i64>,
    pub option: Vec<String>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub a7: Vec<f64>,
}
impl DEFINE_TRANSFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TRANSFORM").unwrap());
        Self {
            tranid: t.take("TRANID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            a7: t.take("A7").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TRANSFORMATION {
    pub tranid: Vec<i64>,
    pub option: Vec<String>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub a7: Vec<f64>,
}
impl DEFINE_TRANSFORMATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TRANSFORMATION").unwrap());
        Self {
            tranid: t.take("TRANID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            a7: t.take("A7").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_TRIM_SEED_POINT_COORDINATES {
    pub nseed: Vec<i64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
}
impl DEFINE_TRIM_SEED_POINT_COORDINATES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_TRIM_SEED_POINT_COORDINATES").unwrap());
        Self {
            nseed: t.take("NSEED").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_VECTOR {
    pub vid: Vec<i64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub cid: Vec<i64>,
}
impl DEFINE_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_VECTOR").unwrap());
        Self {
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFINE_VECTOR_NODES {
    pub vid: Vec<i64>,
    pub nodet: Vec<i64>,
    pub nodeh: Vec<i64>,
}
impl DEFINE_VECTOR_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFINE_VECTOR_NODES").unwrap());
        Self {
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            nodet: t.take("NODET").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeh: t.take("NODEH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DEFORMABLE_TO_RIGID {
    pub pid: Vec<i64>,
    pub lrb: Vec<i64>,
    pub ptype: Vec<String>,
}
impl DEFORMABLE_TO_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFORMABLE_TO_RIGID").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFORMABLE_TO_RIGID_AUTOMATIC {
    pub swset: Vec<i64>,
    pub code: Vec<i64>,
    pub time1: Vec<f64>,
    pub time2: Vec<f64>,
    pub time3: Vec<f64>,
    pub entno: Vec<i64>,
    pub relsw: Vec<i64>,
    pub paired: Vec<i64>,
    pub nrbf: Vec<i64>,
    pub ncsf: Vec<i64>,
    pub rwf: Vec<i64>,
    pub dtmax: Vec<f64>,
    pub d2r: Vec<i64>,
    pub r2d: Vec<i64>,
    pub offset: Vec<i64>,
    pub pid: Vec<i64>,
    pub lrb: Vec<i64>,
    pub ptype: Vec<String>,
    pub pid_2: Vec<i64>,
    pub ptype_2: Vec<String>,
}
impl DEFORMABLE_TO_RIGID_AUTOMATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFORMABLE_TO_RIGID_AUTOMATIC").unwrap());
        Self {
            swset: t.take("SWSET").and_then(|c| c.into_int()).unwrap_or_default(),
            code: t.take("CODE").and_then(|c| c.into_int()).unwrap_or_default(),
            time1: t.take("TIME1").and_then(|c| c.into_float()).unwrap_or_default(),
            time2: t.take("TIME2").and_then(|c| c.into_float()).unwrap_or_default(),
            time3: t.take("TIME3").and_then(|c| c.into_float()).unwrap_or_default(),
            entno: t.take("ENTNO").and_then(|c| c.into_int()).unwrap_or_default(),
            relsw: t.take("RELSW").and_then(|c| c.into_int()).unwrap_or_default(),
            paired: t.take("PAIRED").and_then(|c| c.into_int()).unwrap_or_default(),
            nrbf: t.take("NRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncsf: t.take("NCSF").and_then(|c| c.into_int()).unwrap_or_default(),
            rwf: t.take("RWF").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            d2r: t.take("D2R").and_then(|c| c.into_int()).unwrap_or_default(),
            r2d: t.take("R2D").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid_2: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype_2: t.take("PTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DEFORMABLE_TO_RIGID_INERTIA {
    pub pid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
}
impl DEFORMABLE_TO_RIGID_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DEFORMABLE_TO_RIGID_INERTIA").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_CONTACT {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DELETE_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_CONTACT").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_CONTACT_2DAUTO {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DELETE_CONTACT_2DAUTO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_CONTACT_2DAUTO").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_ELEMENT_BEAM {
    pub esid: Vec<i64>,
}
impl DELETE_ELEMENT_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_ELEMENT_BEAM").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_ELEMENT_SHELL {
    pub esid: Vec<i64>,
}
impl DELETE_ELEMENT_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_ELEMENT_SHELL").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_ELEMENT_SOLID {
    pub esid: Vec<i64>,
}
impl DELETE_ELEMENT_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_ELEMENT_SOLID").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_ELEMENT_TSHELL {
    pub esid: Vec<i64>,
}
impl DELETE_ELEMENT_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_ELEMENT_TSHELL").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_ENTITY {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DELETE_ENTITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_ENTITY").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_FSI {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DELETE_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_FSI").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DELETE_PART {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl DELETE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DELETE_PART").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_AXISYMMETRIC_MSURF {
    pub mspid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_AXISYMMETRIC_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_AXISYMMETRIC_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_AXISYMMETRIC_SEGMENT_SET {
    pub ssid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_AXISYMMETRIC_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_AXISYMMETRIC_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_CYCLIC_MSURF {
    pub mspid1: Vec<i64>,
    pub mspid2: Vec<i64>,
    pub cyctyp: Vec<i64>,
}
impl DUALCESE_BOUNDARY_CYCLIC_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_CYCLIC_MSURF").unwrap());
        Self {
            mspid1: t.take("MSPID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mspid2: t.take("MSPID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_CYCLIC_SEGMENT_SET {
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub cyctyp: Vec<i64>,
}
impl DUALCESE_BOUNDARY_CYCLIC_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_CYCLIC_SEGMENT_SET").unwrap());
        Self {
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cyctyp: t.take("CYCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_FSI_MSURF {
    pub mspid: Vec<i64>,
    pub ref_p: Vec<f64>,
}
impl DUALCESE_BOUNDARY_FSI_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_FSI_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_p: t.take("REF_P").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_FSI_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub ref_p: Vec<f64>,
}
impl DUALCESE_BOUNDARY_FSI_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_FSI_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ref_p: t.take("REF_P").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_NON_REFLECTIVE_MSURF {
    pub mspid: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_NON_REFLECTIVE_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_NON_REFLECTIVE_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_NON_REFLECTIVE_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_NON_REFLECTIVE_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_NON_REFLECTIVE_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_MSURF {
    pub mspid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_z1: Vec<i64>,
    pub lc_ra: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_d1: Vec<i64>,
    pub lc_da: Vec<i64>,
    pub lc_db: Vec<i64>,
    pub lc_p: Vec<f64>,
    pub lc_t: Vec<f64>,
    pub sf_z1: Vec<f64>,
    pub sf_ra: Vec<f64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_d1: Vec<f64>,
    pub sf_da: Vec<f64>,
    pub sf_db: Vec<f64>,
    pub lc_p_2: Vec<f64>,
    pub lc_t_2: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_z1: t.take("LC_Z1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_ra: t.take("LC_RA").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d1: t.take("LC_D1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_da: t.take("LC_DA").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_db: t.take("LC_DB").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_z1: t.take("SF_Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_ra: t.take("SF_RA").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d1: t.take("SF_D1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_da: t.take("SF_DA").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_db: t.take("SF_DB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_p_2: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t_2: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_z1: Vec<i64>,
    pub lc_ra: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_d1: Vec<i64>,
    pub lc_da: Vec<i64>,
    pub lc_db: Vec<i64>,
    pub lc_p: Vec<f64>,
    pub lc_t: Vec<f64>,
    pub sf_z1: Vec<f64>,
    pub sf_ra: Vec<f64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_d1: Vec<f64>,
    pub sf_da: Vec<f64>,
    pub sf_db: Vec<f64>,
    pub lc_p_2: Vec<f64>,
    pub lc_t_2: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_HYBRID_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_z1: t.take("LC_Z1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_ra: t.take("LC_RA").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d1: t.take("LC_D1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_da: t.take("LC_DA").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_db: t.take("LC_DB").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_z1: t.take("SF_Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_ra: t.take("SF_RA").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d1: t.take("SF_D1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_da: t.take("SF_DA").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_db: t.take("SF_DB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_p_2: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t_2: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_MSURF {
    pub mspid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_TWO_PHASE_MSURF {
    pub mspid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_z1: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_d1: Vec<i64>,
    pub lc_d2: Vec<i64>,
    pub lc_p: Vec<f64>,
    pub lc_t: Vec<f64>,
    pub sf_z1: Vec<f64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_d1: Vec<f64>,
    pub sf_d2: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_TWO_PHASE_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_TWO-PHASE_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_z1: t.take("LC_Z1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d1: t.take("LC_D1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d2: t.take("LC_D2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_z1: t.take("SF_Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d1: t.take("SF_D1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d2: t.take("SF_D2").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_TWO_PHASE_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_z1: Vec<i64>,
    pub lc_u: Vec<i64>,
    pub lc_v: Vec<i64>,
    pub lc_w: Vec<i64>,
    pub lc_d1: Vec<i64>,
    pub lc_d2: Vec<i64>,
    pub lc_p: Vec<f64>,
    pub lc_t: Vec<f64>,
    pub sf_z1: Vec<f64>,
    pub sf_u: Vec<f64>,
    pub sf_v: Vec<f64>,
    pub sf_w: Vec<f64>,
    pub sf_d1: Vec<f64>,
    pub sf_d2: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_TWO_PHASE_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_TWO-PHASE_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_z1: t.take("LC_Z1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_u: t.take("LC_U").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_v: t.take("LC_V").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_w: t.take("LC_W").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d1: t.take("LC_D1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_d2: t.take("LC_D2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_z1: t.take("SF_Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_u: t.take("SF_U").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_v: t.take("SF_V").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_w: t.take("SF_W").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d1: t.take("SF_D1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_d2: t.take("SF_D2").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_VN_MSURF {
    pub mspid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_vn: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_vn: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_VN_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_VN_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_vn: t.take("LC_VN").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_vn: t.take("SF_VN").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_PRESCRIBED_VN_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub idcomp: Vec<i64>,
    pub dirx: Vec<f64>,
    pub diry: Vec<f64>,
    pub dirz: Vec<f64>,
    pub lc_vn: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub lc_rho: Vec<i64>,
    pub lc_p: Vec<i64>,
    pub lc_t: Vec<i64>,
    pub sf_vn: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub sf_rho: Vec<f64>,
    pub sf_p: Vec<f64>,
    pub sf_t: Vec<f64>,
}
impl DUALCESE_BOUNDARY_PRESCRIBED_VN_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_PRESCRIBED_VN_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            idcomp: t.take("IDCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            dirx: t.take("DIRX").and_then(|c| c.into_float()).unwrap_or_default(),
            diry: t.take("DIRY").and_then(|c| c.into_float()).unwrap_or_default(),
            dirz: t.take("DIRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_vn: t.take("LC_VN").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_rho: t.take("LC_RHO").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_p: t.take("LC_P").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_t: t.take("LC_T").and_then(|c| c.into_int()).unwrap_or_default(),
            sf_vn: t.take("SF_VN").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_rho: t.take("SF_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_p: t.take("SF_P").and_then(|c| c.into_float()).unwrap_or_default(),
            sf_t: t.take("SF_T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_REFLECTIVE_MSURF {
    pub mspid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_REFLECTIVE_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_REFLECTIVE_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_REFLECTIVE_SEGMENT_SET {
    pub ssid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_REFLECTIVE_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_REFLECTIVE_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SLIDING_MSURF {
    pub mspid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_SLIDING_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SLIDING_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SLIDING_SEGMENT_SET {
    pub ssid: Vec<i64>,
}
impl DUALCESE_BOUNDARY_SLIDING_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SLIDING_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SOLID_WALL_MSURF {
    pub mspid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_SOLID_WALL_MSURF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SOLID_WALL_MSURF").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE {
    pub mspid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SOLID_WALL_MSURF_ROTATE").unwrap());
        Self {
            mspid: t.take("MSPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET_ROTATE {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
}
impl DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET_ROTATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_BOUNDARY_SOLID_WALL_SEGMENT_SET_ROTATE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_CONTROL_LIMITER {
    pub idlmt: Vec<i64>,
    pub alfa: Vec<f64>,
    pub beta: Vec<f64>,
    pub epsr: Vec<f64>,
}
impl DUALCESE_CONTROL_LIMITER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_CONTROL_LIMITER").unwrap());
        Self {
            idlmt: t.take("IDLMT").and_then(|c| c.into_int()).unwrap_or_default(),
            alfa: t.take("ALFA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_CONTROL_MESH_MOV {
    pub id: Vec<i64>,
    pub ialg: Vec<i64>,
    pub ninter: Vec<i64>,
    pub relerr: Vec<f64>,
    pub mxdispr: Vec<f64>,
}
impl DUALCESE_CONTROL_MESH_MOV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_CONTROL_MESH_MOV").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            ialg: t.take("IALG").and_then(|c| c.into_int()).unwrap_or_default(),
            ninter: t.take("NINTER").and_then(|c| c.into_int()).unwrap_or_default(),
            relerr: t.take("RELERR").and_then(|c| c.into_float()).unwrap_or_default(),
            mxdispr: t.take("MXDISPR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_CONTROL_SOLVER {
    pub eqns: Vec<String>,
    pub igeom: Vec<String>,
    pub iframe: Vec<String>,
    pub mixtype: Vec<String>,
    pub idc: Vec<f64>,
    pub isnan: Vec<i64>,
}
impl DUALCESE_CONTROL_SOLVER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_CONTROL_SOLVER").unwrap());
        Self {
            eqns: t.take("EQNS").and_then(|c| c.into_str()).unwrap_or_default(),
            igeom: t.take("IGEOM").and_then(|c| c.into_str()).unwrap_or_default(),
            iframe: t.take("IFRAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mixtype: t.take("MIXTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            idc: t.take("IDC").and_then(|c| c.into_float()).unwrap_or_default(),
            isnan: t.take("ISNAN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_CONTROL_TIMESTEP {
    pub iddt: Vec<i64>,
    pub cfl: Vec<f64>,
    pub dtint: Vec<f64>,
}
impl DUALCESE_CONTROL_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_CONTROL_TIMESTEP").unwrap());
        Self {
            iddt: t.take("IDDT").and_then(|c| c.into_int()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtint: t.take("DTINT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_D3PLOT {
    pub flow_var: Vec<String>,
}
impl DUALCESE_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_D3PLOT").unwrap());
        Self {
            flow_var: t.take("FLOW_VAR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_D3PLOT_FLUID_SSID {
    pub ssid: Vec<i64>,
    pub flow_var: Vec<String>,
}
impl DUALCESE_D3PLOT_FLUID_SSID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_D3PLOT_FLUID_SSID").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            flow_var: t.take("FLOW_VAR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_ELE2D {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl DUALCESE_ELE2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_ELE2D").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_ELE3D {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl DUALCESE_ELE3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_ELE3D").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_ELEMENTSET {
    pub esid: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl DUALCESE_ELEMENTSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_ELEMENTSET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_COCHRAN_CHAN {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub gammao: Vec<f64>,
    pub rhoo: Vec<f64>,
    pub eo: Vec<f64>,
    pub cv: Vec<f64>,
}
impl DUALCESE_EOS_COCHRAN_CHAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_COCHRAN_CHAN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            gammao: t.take("GAMMAO").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoo: t.take("RHOO").and_then(|c| c.into_float()).unwrap_or_default(),
            eo: t.take("EO").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_COOLPROP {
    pub eosid: Vec<i64>,
    pub ncomp: Vec<i64>,
    pub type_: Vec<String>,
    pub phase: Vec<String>,
    pub tabular: Vec<String>,
    pub mol_fr1: Vec<f64>,
    pub mol_fr2: Vec<f64>,
    pub mol_fr3: Vec<f64>,
    pub mol_fr4: Vec<f64>,
    pub mol_fr5: Vec<f64>,
    pub mol_fr6: Vec<f64>,
    pub mol_fr7: Vec<f64>,
    pub mol_fr8: Vec<f64>,
    pub n_t: Vec<f64>,
    pub n_den: Vec<f64>,
    pub den_low: Vec<f64>,
    pub den_high: Vec<f64>,
    pub t_low: Vec<f64>,
    pub t_high: Vec<f64>,
    pub fluidname: Vec<String>,
}
impl DUALCESE_EOS_COOLPROP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_COOLPROP").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ncomp: t.take("NCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_str()).unwrap_or_default(),
            tabular: t.take("TABULAR").and_then(|c| c.into_str()).unwrap_or_default(),
            mol_fr1: t.take("MOL_FR1").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr2: t.take("MOL_FR2").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr3: t.take("MOL_FR3").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr4: t.take("MOL_FR4").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr5: t.take("MOL_FR5").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr6: t.take("MOL_FR6").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr7: t.take("MOL_FR7").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr8: t.take("MOL_FR8").and_then(|c| c.into_float()).unwrap_or_default(),
            n_t: t.take("N_T").and_then(|c| c.into_float()).unwrap_or_default(),
            n_den: t.take("N_DEN").and_then(|c| c.into_float()).unwrap_or_default(),
            den_low: t.take("DEN_LOW").and_then(|c| c.into_float()).unwrap_or_default(),
            den_high: t.take("DEN_HIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            t_low: t.take("T_LOW").and_then(|c| c.into_float()).unwrap_or_default(),
            t_high: t.take("T_HIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            fluidname: t.take("FLUIDNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_IDEAL_GAS {
    pub eosid: Vec<i64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub e0: Vec<f64>,
}
impl DUALCESE_EOS_IDEAL_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_IDEAL_GAS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_INFLATOR1 {
    pub eosid: Vec<i64>,
    pub cp0: Vec<f64>,
    pub cp1: Vec<f64>,
    pub cp2: Vec<f64>,
    pub cp3: Vec<f64>,
    pub cp4: Vec<f64>,
    pub cv0: Vec<f64>,
    pub cv1: Vec<f64>,
    pub cv2: Vec<f64>,
    pub cv3: Vec<f64>,
    pub cv4: Vec<f64>,
}
impl DUALCESE_EOS_INFLATOR1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_INFLATOR1").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cp0: t.take("Cp0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp1: t.take("Cp1").and_then(|c| c.into_float()).unwrap_or_default(),
            cp2: t.take("Cp2").and_then(|c| c.into_float()).unwrap_or_default(),
            cp3: t.take("Cp3").and_then(|c| c.into_float()).unwrap_or_default(),
            cp4: t.take("Cp4").and_then(|c| c.into_float()).unwrap_or_default(),
            cv0: t.take("Cv0").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1: t.take("Cv1").and_then(|c| c.into_float()).unwrap_or_default(),
            cv2: t.take("Cv2").and_then(|c| c.into_float()).unwrap_or_default(),
            cv3: t.take("Cv3").and_then(|c| c.into_float()).unwrap_or_default(),
            cv4: t.take("Cv4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_INFLATOR2 {
    pub eosid: Vec<i64>,
    pub cp10: Vec<f64>,
    pub cp11: Vec<f64>,
    pub cp12: Vec<f64>,
    pub cp13: Vec<f64>,
    pub cp14: Vec<f64>,
    pub cp20: Vec<f64>,
    pub cp21: Vec<f64>,
    pub cp22: Vec<f64>,
    pub cp23: Vec<f64>,
    pub cp24: Vec<f64>,
    pub cv10: Vec<f64>,
    pub cv11: Vec<f64>,
    pub cv12: Vec<f64>,
    pub cv13: Vec<f64>,
    pub cv14: Vec<f64>,
    pub cv20: Vec<f64>,
    pub cv21: Vec<f64>,
    pub cv22: Vec<f64>,
    pub cv23: Vec<f64>,
    pub cv24: Vec<f64>,
}
impl DUALCESE_EOS_INFLATOR2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_INFLATOR2").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cp10: t.take("CP10").and_then(|c| c.into_float()).unwrap_or_default(),
            cp11: t.take("CP11").and_then(|c| c.into_float()).unwrap_or_default(),
            cp12: t.take("CP12").and_then(|c| c.into_float()).unwrap_or_default(),
            cp13: t.take("CP13").and_then(|c| c.into_float()).unwrap_or_default(),
            cp14: t.take("CP14").and_then(|c| c.into_float()).unwrap_or_default(),
            cp20: t.take("CP20").and_then(|c| c.into_float()).unwrap_or_default(),
            cp21: t.take("CP21").and_then(|c| c.into_float()).unwrap_or_default(),
            cp22: t.take("CP22").and_then(|c| c.into_float()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            cp24: t.take("CP24").and_then(|c| c.into_float()).unwrap_or_default(),
            cv10: t.take("CV10").and_then(|c| c.into_float()).unwrap_or_default(),
            cv11: t.take("CV11").and_then(|c| c.into_float()).unwrap_or_default(),
            cv12: t.take("CV12").and_then(|c| c.into_float()).unwrap_or_default(),
            cv13: t.take("CV13").and_then(|c| c.into_float()).unwrap_or_default(),
            cv14: t.take("CV14").and_then(|c| c.into_float()).unwrap_or_default(),
            cv20: t.take("CV20").and_then(|c| c.into_float()).unwrap_or_default(),
            cv21: t.take("CV21").and_then(|c| c.into_float()).unwrap_or_default(),
            cv22: t.take("CV22").and_then(|c| c.into_float()).unwrap_or_default(),
            cv23: t.take("CV23").and_then(|c| c.into_float()).unwrap_or_default(),
            cv24: t.take("CV24").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_JWL {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub gammao: Vec<f64>,
    pub rhoo: Vec<f64>,
    pub eo: Vec<f64>,
    pub cv: Vec<f64>,
}
impl DUALCESE_EOS_JWL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_JWL").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            gammao: t.take("GAMMAO").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoo: t.take("RHOO").and_then(|c| c.into_float()).unwrap_or_default(),
            eo: t.take("EO").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_REFPROP {
    pub eosid: Vec<i64>,
    pub ncomp: Vec<i64>,
    pub type_: Vec<String>,
    pub phase: Vec<String>,
    pub tabular: Vec<String>,
    pub mol_fr1: Vec<f64>,
    pub mol_fr2: Vec<f64>,
    pub mol_fr3: Vec<f64>,
    pub mol_fr4: Vec<f64>,
    pub mol_fr5: Vec<f64>,
    pub mol_fr6: Vec<f64>,
    pub mol_fr7: Vec<f64>,
    pub mol_fr8: Vec<f64>,
    pub n_t: Vec<f64>,
    pub n_den: Vec<f64>,
    pub den_low: Vec<f64>,
    pub den_high: Vec<f64>,
    pub t_low: Vec<f64>,
    pub t_high: Vec<f64>,
    pub fluidname: Vec<String>,
}
impl DUALCESE_EOS_REFPROP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_REFPROP").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ncomp: t.take("NCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_str()).unwrap_or_default(),
            tabular: t.take("TABULAR").and_then(|c| c.into_str()).unwrap_or_default(),
            mol_fr1: t.take("MOL_FR1").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr2: t.take("MOL_FR2").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr3: t.take("MOL_FR3").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr4: t.take("MOL_FR4").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr5: t.take("MOL_FR5").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr6: t.take("MOL_FR6").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr7: t.take("MOL_FR7").and_then(|c| c.into_float()).unwrap_or_default(),
            mol_fr8: t.take("MOL_FR8").and_then(|c| c.into_float()).unwrap_or_default(),
            n_t: t.take("N_T").and_then(|c| c.into_float()).unwrap_or_default(),
            n_den: t.take("N_DEN").and_then(|c| c.into_float()).unwrap_or_default(),
            den_low: t.take("DEN_LOW").and_then(|c| c.into_float()).unwrap_or_default(),
            den_high: t.take("DEN_HIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            t_low: t.take("T_LOW").and_then(|c| c.into_float()).unwrap_or_default(),
            t_high: t.take("T_HIGH").and_then(|c| c.into_float()).unwrap_or_default(),
            fluidname: t.take("FLUIDNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_REFPROP_PATH {
    pub path: Vec<String>,
}
impl DUALCESE_EOS_REFPROP_PATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_REFPROP_PATH").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_SET {
    pub eossid: Vec<i64>,
    pub eosinid: Vec<i64>,
    pub eosrctid: Vec<i64>,
    pub eosprdi: Vec<i64>,
}
impl DUALCESE_EOS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_SET").unwrap());
        Self {
            eossid: t.take("EOSSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosinid: t.take("EOSINID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosrctid: t.take("EOSRCTID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosprdi: t.take("EOSPRDI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_STIFFENED_GAS {
    pub eosid: Vec<i64>,
    pub ga: Vec<f64>,
    pub gb: Vec<f64>,
}
impl DUALCESE_EOS_STIFFENED_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_STIFFENED_GAS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ga: t.take("GA").and_then(|c| c.into_float()).unwrap_or_default(),
            gb: t.take("GB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_EOS_VAN_DER_WAALS_GENERALIZED {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub ga: Vec<f64>,
    pub bt: Vec<f64>,
}
impl DUALCESE_EOS_VAN_DER_WAALS_GENERALIZED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_EOS_VAN_DER_WAALS_GENERALIZED").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ga: t.take("GA").and_then(|c| c.into_float()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_FSI_EXCLUDE {
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl DUALCESE_FSI_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_FSI_EXCLUDE").unwrap());
        Self {
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INCLUDE_MODEL {
    pub filename: Vec<String>,
}
impl DUALCESE_INCLUDE_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INCLUDE_MODEL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL {
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub rh: Vec<f64>,
    pub p: Vec<f64>,
    pub t: Vec<f64>,
    pub unused: Vec<i64>,
    pub ifunc: Vec<i64>,
}
impl DUALCESE_INITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL").unwrap());
        Self {
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            rh: t.take("RH").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_HYBRID {
    pub z1: Vec<f64>,
    pub ra: Vec<f64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rho1: Vec<f64>,
    pub rho_a: Vec<f64>,
    pub rho_b: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub unused: Vec<i64>,
    pub ifunc: Vec<i64>,
}
impl DUALCESE_INITIAL_HYBRID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_HYBRID").unwrap());
        Self {
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            ra: t.take("RA").and_then(|c| c.into_float()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rho1: t.take("RHO1").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_a: t.take("RHO_A").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_b: t.take("RHO_B").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_HYBRID_SET {
    pub esid: Vec<i64>,
    pub ifunc: Vec<i64>,
    pub z1: Vec<f64>,
    pub ra: Vec<f64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rho1: Vec<f64>,
    pub rho_a: Vec<f64>,
    pub rho_b: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
}
impl DUALCESE_INITIAL_HYBRID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_HYBRID_SET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            ra: t.take("RA").and_then(|c| c.into_float()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rho1: t.take("RHO1").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_a: t.take("RHO_A").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_b: t.take("RHO_B").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_SET {
    pub esid: Vec<i64>,
    pub ifunc: Vec<i64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub rho: Vec<f64>,
    pub p: Vec<f64>,
    pub t: Vec<f64>,
}
impl DUALCESE_INITIAL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_SET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_TWO_PHASE {
    pub z1: Vec<f64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rho_1: Vec<f64>,
    pub rho_2: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub ifunc: Vec<i64>,
}
impl DUALCESE_INITIAL_TWO_PHASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_TWO-PHASE").unwrap());
        Self {
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_1: t.take("RHO_1").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_2: t.take("RHO_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_TWO_PHASE_SET {
    pub esid: Vec<i64>,
    pub ifunc: Vec<i64>,
    pub z1: Vec<f64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rho_1: Vec<f64>,
    pub rho_2: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
}
impl DUALCESE_INITIAL_TWO_PHASE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_TWO-PHASE_SET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_1: t.take("RHO_1").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_2: t.take("RHO_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_INITIAL_TWOPHASE {
    pub z1: Vec<f64>,
    pub uic: Vec<f64>,
    pub vic: Vec<f64>,
    pub wic: Vec<f64>,
    pub rho_1: Vec<f64>,
    pub rho_2: Vec<f64>,
    pub pic: Vec<f64>,
    pub tic: Vec<f64>,
    pub ifunc: Vec<i64>,
}
impl DUALCESE_INITIAL_TWOPHASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_INITIAL_TWOPHASE").unwrap());
        Self {
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            uic: t.take("UIC").and_then(|c| c.into_float()).unwrap_or_default(),
            vic: t.take("VIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wic: t.take("WIC").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_1: t.take("RHO_1").and_then(|c| c.into_float()).unwrap_or_default(),
            rho_2: t.take("RHO_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pic: t.take("PIC").and_then(|c| c.into_float()).unwrap_or_default(),
            tic: t.take("TIC").and_then(|c| c.into_float()).unwrap_or_default(),
            ifunc: t.take("IFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_MAT_GAS {
    pub mid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub prnd: Vec<f64>,
}
impl DUALCESE_MAT_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_MAT_GAS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            prnd: t.take("PRND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_MAT_GAS_0 {
    pub mid: Vec<i64>,
    pub mu: Vec<f64>,
    pub k: Vec<f64>,
}
impl DUALCESE_MAT_GAS_0 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_MAT_GAS_0").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_MAT_GAS_2 {
    pub mid: Vec<i64>,
    pub mu0: Vec<f64>,
    pub smu: Vec<f64>,
    pub k0: Vec<f64>,
    pub sk: Vec<f64>,
    pub t0: Vec<f64>,
}
impl DUALCESE_MAT_GAS_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_MAT_GAS_2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mu0: t.take("MU0").and_then(|c| c.into_float()).unwrap_or_default(),
            smu: t.take("SMU").and_then(|c| c.into_float()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            sk: t.take("SK").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_MODEL {
    pub unitsys: Vec<String>,
    pub filename: Vec<String>,
}
impl DUALCESE_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_MODEL").unwrap());
        Self {
            unitsys: t.take("UNITSYS").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_NODE2D {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
}
impl DUALCESE_NODE2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_NODE2D").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_NODE3D {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl DUALCESE_NODE3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_NODE3D").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_NODESET {
    pub nsid: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl DUALCESE_NODESET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_NODESET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_PART {
    pub pid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub fsitype: Vec<String>,
    pub mmshid: Vec<i64>,
}
impl DUALCESE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            fsitype: t.take("FSITYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            mmshid: t.take("MMSHID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_PART_MULTIPHASE {
    pub pid: Vec<i64>,
    pub react_id: Vec<i64>,
    pub eossid: Vec<i64>,
    pub mid: Vec<i64>,
    pub fsitype: Vec<String>,
    pub mmshid: Vec<i64>,
}
impl DUALCESE_PART_MULTIPHASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_PART_MULTIPHASE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            react_id: t.take("REACT_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            eossid: t.take("EOSSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            fsitype: t.take("FSITYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            mmshid: t.take("MMSHID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_PART_SURFACE {
    pub pid: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
}
impl DUALCESE_PART_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_PART_SURFACE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_REACTION_RATE_IG {
    pub react_id: Vec<i64>,
    pub ign: Vec<f64>,
    pub aa: Vec<f64>,
    pub bb: Vec<f64>,
    pub xx: Vec<f64>,
    pub grow1: Vec<f64>,
    pub cc: Vec<f64>,
    pub dd: Vec<f64>,
    pub yy: Vec<f64>,
    pub grow2: Vec<f64>,
    pub ee: Vec<f64>,
    pub gg: Vec<f64>,
    pub zz: Vec<f64>,
    pub igmax: Vec<f64>,
    pub g1max: Vec<f64>,
    pub g2max: Vec<f64>,
}
impl DUALCESE_REACTION_RATE_IG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_REACTION_RATE_IG").unwrap());
        Self {
            react_id: t.take("REACT_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            ign: t.take("IGN").and_then(|c| c.into_float()).unwrap_or_default(),
            aa: t.take("AA").and_then(|c| c.into_float()).unwrap_or_default(),
            bb: t.take("BB").and_then(|c| c.into_float()).unwrap_or_default(),
            xx: t.take("XX").and_then(|c| c.into_float()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            dd: t.take("DD").and_then(|c| c.into_float()).unwrap_or_default(),
            yy: t.take("YY").and_then(|c| c.into_float()).unwrap_or_default(),
            grow2: t.take("GROW2").and_then(|c| c.into_float()).unwrap_or_default(),
            ee: t.take("EE").and_then(|c| c.into_float()).unwrap_or_default(),
            gg: t.take("GG").and_then(|c| c.into_float()).unwrap_or_default(),
            zz: t.take("ZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            igmax: t.take("IGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            g1max: t.take("G1MAX").and_then(|c| c.into_float()).unwrap_or_default(),
            g2max: t.take("G2MAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_REACTION_RATE_IG_REDUCED {
    pub react_id: Vec<i64>,
    pub grow1: Vec<f64>,
    pub cc: Vec<f64>,
    pub dd: Vec<f64>,
    pub yy: Vec<f64>,
    pub ph10: Vec<f64>,
}
impl DUALCESE_REACTION_RATE_IG_REDUCED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_REACTION_RATE_IG_REDUCED").unwrap());
        Self {
            react_id: t.take("REACT_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            dd: t.take("DD").and_then(|c| c.into_float()).unwrap_or_default(),
            yy: t.take("YY").and_then(|c| c.into_float()).unwrap_or_default(),
            ph10: t.take("PH10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_REACTION_RATE_P_DEPEND {
    pub react_id: Vec<i64>,
    pub sigma: Vec<f64>,
    pub nu: Vec<f64>,
    pub n: Vec<f64>,
}
impl DUALCESE_REACTION_RATE_P_DEPEND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_REACTION_RATE_P_DEPEND").unwrap());
        Self {
            react_id: t.take("REACT_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct DUALCESE_SEGMENTSET {
    pub sid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl DUALCESE_SEGMENTSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("DUALCESE_SEGMENTSET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EF_CONTROL {
    pub nphton: Vec<i64>,
    pub nrefs: Vec<i64>,
    pub nwarns: Vec<i64>,
    pub nlost: Vec<i64>,
    pub nloops: Vec<i64>,
    pub errodef: Vec<f64>,
    pub inseed: Vec<i64>,
    pub nphton_2: Vec<i64>,
    pub nrefs_2: Vec<i64>,
    pub nwarns_2: Vec<i64>,
    pub nlost_2: Vec<i64>,
}
impl EF_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EF_CONTROL").unwrap());
        Self {
            nphton: t.take("NPHTON").and_then(|c| c.into_int()).unwrap_or_default(),
            nrefs: t.take("NREFS").and_then(|c| c.into_int()).unwrap_or_default(),
            nwarns: t.take("NWARNS").and_then(|c| c.into_int()).unwrap_or_default(),
            nlost: t.take("NLOST").and_then(|c| c.into_int()).unwrap_or_default(),
            nloops: t.take("NLOOPS").and_then(|c| c.into_int()).unwrap_or_default(),
            errodef: t.take("ERRODEF").and_then(|c| c.into_float()).unwrap_or_default(),
            inseed: t.take("INSEED").and_then(|c| c.into_int()).unwrap_or_default(),
            nphton_2: t.take("NPHTON").and_then(|c| c.into_int()).unwrap_or_default(),
            nrefs_2: t.take("NREFS").and_then(|c| c.into_int()).unwrap_or_default(),
            nwarns_2: t.take("NWARNS").and_then(|c| c.into_int()).unwrap_or_default(),
            nlost_2: t.take("NLOST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EF_GRID {
    pub ngx: Vec<i64>,
    pub ngy: Vec<i64>,
    pub ngz: Vec<i64>,
}
impl EF_GRID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EF_GRID").unwrap());
        Self {
            ngx: t.take("NGX").and_then(|c| c.into_int()).unwrap_or_default(),
            ngy: t.take("NGY").and_then(|c| c.into_int()).unwrap_or_default(),
            ngz: t.take("NGZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EF_MATERIAL {
    pub nmat: Vec<i64>,
    pub name: Vec<String>,
    pub mtyp: Vec<i64>,
    pub exe: Vec<f64>,
    pub eye: Vec<f64>,
    pub eze: Vec<f64>,
    pub rhos: Vec<f64>,
    pub rhod: Vec<f64>,
    pub taus: Vec<f64>,
    pub taud: Vec<f64>,
    pub rdiffr: Vec<f64>,
    pub rdifft: Vec<f64>,
}
impl EF_MATERIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EF_MATERIAL").unwrap());
        Self {
            nmat: t.take("NMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            name: t.take("NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mtyp: t.take("MTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            exe: t.take("EXE").and_then(|c| c.into_float()).unwrap_or_default(),
            eye: t.take("EYE").and_then(|c| c.into_float()).unwrap_or_default(),
            eze: t.take("EZE").and_then(|c| c.into_float()).unwrap_or_default(),
            rhos: t.take("RHOS").and_then(|c| c.into_float()).unwrap_or_default(),
            rhod: t.take("RHOD").and_then(|c| c.into_float()).unwrap_or_default(),
            taus: t.take("TAUS").and_then(|c| c.into_float()).unwrap_or_default(),
            taud: t.take("TAUD").and_then(|c| c.into_float()).unwrap_or_default(),
            rdiffr: t.take("RDIFFR").and_then(|c| c.into_float()).unwrap_or_default(),
            rdifft: t.take("RDIFFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EF_TOGGLES {
    pub iprint1: Vec<i64>,
    pub iprint2: Vec<i64>,
    pub iprint3: Vec<i64>,
    pub iprint4: Vec<i64>,
    pub idata: Vec<i64>,
    pub itraces: Vec<i64>,
    pub irstrt: Vec<i64>,
}
impl EF_TOGGLES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EF_TOGGLES").unwrap());
        Self {
            iprint1: t.take("IPRINT1").and_then(|c| c.into_int()).unwrap_or_default(),
            iprint2: t.take("IPRINT2").and_then(|c| c.into_int()).unwrap_or_default(),
            iprint3: t.take("IPRINT3").and_then(|c| c.into_int()).unwrap_or_default(),
            iprint4: t.take("IPRINT4").and_then(|c| c.into_int()).unwrap_or_default(),
            idata: t.take("IDATA").and_then(|c| c.into_int()).unwrap_or_default(),
            itraces: t.take("ITRACES").and_then(|c| c.into_int()).unwrap_or_default(),
            irstrt: t.take("IRSTRT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
}
impl ELEMENT_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_ELBOW {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n5: Vec<i64>,
    pub n3: Vec<i64>,
}
impl ELEMENT_BEAM_ELBOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_ELBOW").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_ORIENTATION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl ELEMENT_BEAM_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_ORIENTATION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_ORIENTATION_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_ORIENTATION_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_ORIENTATION_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_PID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
}
impl ELEMENT_BEAM_PID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_PID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_PID_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_PID_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_PID_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_PID_ORIENTATION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl ELEMENT_BEAM_PID_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_PID_ORIENTATION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_PULLEY {
    pub puid: Vec<i64>,
    pub bid1: Vec<i64>,
    pub bid2: Vec<i64>,
    pub pnid: Vec<i64>,
    pub fd: Vec<f64>,
    pub fs: Vec<f64>,
    pub lmin: Vec<f64>,
    pub dc: Vec<f64>,
}
impl ELEMENT_BEAM_PULLEY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_PULLEY").unwrap());
        Self {
            puid: t.take("PUID").and_then(|c| c.into_int()).unwrap_or_default(),
            bid1: t.take("BID1").and_then(|c| c.into_int()).unwrap_or_default(),
            bid2: t.take("BID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pnid: t.take("PNID").and_then(|c| c.into_int()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SCALAR {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
}
impl ELEMENT_BEAM_SCALAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SCALAR").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SCALAR_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_SCALAR_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SCALAR_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SCALAR_ORIENTATION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl ELEMENT_BEAM_SCALAR_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SCALAR_ORIENTATION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SCALAR_PID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
}
impl ELEMENT_BEAM_SCALAR_PID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SCALAR_PID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SECTION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub stype: Vec<String>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
}
impl ELEMENT_BEAM_SECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SECTION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SECTION_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub stype: Vec<String>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_SECTION_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SECTION_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SECTION_ORIENTATION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub stype: Vec<String>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl ELEMENT_BEAM_SECTION_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SECTION_ORIENTATION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SECTION_PID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub stype: Vec<String>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
}
impl ELEMENT_BEAM_SECTION_PID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SECTION_PID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SECTION_SCALAR {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub stype: Vec<String>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
}
impl ELEMENT_BEAM_SECTION_SCALAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SECTION_SCALAR").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_SOURCE {
    pub bsid: Vec<i64>,
    pub bsnid: Vec<i64>,
    pub bseid: Vec<i64>,
    pub nele: Vec<i64>,
    pub lfed: Vec<f64>,
    pub fpull: Vec<f64>,
    pub lmin: Vec<f64>,
}
impl ELEMENT_BEAM_SOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_SOURCE").unwrap());
        Self {
            bsid: t.take("BSID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsnid: t.take("BSNID").and_then(|c| c.into_int()).unwrap_or_default(),
            bseid: t.take("BSEID").and_then(|c| c.into_int()).unwrap_or_default(),
            nele: t.take("NELE").and_then(|c| c.into_int()).unwrap_or_default(),
            lfed: t.take("LFED").and_then(|c| c.into_float()).unwrap_or_default(),
            fpull: t.take("FPULL").and_then(|c| c.into_float()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_THICKNESS {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub parm1: Vec<f64>,
    pub parm2: Vec<f64>,
    pub parm3: Vec<f64>,
    pub parm4: Vec<f64>,
    pub parm5: Vec<f64>,
}
impl ELEMENT_BEAM_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_THICKNESS").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            parm1: t.take("PARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            parm2: t.take("PARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            parm3: t.take("PARM3").and_then(|c| c.into_float()).unwrap_or_default(),
            parm4: t.take("PARM4").and_then(|c| c.into_float()).unwrap_or_default(),
            parm5: t.take("PARM5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_THICKNESS_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub parm1: Vec<f64>,
    pub parm2: Vec<f64>,
    pub parm3: Vec<f64>,
    pub parm4: Vec<f64>,
    pub parm5: Vec<f64>,
    pub wx1: Vec<f64>,
    pub wy1: Vec<f64>,
    pub wz1: Vec<f64>,
    pub wx2: Vec<f64>,
    pub wy2: Vec<f64>,
    pub wz2: Vec<f64>,
}
impl ELEMENT_BEAM_THICKNESS_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_THICKNESS_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            parm1: t.take("PARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            parm2: t.take("PARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            parm3: t.take("PARM3").and_then(|c| c.into_float()).unwrap_or_default(),
            parm4: t.take("PARM4").and_then(|c| c.into_float()).unwrap_or_default(),
            parm5: t.take("PARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            wx1: t.take("WX1").and_then(|c| c.into_float()).unwrap_or_default(),
            wy1: t.take("WY1").and_then(|c| c.into_float()).unwrap_or_default(),
            wz1: t.take("WZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            wx2: t.take("WX2").and_then(|c| c.into_float()).unwrap_or_default(),
            wy2: t.take("WY2").and_then(|c| c.into_float()).unwrap_or_default(),
            wz2: t.take("WZ2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_THICKNESS_ORIENTATION {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub parm1: Vec<f64>,
    pub parm2: Vec<f64>,
    pub parm3: Vec<f64>,
    pub parm4: Vec<f64>,
    pub parm5: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl ELEMENT_BEAM_THICKNESS_ORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_THICKNESS_ORIENTATION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            parm1: t.take("PARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            parm2: t.take("PARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            parm3: t.take("PARM3").and_then(|c| c.into_float()).unwrap_or_default(),
            parm4: t.take("PARM4").and_then(|c| c.into_float()).unwrap_or_default(),
            parm5: t.take("PARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_THICKNESS_PID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub parm1: Vec<f64>,
    pub parm2: Vec<f64>,
    pub parm3: Vec<f64>,
    pub parm4: Vec<f64>,
    pub parm5: Vec<f64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
}
impl ELEMENT_BEAM_THICKNESS_PID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_THICKNESS_PID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            parm1: t.take("PARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            parm2: t.take("PARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            parm3: t.take("PARM3").and_then(|c| c.into_float()).unwrap_or_default(),
            parm4: t.take("PARM4").and_then(|c| c.into_float()).unwrap_or_default(),
            parm5: t.take("PARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_THICKNESS_SCALAR {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub parm1: Vec<f64>,
    pub parm2: Vec<f64>,
    pub parm3: Vec<f64>,
    pub parm4: Vec<f64>,
    pub parm5: Vec<f64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
}
impl ELEMENT_BEAM_THICKNESS_SCALAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_THICKNESS_SCALAR").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            parm1: t.take("PARM1").and_then(|c| c.into_float()).unwrap_or_default(),
            parm2: t.take("PARM2").and_then(|c| c.into_float()).unwrap_or_default(),
            parm3: t.take("PARM3").and_then(|c| c.into_float()).unwrap_or_default(),
            parm4: t.take("PARM4").and_then(|c| c.into_float()).unwrap_or_default(),
            parm5: t.take("PARM5").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEAM_WARPAGE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub rt1: Vec<i64>,
    pub rr1: Vec<i64>,
    pub rt2: Vec<i64>,
    pub rr2: Vec<i64>,
    pub local: Vec<i64>,
    pub sn1: Vec<i64>,
    pub sn2: Vec<i64>,
}
impl ELEMENT_BEAM_WARPAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEAM_WARPAGE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            rt1: t.take("RT1").and_then(|c| c.into_int()).unwrap_or_default(),
            rr1: t.take("RR1").and_then(|c| c.into_int()).unwrap_or_default(),
            rt2: t.take("RT2").and_then(|c| c.into_int()).unwrap_or_default(),
            rr2: t.take("RR2").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            sn1: t.take("SN1").and_then(|c| c.into_int()).unwrap_or_default(),
            sn2: t.take("SN2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BEARING {
    pub id: Vec<i64>,
    pub itype: Vec<i64>,
    pub n1: Vec<i64>,
    pub cid1: Vec<i64>,
    pub n2: Vec<i64>,
    pub ci2: Vec<i64>,
    pub nb: Vec<i64>,
    pub eball: Vec<f64>,
    pub prball: Vec<f64>,
    pub erace: Vec<f64>,
    pub prrace: Vec<f64>,
    pub stresl: Vec<f64>,
    pub d: Vec<f64>,
    pub di: Vec<f64>,
    pub do_: Vec<f64>,
    pub dm: Vec<f64>,
    pub ao: Vec<f64>,
    pub ai: Vec<f64>,
    pub bo: Vec<f64>,
    pub pd: Vec<f64>,
    pub ipflag: Vec<i64>,
    pub xtran: Vec<f64>,
    pub ytran: Vec<f64>,
    pub ztran: Vec<f64>,
    pub xrot: Vec<f64>,
    pub yrot: Vec<f64>,
}
impl ELEMENT_BEARING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BEARING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            cid1: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            ci2: t.take("CI2").and_then(|c| c.into_int()).unwrap_or_default(),
            nb: t.take("NB").and_then(|c| c.into_int()).unwrap_or_default(),
            eball: t.take("EBALL").and_then(|c| c.into_float()).unwrap_or_default(),
            prball: t.take("PRBALL").and_then(|c| c.into_float()).unwrap_or_default(),
            erace: t.take("ERACE").and_then(|c| c.into_float()).unwrap_or_default(),
            prrace: t.take("PRRACE").and_then(|c| c.into_float()).unwrap_or_default(),
            stresl: t.take("STRESL").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            di: t.take("DI").and_then(|c| c.into_float()).unwrap_or_default(),
            do_: t.take("DO").and_then(|c| c.into_float()).unwrap_or_default(),
            dm: t.take("DM").and_then(|c| c.into_float()).unwrap_or_default(),
            ao: t.take("AO").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("AI").and_then(|c| c.into_float()).unwrap_or_default(),
            bo: t.take("BO").and_then(|c| c.into_float()).unwrap_or_default(),
            pd: t.take("PD").and_then(|c| c.into_float()).unwrap_or_default(),
            ipflag: t.take("IPFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            xtran: t.take("XTRAN").and_then(|c| c.into_float()).unwrap_or_default(),
            ytran: t.take("YTRAN").and_then(|c| c.into_float()).unwrap_or_default(),
            ztran: t.take("ZTRAN").and_then(|c| c.into_float()).unwrap_or_default(),
            xrot: t.take("XROT").and_then(|c| c.into_float()).unwrap_or_default(),
            yrot: t.take("YROT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_BLANKING {
    pub pid: Vec<i64>,
}
impl ELEMENT_BLANKING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_BLANKING").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DIRECT_MATRIX_INPUT {
    pub eid: Vec<i64>,
    pub ifrmt: Vec<i64>,
    pub filename: Vec<String>,
    pub mass: Vec<String>,
    pub damp: Vec<String>,
    pub stif: Vec<String>,
    pub inert: Vec<String>,
}
impl ELEMENT_DIRECT_MATRIX_INPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DIRECT_MATRIX_INPUT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifrmt: t.take("IFRMT").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_str()).unwrap_or_default(),
            stif: t.take("STIF").and_then(|c| c.into_str()).unwrap_or_default(),
            inert: t.take("INERT").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DIRECT_MATRIX_INPUT_BINARY {
    pub eid: Vec<i64>,
    pub ifrmt: Vec<i64>,
    pub filename: Vec<String>,
    pub mass: Vec<String>,
    pub damp: Vec<String>,
    pub stif: Vec<String>,
    pub inert: Vec<String>,
}
impl ELEMENT_DIRECT_MATRIX_INPUT_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DIRECT_MATRIX_INPUT_BINARY").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifrmt: t.take("IFRMT").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_str()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_str()).unwrap_or_default(),
            stif: t.take("STIF").and_then(|c| c.into_str()).unwrap_or_default(),
            inert: t.take("INERT").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DISCRETE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub vid: Vec<i64>,
    pub s: Vec<f64>,
    pub pf: Vec<i64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_DISCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DISCRETE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            pf: t.take("PF").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DISCRETE_LCO {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub vid: Vec<i64>,
    pub s: Vec<f64>,
    pub pf: Vec<i64>,
    pub offset: Vec<f64>,
    pub lcid: Vec<i64>,
    pub lciddr: Vec<i64>,
}
impl ELEMENT_DISCRETE_LCO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DISCRETE_LCO").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            pf: t.take("PF").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DISCRETE_SPHERE {
    pub nid: Vec<i64>,
    pub pid: Vec<i64>,
    pub mass: Vec<f64>,
    pub inertia: Vec<f64>,
    pub radii: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub nid2: Vec<i64>,
}
impl ELEMENT_DISCRETE_SPHERE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DISCRETE_SPHERE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            inertia: t.take("INERTIA").and_then(|c| c.into_float()).unwrap_or_default(),
            radii: t.take("RADII").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_DISCRETE_SPHERE_VOLUME {
    pub nid: Vec<i64>,
    pub pid: Vec<i64>,
    pub volume: Vec<f64>,
    pub inertia: Vec<f64>,
    pub radii: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub nid2: Vec<i64>,
}
impl ELEMENT_DISCRETE_SPHERE_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_DISCRETE_SPHERE_VOLUME").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            volume: t.take("VOLUME").and_then(|c| c.into_float()).unwrap_or_default(),
            inertia: t.take("INERTIA").and_then(|c| c.into_float()).unwrap_or_default(),
            radii: t.take("RADII").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_GENERALIZED_SHELL {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nmnp: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_GENERALIZED_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_GENERALIZED_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmnp: t.take("NMNP").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_GENERALIZED_SOLID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nmnp: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_GENERALIZED_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_GENERALIZED_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmnp: t.take("NMNP").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_INERTIA {
    pub eid: Vec<i64>,
    pub nid: Vec<i64>,
    pub csid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub mass: Vec<f64>,
}
impl ELEMENT_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_INERTIA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            csid: t.take("CSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_INERTIA_OFFSET {
    pub eid: Vec<i64>,
    pub nid: Vec<i64>,
    pub csid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub mass: Vec<f64>,
    pub x_off: Vec<f64>,
    pub y_off: Vec<f64>,
    pub z_off: Vec<f64>,
}
impl ELEMENT_INERTIA_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_INERTIA_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            csid: t.take("CSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            x_off: t.take("X-OFF").and_then(|c| c.into_float()).unwrap_or_default(),
            y_off: t.take("Y_OFF").and_then(|c| c.into_float()).unwrap_or_default(),
            z_off: t.take("Z_OFF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_INTERPOLATION_SHELL {
    pub eids: Vec<i64>,
    pub eidgs: Vec<i64>,
    pub ngp: Vec<i64>,
    pub ip1: Vec<i64>,
    pub w1: Vec<f64>,
    pub ip2: Vec<i64>,
    pub w2: Vec<f64>,
    pub ip3: Vec<i64>,
    pub w3: Vec<f64>,
    pub ip4: Vec<i64>,
    pub w4: Vec<f64>,
}
impl ELEMENT_INTERPOLATION_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_INTERPOLATION_SHELL").unwrap());
        Self {
            eids: t.take("EIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            eidgs: t.take("EIDGS").and_then(|c| c.into_int()).unwrap_or_default(),
            ngp: t.take("NGP").and_then(|c| c.into_int()).unwrap_or_default(),
            ip1: t.take("IP1").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            ip2: t.take("IP2").and_then(|c| c.into_int()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            ip3: t.take("IP3").and_then(|c| c.into_int()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            ip4: t.take("IP4").and_then(|c| c.into_int()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_INTERPOLATION_SOLID {
    pub eids: Vec<i64>,
    pub eidgs: Vec<i64>,
    pub ngp: Vec<i64>,
    pub ip1: Vec<i64>,
    pub w1: Vec<f64>,
    pub ip2: Vec<i64>,
    pub w2: Vec<f64>,
    pub ip3: Vec<i64>,
    pub w3: Vec<f64>,
    pub ip4: Vec<i64>,
    pub w4: Vec<f64>,
}
impl ELEMENT_INTERPOLATION_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_INTERPOLATION_SOLID").unwrap());
        Self {
            eids: t.take("EIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            eidgs: t.take("EIDGS").and_then(|c| c.into_int()).unwrap_or_default(),
            ngp: t.take("NGP").and_then(|c| c.into_int()).unwrap_or_default(),
            ip1: t.take("IP1").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            ip2: t.take("IP2").and_then(|c| c.into_int()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            ip3: t.take("IP3").and_then(|c| c.into_int()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            ip4: t.take("IP4").and_then(|c| c.into_int()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_LANCING {
    pub idpt: Vec<i64>,
    pub idcv: Vec<i64>,
    pub irefine: Vec<i64>,
    pub smin: Vec<f64>,
    pub at: Vec<f64>,
    pub endt: Vec<f64>,
    pub ntimes: Vec<i64>,
}
impl ELEMENT_LANCING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_LANCING").unwrap());
        Self {
            idpt: t.take("IDPT").and_then(|c| c.into_int()).unwrap_or_default(),
            idcv: t.take("IDCV").and_then(|c| c.into_int()).unwrap_or_default(),
            irefine: t.take("IREFINE").and_then(|c| c.into_int()).unwrap_or_default(),
            smin: t.take("SMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            endt: t.take("ENDT").and_then(|c| c.into_float()).unwrap_or_default(),
            ntimes: t.take("NTIMES").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS {
    pub eid: Vec<i64>,
    pub nid: Vec<i64>,
    pub mass: Vec<f64>,
    pub pid: Vec<i64>,
}
impl ELEMENT_MASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS_MATRIX {
    pub eid: Vec<i64>,
    pub id: Vec<i64>,
    pub cid: Vec<f64>,
    pub m11: Vec<f64>,
    pub m21: Vec<f64>,
    pub m22: Vec<f64>,
    pub m31: Vec<f64>,
    pub m32: Vec<f64>,
    pub m33: Vec<f64>,
    pub m41: Vec<f64>,
    pub m41_2: Vec<f64>,
    pub m43: Vec<f64>,
    pub m44: Vec<f64>,
    pub m51: Vec<f64>,
    pub m52: Vec<f64>,
    pub m53: Vec<f64>,
    pub m54: Vec<f64>,
    pub m55: Vec<f64>,
    pub m61: Vec<f64>,
    pub m62: Vec<f64>,
    pub m63: Vec<f64>,
    pub m64: Vec<f64>,
    pub m65: Vec<f64>,
    pub m66: Vec<f64>,
}
impl ELEMENT_MASS_MATRIX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS_MATRIX").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_float()).unwrap_or_default(),
            m11: t.take("M11").and_then(|c| c.into_float()).unwrap_or_default(),
            m21: t.take("M21").and_then(|c| c.into_float()).unwrap_or_default(),
            m22: t.take("M22").and_then(|c| c.into_float()).unwrap_or_default(),
            m31: t.take("M31").and_then(|c| c.into_float()).unwrap_or_default(),
            m32: t.take("M32").and_then(|c| c.into_float()).unwrap_or_default(),
            m33: t.take("M33").and_then(|c| c.into_float()).unwrap_or_default(),
            m41: t.take("M41").and_then(|c| c.into_float()).unwrap_or_default(),
            m41_2: t.take("M41").and_then(|c| c.into_float()).unwrap_or_default(),
            m43: t.take("M43").and_then(|c| c.into_float()).unwrap_or_default(),
            m44: t.take("M44").and_then(|c| c.into_float()).unwrap_or_default(),
            m51: t.take("M51").and_then(|c| c.into_float()).unwrap_or_default(),
            m52: t.take("M52").and_then(|c| c.into_float()).unwrap_or_default(),
            m53: t.take("M53").and_then(|c| c.into_float()).unwrap_or_default(),
            m54: t.take("M54").and_then(|c| c.into_float()).unwrap_or_default(),
            m55: t.take("M55").and_then(|c| c.into_float()).unwrap_or_default(),
            m61: t.take("M61").and_then(|c| c.into_float()).unwrap_or_default(),
            m62: t.take("M62").and_then(|c| c.into_float()).unwrap_or_default(),
            m63: t.take("M63").and_then(|c| c.into_float()).unwrap_or_default(),
            m64: t.take("M64").and_then(|c| c.into_float()).unwrap_or_default(),
            m65: t.take("M65").and_then(|c| c.into_float()).unwrap_or_default(),
            m66: t.take("M66").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS_MATRIX_NODE_SET {
    pub eid: Vec<i64>,
    pub id: Vec<i64>,
    pub cid: Vec<f64>,
    pub m11: Vec<f64>,
    pub m21: Vec<f64>,
    pub m22: Vec<f64>,
    pub m31: Vec<f64>,
    pub m32: Vec<f64>,
    pub m33: Vec<f64>,
    pub m41: Vec<f64>,
    pub m41_2: Vec<f64>,
    pub m43: Vec<f64>,
    pub m44: Vec<f64>,
    pub m51: Vec<f64>,
    pub m52: Vec<f64>,
    pub m53: Vec<f64>,
    pub m54: Vec<f64>,
    pub m55: Vec<f64>,
    pub m61: Vec<f64>,
    pub m62: Vec<f64>,
    pub m63: Vec<f64>,
    pub m64: Vec<f64>,
    pub m65: Vec<f64>,
    pub m66: Vec<f64>,
}
impl ELEMENT_MASS_MATRIX_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS_MATRIX_NODE_SET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_float()).unwrap_or_default(),
            m11: t.take("M11").and_then(|c| c.into_float()).unwrap_or_default(),
            m21: t.take("M21").and_then(|c| c.into_float()).unwrap_or_default(),
            m22: t.take("M22").and_then(|c| c.into_float()).unwrap_or_default(),
            m31: t.take("M31").and_then(|c| c.into_float()).unwrap_or_default(),
            m32: t.take("M32").and_then(|c| c.into_float()).unwrap_or_default(),
            m33: t.take("M33").and_then(|c| c.into_float()).unwrap_or_default(),
            m41: t.take("M41").and_then(|c| c.into_float()).unwrap_or_default(),
            m41_2: t.take("M41").and_then(|c| c.into_float()).unwrap_or_default(),
            m43: t.take("M43").and_then(|c| c.into_float()).unwrap_or_default(),
            m44: t.take("M44").and_then(|c| c.into_float()).unwrap_or_default(),
            m51: t.take("M51").and_then(|c| c.into_float()).unwrap_or_default(),
            m52: t.take("M52").and_then(|c| c.into_float()).unwrap_or_default(),
            m53: t.take("M53").and_then(|c| c.into_float()).unwrap_or_default(),
            m54: t.take("M54").and_then(|c| c.into_float()).unwrap_or_default(),
            m55: t.take("M55").and_then(|c| c.into_float()).unwrap_or_default(),
            m61: t.take("M61").and_then(|c| c.into_float()).unwrap_or_default(),
            m62: t.take("M62").and_then(|c| c.into_float()).unwrap_or_default(),
            m63: t.take("M63").and_then(|c| c.into_float()).unwrap_or_default(),
            m64: t.take("M64").and_then(|c| c.into_float()).unwrap_or_default(),
            m65: t.take("M65").and_then(|c| c.into_float()).unwrap_or_default(),
            m66: t.take("M66").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS_NODE_SET {
    pub eid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub mass: Vec<f64>,
    pub pid: Vec<i64>,
}
impl ELEMENT_MASS_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS_NODE_SET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS_PART {
    pub pid: Vec<i64>,
    pub addmass: Vec<f64>,
    pub finmass: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl ELEMENT_MASS_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            addmass: t.take("ADDMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            finmass: t.take("FINMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_MASS_PART_SET {
    pub psid: Vec<i64>,
    pub addmass: Vec<f64>,
    pub finmass: Vec<f64>,
}
impl ELEMENT_MASS_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_MASS_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            addmass: t.take("ADDMASS").and_then(|c| c.into_float()).unwrap_or_default(),
            finmass: t.take("FINMASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_NURBS_PATCH_2D_TRIMMED {
    pub npid: Vec<i64>,
    pub pid: Vec<i64>,
    pub npr: Vec<i64>,
    pub pr: Vec<i64>,
    pub nps: Vec<i64>,
    pub ps: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub wfl: Vec<i64>,
    pub elform: Vec<i64>,
    pub int: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub imass: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub idfne: Vec<i64>,
    pub rk1: Vec<f64>,
    pub rk2: Vec<f64>,
    pub rk3: Vec<f64>,
    pub rk4: Vec<f64>,
    pub rk5: Vec<f64>,
    pub rk6: Vec<f64>,
    pub rk7: Vec<f64>,
    pub rk8: Vec<f64>,
    pub sk1: Vec<f64>,
    pub sk2: Vec<f64>,
    pub sk3: Vec<f64>,
    pub sk4: Vec<f64>,
    pub sk5: Vec<f64>,
    pub sk6: Vec<f64>,
    pub sk7: Vec<f64>,
    pub sk8: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub title: Vec<String>,
    pub c1: Vec<i64>,
    pub c2: Vec<i64>,
    pub c3: Vec<i64>,
    pub c4: Vec<i64>,
    pub c5: Vec<i64>,
    pub c6: Vec<i64>,
    pub c7: Vec<i64>,
    pub c8: Vec<i64>,
}
impl ELEMENT_NURBS_PATCH_2D_TRIMMED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_NURBS_PATCH_2D_TRIMMED").unwrap());
        Self {
            npid: t.take("NPID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            nps: t.take("NPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            idfne: t.take("IDFNE").and_then(|c| c.into_int()).unwrap_or_default(),
            rk1: t.take("RK1").and_then(|c| c.into_float()).unwrap_or_default(),
            rk2: t.take("RK2").and_then(|c| c.into_float()).unwrap_or_default(),
            rk3: t.take("RK3").and_then(|c| c.into_float()).unwrap_or_default(),
            rk4: t.take("RK4").and_then(|c| c.into_float()).unwrap_or_default(),
            rk5: t.take("RK5").and_then(|c| c.into_float()).unwrap_or_default(),
            rk6: t.take("RK6").and_then(|c| c.into_float()).unwrap_or_default(),
            rk7: t.take("RK7").and_then(|c| c.into_float()).unwrap_or_default(),
            rk8: t.take("RK8").and_then(|c| c.into_float()).unwrap_or_default(),
            sk1: t.take("SK1").and_then(|c| c.into_float()).unwrap_or_default(),
            sk2: t.take("SK2").and_then(|c| c.into_float()).unwrap_or_default(),
            sk3: t.take("SK3").and_then(|c| c.into_float()).unwrap_or_default(),
            sk4: t.take("SK4").and_then(|c| c.into_float()).unwrap_or_default(),
            sk5: t.take("SK5").and_then(|c| c.into_float()).unwrap_or_default(),
            sk6: t.take("SK6").and_then(|c| c.into_float()).unwrap_or_default(),
            sk7: t.take("SK7").and_then(|c| c.into_float()).unwrap_or_default(),
            sk8: t.take("SK8").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_int()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_int()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_int()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_int()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_int()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_int()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_int()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_PLOTEL {
    pub eid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
}
impl ELEMENT_PLOTEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_PLOTEL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub sbrid: Vec<i64>,
    pub slen: Vec<f64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl ELEMENT_SEATBELT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            sbrid: t.take("SBRID").and_then(|c| c.into_int()).unwrap_or_default(),
            slen: t.take("SLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT_ACCELEROMETER {
    pub sbacid: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub igrav: Vec<i64>,
    pub intopt: Vec<i64>,
    pub mass: Vec<f64>,
}
impl ELEMENT_SEATBELT_ACCELEROMETER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT_ACCELEROMETER").unwrap());
        Self {
            sbacid: t.take("SBACID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            igrav: t.take("IGRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            intopt: t.take("INTOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT_PRETENSIONER {
    pub sbprid: Vec<i64>,
    pub sbprty: Vec<i64>,
    pub sbsid1: Vec<i64>,
    pub sbsid2: Vec<i64>,
    pub sbsid3: Vec<i64>,
    pub sbsid4: Vec<i64>,
    pub sbrid: Vec<i64>,
    pub time: Vec<f64>,
    pub ptlcid: Vec<i64>,
    pub lmtfrc: Vec<f64>,
}
impl ELEMENT_SEATBELT_PRETENSIONER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT_PRETENSIONER").unwrap());
        Self {
            sbprid: t.take("SBPRID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbprty: t.take("SBPRTY").and_then(|c| c.into_int()).unwrap_or_default(),
            sbsid1: t.take("SBSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sbsid2: t.take("SBSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sbsid3: t.take("SBSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            sbsid4: t.take("SBSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            sbrid: t.take("SBRID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            ptlcid: t.take("PTLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lmtfrc: t.take("LMTFRC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT_RETRACTOR {
    pub sbrid: Vec<i64>,
    pub sbrnid: Vec<i64>,
    pub sbid: Vec<i64>,
    pub sid1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub sid3: Vec<i64>,
    pub sid4: Vec<i64>,
    pub tdel: Vec<f64>,
    pub pull: Vec<f64>,
    pub llcid: Vec<i64>,
    pub ulcid: Vec<i64>,
    pub lfed: Vec<f64>,
}
impl ELEMENT_SEATBELT_RETRACTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT_RETRACTOR").unwrap());
        Self {
            sbrid: t.take("SBRID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbrnid: t.take("SBRNID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbid: t.take("SBID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            sid4: t.take("SID4").and_then(|c| c.into_int()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            pull: t.take("PULL").and_then(|c| c.into_float()).unwrap_or_default(),
            llcid: t.take("LLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ulcid: t.take("ULCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lfed: t.take("LFED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT_SENSOR {
    pub sbsid: Vec<i64>,
    pub sbstyp: Vec<i64>,
    pub sbsfl: Vec<i64>,
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub acc: Vec<f64>,
    pub atime: Vec<f64>,
    pub sbrid: Vec<i64>,
    pub pulrat: Vec<f64>,
    pub pultim: Vec<f64>,
    pub time: Vec<f64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub dmx: Vec<f64>,
    pub dmn: Vec<f64>,
    pub sbrid_2: Vec<i64>,
    pub pulmx: Vec<f64>,
    pub pulmn: Vec<f64>,
}
impl ELEMENT_SEATBELT_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT_SENSOR").unwrap());
        Self {
            sbsid: t.take("SBSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbstyp: t.take("SBSTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sbsfl: t.take("SBSFL").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            acc: t.take("ACC").and_then(|c| c.into_float()).unwrap_or_default(),
            atime: t.take("ATIME").and_then(|c| c.into_float()).unwrap_or_default(),
            sbrid: t.take("SBRID").and_then(|c| c.into_int()).unwrap_or_default(),
            pulrat: t.take("PULRAT").and_then(|c| c.into_float()).unwrap_or_default(),
            pultim: t.take("PULTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            dmx: t.take("DMX").and_then(|c| c.into_float()).unwrap_or_default(),
            dmn: t.take("DMN").and_then(|c| c.into_float()).unwrap_or_default(),
            sbrid_2: t.take("SBRID").and_then(|c| c.into_int()).unwrap_or_default(),
            pulmx: t.take("PULMX").and_then(|c| c.into_float()).unwrap_or_default(),
            pulmn: t.take("PULMN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SEATBELT_SLIPRING {
    pub sbsrid: Vec<i64>,
    pub sbid1: Vec<i64>,
    pub sbid2: Vec<i64>,
    pub fc: Vec<f64>,
    pub sbrnid: Vec<i64>,
    pub ltime: Vec<f64>,
    pub fcs: Vec<f64>,
    pub onid: Vec<i64>,
    pub k: Vec<f64>,
    pub funcid: Vec<i64>,
    pub direct: Vec<i64>,
    pub dc: Vec<f64>,
    pub unused: Vec<i64>,
    pub lcnffd: Vec<i64>,
    pub lcnffs: Vec<i64>,
}
impl ELEMENT_SEATBELT_SLIPRING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SEATBELT_SLIPRING").unwrap());
        Self {
            sbsrid: t.take("SBSRID").and_then(|c| c.into_int()).unwrap_or_default(),
            sbid1: t.take("SBID1").and_then(|c| c.into_int()).unwrap_or_default(),
            sbid2: t.take("SBID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            sbrnid: t.take("SBRNID").and_then(|c| c.into_int()).unwrap_or_default(),
            ltime: t.take("LTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            fcs: t.take("FCS").and_then(|c| c.into_float()).unwrap_or_default(),
            onid: t.take("ONID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            funcid: t.take("FUNCID").and_then(|c| c.into_int()).unwrap_or_default(),
            direct: t.take("DIRECT").and_then(|c| c.into_int()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            lcnffd: t.take("LCNFFD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcnffs: t.take("LCNFFS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
}
impl ELEMENT_SHELL_BETA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_SHELL_BETA_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA_COMPOSITE_LONG {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub plyid1: Vec<i64>,
}
impl ELEMENT_SHELL_BETA_COMPOSITE_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA_COMPOSITE_LONG").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid1: t.take("PLYID1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_BETA_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA_OFFSET_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_SHELL_BETA_OFFSET_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA_OFFSET_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BETA_OFFSET_COMPOSITE_LONG {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub plyid1: Vec<i64>,
}
impl ELEMENT_SHELL_BETA_OFFSET_COMPOSITE_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BETA_OFFSET_COMPOSITE_LONG").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid1: t.take("PLYID1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_BEXT_PATCH {
    pub epid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nel: Vec<i64>,
    pub wfl: Vec<i64>,
    pub form: Vec<i64>,
    pub int: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub imass: Vec<i64>,
    pub nl: Vec<i64>,
    pub shpe: Vec<i64>,
    pub pr: Vec<i64>,
    pub ps: Vec<i64>,
    pub bdry: Vec<i64>,
    pub trm: Vec<i64>,
    pub smth: Vec<i64>,
    pub b1: Vec<i64>,
    pub b2: Vec<i64>,
    pub b3: Vec<i64>,
    pub b4: Vec<i64>,
    pub b5: Vec<i64>,
    pub b6: Vec<i64>,
    pub b7: Vec<i64>,
    pub b8: Vec<i64>,
    pub s1: Vec<i64>,
    pub s2: Vec<i64>,
    pub s3: Vec<i64>,
    pub s4: Vec<i64>,
    pub s5: Vec<i64>,
    pub s6: Vec<i64>,
    pub s7: Vec<i64>,
    pub s8: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub o1: Vec<f64>,
    pub o2: Vec<f64>,
    pub o3: Vec<f64>,
    pub o4: Vec<f64>,
    pub o5: Vec<f64>,
    pub o6: Vec<f64>,
    pub o7: Vec<f64>,
    pub o8: Vec<f64>,
}
impl ELEMENT_SHELL_BEXT_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_BEXT_PATCH").unwrap());
        Self {
            epid: t.take("EPID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nel: t.take("NEL").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            shpe: t.take("SHPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            bdry: t.take("BDRY").and_then(|c| c.into_int()).unwrap_or_default(),
            trm: t.take("TRM").and_then(|c| c.into_int()).unwrap_or_default(),
            smth: t.take("SMTH").and_then(|c| c.into_int()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_int()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_int()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_int()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_int()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_int()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_int()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_int()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_int()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_int()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_int()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_int()).unwrap_or_default(),
            s5: t.take("S5").and_then(|c| c.into_int()).unwrap_or_default(),
            s6: t.take("S6").and_then(|c| c.into_int()).unwrap_or_default(),
            s7: t.take("S7").and_then(|c| c.into_int()).unwrap_or_default(),
            s8: t.take("S8").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            o1: t.take("O1").and_then(|c| c.into_float()).unwrap_or_default(),
            o2: t.take("O2").and_then(|c| c.into_float()).unwrap_or_default(),
            o3: t.take("O3").and_then(|c| c.into_float()).unwrap_or_default(),
            o4: t.take("O4").and_then(|c| c.into_float()).unwrap_or_default(),
            o5: t.take("O5").and_then(|c| c.into_float()).unwrap_or_default(),
            o6: t.take("O6").and_then(|c| c.into_float()).unwrap_or_default(),
            o7: t.take("O7").and_then(|c| c.into_float()).unwrap_or_default(),
            o8: t.take("O8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_SHELL_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_COMPOSITE_LONG {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub plyid1: Vec<i64>,
}
impl ELEMENT_SHELL_COMPOSITE_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_COMPOSITE_LONG").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid1: t.take("PLYID1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub null: Vec<i64>,
    pub null_2: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
}
impl ELEMENT_SHELL_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_int()).unwrap_or_default(),
            null_2: t.take("NULL_2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_MCID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub mcid: Vec<i64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
}
impl ELEMENT_SHELL_MCID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_MCID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_MCID_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub mcid: Vec<i64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_MCID_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_MCID_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_NURBS_PATCH {
    pub npid: Vec<i64>,
    pub pid: Vec<i64>,
    pub npr: Vec<i64>,
    pub pr: Vec<i64>,
    pub nps: Vec<i64>,
    pub ps: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub wfl: Vec<i64>,
    pub elform: Vec<i64>,
    pub int: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub imass: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub idfne: Vec<i64>,
    pub rk1: Vec<f64>,
    pub rk2: Vec<f64>,
    pub rk3: Vec<f64>,
    pub rk4: Vec<f64>,
    pub rk5: Vec<f64>,
    pub rk6: Vec<f64>,
    pub rk7: Vec<f64>,
    pub rk8: Vec<f64>,
    pub sk1: Vec<f64>,
    pub sk2: Vec<f64>,
    pub sk3: Vec<f64>,
    pub sk4: Vec<f64>,
    pub sk5: Vec<f64>,
    pub sk6: Vec<f64>,
    pub sk7: Vec<f64>,
    pub sk8: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
}
impl ELEMENT_SHELL_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_NURBS_PATCH").unwrap());
        Self {
            npid: t.take("NPID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            nps: t.take("NPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            idfne: t.take("IDFNE").and_then(|c| c.into_int()).unwrap_or_default(),
            rk1: t.take("RK1").and_then(|c| c.into_float()).unwrap_or_default(),
            rk2: t.take("RK2").and_then(|c| c.into_float()).unwrap_or_default(),
            rk3: t.take("RK3").and_then(|c| c.into_float()).unwrap_or_default(),
            rk4: t.take("RK4").and_then(|c| c.into_float()).unwrap_or_default(),
            rk5: t.take("RK5").and_then(|c| c.into_float()).unwrap_or_default(),
            rk6: t.take("RK6").and_then(|c| c.into_float()).unwrap_or_default(),
            rk7: t.take("RK7").and_then(|c| c.into_float()).unwrap_or_default(),
            rk8: t.take("RK8").and_then(|c| c.into_float()).unwrap_or_default(),
            sk1: t.take("SK1").and_then(|c| c.into_float()).unwrap_or_default(),
            sk2: t.take("SK2").and_then(|c| c.into_float()).unwrap_or_default(),
            sk3: t.take("SK3").and_then(|c| c.into_float()).unwrap_or_default(),
            sk4: t.take("SK4").and_then(|c| c.into_float()).unwrap_or_default(),
            sk5: t.take("SK5").and_then(|c| c.into_float()).unwrap_or_default(),
            sk6: t.take("SK6").and_then(|c| c.into_float()).unwrap_or_default(),
            sk7: t.take("SK7").and_then(|c| c.into_float()).unwrap_or_default(),
            sk8: t.take("SK8").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_NURBS_PATCH_TRIMMED {
    pub npid: Vec<i64>,
    pub pid: Vec<i64>,
    pub npr: Vec<i64>,
    pub pr: Vec<i64>,
    pub nps: Vec<i64>,
    pub ps: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub wfl: Vec<i64>,
    pub elform: Vec<i64>,
    pub int: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub imass: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub idfne: Vec<i64>,
    pub rk1: Vec<f64>,
    pub rk2: Vec<f64>,
    pub rk3: Vec<f64>,
    pub rk4: Vec<f64>,
    pub rk5: Vec<f64>,
    pub rk6: Vec<f64>,
    pub rk7: Vec<f64>,
    pub rk8: Vec<f64>,
    pub sk1: Vec<f64>,
    pub sk2: Vec<f64>,
    pub sk3: Vec<f64>,
    pub sk4: Vec<f64>,
    pub sk5: Vec<f64>,
    pub sk6: Vec<f64>,
    pub sk7: Vec<f64>,
    pub sk8: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub title: Vec<String>,
    pub c1: Vec<i64>,
    pub c2: Vec<i64>,
    pub c3: Vec<i64>,
    pub c4: Vec<i64>,
    pub c5: Vec<i64>,
    pub c6: Vec<i64>,
    pub c7: Vec<i64>,
    pub c8: Vec<i64>,
}
impl ELEMENT_SHELL_NURBS_PATCH_TRIMMED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_NURBS_PATCH_TRIMMED").unwrap());
        Self {
            npid: t.take("NPID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            nps: t.take("NPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            idfne: t.take("IDFNE").and_then(|c| c.into_int()).unwrap_or_default(),
            rk1: t.take("RK1").and_then(|c| c.into_float()).unwrap_or_default(),
            rk2: t.take("RK2").and_then(|c| c.into_float()).unwrap_or_default(),
            rk3: t.take("RK3").and_then(|c| c.into_float()).unwrap_or_default(),
            rk4: t.take("RK4").and_then(|c| c.into_float()).unwrap_or_default(),
            rk5: t.take("RK5").and_then(|c| c.into_float()).unwrap_or_default(),
            rk6: t.take("RK6").and_then(|c| c.into_float()).unwrap_or_default(),
            rk7: t.take("RK7").and_then(|c| c.into_float()).unwrap_or_default(),
            rk8: t.take("RK8").and_then(|c| c.into_float()).unwrap_or_default(),
            sk1: t.take("SK1").and_then(|c| c.into_float()).unwrap_or_default(),
            sk2: t.take("SK2").and_then(|c| c.into_float()).unwrap_or_default(),
            sk3: t.take("SK3").and_then(|c| c.into_float()).unwrap_or_default(),
            sk4: t.take("SK4").and_then(|c| c.into_float()).unwrap_or_default(),
            sk5: t.take("SK5").and_then(|c| c.into_float()).unwrap_or_default(),
            sk6: t.take("SK6").and_then(|c| c.into_float()).unwrap_or_default(),
            sk7: t.take("SK7").and_then(|c| c.into_float()).unwrap_or_default(),
            sk8: t.take("SK8").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_int()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_int()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_int()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_int()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_int()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_int()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_int()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_NURBS_PATCH_V3 {
    pub npeid: Vec<i64>,
    pub pid: Vec<i64>,
    pub npr: Vec<i64>,
    pub pr: Vec<i64>,
    pub nps: Vec<i64>,
    pub ps: Vec<i64>,
    pub wfl: Vec<i64>,
    pub form: Vec<i64>,
    pub int: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub imass: Vec<i64>,
    pub nl: Vec<i64>,
    pub rk1: Vec<f64>,
    pub rk2: Vec<f64>,
    pub rk3: Vec<f64>,
    pub rk4: Vec<f64>,
    pub rk5: Vec<f64>,
    pub rk6: Vec<f64>,
    pub rk7: Vec<f64>,
    pub rk8: Vec<f64>,
    pub sk1: Vec<f64>,
    pub sk2: Vec<f64>,
    pub sk3: Vec<f64>,
    pub sk4: Vec<f64>,
    pub sk5: Vec<f64>,
    pub sk6: Vec<f64>,
    pub sk7: Vec<f64>,
    pub sk8: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
    pub nel: Vec<i64>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
    pub e8: Vec<i64>,
}
impl ELEMENT_SHELL_NURBS_PATCH_V3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_NURBS_PATCH_V3").unwrap());
        Self {
            npeid: t.take("NPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            nps: t.take("NPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            rk1: t.take("RK1").and_then(|c| c.into_float()).unwrap_or_default(),
            rk2: t.take("RK2").and_then(|c| c.into_float()).unwrap_or_default(),
            rk3: t.take("RK3").and_then(|c| c.into_float()).unwrap_or_default(),
            rk4: t.take("RK4").and_then(|c| c.into_float()).unwrap_or_default(),
            rk5: t.take("RK5").and_then(|c| c.into_float()).unwrap_or_default(),
            rk6: t.take("RK6").and_then(|c| c.into_float()).unwrap_or_default(),
            rk7: t.take("RK7").and_then(|c| c.into_float()).unwrap_or_default(),
            rk8: t.take("RK8").and_then(|c| c.into_float()).unwrap_or_default(),
            sk1: t.take("SK1").and_then(|c| c.into_float()).unwrap_or_default(),
            sk2: t.take("SK2").and_then(|c| c.into_float()).unwrap_or_default(),
            sk3: t.take("SK3").and_then(|c| c.into_float()).unwrap_or_default(),
            sk4: t.take("SK4").and_then(|c| c.into_float()).unwrap_or_default(),
            sk5: t.take("SK5").and_then(|c| c.into_float()).unwrap_or_default(),
            sk6: t.take("SK6").and_then(|c| c.into_float()).unwrap_or_default(),
            sk7: t.take("SK7").and_then(|c| c.into_float()).unwrap_or_default(),
            sk8: t.take("SK8").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
            nel: t.take("NEL").and_then(|c| c.into_int()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_OFFSET_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub offset: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_SHELL_OFFSET_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_OFFSET_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_OFFSET_COMPOSITE_LONG {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub offset: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub plyid1: Vec<i64>,
}
impl ELEMENT_SHELL_OFFSET_COMPOSITE_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_OFFSET_COMPOSITE_LONG").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid1: t.take("PLYID1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_SHL4_TO_SHL8 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl ELEMENT_SHELL_SHL4_TO_SHL8 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_SHL4_TO_SHL8").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_SOURCE_SINK {
    pub nssr: Vec<i64>,
    pub nssk: Vec<i64>,
    pub pid: Vec<i64>,
}
impl ELEMENT_SHELL_SOURCE_SINK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_SOURCE_SINK").unwrap());
        Self {
            nssr: t.take("NSSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nssk: t.take("NSSK").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS_BETA {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub psi: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS_BETA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS_BETA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS_BETA_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS_BETA_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS_BETA_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS_MCID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub mcid: Vec<i64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS_MCID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS_MCID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS_MCID_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub mcid: Vec<i64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS_MCID_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS_MCID_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SHELL_THICKNESS_OFFSET {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub thic1: Vec<f64>,
    pub thic2: Vec<f64>,
    pub thic3: Vec<f64>,
    pub thic4: Vec<f64>,
    pub beta: Vec<f64>,
    pub thic5: Vec<f64>,
    pub thic6: Vec<f64>,
    pub thic7: Vec<f64>,
    pub thic8: Vec<f64>,
    pub offset: Vec<f64>,
}
impl ELEMENT_SHELL_THICKNESS_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SHELL_THICKNESS_OFFSET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            thic1: t.take("THIC1").and_then(|c| c.into_float()).unwrap_or_default(),
            thic2: t.take("THIC2").and_then(|c| c.into_float()).unwrap_or_default(),
            thic3: t.take("THIC3").and_then(|c| c.into_float()).unwrap_or_default(),
            thic4: t.take("THIC4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            thic5: t.take("THIC5").and_then(|c| c.into_float()).unwrap_or_default(),
            thic6: t.take("THIC6").and_then(|c| c.into_float()).unwrap_or_default(),
            thic7: t.take("THIC7").and_then(|c| c.into_float()).unwrap_or_default(),
            thic8: t.take("THIC8").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID__TEN_NODES_FORMAT_ {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID__TEN_NODES_FORMAT_ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID (ten nodes format)").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_DOF__TEN_NODES_FORMAT_ {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_DOF__TEN_NODES_FORMAT_ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_DOF (ten nodes format)").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H20 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
}
impl ELEMENT_SOLID_H20 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H20").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H20_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_H20_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H20_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H27 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub n21: Vec<i64>,
    pub n22: Vec<i64>,
    pub n23: Vec<i64>,
    pub n24: Vec<i64>,
    pub n25: Vec<i64>,
    pub n26: Vec<i64>,
    pub n27: Vec<i64>,
}
impl ELEMENT_SOLID_H27 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H27").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            n21: t.take("N21").and_then(|c| c.into_int()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_int()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_int()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_int()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_int()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_int()).unwrap_or_default(),
            n27: t.take("N27").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H27_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub n21: Vec<i64>,
    pub n22: Vec<i64>,
    pub n23: Vec<i64>,
    pub n24: Vec<i64>,
    pub n25: Vec<i64>,
    pub n26: Vec<i64>,
    pub n27: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_H27_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H27_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            n21: t.take("N21").and_then(|c| c.into_int()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_int()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_int()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_int()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_int()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_int()).unwrap_or_default(),
            n27: t.take("N27").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H64 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub n21: Vec<i64>,
    pub n22: Vec<i64>,
    pub n23: Vec<i64>,
    pub n24: Vec<i64>,
    pub n25: Vec<i64>,
    pub n26: Vec<i64>,
    pub n27: Vec<i64>,
    pub n28: Vec<i64>,
    pub n29: Vec<i64>,
    pub n30: Vec<i64>,
    pub n31: Vec<i64>,
    pub n32: Vec<i64>,
    pub n33: Vec<i64>,
    pub n34: Vec<i64>,
    pub n35: Vec<i64>,
    pub n36: Vec<i64>,
    pub n37: Vec<i64>,
    pub n38: Vec<i64>,
    pub n39: Vec<i64>,
    pub n40: Vec<i64>,
    pub n41: Vec<i64>,
    pub n42: Vec<i64>,
    pub n43: Vec<i64>,
    pub n44: Vec<i64>,
    pub n45: Vec<i64>,
    pub n46: Vec<i64>,
    pub n47: Vec<i64>,
    pub n48: Vec<i64>,
    pub n49: Vec<i64>,
    pub n50: Vec<i64>,
    pub n51: Vec<i64>,
    pub n52: Vec<i64>,
    pub n53: Vec<i64>,
    pub n54: Vec<i64>,
    pub n55: Vec<i64>,
    pub n56: Vec<i64>,
    pub n57: Vec<i64>,
    pub n58: Vec<i64>,
    pub n59: Vec<i64>,
    pub n50_2: Vec<i64>,
    pub n61: Vec<i64>,
    pub n62: Vec<i64>,
    pub n63: Vec<i64>,
    pub n64: Vec<i64>,
}
impl ELEMENT_SOLID_H64 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H64").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            n21: t.take("N21").and_then(|c| c.into_int()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_int()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_int()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_int()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_int()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_int()).unwrap_or_default(),
            n27: t.take("N27").and_then(|c| c.into_int()).unwrap_or_default(),
            n28: t.take("N28").and_then(|c| c.into_int()).unwrap_or_default(),
            n29: t.take("N29").and_then(|c| c.into_int()).unwrap_or_default(),
            n30: t.take("N30").and_then(|c| c.into_int()).unwrap_or_default(),
            n31: t.take("N31").and_then(|c| c.into_int()).unwrap_or_default(),
            n32: t.take("N32").and_then(|c| c.into_int()).unwrap_or_default(),
            n33: t.take("N33").and_then(|c| c.into_int()).unwrap_or_default(),
            n34: t.take("N34").and_then(|c| c.into_int()).unwrap_or_default(),
            n35: t.take("N35").and_then(|c| c.into_int()).unwrap_or_default(),
            n36: t.take("N36").and_then(|c| c.into_int()).unwrap_or_default(),
            n37: t.take("N37").and_then(|c| c.into_int()).unwrap_or_default(),
            n38: t.take("N38").and_then(|c| c.into_int()).unwrap_or_default(),
            n39: t.take("N39").and_then(|c| c.into_int()).unwrap_or_default(),
            n40: t.take("N40").and_then(|c| c.into_int()).unwrap_or_default(),
            n41: t.take("N41").and_then(|c| c.into_int()).unwrap_or_default(),
            n42: t.take("N42").and_then(|c| c.into_int()).unwrap_or_default(),
            n43: t.take("N43").and_then(|c| c.into_int()).unwrap_or_default(),
            n44: t.take("N44").and_then(|c| c.into_int()).unwrap_or_default(),
            n45: t.take("N45").and_then(|c| c.into_int()).unwrap_or_default(),
            n46: t.take("N46").and_then(|c| c.into_int()).unwrap_or_default(),
            n47: t.take("N47").and_then(|c| c.into_int()).unwrap_or_default(),
            n48: t.take("N48").and_then(|c| c.into_int()).unwrap_or_default(),
            n49: t.take("N49").and_then(|c| c.into_int()).unwrap_or_default(),
            n50: t.take("N50").and_then(|c| c.into_int()).unwrap_or_default(),
            n51: t.take("N51").and_then(|c| c.into_int()).unwrap_or_default(),
            n52: t.take("N52").and_then(|c| c.into_int()).unwrap_or_default(),
            n53: t.take("N53").and_then(|c| c.into_int()).unwrap_or_default(),
            n54: t.take("N54").and_then(|c| c.into_int()).unwrap_or_default(),
            n55: t.take("N55").and_then(|c| c.into_int()).unwrap_or_default(),
            n56: t.take("N56").and_then(|c| c.into_int()).unwrap_or_default(),
            n57: t.take("N57").and_then(|c| c.into_int()).unwrap_or_default(),
            n58: t.take("N58").and_then(|c| c.into_int()).unwrap_or_default(),
            n59: t.take("N59").and_then(|c| c.into_int()).unwrap_or_default(),
            n50_2: t.take("N50").and_then(|c| c.into_int()).unwrap_or_default(),
            n61: t.take("N61").and_then(|c| c.into_int()).unwrap_or_default(),
            n62: t.take("N62").and_then(|c| c.into_int()).unwrap_or_default(),
            n63: t.take("N63").and_then(|c| c.into_int()).unwrap_or_default(),
            n64: t.take("N64").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH20 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH20_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH20_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20_ORTHO {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_H8TOH20_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20_ORTHO").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20_ORTHO_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH20_ORTHO_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20_ORTHO_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH20_ORTHO_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_H8TOH20_ORTHO_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH20_ORTHO_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH27 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH27_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH27_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27_ORTHO {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_H8TOH27_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27_ORTHO").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27_ORTHO_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH27_ORTHO_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27_ORTHO_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH27_ORTHO_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_H8TOH27_ORTHO_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH27_ORTHO_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH64 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH64 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH64").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_H8TOH64_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_H8TOH64_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_H8TOH64_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_NURBS_PATCH {
    pub npeid: Vec<i64>,
    pub pid: Vec<i64>,
    pub npr: Vec<i64>,
    pub pr: Vec<i64>,
    pub nps: Vec<i64>,
    pub ps: Vec<i64>,
    pub npt: Vec<i64>,
    pub pt: Vec<i64>,
    pub wfl: Vec<i64>,
    pub nisr: Vec<i64>,
    pub niss: Vec<i64>,
    pub nist: Vec<i64>,
    pub imass: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub idfne: Vec<i64>,
    pub rk1: Vec<f64>,
    pub rk2: Vec<f64>,
    pub rk3: Vec<f64>,
    pub rk4: Vec<f64>,
    pub rk5: Vec<f64>,
    pub rk6: Vec<f64>,
    pub rk7: Vec<f64>,
    pub rk8: Vec<f64>,
    pub sk1: Vec<f64>,
    pub sk2: Vec<f64>,
    pub sk3: Vec<f64>,
    pub sk4: Vec<f64>,
    pub sk5: Vec<f64>,
    pub sk6: Vec<f64>,
    pub sk7: Vec<f64>,
    pub sk8: Vec<f64>,
    pub tk1: Vec<f64>,
    pub tk2: Vec<f64>,
    pub tk3: Vec<f64>,
    pub tk4: Vec<f64>,
    pub tk5: Vec<f64>,
    pub tk6: Vec<f64>,
    pub tk7: Vec<f64>,
    pub tk8: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub w4: Vec<f64>,
    pub w5: Vec<f64>,
    pub w6: Vec<f64>,
    pub w7: Vec<f64>,
    pub w8: Vec<f64>,
}
impl ELEMENT_SOLID_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_NURBS_PATCH").unwrap());
        Self {
            npeid: t.take("NPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            npr: t.take("NPR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            nps: t.take("NPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            npt: t.take("NPT").and_then(|c| c.into_int()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_int()).unwrap_or_default(),
            wfl: t.take("WFL").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_int()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_int()).unwrap_or_default(),
            nist: t.take("NIST").and_then(|c| c.into_int()).unwrap_or_default(),
            imass: t.take("IMASS").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            idfne: t.take("IDFNE").and_then(|c| c.into_int()).unwrap_or_default(),
            rk1: t.take("RK1").and_then(|c| c.into_float()).unwrap_or_default(),
            rk2: t.take("RK2").and_then(|c| c.into_float()).unwrap_or_default(),
            rk3: t.take("RK3").and_then(|c| c.into_float()).unwrap_or_default(),
            rk4: t.take("RK4").and_then(|c| c.into_float()).unwrap_or_default(),
            rk5: t.take("RK5").and_then(|c| c.into_float()).unwrap_or_default(),
            rk6: t.take("RK6").and_then(|c| c.into_float()).unwrap_or_default(),
            rk7: t.take("RK7").and_then(|c| c.into_float()).unwrap_or_default(),
            rk8: t.take("RK8").and_then(|c| c.into_float()).unwrap_or_default(),
            sk1: t.take("SK1").and_then(|c| c.into_float()).unwrap_or_default(),
            sk2: t.take("SK2").and_then(|c| c.into_float()).unwrap_or_default(),
            sk3: t.take("SK3").and_then(|c| c.into_float()).unwrap_or_default(),
            sk4: t.take("SK4").and_then(|c| c.into_float()).unwrap_or_default(),
            sk5: t.take("SK5").and_then(|c| c.into_float()).unwrap_or_default(),
            sk6: t.take("SK6").and_then(|c| c.into_float()).unwrap_or_default(),
            sk7: t.take("SK7").and_then(|c| c.into_float()).unwrap_or_default(),
            sk8: t.take("SK8").and_then(|c| c.into_float()).unwrap_or_default(),
            tk1: t.take("TK1").and_then(|c| c.into_float()).unwrap_or_default(),
            tk2: t.take("TK2").and_then(|c| c.into_float()).unwrap_or_default(),
            tk3: t.take("TK3").and_then(|c| c.into_float()).unwrap_or_default(),
            tk4: t.take("TK4").and_then(|c| c.into_float()).unwrap_or_default(),
            tk5: t.take("TK5").and_then(|c| c.into_float()).unwrap_or_default(),
            tk6: t.take("TK6").and_then(|c| c.into_float()).unwrap_or_default(),
            tk7: t.take("TK7").and_then(|c| c.into_float()).unwrap_or_default(),
            tk8: t.take("TK8").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            w4: t.take("W4").and_then(|c| c.into_float()).unwrap_or_default(),
            w5: t.take("W5").and_then(|c| c.into_float()).unwrap_or_default(),
            w6: t.take("W6").and_then(|c| c.into_float()).unwrap_or_default(),
            w7: t.take("W7").and_then(|c| c.into_float()).unwrap_or_default(),
            w8: t.take("W8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_ORTHO {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_ORTHO").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_ORTHO__TEN_NODES_FORMAT_ {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_ORTHO__TEN_NODES_FORMAT_ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_ORTHO (ten nodes format)").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_ORTHO_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_ORTHO_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_ORTHO_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_ORTHO_DOF__TEN_NODES_FORMAT_ {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_ORTHO_DOF__TEN_NODES_FORMAT_ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_ORTHO_DOF (ten nodes format)").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_P21 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub n21: Vec<i64>,
}
impl ELEMENT_SOLID_P21 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_P21").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            n21: t.take("N21").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_P40 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
    pub n21: Vec<i64>,
    pub n22: Vec<i64>,
    pub n23: Vec<i64>,
    pub n24: Vec<i64>,
    pub n25: Vec<i64>,
    pub n26: Vec<i64>,
    pub n27: Vec<i64>,
    pub n28: Vec<i64>,
    pub n29: Vec<i64>,
    pub n30: Vec<i64>,
    pub n31: Vec<i64>,
    pub n32: Vec<i64>,
    pub n33: Vec<i64>,
    pub n34: Vec<i64>,
    pub n35: Vec<i64>,
    pub n36: Vec<i64>,
    pub n37: Vec<i64>,
    pub n38: Vec<i64>,
    pub n39: Vec<i64>,
    pub n40: Vec<i64>,
}
impl ELEMENT_SOLID_P40 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_P40").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
            n21: t.take("N21").and_then(|c| c.into_int()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_int()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_int()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_int()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_int()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_int()).unwrap_or_default(),
            n27: t.take("N27").and_then(|c| c.into_int()).unwrap_or_default(),
            n28: t.take("N28").and_then(|c| c.into_int()).unwrap_or_default(),
            n29: t.take("N29").and_then(|c| c.into_int()).unwrap_or_default(),
            n30: t.take("N30").and_then(|c| c.into_int()).unwrap_or_default(),
            n31: t.take("N31").and_then(|c| c.into_int()).unwrap_or_default(),
            n32: t.take("N32").and_then(|c| c.into_int()).unwrap_or_default(),
            n33: t.take("N33").and_then(|c| c.into_int()).unwrap_or_default(),
            n34: t.take("N34").and_then(|c| c.into_int()).unwrap_or_default(),
            n35: t.take("N35").and_then(|c| c.into_int()).unwrap_or_default(),
            n36: t.take("N36").and_then(|c| c.into_int()).unwrap_or_default(),
            n37: t.take("N37").and_then(|c| c.into_int()).unwrap_or_default(),
            n38: t.take("N38").and_then(|c| c.into_int()).unwrap_or_default(),
            n39: t.take("N39").and_then(|c| c.into_int()).unwrap_or_default(),
            n40: t.take("N40").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_P6TOP21 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_P6TOP21 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_P6TOP21").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_P6TOP21_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_P6TOP21_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_P6TOP21_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_PERI {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl ELEMENT_SOLID_PERI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_PERI").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T15 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
}
impl ELEMENT_SOLID_T15 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T15").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T20 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub n11: Vec<i64>,
    pub n12: Vec<i64>,
    pub n13: Vec<i64>,
    pub n14: Vec<i64>,
    pub n15: Vec<i64>,
    pub n16: Vec<i64>,
    pub n17: Vec<i64>,
    pub n18: Vec<i64>,
    pub n19: Vec<i64>,
    pub n20: Vec<i64>,
}
impl ELEMENT_SOLID_T20 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T20").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_int()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_int()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_int()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_int()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_int()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_int()).unwrap_or_default(),
            n17: t.take("N17").and_then(|c| c.into_int()).unwrap_or_default(),
            n18: t.take("N18").and_then(|c| c.into_int()).unwrap_or_default(),
            n19: t.take("N19").and_then(|c| c.into_int()).unwrap_or_default(),
            n20: t.take("N20").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T4TOT10 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_T4TOT10 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T4TOT10").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T4TOT10_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
}
impl ELEMENT_SOLID_T4TOT10_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T4TOT10_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T4TOT15 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_T4TOT15 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T4TOT15").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_T4TOT15_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_T4TOT15_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_T4TOT15_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_TET4TOTET10 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_SOLID_TET4TOTET10 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_TET4TOTET10").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_TET4TOTET10_DOF_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_TET4TOTET10_DOF_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_TET4TOTET10_DOF_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_TET4TOTET10_FORMAT2 {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub n9: Vec<i64>,
    pub n10: Vec<i64>,
}
impl ELEMENT_SOLID_TET4TOTET10_FORMAT2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_TET4TOTET10_FORMAT2").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            n9: t.take("N9").and_then(|c| c.into_int()).unwrap_or_default(),
            n10: t.take("N10").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_TET4TOTET10_ORTHO {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl ELEMENT_SOLID_TET4TOTET10_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_TET4TOTET10_ORTHO").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SOLID_TET4TOTET10_ORTHO_DOF {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f0: Vec<i64>,
    pub f8: Vec<i64>,
    pub ns1: Vec<i64>,
    pub ns2: Vec<i64>,
    pub ns3: Vec<i64>,
    pub ns4: Vec<i64>,
    pub ns5: Vec<i64>,
    pub ns6: Vec<i64>,
    pub ns7: Vec<i64>,
    pub ns8: Vec<i64>,
}
impl ELEMENT_SOLID_TET4TOTET10_ORTHO_DOF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SOLID_TET4TOTET10_ORTHO_DOF").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f8: t.take("f8").and_then(|c| c.into_int()).unwrap_or_default(),
            ns1: t.take("NS1").and_then(|c| c.into_int()).unwrap_or_default(),
            ns2: t.take("NS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ns3: t.take("NS3").and_then(|c| c.into_int()).unwrap_or_default(),
            ns4: t.take("NS4").and_then(|c| c.into_int()).unwrap_or_default(),
            ns5: t.take("NS5").and_then(|c| c.into_int()).unwrap_or_default(),
            ns6: t.take("NS6").and_then(|c| c.into_int()).unwrap_or_default(),
            ns7: t.take("NS7").and_then(|c| c.into_int()).unwrap_or_default(),
            ns8: t.take("NS8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_SPH {
    pub nid: Vec<i64>,
    pub pid: Vec<i64>,
    pub mass: Vec<f64>,
}
impl ELEMENT_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_SPH").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TRIM {
    pub psid: Vec<i64>,
}
impl ELEMENT_TRIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TRIM").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TSHELL {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl ELEMENT_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TSHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TSHELL_BETA {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub beta: Vec<f64>,
}
impl ELEMENT_TSHELL_BETA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TSHELL_BETA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TSHELL_BETA_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub beta: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_TSHELL_BETA_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TSHELL_BETA_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TSHELL_COMPOSITE {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_TSHELL_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TSHELL_COMPOSITE").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ELEMENT_TSHELL_COMPOSITE_BETA {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub beta: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub unused: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
}
impl ELEMENT_TSHELL_COMPOSITE_BETA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ELEMENT_TSHELL_COMPOSITE_BETA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_2DAXI {
    pub pid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub starssid: Vec<i64>,
    pub endssid: Vec<i64>,
    pub numsec: Vec<i64>,
}
impl EM_2DAXI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_2DAXI").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            starssid: t.take("STARSSID").and_then(|c| c.into_int()).unwrap_or_default(),
            endssid: t.take("ENDSSID").and_then(|c| c.into_int()).unwrap_or_default(),
            numsec: t.take("NUMSEC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_BATTERY_RANDLES {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub rdlarea: Vec<i64>,
    pub ccppart: Vec<i64>,
    pub ccnpart: Vec<i64>,
    pub seppart: Vec<i64>,
    pub poselpart: Vec<i64>,
    pub negelpart: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub temp: Vec<f64>,
    pub frtherm: Vec<i64>,
    pub r0toth: Vec<i64>,
    pub dudt: Vec<f64>,
    pub tempu: Vec<i64>,
    pub usesocs: Vec<i64>,
    pub tausocs: Vec<f64>,
    pub sicslcid: Vec<i64>,
}
impl EM_BATTERY_RANDLES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_BATTERY_RANDLES").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rdlarea: t.take("RDLAREA").and_then(|c| c.into_int()).unwrap_or_default(),
            ccppart: t.take("CCPPART").and_then(|c| c.into_int()).unwrap_or_default(),
            ccnpart: t.take("CCNPART").and_then(|c| c.into_int()).unwrap_or_default(),
            seppart: t.take("SEPPART").and_then(|c| c.into_int()).unwrap_or_default(),
            poselpart: t.take("POSELPART").and_then(|c| c.into_int()).unwrap_or_default(),
            negelpart: t.take("NEGELPART").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            frtherm: t.take("FRTHERM").and_then(|c| c.into_int()).unwrap_or_default(),
            r0toth: t.take("R0TOTH").and_then(|c| c.into_int()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            tempu: t.take("TEMPU").and_then(|c| c.into_int()).unwrap_or_default(),
            usesocs: t.take("USESOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tausocs: t.take("TAUSOCS").and_then(|c| c.into_float()).unwrap_or_default(),
            sicslcid: t.take("SICSLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_BOUNDARY {
    pub ssid: Vec<i64>,
    pub btype: Vec<i64>,
}
impl EM_BOUNDARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_BOUNDARY").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            btype: t.take("BTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_BOUNDARY_PRESCRIBED {
    pub bpid: Vec<i64>,
    pub bptype: Vec<i64>,
    pub settype: Vec<i64>,
    pub setid: Vec<i64>,
    pub val: Vec<f64>,
    pub lcid: Vec<i64>,
    pub birtht: Vec<f64>,
    pub deatht: Vec<f64>,
}
impl EM_BOUNDARY_PRESCRIBED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_BOUNDARY_PRESCRIBED").unwrap());
        Self {
            bpid: t.take("BPID").and_then(|c| c.into_int()).unwrap_or_default(),
            bptype: t.take("BPTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            settype: t.take("SETTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            birtht: t.take("BIRTHT").and_then(|c| c.into_float()).unwrap_or_default(),
            deatht: t.take("DEATHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CIRCUIT {
    pub circid: Vec<i64>,
    pub circtyp: Vec<i64>,
    pub lcid: Vec<i64>,
    pub r_f: Vec<f64>,
    pub l_a: Vec<f64>,
    pub c_t0: Vec<f64>,
    pub v0: Vec<f64>,
    pub t0: Vec<f64>,
    pub sidcurr: Vec<i64>,
    pub sidvin: Vec<i64>,
    pub sidvout: Vec<i64>,
    pub partid: Vec<i64>,
}
impl EM_CIRCUIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CIRCUIT").unwrap());
        Self {
            circid: t.take("CIRCID").and_then(|c| c.into_int()).unwrap_or_default(),
            circtyp: t.take("CIRCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            r_f: t.take("R/F").and_then(|c| c.into_float()).unwrap_or_default(),
            l_a: t.take("L/A").and_then(|c| c.into_float()).unwrap_or_default(),
            c_t0: t.take("C/t0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            sidcurr: t.take("SIDCURR").and_then(|c| c.into_int()).unwrap_or_default(),
            sidvin: t.take("SIDVIN").and_then(|c| c.into_int()).unwrap_or_default(),
            sidvout: t.take("SIDVOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            partid: t.take("PARTID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CIRCUIT_CONNECT {
    pub conid: Vec<i64>,
    pub contype: Vec<i64>,
    pub circ1: Vec<i64>,
    pub circ2: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
}
impl EM_CIRCUIT_CONNECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CIRCUIT_CONNECT").unwrap());
        Self {
            conid: t.take("CONID").and_then(|c| c.into_int()).unwrap_or_default(),
            contype: t.take("CONTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            circ1: t.take("CIRC1").and_then(|c| c.into_int()).unwrap_or_default(),
            circ2: t.take("CIRC2").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CIRCUIT_ROGO {
    pub rogoid: Vec<i64>,
    pub setid: Vec<i64>,
    pub settype: Vec<i64>,
    pub curtyp: Vec<i64>,
}
impl EM_CIRCUIT_ROGO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CIRCUIT_ROGO").unwrap());
        Self {
            rogoid: t.take("ROGOID").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            settype: t.take("SETTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            curtyp: t.take("CURTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CIRCUIT_SOURCE {
    pub circid: Vec<i64>,
    pub circtyp: Vec<i64>,
    pub lcid: Vec<i64>,
    pub r_f: Vec<f64>,
    pub l_a: Vec<f64>,
    pub c_t0: Vec<f64>,
    pub v0: Vec<f64>,
    pub t0: Vec<f64>,
    pub sidcurr: Vec<i64>,
    pub sidvin: Vec<i64>,
    pub sidvout: Vec<i64>,
    pub partid: Vec<i64>,
}
impl EM_CIRCUIT_SOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CIRCUIT_SOURCE").unwrap());
        Self {
            circid: t.take("CIRCID").and_then(|c| c.into_int()).unwrap_or_default(),
            circtyp: t.take("CIRCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            r_f: t.take("R/F").and_then(|c| c.into_float()).unwrap_or_default(),
            l_a: t.take("L/A").and_then(|c| c.into_float()).unwrap_or_default(),
            c_t0: t.take("C/t0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            sidcurr: t.take("SIDCURR").and_then(|c| c.into_int()).unwrap_or_default(),
            sidvin: t.take("SIDVIN").and_then(|c| c.into_int()).unwrap_or_default(),
            sidvout: t.take("SIDVOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            partid: t.take("PARTID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTACT {
    pub contid: Vec<i64>,
    pub conttype: Vec<i64>,
    pub psidm: Vec<i64>,
    pub psids: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub d0: Vec<f64>,
}
impl EM_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTACT").unwrap());
        Self {
            contid: t.take("CONTID").and_then(|c| c.into_int()).unwrap_or_default(),
            conttype: t.take("CONTTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            psidm: t.take("PSIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            psids: t.take("PSIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTACT_RESISTANCE {
    pub crid: Vec<i64>,
    pub contid: Vec<i64>,
    pub ctype: Vec<i64>,
    pub unused: Vec<i64>,
    pub jhrtype: Vec<i64>,
    pub dfid: Vec<i64>,
}
impl EM_CONTACT_RESISTANCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTACT_RESISTANCE").unwrap());
        Self {
            crid: t.take("CRID").and_then(|c| c.into_int()).unwrap_or_default(),
            contid: t.take("CONTID").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            jhrtype: t.take("JHRTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dfid: t.take("DFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTACT_SUBDOM {
    pub sdtype: Vec<i64>,
    pub mvtype: Vec<i64>,
    pub lcidx_nid: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub r: Vec<f64>,
    pub pminx: Vec<f64>,
    pub pminy: Vec<f64>,
    pub pminz: Vec<f64>,
    pub pmaxx: Vec<f64>,
    pub pmaxy: Vec<f64>,
    pub pmaxz: Vec<f64>,
}
impl EM_CONTACT_SUBDOM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTACT_SUBDOM").unwrap());
        Self {
            sdtype: t.take("SDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mvtype: t.take("MVTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx_nid: t.take("LCIDX/NID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            pminx: t.take("PMINX").and_then(|c| c.into_float()).unwrap_or_default(),
            pminy: t.take("PMINY").and_then(|c| c.into_float()).unwrap_or_default(),
            pminz: t.take("PMINZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxx: t.take("PMAXX").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxy: t.take("PMAXY").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxz: t.take("PMAXZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL {
    pub emsol: Vec<i64>,
    pub numls: Vec<i64>,
    pub macrodt: Vec<f64>,
    pub dimtype: Vec<i64>,
    pub nperio: Vec<i64>,
    pub f: Vec<i64>,
    pub ncylfem: Vec<i64>,
    pub ncylbem: Vec<i64>,
}
impl EM_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL").unwrap());
        Self {
            emsol: t.take("EMSOL").and_then(|c| c.into_int()).unwrap_or_default(),
            numls: t.take("NUMLS").and_then(|c| c.into_int()).unwrap_or_default(),
            macrodt: t.take("MACRODT").and_then(|c| c.into_float()).unwrap_or_default(),
            dimtype: t.take("DIMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nperio: t.take("NPERIO").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ncylfem: t.take("NCYLFEM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncylbem: t.take("NCYLBEM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_CONTACT {
    pub emct: Vec<i64>,
    pub cconly: Vec<i64>,
    pub ctype: Vec<i64>,
    pub cotype: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub d0: Vec<f64>,
}
impl EM_CONTROL_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_CONTACT").unwrap());
        Self {
            emct: t.take("EMCT").and_then(|c| c.into_int()).unwrap_or_default(),
            cconly: t.take("CCONLY").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cotype: t.take("COTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_COUPLING {
    pub thcoupl: Vec<i64>,
    pub smcoupl: Vec<i64>,
    pub thlcid: Vec<i64>,
    pub smlcid: Vec<i64>,
    pub thcplfl: Vec<i64>,
    pub smcplfl: Vec<i64>,
    pub cflag: Vec<i64>,
    pub nflag: Vec<i64>,
    pub smmod: Vec<i64>,
    pub dfx: Vec<i64>,
    pub dfy: Vec<i64>,
    pub dfz: Vec<i64>,
}
impl EM_CONTROL_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_COUPLING").unwrap());
        Self {
            thcoupl: t.take("THCOUPL").and_then(|c| c.into_int()).unwrap_or_default(),
            smcoupl: t.take("SMCOUPL").and_then(|c| c.into_int()).unwrap_or_default(),
            thlcid: t.take("THLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            smlcid: t.take("SMLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            thcplfl: t.take("THCPLFL").and_then(|c| c.into_int()).unwrap_or_default(),
            smcplfl: t.take("SMCPLFL").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nflag: t.take("NFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            smmod: t.take("SMMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            dfx: t.take("DFX").and_then(|c| c.into_int()).unwrap_or_default(),
            dfy: t.take("DFY").and_then(|c| c.into_int()).unwrap_or_default(),
            dfz: t.take("DFZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_EP {
    pub solvetype: Vec<i64>,
    pub numspliti: Vec<i64>,
    pub actusigma: Vec<i64>,
}
impl EM_CONTROL_EP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_EP").unwrap());
        Self {
            solvetype: t.take("SOLVETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            numspliti: t.take("NUMSPLITI").and_then(|c| c.into_int()).unwrap_or_default(),
            actusigma: t.take("ACTUSIGMA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_EROSION {
    pub ectrl: Vec<i64>,
}
impl EM_CONTROL_EROSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_EROSION").unwrap());
        Self {
            ectrl: t.take("ECTRL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_MAGENT {
    pub mcomp: Vec<i64>,
}
impl EM_CONTROL_MAGENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_MAGENT").unwrap());
        Self {
            mcomp: t.take("MCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_SOLUTION {
    pub ncylfem: Vec<i64>,
    pub ncylbem: Vec<i64>,
    pub autofem: Vec<i64>,
    pub autobem: Vec<i64>,
    pub tol1fem: Vec<f64>,
    pub tol2fem: Vec<f64>,
    pub tol1bem: Vec<f64>,
    pub tol2bem: Vec<f64>,
}
impl EM_CONTROL_SOLUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_SOLUTION").unwrap());
        Self {
            ncylfem: t.take("NCYLFEM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncylbem: t.take("NCYLBEM").and_then(|c| c.into_int()).unwrap_or_default(),
            autofem: t.take("AUTOFEM").and_then(|c| c.into_int()).unwrap_or_default(),
            autobem: t.take("AUTOBEM").and_then(|c| c.into_int()).unwrap_or_default(),
            tol1fem: t.take("TOL1FEM").and_then(|c| c.into_float()).unwrap_or_default(),
            tol2fem: t.take("TOL2FEM").and_then(|c| c.into_float()).unwrap_or_default(),
            tol1bem: t.take("TOL1BEM").and_then(|c| c.into_float()).unwrap_or_default(),
            tol2bem: t.take("TOL2BEM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_SWITCH {
    pub lcid: Vec<i64>,
    pub femcomp: Vec<i64>,
    pub bemcomp: Vec<i64>,
}
impl EM_CONTROL_SWITCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_SWITCH").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            femcomp: t.take("FEMCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            bemcomp: t.take("BEMCOMP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_SWITCH_CONTACT {
    pub lcid: Vec<i64>,
    pub ncylfem: Vec<i64>,
    pub ncylfem_2: Vec<i64>,
}
impl EM_CONTROL_SWITCH_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_SWITCH_CONTACT").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ncylfem: t.take("NCYLFEM").and_then(|c| c.into_int()).unwrap_or_default(),
            ncylfem_2: t.take("NCYLFEM_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_CONTROL_TIMESTEP {
    pub tstype: Vec<i64>,
    pub dtcons: Vec<f64>,
    pub lcid: Vec<i64>,
    pub factor: Vec<f64>,
    pub tsmin: Vec<f64>,
    pub tsmas: Vec<f64>,
    pub rlcsf: Vec<i64>,
    pub mecats: Vec<i64>,
}
impl EM_CONTROL_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_CONTROL_TIMESTEP").unwrap());
        Self {
            tstype: t.take("TSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dtcons: t.take("DTCONS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            factor: t.take("FACTOR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmin: t.take("TSMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmas: t.take("TSMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            rlcsf: t.take("RLCSF").and_then(|c| c.into_int()).unwrap_or_default(),
            mecats: t.take("MECATS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_CIRCUIT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl EM_DATABASE_CIRCUIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_CIRCUIT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_CIRCUIT0D {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl EM_DATABASE_CIRCUIT0D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_CIRCUIT0D").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_ELOUT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub elsid: Vec<i64>,
}
impl EM_DATABASE_ELOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_ELOUT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            elsid: t.take("ELSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_FIELDLINE {
    pub flid: Vec<i64>,
    pub psid: Vec<i64>,
    pub dtout: Vec<f64>,
    pub npoint: Vec<i64>,
    pub integ: Vec<i64>,
    pub h: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub tolabs: Vec<f64>,
    pub tolrel: Vec<f64>,
    pub btype: Vec<i64>,
}
impl EM_DATABASE_FIELDLINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_FIELDLINE").unwrap());
        Self {
            flid: t.take("FLID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            npoint: t.take("NPOINT").and_then(|c| c.into_int()).unwrap_or_default(),
            integ: t.take("INTEG").and_then(|c| c.into_int()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            tolabs: t.take("TOLABS").and_then(|c| c.into_float()).unwrap_or_default(),
            tolrel: t.take("TOLREL").and_then(|c| c.into_float()).unwrap_or_default(),
            btype: t.take("BTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_GLOBALENERGY {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl EM_DATABASE_GLOBALENERGY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_GLOBALENERGY").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_NODOUT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub nsid: Vec<i64>,
}
impl EM_DATABASE_NODOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_NODOUT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_PARTDATA {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl EM_DATABASE_PARTDATA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_PARTDATA").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_POINTOUT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub psid: Vec<i64>,
}
impl EM_DATABASE_POINTOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_POINTOUT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_ROGO {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl EM_DATABASE_ROGO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_ROGO").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_DATABASE_TIMESTEP {
    pub outlv: Vec<i64>,
}
impl EM_DATABASE_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_DATABASE_TIMESTEP").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EOS_BURGESS {
    pub eosid: Vec<i64>,
    pub v0: Vec<f64>,
    pub gamma: Vec<f64>,
    pub theta: Vec<f64>,
    pub lf: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub k: Vec<f64>,
    pub expon: Vec<i64>,
    pub lgtunit: Vec<f64>,
    pub timunit: Vec<f64>,
    pub temuni: Vec<i64>,
    pub adjust: Vec<i64>,
}
impl EM_EOS_BURGESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EOS_BURGESS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lf: t.take("LF").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_int()).unwrap_or_default(),
            lgtunit: t.take("LGTUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            timunit: t.take("TIMUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            temuni: t.take("TEMUNI").and_then(|c| c.into_int()).unwrap_or_default(),
            adjust: t.take("ADJUST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EOS_MEADON {
    pub eosid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub temuni: Vec<i64>,
    pub v0: Vec<f64>,
    pub gamma: Vec<f64>,
    pub expon: Vec<i64>,
    pub lgtunit: Vec<f64>,
    pub timunit: Vec<f64>,
    pub adjust: Vec<i64>,
}
impl EM_EOS_MEADON {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EOS_MEADON").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            temuni: t.take("TEMUNI").and_then(|c| c.into_int()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_int()).unwrap_or_default(),
            lgtunit: t.take("LGTUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            timunit: t.take("TIMUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            adjust: t.take("ADJUST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EOS_PERMEABILITY {
    pub eosid: Vec<i64>,
    pub eostype: Vec<i64>,
    pub lcid: Vec<i64>,
}
impl EM_EOS_PERMEABILITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EOS_PERMEABILITY").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eostype: t.take("EOSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EOS_TABULATED1 {
    pub eosid: Vec<i64>,
    pub lcid: Vec<i64>,
}
impl EM_EOS_TABULATED1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EOS_TABULATED1").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EOS_TABULATED2 {
    pub eosid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub iflag: Vec<i64>,
}
impl EM_EOS_TABULATED2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EOS_TABULATED2").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_DEFINEFUNCTION {
    pub mid: Vec<i64>,
    pub nstate: Vec<i64>,
    pub fswitch: Vec<i64>,
    pub dvdt: Vec<i64>,
    pub du1dt: Vec<i64>,
    pub du2dt: Vec<i64>,
    pub du3dt: Vec<i64>,
    pub du4dt: Vec<i64>,
    pub du5dt: Vec<i64>,
    pub du6dt: Vec<i64>,
    pub du7dt: Vec<i64>,
    pub v0: Vec<i64>,
    pub u1: Vec<i64>,
    pub u2: Vec<i64>,
    pub u3: Vec<i64>,
    pub u4: Vec<i64>,
    pub u5: Vec<i64>,
    pub u6: Vec<i64>,
    pub u7: Vec<i64>,
}
impl EM_EP_CELLMODEL_DEFINEFUNCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_DEFINEFUNCTION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            nstate: t.take("NSTATE").and_then(|c| c.into_int()).unwrap_or_default(),
            fswitch: t.take("FSWITCH").and_then(|c| c.into_int()).unwrap_or_default(),
            dvdt: t.take("DVDT").and_then(|c| c.into_int()).unwrap_or_default(),
            du1dt: t.take("DU1DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du2dt: t.take("DU2DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du3dt: t.take("DU3DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du4dt: t.take("DU4DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du5dt: t.take("DU5DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du6dt: t.take("DU6DT").and_then(|c| c.into_int()).unwrap_or_default(),
            du7dt: t.take("DU7DT").and_then(|c| c.into_int()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_int()).unwrap_or_default(),
            u1: t.take("U1").and_then(|c| c.into_int()).unwrap_or_default(),
            u2: t.take("U2").and_then(|c| c.into_int()).unwrap_or_default(),
            u3: t.take("U3").and_then(|c| c.into_int()).unwrap_or_default(),
            u4: t.take("U4").and_then(|c| c.into_int()).unwrap_or_default(),
            u5: t.take("U5").and_then(|c| c.into_int()).unwrap_or_default(),
            u6: t.take("U6").and_then(|c| c.into_int()).unwrap_or_default(),
            u7: t.take("U7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_FENTONKARMA {
    pub matid: Vec<i64>,
    pub taud: Vec<f64>,
    pub taur: Vec<f64>,
    pub tausi: Vec<f64>,
    pub tauo: Vec<f64>,
    pub tauvp: Vec<f64>,
    pub tauvm: Vec<f64>,
    pub tauwp: Vec<f64>,
    pub tauwm: Vec<f64>,
    pub uc: Vec<f64>,
    pub ucsi: Vec<f64>,
    pub k: Vec<f64>,
    pub u0: Vec<f64>,
    pub v0: Vec<f64>,
    pub w0: Vec<f64>,
}
impl EM_EP_CELLMODEL_FENTONKARMA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_FENTONKARMA").unwrap());
        Self {
            matid: t.take("MATID").and_then(|c| c.into_int()).unwrap_or_default(),
            taud: t.take("TAUD").and_then(|c| c.into_float()).unwrap_or_default(),
            taur: t.take("TAUR").and_then(|c| c.into_float()).unwrap_or_default(),
            tausi: t.take("TAUSI").and_then(|c| c.into_float()).unwrap_or_default(),
            tauo: t.take("TAUO").and_then(|c| c.into_float()).unwrap_or_default(),
            tauvp: t.take("TAUVP").and_then(|c| c.into_float()).unwrap_or_default(),
            tauvm: t.take("TAUVM").and_then(|c| c.into_float()).unwrap_or_default(),
            tauwp: t.take("TAUWP").and_then(|c| c.into_float()).unwrap_or_default(),
            tauwm: t.take("TAUWM").and_then(|c| c.into_float()).unwrap_or_default(),
            uc: t.take("UC").and_then(|c| c.into_float()).unwrap_or_default(),
            ucsi: t.take("UCSI").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            u0: t.take("U0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            w0: t.take("W0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_FITZHUGHNAGUMO {
    pub matid: Vec<i64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub c: Vec<f64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub v: Vec<f64>,
    pub r: Vec<f64>,
}
impl EM_EP_CELLMODEL_FITZHUGHNAGUMO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_FITZHUGHNAGUMO").unwrap());
        Self {
            matid: t.take("MATID").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_TENTUSSCHER {
    pub mid: Vec<i64>,
    pub r: Vec<f64>,
    pub t: Vec<f64>,
    pub f: Vec<f64>,
    pub cm: Vec<f64>,
    pub vc: Vec<f64>,
    pub vsr: Vec<f64>,
    pub vss: Vec<f64>,
    pub pkna: Vec<f64>,
    pub ko: Vec<f64>,
    pub nao: Vec<f64>,
    pub cao: Vec<f64>,
    pub gk1: Vec<f64>,
    pub gkr: Vec<f64>,
    pub gks: Vec<f64>,
    pub gna: Vec<f64>,
    pub gbna: Vec<f64>,
    pub gcal: Vec<f64>,
    pub gbca: Vec<f64>,
    pub gto: Vec<f64>,
    pub gpca: Vec<f64>,
    pub gpk: Vec<f64>,
    pub pnak: Vec<f64>,
    pub km: Vec<f64>,
    pub kmna: Vec<f64>,
    pub knaca: Vec<f64>,
    pub ksat: Vec<f64>,
    pub alpha: Vec<f64>,
    pub gamma: Vec<f64>,
    pub kmca: Vec<f64>,
    pub kmnai: Vec<f64>,
    pub kpca: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub ec: Vec<f64>,
    pub maxsr: Vec<f64>,
    pub minsr: Vec<f64>,
    pub vrel: Vec<f64>,
    pub vleak: Vec<f64>,
    pub vxfer: Vec<f64>,
    pub vmaxup: Vec<f64>,
    pub kup: Vec<f64>,
    pub bufc: Vec<f64>,
    pub kbufc: Vec<f64>,
    pub bufsr: Vec<f64>,
    pub kbufsf: Vec<f64>,
    pub bufss: Vec<f64>,
    pub kbufss: Vec<f64>,
    pub v: Vec<f64>,
    pub ki: Vec<f64>,
    pub nai: Vec<f64>,
    pub cai: Vec<f64>,
    pub cass: Vec<f64>,
    pub casr: Vec<f64>,
    pub rpri: Vec<f64>,
    pub xr1: Vec<f64>,
    pub xr2: Vec<f64>,
    pub xs: Vec<f64>,
    pub m: Vec<f64>,
    pub h: Vec<f64>,
    pub j: Vec<f64>,
    pub d: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f2: Vec<f64>,
    pub fcass: Vec<f64>,
    pub s: Vec<f64>,
    pub r_2: Vec<f64>,
}
impl EM_EP_CELLMODEL_TENTUSSCHER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_TENTUSSCHER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            cm: t.take("CM").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            vsr: t.take("VSR").and_then(|c| c.into_float()).unwrap_or_default(),
            vss: t.take("VSS").and_then(|c| c.into_float()).unwrap_or_default(),
            pkna: t.take("PKNA").and_then(|c| c.into_float()).unwrap_or_default(),
            ko: t.take("KO").and_then(|c| c.into_float()).unwrap_or_default(),
            nao: t.take("NAO").and_then(|c| c.into_float()).unwrap_or_default(),
            cao: t.take("CAO").and_then(|c| c.into_float()).unwrap_or_default(),
            gk1: t.take("GK1").and_then(|c| c.into_float()).unwrap_or_default(),
            gkr: t.take("GKR").and_then(|c| c.into_float()).unwrap_or_default(),
            gks: t.take("GKS").and_then(|c| c.into_float()).unwrap_or_default(),
            gna: t.take("GNA").and_then(|c| c.into_float()).unwrap_or_default(),
            gbna: t.take("GBNA").and_then(|c| c.into_float()).unwrap_or_default(),
            gcal: t.take("GCAL").and_then(|c| c.into_float()).unwrap_or_default(),
            gbca: t.take("GBCA").and_then(|c| c.into_float()).unwrap_or_default(),
            gto: t.take("GTO").and_then(|c| c.into_float()).unwrap_or_default(),
            gpca: t.take("GPCA").and_then(|c| c.into_float()).unwrap_or_default(),
            gpk: t.take("GPK").and_then(|c| c.into_float()).unwrap_or_default(),
            pnak: t.take("PNAK").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            kmna: t.take("KMNA").and_then(|c| c.into_float()).unwrap_or_default(),
            knaca: t.take("KNACA").and_then(|c| c.into_float()).unwrap_or_default(),
            ksat: t.take("KSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            kmca: t.take("KMCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kmnai: t.take("KMNAI").and_then(|c| c.into_float()).unwrap_or_default(),
            kpca: t.take("KPCA").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            maxsr: t.take("MAXSR").and_then(|c| c.into_float()).unwrap_or_default(),
            minsr: t.take("MINSR").and_then(|c| c.into_float()).unwrap_or_default(),
            vrel: t.take("VREL").and_then(|c| c.into_float()).unwrap_or_default(),
            vleak: t.take("VLEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            vxfer: t.take("VXFER").and_then(|c| c.into_float()).unwrap_or_default(),
            vmaxup: t.take("VMAXUP").and_then(|c| c.into_float()).unwrap_or_default(),
            kup: t.take("KUP").and_then(|c| c.into_float()).unwrap_or_default(),
            bufc: t.take("BUFC").and_then(|c| c.into_float()).unwrap_or_default(),
            kbufc: t.take("KBUFC").and_then(|c| c.into_float()).unwrap_or_default(),
            bufsr: t.take("BUFSR").and_then(|c| c.into_float()).unwrap_or_default(),
            kbufsf: t.take("KBUFSF").and_then(|c| c.into_float()).unwrap_or_default(),
            bufss: t.take("BUFSS").and_then(|c| c.into_float()).unwrap_or_default(),
            kbufss: t.take("KBUFSS").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            nai: t.take("NAI").and_then(|c| c.into_float()).unwrap_or_default(),
            cai: t.take("CAI").and_then(|c| c.into_float()).unwrap_or_default(),
            cass: t.take("CASS").and_then(|c| c.into_float()).unwrap_or_default(),
            casr: t.take("CASR").and_then(|c| c.into_float()).unwrap_or_default(),
            rpri: t.take("RPRI").and_then(|c| c.into_float()).unwrap_or_default(),
            xr1: t.take("XR1").and_then(|c| c.into_float()).unwrap_or_default(),
            xr2: t.take("XR2").and_then(|c| c.into_float()).unwrap_or_default(),
            xs: t.take("XS").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            fcass: t.take("FCASS").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            r_2: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_TOMEK {
    pub matid: Vec<i64>,
    pub phiendmid: Vec<f64>,
    pub phimidepl: Vec<f64>,
}
impl EM_EP_CELLMODEL_TOMEK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_TOMEK").unwrap());
        Self {
            matid: t.take("MATID").and_then(|c| c.into_int()).unwrap_or_default(),
            phiendmid: t.take("PHIENDMID").and_then(|c| c.into_float()).unwrap_or_default(),
            phimidepl: t.take("PHIMIDEPL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CELLMODEL_USERMAT {
    pub mid: Vec<i64>,
}
impl EM_EP_CELLMODEL_USERMAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CELLMODEL_USERMAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_CREATEFIBERORIENTATION {
    pub parstld: Vec<i64>,
    pub solvelde: Vec<i64>,
    pub solvelde_2: Vec<i64>,
    pub alpha: Vec<i64>,
    pub beta: Vec<i64>,
    pub w_file: Vec<i64>,
    pub prerun: Vec<i64>,
}
impl EM_EP_CREATEFIBERORIENTATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_CREATEFIBERORIENTATION").unwrap());
        Self {
            parstld: t.take("PARSTLD").and_then(|c| c.into_int()).unwrap_or_default(),
            solvelde: t.take("SOLVELDE").and_then(|c| c.into_int()).unwrap_or_default(),
            solvelde_2: t.take("SOLVELDE_2").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_int()).unwrap_or_default(),
            w_file: t.take("W_FILE").and_then(|c| c.into_int()).unwrap_or_default(),
            prerun: t.take("PRERUN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_EKG {
    pub ekgid: Vec<i64>,
    pub psid: Vec<f64>,
}
impl EM_EP_EKG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_EKG").unwrap());
        Self {
            ekgid: t.take("EKGid").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("Psid").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_FIBERINITIAL {
    pub id: Vec<i64>,
    pub partid: Vec<i64>,
    pub stype: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid0: Vec<i64>,
}
impl EM_EP_FIBERINITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_FIBERINITIAL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            partid: t.take("PARTID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid0: t.take("SSID0").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EP_PURKINJE_NETWORK {
    pub purkid: Vec<i64>,
    pub buildnet: Vec<i64>,
    pub ssid: Vec<i64>,
    pub mid: Vec<i64>,
    pub pointstx: Vec<f64>,
    pub pointsty: Vec<f64>,
    pub pointstz: Vec<f64>,
    pub edgetlen: Vec<f64>,
    pub numgen: Vec<i64>,
    pub numbrinit: Vec<i64>,
    pub numsplit: Vec<i64>,
    pub inodestld: Vec<i64>,
    pub iedgestld: Vec<i64>,
}
impl EM_EP_PURKINJE_NETWORK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EP_PURKINJE_NETWORK").unwrap());
        Self {
            purkid: t.take("PURKID").and_then(|c| c.into_int()).unwrap_or_default(),
            buildnet: t.take("BUILDNET").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            pointstx: t.take("POINTSTX").and_then(|c| c.into_float()).unwrap_or_default(),
            pointsty: t.take("POINTSTY").and_then(|c| c.into_float()).unwrap_or_default(),
            pointstz: t.take("POINTSTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            edgetlen: t.take("EDGETLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            numgen: t.take("NUMGEN").and_then(|c| c.into_int()).unwrap_or_default(),
            numbrinit: t.take("NUMBRINIT").and_then(|c| c.into_int()).unwrap_or_default(),
            numsplit: t.take("NUMSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            inodestld: t.take("INODESTLD").and_then(|c| c.into_int()).unwrap_or_default(),
            iedgestld: t.take("IEDGESTLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_EXTERNAL_FIELD {
    pub fieldid: Vec<i64>,
    pub ftype: Vec<i64>,
    pub fdef: Vec<i64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
}
impl EM_EXTERNAL_FIELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_EXTERNAL_FIELD").unwrap());
        Self {
            fieldid: t.take("FIELDID").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            fdef: t.take("FDEF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_ISOPOTENTIAL {
    pub isoid: Vec<i64>,
    pub settype: Vec<i64>,
    pub setid: Vec<i64>,
    pub rdltype: Vec<i64>,
}
impl EM_ISOPOTENTIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_ISOPOTENTIAL").unwrap());
        Self {
            isoid: t.take("ISOID").and_then(|c| c.into_int()).unwrap_or_default(),
            settype: t.take("SETTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_ISOPOTENTIAL_CONNECT {
    pub conid: Vec<i64>,
    pub contype: Vec<i64>,
    pub isoid1: Vec<i64>,
    pub isoid2: Vec<i64>,
    pub val: Vec<f64>,
    pub lcid_rdlid: Vec<i64>,
    pub psid: Vec<i64>,
    pub l: Vec<f64>,
    pub c: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EM_ISOPOTENTIAL_CONNECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_ISOPOTENTIAL_CONNECT").unwrap());
        Self {
            conid: t.take("CONID").and_then(|c| c.into_int()).unwrap_or_default(),
            contype: t.take("CONTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            isoid1: t.take("ISOID1").and_then(|c| c.into_int()).unwrap_or_default(),
            isoid2: t.take("ISOID2").and_then(|c| c.into_int()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_rdlid: t.take("LCID/RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_ISOPOTENTIAL_ROGO {
    pub isoid: Vec<i64>,
    pub settype: Vec<i64>,
    pub setid: Vec<i64>,
}
impl EM_ISOPOTENTIAL_ROGO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_ISOPOTENTIAL_ROGO").unwrap());
        Self {
            isoid: t.take("ISOID").and_then(|c| c.into_int()).unwrap_or_default(),
            settype: t.take("SETTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_001 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigma: Vec<f64>,
    pub eosid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub deatht: Vec<f64>,
    pub rdltype: Vec<i64>,
}
impl EM_MAT_001 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_001").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            deatht: t.take("DEATHT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_002 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigma: Vec<f64>,
    pub eosid: Vec<i64>,
    pub murel: Vec<f64>,
    pub eosmu: Vec<i64>,
    pub deatht: Vec<f64>,
    pub f: Vec<i64>,
    pub eosid2: Vec<i64>,
}
impl EM_MAT_002 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_002").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            murel: t.take("MUREL").and_then(|c| c.into_float()).unwrap_or_default(),
            eosmu: t.take("EOSMU").and_then(|c| c.into_int()).unwrap_or_default(),
            deatht: t.take("DEATHT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid2: t.take("EOSID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_003 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigma11: Vec<f64>,
    pub sigma22: Vec<f64>,
    pub sigma33: Vec<f64>,
    pub sigma12: Vec<i64>,
    pub sigma13: Vec<i64>,
    pub sigma21: Vec<f64>,
    pub sigma23: Vec<f64>,
    pub sigma31: Vec<f64>,
    pub sigma32: Vec<f64>,
    pub aopt: Vec<i64>,
    pub lambda: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl EM_MAT_003 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_003").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma11: t.take("SIGMA11").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma22: t.take("SIGMA22").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma33: t.take("SIGMA33").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma12: t.take("SIGMA12").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma13: t.take("SIGMA13").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma21: t.take("SIGMA21").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma23: t.take("SIGMA23").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma31: t.take("SIGMA31").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma32: t.take("SIGMA32").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_004 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigma: Vec<f64>,
    pub eosid: Vec<i64>,
    pub nele: Vec<i64>,
    pub murel: Vec<f64>,
    pub eosmu: Vec<i64>,
    pub deatht: Vec<f64>,
}
impl EM_MAT_004 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_004").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nele: t.take("NELE").and_then(|c| c.into_int()).unwrap_or_default(),
            murel: t.take("MUREL").and_then(|c| c.into_float()).unwrap_or_default(),
            eosmu: t.take("EOSMU").and_then(|c| c.into_int()).unwrap_or_default(),
            deatht: t.take("DEATHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_005 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigmaxxa: Vec<f64>,
    pub sigmayya: Vec<f64>,
    pub sigmazza: Vec<f64>,
    pub sigmaxya: Vec<f64>,
    pub sigmaxza: Vec<f64>,
    pub sigmayxa: Vec<f64>,
    pub sigmayza: Vec<f64>,
    pub sigmazxa: Vec<f64>,
    pub sigmazya: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub sigmaxxb: Vec<f64>,
    pub sigmayyb: Vec<f64>,
    pub sigmazzb: Vec<f64>,
    pub sigmaxyb: Vec<f64>,
    pub sigmaxzb: Vec<f64>,
    pub sigmayxb: Vec<f64>,
    pub sigmayzb: Vec<f64>,
    pub sigmazxb: Vec<f64>,
    pub sigmazyb: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl EM_MAT_005 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_005").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigmaxxa: t.take("SIGMAXXA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayya: t.take("SIGMAYYA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazza: t.take("SIGMAZZA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmaxya: t.take("SIGMAXYA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmaxza: t.take("SIGMAXZA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayxa: t.take("SIGMAYXA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayza: t.take("SIGMAYZA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazxa: t.take("SIGMAZXA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazya: t.take("SIGMAZYA").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            sigmaxxb: t.take("SIGMAXXB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayyb: t.take("SIGMAYYB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazzb: t.take("SIGMAZZB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmaxyb: t.take("SIGMAXYB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmaxzb: t.take("SIGMAXZB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayxb: t.take("SIGMAYXB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmayzb: t.take("SIGMAYZB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazxb: t.take("SIGMAZXB").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmazyb: t.take("SIGMAZYB").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_MAT_006 {
    pub mid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub sigp: Vec<f64>,
    pub eosp: Vec<i64>,
    pub sign: Vec<f64>,
    pub eosn: Vec<i64>,
    pub deatht: Vec<f64>,
}
impl EM_MAT_006 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_MAT_006").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            sigp: t.take("SIGP").and_then(|c| c.into_float()).unwrap_or_default(),
            eosp: t.take("EOSP").and_then(|c| c.into_int()).unwrap_or_default(),
            sign: t.take("SIGN").and_then(|c| c.into_float()).unwrap_or_default(),
            eosn: t.take("EOSN").and_then(|c| c.into_int()).unwrap_or_default(),
            deatht: t.take("DEATHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_OUTPUT {
    pub mats: Vec<i64>,
    pub matf: Vec<i64>,
    pub sols: Vec<i64>,
    pub solf: Vec<i64>,
    pub mesh: Vec<i64>,
    pub mem: Vec<i64>,
    pub timing: Vec<i64>,
}
impl EM_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_OUTPUT").unwrap());
        Self {
            mats: t.take("MATS").and_then(|c| c.into_int()).unwrap_or_default(),
            matf: t.take("MATF").and_then(|c| c.into_int()).unwrap_or_default(),
            sols: t.take("SOLS").and_then(|c| c.into_int()).unwrap_or_default(),
            solf: t.take("SOLF").and_then(|c| c.into_int()).unwrap_or_default(),
            mesh: t.take("MESH").and_then(|c| c.into_int()).unwrap_or_default(),
            mem: t.take("MEM").and_then(|c| c.into_int()).unwrap_or_default(),
            timing: t.take("TIMING").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_PERMANENT_MAGNET {
    pub id: Vec<i64>,
    pub partid: Vec<i64>,
    pub mtype: Vec<i64>,
    pub north: Vec<i64>,
    pub south: Vec<i64>,
    pub hc: Vec<f64>,
    pub x_nid1: Vec<f64>,
    pub y_nid2: Vec<f64>,
    pub z: Vec<f64>,
}
impl EM_PERMANENT_MAGNET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_PERMANENT_MAGNET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            partid: t.take("PARTID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            north: t.take("NORTH").and_then(|c| c.into_int()).unwrap_or_default(),
            south: t.take("SOUTH").and_then(|c| c.into_int()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            x_nid1: t.take("X/NID1").and_then(|c| c.into_float()).unwrap_or_default(),
            y_nid2: t.take("Y/NID2").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_POINT_SET {
    pub psid: Vec<i64>,
    pub pstype: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub pos: Vec<i64>,
}
impl EM_POINT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_POINT_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            pstype: t.take("PSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            pos: t.take("POS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_BATMAC {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub rdlarea: Vec<i64>,
    pub psid: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub r20cha: Vec<f64>,
    pub r20dis: Vec<f64>,
    pub c20cha: Vec<f64>,
    pub c20dis: Vec<f64>,
    pub r30cha: Vec<f64>,
    pub r30dis: Vec<f64>,
    pub c30cha: Vec<f64>,
    pub c30dis: Vec<f64>,
    pub temp: Vec<f64>,
    pub frther: Vec<i64>,
    pub r0toth: Vec<i64>,
    pub dudt: Vec<f64>,
    pub tempu: Vec<i64>,
    pub usesocs: Vec<i64>,
    pub tau: Vec<f64>,
    pub flcid: Vec<i64>,
}
impl EM_RANDLES_BATMAC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_BATMAC").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rdlarea: t.take("RDLAREA").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r20cha: t.take("R20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r20dis: t.take("R20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c20cha: t.take("C20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c20dis: t.take("C20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r30cha: t.take("R30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r30dis: t.take("R30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c30cha: t.take("C30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c30dis: t.take("C30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            frther: t.take("FRTHER").and_then(|c| c.into_int()).unwrap_or_default(),
            r0toth: t.take("R0TOTH").and_then(|c| c.into_int()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            tempu: t.take("TEMPU").and_then(|c| c.into_int()).unwrap_or_default(),
            usesocs: t.take("USESOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau: t.take("TAU").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_EXOTHERMIC_REACTION {
    pub areatype: Vec<i64>,
    pub funcid: Vec<i64>,
}
impl EM_RANDLES_EXOTHERMIC_REACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_EXOTHERMIC_REACTION").unwrap());
        Self {
            areatype: t.take("AREATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            funcid: t.take("FUNCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_LAYERED {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub psid: Vec<i64>,
    pub rdlarea: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub r20cha: Vec<f64>,
    pub r20dis: Vec<f64>,
    pub c20cha: Vec<f64>,
    pub c20dis: Vec<f64>,
    pub r30cha: Vec<f64>,
    pub temp: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
}
impl EM_RANDLES_LAYERED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_LAYERED").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdlarea: t.take("RDLAREA").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r20cha: t.take("R20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r20dis: t.take("R20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c20cha: t.take("C20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c20dis: t.take("C20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r30cha: t.take("R30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_MESHLESS {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub r20cha: Vec<f64>,
    pub r20dis: Vec<f64>,
    pub c20cha: Vec<f64>,
    pub c20dis: Vec<f64>,
    pub r30cha: Vec<f64>,
    pub r30dis: Vec<f64>,
    pub c30cha: Vec<f64>,
    pub c30dis: Vec<f64>,
    pub temp: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub dudt: Vec<f64>,
    pub tempu: Vec<i64>,
    pub usesocs: Vec<i64>,
    pub tau: Vec<f64>,
    pub flcid: Vec<i64>,
}
impl EM_RANDLES_MESHLESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_MESHLESS").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r20cha: t.take("R20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r20dis: t.take("R20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c20cha: t.take("C20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c20dis: t.take("C20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r30cha: t.take("R30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r30dis: t.take("R30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c30cha: t.take("C30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c30dis: t.take("C30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            tempu: t.take("TEMPU").and_then(|c| c.into_int()).unwrap_or_default(),
            usesocs: t.take("USESOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau: t.take("TAU").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_SHORT {
    pub areatype: Vec<i64>,
    pub funcid: Vec<i64>,
}
impl EM_RANDLES_SHORT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_SHORT").unwrap());
        Self {
            areatype: t.take("AREATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            funcid: t.take("FUNCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_SOLID {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub rdlarea: Vec<i64>,
    pub ccppart: Vec<i64>,
    pub ccnpart: Vec<i64>,
    pub seppart: Vec<i64>,
    pub pelpart: Vec<i64>,
    pub nelpart: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub r20cha: Vec<f64>,
    pub r20dis: Vec<f64>,
    pub c20cha: Vec<f64>,
    pub c20dis: Vec<f64>,
    pub r30cha: Vec<f64>,
    pub r30dis: Vec<f64>,
    pub c30cha: Vec<f64>,
    pub c30dis: Vec<f64>,
    pub temp: Vec<f64>,
    pub frther: Vec<i64>,
    pub r0toth: Vec<i64>,
    pub dudt: Vec<f64>,
    pub tempu: Vec<i64>,
    pub usesocs: Vec<i64>,
    pub tau: Vec<f64>,
    pub flcid: Vec<i64>,
}
impl EM_RANDLES_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_SOLID").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rdlarea: t.take("RDLAREA").and_then(|c| c.into_int()).unwrap_or_default(),
            ccppart: t.take("CCPPART").and_then(|c| c.into_int()).unwrap_or_default(),
            ccnpart: t.take("CCNPART").and_then(|c| c.into_int()).unwrap_or_default(),
            seppart: t.take("SEPPART").and_then(|c| c.into_int()).unwrap_or_default(),
            pelpart: t.take("PELPART").and_then(|c| c.into_int()).unwrap_or_default(),
            nelpart: t.take("NELPART").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r20cha: t.take("R20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r20dis: t.take("R20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c20cha: t.take("C20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c20dis: t.take("C20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r30cha: t.take("R30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r30dis: t.take("R30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c30cha: t.take("C30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c30dis: t.take("C30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            frther: t.take("FRTHER").and_then(|c| c.into_int()).unwrap_or_default(),
            r0toth: t.take("R0TOTH").and_then(|c| c.into_int()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            tempu: t.take("TEMPU").and_then(|c| c.into_int()).unwrap_or_default(),
            usesocs: t.take("USESOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau: t.take("TAU").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_RANDLES_TSHELL {
    pub rdlid: Vec<i64>,
    pub rdltype: Vec<i64>,
    pub rdlarea: Vec<i64>,
    pub psid: Vec<i64>,
    pub q: Vec<f64>,
    pub cq: Vec<f64>,
    pub socinit: Vec<f64>,
    pub soctou: Vec<f64>,
    pub r0cha: Vec<f64>,
    pub r0dis: Vec<f64>,
    pub r10cha: Vec<f64>,
    pub r10dis: Vec<f64>,
    pub c10cha: Vec<f64>,
    pub c10dis: Vec<f64>,
    pub r20cha: Vec<f64>,
    pub r20dis: Vec<f64>,
    pub c20cha: Vec<f64>,
    pub c20dis: Vec<f64>,
    pub r30cha: Vec<f64>,
    pub r30dis: Vec<f64>,
    pub c30cha: Vec<f64>,
    pub c30dis: Vec<f64>,
    pub temp: Vec<f64>,
    pub frther: Vec<i64>,
    pub r0toth: Vec<i64>,
    pub dudt: Vec<f64>,
    pub tempu: Vec<i64>,
    pub usesocs: Vec<i64>,
    pub tau: Vec<f64>,
    pub flcid: Vec<i64>,
}
impl EM_RANDLES_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_RANDLES_TSHELL").unwrap());
        Self {
            rdlid: t.take("RDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            rdltype: t.take("RDLTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            rdlarea: t.take("RDLAREA").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            socinit: t.take("SOCINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            soctou: t.take("SOCTOU").and_then(|c| c.into_float()).unwrap_or_default(),
            r0cha: t.take("R0CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r0dis: t.take("R0DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r10cha: t.take("R10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r10dis: t.take("R10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c10cha: t.take("C10CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c10dis: t.take("C10DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r20cha: t.take("R20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r20dis: t.take("R20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c20cha: t.take("C20CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c20dis: t.take("C20DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            r30cha: t.take("R30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r30dis: t.take("R30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            c30cha: t.take("C30CHA").and_then(|c| c.into_float()).unwrap_or_default(),
            c30dis: t.take("C30DIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            frther: t.take("FRTHER").and_then(|c| c.into_int()).unwrap_or_default(),
            r0toth: t.take("R0TOTH").and_then(|c| c.into_int()).unwrap_or_default(),
            dudt: t.take("DUDT").and_then(|c| c.into_float()).unwrap_or_default(),
            tempu: t.take("TEMPU").and_then(|c| c.into_int()).unwrap_or_default(),
            usesocs: t.take("USESOCS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau: t.take("TAU").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_ROTATION_AXIS {
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub xd: Vec<f64>,
    pub yd: Vec<f64>,
    pub zd: Vec<f64>,
    pub numsec: Vec<i64>,
}
impl EM_ROTATION_AXIS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_ROTATION_AXIS").unwrap());
        Self {
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            xd: t.take("XD").and_then(|c| c.into_float()).unwrap_or_default(),
            yd: t.take("YD").and_then(|c| c.into_float()).unwrap_or_default(),
            zd: t.take("ZD").and_then(|c| c.into_float()).unwrap_or_default(),
            numsec: t.take("NUMSEC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_SOLVER_BEM {
    pub reltol: Vec<f64>,
    pub maxite: Vec<i64>,
    pub stype: Vec<i64>,
    pub precon: Vec<i64>,
    pub uselast: Vec<i64>,
    pub ncyclbem: Vec<i64>,
}
impl EM_SOLVER_BEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_SOLVER_BEM").unwrap());
        Self {
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            maxite: t.take("MAXITE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            precon: t.take("PRECON").and_then(|c| c.into_int()).unwrap_or_default(),
            uselast: t.take("USELAST").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyclbem: t.take("NCYCLBEM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_SOLVER_BEMMAT {
    pub matid: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub unused_6: Vec<i64>,
    pub reltol: Vec<f64>,
}
impl EM_SOLVER_BEMMAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_SOLVER_BEMMAT").unwrap());
        Self {
            matid: t.take("MATID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EM_SOLVER_FEM {
    pub reltol: Vec<f64>,
    pub maxite: Vec<i64>,
    pub stype: Vec<i64>,
    pub precon: Vec<i64>,
    pub uselast: Vec<i64>,
    pub ncyclfem: Vec<i64>,
}
impl EM_SOLVER_FEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_SOLVER_FEM").unwrap());
        Self {
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            maxite: t.take("MAXITE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            precon: t.take("PRECON").and_then(|c| c.into_int()).unwrap_or_default(),
            uselast: t.take("USELAST").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyclfem: t.take("NCYCLFEM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_SOLVER_FEMBEM {
    pub reltol: Vec<f64>,
    pub maxite: Vec<i64>,
    pub forcon: Vec<i64>,
}
impl EM_SOLVER_FEMBEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_SOLVER_FEMBEM").unwrap());
        Self {
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            maxite: t.take("MAXITE").and_then(|c| c.into_int()).unwrap_or_default(),
            forcon: t.take("FORCON").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_SOLVER_FEMBEM_MONOLITHIC {
    pub mtype: Vec<i64>,
    pub stype: Vec<i64>,
    pub abstol: Vec<f64>,
    pub reltol: Vec<f64>,
    pub maxit: Vec<i64>,
}
impl EM_SOLVER_FEMBEM_MONOLITHIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_SOLVER_FEMBEM_MONOLITHIC").unwrap());
        Self {
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            abstol: t.take("ABSTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EM_VOLTAGE_DROP {
    pub vdid: Vec<i64>,
    pub vdtype: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub volt: Vec<f64>,
}
impl EM_VOLTAGE_DROP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EM_VOLTAGE_DROP").unwrap());
        Self {
            vdid: t.take("VDID").and_then(|c| c.into_int()).unwrap_or_default(),
            vdtype: t.take("VDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            volt: t.take("VOLT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_001 {
    pub eosid: Vec<i64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_001 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_001").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_002 {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub omeg: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
}
impl EOS_002 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_002").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg: t.take("OMEG").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_002_AFTERBURN {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub omeg: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub opt: Vec<f64>,
    pub qt: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub opt_2: Vec<f64>,
    pub q0: Vec<f64>,
    pub qa: Vec<f64>,
    pub qm: Vec<f64>,
    pub qn: Vec<f64>,
    pub conm: Vec<f64>,
    pub conl: Vec<f64>,
    pub cont: Vec<f64>,
}
impl EOS_002_AFTERBURN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_002_AFTERBURN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg: t.take("OMEG").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qt: t.take("QT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            opt_2: t.take("OPT").and_then(|c| c.into_float()).unwrap_or_default(),
            q0: t.take("Q0").and_then(|c| c.into_float()).unwrap_or_default(),
            qa: t.take("QA").and_then(|c| c.into_float()).unwrap_or_default(),
            qm: t.take("QM").and_then(|c| c.into_float()).unwrap_or_default(),
            qn: t.take("QN").and_then(|c| c.into_float()).unwrap_or_default(),
            conm: t.take("CONM").and_then(|c| c.into_float()).unwrap_or_default(),
            conl: t.take("CONL").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_003 {
    pub eosid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
}
impl EOS_003 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_003").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_004 {
    pub eosid: Vec<i64>,
    pub c: Vec<f64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub gamao: Vec<f64>,
    pub a: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
    pub unused: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl EOS_004 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_004").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamao: t.take("GAMAO").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EOS_005 {
    pub eosid: Vec<i64>,
    pub a10: Vec<f64>,
    pub a11: Vec<f64>,
    pub a12: Vec<f64>,
    pub a13: Vec<f64>,
    pub a20: Vec<f64>,
    pub a21: Vec<f64>,
    pub a22: Vec<f64>,
    pub a23: Vec<f64>,
    pub a30: Vec<f64>,
    pub a31: Vec<f64>,
    pub a32: Vec<f64>,
    pub a33: Vec<f64>,
    pub a40: Vec<f64>,
    pub a41: Vec<f64>,
    pub a42: Vec<f64>,
    pub a43: Vec<f64>,
    pub a50: Vec<f64>,
    pub a51: Vec<f64>,
    pub a52: Vec<f64>,
    pub a53: Vec<f64>,
    pub a60: Vec<f64>,
    pub a61: Vec<f64>,
    pub a62: Vec<f64>,
    pub a63: Vec<f64>,
    pub a70: Vec<f64>,
    pub a71: Vec<f64>,
    pub a72: Vec<f64>,
    pub a73: Vec<f64>,
    pub a14: Vec<f64>,
    pub a24: Vec<f64>,
    pub alph: Vec<f64>,
    pub beta: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_005 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_005").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a10: t.take("A10").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a13: t.take("A13").and_then(|c| c.into_float()).unwrap_or_default(),
            a20: t.take("A20").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            a30: t.take("A30").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            a33: t.take("A33").and_then(|c| c.into_float()).unwrap_or_default(),
            a40: t.take("A40").and_then(|c| c.into_float()).unwrap_or_default(),
            a41: t.take("A41").and_then(|c| c.into_float()).unwrap_or_default(),
            a42: t.take("A42").and_then(|c| c.into_float()).unwrap_or_default(),
            a43: t.take("A43").and_then(|c| c.into_float()).unwrap_or_default(),
            a50: t.take("A50").and_then(|c| c.into_float()).unwrap_or_default(),
            a51: t.take("A51").and_then(|c| c.into_float()).unwrap_or_default(),
            a52: t.take("A52").and_then(|c| c.into_float()).unwrap_or_default(),
            a53: t.take("A53").and_then(|c| c.into_float()).unwrap_or_default(),
            a60: t.take("A60").and_then(|c| c.into_float()).unwrap_or_default(),
            a61: t.take("A61").and_then(|c| c.into_float()).unwrap_or_default(),
            a62: t.take("A62").and_then(|c| c.into_float()).unwrap_or_default(),
            a63: t.take("A63").and_then(|c| c.into_float()).unwrap_or_default(),
            a70: t.take("A70").and_then(|c| c.into_float()).unwrap_or_default(),
            a71: t.take("A71").and_then(|c| c.into_float()).unwrap_or_default(),
            a72: t.take("A72").and_then(|c| c.into_float()).unwrap_or_default(),
            a73: t.take("A73").and_then(|c| c.into_float()).unwrap_or_default(),
            a14: t.take("A14").and_then(|c| c.into_float()).unwrap_or_default(),
            a24: t.take("A24").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_006 {
    pub eosid: Vec<i64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl EOS_006 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_006").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EOS_007 {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub xp1: Vec<f64>,
    pub xp2: Vec<f64>,
    pub frer: Vec<f64>,
    pub g: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r5: Vec<f64>,
    pub r6: Vec<f64>,
    pub fmxig: Vec<f64>,
    pub freq: Vec<f64>,
    pub grow1: Vec<f64>,
    pub em: Vec<f64>,
    pub ar1: Vec<f64>,
    pub es1: Vec<f64>,
    pub cvp: Vec<f64>,
    pub cvr: Vec<f64>,
    pub eetal: Vec<f64>,
    pub ccrit: Vec<f64>,
    pub enq: Vec<f64>,
    pub tmp0: Vec<f64>,
    pub grow2: Vec<f64>,
    pub ar2: Vec<f64>,
    pub es2: Vec<f64>,
    pub en: Vec<f64>,
    pub fmxgr: Vec<f64>,
    pub fmngr: Vec<f64>,
}
impl EOS_007 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_007").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            frer: t.take("FRER").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            r6: t.take("R6").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxig: t.take("FMXIG").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ar1: t.take("AR1").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvp: t.take("CVP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvr: t.take("CVR").and_then(|c| c.into_float()).unwrap_or_default(),
            eetal: t.take("EETAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ccrit: t.take("CCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            enq: t.take("ENQ").and_then(|c| c.into_float()).unwrap_or_default(),
            tmp0: t.take("TMP0").and_then(|c| c.into_float()).unwrap_or_default(),
            grow2: t.take("GROW2").and_then(|c| c.into_float()).unwrap_or_default(),
            ar2: t.take("AR2").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxgr: t.take("FMXGR").and_then(|c| c.into_float()).unwrap_or_default(),
            fmngr: t.take("FMNGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_008 {
    pub eosid: Vec<i64>,
    pub gama: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lct: Vec<i64>,
    pub lck: Vec<i64>,
    pub lcid: Vec<i64>,
    pub ev1: Vec<f64>,
    pub ev2: Vec<f64>,
    pub ev3: Vec<f64>,
    pub ev4: Vec<f64>,
    pub ev5: Vec<f64>,
    pub ev6: Vec<f64>,
    pub ev7: Vec<f64>,
    pub ev8: Vec<f64>,
    pub ev9: Vec<f64>,
    pub ev10: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub t9: Vec<f64>,
    pub t10: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
    pub k9: Vec<f64>,
    pub k10: Vec<f64>,
}
impl EOS_008 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_008").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lck: t.take("LCK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ev1: t.take("EV1").and_then(|c| c.into_float()).unwrap_or_default(),
            ev2: t.take("EV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ev3: t.take("EV3").and_then(|c| c.into_float()).unwrap_or_default(),
            ev4: t.take("EV4").and_then(|c| c.into_float()).unwrap_or_default(),
            ev5: t.take("EV5").and_then(|c| c.into_float()).unwrap_or_default(),
            ev6: t.take("EV6").and_then(|c| c.into_float()).unwrap_or_default(),
            ev7: t.take("EV7").and_then(|c| c.into_float()).unwrap_or_default(),
            ev8: t.take("EV8").and_then(|c| c.into_float()).unwrap_or_default(),
            ev9: t.take("EV9").and_then(|c| c.into_float()).unwrap_or_default(),
            ev10: t.take("EV10").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            t9: t.take("T9").and_then(|c| c.into_float()).unwrap_or_default(),
            t10: t.take("T10").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
            k9: t.take("K9").and_then(|c| c.into_float()).unwrap_or_default(),
            k10: t.take("K10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_009 {
    pub eosid: Vec<i64>,
    pub gama: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub ev1: Vec<f64>,
    pub ev2: Vec<f64>,
    pub ev3: Vec<f64>,
    pub ev4: Vec<f64>,
    pub ev5: Vec<f64>,
    pub ev6: Vec<f64>,
    pub ev7: Vec<f64>,
    pub ev8: Vec<f64>,
    pub ev9: Vec<f64>,
    pub ev10: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub t9: Vec<f64>,
    pub t10: Vec<f64>,
}
impl EOS_009 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_009").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            ev1: t.take("EV1").and_then(|c| c.into_float()).unwrap_or_default(),
            ev2: t.take("EV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ev3: t.take("EV3").and_then(|c| c.into_float()).unwrap_or_default(),
            ev4: t.take("EV4").and_then(|c| c.into_float()).unwrap_or_default(),
            ev5: t.take("EV5").and_then(|c| c.into_float()).unwrap_or_default(),
            ev6: t.take("EV6").and_then(|c| c.into_float()).unwrap_or_default(),
            ev7: t.take("EV7").and_then(|c| c.into_float()).unwrap_or_default(),
            ev8: t.take("EV8").and_then(|c| c.into_float()).unwrap_or_default(),
            ev9: t.take("EV9").and_then(|c| c.into_float()).unwrap_or_default(),
            ev10: t.take("EV10").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            t9: t.take("T9").and_then(|c| c.into_float()).unwrap_or_default(),
            t10: t.take("T10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_010 {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub xp1: Vec<f64>,
    pub xp2: Vec<f64>,
    pub frer: Vec<f64>,
    pub g: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r5: Vec<f64>,
    pub r6: Vec<f64>,
    pub fmxig: Vec<f64>,
    pub freq: Vec<f64>,
    pub grow1: Vec<f64>,
    pub em: Vec<f64>,
    pub ar1: Vec<f64>,
    pub es1: Vec<f64>,
    pub cvp: Vec<f64>,
    pub cvr: Vec<f64>,
    pub eetal: Vec<f64>,
    pub ccrit: Vec<f64>,
    pub enq: Vec<f64>,
    pub tmp0: Vec<f64>,
    pub grow2: Vec<f64>,
    pub ar2: Vec<f64>,
    pub es2: Vec<f64>,
    pub en: Vec<f64>,
    pub fmxgr: Vec<f64>,
    pub fmngr: Vec<f64>,
}
impl EOS_010 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_010").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            frer: t.take("FRER").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            r6: t.take("R6").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxig: t.take("FMXIG").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ar1: t.take("AR1").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvp: t.take("CVP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvr: t.take("CVR").and_then(|c| c.into_float()).unwrap_or_default(),
            eetal: t.take("EETAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ccrit: t.take("CCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            enq: t.take("ENQ").and_then(|c| c.into_float()).unwrap_or_default(),
            tmp0: t.take("TMP0").and_then(|c| c.into_float()).unwrap_or_default(),
            grow2: t.take("GROW2").and_then(|c| c.into_float()).unwrap_or_default(),
            ar2: t.take("AR2").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxgr: t.take("FMXGR").and_then(|c| c.into_float()).unwrap_or_default(),
            fmngr: t.take("FMNGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_011 {
    pub eosid: Vec<i64>,
    pub nld: Vec<i64>,
    pub ncr: Vec<i64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub ie0: Vec<f64>,
    pub ec0: Vec<f64>,
}
impl EOS_011 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_011").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nld: t.take("NLD").and_then(|c| c.into_int()).unwrap_or_default(),
            ncr: t.take("NCR").and_then(|c| c.into_int()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            ie0: t.take("IE0").and_then(|c| c.into_float()).unwrap_or_default(),
            ec0: t.take("EC0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_012 {
    pub eosid: Vec<i64>,
    pub cv0: Vec<f64>,
    pub cp0: Vec<f64>,
    pub cl: Vec<f64>,
    pub cq: Vec<f64>,
    pub t0: Vec<f64>,
    pub v0: Vec<f64>,
    pub vc0: Vec<f64>,
    pub adiab: Vec<f64>,
}
impl EOS_012 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_012").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cv0: t.take("CV0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp0: t.take("CP0").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            vc0: t.take("VC0").and_then(|c| c.into_float()).unwrap_or_default(),
            adiab: t.take("ADIAB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_013 {
    pub eosid: Vec<i64>,
    pub rhol: Vec<f64>,
    pub rhov: Vec<f64>,
    pub cl: Vec<f64>,
    pub cv: Vec<f64>,
    pub gamal: Vec<f64>,
    pub pv: Vec<f64>,
    pub kl: Vec<f64>,
}
impl EOS_013 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_013").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rhol: t.take("RHOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rhov: t.take("RHOV").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            gamal: t.take("GAMAL").and_then(|c| c.into_float()).unwrap_or_default(),
            pv: t.take("PV").and_then(|c| c.into_float()).unwrap_or_default(),
            kl: t.take("KL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_014 {
    pub eosid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub r5: Vec<f64>,
    pub al1: Vec<f64>,
    pub al2: Vec<f64>,
    pub al3: Vec<f64>,
    pub al4: Vec<f64>,
    pub al5: Vec<f64>,
    pub bl1: Vec<f64>,
    pub bl2: Vec<f64>,
    pub bl3: Vec<f64>,
    pub bl4: Vec<f64>,
    pub bl5: Vec<f64>,
    pub rl1: Vec<f64>,
    pub rl2: Vec<f64>,
    pub rl3: Vec<f64>,
    pub rl4: Vec<f64>,
    pub rl5: Vec<f64>,
    pub c: Vec<f64>,
    pub omega: Vec<f64>,
    pub e: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_014 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_014").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            al1: t.take("AL1").and_then(|c| c.into_float()).unwrap_or_default(),
            al2: t.take("AL2").and_then(|c| c.into_float()).unwrap_or_default(),
            al3: t.take("AL3").and_then(|c| c.into_float()).unwrap_or_default(),
            al4: t.take("AL4").and_then(|c| c.into_float()).unwrap_or_default(),
            al5: t.take("AL5").and_then(|c| c.into_float()).unwrap_or_default(),
            bl1: t.take("BL1").and_then(|c| c.into_float()).unwrap_or_default(),
            bl2: t.take("BL2").and_then(|c| c.into_float()).unwrap_or_default(),
            bl3: t.take("BL3").and_then(|c| c.into_float()).unwrap_or_default(),
            bl4: t.take("BL4").and_then(|c| c.into_float()).unwrap_or_default(),
            bl5: t.take("BL5").and_then(|c| c.into_float()).unwrap_or_default(),
            rl1: t.take("RL1").and_then(|c| c.into_float()).unwrap_or_default(),
            rl2: t.take("RL2").and_then(|c| c.into_float()).unwrap_or_default(),
            rl3: t.take("RL3").and_then(|c| c.into_float()).unwrap_or_default(),
            rl4: t.take("RL4").and_then(|c| c.into_float()).unwrap_or_default(),
            rl5: t.take("RL5").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_015 {
    pub eosid: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub lcid3: Vec<i64>,
    pub lcid4: Vec<i64>,
    pub unload: Vec<f64>,
    pub et: Vec<f64>,
    pub dmpf: Vec<f64>,
    pub tfs: Vec<f64>,
    pub cfs: Vec<f64>,
    pub loffset: Vec<f64>,
    pub ivs: Vec<f64>,
}
impl EOS_015 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_015").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid3: t.take("LCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid4: t.take("LCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            unload: t.take("UNLOAD").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpf: t.take("DMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tfs: t.take("TFS").and_then(|c| c.into_float()).unwrap_or_default(),
            cfs: t.take("CFS").and_then(|c| c.into_float()).unwrap_or_default(),
            loffset: t.take("LOFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            ivs: t.take("IVS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_016 {
    pub eosid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub pel: Vec<f64>,
    pub pco: Vec<f64>,
    pub n: Vec<f64>,
    pub alpha0: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_016 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_016").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            pel: t.take("PEL").and_then(|c| c.into_float()).unwrap_or_default(),
            pco: t.take("PCO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha0: t.take("ALPHA0").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_019 {
    pub eosid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub k0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_019 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_019").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_021 {
    pub eosid: Vec<i64>,
    pub eost: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub ivect: Vec<i64>,
    pub eo: Vec<f64>,
    pub vo: Vec<f64>,
    pub bulk: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl EOS_021 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_021").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eost: t.take("EOST").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ivect: t.take("IVECT").and_then(|c| c.into_int()).unwrap_or_default(),
            eo: t.take("EO").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_GASKET {
    pub eosid: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub lcid3: Vec<i64>,
    pub lcid4: Vec<i64>,
    pub unload: Vec<f64>,
    pub et: Vec<f64>,
    pub dmpf: Vec<f64>,
    pub tfs: Vec<f64>,
    pub cfs: Vec<f64>,
    pub loffset: Vec<f64>,
    pub ivs: Vec<f64>,
}
impl EOS_GASKET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_GASKET").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid3: t.take("LCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid4: t.take("LCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            unload: t.take("UNLOAD").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpf: t.take("DMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tfs: t.take("TFS").and_then(|c| c.into_float()).unwrap_or_default(),
            cfs: t.take("CFS").and_then(|c| c.into_float()).unwrap_or_default(),
            loffset: t.take("LOFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            ivs: t.take("IVS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_GRUNEISEN {
    pub eosid: Vec<i64>,
    pub c: Vec<f64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub gamao: Vec<f64>,
    pub a: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
    pub unused: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl EOS_GRUNEISEN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_GRUNEISEN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamao: t.take("GAMAO").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EOS_IDEAL_GAS {
    pub eosid: Vec<i64>,
    pub cv0: Vec<f64>,
    pub cp0: Vec<f64>,
    pub cl: Vec<f64>,
    pub cq: Vec<f64>,
    pub t0: Vec<f64>,
    pub v0: Vec<f64>,
    pub vc0: Vec<f64>,
    pub adiab: Vec<f64>,
}
impl EOS_IDEAL_GAS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_IDEAL_GAS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cv0: t.take("CV0").and_then(|c| c.into_float()).unwrap_or_default(),
            cp0: t.take("CP0").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            cq: t.take("CQ").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            vc0: t.take("VC0").and_then(|c| c.into_float()).unwrap_or_default(),
            adiab: t.take("ADIAB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_IGNITION_AND_GROWTH_OF_REACTION_IN_HE {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub xp1: Vec<f64>,
    pub xp2: Vec<f64>,
    pub frer: Vec<f64>,
    pub g: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r5: Vec<f64>,
    pub r6: Vec<f64>,
    pub fmxig: Vec<f64>,
    pub freq: Vec<f64>,
    pub grow1: Vec<f64>,
    pub em: Vec<f64>,
    pub ar1: Vec<f64>,
    pub es1: Vec<f64>,
    pub cvp: Vec<f64>,
    pub cvr: Vec<f64>,
    pub eetal: Vec<f64>,
    pub ccrit: Vec<f64>,
    pub enq: Vec<f64>,
    pub tmp0: Vec<f64>,
    pub grow2: Vec<f64>,
    pub ar2: Vec<f64>,
    pub es2: Vec<f64>,
    pub en: Vec<f64>,
    pub fmxgr: Vec<f64>,
    pub fmngr: Vec<f64>,
}
impl EOS_IGNITION_AND_GROWTH_OF_REACTION_IN_HE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_IGNITION_AND_GROWTH_OF_REACTION_IN_HE").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            frer: t.take("FRER").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            r6: t.take("R6").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxig: t.take("FMXIG").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ar1: t.take("AR1").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvp: t.take("CVP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvr: t.take("CVR").and_then(|c| c.into_float()).unwrap_or_default(),
            eetal: t.take("EETAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ccrit: t.take("CCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            enq: t.take("ENQ").and_then(|c| c.into_float()).unwrap_or_default(),
            tmp0: t.take("TMP0").and_then(|c| c.into_float()).unwrap_or_default(),
            grow2: t.take("GROW2").and_then(|c| c.into_float()).unwrap_or_default(),
            ar2: t.take("AR2").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxgr: t.take("FMXGR").and_then(|c| c.into_float()).unwrap_or_default(),
            fmngr: t.take("FMNGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_JWL {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub omeg: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
}
impl EOS_JWL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_JWL").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg: t.take("OMEG").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_JWLB {
    pub eosid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub r5: Vec<f64>,
    pub al1: Vec<f64>,
    pub al2: Vec<f64>,
    pub al3: Vec<f64>,
    pub al4: Vec<f64>,
    pub al5: Vec<f64>,
    pub bl1: Vec<f64>,
    pub bl2: Vec<f64>,
    pub bl3: Vec<f64>,
    pub bl4: Vec<f64>,
    pub bl5: Vec<f64>,
    pub rl1: Vec<f64>,
    pub rl2: Vec<f64>,
    pub rl3: Vec<f64>,
    pub rl4: Vec<f64>,
    pub rl5: Vec<f64>,
    pub c: Vec<f64>,
    pub omega: Vec<f64>,
    pub e: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_JWLB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_JWLB").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            al1: t.take("AL1").and_then(|c| c.into_float()).unwrap_or_default(),
            al2: t.take("AL2").and_then(|c| c.into_float()).unwrap_or_default(),
            al3: t.take("AL3").and_then(|c| c.into_float()).unwrap_or_default(),
            al4: t.take("AL4").and_then(|c| c.into_float()).unwrap_or_default(),
            al5: t.take("AL5").and_then(|c| c.into_float()).unwrap_or_default(),
            bl1: t.take("BL1").and_then(|c| c.into_float()).unwrap_or_default(),
            bl2: t.take("BL2").and_then(|c| c.into_float()).unwrap_or_default(),
            bl3: t.take("BL3").and_then(|c| c.into_float()).unwrap_or_default(),
            bl4: t.take("BL4").and_then(|c| c.into_float()).unwrap_or_default(),
            bl5: t.take("BL5").and_then(|c| c.into_float()).unwrap_or_default(),
            rl1: t.take("RL1").and_then(|c| c.into_float()).unwrap_or_default(),
            rl2: t.take("RL2").and_then(|c| c.into_float()).unwrap_or_default(),
            rl3: t.take("RL3").and_then(|c| c.into_float()).unwrap_or_default(),
            rl4: t.take("RL4").and_then(|c| c.into_float()).unwrap_or_default(),
            rl5: t.take("RL5").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_JWL_AFTERBURN {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub omeg: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub opt: Vec<f64>,
    pub qt: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub opt_2: Vec<f64>,
    pub q0: Vec<f64>,
    pub qa: Vec<f64>,
    pub qm: Vec<f64>,
    pub qn: Vec<f64>,
    pub conm: Vec<f64>,
    pub conl: Vec<f64>,
    pub cont: Vec<f64>,
}
impl EOS_JWL_AFTERBURN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_JWL_AFTERBURN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            omeg: t.take("OMEG").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qt: t.take("QT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            opt_2: t.take("OPT").and_then(|c| c.into_float()).unwrap_or_default(),
            q0: t.take("Q0").and_then(|c| c.into_float()).unwrap_or_default(),
            qa: t.take("QA").and_then(|c| c.into_float()).unwrap_or_default(),
            qm: t.take("QM").and_then(|c| c.into_float()).unwrap_or_default(),
            qn: t.take("QN").and_then(|c| c.into_float()).unwrap_or_default(),
            conm: t.take("CONM").and_then(|c| c.into_float()).unwrap_or_default(),
            conl: t.take("CONL").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_LINEAR_POLYNOMIAL {
    pub eosid: Vec<i64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_LINEAR_POLYNOMIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_LINEAR_POLYNOMIAL").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_LINEAR_POLYNOMIAL_WITH_ENERGY_LEAK {
    pub eosid: Vec<i64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl EOS_LINEAR_POLYNOMIAL_WITH_ENERGY_LEAK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_LINEAR_POLYNOMIAL_WITH_ENERGY_LEAK").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct EOS_MIE_GRUNEISEN {
    pub eosid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub pel: Vec<f64>,
    pub pco: Vec<f64>,
    pub n: Vec<f64>,
    pub alpha0: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_MIE_GRUNEISEN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_MIE_GRUNEISEN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            pel: t.take("PEL").and_then(|c| c.into_float()).unwrap_or_default(),
            pco: t.take("PCO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha0: t.take("ALPHA0").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_MURNAGHAN {
    pub eosid: Vec<i64>,
    pub gamma: Vec<f64>,
    pub k0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_MURNAGHAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_MURNAGHAN").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_PHASE_CHANGE {
    pub eosid: Vec<i64>,
    pub rhol: Vec<f64>,
    pub rhov: Vec<f64>,
    pub cl: Vec<f64>,
    pub cv: Vec<f64>,
    pub gamal: Vec<f64>,
    pub pv: Vec<f64>,
    pub kl: Vec<f64>,
}
impl EOS_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_PHASE_CHANGE").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            rhol: t.take("RHOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rhov: t.take("RHOV").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            gamal: t.take("GAMAL").and_then(|c| c.into_float()).unwrap_or_default(),
            pv: t.take("PV").and_then(|c| c.into_float()).unwrap_or_default(),
            kl: t.take("KL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_PROPELLANT_DEFLAGRATION {
    pub eosid: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub xp1: Vec<f64>,
    pub xp2: Vec<f64>,
    pub frer: Vec<f64>,
    pub g: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r5: Vec<f64>,
    pub r6: Vec<f64>,
    pub fmxig: Vec<f64>,
    pub freq: Vec<f64>,
    pub grow1: Vec<f64>,
    pub em: Vec<f64>,
    pub ar1: Vec<f64>,
    pub es1: Vec<f64>,
    pub cvp: Vec<f64>,
    pub cvr: Vec<f64>,
    pub eetal: Vec<f64>,
    pub ccrit: Vec<f64>,
    pub enq: Vec<f64>,
    pub tmp0: Vec<f64>,
    pub grow2: Vec<f64>,
    pub ar2: Vec<f64>,
    pub es2: Vec<f64>,
    pub en: Vec<f64>,
    pub fmxgr: Vec<f64>,
    pub fmngr: Vec<f64>,
}
impl EOS_PROPELLANT_DEFLAGRATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_PROPELLANT_DEFLAGRATION").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            frer: t.take("FRER").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r5: t.take("R5").and_then(|c| c.into_float()).unwrap_or_default(),
            r6: t.take("R6").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxig: t.take("FMXIG").and_then(|c| c.into_float()).unwrap_or_default(),
            freq: t.take("FREQ").and_then(|c| c.into_float()).unwrap_or_default(),
            grow1: t.take("GROW1").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ar1: t.take("AR1").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvp: t.take("CVP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvr: t.take("CVR").and_then(|c| c.into_float()).unwrap_or_default(),
            eetal: t.take("EETAL").and_then(|c| c.into_float()).unwrap_or_default(),
            ccrit: t.take("CCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            enq: t.take("ENQ").and_then(|c| c.into_float()).unwrap_or_default(),
            tmp0: t.take("TMP0").and_then(|c| c.into_float()).unwrap_or_default(),
            grow2: t.take("GROW2").and_then(|c| c.into_float()).unwrap_or_default(),
            ar2: t.take("AR2").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmxgr: t.take("FMXGR").and_then(|c| c.into_float()).unwrap_or_default(),
            fmngr: t.take("FMNGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_RATIO_OF_POLYNOMIALS {
    pub eosid: Vec<i64>,
    pub a10: Vec<f64>,
    pub a11: Vec<f64>,
    pub a12: Vec<f64>,
    pub a13: Vec<f64>,
    pub a20: Vec<f64>,
    pub a21: Vec<f64>,
    pub a22: Vec<f64>,
    pub a23: Vec<f64>,
    pub a30: Vec<f64>,
    pub a31: Vec<f64>,
    pub a32: Vec<f64>,
    pub a33: Vec<f64>,
    pub a40: Vec<f64>,
    pub a41: Vec<f64>,
    pub a42: Vec<f64>,
    pub a43: Vec<f64>,
    pub a50: Vec<f64>,
    pub a51: Vec<f64>,
    pub a52: Vec<f64>,
    pub a53: Vec<f64>,
    pub a60: Vec<f64>,
    pub a61: Vec<f64>,
    pub a62: Vec<f64>,
    pub a63: Vec<f64>,
    pub a70: Vec<f64>,
    pub a71: Vec<f64>,
    pub a72: Vec<f64>,
    pub a73: Vec<f64>,
    pub a14: Vec<f64>,
    pub a24: Vec<f64>,
    pub alph: Vec<f64>,
    pub beta: Vec<f64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_RATIO_OF_POLYNOMIALS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_RATIO_OF_POLYNOMIALS").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a10: t.take("A10").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a13: t.take("A13").and_then(|c| c.into_float()).unwrap_or_default(),
            a20: t.take("A20").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a23: t.take("A23").and_then(|c| c.into_float()).unwrap_or_default(),
            a30: t.take("A30").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            a33: t.take("A33").and_then(|c| c.into_float()).unwrap_or_default(),
            a40: t.take("A40").and_then(|c| c.into_float()).unwrap_or_default(),
            a41: t.take("A41").and_then(|c| c.into_float()).unwrap_or_default(),
            a42: t.take("A42").and_then(|c| c.into_float()).unwrap_or_default(),
            a43: t.take("A43").and_then(|c| c.into_float()).unwrap_or_default(),
            a50: t.take("A50").and_then(|c| c.into_float()).unwrap_or_default(),
            a51: t.take("A51").and_then(|c| c.into_float()).unwrap_or_default(),
            a52: t.take("A52").and_then(|c| c.into_float()).unwrap_or_default(),
            a53: t.take("A53").and_then(|c| c.into_float()).unwrap_or_default(),
            a60: t.take("A60").and_then(|c| c.into_float()).unwrap_or_default(),
            a61: t.take("A61").and_then(|c| c.into_float()).unwrap_or_default(),
            a62: t.take("A62").and_then(|c| c.into_float()).unwrap_or_default(),
            a63: t.take("A63").and_then(|c| c.into_float()).unwrap_or_default(),
            a70: t.take("A70").and_then(|c| c.into_float()).unwrap_or_default(),
            a71: t.take("A71").and_then(|c| c.into_float()).unwrap_or_default(),
            a72: t.take("A72").and_then(|c| c.into_float()).unwrap_or_default(),
            a73: t.take("A73").and_then(|c| c.into_float()).unwrap_or_default(),
            a14: t.take("A14").and_then(|c| c.into_float()).unwrap_or_default(),
            a24: t.take("A24").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_SACK_TUESDAY {
    pub eosid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
}
impl EOS_SACK_TUESDAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_SACK_TUESDAY").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_TABULATED {
    pub eosid: Vec<i64>,
    pub gama: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub ev1: Vec<f64>,
    pub ev2: Vec<f64>,
    pub ev3: Vec<f64>,
    pub ev4: Vec<f64>,
    pub ev5: Vec<f64>,
    pub ev6: Vec<f64>,
    pub ev7: Vec<f64>,
    pub ev8: Vec<f64>,
    pub ev9: Vec<f64>,
    pub ev10: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub t9: Vec<f64>,
    pub t10: Vec<f64>,
}
impl EOS_TABULATED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_TABULATED").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            ev1: t.take("EV1").and_then(|c| c.into_float()).unwrap_or_default(),
            ev2: t.take("EV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ev3: t.take("EV3").and_then(|c| c.into_float()).unwrap_or_default(),
            ev4: t.take("EV4").and_then(|c| c.into_float()).unwrap_or_default(),
            ev5: t.take("EV5").and_then(|c| c.into_float()).unwrap_or_default(),
            ev6: t.take("EV6").and_then(|c| c.into_float()).unwrap_or_default(),
            ev7: t.take("EV7").and_then(|c| c.into_float()).unwrap_or_default(),
            ev8: t.take("EV8").and_then(|c| c.into_float()).unwrap_or_default(),
            ev9: t.take("EV9").and_then(|c| c.into_float()).unwrap_or_default(),
            ev10: t.take("EV10").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            t9: t.take("T9").and_then(|c| c.into_float()).unwrap_or_default(),
            t10: t.take("T10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_TABULATED_COMPACTION {
    pub eosid: Vec<i64>,
    pub gama: Vec<f64>,
    pub e0: Vec<f64>,
    pub vo: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lct: Vec<i64>,
    pub lck: Vec<i64>,
    pub lcid: Vec<i64>,
    pub ev1: Vec<f64>,
    pub ev2: Vec<f64>,
    pub ev3: Vec<f64>,
    pub ev4: Vec<f64>,
    pub ev5: Vec<f64>,
    pub ev6: Vec<f64>,
    pub ev7: Vec<f64>,
    pub ev8: Vec<f64>,
    pub ev9: Vec<f64>,
    pub ev10: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub t9: Vec<f64>,
    pub t10: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
    pub k9: Vec<f64>,
    pub k10: Vec<f64>,
}
impl EOS_TABULATED_COMPACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_TABULATED_COMPACTION").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lct: t.take("LCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lck: t.take("LCK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ev1: t.take("EV1").and_then(|c| c.into_float()).unwrap_or_default(),
            ev2: t.take("EV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ev3: t.take("EV3").and_then(|c| c.into_float()).unwrap_or_default(),
            ev4: t.take("EV4").and_then(|c| c.into_float()).unwrap_or_default(),
            ev5: t.take("EV5").and_then(|c| c.into_float()).unwrap_or_default(),
            ev6: t.take("EV6").and_then(|c| c.into_float()).unwrap_or_default(),
            ev7: t.take("EV7").and_then(|c| c.into_float()).unwrap_or_default(),
            ev8: t.take("EV8").and_then(|c| c.into_float()).unwrap_or_default(),
            ev9: t.take("EV9").and_then(|c| c.into_float()).unwrap_or_default(),
            ev10: t.take("EV10").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            t9: t.take("T9").and_then(|c| c.into_float()).unwrap_or_default(),
            t10: t.take("T10").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
            k9: t.take("K9").and_then(|c| c.into_float()).unwrap_or_default(),
            k10: t.take("K10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_TENSOR_PORE_COLLAPSE {
    pub eosid: Vec<i64>,
    pub nld: Vec<i64>,
    pub ncr: Vec<i64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub ie0: Vec<f64>,
    pub ec0: Vec<f64>,
}
impl EOS_TENSOR_PORE_COLLAPSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_TENSOR_PORE_COLLAPSE").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nld: t.take("NLD").and_then(|c| c.into_int()).unwrap_or_default(),
            ncr: t.take("NCR").and_then(|c| c.into_int()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            ie0: t.take("IE0").and_then(|c| c.into_float()).unwrap_or_default(),
            ec0: t.take("EC0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_USER_DEFINED {
    pub eosid: Vec<i64>,
    pub eost: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub ivect: Vec<i64>,
    pub eo: Vec<f64>,
    pub vo: Vec<f64>,
    pub bulk: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl EOS_USER_DEFINED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_USER_DEFINED").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            eost: t.take("EOST").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ivect: t.take("IVECT").and_then(|c| c.into_int()).unwrap_or_default(),
            eo: t.take("EO").and_then(|c| c.into_float()).unwrap_or_default(),
            vo: t.take("VO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct EOS_USER_LIBRARY {
    pub eosid: Vec<i64>,
    pub sesmid: Vec<i64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
}
impl EOS_USER_LIBRARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("EOS_USER_LIBRARY").unwrap());
        Self {
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sesmid: t.take("SESMID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_D3PLOT {
    pub strsn: Vec<i64>,
    pub index: Vec<i64>,
    pub restrt: Vec<i64>,
    pub texpos: Vec<f64>,
    pub dmgmin: Vec<f64>,
    pub filename: Vec<String>,
}
impl FATIGUE_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_D3PLOT").unwrap());
        Self {
            strsn: t.take("STRSN").and_then(|c| c.into_int()).unwrap_or_default(),
            index: t.take("INDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgmin: t.take("DMGMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_ELOUT {
    pub strsn: Vec<i64>,
    pub index: Vec<i64>,
    pub restrt: Vec<i64>,
    pub texpos: Vec<f64>,
    pub dmgmin: Vec<f64>,
    pub filename: Vec<String>,
}
impl FATIGUE_ELOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_ELOUT").unwrap());
        Self {
            strsn: t.take("STRSN").and_then(|c| c.into_int()).unwrap_or_default(),
            index: t.take("INDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgmin: t.take("DMGMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_FAILURE {
    pub ifailure: Vec<i64>,
    pub dratio: Vec<f64>,
}
impl FATIGUE_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_FAILURE").unwrap());
        Self {
            ifailure: t.take("IFAILURE").and_then(|c| c.into_int()).unwrap_or_default(),
            dratio: t.take("DRATIO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_FATIGUE {
    pub pid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub dt: Vec<f64>,
    pub strsn: Vec<i64>,
    pub index: Vec<i64>,
    pub restrt: Vec<i64>,
    pub texpos: Vec<f64>,
    pub dmgmin: Vec<f64>,
    pub filename: Vec<String>,
}
impl FATIGUE_FATIGUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_FATIGUE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            strsn: t.take("STRSN").and_then(|c| c.into_int()).unwrap_or_default(),
            index: t.take("INDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgmin: t.take("DMGMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_LOADSTEP {
    pub tstart: Vec<f64>,
    pub tend: Vec<f64>,
    pub texpos: Vec<f64>,
}
impl FATIGUE_LOADSTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_LOADSTEP").unwrap());
        Self {
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_MEAN_STRESS_CORRECTION {
    pub method: Vec<i64>,
    pub mid: Vec<i64>,
    pub sigma: Vec<f64>,
}
impl FATIGUE_MEAN_STRESS_CORRECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_MEAN_STRESS_CORRECTION").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FATIGUE_MULTIAXIAL {
    pub maxial: Vec<i64>,
    pub nplane: Vec<i64>,
}
impl FATIGUE_MULTIAXIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FATIGUE_MULTIAXIAL").unwrap());
        Self {
            maxial: t.take("MAXIAL").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACCELERATION_UNIT {
    pub unit: Vec<i64>,
    pub umlt: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACCELERATION_UNIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACCELERATION_UNIT").unwrap());
        Self {
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            umlt: t.take("UMLT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_BEM {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsidext: Vec<i64>,
    pub typext: Vec<i64>,
    pub nsidint: Vec<i64>,
    pub typint: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub trslt: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub iunits: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub tolitr: Vec<f64>,
    pub ndd: Vec<i64>,
    pub tollr: Vec<f64>,
    pub tolfct: Vec<f64>,
    pub ibdim: Vec<i64>,
    pub npg: Vec<i64>,
    pub unused: Vec<i64>,
    pub nbc: Vec<i64>,
    pub restrt: Vec<i64>,
    pub iedge: Vec<i64>,
    pub noel: Vec<i64>,
    pub nfrup: Vec<i64>,
    pub velout: Vec<i64>,
    pub dba: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bemtype: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub t_hold: Vec<f64>,
    pub decay: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_BEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_BEM").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidext: t.take("NSIDEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            typext: t.take("TYPEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidint: t.take("NSIDINT").and_then(|c| c.into_int()).unwrap_or_default(),
            typint: t.take("TYPINT").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            trslt: t.take("TRSLT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            iunits: t.take("IUNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tolitr: t.take("TOLITR").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            tollr: t.take("TOLLR").and_then(|c| c.into_float()).unwrap_or_default(),
            tolfct: t.take("TOLFCT").and_then(|c| c.into_float()).unwrap_or_default(),
            ibdim: t.take("IBDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            npg: t.take("NPG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nbc: t.take("NBC").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            iedge: t.take("IEDGE").and_then(|c| c.into_int()).unwrap_or_default(),
            noel: t.take("NOEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nfrup: t.take("NFRUP").and_then(|c| c.into_int()).unwrap_or_default(),
            velout: t.take("VELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bemtype: t.take("BEMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            t_hold: t.take("T_HOLD").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_BEM_ATV {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsidext: Vec<i64>,
    pub typext: Vec<i64>,
    pub nsidint: Vec<i64>,
    pub typint: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub trslt: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub iunits: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub tolitr: Vec<f64>,
    pub ndd: Vec<i64>,
    pub tollr: Vec<f64>,
    pub tolfct: Vec<f64>,
    pub ibdim: Vec<i64>,
    pub npg: Vec<i64>,
    pub unused: Vec<i64>,
    pub nbc: Vec<i64>,
    pub restrt: Vec<i64>,
    pub iedge: Vec<i64>,
    pub noel: Vec<i64>,
    pub nfrup: Vec<i64>,
    pub velout: Vec<i64>,
    pub dba: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bemtype: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub t_hold: Vec<f64>,
    pub decay: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_BEM_ATV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_BEM_ATV").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidext: t.take("NSIDEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            typext: t.take("TYPEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidint: t.take("NSIDINT").and_then(|c| c.into_int()).unwrap_or_default(),
            typint: t.take("TYPINT").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            trslt: t.take("TRSLT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            iunits: t.take("IUNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tolitr: t.take("TOLITR").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            tollr: t.take("TOLLR").and_then(|c| c.into_float()).unwrap_or_default(),
            tolfct: t.take("TOLFCT").and_then(|c| c.into_float()).unwrap_or_default(),
            ibdim: t.take("IBDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            npg: t.take("NPG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nbc: t.take("NBC").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            iedge: t.take("IEDGE").and_then(|c| c.into_int()).unwrap_or_default(),
            noel: t.take("NOEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nfrup: t.take("NFRUP").and_then(|c| c.into_int()).unwrap_or_default(),
            velout: t.take("VELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bemtype: t.take("BEMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            t_hold: t.take("T_HOLD").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_BEM_HALF_SPACE {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsidext: Vec<i64>,
    pub typext: Vec<i64>,
    pub nsidint: Vec<i64>,
    pub typint: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub trslt: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub iunits: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub tolitr: Vec<f64>,
    pub ndd: Vec<i64>,
    pub tollr: Vec<f64>,
    pub tolfct: Vec<f64>,
    pub ibdim: Vec<i64>,
    pub npg: Vec<i64>,
    pub unused: Vec<i64>,
    pub nbc: Vec<i64>,
    pub restrt: Vec<i64>,
    pub iedge: Vec<i64>,
    pub noel: Vec<i64>,
    pub nfrup: Vec<i64>,
    pub velout: Vec<i64>,
    pub dba: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bemtype: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub pid: Vec<i64>,
    pub t_hold: Vec<f64>,
    pub decay: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_BEM_HALF_SPACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_BEM_HALF_SPACE").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidext: t.take("NSIDEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            typext: t.take("TYPEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidint: t.take("NSIDINT").and_then(|c| c.into_int()).unwrap_or_default(),
            typint: t.take("TYPINT").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            trslt: t.take("TRSLT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            iunits: t.take("IUNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tolitr: t.take("TOLITR").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            tollr: t.take("TOLLR").and_then(|c| c.into_float()).unwrap_or_default(),
            tolfct: t.take("TOLFCT").and_then(|c| c.into_float()).unwrap_or_default(),
            ibdim: t.take("IBDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            npg: t.take("NPG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nbc: t.take("NBC").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            iedge: t.take("IEDGE").and_then(|c| c.into_int()).unwrap_or_default(),
            noel: t.take("NOEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nfrup: t.take("NFRUP").and_then(|c| c.into_int()).unwrap_or_default(),
            velout: t.take("VELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bemtype: t.take("BEMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            t_hold: t.take("T_HOLD").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_BEM_MATV {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsidext: Vec<i64>,
    pub typext: Vec<i64>,
    pub nsidint: Vec<i64>,
    pub typint: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub trslt: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub iunits: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub tolitr: Vec<f64>,
    pub ndd: Vec<i64>,
    pub tollr: Vec<f64>,
    pub tolfct: Vec<f64>,
    pub ibdim: Vec<i64>,
    pub npg: Vec<i64>,
    pub unused: Vec<i64>,
    pub nbc: Vec<i64>,
    pub restrt: Vec<i64>,
    pub iedge: Vec<i64>,
    pub noel: Vec<i64>,
    pub nfrup: Vec<i64>,
    pub velout: Vec<i64>,
    pub dba: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bemtype: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub t_hold: Vec<f64>,
    pub decay: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_BEM_MATV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_BEM_MATV").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidext: t.take("NSIDEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            typext: t.take("TYPEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidint: t.take("NSIDINT").and_then(|c| c.into_int()).unwrap_or_default(),
            typint: t.take("TYPINT").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            trslt: t.take("TRSLT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            iunits: t.take("IUNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tolitr: t.take("TOLITR").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            tollr: t.take("TOLLR").and_then(|c| c.into_float()).unwrap_or_default(),
            tolfct: t.take("TOLFCT").and_then(|c| c.into_float()).unwrap_or_default(),
            ibdim: t.take("IBDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            npg: t.take("NPG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nbc: t.take("NBC").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            iedge: t.take("IEDGE").and_then(|c| c.into_int()).unwrap_or_default(),
            noel: t.take("NOEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nfrup: t.take("NFRUP").and_then(|c| c.into_int()).unwrap_or_default(),
            velout: t.take("VELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bemtype: t.take("BEMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            t_hold: t.take("T_HOLD").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_BEM_PANEL_CONTRIBUTION {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub nsidext: Vec<i64>,
    pub typext: Vec<i64>,
    pub nsidint: Vec<i64>,
    pub typint: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub trslt: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub iunits: Vec<i64>,
    pub method: Vec<i64>,
    pub maxit: Vec<i64>,
    pub tolitr: Vec<f64>,
    pub ndd: Vec<i64>,
    pub tollr: Vec<f64>,
    pub tolfct: Vec<f64>,
    pub ibdim: Vec<i64>,
    pub npg: Vec<i64>,
    pub unused: Vec<i64>,
    pub nbc: Vec<i64>,
    pub restrt: Vec<i64>,
    pub iedge: Vec<i64>,
    pub noel: Vec<i64>,
    pub nfrup: Vec<i64>,
    pub velout: Vec<i64>,
    pub dba: Vec<i64>,
    pub ssid: Vec<i64>,
    pub sstype: Vec<i64>,
    pub norm: Vec<i64>,
    pub bemtype: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub nsidpc: Vec<i64>,
    pub t_hold: Vec<f64>,
    pub decay: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_BEM_PANEL_CONTRIBUTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_BEM_PANEL_CONTRIBUTION").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsidext: t.take("NSIDEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            typext: t.take("TYPEXT").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidint: t.take("NSIDINT").and_then(|c| c.into_int()).unwrap_or_default(),
            typint: t.take("TYPINT").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            trslt: t.take("TRSLT").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            iunits: t.take("IUNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tolitr: t.take("TOLITR").and_then(|c| c.into_float()).unwrap_or_default(),
            ndd: t.take("NDD").and_then(|c| c.into_int()).unwrap_or_default(),
            tollr: t.take("TOLLR").and_then(|c| c.into_float()).unwrap_or_default(),
            tolfct: t.take("TOLFCT").and_then(|c| c.into_float()).unwrap_or_default(),
            ibdim: t.take("IBDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            npg: t.take("NPG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nbc: t.take("NBC").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            iedge: t.take("IEDGE").and_then(|c| c.into_int()).unwrap_or_default(),
            noel: t.take("NOEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nfrup: t.take("NFRUP").and_then(|c| c.into_int()).unwrap_or_default(),
            velout: t.take("VELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            sstype: t.take("SSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            bemtype: t.take("BEMTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidpc: t.take("NSIDPC").and_then(|c| c.into_int()).unwrap_or_default(),
            t_hold: t.take("T_HOLD").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FEM {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub unused: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub mixdmp: Vec<i64>,
    pub pid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub vad: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub ipfile: Vec<i64>,
    pub dba: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FEM").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mixdmp: t.take("MIXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ipfile: t.take("IPFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            dba: t.take("DBA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FEM_EIGENVALUE {
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub dtout: Vec<f64>,
    pub tstart: Vec<f64>,
    pub pref: Vec<f64>,
    pub unused: Vec<i64>,
    pub fftwin: Vec<i64>,
    pub mixdmp: Vec<i64>,
    pub pid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub sid: Vec<i64>,
    pub styp: Vec<i64>,
    pub vad: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FEM_EIGENVALUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FEM_EIGENVALUE").unwrap());
        Self {
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            fftwin: t.take("FFTWIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mixdmp: t.take("MIXDMP").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_NODE_SET {
    pub nsid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_NODE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART {
    pub pid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART_SET {
    pub psid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PLATE {
    pub norm: Vec<i64>,
    pub len_x: Vec<f64>,
    pub len_y: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub nelm_x: Vec<i64>,
    pub nelm_y: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PLATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_PLATE").unwrap());
        Self {
            norm: t.take("NORM").and_then(|c| c.into_int()).unwrap_or_default(),
            len_x: t.take("LEN_X").and_then(|c| c.into_float()).unwrap_or_default(),
            len_y: t.take("LEN_Y").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            nelm_x: t.take("NELM_X").and_then(|c| c.into_int()).unwrap_or_default(),
            nelm_y: t.take("NELM_Y").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_SPHERE {
    pub center: Vec<i64>,
    pub r: Vec<f64>,
    pub density: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub half1: Vec<i64>,
    pub half2: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_SPHERE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_FRINGE_PLOT_SPHERE").unwrap());
        Self {
            center: t.take("CENTER").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            half1: t.take("HALF1").and_then(|c| c.into_int()).unwrap_or_default(),
            half2: t.take("HALF2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_INCIDENT_WAVE {
    pub type_: Vec<i64>,
    pub mag: Vec<f64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_INCIDENT_WAVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_INCIDENT_WAVE").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mag: t.take("MAG").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_ACOUSTIC_SOUND_SPEED {
    pub id: Vec<i64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
}
impl FREQUENCY_DOMAIN_ACOUSTIC_SOUND_SPEED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_ACOUSTIC_SOUND_SPEED").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_FRF {
    pub n1: Vec<i64>,
    pub n1typ: Vec<i64>,
    pub dof1: Vec<i64>,
    pub vad1: Vec<i64>,
    pub vid1: Vec<i64>,
    pub fnmax: Vec<f64>,
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub n2: Vec<i64>,
    pub n2typ: Vec<i64>,
    pub dof2: Vec<i64>,
    pub vad2: Vec<i64>,
    pub vid2: Vec<i64>,
    pub relatv: Vec<i64>,
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub fspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
    pub restrt: Vec<i64>,
    pub output: Vec<i64>,
}
impl FREQUENCY_DOMAIN_FRF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_FRF").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n1typ: t.take("N1TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof1: t.take("DOF1").and_then(|c| c.into_int()).unwrap_or_default(),
            vad1: t.take("VAD1").and_then(|c| c.into_int()).unwrap_or_default(),
            vid1: t.take("VID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n2typ: t.take("N2TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof2: t.take("DOF2").and_then(|c| c.into_int()).unwrap_or_default(),
            vad2: t.take("VAD2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid2: t.take("VID2").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            fspace: t.take("FSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_NODE_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_NODE_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_NODE_SET_EXCLUDE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_NODE_SET_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_NODE_SET_EXCLUDE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_PART {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_PART").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_PART_EXCLUDE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_PART_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_PART_EXCLUDE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_PART_SET {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_PART_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_LOCAL_PART_SET_EXCLUDE {
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_LOCAL_PART_SET_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_LOCAL_PART_SET_EXCLUDE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_GENERATE {
    pub m1beg: Vec<i64>,
    pub m1end: Vec<i64>,
    pub m2beg: Vec<i64>,
    pub m2end: Vec<i64>,
    pub m3beg: Vec<i64>,
    pub m3end: Vec<i64>,
    pub m4beg: Vec<i64>,
    pub m4end: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_GENERATE").unwrap());
        Self {
            m1beg: t.take("M1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m1end: t.take("M1END").and_then(|c| c.into_int()).unwrap_or_default(),
            m2beg: t.take("M2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m2end: t.take("M2END").and_then(|c| c.into_int()).unwrap_or_default(),
            m3beg: t.take("M3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m3end: t.take("M3END").and_then(|c| c.into_int()).unwrap_or_default(),
            m4beg: t.take("M4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m4end: t.take("M4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_GENERATE_EXCLUDE {
    pub m1beg: Vec<i64>,
    pub m1end: Vec<i64>,
    pub m2beg: Vec<i64>,
    pub m2end: Vec<i64>,
    pub m3beg: Vec<i64>,
    pub m3end: Vec<i64>,
    pub m4beg: Vec<i64>,
    pub m4end: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_GENERATE_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_GENERATE_EXCLUDE").unwrap());
        Self {
            m1beg: t.take("M1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m1end: t.take("M1END").and_then(|c| c.into_int()).unwrap_or_default(),
            m2beg: t.take("M2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m2end: t.take("M2END").and_then(|c| c.into_int()).unwrap_or_default(),
            m3beg: t.take("M3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m3end: t.take("M3END").and_then(|c| c.into_int()).unwrap_or_default(),
            m4beg: t.take("M4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m4end: t.take("M4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_LIST {
    pub mid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub mid3: Vec<i64>,
    pub mid4: Vec<i64>,
    pub mid5: Vec<i64>,
    pub mid6: Vec<i64>,
    pub mid7: Vec<i64>,
    pub mid8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_LIST").unwrap());
        Self {
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            mid5: t.take("MID5").and_then(|c| c.into_int()).unwrap_or_default(),
            mid6: t.take("MID6").and_then(|c| c.into_int()).unwrap_or_default(),
            mid7: t.take("MID7").and_then(|c| c.into_int()).unwrap_or_default(),
            mid8: t.take("MID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_LIST_EXCLUDE {
    pub mid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub mid3: Vec<i64>,
    pub mid4: Vec<i64>,
    pub mid5: Vec<i64>,
    pub mid6: Vec<i64>,
    pub mid7: Vec<i64>,
    pub mid8: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_LIST_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_LIST_EXCLUDE").unwrap());
        Self {
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            mid5: t.take("MID5").and_then(|c| c.into_int()).unwrap_or_default(),
            mid6: t.take("MID6").and_then(|c| c.into_int()).unwrap_or_default(),
            mid7: t.take("MID7").and_then(|c| c.into_int()).unwrap_or_default(),
            mid8: t.take("MID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION {
    pub nmsort: Vec<i64>,
    pub dskip: Vec<f64>,
}
impl FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION").unwrap());
        Self {
            nmsort: t.take("NMSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            dskip: t.take("DSKIP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION_EXCLUDE {
    pub nmsort: Vec<i64>,
    pub dskip: Vec<f64>,
}
impl FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_LOAD_PROJECTION_EXCLUDE").unwrap());
        Self {
            nmsort: t.take("NMSORT").and_then(|c| c.into_int()).unwrap_or_default(),
            dskip: t.take("DSKIP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT {
    pub dskip: Vec<f64>,
}
impl FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT").unwrap());
        Self {
            dskip: t.take("DSKIP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT_EXCLUDE {
    pub dskip: Vec<f64>,
}
impl FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_MODAL_COEFFICIENT_EXCLUDE").unwrap());
        Self {
            dskip: t.take("DSKIP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_SET {
    pub sid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_MODE_SET_EXCLUDE {
    pub sid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_MODE_SET_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_MODE_SET_EXCLUDE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_PATH {
    pub filename: Vec<String>,
}
impl FREQUENCY_DOMAIN_PATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_PATH").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_PATH_NOJOBID {
    pub filename: Vec<String>,
}
impl FREQUENCY_DOMAIN_PATH_NOJOBID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_PATH_NOJOBID").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_PATH_PARTITION {
    pub fbeg: Vec<f64>,
    pub fend: Vec<f64>,
    pub filename: Vec<String>,
}
impl FREQUENCY_DOMAIN_PATH_PARTITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_PATH_PARTITION").unwrap());
        Self {
            fbeg: t.take("FBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            fend: t.take("FEND").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_RANDOM_VIBRATION {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restrt: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmptyp: Vec<i64>,
    pub vaflag: Vec<i64>,
    pub method: Vec<i64>,
    pub unit: Vec<i64>,
    pub umlt: Vec<f64>,
    pub vapsd: Vec<i64>,
    pub varms: Vec<i64>,
    pub napsd: Vec<i64>,
    pub ncpsd: Vec<i64>,
    pub ldtyp: Vec<i64>,
    pub ipanelu: Vec<i64>,
    pub ipanelv: Vec<i64>,
    pub temper: Vec<f64>,
    pub unused_3: Vec<i64>,
    pub ldflag: Vec<i64>,
    pub icoarse: Vec<i64>,
    pub tcoarse: Vec<f64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub dof: Vec<i64>,
    pub ldpsd: Vec<i64>,
    pub ldvel: Vec<i64>,
    pub ldflw: Vec<i64>,
    pub ldspn: Vec<i64>,
    pub cid: Vec<i64>,
    pub load_i: Vec<i64>,
    pub load_j: Vec<i64>,
    pub lctyp2: Vec<i64>,
    pub ldpsd1: Vec<i64>,
    pub ldpsd2: Vec<i64>,
}
impl FREQUENCY_DOMAIN_RANDOM_VIBRATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_RANDOM_VIBRATION").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmptyp: t.take("DMPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vaflag: t.take("VAFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            umlt: t.take("UMLT").and_then(|c| c.into_float()).unwrap_or_default(),
            vapsd: t.take("VAPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            varms: t.take("VARMS").and_then(|c| c.into_int()).unwrap_or_default(),
            napsd: t.take("NAPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ncpsd: t.take("NCPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ldtyp: t.take("LDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelu: t.take("IPANELU").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelv: t.take("IPANELV").and_then(|c| c.into_int()).unwrap_or_default(),
            temper: t.take("TEMPER").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ldflag: t.take("LDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            icoarse: t.take("ICOARSE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcoarse: t.take("TCOARSE").and_then(|c| c.into_float()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd: t.take("LDPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ldvel: t.take("LDVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            ldflw: t.take("LDFLW").and_then(|c| c.into_int()).unwrap_or_default(),
            ldspn: t.take("LDSPN").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            load_i: t.take("LOAD_I").and_then(|c| c.into_int()).unwrap_or_default(),
            load_j: t.take("LOAD_J").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp2: t.take("LCTYP2").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd1: t.take("LDPSD1").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd2: t.take("LDPSD2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_RANDOM_VIBRATION_FATIGUE {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restrt: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmptyp: Vec<i64>,
    pub vaflag: Vec<i64>,
    pub method: Vec<i64>,
    pub unit: Vec<i64>,
    pub umlt: Vec<f64>,
    pub vapsd: Vec<i64>,
    pub varms: Vec<i64>,
    pub napsd: Vec<i64>,
    pub ncpsd: Vec<i64>,
    pub ldtyp: Vec<i64>,
    pub ipanelu: Vec<i64>,
    pub ipanelv: Vec<i64>,
    pub temper: Vec<f64>,
    pub unused_3: Vec<i64>,
    pub ldflag: Vec<i64>,
    pub icoarse: Vec<i64>,
    pub tcoarse: Vec<f64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub dof: Vec<i64>,
    pub ldpsd: Vec<i64>,
    pub ldvel: Vec<i64>,
    pub ldflw: Vec<i64>,
    pub ldspn: Vec<i64>,
    pub cid: Vec<i64>,
    pub load_i: Vec<i64>,
    pub load_j: Vec<i64>,
    pub lctyp2: Vec<i64>,
    pub ldpsd1: Vec<i64>,
    pub ldpsd2: Vec<i64>,
    pub mftg: Vec<i64>,
    pub nftg: Vec<i64>,
    pub sntype: Vec<i64>,
    pub texpos: Vec<f64>,
    pub strsf: Vec<f64>,
    pub inftg: Vec<i64>,
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub ltype: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub sthres: Vec<f64>,
    pub snlimt: Vec<i64>,
    pub filename: Vec<String>,
}
impl FREQUENCY_DOMAIN_RANDOM_VIBRATION_FATIGUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_RANDOM_VIBRATION_FATIGUE").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmptyp: t.take("DMPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            vaflag: t.take("VAFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            umlt: t.take("UMLT").and_then(|c| c.into_float()).unwrap_or_default(),
            vapsd: t.take("VAPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            varms: t.take("VARMS").and_then(|c| c.into_int()).unwrap_or_default(),
            napsd: t.take("NAPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ncpsd: t.take("NCPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ldtyp: t.take("LDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelu: t.take("IPANELU").and_then(|c| c.into_int()).unwrap_or_default(),
            ipanelv: t.take("IPANELV").and_then(|c| c.into_int()).unwrap_or_default(),
            temper: t.take("TEMPER").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            ldflag: t.take("LDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            icoarse: t.take("ICOARSE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcoarse: t.take("TCOARSE").and_then(|c| c.into_float()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd: t.take("LDPSD").and_then(|c| c.into_int()).unwrap_or_default(),
            ldvel: t.take("LDVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            ldflw: t.take("LDFLW").and_then(|c| c.into_int()).unwrap_or_default(),
            ldspn: t.take("LDSPN").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            load_i: t.take("LOAD_I").and_then(|c| c.into_int()).unwrap_or_default(),
            load_j: t.take("LOAD_J").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp2: t.take("LCTYP2").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd1: t.take("LDPSD1").and_then(|c| c.into_int()).unwrap_or_default(),
            ldpsd2: t.take("LDPSD2").and_then(|c| c.into_int()).unwrap_or_default(),
            mftg: t.take("MFTG").and_then(|c| c.into_int()).unwrap_or_default(),
            nftg: t.take("NFTG").and_then(|c| c.into_int()).unwrap_or_default(),
            sntype: t.take("SNTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
            strsf: t.take("STRSF").and_then(|c| c.into_float()).unwrap_or_default(),
            inftg: t.take("INFTG").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ltype: t.take("LTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            sthres: t.take("STHRES").and_then(|c| c.into_float()).unwrap_or_default(),
            snlimt: t.take("SNLIMT").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_RESPONSE_SPECTRUM {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restrt: Vec<i64>,
    pub mcomb: Vec<i64>,
    pub relatv: Vec<i64>,
    pub mprs: Vec<i64>,
    pub mcomb1: Vec<i64>,
    pub mcomb2: Vec<i64>,
    pub w1: Vec<f64>,
    pub w1_2: Vec<f64>,
    pub r40: Vec<f64>,
    pub dampf: Vec<f64>,
    pub lcdamp: Vec<i64>,
    pub ldtyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub lctyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub lc_tbid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub lnid: Vec<i64>,
    pub lntyp: Vec<i64>,
    pub inflag: Vec<i64>,
}
impl FREQUENCY_DOMAIN_RESPONSE_SPECTRUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_RESPONSE_SPECTRUM").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb: t.take("MCOMB").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            mprs: t.take("MPRS").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb1: t.take("MCOMB1").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb2: t.take("MCOMB2").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w1_2: t.take("W1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            r40: t.take("R40").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdamp: t.take("LCDAMP").and_then(|c| c.into_int()).unwrap_or_default(),
            ldtyp: t.take("LDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            lnid: t.take("LNID").and_then(|c| c.into_int()).unwrap_or_default(),
            lntyp: t.take("LNTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            inflag: t.take("INFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_RESPONSE_SPECTRUM_DDAM {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restrt: Vec<i64>,
    pub mcomb: Vec<i64>,
    pub relatv: Vec<i64>,
    pub mprs: Vec<i64>,
    pub mcomb1: Vec<i64>,
    pub mcomb2: Vec<i64>,
    pub w1: Vec<f64>,
    pub w1_2: Vec<f64>,
    pub r40: Vec<f64>,
    pub dampf: Vec<f64>,
    pub lcdamp: Vec<i64>,
    pub ldtyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub std: Vec<i64>,
    pub unit: Vec<i64>,
    pub amin: Vec<f64>,
    pub vid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub effmas: Vec<f64>,
    pub shptyp: Vec<i64>,
    pub mount: Vec<i64>,
    pub movemt: Vec<i64>,
    pub mattyp: Vec<i64>,
}
impl FREQUENCY_DOMAIN_RESPONSE_SPECTRUM_DDAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_RESPONSE_SPECTRUM_DDAM").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restrt: t.take("RESTRT").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb: t.take("MCOMB").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            mprs: t.take("MPRS").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb1: t.take("MCOMB1").and_then(|c| c.into_int()).unwrap_or_default(),
            mcomb2: t.take("MCOMB2").and_then(|c| c.into_int()).unwrap_or_default(),
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w1_2: t.take("W1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            r40: t.take("R40").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdamp: t.take("LCDAMP").and_then(|c| c.into_int()).unwrap_or_default(),
            ldtyp: t.take("LDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            std: t.take("STD").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            amin: t.take("AMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            effmas: t.take("EFFMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            shptyp: t.take("SHPTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            mount: t.take("MOUNT").and_then(|c| c.into_int()).unwrap_or_default(),
            movemt: t.take("MOVEMT").and_then(|c| c.into_int()).unwrap_or_default(),
            mattyp: t.take("MATTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SEA_CONNECTION {
    pub conid: Vec<i64>,
    pub ctype: Vec<i64>,
    pub nsub: Vec<i64>,
    pub ibeam: Vec<i64>,
    pub sub1: Vec<i64>,
    pub sub2: Vec<i64>,
    pub sub3: Vec<i64>,
    pub sub4: Vec<i64>,
    pub sub5: Vec<i64>,
    pub sub6: Vec<i64>,
    pub sub7: Vec<i64>,
    pub sub8: Vec<i64>,
    pub ang1: Vec<f64>,
    pub ang2: Vec<f64>,
    pub ang3: Vec<f64>,
    pub ang4: Vec<f64>,
    pub ang5: Vec<f64>,
    pub ang6: Vec<f64>,
    pub ang7: Vec<f64>,
    pub ang8: Vec<f64>,
    pub length: Vec<f64>,
    pub absorb: Vec<f64>,
    pub thick: Vec<f64>,
}
impl FREQUENCY_DOMAIN_SEA_CONNECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SEA_CONNECTION").unwrap());
        Self {
            conid: t.take("CONID").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nsub: t.take("NSUB").and_then(|c| c.into_int()).unwrap_or_default(),
            ibeam: t.take("IBEAM").and_then(|c| c.into_int()).unwrap_or_default(),
            sub1: t.take("SUB1").and_then(|c| c.into_int()).unwrap_or_default(),
            sub2: t.take("SUB2").and_then(|c| c.into_int()).unwrap_or_default(),
            sub3: t.take("SUB3").and_then(|c| c.into_int()).unwrap_or_default(),
            sub4: t.take("SUB4").and_then(|c| c.into_int()).unwrap_or_default(),
            sub5: t.take("SUB5").and_then(|c| c.into_int()).unwrap_or_default(),
            sub6: t.take("SUB6").and_then(|c| c.into_int()).unwrap_or_default(),
            sub7: t.take("SUB7").and_then(|c| c.into_int()).unwrap_or_default(),
            sub8: t.take("SUB8").and_then(|c| c.into_int()).unwrap_or_default(),
            ang1: t.take("ANG1").and_then(|c| c.into_float()).unwrap_or_default(),
            ang2: t.take("ANG2").and_then(|c| c.into_float()).unwrap_or_default(),
            ang3: t.take("ANG3").and_then(|c| c.into_float()).unwrap_or_default(),
            ang4: t.take("ANG4").and_then(|c| c.into_float()).unwrap_or_default(),
            ang5: t.take("ANG5").and_then(|c| c.into_float()).unwrap_or_default(),
            ang6: t.take("ANG6").and_then(|c| c.into_float()).unwrap_or_default(),
            ang7: t.take("ANG7").and_then(|c| c.into_float()).unwrap_or_default(),
            ang8: t.take("ANG8").and_then(|c| c.into_float()).unwrap_or_default(),
            length: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            absorb: t.take("ABSORB").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SEA_INPUT {
    pub subid: Vec<i64>,
    pub subtyp: Vec<i64>,
    pub loadtyp: Vec<i64>,
    pub bwave: Vec<f64>,
    pub lwave: Vec<f64>,
    pub swave: Vec<f64>,
    pub twave: Vec<f64>,
}
impl FREQUENCY_DOMAIN_SEA_INPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SEA_INPUT").unwrap());
        Self {
            subid: t.take("SUBID").and_then(|c| c.into_int()).unwrap_or_default(),
            subtyp: t.take("SUBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            loadtyp: t.take("LOADTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            bwave: t.take("BWAVE").and_then(|c| c.into_float()).unwrap_or_default(),
            lwave: t.take("LWAVE").and_then(|c| c.into_float()).unwrap_or_default(),
            swave: t.take("SWAVE").and_then(|c| c.into_float()).unwrap_or_default(),
            twave: t.take("TWAVE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SEA_SUBSYSTEM {
    pub fmin: Vec<f64>,
    pub fmax: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub nfspace: Vec<i64>,
    pub lcfreq: Vec<i64>,
    pub iread: Vec<i64>,
    pub subid: Vec<i64>,
    pub subtyp: Vec<i64>,
    pub density: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub output: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub a: Vec<f64>,
    pub perim: Vec<f64>,
    pub thick: Vec<f64>,
    pub width: Vec<f64>,
    pub length: Vec<f64>,
    pub unused_3: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub dampb: Vec<f64>,
    pub dampl: Vec<f64>,
    pub damps: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub unused_2_3: Vec<i64>,
    pub a_2: Vec<f64>,
    pub perim_2: Vec<f64>,
    pub volume: Vec<f64>,
    pub width_2: Vec<f64>,
    pub length_2: Vec<f64>,
    pub height: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub unused_2_4: Vec<i64>,
    pub dampb_2: Vec<f64>,
    pub lc1_2: Vec<i64>,
    pub unused_6: Vec<f64>,
    pub unused_2_5: Vec<f64>,
    pub unused_3_3: Vec<f64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5_2: Vec<i64>,
    pub unused_6_2: Vec<i64>,
    pub a_3: Vec<f64>,
    pub iss: Vec<f64>,
    pub itt: Vec<f64>,
    pub j: Vec<f64>,
    pub length_3: Vec<f64>,
    pub unused_7: Vec<i64>,
    pub unused_2_6: Vec<i64>,
    pub unused_3_4: Vec<i64>,
    pub dampb_3: Vec<f64>,
    pub dampl_2: Vec<f64>,
    pub damps_2: Vec<f64>,
    pub dampt: Vec<f64>,
    pub lc1_3: Vec<i64>,
    pub lc2_2: Vec<i64>,
    pub lc3_2: Vec<i64>,
    pub lc4: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SEA_SUBSYSTEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SEA_SUBSYSTEM").unwrap());
        Self {
            fmin: t.take("FMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            nfspace: t.take("NFSPACE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfreq: t.take("LCFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            iread: t.take("IREAD").and_then(|c| c.into_int()).unwrap_or_default(),
            subid: t.take("SUBID").and_then(|c| c.into_int()).unwrap_or_default(),
            subtyp: t.take("SUBTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            perim: t.take("PERIM").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            width: t.take("WIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            length: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            dampb: t.take("DAMPB").and_then(|c| c.into_float()).unwrap_or_default(),
            dampl: t.take("DAMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            damps: t.take("DAMPS").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_3: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            a_2: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            perim_2: t.take("PERIM").and_then(|c| c.into_float()).unwrap_or_default(),
            volume: t.take("VOLUME").and_then(|c| c.into_float()).unwrap_or_default(),
            width_2: t.take("WIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            length_2: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_4: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            dampb_2: t.take("DAMPB").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_2: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_5: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_3: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5_2: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6_2: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            a_3: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            iss: t.take("ISS").and_then(|c| c.into_float()).unwrap_or_default(),
            itt: t.take("ITT").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            length_3: t.take("LENGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_6: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_4: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            dampb_3: t.take("DAMPB").and_then(|c| c.into_float()).unwrap_or_default(),
            dampl_2: t.take("DAMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            damps_2: t.take("DAMPS").and_then(|c| c.into_float()).unwrap_or_default(),
            dampt: t.take("DAMPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1_3: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2_2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3_2: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_DIRECT {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_DIRECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_DIRECT").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_DIRECT_FREQUENCY_DEPENDENT {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_DIRECT_FREQUENCY_DEPENDENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_DIRECT_FREQUENCY_DEPENDENT").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_ERP {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub erprlf: Vec<f64>,
    pub erpref: Vec<f64>,
    pub radeff: Vec<i64>,
    pub pid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_ERP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_ERP").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            erprlf: t.take("ERPRLF").and_then(|c| c.into_float()).unwrap_or_default(),
            erpref: t.take("ERPREF").and_then(|c| c.into_float()).unwrap_or_default(),
            radeff: t.take("RADEFF").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_FATIGUE {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub lcftg: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_FATIGUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_FATIGUE").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcftg: t.take("LCFTG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_FRF {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_FRF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_FRF").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct FREQUENCY_DOMAIN_SSD_SUBCASE {
    pub mdmin: Vec<i64>,
    pub mdmax: Vec<i64>,
    pub fnmin: Vec<f64>,
    pub fnmax: Vec<f64>,
    pub restmd: Vec<i64>,
    pub restdp: Vec<i64>,
    pub lcflag: Vec<i64>,
    pub relatv: Vec<i64>,
    pub dampf: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub lctyp: Vec<i64>,
    pub dmpmas: Vec<f64>,
    pub dmpstf: Vec<f64>,
    pub dmpflg: Vec<i64>,
    pub unused: Vec<i64>,
    pub istress: Vec<i64>,
    pub memory: Vec<i64>,
    pub nerp: Vec<i64>,
    pub strtyp: Vec<i64>,
    pub nout: Vec<i64>,
    pub notyp: Vec<i64>,
    pub nova: Vec<i64>,
    pub caseid: Vec<String>,
    pub title: Vec<String>,
    pub nload: Vec<i64>,
    pub nid: Vec<i64>,
    pub ntyp: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
}
impl FREQUENCY_DOMAIN_SSD_SUBCASE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("FREQUENCY_DOMAIN_SSD_SUBCASE").unwrap());
        Self {
            mdmin: t.take("MDMIN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdmax: t.take("MDMAX").and_then(|c| c.into_int()).unwrap_or_default(),
            fnmin: t.take("FNMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            fnmax: t.take("FNMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            restmd: t.take("RESTMD").and_then(|c| c.into_int()).unwrap_or_default(),
            restdp: t.take("RESTDP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcflag: t.take("LCFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            relatv: t.take("RELATV").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctyp: t.take("LCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dmpmas: t.take("DMPMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpstf: t.take("DMPSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmpflg: t.take("DMPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            istress: t.take("ISTRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            memory: t.take("MEMORY").and_then(|c| c.into_int()).unwrap_or_default(),
            nerp: t.take("NERP").and_then(|c| c.into_int()).unwrap_or_default(),
            strtyp: t.take("STRTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            notyp: t.take("NOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            nova: t.take("NOVA").and_then(|c| c.into_int()).unwrap_or_default(),
            caseid: t.take("CASEID").and_then(|c| c.into_str()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nload: t.take("NLOAD").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ntyp: t.take("NTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct HOURGLASS_HOURGLASS {
    pub hgid: Vec<i64>,
    pub ihq: Vec<i64>,
    pub qm: Vec<f64>,
    pub ibq: Vec<i64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub qb_vdc: Vec<f64>,
    pub qw: Vec<f64>,
}
impl HOURGLASS_HOURGLASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("HOURGLASS_HOURGLASS").unwrap());
        Self {
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            ihq: t.take("IHQ").and_then(|c| c.into_int()).unwrap_or_default(),
            qm: t.take("QM").and_then(|c| c.into_float()).unwrap_or_default(),
            ibq: t.take("IBQ").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            qb_vdc: t.take("QB/VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            qw: t.take("QW").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_CONJ_HEAT {
    pub pid: Vec<i64>,
    pub ctype: Vec<i64>,
    pub val: Vec<f64>,
    pub sflcid: Vec<f64>,
}
impl ICFD_BOUNDARY_CONJ_HEAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_CONJ_HEAT").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_float()).unwrap_or_default(),
            sflcid: t.take("SFLCID").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_CONVECTION_TEMP {
    pub pid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub hsf: Vec<f64>,
    pub tblcid: Vec<i64>,
    pub tbsf: Vec<f64>,
}
impl ICFD_BOUNDARY_CONVECTION_TEMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_CONVECTION_TEMP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hsf: t.take("HSF").and_then(|c| c.into_float()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbsf: t.take("TBSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FLUX_TEMP {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
}
impl ICFD_BOUNDARY_FLUX_TEMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FLUX_TEMP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FREESLIP {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_FREESLIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FREESLIP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FSI {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FSI").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FSI_EXCLUDE {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_FSI_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FSI_EXCLUDE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FSI_FIXED {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_FSI_FIXED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FSI_FIXED").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_FSWAVE {
    pub pid: Vec<i64>,
    pub wtype: Vec<i64>,
    pub h0: Vec<f64>,
    pub wamp: Vec<f64>,
    pub wleng: Vec<f64>,
    pub wmax: Vec<f64>,
    pub sflcid: Vec<i64>,
    pub wang: Vec<f64>,
    pub wpeak: Vec<f64>,
}
impl ICFD_BOUNDARY_FSWAVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_FSWAVE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            wtype: t.take("WTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            wamp: t.take("WAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            wleng: t.take("WLENG").and_then(|c| c.into_float()).unwrap_or_default(),
            wmax: t.take("WMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sflcid: t.take("SFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            wang: t.take("WANG").and_then(|c| c.into_float()).unwrap_or_default(),
            wpeak: t.take("WPEAK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_GROUND {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_GROUND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_GROUND").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_NAVIERSLIP {
    pub pid: Vec<i64>,
    pub fric: Vec<f64>,
}
impl ICFD_BOUNDARY_NAVIERSLIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_NAVIERSLIP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_NONSLIP {
    pub pid: Vec<i64>,
}
impl ICFD_BOUNDARY_NONSLIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_NONSLIP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PERIODIC {
    pub pid: Vec<i64>,
    pub ptype: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pdlcid: Vec<i64>,
    pub axe: Vec<i64>,
    pub ptid: Vec<i64>,
    pub angle: Vec<i64>,
}
impl ICFD_BOUNDARY_PERIODIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PERIODIC").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype: t.take("PTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pdlcid: t.take("PDLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            axe: t.take("AXE").and_then(|c| c.into_int()).unwrap_or_default(),
            ptid: t.take("PTID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_LEVELSET {
    pub pid: Vec<i64>,
    pub ptid: Vec<i64>,
    pub axe: Vec<i64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_LEVELSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_LEVELSET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptid: t.take("PTID").and_then(|c| c.into_int()).unwrap_or_default(),
            axe: t.take("AXE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_MOVEMESH {
    pub pid: Vec<i64>,
    pub dofx: Vec<i64>,
    pub dofy: Vec<i64>,
    pub dofz: Vec<i64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_MOVEMESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_MOVEMESH").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofx: t.take("dofx").and_then(|c| c.into_int()).unwrap_or_default(),
            dofy: t.take("dofy").and_then(|c| c.into_int()).unwrap_or_default(),
            dofz: t.take("dofz").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_PRE {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_PRE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_PRE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_TEMP {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_TEMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_TEMP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_TURBULENCE {
    pub pid: Vec<i64>,
    pub vtype: Vec<i64>,
    pub imp: Vec<i64>,
    pub lcid: Vec<i64>,
    pub ks: Vec<f64>,
    pub cs: Vec<f64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_TURBULENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_TURBULENCE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vtype: t.take("VTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            imp: t.take("IMP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ks: t.take("KS").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_PRESCRIBED_VEL {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub vad: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub vid: Vec<i64>,
    pub death: Vec<f64>,
    pub birth: Vec<f64>,
}
impl ICFD_BOUNDARY_PRESCRIBED_VEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_PRESCRIBED_VEL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            vad: t.take("VAD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_BOUNDARY_WINDKESSEL {
    pub pid: Vec<i64>,
    pub wtype: Vec<i64>,
    pub r1: Vec<f64>,
    pub c1: Vec<f64>,
    pub r2: Vec<f64>,
    pub l1: Vec<f64>,
}
impl ICFD_BOUNDARY_WINDKESSEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_BOUNDARY_WINDKESSEL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            wtype: t.take("WTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_ADAPT {
    pub minh: Vec<f64>,
    pub maxh: Vec<f64>,
    pub err: Vec<f64>,
    pub mth: Vec<i64>,
    pub nit: Vec<i64>,
    pub var: Vec<i64>,
    pub unused: Vec<i64>,
    pub kis: Vec<i64>,
}
impl ICFD_CONTROL_ADAPT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_ADAPT").unwrap());
        Self {
            minh: t.take("MINH").and_then(|c| c.into_float()).unwrap_or_default(),
            maxh: t.take("MAXH").and_then(|c| c.into_float()).unwrap_or_default(),
            err: t.take("ERR").and_then(|c| c.into_float()).unwrap_or_default(),
            mth: t.take("MTH").and_then(|c| c.into_int()).unwrap_or_default(),
            nit: t.take("NIT").and_then(|c| c.into_int()).unwrap_or_default(),
            var: t.take("VAR").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            kis: t.take("KIS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_ADAPT_SIZE {
    pub asize: Vec<i64>,
    pub nit: Vec<i64>,
}
impl ICFD_CONTROL_ADAPT_SIZE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_ADAPT_SIZE").unwrap());
        Self {
            asize: t.take("ASIZE").and_then(|c| c.into_int()).unwrap_or_default(),
            nit: t.take("NIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_CONJ {
    pub ctype: Vec<i64>,
    pub f10: Vec<i64>,
    pub f20: Vec<i64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub f60: Vec<i64>,
    pub tsf: Vec<f64>,
}
impl ICFD_CONTROL_CONJ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_CONJ").unwrap());
        Self {
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            tsf: t.take("TSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_DEM_COUPLING {
    pub ctype: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub sf: Vec<f64>,
    pub maxvel: Vec<f64>,
    pub dtype: Vec<i64>,
}
impl ICFD_CONTROL_DEM_COUPLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_DEM_COUPLING").unwrap());
        Self {
            ctype: t.take("CTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            maxvel: t.take("MAXVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtype: t.take("DTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_EMBEDSHELL {
    pub gtype: Vec<i64>,
    pub dist: Vec<f64>,
    pub tps: Vec<i64>,
}
impl ICFD_CONTROL_EMBEDSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_EMBEDSHELL").unwrap());
        Self {
            gtype: t.take("GTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            tps: t.take("TPS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_FSI {
    pub owc: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub idc: Vec<f64>,
    pub lcidsf: Vec<i64>,
    pub xproj: Vec<i64>,
    pub nsub: Vec<i64>,
}
impl ICFD_CONTROL_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_FSI").unwrap());
        Self {
            owc: t.take("OWC").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            idc: t.take("IDC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidsf: t.take("LCIDSF").and_then(|c| c.into_int()).unwrap_or_default(),
            xproj: t.take("XPROJ").and_then(|c| c.into_int()).unwrap_or_default(),
            nsub: t.take("NSUB").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_GENERAL {
    pub atype: Vec<i64>,
    pub mtype: Vec<i64>,
    pub dvcl: Vec<i64>,
    pub rdvcl: Vec<i64>,
}
impl ICFD_CONTROL_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_GENERAL").unwrap());
        Self {
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dvcl: t.take("DVCL").and_then(|c| c.into_int()).unwrap_or_default(),
            rdvcl: t.take("RDVCL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_IMPOSED_MOVE {
    pub pid: Vec<i64>,
    pub lcvx: Vec<i64>,
    pub lcvy: Vec<i64>,
    pub lcvz: Vec<i64>,
    pub vadt: Vec<i64>,
    pub alphal: Vec<i64>,
    pub betal: Vec<i64>,
    pub gammal: Vec<i64>,
    pub alphag: Vec<i64>,
    pub betag: Vec<i64>,
    pub gammag: Vec<i64>,
    pub vadr: Vec<i64>,
    pub ptid: Vec<i64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub ptido: Vec<i64>,
    pub axe: Vec<i64>,
    pub ptidv: Vec<i64>,
}
impl ICFD_CONTROL_IMPOSED_MOVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_IMPOSED_MOVE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvx: t.take("LCVX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvy: t.take("LCVY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcvz: t.take("LCVZ").and_then(|c| c.into_int()).unwrap_or_default(),
            vadt: t.take("VADT").and_then(|c| c.into_int()).unwrap_or_default(),
            alphal: t.take("ALPHAL").and_then(|c| c.into_int()).unwrap_or_default(),
            betal: t.take("BETAL").and_then(|c| c.into_int()).unwrap_or_default(),
            gammal: t.take("GAMMAL").and_then(|c| c.into_int()).unwrap_or_default(),
            alphag: t.take("ALPHAG").and_then(|c| c.into_int()).unwrap_or_default(),
            betag: t.take("BETAG").and_then(|c| c.into_int()).unwrap_or_default(),
            gammag: t.take("GAMMAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vadr: t.take("VADR").and_then(|c| c.into_int()).unwrap_or_default(),
            ptid: t.take("PTID").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            ptido: t.take("PTIDO").and_then(|c| c.into_int()).unwrap_or_default(),
            axe: t.take("AXE").and_then(|c| c.into_int()).unwrap_or_default(),
            ptidv: t.take("PTIDV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_LOAD {
    pub abl: Vec<i64>,
}
impl ICFD_CONTROL_LOAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_LOAD").unwrap());
        Self {
            abl: t.take("ABL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_MESH {
    pub mgsf: Vec<f64>,
    pub f: Vec<i64>,
    pub mstrat: Vec<i64>,
    pub f_2dstruc: Vec<i64>,
    pub nrmsh: Vec<i64>,
    pub aver: Vec<i64>,
}
impl ICFD_CONTROL_MESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_MESH").unwrap());
        Self {
            mgsf: t.take("MGSF").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mstrat: t.take("MSTRAT").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2dstruc: t.take("2DSTRUC").and_then(|c| c.into_int()).unwrap_or_default(),
            nrmsh: t.take("NRMSH").and_then(|c| c.into_int()).unwrap_or_default(),
            aver: t.take("AVER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_MESH_MOV {
    pub mmsh: Vec<i64>,
    pub lim_iter: Vec<i64>,
    pub reltol: Vec<f64>,
}
impl ICFD_CONTROL_MESH_MOV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_MESH_MOV").unwrap());
        Self {
            mmsh: t.take("MMSH").and_then(|c| c.into_int()).unwrap_or_default(),
            lim_iter: t.take("LIM_ITER").and_then(|c| c.into_int()).unwrap_or_default(),
            reltol: t.take("RELTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_MONOLITHIC {
    pub sid: Vec<i64>,
}
impl ICFD_CONTROL_MONOLITHIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_MONOLITHIC").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_OUTPUT {
    pub msgl: Vec<i64>,
    pub outl: Vec<i64>,
    pub dtout: Vec<f64>,
    pub lsppout: Vec<i64>,
    pub f: Vec<i64>,
    pub itout: Vec<i64>,
    pub pitout: Vec<i64>,
}
impl ICFD_CONTROL_OUTPUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_OUTPUT").unwrap());
        Self {
            msgl: t.take("MSGL").and_then(|c| c.into_int()).unwrap_or_default(),
            outl: t.take("OUTL").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            lsppout: t.take("LSPPOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            itout: t.take("ITOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            pitout: t.take("PITOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_OUTPUT_SUBDOM {
    pub sname: Vec<String>,
    pub pminx: Vec<f64>,
    pub pminy: Vec<f64>,
    pub pminz: Vec<f64>,
    pub pmaxx: Vec<f64>,
    pub pmaxy: Vec<f64>,
    pub pmaxz: Vec<f64>,
    pub radius: Vec<f64>,
    pub centerx: Vec<f64>,
    pub centery: Vec<f64>,
    pub centerz: Vec<f64>,
    pub radius_2: Vec<f64>,
    pub pminx_2: Vec<f64>,
    pub pminy_2: Vec<f64>,
    pub pminz_2: Vec<f64>,
    pub pmaxx_2: Vec<f64>,
    pub pmaxy_2: Vec<f64>,
    pub pmaxz_2: Vec<f64>,
}
impl ICFD_CONTROL_OUTPUT_SUBDOM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_OUTPUT_SUBDOM").unwrap());
        Self {
            sname: t.take("SNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            pminx: t.take("PMINX").and_then(|c| c.into_float()).unwrap_or_default(),
            pminy: t.take("PMINY").and_then(|c| c.into_float()).unwrap_or_default(),
            pminz: t.take("PMINZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxx: t.take("PMAXX").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxy: t.take("PMAXY").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxz: t.take("PMAXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            centerx: t.take("CENTERX").and_then(|c| c.into_float()).unwrap_or_default(),
            centery: t.take("CENTERY").and_then(|c| c.into_float()).unwrap_or_default(),
            centerz: t.take("CENTERZ").and_then(|c| c.into_float()).unwrap_or_default(),
            radius_2: t.take("RADIUS").and_then(|c| c.into_float()).unwrap_or_default(),
            pminx_2: t.take("PMINX").and_then(|c| c.into_float()).unwrap_or_default(),
            pminy_2: t.take("PMINY").and_then(|c| c.into_float()).unwrap_or_default(),
            pminz_2: t.take("PMINZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxx_2: t.take("PMAXX").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxy_2: t.take("PMAXY").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxz_2: t.take("PMAXZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_OUTPUT_VAR {
    pub vel: Vec<i64>,
    pub avgvel: Vec<i64>,
    pub vort: Vec<i64>,
    pub pre: Vec<i64>,
    pub preavg: Vec<i64>,
    pub lset: Vec<i64>,
    pub oc: Vec<i64>,
    pub cfl: Vec<i64>,
    pub temp: Vec<i64>,
    pub tempavg: Vec<i64>,
    pub kp: Vec<i64>,
    pub ep: Vec<i64>,
    pub mut_: Vec<i64>,
    pub int: Vec<i64>,
    pub cmu: Vec<i64>,
}
impl ICFD_CONTROL_OUTPUT_VAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_OUTPUT_VAR").unwrap());
        Self {
            vel: t.take("VEL").and_then(|c| c.into_int()).unwrap_or_default(),
            avgvel: t.take("AVGVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            vort: t.take("VORT").and_then(|c| c.into_int()).unwrap_or_default(),
            pre: t.take("PRE").and_then(|c| c.into_int()).unwrap_or_default(),
            preavg: t.take("PREAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            lset: t.take("LSET").and_then(|c| c.into_int()).unwrap_or_default(),
            oc: t.take("OC").and_then(|c| c.into_int()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_int()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_int()).unwrap_or_default(),
            tempavg: t.take("TEMPAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_int()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_int()).unwrap_or_default(),
            mut_: t.take("MUT").and_then(|c| c.into_int()).unwrap_or_default(),
            int: t.take("INT").and_then(|c| c.into_int()).unwrap_or_default(),
            cmu: t.take("CMU").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_PARTITION {
    pub ptech: Vec<i64>,
}
impl ICFD_CONTROL_PARTITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_PARTITION").unwrap());
        Self {
            ptech: t.take("PTECH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_POROUS {
    pub pmstype: Vec<i64>,
}
impl ICFD_CONTROL_POROUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_POROUS").unwrap());
        Self {
            pmstype: t.take("PMSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_STEADY {
    pub its: Vec<i64>,
    pub tol1: Vec<f64>,
    pub tol2: Vec<f64>,
    pub tol3: Vec<f64>,
    pub rel1: Vec<f64>,
    pub rel2: Vec<f64>,
    pub urel: Vec<f64>,
    pub order: Vec<i64>,
}
impl ICFD_CONTROL_STEADY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_STEADY").unwrap());
        Self {
            its: t.take("ITS").and_then(|c| c.into_int()).unwrap_or_default(),
            tol1: t.take("TOL1").and_then(|c| c.into_float()).unwrap_or_default(),
            tol2: t.take("TOL2").and_then(|c| c.into_float()).unwrap_or_default(),
            tol3: t.take("TOL3").and_then(|c| c.into_float()).unwrap_or_default(),
            rel1: t.take("REL1").and_then(|c| c.into_float()).unwrap_or_default(),
            rel2: t.take("REL2").and_then(|c| c.into_float()).unwrap_or_default(),
            urel: t.take("UREL").and_then(|c| c.into_float()).unwrap_or_default(),
            order: t.take("ORDER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_SURFMESH {
    pub rsrf: Vec<i64>,
    pub sadapt: Vec<i64>,
}
impl ICFD_CONTROL_SURFMESH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_SURFMESH").unwrap());
        Self {
            rsrf: t.take("RSRF").and_then(|c| c.into_int()).unwrap_or_default(),
            sadapt: t.take("SADAPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_TAVERAGE {
    pub dt: Vec<f64>,
}
impl ICFD_CONTROL_TAVERAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_TAVERAGE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_TIME {
    pub ttm: Vec<f64>,
    pub dt: Vec<f64>,
    pub cfl: Vec<f64>,
    pub lcidsf: Vec<i64>,
    pub dtmin: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub dtinit: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub dtt: Vec<f64>,
    pub btbl: Vec<i64>,
}
impl ICFD_CONTROL_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_TIME").unwrap());
        Self {
            ttm: t.take("TTM").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidsf: t.take("LCIDSF").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            dtinit: t.take("DTINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtt: t.take("DTT").and_then(|c| c.into_float()).unwrap_or_default(),
            btbl: t.take("BTBL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_TRANSIENT {
    pub tord: Vec<i64>,
    pub fsord: Vec<i64>,
}
impl ICFD_CONTROL_TRANSIENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_TRANSIENT").unwrap());
        Self {
            tord: t.take("TORD").and_then(|c| c.into_int()).unwrap_or_default(),
            fsord: t.take("FSORD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_TURBULENCE {
    pub tmod: Vec<i64>,
    pub submod: Vec<i64>,
    pub wlaw: Vec<i64>,
    pub ks: Vec<f64>,
    pub cs: Vec<f64>,
    pub f: Vec<i64>,
    pub lcids1: Vec<i64>,
    pub lcids2: Vec<i64>,
    pub ce1: Vec<f64>,
    pub ce2: Vec<f64>,
    pub qe: Vec<f64>,
    pub qk: Vec<f64>,
    pub cu: Vec<f64>,
    pub ccut: Vec<f64>,
    pub cs_2: Vec<f64>,
    pub cs_3: Vec<f64>,
    pub r: Vec<f64>,
    pub beta_01: Vec<f64>,
    pub beta_w1: Vec<f64>,
    pub sigma_w1: Vec<f64>,
    pub sigma_k1: Vec<f64>,
    pub ccut_2: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub beta_02: Vec<f64>,
    pub sigma_w2: Vec<f64>,
    pub sigma_k2: Vec<f64>,
    pub cl: Vec<f64>,
    pub cb1: Vec<f64>,
    pub cb2: Vec<f64>,
    pub sigma_v: Vec<f64>,
    pub cv1: Vec<f64>,
    pub cw1: Vec<f64>,
    pub cw2: Vec<f64>,
}
impl ICFD_CONTROL_TURBULENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_TURBULENCE").unwrap());
        Self {
            tmod: t.take("TMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            submod: t.take("SUBMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            wlaw: t.take("WLAW").and_then(|c| c.into_int()).unwrap_or_default(),
            ks: t.take("KS").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcids1: t.take("LCIDS1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcids2: t.take("LCIDS2").and_then(|c| c.into_int()).unwrap_or_default(),
            ce1: t.take("Ce1").and_then(|c| c.into_float()).unwrap_or_default(),
            ce2: t.take("Ce2").and_then(|c| c.into_float()).unwrap_or_default(),
            qe: t.take("Qe").and_then(|c| c.into_float()).unwrap_or_default(),
            qk: t.take("Qk").and_then(|c| c.into_float()).unwrap_or_default(),
            cu: t.take("Cu").and_then(|c| c.into_float()).unwrap_or_default(),
            ccut: t.take("Ccut").and_then(|c| c.into_float()).unwrap_or_default(),
            cs_2: t.take("Cs").and_then(|c| c.into_float()).unwrap_or_default(),
            cs_3: t.take("Cs").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("r").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_01: t.take("Beta-01").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_w1: t.take("Beta-w1").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma_w1: t.take("Sigma-w1").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma_k1: t.take("Sigma-k1").and_then(|c| c.into_float()).unwrap_or_default(),
            ccut_2: t.take("Ccut").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("Alpha1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_02: t.take("Beta-02").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma_w2: t.take("Sigma-w2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma_k2: t.take("Sigma-k2").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("Cl").and_then(|c| c.into_float()).unwrap_or_default(),
            cb1: t.take("Cb1").and_then(|c| c.into_float()).unwrap_or_default(),
            cb2: t.take("Cb2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma_v: t.take("Sigma-v").and_then(|c| c.into_float()).unwrap_or_default(),
            cv1: t.take("Cv1").and_then(|c| c.into_float()).unwrap_or_default(),
            cw1: t.take("Cw1").and_then(|c| c.into_float()).unwrap_or_default(),
            cw2: t.take("Cw2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_CONTROL_TURB_SYNTHESIS {
    pub pid: Vec<i64>,
    pub iu: Vec<f64>,
    pub iv: Vec<f64>,
    pub iw: Vec<f64>,
    pub ls: Vec<f64>,
}
impl ICFD_CONTROL_TURB_SYNTHESIS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_CONTROL_TURB_SYNTHESIS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            iu: t.take("Iu").and_then(|c| c.into_float()).unwrap_or_default(),
            iv: t.take("Iv").and_then(|c| c.into_float()).unwrap_or_default(),
            iw: t.take("Iw").and_then(|c| c.into_float()).unwrap_or_default(),
            ls: t.take("Ls").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_AVERAGE {
    pub dt: Vec<f64>,
}
impl ICFD_DATABASE_AVERAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_AVERAGE").unwrap());
        Self {
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_DRAG {
    pub pid: Vec<i64>,
    pub cpid: Vec<i64>,
    pub dtout: Vec<f64>,
    pub perout: Vec<i64>,
    pub divi: Vec<i64>,
    pub elout: Vec<i64>,
    pub ssout: Vec<i64>,
}
impl ICFD_DATABASE_DRAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_DRAG").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cpid: t.take("CPID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            perout: t.take("PEROUT").and_then(|c| c.into_int()).unwrap_or_default(),
            divi: t.take("DIVI").and_then(|c| c.into_int()).unwrap_or_default(),
            elout: t.take("ELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssout: t.take("SSOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_DRAG_VOL {
    pub pid: Vec<i64>,
    pub cpid: Vec<i64>,
    pub dtout: Vec<f64>,
    pub perout: Vec<i64>,
    pub divi: Vec<i64>,
    pub elout: Vec<i64>,
    pub ssout: Vec<i64>,
}
impl ICFD_DATABASE_DRAG_VOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_DRAG_VOL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            cpid: t.take("CPID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            perout: t.take("PEROUT").and_then(|c| c.into_int()).unwrap_or_default(),
            divi: t.take("DIVI").and_then(|c| c.into_int()).unwrap_or_default(),
            elout: t.take("ELOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssout: t.take("SSOUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_FLUX {
    pub pid: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl ICFD_DATABASE_FLUX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_FLUX").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_HTC {
    pub out: Vec<i64>,
    pub htc: Vec<i64>,
    pub tb: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub outdt: Vec<f64>,
}
impl ICFD_DATABASE_HTC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_HTC").unwrap());
        Self {
            out: t.take("OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            outdt: t.take("OUTDT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_NODEAVG {
    pub on: Vec<i64>,
}
impl ICFD_DATABASE_NODEAVG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_NODEAVG").unwrap());
        Self {
            on: t.take("ON").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_NODOUT {
    pub outlv: Vec<i64>,
    pub dtout: Vec<f64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl ICFD_DATABASE_NODOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_NODOUT").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_NTEMPOUT {
    pub nid: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl ICFD_DATABASE_NTEMPOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_NTEMPOUT").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_POINTAVG {
    pub on: Vec<i64>,
}
impl ICFD_DATABASE_POINTAVG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_POINTAVG").unwrap());
        Self {
            on: t.take("ON").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_POINTOUT {
    pub psid: Vec<i64>,
    pub dtout: Vec<f64>,
    pub pstype: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl ICFD_DATABASE_POINTOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_POINTOUT").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            pstype: t.take("PSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_RESIDUALS {
    pub rlvl: Vec<i64>,
}
impl ICFD_DATABASE_RESIDUALS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_RESIDUALS").unwrap());
        Self {
            rlvl: t.take("RLVL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_SSOUT {
    pub out: Vec<i64>,
    pub outdt: Vec<i64>,
    pub lcidsf: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub poff: Vec<f64>,
}
impl ICFD_DATABASE_SSOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_SSOUT").unwrap());
        Self {
            out: t.take("OUT").and_then(|c| c.into_int()).unwrap_or_default(),
            outdt: t.take("OUTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidsf: t.take("LCIDSF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            poff: t.take("POFF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_SSOUT_EXCLUDE {
    pub ssoutid: Vec<i64>,
}
impl ICFD_DATABASE_SSOUT_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_SSOUT_EXCLUDE").unwrap());
        Self {
            ssoutid: t.take("SSOUTID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_TEMP {
    pub pid: Vec<i64>,
    pub dtout: Vec<f64>,
}
impl ICFD_DATABASE_TEMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_TEMP").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_TIMESTEP {
    pub outlv: Vec<i64>,
}
impl ICFD_DATABASE_TIMESTEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_TIMESTEP").unwrap());
        Self {
            outlv: t.take("OUTLV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DATABASE_UINDEX {
    pub out: Vec<i64>,
}
impl ICFD_DATABASE_UINDEX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DATABASE_UINDEX").unwrap());
        Self {
            out: t.take("OUT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_HEATSOURCE {
    pub hsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub shape: Vec<i64>,
    pub r: Vec<f64>,
    pub ptid1: Vec<i64>,
    pub ptid2: Vec<i64>,
}
impl ICFD_DEFINE_HEATSOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_HEATSOURCE").unwrap());
        Self {
            hsid: t.take("HSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            ptid1: t.take("PTID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptid2: t.take("PTID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_NONINERTIAL {
    pub w1: Vec<f64>,
    pub w2: Vec<f64>,
    pub w3: Vec<f64>,
    pub r: Vec<f64>,
    pub ptid: Vec<i64>,
    pub l: Vec<f64>,
    pub lcid: Vec<i64>,
    pub relv: Vec<i64>,
}
impl ICFD_DEFINE_NONINERTIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_NONINERTIAL").unwrap());
        Self {
            w1: t.take("W1").and_then(|c| c.into_float()).unwrap_or_default(),
            w2: t.take("W2").and_then(|c| c.into_float()).unwrap_or_default(),
            w3: t.take("W3").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            ptid: t.take("PTID").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            relv: t.take("RELV").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_POINT {
    pub poid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub constpid: Vec<i64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub lcidw: Vec<i64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
}
impl ICFD_DEFINE_POINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_POINT").unwrap());
        Self {
            poid: t.take("POID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            constpid: t.take("CONSTPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidw: t.take("LCIDW").and_then(|c| c.into_int()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_SOURCE {
    pub sid: Vec<i64>,
    pub lcidk: Vec<i64>,
    pub shape: Vec<i64>,
    pub r: Vec<f64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
}
impl ICFD_DEFINE_SOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_SOURCE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_TURBSOURCE {
    pub sid: Vec<i64>,
    pub lcidx: Vec<i64>,
    pub lcidy: Vec<i64>,
    pub lcidz: Vec<i64>,
    pub shape: Vec<i64>,
    pub r: Vec<f64>,
    pub ptid1: Vec<i64>,
    pub ptid2: Vec<i64>,
}
impl ICFD_DEFINE_TURBSOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_TURBSOURCE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidx: t.take("LCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            ptid1: t.take("PTID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptid2: t.take("PTID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_DEFINE_WAVE_DAMPING {
    pub pid: Vec<i64>,
    pub nid: Vec<i64>,
    pub l: Vec<f64>,
    pub f1: Vec<f64>,
    pub f2: Vec<f64>,
    pub n: Vec<i64>,
    pub lcid: Vec<i64>,
}
impl ICFD_DEFINE_WAVE_DAMPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_DEFINE_WAVE_DAMPING").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            f1: t.take("F1").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_INITIAL {
    pub pid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub t: Vec<f64>,
    pub p: Vec<f64>,
    pub f60: Vec<i64>,
    pub dfunc: Vec<i64>,
}
impl ICFD_INITIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_INITIAL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("Vx").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("Vy").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("Vz").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            dfunc: t.take("DFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_INITIAL_LEVELSET {
    pub stype: Vec<i64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub invert: Vec<i64>,
}
impl ICFD_INITIAL_LEVELSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_INITIAL_LEVELSET").unwrap());
        Self {
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            invert: t.take("INVERT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_INITIAL_TEMPNODE {
    pub nid: Vec<i64>,
    pub temp: Vec<f64>,
}
impl ICFD_INITIAL_TEMPNODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_INITIAL_TEMPNODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_INITIAL_TURBULENCE {
    pub pid: Vec<i64>,
    pub i: Vec<f64>,
    pub r: Vec<f64>,
    pub k: Vec<f64>,
    pub ew: Vec<f64>,
}
impl ICFD_INITIAL_TURBULENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_INITIAL_TURBULENCE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            i: t.take("I").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            ew: t.take("EW").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_MAT {
    pub mid: Vec<i64>,
    pub flg: Vec<i64>,
    pub ro: Vec<f64>,
    pub vis: Vec<f64>,
    pub st: Vec<f64>,
    pub stsflcid: Vec<i64>,
    pub ca: Vec<f64>,
    pub hc: Vec<f64>,
    pub tc: Vec<f64>,
    pub beta: Vec<f64>,
    pub prt: Vec<f64>,
    pub hcsflcid: Vec<i64>,
    pub tcsflcid: Vec<i64>,
    pub nnmoid: Vec<i64>,
    pub pmmoid: Vec<i64>,
}
impl ICFD_MAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_MAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            flg: t.take("FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            vis: t.take("VIS").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            stsflcid: t.take("STSFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            prt: t.take("PRT").and_then(|c| c.into_float()).unwrap_or_default(),
            hcsflcid: t.take("HCSFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tcsflcid: t.take("TCSFLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nnmoid: t.take("NNMOID").and_then(|c| c.into_int()).unwrap_or_default(),
            pmmoid: t.take("PMMOID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_MODEL_NONNEWT {
    pub nnmoid: Vec<i64>,
    pub nnid: Vec<i64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub mumin: Vec<f64>,
    pub lambda: Vec<f64>,
    pub alpha: Vec<f64>,
    pub talpha: Vec<f64>,
}
impl ICFD_MODEL_NONNEWT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_MODEL_NONNEWT").unwrap());
        Self {
            nnmoid: t.take("NNMOID").and_then(|c| c.into_int()).unwrap_or_default(),
            nnid: t.take("NNID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            mumin: t.take("MUMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            talpha: t.take("TALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_MODEL_POROUS {
    pub pmmoid: Vec<i64>,
    pub pmid: Vec<i64>,
    pub por: Vec<f64>,
    pub per_thx: Vec<f64>,
    pub ff_thy: Vec<f64>,
    pub thz: Vec<f64>,
    pub pvlcidx: Vec<i64>,
    pub pvlcidy: Vec<i64>,
    pub pvlcidz: Vec<i64>,
    pub kxp: Vec<f64>,
    pub kyp: Vec<f64>,
    pub kzp: Vec<f64>,
    pub p_x_pid1r: Vec<f64>,
    pub p_y_pid2r: Vec<f64>,
    pub projxp_z: Vec<f64>,
    pub projyp_x: Vec<f64>,
    pub projyp_y: Vec<f64>,
    pub projyp_z: Vec<f64>,
}
impl ICFD_MODEL_POROUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_MODEL_POROUS").unwrap());
        Self {
            pmmoid: t.take("PMMOID").and_then(|c| c.into_int()).unwrap_or_default(),
            pmid: t.take("PMID").and_then(|c| c.into_int()).unwrap_or_default(),
            por: t.take("POR").and_then(|c| c.into_float()).unwrap_or_default(),
            per_thx: t.take("PER/THX").and_then(|c| c.into_float()).unwrap_or_default(),
            ff_thy: t.take("FF/THY").and_then(|c| c.into_float()).unwrap_or_default(),
            thz: t.take("THZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pvlcidx: t.take("PVLCIDX").and_then(|c| c.into_int()).unwrap_or_default(),
            pvlcidy: t.take("PVLCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            pvlcidz: t.take("PVLCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            kxp: t.take("KXP'").and_then(|c| c.into_float()).unwrap_or_default(),
            kyp: t.take("KYP'").and_then(|c| c.into_float()).unwrap_or_default(),
            kzp: t.take("KZP'").and_then(|c| c.into_float()).unwrap_or_default(),
            p_x_pid1r: t.take("P-X/PID1R").and_then(|c| c.into_float()).unwrap_or_default(),
            p_y_pid2r: t.take("P-Y/PID2R").and_then(|c| c.into_float()).unwrap_or_default(),
            projxp_z: t.take("PROJXP-Z").and_then(|c| c.into_float()).unwrap_or_default(),
            projyp_x: t.take("PROJYP-X").and_then(|c| c.into_float()).unwrap_or_default(),
            projyp_y: t.take("PROJYP-Y").and_then(|c| c.into_float()).unwrap_or_default(),
            projyp_z: t.take("PROJYP-Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_PART {
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
}
impl ICFD_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_PART_VOL {
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub spid1: Vec<i64>,
    pub spid2: Vec<i64>,
    pub spid3: Vec<i64>,
    pub spid4: Vec<i64>,
    pub spid5: Vec<i64>,
    pub spid6: Vec<i64>,
    pub spid7: Vec<i64>,
    pub spid8: Vec<i64>,
}
impl ICFD_PART_VOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_PART_VOL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            spid1: t.take("SPID1").and_then(|c| c.into_int()).unwrap_or_default(),
            spid2: t.take("SPID2").and_then(|c| c.into_int()).unwrap_or_default(),
            spid3: t.take("SPID3").and_then(|c| c.into_int()).unwrap_or_default(),
            spid4: t.take("SPID4").and_then(|c| c.into_int()).unwrap_or_default(),
            spid5: t.take("SPID5").and_then(|c| c.into_int()).unwrap_or_default(),
            spid6: t.take("SPID6").and_then(|c| c.into_int()).unwrap_or_default(),
            spid7: t.take("SPID7").and_then(|c| c.into_int()).unwrap_or_default(),
            spid8: t.take("SPID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SECTION {
    pub sid: Vec<i64>,
}
impl ICFD_SECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SECTION").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SET_NODE_LIST {
    pub sid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl ICFD_SET_NODE_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SET_NODE_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_SPLIT {
    pub nit: Vec<i64>,
    pub tol: Vec<f64>,
}
impl ICFD_SOLVER_SPLIT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_SPLIT").unwrap());
        Self {
            nit: t.take("NIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_FSI {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub f: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_FSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_FSI").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_LSET {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub f: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_LSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_LSET").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_MMOV {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub f: Vec<i64>,
    pub maxit: Vec<i64>,
    pub f_2: Vec<i64>,
    pub disptol: Vec<f64>,
}
impl ICFD_SOLVER_TOL_MMOV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_MMOV").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            disptol: t.take("DISPTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_MOM {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub unused: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_MOM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_MOM").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_MONOLITHIC {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub unused: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_MONOLITHIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_MONOLITHIC").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_PRE {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub unused: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_PRE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_PRE").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct ICFD_SOLVER_TOL_TEMP {
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub unused: Vec<i64>,
    pub maxit: Vec<i64>,
}
impl ICFD_SOLVER_TOL_TEMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("ICFD_SOLVER_TOL_TEMP").unwrap());
        Self {
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            maxit: t.take("MAXIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_1D_BREP {
    pub brid: Vec<i64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl IGA_1D_BREP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_1D_BREP").unwrap());
        Self {
            brid: t.take("BRID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_1D_NURBS_UVW {
    pub nid: Vec<i64>,
    pub nr: Vec<i64>,
    pub pr: Vec<i64>,
    pub unir: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub rfirst: Vec<f64>,
    pub rlast: Vec<f64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub wgt: Vec<f64>,
}
impl IGA_1D_NURBS_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_1D_NURBS_UVW").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            unir: t.take("UNIR").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            rfirst: t.take("RFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            rlast: t.take("RLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_1D_NURBS_XYZ {
    pub nid: Vec<i64>,
    pub nr: Vec<i64>,
    pub pr: Vec<i64>,
    pub unir: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub rfirst: Vec<f64>,
    pub rlast: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub wgt: Vec<f64>,
}
impl IGA_1D_NURBS_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_1D_NURBS_XYZ").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            unir: t.take("UNIR").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            rfirst: t.take("RFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            rlast: t.take("RLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("x").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("z").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_2D_BREP {
    pub brid: Vec<i64>,
    pub fid1: Vec<i64>,
    pub fid2: Vec<i64>,
    pub fid3: Vec<i64>,
    pub fid4: Vec<i64>,
    pub fid5: Vec<i64>,
    pub fid6: Vec<i64>,
    pub fid7: Vec<i64>,
    pub fid8: Vec<i64>,
}
impl IGA_2D_BREP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_2D_BREP").unwrap());
        Self {
            brid: t.take("BRID").and_then(|c| c.into_int()).unwrap_or_default(),
            fid1: t.take("FID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fid2: t.take("FID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fid3: t.take("FID3").and_then(|c| c.into_int()).unwrap_or_default(),
            fid4: t.take("FID4").and_then(|c| c.into_int()).unwrap_or_default(),
            fid5: t.take("FID5").and_then(|c| c.into_int()).unwrap_or_default(),
            fid6: t.take("FID6").and_then(|c| c.into_int()).unwrap_or_default(),
            fid7: t.take("FID7").and_then(|c| c.into_int()).unwrap_or_default(),
            fid8: t.take("FID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_2D_NURBS_UVW {
    pub nid: Vec<i64>,
    pub nr: Vec<i64>,
    pub ns: Vec<i64>,
    pub pr: Vec<i64>,
    pub ps: Vec<i64>,
    pub unir: Vec<i64>,
    pub unis: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub rfirst: Vec<f64>,
    pub rlast: Vec<f64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub s4: Vec<f64>,
    pub sfirst: Vec<f64>,
    pub slast: Vec<f64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub wgt: Vec<f64>,
}
impl IGA_2D_NURBS_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_2D_NURBS_UVW").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            ns: t.take("NS").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            unir: t.take("UNIR").and_then(|c| c.into_int()).unwrap_or_default(),
            unis: t.take("UNIS").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            rfirst: t.take("RFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            rlast: t.take("RLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_float()).unwrap_or_default(),
            sfirst: t.take("SFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            slast: t.take("SLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_2D_NURBS_XYZ {
    pub nid: Vec<i64>,
    pub nr: Vec<i64>,
    pub ns: Vec<i64>,
    pub pr: Vec<i64>,
    pub ps: Vec<i64>,
    pub unir: Vec<i64>,
    pub unis: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub rfirst: Vec<f64>,
    pub rlast: Vec<f64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub s4: Vec<f64>,
    pub sfirst: Vec<f64>,
    pub slast: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub wgt: Vec<f64>,
}
impl IGA_2D_NURBS_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_2D_NURBS_XYZ").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            ns: t.take("NS").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            unir: t.take("UNIR").and_then(|c| c.into_int()).unwrap_or_default(),
            unis: t.take("UNIS").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            rfirst: t.take("RFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            rlast: t.take("RLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_float()).unwrap_or_default(),
            sfirst: t.take("SFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            slast: t.take("SLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_3D_NURBS_XYZ {
    pub nid: Vec<i64>,
    pub nr: Vec<i64>,
    pub ns: Vec<i64>,
    pub nt: Vec<i64>,
    pub pr: Vec<i64>,
    pub ps: Vec<i64>,
    pub pt: Vec<i64>,
    pub unir: Vec<i64>,
    pub unis: Vec<i64>,
    pub unit: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub r3: Vec<f64>,
    pub r4: Vec<f64>,
    pub rfirst: Vec<f64>,
    pub rlast: Vec<f64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub s4: Vec<f64>,
    pub sfirst: Vec<f64>,
    pub slast: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub tfirst: Vec<f64>,
    pub tlast: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub wgt: Vec<f64>,
}
impl IGA_3D_NURBS_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_3D_NURBS_XYZ").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            ns: t.take("NS").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_int()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_int()).unwrap_or_default(),
            unir: t.take("UNIR").and_then(|c| c.into_int()).unwrap_or_default(),
            unis: t.take("UNIS").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            r3: t.take("R3").and_then(|c| c.into_float()).unwrap_or_default(),
            r4: t.take("R4").and_then(|c| c.into_float()).unwrap_or_default(),
            rfirst: t.take("RFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            rlast: t.take("RLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_float()).unwrap_or_default(),
            sfirst: t.take("SFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            slast: t.take("SLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            tfirst: t.take("TFIRST").and_then(|c| c.into_float()).unwrap_or_default(),
            tlast: t.take("TLAST").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_EDGE_UVW {
    pub eid: Vec<i64>,
    pub exyzid: Vec<i64>,
    pub nid: Vec<i64>,
    pub sense: Vec<i64>,
    pub rstart: Vec<f64>,
    pub rend: Vec<f64>,
}
impl IGA_EDGE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_EDGE_UVW").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            exyzid: t.take("EXYZID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            sense: t.take("SENSE").and_then(|c| c.into_int()).unwrap_or_default(),
            rstart: t.take("RSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            rend: t.take("REND").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_EDGE_XYZ {
    pub eid: Vec<i64>,
    pub nid: Vec<i64>,
    pub ori: Vec<i64>,
    pub pidstart: Vec<i64>,
    pub pidend: Vec<i64>,
    pub psid: Vec<i64>,
}
impl IGA_EDGE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_EDGE_XYZ").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ori: t.take("ORI").and_then(|c| c.into_int()).unwrap_or_default(),
            pidstart: t.take("PIDSTART").and_then(|c| c.into_int()).unwrap_or_default(),
            pidend: t.take("PIDEND").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_FACE_UVW {
    pub fid: Vec<i64>,
    pub fxyzid: Vec<i64>,
    pub nid: Vec<i64>,
    pub sense: Vec<i64>,
    pub brid1: Vec<i64>,
    pub brid2: Vec<i64>,
    pub brid3: Vec<i64>,
    pub brid4: Vec<i64>,
    pub brid5: Vec<i64>,
    pub brid6: Vec<i64>,
    pub brid7: Vec<i64>,
    pub brid8: Vec<i64>,
}
impl IGA_FACE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_FACE_UVW").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            fxyzid: t.take("FXYZID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            sense: t.take("SENSE").and_then(|c| c.into_int()).unwrap_or_default(),
            brid1: t.take("BRID1").and_then(|c| c.into_int()).unwrap_or_default(),
            brid2: t.take("BRID2").and_then(|c| c.into_int()).unwrap_or_default(),
            brid3: t.take("BRID3").and_then(|c| c.into_int()).unwrap_or_default(),
            brid4: t.take("BRID4").and_then(|c| c.into_int()).unwrap_or_default(),
            brid5: t.take("BRID5").and_then(|c| c.into_int()).unwrap_or_default(),
            brid6: t.take("BRID6").and_then(|c| c.into_int()).unwrap_or_default(),
            brid7: t.take("BRID7").and_then(|c| c.into_int()).unwrap_or_default(),
            brid8: t.take("BRID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_FACE_XYZ {
    pub fid: Vec<i64>,
    pub nid: Vec<i64>,
    pub ori: Vec<i64>,
    pub psid: Vec<i64>,
    pub esid: Vec<i64>,
    pub brid1: Vec<i64>,
    pub brid2: Vec<i64>,
    pub brid3: Vec<i64>,
    pub brid4: Vec<i64>,
    pub brid5: Vec<i64>,
    pub brid6: Vec<i64>,
    pub brid7: Vec<i64>,
    pub brid8: Vec<i64>,
}
impl IGA_FACE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_FACE_XYZ").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ori: t.take("ORI").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            brid1: t.take("BRID1").and_then(|c| c.into_int()).unwrap_or_default(),
            brid2: t.take("BRID2").and_then(|c| c.into_int()).unwrap_or_default(),
            brid3: t.take("BRID3").and_then(|c| c.into_int()).unwrap_or_default(),
            brid4: t.take("BRID4").and_then(|c| c.into_int()).unwrap_or_default(),
            brid5: t.take("BRID5").and_then(|c| c.into_int()).unwrap_or_default(),
            brid6: t.take("BRID6").and_then(|c| c.into_int()).unwrap_or_default(),
            brid7: t.take("BRID7").and_then(|c| c.into_int()).unwrap_or_default(),
            brid8: t.take("BRID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_INCLUDE_BEZIER {
    pub filename: Vec<String>,
    pub filetype: Vec<i64>,
    pub pid: Vec<i64>,
    pub dim: Vec<i64>,
}
impl IGA_INCLUDE_BEZIER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_INCLUDE_BEZIER").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            filetype: t.take("FILETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dim: t.take("DIM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_POINT_UVW {
    pub pid: Vec<i64>,
    pub nid: Vec<i64>,
    pub u: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
}
impl IGA_POINT_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_POINT_UVW").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            u: t.take("U").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_SHELL {
    pub sid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nisr: Vec<f64>,
    pub niss: Vec<f64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub idfne: Vec<i64>,
    pub f70: Vec<i64>,
}
impl IGA_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_SHELL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_float()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            idfne: t.take("IDFNE").and_then(|c| c.into_int()).unwrap_or_default(),
            f70: t.take("f70").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct IGA_SOLID {
    pub sid: Vec<i64>,
    pub pid: Vec<i64>,
    pub nisr: Vec<f64>,
    pub niss: Vec<f64>,
    pub nist: Vec<f64>,
}
impl IGA_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_SOLID").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nisr: t.take("NISR").and_then(|c| c.into_float()).unwrap_or_default(),
            niss: t.take("NISS").and_then(|c| c.into_float()).unwrap_or_default(),
            nist: t.take("NIST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_TIED_EDGE_TO_EDGE {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub form: Vec<i64>,
    pub sfd: Vec<f64>,
    pub sfr: Vec<f64>,
    pub sft: Vec<f64>,
}
impl IGA_TIED_EDGE_TO_EDGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_TIED_EDGE_TO_EDGE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            sfd: t.take("SFD").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct IGA_VOLUME_XYZ {
    pub vid: Vec<i64>,
    pub nid: Vec<i64>,
    pub psid: Vec<i64>,
    pub esid: Vec<i64>,
    pub fsid: Vec<i64>,
    pub brid1: Vec<i64>,
    pub brid2: Vec<i64>,
    pub brid3: Vec<i64>,
    pub brid4: Vec<i64>,
    pub brid5: Vec<i64>,
    pub brid6: Vec<i64>,
    pub brid7: Vec<i64>,
    pub brid8: Vec<i64>,
}
impl IGA_VOLUME_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("IGA_VOLUME_XYZ").unwrap());
        Self {
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            fsid: t.take("FSID").and_then(|c| c.into_int()).unwrap_or_default(),
            brid1: t.take("BRID1").and_then(|c| c.into_int()).unwrap_or_default(),
            brid2: t.take("BRID2").and_then(|c| c.into_int()).unwrap_or_default(),
            brid3: t.take("BRID3").and_then(|c| c.into_int()).unwrap_or_default(),
            brid4: t.take("BRID4").and_then(|c| c.into_int()).unwrap_or_default(),
            brid5: t.take("BRID5").and_then(|c| c.into_int()).unwrap_or_default(),
            brid6: t.take("BRID6").and_then(|c| c.into_int()).unwrap_or_default(),
            brid7: t.take("BRID7").and_then(|c| c.into_int()).unwrap_or_default(),
            brid8: t.take("BRID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_AUTO_OFFSET {
    pub filename: Vec<String>,
}
impl INCLUDE_AUTO_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_AUTO_OFFSET").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_AUTO_OFFSET_USER {
    pub filename: Vec<String>,
    pub noffset: Vec<i64>,
    pub neoffset: Vec<i64>,
}
impl INCLUDE_AUTO_OFFSET_USER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_AUTO_OFFSET_USER").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            noffset: t.take("NOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
            neoffset: t.take("NEOFFSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_BINARY {
    pub filename: Vec<String>,
}
impl INCLUDE_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_BINARY").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_BEFORE_SPRINGBACK {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_BEFORE_SPRINGBACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_BEFORE_SPRINGBACK").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_BLANK_AFTER_SPRINGBACK {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_BLANK_AFTER_SPRINGBACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_BLANK_AFTER_SPRINGBACK").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_BLANK_BEFORE_SPRINGBACK {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_BLANK_BEFORE_SPRINGBACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_BLANK_BEFORE_SPRINGBACK").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_COMPENSATED_SHAPE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_COMPENSATED_SHAPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_COMPENSATED_SHAPE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_COMPENSATED_SHAP_ENEXT_STEP {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_COMPENSATED_SHAP_ENEXT_STEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_COMPENSATED_SHAP_ENEXT_STEP").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_CURRENT_TOOLS {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_CURRENT_TOOLS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_CURRENT_TOOLS").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_CURVE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_CURVE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_DESIRED_BLANK_SHAPE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_DESIRED_BLANK_SHAPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_DESIRED_BLANK_SHAPE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_NEW_RIGID_TOOL {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_NEW_RIGID_TOOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_NEW_RIGID_TOOL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_ORIGINAL_DYNAIN {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_ORIGINAL_DYNAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_ORIGINAL_DYNAIN").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_ORIGINAL_RIGID_TOOL {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_ORIGINAL_RIGID_TOOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_ORIGINAL_RIGID_TOOL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_ORIGINAL_TOOL {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_ORIGINAL_TOOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_ORIGINAL_TOOL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_SPRINGBACK_INPUTE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_SPRINGBACK_INPUTE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_SPRINGBACK_INPUTE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_SYMMETRIC_LINES {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_SYMMETRIC_LINES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_SYMMETRIC_LINES").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_TANGENT_CONSTRAINT {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_TANGENT_CONSTRAINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_TANGENT_CONSTRAINT").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_TRIM_CURVE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_TRIM_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_TRIM_CURVE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_TRIM_NODE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_TRIM_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_TRIM_NODE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_UPDATED_BLANK_SHAPE {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_UPDATED_BLANK_SHAPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_UPDATED_BLANK_SHAPE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COMPENSATION_UPDATED_RIGID_TOOL {
    pub filename: Vec<String>,
}
impl INCLUDE_COMPENSATION_UPDATED_RIGID_TOOL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COMPENSATION_UPDATED_RIGID_TOOL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_COSIM {
    pub filename: Vec<String>,
}
impl INCLUDE_COSIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_COSIM").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_INCLUDE {
    pub filename: Vec<String>,
}
impl INCLUDE_INCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_INCLUDE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_MULTISCALE {
    pub id: Vec<i64>,
    pub filename: Vec<String>,
}
impl INCLUDE_MULTISCALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_MULTISCALE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_MULTISCALE_SPOTWELD {
    pub type_: Vec<i64>,
    pub filename: Vec<String>,
}
impl INCLUDE_MULTISCALE_SPOTWELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_MULTISCALE_SPOTWELD").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_NASTRAN {
    pub filename: Vec<String>,
    pub beamdf: Vec<i64>,
    pub shelldf: Vec<i64>,
    pub soliddf: Vec<i64>,
}
impl INCLUDE_NASTRAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_NASTRAN").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            beamdf: t.take("BEAMDF").and_then(|c| c.into_int()).unwrap_or_default(),
            shelldf: t.take("SHELLDF").and_then(|c| c.into_int()).unwrap_or_default(),
            soliddf: t.take("SOLIDDF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_PATH {
    pub path: Vec<String>,
}
impl INCLUDE_PATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_PATH").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_PATH_RELATIVE {
    pub path: Vec<String>,
}
impl INCLUDE_PATH_RELATIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_PATH_RELATIVE").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_PART {
    pub filename: Vec<String>,
    pub pid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub n1s: Vec<i64>,
    pub n2s: Vec<i64>,
    pub n3s: Vec<i64>,
    pub n1c: Vec<i64>,
    pub n2c: Vec<i64>,
    pub n3c: Vec<i64>,
    pub tensor: Vec<i64>,
    pub thkscl: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_PART").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n1s: t.take("N1S").and_then(|c| c.into_int()).unwrap_or_default(),
            n2s: t.take("N2S").and_then(|c| c.into_int()).unwrap_or_default(),
            n3s: t.take("N3S").and_then(|c| c.into_int()).unwrap_or_default(),
            n1c: t.take("N1C").and_then(|c| c.into_int()).unwrap_or_default(),
            n2c: t.take("N2C").and_then(|c| c.into_int()).unwrap_or_default(),
            n3c: t.take("N3C").and_then(|c| c.into_int()).unwrap_or_default(),
            tensor: t.take("TENSOR").and_then(|c| c.into_int()).unwrap_or_default(),
            thkscl: t.take("THKSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_PART_MATRIX {
    pub filename: Vec<String>,
    pub pid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub r11: Vec<f64>,
    pub r12: Vec<f64>,
    pub r13: Vec<f64>,
    pub xp: Vec<f64>,
    pub r21: Vec<f64>,
    pub r22: Vec<f64>,
    pub r23: Vec<f64>,
    pub yp: Vec<f64>,
    pub r31: Vec<f64>,
    pub r32: Vec<f64>,
    pub r33: Vec<f64>,
    pub zp: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_PART_MATRIX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_PART_MATRIX").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            r13: t.take("R13").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            r21: t.take("R21").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r23: t.take("R23").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            r31: t.take("R31").and_then(|c| c.into_float()).unwrap_or_default(),
            r32: t.take("R32").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_PART_MATRIX_INVERSE {
    pub filename: Vec<String>,
    pub pid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub r11: Vec<f64>,
    pub r12: Vec<f64>,
    pub r13: Vec<f64>,
    pub xp: Vec<f64>,
    pub r21: Vec<f64>,
    pub r22: Vec<f64>,
    pub r23: Vec<f64>,
    pub yp: Vec<f64>,
    pub r31: Vec<f64>,
    pub r32: Vec<f64>,
    pub r33: Vec<f64>,
    pub zp: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_PART_MATRIX_INVERSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_PART_MATRIX_INVERSE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            r13: t.take("R13").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            r21: t.take("R21").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r23: t.take("R23").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            r31: t.take("R31").and_then(|c| c.into_float()).unwrap_or_default(),
            r32: t.take("R32").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_PART_SET {
    pub filename: Vec<String>,
    pub psid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub n1s: Vec<i64>,
    pub n2s: Vec<i64>,
    pub n3s: Vec<i64>,
    pub n1c: Vec<i64>,
    pub n2c: Vec<i64>,
    pub n3c: Vec<i64>,
    pub tensor: Vec<i64>,
    pub thkscl: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_PART_SET").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n1s: t.take("N1S").and_then(|c| c.into_int()).unwrap_or_default(),
            n2s: t.take("N2S").and_then(|c| c.into_int()).unwrap_or_default(),
            n3s: t.take("N3S").and_then(|c| c.into_int()).unwrap_or_default(),
            n1c: t.take("N1C").and_then(|c| c.into_int()).unwrap_or_default(),
            n2c: t.take("N2C").and_then(|c| c.into_int()).unwrap_or_default(),
            n3c: t.take("N3C").and_then(|c| c.into_int()).unwrap_or_default(),
            tensor: t.take("TENSOR").and_then(|c| c.into_int()).unwrap_or_default(),
            thkscl: t.take("THKSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_PART_SOLID_TO_SOLID {
    pub filename: Vec<String>,
    pub pid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub n1sorc: Vec<i64>,
    pub n2sorc: Vec<i64>,
    pub n3sorc: Vec<i64>,
    pub n1trgt: Vec<i64>,
    pub n2trgt: Vec<i64>,
    pub n3trgt: Vec<i64>,
}
impl INCLUDE_STAMPED_PART_SOLID_TO_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_PART_SOLID_TO_SOLID").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            n1sorc: t.take("N1SORC").and_then(|c| c.into_int()).unwrap_or_default(),
            n2sorc: t.take("N2SORC").and_then(|c| c.into_int()).unwrap_or_default(),
            n3sorc: t.take("N3SORC").and_then(|c| c.into_int()).unwrap_or_default(),
            n1trgt: t.take("N1TRGT").and_then(|c| c.into_int()).unwrap_or_default(),
            n2trgt: t.take("N2TRGT").and_then(|c| c.into_int()).unwrap_or_default(),
            n3trgt: t.take("N3TRGT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_SET {
    pub filename: Vec<String>,
    pub psid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub n1s: Vec<i64>,
    pub n2s: Vec<i64>,
    pub n3s: Vec<i64>,
    pub n1c: Vec<i64>,
    pub n2c: Vec<i64>,
    pub n3c: Vec<i64>,
    pub tensor: Vec<i64>,
    pub thkscl: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_SET").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n1s: t.take("N1S").and_then(|c| c.into_int()).unwrap_or_default(),
            n2s: t.take("N2S").and_then(|c| c.into_int()).unwrap_or_default(),
            n3s: t.take("N3S").and_then(|c| c.into_int()).unwrap_or_default(),
            n1c: t.take("N1C").and_then(|c| c.into_int()).unwrap_or_default(),
            n2c: t.take("N2C").and_then(|c| c.into_int()).unwrap_or_default(),
            n3c: t.take("N3C").and_then(|c| c.into_int()).unwrap_or_default(),
            tensor: t.take("TENSOR").and_then(|c| c.into_int()).unwrap_or_default(),
            thkscl: t.take("THKSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_SET_MATRIX {
    pub filename: Vec<String>,
    pub psid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub r11: Vec<f64>,
    pub r12: Vec<f64>,
    pub r13: Vec<f64>,
    pub xp: Vec<f64>,
    pub r21: Vec<f64>,
    pub r22: Vec<f64>,
    pub r23: Vec<f64>,
    pub yp: Vec<f64>,
    pub r31: Vec<f64>,
    pub r32: Vec<f64>,
    pub r33: Vec<f64>,
    pub zp: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_SET_MATRIX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_SET_MATRIX").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            r13: t.take("R13").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            r21: t.take("R21").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r23: t.take("R23").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            r31: t.take("R31").and_then(|c| c.into_float()).unwrap_or_default(),
            r32: t.take("R32").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_STAMPED_SET_MATRIX_INVERSE {
    pub filename: Vec<String>,
    pub psid: Vec<i64>,
    pub thick: Vec<i64>,
    pub pstrn: Vec<i64>,
    pub strain: Vec<i64>,
    pub stress: Vec<i64>,
    pub incout: Vec<i64>,
    pub f: Vec<i64>,
    pub rmax: Vec<f64>,
    pub r11: Vec<f64>,
    pub r12: Vec<f64>,
    pub r13: Vec<f64>,
    pub xp: Vec<f64>,
    pub r21: Vec<f64>,
    pub r22: Vec<f64>,
    pub r23: Vec<f64>,
    pub yp: Vec<f64>,
    pub r31: Vec<f64>,
    pub r32: Vec<f64>,
    pub r33: Vec<f64>,
    pub zp: Vec<f64>,
    pub isym: Vec<i64>,
    pub iafter: Vec<i64>,
    pub percele: Vec<f64>,
    pub iortho: Vec<i64>,
    pub f_2: Vec<i64>,
    pub isrocut: Vec<i64>,
    pub x01: Vec<f64>,
    pub y01: Vec<f64>,
    pub z01: Vec<f64>,
    pub x02: Vec<f64>,
    pub y02: Vec<f64>,
    pub z02: Vec<f64>,
    pub x03: Vec<f64>,
    pub y03: Vec<f64>,
    pub z03: Vec<f64>,
}
impl INCLUDE_STAMPED_SET_MATRIX_INVERSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_STAMPED_SET_MATRIX_INVERSE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_int()).unwrap_or_default(),
            pstrn: t.take("PSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            strain: t.take("STRAIN").and_then(|c| c.into_int()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_int()).unwrap_or_default(),
            incout: t.take("INCOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            r13: t.take("R13").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            r21: t.take("R21").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r23: t.take("R23").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            r31: t.take("R31").and_then(|c| c.into_float()).unwrap_or_default(),
            r32: t.take("R32").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            isym: t.take("ISYM").and_then(|c| c.into_int()).unwrap_or_default(),
            iafter: t.take("IAFTER").and_then(|c| c.into_int()).unwrap_or_default(),
            percele: t.take("PERCELE").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            isrocut: t.take("ISROCUT").and_then(|c| c.into_int()).unwrap_or_default(),
            x01: t.take("X01").and_then(|c| c.into_float()).unwrap_or_default(),
            y01: t.take("Y01").and_then(|c| c.into_float()).unwrap_or_default(),
            z01: t.take("Z01").and_then(|c| c.into_float()).unwrap_or_default(),
            x02: t.take("X02").and_then(|c| c.into_float()).unwrap_or_default(),
            y02: t.take("Y02").and_then(|c| c.into_float()).unwrap_or_default(),
            z02: t.take("Z02").and_then(|c| c.into_float()).unwrap_or_default(),
            x03: t.take("X03").and_then(|c| c.into_float()).unwrap_or_default(),
            y03: t.take("Y03").and_then(|c| c.into_float()).unwrap_or_default(),
            z03: t.take("Z03").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_TRANSFORM {
    pub filename: Vec<String>,
    pub idnoff: Vec<i64>,
    pub ideoff: Vec<i64>,
    pub idpoff: Vec<i64>,
    pub idmoff: Vec<i64>,
    pub idsoff: Vec<i64>,
    pub idfoff: Vec<i64>,
    pub iddoff: Vec<i64>,
    pub idroff: Vec<i64>,
    pub f: Vec<i64>,
    pub prefix: Vec<String>,
    pub suffix: Vec<String>,
    pub fctmas: Vec<f64>,
    pub fcttim: Vec<f64>,
    pub fctlen: Vec<f64>,
    pub fcttem: Vec<String>,
    pub incout1: Vec<i64>,
    pub fctchg: Vec<f64>,
    pub tranid: Vec<i64>,
}
impl INCLUDE_TRANSFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_TRANSFORM").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            idnoff: t.take("IDNOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ideoff: t.take("IDEOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idpoff: t.take("IDPOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idmoff: t.take("IDMOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idsoff: t.take("IDSOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idfoff: t.take("IDFOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            iddoff: t.take("IDDOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idroff: t.take("IDROFF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            prefix: t.take("PREFIX").and_then(|c| c.into_str()).unwrap_or_default(),
            suffix: t.take("SUFFIX").and_then(|c| c.into_str()).unwrap_or_default(),
            fctmas: t.take("FCTMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            fcttim: t.take("FCTTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            fctlen: t.take("FCTLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fcttem: t.take("FCTTEM").and_then(|c| c.into_str()).unwrap_or_default(),
            incout1: t.take("INCOUT1").and_then(|c| c.into_int()).unwrap_or_default(),
            fctchg: t.take("FCTCHG").and_then(|c| c.into_float()).unwrap_or_default(),
            tranid: t.take("TRANID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_TRANSFORM_BINARY {
    pub filename: Vec<String>,
    pub idnoff: Vec<i64>,
    pub ideoff: Vec<i64>,
    pub idpoff: Vec<i64>,
    pub idmoff: Vec<i64>,
    pub idsoff: Vec<i64>,
    pub idfoff: Vec<i64>,
    pub iddoff: Vec<i64>,
    pub idroff: Vec<i64>,
    pub f: Vec<i64>,
    pub prefix: Vec<String>,
    pub suffix: Vec<String>,
    pub fctmas: Vec<f64>,
    pub fcttim: Vec<f64>,
    pub fctlen: Vec<f64>,
    pub fcttem: Vec<String>,
    pub incout1: Vec<i64>,
    pub fctchg: Vec<f64>,
    pub tranid: Vec<i64>,
}
impl INCLUDE_TRANSFORM_BINARY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_TRANSFORM_BINARY").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            idnoff: t.take("IDNOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ideoff: t.take("IDEOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idpoff: t.take("IDPOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idmoff: t.take("IDMOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idsoff: t.take("IDSOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idfoff: t.take("IDFOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            iddoff: t.take("IDDOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idroff: t.take("IDROFF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            prefix: t.take("PREFIX").and_then(|c| c.into_str()).unwrap_or_default(),
            suffix: t.take("SUFFIX").and_then(|c| c.into_str()).unwrap_or_default(),
            fctmas: t.take("FCTMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            fcttim: t.take("FCTTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            fctlen: t.take("FCTLEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fcttem: t.take("FCTTEM").and_then(|c| c.into_str()).unwrap_or_default(),
            incout1: t.take("INCOUT1").and_then(|c| c.into_int()).unwrap_or_default(),
            fctchg: t.take("FCTCHG").and_then(|c| c.into_float()).unwrap_or_default(),
            tranid: t.take("TRANID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_TRIM {
    pub filename: Vec<String>,
}
impl INCLUDE_TRIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_TRIM").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_UNITCELL {
    pub filename: Vec<String>,
    pub inpt: Vec<i64>,
    pub oupt: Vec<i64>,
    pub nedof: Vec<i64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub nex: Vec<i64>,
    pub ney: Vec<i64>,
    pub nez: Vec<i64>,
    pub nnpe: Vec<i64>,
    pub tol: Vec<f64>,
    pub noff: Vec<i64>,
    pub eoff: Vec<i64>,
    pub pnm: Vec<i64>,
    pub cnx: Vec<i64>,
    pub cny: Vec<i64>,
    pub cnz: Vec<i64>,
    pub ecnx: Vec<i64>,
    pub ecny: Vec<i64>,
    pub ecnz: Vec<i64>,
}
impl INCLUDE_UNITCELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_UNITCELL").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            inpt: t.take("INPT").and_then(|c| c.into_int()).unwrap_or_default(),
            oupt: t.take("OUPT").and_then(|c| c.into_int()).unwrap_or_default(),
            nedof: t.take("NEDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            nex: t.take("NEX").and_then(|c| c.into_int()).unwrap_or_default(),
            ney: t.take("NEY").and_then(|c| c.into_int()).unwrap_or_default(),
            nez: t.take("NEZ").and_then(|c| c.into_int()).unwrap_or_default(),
            nnpe: t.take("NNPE").and_then(|c| c.into_int()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            noff: t.take("NOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            eoff: t.take("EOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            pnm: t.take("PNM").and_then(|c| c.into_int()).unwrap_or_default(),
            cnx: t.take("CNX").and_then(|c| c.into_int()).unwrap_or_default(),
            cny: t.take("CNY").and_then(|c| c.into_int()).unwrap_or_default(),
            cnz: t.take("CNZ").and_then(|c| c.into_int()).unwrap_or_default(),
            ecnx: t.take("ECNX").and_then(|c| c.into_int()).unwrap_or_default(),
            ecny: t.take("ECNY").and_then(|c| c.into_int()).unwrap_or_default(),
            ecnz: t.take("ECNZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_WD_FINIAL_PART {
    pub filename: Vec<String>,
}
impl INCLUDE_WD_FINIAL_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_WD_FINIAL_PART").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_WD_INITIAL_BLANK {
    pub filename: Vec<String>,
}
impl INCLUDE_WD_INITIAL_BLANK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_WD_INITIAL_BLANK").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INCLUDE_WD_WELDLINE_CURVE {
    pub filename: Vec<String>,
}
impl INCLUDE_WD_WELDLINE_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INCLUDE_WD_WELDLINE_CURVE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_AIRBAG_PARTICLE_POSITION {
    pub bag_id: Vec<i64>,
    pub f: Vec<f64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl INITIAL_AIRBAG_PARTICLE_POSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_AIRBAG_PARTICLE_POSITION").unwrap());
        Self {
            bag_id: t.take("Bag_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_ALE_MAPPING {
    pub pid: Vec<i64>,
    pub typ: Vec<i64>,
    pub ammsid: Vec<i64>,
    pub xo: Vec<f64>,
    pub yo: Vec<f64>,
    pub zo: Vec<f64>,
    pub vecid: Vec<i64>,
    pub angle: Vec<f64>,
    pub sym: Vec<i64>,
    pub tbeg: Vec<f64>,
}
impl INITIAL_ALE_MAPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_ALE_MAPPING").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            typ: t.take("TYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ammsid: t.take("AMMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            yo: t.take("YO").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("ZO").and_then(|c| c.into_float()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            sym: t.take("SYM").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_AXIAL_FORCE_BEAM {
    pub bsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub scale: Vec<f64>,
    pub kbend: Vec<i64>,
}
impl INITIAL_AXIAL_FORCE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_AXIAL_FORCE_BEAM").unwrap());
        Self {
            bsid: t.take("BSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            kbend: t.take("KBEND").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_CONTACT_WEAR {
    pub cid: Vec<i64>,
    pub nid: Vec<i64>,
    pub wdepth: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
    pub iseq: Vec<i64>,
    pub ncyc: Vec<i64>,
}
impl INITIAL_CONTACT_WEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_CONTACT_WEAR").unwrap());
        Self {
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            wdepth: t.take("WDEPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iseq: t.take("ISEQ").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_CRASHFRONT {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
}
impl INITIAL_CRASHFRONT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_CRASHFRONT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_DETONATION {
    pub pid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub lt: Vec<f64>,
    pub f: Vec<i64>,
    pub mmgset: Vec<i64>,
    pub peak: Vec<f64>,
    pub decay: Vec<f64>,
    pub xs: Vec<f64>,
    pub ys: Vec<f64>,
    pub zs: Vec<f64>,
    pub nid: Vec<i64>,
}
impl INITIAL_DETONATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_DETONATION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            lt: t.take("LT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgset: t.take("MMGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            peak: t.take("PEAK").and_then(|c| c.into_float()).unwrap_or_default(),
            decay: t.take("DECAY").and_then(|c| c.into_float()).unwrap_or_default(),
            xs: t.take("XS").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            zs: t.take("ZS").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_DETONATION_GEOMETRY {
    pub heid: Vec<i64>,
    pub hetyp: Vec<f64>,
    pub mmgse: Vec<f64>,
    pub geotyp: Vec<i64>,
    pub lt: Vec<f64>,
    pub dgeo: Vec<f64>,
    pub v1: Vec<i64>,
    pub v2: Vec<i64>,
    pub v3: Vec<i64>,
    pub v4: Vec<i64>,
}
impl INITIAL_DETONATION_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_DETONATION_GEOMETRY").unwrap());
        Self {
            heid: t.take("HEID").and_then(|c| c.into_int()).unwrap_or_default(),
            hetyp: t.take("HETYP").and_then(|c| c.into_float()).unwrap_or_default(),
            mmgse: t.take("MMGSE").and_then(|c| c.into_float()).unwrap_or_default(),
            geotyp: t.take("GEOTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lt: t.take("LT").and_then(|c| c.into_float()).unwrap_or_default(),
            dgeo: t.take("DGEO").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_int()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_int()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_int()).unwrap_or_default(),
            v4: t.take("V4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FATIGUE_DAMAGE_RATIO {
    pub pid_sid: Vec<i64>,
    pub ptyp: Vec<i64>,
    pub dratio: Vec<f64>,
}
impl INITIAL_FATIGUE_DAMAGE_RATIO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FATIGUE_DAMAGE_RATIO").unwrap());
        Self {
            pid_sid: t.take("PID/SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ptyp: t.take("PTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            dratio: t.take("DRATIO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FATIGUE_DAMAGE_RATIO_D3FTG {
    pub filename: Vec<String>,
}
impl INITIAL_FATIGUE_DAMAGE_RATIO_D3FTG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FATIGUE_DAMAGE_RATIO_D3FTG").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FATIGUE_DAMAGE_RATIO_D3PLOT {
    pub filename: Vec<String>,
    pub nstate: Vec<i64>,
    pub neiphd: Vec<i64>,
    pub neipsd: Vec<i64>,
}
impl INITIAL_FATIGUE_DAMAGE_RATIO_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FATIGUE_DAMAGE_RATIO_D3PLOT").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            nstate: t.take("NSTATE").and_then(|c| c.into_int()).unwrap_or_default(),
            neiphd: t.take("NEIPHD").and_then(|c| c.into_int()).unwrap_or_default(),
            neipsd: t.take("NEIPSD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FIELD_SOLID {
    pub eid: Vec<i64>,
    pub nint: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub fld1: Vec<i64>,
    pub fld2: Vec<i64>,
    pub fld3: Vec<i64>,
    pub fld4: Vec<i64>,
    pub fld5: Vec<i64>,
    pub fld6: Vec<i64>,
    pub fld7: Vec<i64>,
    pub fld8: Vec<i64>,
}
impl INITIAL_FIELD_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FIELD_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            fld1: t.take("FLD1").and_then(|c| c.into_int()).unwrap_or_default(),
            fld2: t.take("FLD2").and_then(|c| c.into_int()).unwrap_or_default(),
            fld3: t.take("FLD3").and_then(|c| c.into_int()).unwrap_or_default(),
            fld4: t.take("FLD4").and_then(|c| c.into_int()).unwrap_or_default(),
            fld5: t.take("FLD5").and_then(|c| c.into_int()).unwrap_or_default(),
            fld6: t.take("FLD6").and_then(|c| c.into_int()).unwrap_or_default(),
            fld7: t.take("FLD7").and_then(|c| c.into_int()).unwrap_or_default(),
            fld8: t.take("FLD8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FOAM_REFERENCE_GEOMETRY {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl INITIAL_FOAM_REFERENCE_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FOAM_REFERENCE_GEOMETRY").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_FOAM_REFERENCE_GEOMETRY_RAMP {
    pub ndtrrg: Vec<i64>,
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl INITIAL_FOAM_REFERENCE_GEOMETRY_RAMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_FOAM_REFERENCE_GEOMETRY_RAMP").unwrap());
        Self {
            ndtrrg: t.take("NDTRRG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_GAS_MIXTURE {
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub mmgid: Vec<i64>,
    pub temp: Vec<f64>,
    pub ro1: Vec<f64>,
    pub ro2: Vec<f64>,
    pub ro3: Vec<f64>,
    pub ro4: Vec<f64>,
    pub ro5: Vec<f64>,
    pub ro6: Vec<f64>,
    pub ro7: Vec<f64>,
    pub ro8: Vec<f64>,
}
impl INITIAL_GAS_MIXTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_GAS_MIXTURE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgid: t.take("MMGID").and_then(|c| c.into_int()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            ro1: t.take("RO1").and_then(|c| c.into_float()).unwrap_or_default(),
            ro2: t.take("RO2").and_then(|c| c.into_float()).unwrap_or_default(),
            ro3: t.take("RO3").and_then(|c| c.into_float()).unwrap_or_default(),
            ro4: t.take("RO4").and_then(|c| c.into_float()).unwrap_or_default(),
            ro5: t.take("RO5").and_then(|c| c.into_float()).unwrap_or_default(),
            ro6: t.take("RO6").and_then(|c| c.into_float()).unwrap_or_default(),
            ro7: t.take("RO7").and_then(|c| c.into_float()).unwrap_or_default(),
            ro8: t.take("RO8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_HISTORY_NODE {
    pub nid: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub hindex: Vec<i64>,
    pub val: Vec<f64>,
}
impl INITIAL_HISTORY_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_HISTORY_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            hindex: t.take("HINDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_HISTORY_NODE_SET {
    pub nid: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub hindex: Vec<i64>,
    pub val: Vec<f64>,
}
impl INITIAL_HISTORY_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_HISTORY_NODE_SET").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            hindex: t.take("HINDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_HYDROSTATIC_ALE {
    pub alesid: Vec<i64>,
    pub stype: Vec<i64>,
    pub vecid: Vec<i64>,
    pub grav: Vec<f64>,
    pub pbase: Vec<f64>,
    pub f: Vec<i64>,
    pub nid: Vec<i64>,
    pub mmgblo: Vec<i64>,
}
impl INITIAL_HYDROSTATIC_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_HYDROSTATIC_ALE").unwrap());
        Self {
            alesid: t.take("ALESID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            pbase: t.take("PBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            mmgblo: t.take("MMGBLO").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_II_EOS_ALE {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub mmg: Vec<i64>,
    pub e0: Vec<f64>,
    pub v0: Vec<f64>,
    pub p0: Vec<f64>,
}
impl INITIAL_II_EOS_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_II_EOS_ALE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            mmg: t.take("MMG").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_IMPULSE_MINE {
    pub ssid: Vec<i64>,
    pub mtnt: Vec<f64>,
    pub rhos: Vec<f64>,
    pub depth: Vec<f64>,
    pub area: Vec<f64>,
    pub scale: Vec<f64>,
    pub unused: Vec<i64>,
    pub unit: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub nidmc: Vec<i64>,
    pub gvid: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub psid: Vec<i64>,
    pub search: Vec<f64>,
}
impl INITIAL_IMPULSE_MINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_IMPULSE_MINE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtnt: t.take("MTNT").and_then(|c| c.into_float()).unwrap_or_default(),
            rhos: t.take("RHOS").and_then(|c| c.into_float()).unwrap_or_default(),
            depth: t.take("DEPTH").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            nidmc: t.take("NIDMC").and_then(|c| c.into_int()).unwrap_or_default(),
            gvid: t.take("GVID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            search: t.take("SEARCH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_INTERNAL_DOF_SOLID_TYPE3 {
    pub lid: Vec<i64>,
    pub valx: Vec<f64>,
    pub valy: Vec<f64>,
    pub valz: Vec<f64>,
}
impl INITIAL_INTERNAL_DOF_SOLID_TYPE3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_INTERNAL_DOF_SOLID_TYPE3").unwrap());
        Self {
            lid: t.take("LID").and_then(|c| c.into_int()).unwrap_or_default(),
            valx: t.take("VALX").and_then(|c| c.into_float()).unwrap_or_default(),
            valy: t.take("VALY").and_then(|c| c.into_float()).unwrap_or_default(),
            valz: t.take("VALZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_INTERNAL_DOF_SOLID_TYPE4 {
    pub lid: Vec<i64>,
    pub valx: Vec<f64>,
    pub valy: Vec<f64>,
    pub valz: Vec<f64>,
}
impl INITIAL_INTERNAL_DOF_SOLID_TYPE4 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_INTERNAL_DOF_SOLID_TYPE4").unwrap());
        Self {
            lid: t.take("LID").and_then(|c| c.into_int()).unwrap_or_default(),
            valx: t.take("VALX").and_then(|c| c.into_float()).unwrap_or_default(),
            valy: t.take("VALY").and_then(|c| c.into_float()).unwrap_or_default(),
            valz: t.take("VALZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_LAG_MAPPING {
    pub setid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub vecid: Vec<i64>,
    pub angle: Vec<f64>,
    pub nelangl: Vec<i64>,
}
impl INITIAL_LAG_MAPPING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_LAG_MAPPING").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nelangl: t.take("NELANGL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_LAG_MAPPING_WRITE {
    pub setid: Vec<i64>,
}
impl INITIAL_LAG_MAPPING_WRITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_LAG_MAPPING_WRITE").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_LAG_MAPPING_WRITE3DAXI {
    pub setid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub vecid: Vec<i64>,
    pub angle: Vec<f64>,
    pub nelangl: Vec<i64>,
}
impl INITIAL_LAG_MAPPING_WRITE3DAXI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_LAG_MAPPING_WRITE3DAXI").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nelangl: t.take("NELANGL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_MOMENTUM {
    pub eid: Vec<i64>,
    pub mx: Vec<f64>,
    pub my: Vec<f64>,
    pub mz: Vec<f64>,
    pub dept: Vec<f64>,
}
impl INITIAL_MOMENTUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_MOMENTUM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            mx: t.take("MX").and_then(|c| c.into_float()).unwrap_or_default(),
            my: t.take("MY").and_then(|c| c.into_float()).unwrap_or_default(),
            mz: t.take("MZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dept: t.take("DEPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_PWP_DEPTH {
    pub pid: Vec<i64>,
    pub lc: Vec<i64>,
}
impl INITIAL_PWP_DEPTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_PWP_DEPTH").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_PWP_DEPTH_SET {
    pub pid: Vec<i64>,
    pub lc: Vec<i64>,
}
impl INITIAL_PWP_DEPTH_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_PWP_DEPTH_SET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_PWP_NODAL_DATA {
    pub nid: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub pid: Vec<i64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
}
impl INITIAL_PWP_NODAL_DATA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_PWP_NODAL_DATA").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_SOLID_VOLUME {
    pub psid: Vec<i64>,
}
impl INITIAL_SOLID_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_SOLID_VOLUME").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SHELL {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub large: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub ilocal: Vec<i64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
    pub t: Vec<f64>,
}
impl INITIAL_STRAIN_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            ilocal: t.take("ILOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SHELL_IGA {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthk: Vec<i64>,
    pub large: Vec<i64>,
    pub r: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
    pub thki: Vec<f64>,
}
impl INITIAL_STRAIN_SHELL_IGA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SHELL_IGA").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthk: t.take("NTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            thki: t.take("THKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SHELL_NURBS_PATCH {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthk: Vec<i64>,
    pub large: Vec<i64>,
    pub r: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
    pub thki: Vec<f64>,
}
impl INITIAL_STRAIN_SHELL_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SHELL_NURBS_PATCH").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthk: t.take("NTHK").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            thki: t.take("THKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SHELL_SET {
    pub eid: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub large: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub ilocal: Vec<i64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
    pub t: Vec<f64>,
    pub epsxx_2: Vec<f64>,
    pub epsyy_2: Vec<f64>,
    pub epszz_2: Vec<f64>,
    pub epsxy_2: Vec<f64>,
    pub epsyz_2: Vec<f64>,
    pub epszx_2: Vec<f64>,
    pub t_2: Vec<f64>,
}
impl INITIAL_STRAIN_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SHELL_SET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            ilocal: t.take("ILOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxx_2: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy_2: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz_2: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy_2: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz_2: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx_2: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            t_2: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SOLID {
    pub eid: Vec<i64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
}
impl INITIAL_STRAIN_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_SOLID_SET {
    pub eid: Vec<i64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
}
impl INITIAL_STRAIN_SOLID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_SOLID_SET").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRAIN_TSHELL {
    pub eid: Vec<i64>,
    pub epsxx: Vec<f64>,
    pub epsyy: Vec<f64>,
    pub epszz: Vec<f64>,
    pub epsxy: Vec<f64>,
    pub epsyz: Vec<f64>,
    pub epszx: Vec<f64>,
    pub epsxx_2: Vec<f64>,
    pub epsyy_2: Vec<f64>,
    pub epszz_2: Vec<f64>,
    pub epsxy_2: Vec<f64>,
    pub epsyz_2: Vec<f64>,
    pub epszx_2: Vec<f64>,
}
impl INITIAL_STRAIN_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRAIN_TSHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            epsxx: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxx_2: t.take("EPSXX").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyy_2: t.take("EPSYY").and_then(|c| c.into_float()).unwrap_or_default(),
            epszz_2: t.take("EPSZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epsxy_2: t.take("EPSXY").and_then(|c| c.into_float()).unwrap_or_default(),
            epsyz_2: t.take("EPSYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            epszx_2: t.take("EPSZX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_BEAM {
    pub eid: Vec<i64>,
    pub rule: Vec<i64>,
    pub npts: Vec<i64>,
    pub local: Vec<i64>,
    pub large: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub naxes: Vec<i64>,
    pub f11: Vec<f64>,
    pub t11: Vec<f64>,
    pub m12: Vec<f64>,
    pub m13: Vec<f64>,
    pub m22: Vec<f64>,
    pub m23: Vec<f64>,
    pub parm: Vec<f64>,
    pub f11_2: Vec<f64>,
    pub t11_2: Vec<f64>,
    pub m12_2: Vec<f64>,
    pub m13_2: Vec<f64>,
    pub m22_2: Vec<f64>,
    pub m23_2: Vec<f64>,
    pub parm_2: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub sig11: Vec<f64>,
    pub sig22: Vec<f64>,
    pub sig33: Vec<f64>,
    pub sig12: Vec<f64>,
    pub sig23: Vec<f64>,
    pub sig31: Vec<f64>,
    pub eps: Vec<f64>,
    pub sig11_2: Vec<f64>,
    pub sig22_2: Vec<f64>,
    pub sig33_2: Vec<f64>,
    pub sig12_2: Vec<f64>,
    pub sig23_2: Vec<f64>,
    pub sig31_2: Vec<f64>,
    pub eps_2: Vec<f64>,
    pub hisv1_2: Vec<f64>,
    pub hisv2_2: Vec<f64>,
    pub hisv3_2: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
    pub ax1: Vec<f64>,
    pub ax2: Vec<f64>,
    pub ax3: Vec<f64>,
    pub ax4: Vec<f64>,
    pub ax5: Vec<f64>,
    pub ax6: Vec<f64>,
    pub ax7: Vec<f64>,
    pub ax8: Vec<f64>,
    pub ax9: Vec<f64>,
    pub ax10: Vec<f64>,
    pub ax11: Vec<f64>,
    pub ax12: Vec<f64>,
}
impl INITIAL_STRESS_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_BEAM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            rule: t.take("RULE").and_then(|c| c.into_int()).unwrap_or_default(),
            npts: t.take("NPTS").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            naxes: t.take("NAXES").and_then(|c| c.into_int()).unwrap_or_default(),
            f11: t.take("F11").and_then(|c| c.into_float()).unwrap_or_default(),
            t11: t.take("T11").and_then(|c| c.into_float()).unwrap_or_default(),
            m12: t.take("M12").and_then(|c| c.into_float()).unwrap_or_default(),
            m13: t.take("M13").and_then(|c| c.into_float()).unwrap_or_default(),
            m22: t.take("M22").and_then(|c| c.into_float()).unwrap_or_default(),
            m23: t.take("M23").and_then(|c| c.into_float()).unwrap_or_default(),
            parm: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            f11_2: t.take("F11").and_then(|c| c.into_float()).unwrap_or_default(),
            t11_2: t.take("T11").and_then(|c| c.into_float()).unwrap_or_default(),
            m12_2: t.take("M12").and_then(|c| c.into_float()).unwrap_or_default(),
            m13_2: t.take("M13").and_then(|c| c.into_float()).unwrap_or_default(),
            m22_2: t.take("M22").and_then(|c| c.into_float()).unwrap_or_default(),
            m23_2: t.take("M23").and_then(|c| c.into_float()).unwrap_or_default(),
            parm_2: t.take("PARM").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            sig11: t.take("SIG11").and_then(|c| c.into_float()).unwrap_or_default(),
            sig22: t.take("SIG22").and_then(|c| c.into_float()).unwrap_or_default(),
            sig33: t.take("SIG33").and_then(|c| c.into_float()).unwrap_or_default(),
            sig12: t.take("SIG12").and_then(|c| c.into_float()).unwrap_or_default(),
            sig23: t.take("SIG23").and_then(|c| c.into_float()).unwrap_or_default(),
            sig31: t.take("SIG31").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig11_2: t.take("SIG11").and_then(|c| c.into_float()).unwrap_or_default(),
            sig22_2: t.take("SIG22").and_then(|c| c.into_float()).unwrap_or_default(),
            sig33_2: t.take("SIG33").and_then(|c| c.into_float()).unwrap_or_default(),
            sig12_2: t.take("SIG12").and_then(|c| c.into_float()).unwrap_or_default(),
            sig23_2: t.take("SIG23").and_then(|c| c.into_float()).unwrap_or_default(),
            sig31_2: t.take("SIG31").and_then(|c| c.into_float()).unwrap_or_default(),
            eps_2: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1_2: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2_2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3_2: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
            ax1: t.take("AX1").and_then(|c| c.into_float()).unwrap_or_default(),
            ax2: t.take("AX2").and_then(|c| c.into_float()).unwrap_or_default(),
            ax3: t.take("AX3").and_then(|c| c.into_float()).unwrap_or_default(),
            ax4: t.take("AX4").and_then(|c| c.into_float()).unwrap_or_default(),
            ax5: t.take("AX5").and_then(|c| c.into_float()).unwrap_or_default(),
            ax6: t.take("AX6").and_then(|c| c.into_float()).unwrap_or_default(),
            ax7: t.take("AX7").and_then(|c| c.into_float()).unwrap_or_default(),
            ax8: t.take("AX8").and_then(|c| c.into_float()).unwrap_or_default(),
            ax9: t.take("AX9").and_then(|c| c.into_float()).unwrap_or_default(),
            ax10: t.take("AX10").and_then(|c| c.into_float()).unwrap_or_default(),
            ax11: t.take("AX11").and_then(|c| c.into_float()).unwrap_or_default(),
            ax12: t.take("AX12").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_DEPTH {
    pub pid: Vec<i64>,
    pub ro_g: Vec<f64>,
    pub zdatum: Vec<f64>,
    pub kfact: Vec<f64>,
    pub lc: Vec<i64>,
    pub lch: Vec<i64>,
    pub lck0: Vec<i64>,
}
impl INITIAL_STRESS_DEPTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_DEPTH").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro_g: t.take("RO_G").and_then(|c| c.into_float()).unwrap_or_default(),
            zdatum: t.take("ZDATUM").and_then(|c| c.into_float()).unwrap_or_default(),
            kfact: t.take("KFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_DEPTH_SET {
    pub psid: Vec<i64>,
    pub ro_g: Vec<f64>,
    pub zdatum: Vec<f64>,
    pub kfact: Vec<f64>,
    pub lc: Vec<i64>,
    pub lch: Vec<i64>,
    pub lck0: Vec<i64>,
}
impl INITIAL_STRESS_DEPTH_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_DEPTH_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro_g: t.take("RO_G").and_then(|c| c.into_float()).unwrap_or_default(),
            zdatum: t.take("ZDATUM").and_then(|c| c.into_float()).unwrap_or_default(),
            kfact: t.take("KFACT").and_then(|c| c.into_float()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_DES {
    pub eid: Vec<i64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub coor: Vec<f64>,
}
impl INITIAL_STRESS_DES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_DES").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            coor: t.take("COOR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_IGA_SHELL {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub r: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
}
impl INITIAL_STRESS_IGA_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_IGA_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SECTION {
    pub issid: Vec<i64>,
    pub csid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub psid: Vec<i64>,
    pub vid: Vec<i64>,
    pub izshear: Vec<i64>,
    pub istiff: Vec<i64>,
}
impl INITIAL_STRESS_SECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SECTION").unwrap());
        Self {
            issid: t.take("ISSID").and_then(|c| c.into_int()).unwrap_or_default(),
            csid: t.take("CSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_int()).unwrap_or_default(),
            izshear: t.take("IZSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            istiff: t.take("ISTIFF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SHELL {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub ntensr: Vec<i64>,
    pub large: Vec<i64>,
    pub nthint: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
}
impl INITIAL_STRESS_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            ntensr: t.take("NTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthint: t.take("NTHINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SHELL_NURBS_PATCH {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub r: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
}
impl INITIAL_STRESS_SHELL_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SHELL_NURBS_PATCH").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SHELL_SET {
    pub sid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub ntensr: Vec<i64>,
    pub large: Vec<i64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
}
impl INITIAL_STRESS_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SHELL_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            ntensr: t.take("NTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SOLID {
    pub eid: Vec<i64>,
    pub nint: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub iveflg: Vec<i64>,
    pub ialegp: Vec<i64>,
    pub nthint: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub sigxx_2: Vec<f64>,
    pub sigyy_2: Vec<f64>,
    pub sigzz_2: Vec<f64>,
    pub sigxy_2: Vec<f64>,
    pub sigyz_2: Vec<f64>,
}
impl INITIAL_STRESS_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            iveflg: t.take("IVEFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ialegp: t.take("IALEGP").and_then(|c| c.into_int()).unwrap_or_default(),
            nthint: t.take("NTHINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx_2: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy_2: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz_2: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy_2: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz_2: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SOLID_NURBS_PATCH {
    pub eid: Vec<i64>,
    pub nint: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub r: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub hisv1: Vec<f64>,
    pub hisv2: Vec<f64>,
    pub hisv3: Vec<f64>,
    pub hisv4: Vec<f64>,
    pub hisv5: Vec<f64>,
    pub hisv6: Vec<f64>,
    pub hisv7: Vec<f64>,
    pub hisv8: Vec<f64>,
}
impl INITIAL_STRESS_SOLID_NURBS_PATCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SOLID_NURBS_PATCH").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv1: t.take("HISV1").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv2: t.take("HISV2").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv3: t.take("HISV3").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv4: t.take("HISV4").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv5: t.take("HISV5").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv6: t.take("HISV6").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv7: t.take("HISV7").and_then(|c| c.into_float()).unwrap_or_default(),
            hisv8: t.take("HISV8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SOLID_SET {
    pub sid: Vec<i64>,
    pub nint: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub iveflg: Vec<i64>,
    pub ialegp: Vec<i64>,
    pub nthint: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
    pub sigxx_2: Vec<f64>,
    pub sigyy_2: Vec<f64>,
    pub sigzz_2: Vec<f64>,
    pub sigxy_2: Vec<f64>,
    pub sigyz_2: Vec<f64>,
}
impl INITIAL_STRESS_SOLID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SOLID_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            iveflg: t.take("IVEFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ialegp: t.take("IALEGP").and_then(|c| c.into_int()).unwrap_or_default(),
            nthint: t.take("NTHINT").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx_2: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy_2: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz_2: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy_2: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz_2: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_SPH {
    pub eid: Vec<i64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
}
impl INITIAL_STRESS_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_SPH").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_TSHELL {
    pub eid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
}
impl INITIAL_STRESS_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_TSHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_STRESS_TSHELL_SET {
    pub sid: Vec<i64>,
    pub nplane: Vec<i64>,
    pub nthick: Vec<i64>,
    pub nhisv: Vec<i64>,
    pub large: Vec<i64>,
    pub t: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigzz: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub sigyz: Vec<f64>,
    pub sigzx: Vec<f64>,
    pub eps: Vec<f64>,
}
impl INITIAL_STRESS_TSHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_STRESS_TSHELL_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplane: t.take("NPLANE").and_then(|c| c.into_int()).unwrap_or_default(),
            nthick: t.take("NTHICK").and_then(|c| c.into_int()).unwrap_or_default(),
            nhisv: t.take("NHISV").and_then(|c| c.into_int()).unwrap_or_default(),
            large: t.take("LARGE").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzz: t.take("SIGZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyz: t.take("SIGYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigzx: t.take("SIGZX").and_then(|c| c.into_float()).unwrap_or_default(),
            eps: t.take("EPS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_TEMPERATURE_NODE {
    pub nid: Vec<i64>,
    pub temp: Vec<f64>,
    pub loc: Vec<i64>,
}
impl INITIAL_TEMPERATURE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_TEMPERATURE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_TEMPERATURE_SET {
    pub nsid: Vec<i64>,
    pub temp: Vec<f64>,
    pub loc: Vec<i64>,
}
impl INITIAL_TEMPERATURE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_TEMPERATURE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VAPOR_PART {
    pub pid: Vec<i64>,
}
impl INITIAL_VAPOR_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VAPOR_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VEHICLE_KINEMATICS {
    pub grav: Vec<i64>,
    pub psid: Vec<i64>,
    pub xo: Vec<f64>,
    pub yo: Vec<f64>,
    pub zo: Vec<f64>,
    pub xf: Vec<f64>,
    pub yf: Vec<f64>,
    pub zf: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub aaxis: Vec<i64>,
    pub baxis: Vec<i64>,
    pub caxis: Vec<i64>,
    pub aang: Vec<f64>,
    pub bang: Vec<f64>,
    pub cang: Vec<f64>,
    pub wa: Vec<f64>,
    pub wb: Vec<f64>,
    pub wc: Vec<f64>,
}
impl INITIAL_VEHICLE_KINEMATICS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VEHICLE_KINEMATICS").unwrap());
        Self {
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            yo: t.take("YO").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("ZO").and_then(|c| c.into_float()).unwrap_or_default(),
            xf: t.take("XF").and_then(|c| c.into_float()).unwrap_or_default(),
            yf: t.take("YF").and_then(|c| c.into_float()).unwrap_or_default(),
            zf: t.take("ZF").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            aaxis: t.take("AAXIS").and_then(|c| c.into_int()).unwrap_or_default(),
            baxis: t.take("BAXIS").and_then(|c| c.into_int()).unwrap_or_default(),
            caxis: t.take("CAXIS").and_then(|c| c.into_int()).unwrap_or_default(),
            aang: t.take("AANG").and_then(|c| c.into_float()).unwrap_or_default(),
            bang: t.take("BANG").and_then(|c| c.into_float()).unwrap_or_default(),
            cang: t.take("CANG").and_then(|c| c.into_float()).unwrap_or_default(),
            wa: t.take("WA").and_then(|c| c.into_float()).unwrap_or_default(),
            wb: t.take("WB").and_then(|c| c.into_float()).unwrap_or_default(),
            wc: t.take("WC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VELOCITY {
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub irigid: Vec<i64>,
    pub icid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
    pub vxe: Vec<f64>,
    pub vye: Vec<f64>,
    pub vze: Vec<f64>,
    pub vxre: Vec<f64>,
    pub vyre: Vec<f64>,
    pub vzre: Vec<f64>,
}
impl INITIAL_VELOCITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VELOCITY").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            irigid: t.take("IRIGID").and_then(|c| c.into_int()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
            vxe: t.take("VXE").and_then(|c| c.into_float()).unwrap_or_default(),
            vye: t.take("VYE").and_then(|c| c.into_float()).unwrap_or_default(),
            vze: t.take("VZE").and_then(|c| c.into_float()).unwrap_or_default(),
            vxre: t.take("VXRE").and_then(|c| c.into_float()).unwrap_or_default(),
            vyre: t.take("VYRE").and_then(|c| c.into_float()).unwrap_or_default(),
            vzre: t.take("VZRE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VELOCITY_GENERATION {
    pub id: Vec<i64>,
    pub styp: Vec<i64>,
    pub omega: Vec<f64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub ivatn: Vec<i64>,
    pub icid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub nx: Vec<f64>,
    pub ny: Vec<f64>,
    pub nz: Vec<f64>,
    pub phase: Vec<i64>,
    pub irigid: Vec<i64>,
}
impl INITIAL_VELOCITY_GENERATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VELOCITY_GENERATION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            styp: t.take("STYP").and_then(|c| c.into_int()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ivatn: t.take("IVATN").and_then(|c| c.into_int()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            nx: t.take("NX").and_then(|c| c.into_float()).unwrap_or_default(),
            ny: t.take("NY").and_then(|c| c.into_float()).unwrap_or_default(),
            nz: t.take("NZ").and_then(|c| c.into_float()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            irigid: t.take("IRIGID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VELOCITY_GENERATION_START_TIME {
    pub stime: Vec<f64>,
}
impl INITIAL_VELOCITY_GENERATION_START_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VELOCITY_GENERATION_START_TIME").unwrap());
        Self {
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VELOCITY_NODE {
    pub nid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
    pub icid: Vec<i64>,
}
impl INITIAL_VELOCITY_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VELOCITY_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VELOCITY_RIGID_BODY {
    pub pid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub vxr: Vec<f64>,
    pub vyr: Vec<f64>,
    pub vzr: Vec<f64>,
    pub icid: Vec<i64>,
}
impl INITIAL_VELOCITY_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VELOCITY_RIGID_BODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vxr: t.take("VXR").and_then(|c| c.into_float()).unwrap_or_default(),
            vyr: t.take("VYR").and_then(|c| c.into_float()).unwrap_or_default(),
            vzr: t.take("VZR").and_then(|c| c.into_float()).unwrap_or_default(),
            icid: t.take("ICID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VOID_PART {
    pub pid: Vec<i64>,
}
impl INITIAL_VOID_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VOID_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VOID_SET {
    pub psid: Vec<i64>,
}
impl INITIAL_VOID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VOID_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VOLUME_FRACTION {
    pub eid: Vec<i64>,
    pub vf1: Vec<f64>,
    pub vf2: Vec<f64>,
    pub vf3: Vec<f64>,
    pub vf4: Vec<f64>,
    pub vf5: Vec<f64>,
    pub vf6: Vec<f64>,
    pub vf7: Vec<f64>,
}
impl INITIAL_VOLUME_FRACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VOLUME_FRACTION").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            vf1: t.take("VF1").and_then(|c| c.into_float()).unwrap_or_default(),
            vf2: t.take("VF2").and_then(|c| c.into_float()).unwrap_or_default(),
            vf3: t.take("VF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf4: t.take("VF4").and_then(|c| c.into_float()).unwrap_or_default(),
            vf5: t.take("VF5").and_then(|c| c.into_float()).unwrap_or_default(),
            vf6: t.take("VF6").and_then(|c| c.into_float()).unwrap_or_default(),
            vf7: t.take("VF7").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VOLUME_FRACTION_GEOMETRY {
    pub fmsid: Vec<i64>,
    pub fmidtyp: Vec<i64>,
    pub bammg: Vec<i64>,
    pub ntrace: Vec<i64>,
    pub conttyp: Vec<i64>,
    pub fillopt: Vec<i64>,
    pub fammg: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub sid: Vec<i64>,
    pub stype: Vec<i64>,
    pub normdir: Vec<i64>,
    pub xoffset: Vec<f64>,
    pub unused_3: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub sgsid: Vec<i64>,
    pub normdir_2: Vec<i64>,
    pub xoffset_2: Vec<f64>,
    pub unused_5: Vec<i64>,
    pub unused_2_3: Vec<i64>,
    pub unused_3_3: Vec<i64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5_2: Vec<i64>,
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub xcos: Vec<f64>,
    pub ycos: Vec<f64>,
    pub zcos: Vec<f64>,
    pub unused_6: Vec<f64>,
    pub unused_2_4: Vec<f64>,
    pub x0_2: Vec<f64>,
    pub y0_2: Vec<f64>,
    pub z0_2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub x0_3: Vec<f64>,
    pub y0_3: Vec<f64>,
    pub z0_3: Vec<f64>,
    pub x1_2: Vec<f64>,
    pub y1_2: Vec<f64>,
    pub z1_2: Vec<f64>,
    pub lcsid: Vec<i64>,
    pub unused_7: Vec<f64>,
    pub x0_4: Vec<f64>,
    pub y0_4: Vec<f64>,
    pub z0_4: Vec<f64>,
    pub r0: Vec<f64>,
    pub unused_8: Vec<f64>,
    pub unused_2_5: Vec<f64>,
    pub unused_3_4: Vec<f64>,
    pub unused_4_3: Vec<f64>,
}
impl INITIAL_VOLUME_FRACTION_GEOMETRY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VOLUME_FRACTION_GEOMETRY").unwrap());
        Self {
            fmsid: t.take("FMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            fmidtyp: t.take("FMIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            bammg: t.take("BAMMG").and_then(|c| c.into_int()).unwrap_or_default(),
            ntrace: t.take("NTRACE").and_then(|c| c.into_int()).unwrap_or_default(),
            conttyp: t.take("CONTTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            fillopt: t.take("FILLOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            fammg: t.take("FAMMG").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            normdir: t.take("NORMDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            xoffset: t.take("XOFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            sgsid: t.take("SGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            normdir_2: t.take("NORMDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            xoffset_2: t.take("XOFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_5: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_3: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5_2: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            xcos: t.take("XCOS").and_then(|c| c.into_float()).unwrap_or_default(),
            ycos: t.take("YCOS").and_then(|c| c.into_float()).unwrap_or_default(),
            zcos: t.take("ZCOS").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_4: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            x0_2: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0_2: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0_2: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            x0_3: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0_3: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0_3: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1_2: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1_2: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1_2: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsid: t.take("LCSID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            x0_4: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0_4: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0_4: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_8: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_5: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_4: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4_3: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INITIAL_VOLUME_FRACTION_NALEGP {
    pub nalegp: Vec<i64>,
    pub eid: Vec<i64>,
    pub vf1: Vec<f64>,
    pub vf2: Vec<f64>,
    pub vf3: Vec<f64>,
    pub vf4: Vec<f64>,
    pub vf5: Vec<f64>,
    pub vf6: Vec<f64>,
    pub vf7: Vec<f64>,
    pub vf: Vec<f64>,
    pub vf_2: Vec<f64>,
    pub vf_3: Vec<f64>,
    pub vf_4: Vec<f64>,
    pub vf_5: Vec<f64>,
    pub vf_6: Vec<f64>,
    pub vf_7: Vec<f64>,
    pub vf_8: Vec<f64>,
}
impl INITIAL_VOLUME_FRACTION_NALEGP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INITIAL_VOLUME_FRACTION_NALEGP").unwrap());
        Self {
            nalegp: t.take("NALEGP").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            vf1: t.take("VF1").and_then(|c| c.into_float()).unwrap_or_default(),
            vf2: t.take("VF2").and_then(|c| c.into_float()).unwrap_or_default(),
            vf3: t.take("VF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf4: t.take("VF4").and_then(|c| c.into_float()).unwrap_or_default(),
            vf5: t.take("VF5").and_then(|c| c.into_float()).unwrap_or_default(),
            vf6: t.take("VF6").and_then(|c| c.into_float()).unwrap_or_default(),
            vf7: t.take("VF7").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_2: t.take("VF_2").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_3: t.take("VF_3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_4: t.take("VF_4").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_5: t.take("VF_5").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_6: t.take("VF_6").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_7: t.take("VF_7").and_then(|c| c.into_float()).unwrap_or_default(),
            vf_8: t.take("VF_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTEGRATION_BEAM {
    pub irid: Vec<i64>,
    pub nip: Vec<i64>,
    pub ra: Vec<f64>,
    pub icst: Vec<i64>,
    pub k: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub sref: Vec<f64>,
    pub tref: Vec<f64>,
    pub d5: Vec<f64>,
    pub d6: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub wf: Vec<f64>,
    pub pid: Vec<i64>,
}
impl INTEGRATION_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTEGRATION_BEAM").unwrap());
        Self {
            irid: t.take("IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            ra: t.take("RA").and_then(|c| c.into_float()).unwrap_or_default(),
            icst: t.take("ICST").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            sref: t.take("SREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            d6: t.take("D6").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            wf: t.take("WF").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTEGRATION_SHELL {
    pub irid: Vec<i64>,
    pub nip: Vec<i64>,
    pub esop: Vec<i64>,
    pub failopt: Vec<i64>,
    pub s: Vec<f64>,
    pub wf: Vec<f64>,
    pub pid: Vec<i64>,
}
impl INTEGRATION_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTEGRATION_SHELL").unwrap());
        Self {
            irid: t.take("IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            esop: t.take("ESOP").and_then(|c| c.into_int()).unwrap_or_default(),
            failopt: t.take("FAILOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            wf: t.take("WF").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_ACOUSTIC {
    pub ifid: Vec<i64>,
}
impl INTERFACE_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_ACOUSTIC").unwrap());
        Self {
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_BLANKSIZE_DEVELOPMENT {
    pub ioption: Vec<i64>,
    pub unused: Vec<i64>,
    pub iadapt: Vec<i64>,
    pub maxsize: Vec<f64>,
    pub referenc: Vec<i64>,
    pub space: Vec<f64>,
    pub maxgap: Vec<f64>,
    pub orient: Vec<f64>,
    pub filename1: Vec<String>,
    pub filename2: Vec<String>,
    pub filename3: Vec<String>,
    pub filename4: Vec<String>,
    pub filename13: Vec<String>,
}
impl INTERFACE_BLANKSIZE_DEVELOPMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_BLANKSIZE_DEVELOPMENT").unwrap());
        Self {
            ioption: t.take("IOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            iadapt: t.take("IADAPT").and_then(|c| c.into_int()).unwrap_or_default(),
            maxsize: t.take("MAXSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            referenc: t.take("REFERENC").and_then(|c| c.into_int()).unwrap_or_default(),
            space: t.take("SPACE").and_then(|c| c.into_float()).unwrap_or_default(),
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
            orient: t.take("ORIENT").and_then(|c| c.into_float()).unwrap_or_default(),
            filename1: t.take("FILENAME1").and_then(|c| c.into_str()).unwrap_or_default(),
            filename2: t.take("FILENAME2").and_then(|c| c.into_str()).unwrap_or_default(),
            filename3: t.take("FILENAME3").and_then(|c| c.into_str()).unwrap_or_default(),
            filename4: t.take("FILENAME4").and_then(|c| c.into_str()).unwrap_or_default(),
            filename13: t.take("FILENAME13").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_BLANKSIZE_INITIAL_ADAPTIVE {
    pub filename9: Vec<String>,
    pub filename10: Vec<String>,
    pub filename11: Vec<String>,
    pub filename12: Vec<String>,
}
impl INTERFACE_BLANKSIZE_INITIAL_ADAPTIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_BLANKSIZE_INITIAL_ADAPTIVE").unwrap());
        Self {
            filename9: t.take("FILENAME9").and_then(|c| c.into_str()).unwrap_or_default(),
            filename10: t.take("FILENAME10").and_then(|c| c.into_str()).unwrap_or_default(),
            filename11: t.take("FILENAME11").and_then(|c| c.into_str()).unwrap_or_default(),
            filename12: t.take("FILENAME12").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_BLANKSIZE_INITIAL_TRIM {
    pub filename5: Vec<String>,
    pub filename6: Vec<String>,
    pub filename7: Vec<String>,
    pub filename8: Vec<String>,
}
impl INTERFACE_BLANKSIZE_INITIAL_TRIM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_BLANKSIZE_INITIAL_TRIM").unwrap());
        Self {
            filename5: t.take("FILENAME5").and_then(|c| c.into_str()).unwrap_or_default(),
            filename6: t.take("FILENAME6").and_then(|c| c.into_str()).unwrap_or_default(),
            filename7: t.take("FILENAME7").and_then(|c| c.into_str()).unwrap_or_default(),
            filename8: t.take("FILENAME8").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_BLANKSIZE_SCALE_FACTOR {
    pub idcrv: Vec<i64>,
    pub sf: Vec<f64>,
    pub offx: Vec<f64>,
    pub offy: Vec<f64>,
    pub offz: Vec<f64>,
}
impl INTERFACE_BLANKSIZE_SCALE_FACTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_BLANKSIZE_SCALE_FACTOR").unwrap());
        Self {
            idcrv: t.take("IDCRV").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            offx: t.take("OFFX").and_then(|c| c.into_float()).unwrap_or_default(),
            offy: t.take("OFFY").and_then(|c| c.into_float()).unwrap_or_default(),
            offz: t.take("OFFZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_BLANKSIZE_SYMMETRIC_PLANE {
    pub x0: Vec<f64>,
    pub y0: Vec<f64>,
    pub z0: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl INTERFACE_BLANKSIZE_SYMMETRIC_PLANE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_BLANKSIZE_SYMMETRIC_PLANE").unwrap());
        Self {
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            y0: t.take("Y0").and_then(|c| c.into_float()).unwrap_or_default(),
            z0: t.take("Z0").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION {
    pub stage: Vec<i64>,
    pub psidt: Vec<i64>,
    pub psidb: Vec<i64>,
    pub smooth: Vec<i64>,
    pub scale: Vec<f64>,
    pub dbname: Vec<String>,
    pub outname: Vec<String>,
}
impl INTERFACE_COMPENSATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION").unwrap());
        Self {
            stage: t.take("STAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            psidt: t.take("PSIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            psidb: t.take("PSIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            smooth: t.take("SMOOTH").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            dbname: t.take("DBNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            outname: t.take("OUTNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidp: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinear: Vec<i64>,
    pub tangent: Vec<i64>,
}
impl INTERFACE_COMPENSATION_3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinear: t.take("NLINEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            tangent: t.take("TANGENT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_ACCELERATOR {
    pub isteps: Vec<i64>,
    pub tolx: Vec<f64>,
    pub toly: Vec<f64>,
    pub tolz: Vec<f64>,
    pub option: Vec<i64>,
}
impl INTERFACE_COMPENSATION_3D_ACCELERATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_ACCELERATOR").unwrap());
        Self {
            isteps: t.take("ISTEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            tolx: t.take("TOLX").and_then(|c| c.into_float()).unwrap_or_default(),
            toly: t.take("TOLY").and_then(|c| c.into_float()).unwrap_or_default(),
            tolz: t.take("TOLZ").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_FLANGE {
    pub pid: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl INTERFACE_COMPENSATION_3D_FLANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_FLANGE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_LOCAL_SMOOTH {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidp: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinear: Vec<i64>,
}
impl INTERFACE_COMPENSATION_3D_LOCAL_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_LOCAL_SMOOTH").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinear: t.take("NLINEAR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_MULTI_STEPS {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidp: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinear: Vec<i64>,
}
impl INTERFACE_COMPENSATION_3D_MULTI_STEPS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_MULTI_STEPS").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinear: t.take("NLINEAR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_PART_CHANGE {
    pub maxgap: Vec<f64>,
}
impl INTERFACE_COMPENSATION_3D_PART_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_PART_CHANGE").unwrap());
        Self {
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_3D_REFINE_RIGID {
    pub filename1: Vec<String>,
    pub filename2: Vec<String>,
}
impl INTERFACE_COMPENSATION_3D_REFINE_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_3D_REFINE_RIGID").unwrap());
        Self {
            filename1: t.take("FILENAME1").and_then(|c| c.into_str()).unwrap_or_default(),
            filename2: t.take("FILENAME2").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_NEW {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidm: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinea: Vec<i64>,
}
impl INTERFACE_COMPENSATION_NEW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_NEW").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidm: t.take("PSIDM").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinea: t.take("NLINEA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_NEW_ACCELERATOR {
    pub isteps: Vec<i64>,
    pub tolx: Vec<f64>,
    pub toly: Vec<f64>,
    pub tolz: Vec<f64>,
    pub option: Vec<i64>,
}
impl INTERFACE_COMPENSATION_NEW_ACCELERATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_NEW_ACCELERATOR").unwrap());
        Self {
            isteps: t.take("ISTEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            tolx: t.take("TOLX").and_then(|c| c.into_float()).unwrap_or_default(),
            toly: t.take("TOLY").and_then(|c| c.into_float()).unwrap_or_default(),
            tolz: t.take("TOLZ").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_NEW_LOCAL_SMOOTH {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidm: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinea: Vec<i64>,
}
impl INTERFACE_COMPENSATION_NEW_LOCAL_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_NEW_LOCAL_SMOOTH").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidm: t.take("PSIDM").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinea: t.take("NLINEA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_NEW_MULTI_STEPS {
    pub method: Vec<i64>,
    pub sl: Vec<f64>,
    pub sf: Vec<f64>,
    pub elref: Vec<i64>,
    pub psidm: Vec<f64>,
    pub undct: Vec<f64>,
    pub angle: Vec<f64>,
    pub nlinea: Vec<i64>,
}
impl INTERFACE_COMPENSATION_NEW_MULTI_STEPS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_NEW_MULTI_STEPS").unwrap());
        Self {
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            elref: t.take("ELREF").and_then(|c| c.into_int()).unwrap_or_default(),
            psidm: t.take("PSIDM").and_then(|c| c.into_float()).unwrap_or_default(),
            undct: t.take("UNDCT").and_then(|c| c.into_float()).unwrap_or_default(),
            angle: t.take("ANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            nlinea: t.take("NLINEA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPENSATION_NEW_PART_CHANGE {
    pub maxgap: Vec<f64>,
}
impl INTERFACE_COMPENSATION_NEW_PART_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPENSATION_NEW_PART_CHANGE").unwrap());
        Self {
            maxgap: t.take("MAXGAP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPONENT_FILE {
    pub filename: Vec<String>,
    pub format: Vec<i64>,
}
impl INTERFACE_COMPONENT_FILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPONENT_FILE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            format: t.take("FORMAT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPONENT_NODE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nid: Vec<i64>,
}
impl INTERFACE_COMPONENT_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPONENT_NODE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("Title").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPONENT_SEGMENT {
    pub ssid: Vec<i64>,
}
impl INTERFACE_COMPONENT_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPONENT_SEGMENT").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_COMPONENT_SPH {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub cid: Vec<i64>,
    pub nid: Vec<i64>,
}
impl INTERFACE_COMPONENT_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_COMPONENT_SPH").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("Title").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_DE_HBOND {
    pub id: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub ptype1: Vec<i64>,
    pub ptype2: Vec<i64>,
    pub frmdl: Vec<i64>,
    pub frgk: Vec<f64>,
    pub frgs: Vec<f64>,
    pub dmg: Vec<f64>,
}
impl INTERFACE_DE_HBOND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_DE_HBOND").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype1: t.take("PTYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptype2: t.take("PTYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            frmdl: t.take("FRMDL").and_then(|c| c.into_int()).unwrap_or_default(),
            frgk: t.take("FRGK").and_then(|c| c.into_float()).unwrap_or_default(),
            frgs: t.take("FRGS").and_then(|c| c.into_float()).unwrap_or_default(),
            dmg: t.take("DMG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_ENSIGHT {
    pub nset: Vec<i64>,
    pub unused: Vec<i64>,
    pub nlfile: Vec<i64>,
    pub gfile: Vec<String>,
}
impl INTERFACE_ENSIGHT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_ENSIGHT").unwrap());
        Self {
            nset: t.take("NSET").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nlfile: t.take("NLFILE").and_then(|c| c.into_int()).unwrap_or_default(),
            gfile: t.take("GFILE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_JOY {
    pub sid: Vec<i64>,
}
impl INTERFACE_JOY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_JOY").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_DISCRETE_NODE_NODE {
    pub nid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl INTERFACE_LINKING_DISCRETE_NODE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_DISCRETE_NODE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_DISCRETE_NODE_SET {
    pub nsid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl INTERFACE_LINKING_DISCRETE_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_DISCRETE_NODE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_EDGE {
    pub nsid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl INTERFACE_LINKING_EDGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_EDGE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_FILE {
    pub filename: Vec<String>,
}
impl INTERFACE_LINKING_FILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_FILE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_FORCES {
    pub ssid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl INTERFACE_LINKING_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_FORCES").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_NODE_LOCAL {
    pub nid: Vec<i64>,
    pub ifid: Vec<i64>,
    pub fx: Vec<i64>,
    pub fy: Vec<i64>,
    pub fz: Vec<i64>,
    pub lcid: Vec<i64>,
    pub lnid: Vec<i64>,
    pub usec: Vec<i64>,
    pub usen: Vec<i64>,
}
impl INTERFACE_LINKING_NODE_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_NODE_LOCAL").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
            fx: t.take("FX").and_then(|c| c.into_int()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_int()).unwrap_or_default(),
            fz: t.take("FZ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lnid: t.take("LNID").and_then(|c| c.into_int()).unwrap_or_default(),
            usec: t.take("USEC").and_then(|c| c.into_int()).unwrap_or_default(),
            usen: t.take("USEN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_NODE_NODE {
    pub nid: Vec<i64>,
    pub ifid: Vec<i64>,
    pub fx: Vec<i64>,
    pub fy: Vec<i64>,
    pub fz: Vec<i64>,
}
impl INTERFACE_LINKING_NODE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_NODE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
            fx: t.take("FX").and_then(|c| c.into_int()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_int()).unwrap_or_default(),
            fz: t.take("FZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_NODE_SET {
    pub nsid: Vec<i64>,
    pub ifid: Vec<i64>,
    pub fx: Vec<i64>,
    pub fy: Vec<i64>,
    pub fz: Vec<i64>,
}
impl INTERFACE_LINKING_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_NODE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
            fx: t.take("FX").and_then(|c| c.into_int()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_int()).unwrap_or_default(),
            fz: t.take("FZ").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_NODE_SET_LOCAL {
    pub nsid: Vec<i64>,
    pub ifid: Vec<i64>,
    pub fx: Vec<i64>,
    pub fy: Vec<i64>,
    pub fz: Vec<i64>,
    pub lcid: Vec<i64>,
    pub lnid: Vec<i64>,
    pub usec: Vec<i64>,
    pub usen: Vec<i64>,
}
impl INTERFACE_LINKING_NODE_SET_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_NODE_SET_LOCAL").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
            fx: t.take("FX").and_then(|c| c.into_int()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_int()).unwrap_or_default(),
            fz: t.take("FZ").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lnid: t.take("LNID").and_then(|c| c.into_int()).unwrap_or_default(),
            usec: t.take("USEC").and_then(|c| c.into_int()).unwrap_or_default(),
            usen: t.take("USEN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_LINKING_SEGMENT {
    pub ssid: Vec<i64>,
    pub ifid: Vec<i64>,
}
impl INTERFACE_LINKING_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_LINKING_SEGMENT").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifid: t.take("IFID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPG_1 {
}
impl INTERFACE_SPG_1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPG_1").unwrap());
        Self {
        }
    }
}

pub struct INTERFACE_SPG_2 {
}
impl INTERFACE_SPG_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPG_2").unwrap());
        Self {
        }
    }
}

pub struct INTERFACE_SPRINGBACK {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
}
impl INTERFACE_SPRINGBACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_DYNA3D {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_DYNA3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_DYNA3D").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_DYNA3D_NOTHICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_DYNA3D_NOTHICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_DYNA3D_NOTHICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_DYNA3D_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_DYNA3D_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_DYNA3D_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_EXCLUDE {
    pub kwdname: Vec<String>,
}
impl INTERFACE_SPRINGBACK_EXCLUDE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_EXCLUDE").unwrap());
        Self {
            kwdname: t.take("KWDNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_EXCLUDE_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_EXCLUDE_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_EXCLUDE_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_LSDYNA {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_LSDYNA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_LSDYNA").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_LSDYNA_NOTHICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_LSDYNA_NOTHICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_LSDYNA_NOTHICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_LSDYNA_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_LSDYNA_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_LSDYNA_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_NASTRAN {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_NASTRAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_NASTRAN").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_NASTRAN_NOTHICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_NASTRAN_NOTHICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_NASTRAN_NOTHICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_NASTRAN_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_NASTRAN_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_NASTRAN_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_NIKE3D {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_NIKE3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_NIKE3D").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_NIKE3D_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_NIKE3D_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_NIKE3D_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_SEAMLESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_SEAMLESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_SEAMLESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SPRINGBACK_SEAMLESS_THICKNESS {
    pub psid: Vec<i64>,
    pub nshv: Vec<i64>,
    pub ftype: Vec<i64>,
    pub f: Vec<i64>,
    pub ftensr: Vec<i64>,
    pub nthhsv: Vec<i64>,
    pub rflag: Vec<i64>,
    pub intstrn: Vec<i64>,
    pub optc: Vec<String>,
    pub sldo: Vec<i64>,
    pub ncyc: Vec<i64>,
    pub fsplit: Vec<i64>,
    pub ndflag: Vec<i64>,
    pub cflag: Vec<i64>,
    pub hflag: Vec<i64>,
    pub nid: Vec<i64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl INTERFACE_SPRINGBACK_SEAMLESS_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SPRINGBACK_SEAMLESS_THICKNESS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nshv: t.take("NSHV").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ftensr: t.take("FTENSR").and_then(|c| c.into_int()).unwrap_or_default(),
            nthhsv: t.take("NTHHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            intstrn: t.take("INTSTRN").and_then(|c| c.into_int()).unwrap_or_default(),
            optc: t.take("OPTC").and_then(|c| c.into_str()).unwrap_or_default(),
            sldo: t.take("SLDO").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyc: t.take("NCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            fsplit: t.take("FSPLIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ndflag: t.take("NDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            cflag: t.take("CFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hflag: t.take("HFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
    pub gmset: Vec<i64>,
    pub sf: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub memgm: Vec<i64>,
}
impl INTERFACE_SSI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            memgm: t.take("MEMGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_AUX {
    pub gmset: Vec<i64>,
    pub setid: Vec<i64>,
}
impl INTERFACE_SSI_AUX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_AUX").unwrap());
        Self {
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_AUX_EMBEDDED {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub gmset: Vec<i64>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_AUX_EMBEDDED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_AUX_EMBEDDED").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_AUX_EMBEDDED_CONSTRAINED_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub gmset: Vec<i64>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_AUX_EMBEDDED_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_AUX_EMBEDDED_CONSTRAINED_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_AUX_EMBEDDED_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub gmset: Vec<i64>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_AUX_EMBEDDED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_AUX_EMBEDDED_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_AUX_NODE {
    pub gmset: Vec<i64>,
    pub setid: Vec<i64>,
}
impl INTERFACE_SSI_AUX_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_AUX_NODE").unwrap());
        Self {
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_CONSTRAINED_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
    pub gmset: Vec<i64>,
    pub sf: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub memgm: Vec<i64>,
}
impl INTERFACE_SSI_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_CONSTRAINED_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            memgm: t.take("MEMGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
    pub gmset: Vec<i64>,
    pub sf: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub memgm: Vec<i64>,
}
impl INTERFACE_SSI_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            memgm: t.take("MEMGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_STATIC {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_STATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_STATIC").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_STATIC_CONSTRAINED_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_STATIC_CONSTRAINED_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_STATIC_CONSTRAINED_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_SSI_STATIC_OFFSET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub strid: Vec<i64>,
    pub soilid: Vec<i64>,
    pub spr: Vec<i64>,
    pub mpr: Vec<i64>,
}
impl INTERFACE_SSI_STATIC_OFFSET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_SSI_STATIC_OFFSET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            strid: t.take("STRID").and_then(|c| c.into_int()).unwrap_or_default(),
            soilid: t.take("SOILID").and_then(|c| c.into_int()).unwrap_or_default(),
            spr: t.take("SPR").and_then(|c| c.into_int()).unwrap_or_default(),
            mpr: t.take("MPR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_THICKNESS_CHANGE_COMPENSATION {
    pub iflg: Vec<i64>,
    pub thk0: Vec<f64>,
}
impl INTERFACE_THICKNESS_CHANGE_COMPENSATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_THICKNESS_CHANGE_COMPENSATION").unwrap());
        Self {
            iflg: t.take("IFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            thk0: t.take("THK0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct INTERFACE_WELDLINE_DEVELOPMENT {
    pub ioption: Vec<i64>,
}
impl INTERFACE_WELDLINE_DEVELOPMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("INTERFACE_WELDLINE_DEVELOPMENT").unwrap());
        Self {
            ioption: t.take("IOPTION").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct KEYWORD_KEYWORD {
    pub memory: Vec<String>,
    pub memory2: Vec<String>,
    pub ncpu: Vec<i64>,
}
impl KEYWORD_KEYWORD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("KEYWORD_KEYWORD").unwrap());
        Self {
            memory: t.take("MEMORY").and_then(|c| c.into_str()).unwrap_or_default(),
            memory2: t.take("MEMORY2").and_then(|c| c.into_str()).unwrap_or_default(),
            ncpu: t.take("NCPU").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct KEYWORD_KEYWORD_ID {
    pub memory: Vec<String>,
    pub memory2: Vec<String>,
    pub ncpu: Vec<i64>,
    pub project: Vec<String>,
    pub num: Vec<String>,
    pub stage: Vec<String>,
}
impl KEYWORD_KEYWORD_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("KEYWORD_KEYWORD_ID").unwrap());
        Self {
            memory: t.take("MEMORY").and_then(|c| c.into_str()).unwrap_or_default(),
            memory2: t.take("MEMORY2").and_then(|c| c.into_str()).unwrap_or_default(),
            ncpu: t.take("NCPU").and_then(|c| c.into_int()).unwrap_or_default(),
            project: t.take("PROJECT").and_then(|c| c.into_str()).unwrap_or_default(),
            num: t.take("NUM").and_then(|c| c.into_str()).unwrap_or_default(),
            stage: t.take("STAGE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct KEYWORD_KEYWORD_JOBID {
    pub memory: Vec<String>,
    pub memory2: Vec<String>,
    pub ncpu: Vec<i64>,
    pub jobname: Vec<String>,
}
impl KEYWORD_KEYWORD_JOBID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("KEYWORD_KEYWORD_JOBID").unwrap());
        Self {
            memory: t.take("MEMORY").and_then(|c| c.into_str()).unwrap_or_default(),
            memory2: t.take("MEMORY2").and_then(|c| c.into_str()).unwrap_or_default(),
            ncpu: t.take("NCPU").and_then(|c| c.into_int()).unwrap_or_default(),
            jobname: t.take("JOBNAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_ACOUSTIC_SOURCE {
    pub nid_ssid: Vec<i64>,
    pub srctyp: Vec<i64>,
    pub lcid: Vec<i64>,
    pub data1: Vec<f64>,
    pub data2: Vec<f64>,
    pub data3: Vec<f64>,
    pub data4: Vec<f64>,
    pub data5: Vec<f64>,
}
impl LOAD_ACOUSTIC_SOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_ACOUSTIC_SOURCE").unwrap());
        Self {
            nid_ssid: t.take("NID/SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            srctyp: t.take("SRCTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            data1: t.take("DATA1").and_then(|c| c.into_float()).unwrap_or_default(),
            data2: t.take("DATA2").and_then(|c| c.into_float()).unwrap_or_default(),
            data3: t.take("DATA3").and_then(|c| c.into_float()).unwrap_or_default(),
            data4: t.take("DATA4").and_then(|c| c.into_float()).unwrap_or_default(),
            data5: t.take("DATA5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_ALE_CONVECTION {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub lagpid: Vec<i64>,
    pub lagt: Vec<f64>,
    pub lagcp: Vec<f64>,
    pub h: Vec<f64>,
    pub lagmas: Vec<f64>,
}
impl LOAD_ALE_CONVECTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_ALE_CONVECTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            lagpid: t.take("LAGPID").and_then(|c| c.into_int()).unwrap_or_default(),
            lagt: t.take("LAGT").and_then(|c| c.into_float()).unwrap_or_default(),
            lagcp: t.take("LAGCP").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lagmas: t.take("LAGMAS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BEAM_ELEMENT {
    pub eid: Vec<i64>,
    pub dal: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl LOAD_BEAM_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BEAM_ELEMENT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            dal: t.take("DAL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BEAM_SET {
    pub esid: Vec<i64>,
    pub dal: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl LOAD_BEAM_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BEAM_SET").unwrap());
        Self {
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            dal: t.take("DAL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BLAST {
    pub wgt: Vec<f64>,
    pub xbo: Vec<f64>,
    pub ybo: Vec<f64>,
    pub zbo: Vec<f64>,
    pub tbo: Vec<f64>,
    pub iunit: Vec<i64>,
    pub isurf: Vec<i64>,
    pub cfm: Vec<f64>,
    pub cfl: Vec<f64>,
    pub cft: Vec<f64>,
    pub cfp: Vec<f64>,
    pub death: Vec<f64>,
}
impl LOAD_BLAST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BLAST").unwrap());
        Self {
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            xbo: t.take("XBO").and_then(|c| c.into_float()).unwrap_or_default(),
            ybo: t.take("YBO").and_then(|c| c.into_float()).unwrap_or_default(),
            zbo: t.take("ZBO").and_then(|c| c.into_float()).unwrap_or_default(),
            tbo: t.take("TBO").and_then(|c| c.into_float()).unwrap_or_default(),
            iunit: t.take("IUNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            isurf: t.take("ISURF").and_then(|c| c.into_int()).unwrap_or_default(),
            cfm: t.take("CFM").and_then(|c| c.into_float()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            cft: t.take("CFT").and_then(|c| c.into_float()).unwrap_or_default(),
            cfp: t.take("CFP").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BLAST_CLEARING {
    pub bid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl LOAD_BLAST_CLEARING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BLAST_CLEARING").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BLAST_ENHANCED {
    pub bid: Vec<i64>,
    pub m: Vec<f64>,
    pub xbo: Vec<f64>,
    pub ybo: Vec<f64>,
    pub zbo: Vec<f64>,
    pub tbo: Vec<f64>,
    pub unit: Vec<i64>,
    pub blast: Vec<i64>,
    pub cfm: Vec<f64>,
    pub cfl: Vec<f64>,
    pub cft: Vec<f64>,
    pub cfp: Vec<f64>,
    pub nidbo: Vec<i64>,
    pub death: Vec<f64>,
    pub negphs: Vec<i64>,
}
impl LOAD_BLAST_ENHANCED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BLAST_ENHANCED").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            xbo: t.take("XBO").and_then(|c| c.into_float()).unwrap_or_default(),
            ybo: t.take("YBO").and_then(|c| c.into_float()).unwrap_or_default(),
            zbo: t.take("ZBO").and_then(|c| c.into_float()).unwrap_or_default(),
            tbo: t.take("TBO").and_then(|c| c.into_float()).unwrap_or_default(),
            unit: t.take("UNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            blast: t.take("BLAST").and_then(|c| c.into_int()).unwrap_or_default(),
            cfm: t.take("CFM").and_then(|c| c.into_float()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            cft: t.take("CFT").and_then(|c| c.into_float()).unwrap_or_default(),
            cfp: t.take("CFP").and_then(|c| c.into_float()).unwrap_or_default(),
            nidbo: t.take("NIDBO").and_then(|c| c.into_int()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            negphs: t.take("NEGPHS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BLAST_SEGMENT {
    pub bid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub alepid: Vec<i64>,
    pub sfnrb: Vec<f64>,
    pub scalep: Vec<f64>,
}
impl LOAD_BLAST_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BLAST_SEGMENT").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            alepid: t.take("ALEPID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfnrb: t.take("SFNRB").and_then(|c| c.into_float()).unwrap_or_default(),
            scalep: t.take("SCALEP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BLAST_SEGMENT_SET {
    pub bid: Vec<i64>,
    pub ssid: Vec<i64>,
    pub alepid: Vec<i64>,
    pub sfnrb: Vec<f64>,
    pub scalep: Vec<f64>,
}
impl LOAD_BLAST_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BLAST_SEGMENT_SET").unwrap());
        Self {
            bid: t.take("BID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            alepid: t.take("ALEPID").and_then(|c| c.into_int()).unwrap_or_default(),
            sfnrb: t.take("SFNRB").and_then(|c| c.into_float()).unwrap_or_default(),
            scalep: t.take("SCALEP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_GENERALIZED {
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub lcid: Vec<i64>,
    pub drlcid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub omx: Vec<f64>,
    pub omy: Vec<f64>,
    pub omz: Vec<f64>,
    pub cid: Vec<i64>,
    pub angtyp: Vec<String>,
}
impl LOAD_BODY_GENERALIZED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_GENERALIZED").unwrap());
        Self {
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            drlcid: t.take("DRLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            omx: t.take("OMX").and_then(|c| c.into_float()).unwrap_or_default(),
            omy: t.take("OMY").and_then(|c| c.into_float()).unwrap_or_default(),
            omz: t.take("OMZ").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            angtyp: t.take("ANGTYP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_GENERALIZED_SET_NODE {
    pub nsid: Vec<i64>,
    pub n2: Vec<i64>,
    pub lcid: Vec<i64>,
    pub drlcid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub omx: Vec<f64>,
    pub omy: Vec<f64>,
    pub omz: Vec<f64>,
    pub cid: Vec<i64>,
    pub angtyp: Vec<String>,
}
impl LOAD_BODY_GENERALIZED_SET_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_GENERALIZED_SET_NODE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            drlcid: t.take("DRLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            omx: t.take("OMX").and_then(|c| c.into_float()).unwrap_or_default(),
            omy: t.take("OMY").and_then(|c| c.into_float()).unwrap_or_default(),
            omz: t.take("OMZ").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            angtyp: t.take("ANGTYP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_GENERALIZED_SET_PART {
    pub psid: Vec<i64>,
    pub n2: Vec<i64>,
    pub lcid: Vec<i64>,
    pub drlcid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub omx: Vec<f64>,
    pub omy: Vec<f64>,
    pub omz: Vec<f64>,
    pub cid: Vec<i64>,
    pub angtyp: Vec<String>,
}
impl LOAD_BODY_GENERALIZED_SET_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_GENERALIZED_SET_PART").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            drlcid: t.take("DRLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            omx: t.take("OMX").and_then(|c| c.into_float()).unwrap_or_default(),
            omy: t.take("OMY").and_then(|c| c.into_float()).unwrap_or_default(),
            omz: t.take("OMZ").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            angtyp: t.take("ANGTYP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_PARTS {
    pub psid: Vec<i64>,
}
impl LOAD_BODY_PARTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_PARTS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_POROUS {
    pub sid: Vec<i64>,
    pub sidtyp: Vec<i64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub aopt: Vec<i64>,
}
impl LOAD_BODY_POROUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_POROUS").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            sidtyp: t.take("SIDTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_RX {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_RX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_RX").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_RY {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_RY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_RY").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_RZ {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_RZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_RZ").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_VECTOR {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl LOAD_BODY_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_VECTOR").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_X {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_X {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_X").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_Y {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_Y {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_Y").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BODY_Z {
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub lciddr: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub cid: Vec<i64>,
}
impl LOAD_BODY_Z {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BODY_Z").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_BRODE {
    pub yld: Vec<f64>,
    pub bht: Vec<f64>,
    pub xbo: Vec<f64>,
    pub ybo: Vec<f64>,
    pub zbo: Vec<f64>,
    pub tbo: Vec<f64>,
    pub talc: Vec<i64>,
    pub sflc: Vec<i64>,
    pub cfl: Vec<f64>,
    pub cft: Vec<f64>,
    pub cfp: Vec<f64>,
}
impl LOAD_BRODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_BRODE").unwrap());
        Self {
            yld: t.take("YLD").and_then(|c| c.into_float()).unwrap_or_default(),
            bht: t.take("BHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xbo: t.take("XBO").and_then(|c| c.into_float()).unwrap_or_default(),
            ybo: t.take("YBO").and_then(|c| c.into_float()).unwrap_or_default(),
            zbo: t.take("ZBO").and_then(|c| c.into_float()).unwrap_or_default(),
            tbo: t.take("TBO").and_then(|c| c.into_float()).unwrap_or_default(),
            talc: t.take("TALC").and_then(|c| c.into_int()).unwrap_or_default(),
            sflc: t.take("SFLC").and_then(|c| c.into_int()).unwrap_or_default(),
            cfl: t.take("CFL").and_then(|c| c.into_float()).unwrap_or_default(),
            cft: t.take("CFT").and_then(|c| c.into_float()).unwrap_or_default(),
            cfp: t.take("CFP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_DENSITY_DEPTH {
    pub psid: Vec<i64>,
    pub gc: Vec<f64>,
    pub dir: Vec<i64>,
    pub lcid: Vec<i64>,
}
impl LOAD_DENSITY_DEPTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_DENSITY_DEPTH").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gc: t.take("GC").and_then(|c| c.into_float()).unwrap_or_default(),
            dir: t.take("DIR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_ERODING_PART_SET {
    pub id: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub psid: Vec<i64>,
    pub boxid: Vec<i64>,
    pub mem: Vec<i64>,
    pub alpha: Vec<f64>,
    pub iflag: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub beta: Vec<f64>,
}
impl LOAD_ERODING_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_ERODING_PART_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            mem: t.take("MEM").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_EXPANSION_PRESSURE {
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub nsid: Vec<i64>,
    pub xn: Vec<f64>,
    pub yn: Vec<f64>,
    pub zn: Vec<f64>,
}
impl LOAD_EXPANSION_PRESSURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_EXPANSION_PRESSURE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xn: t.take("XN").and_then(|c| c.into_float()).unwrap_or_default(),
            yn: t.take("YN").and_then(|c| c.into_float()).unwrap_or_default(),
            zn: t.take("ZN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_FACE_UVW {
    pub luid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_FACE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_FACE_UVW").unwrap());
        Self {
            luid: t.take("LUID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_FACE_UVW_SET {
    pub fid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_FACE_UVW_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_FACE_UVW_SET").unwrap());
        Self {
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_FACE_XYZ {
    pub fxyzid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_FACE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_FACE_XYZ").unwrap());
        Self {
            fxyzid: t.take("FXYZID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_FACE_XYZ_SET {
    pub fxyzsid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_FACE_XYZ_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_FACE_XYZ_SET").unwrap());
        Self {
            fxyzsid: t.take("FXYZSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_GRAVITY_PART {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lc: Vec<i64>,
    pub accel: Vec<f64>,
    pub lcdr: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl LOAD_GRAVITY_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_GRAVITY_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            accel: t.take("ACCEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_GRAVITY_PART_SET {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lc: Vec<i64>,
    pub accel: Vec<f64>,
    pub lcdr: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl LOAD_GRAVITY_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_GRAVITY_PART_SET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            accel: t.take("ACCEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_CONTROLLER {
    pub node: Vec<i64>,
    pub pid: Vec<i64>,
    pub load: Vec<f64>,
    pub tset: Vec<f64>,
    pub type_: Vec<i64>,
    pub gp: Vec<f64>,
    pub gi: Vec<f64>,
}
impl LOAD_HEAT_CONTROLLER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_CONTROLLER").unwrap());
        Self {
            node: t.take("NODE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            load: t.take("LOAD").and_then(|c| c.into_float()).unwrap_or_default(),
            tset: t.take("TSET").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            gp: t.take("GP").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_EXOTHERMIC_REACTION {
    pub hsid: Vec<i64>,
    pub stype: Vec<i64>,
    pub nsid: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub tmin: Vec<f64>,
    pub tmax: Vec<f64>,
    pub toff: Vec<f64>,
    pub csei0: Vec<f64>,
    pub asei: Vec<f64>,
    pub easei: Vec<f64>,
    pub msei: Vec<f64>,
    pub hsei: Vec<f64>,
    pub wc: Vec<f64>,
    pub f: Vec<f64>,
    pub ru: Vec<f64>,
    pub cne0: Vec<f64>,
    pub ane: Vec<f64>,
    pub eane: Vec<f64>,
    pub mne: Vec<f64>,
    pub hne: Vec<f64>,
    pub wcne: Vec<f64>,
    pub tsei0: Vec<f64>,
    pub tseir: Vec<f64>,
    pub alpha0: Vec<f64>,
    pub ape: Vec<f64>,
    pub eape: Vec<f64>,
    pub mpep1: Vec<f64>,
    pub hpe: Vec<f64>,
    pub wpe: Vec<f64>,
    pub mpep2: Vec<f64>,
    pub ce0: Vec<f64>,
    pub ae: Vec<f64>,
    pub eae: Vec<f64>,
    pub me: Vec<f64>,
    pub he: Vec<f64>,
    pub we: Vec<f64>,
}
impl LOAD_HEAT_EXOTHERMIC_REACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_EXOTHERMIC_REACTION").unwrap());
        Self {
            hsid: t.take("HSID").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tmin: t.take("TMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            toff: t.take("TOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            csei0: t.take("CSEI0").and_then(|c| c.into_float()).unwrap_or_default(),
            asei: t.take("ASEI").and_then(|c| c.into_float()).unwrap_or_default(),
            easei: t.take("EASEI").and_then(|c| c.into_float()).unwrap_or_default(),
            msei: t.take("MSEI").and_then(|c| c.into_float()).unwrap_or_default(),
            hsei: t.take("HSEI").and_then(|c| c.into_float()).unwrap_or_default(),
            wc: t.take("WC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            ru: t.take("RU").and_then(|c| c.into_float()).unwrap_or_default(),
            cne0: t.take("CNE0").and_then(|c| c.into_float()).unwrap_or_default(),
            ane: t.take("ANE").and_then(|c| c.into_float()).unwrap_or_default(),
            eane: t.take("EANE").and_then(|c| c.into_float()).unwrap_or_default(),
            mne: t.take("MNE").and_then(|c| c.into_float()).unwrap_or_default(),
            hne: t.take("HNE").and_then(|c| c.into_float()).unwrap_or_default(),
            wcne: t.take("WCNE").and_then(|c| c.into_float()).unwrap_or_default(),
            tsei0: t.take("TSEI0").and_then(|c| c.into_float()).unwrap_or_default(),
            tseir: t.take("TSEIR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha0: t.take("ALPHA0").and_then(|c| c.into_float()).unwrap_or_default(),
            ape: t.take("APE").and_then(|c| c.into_float()).unwrap_or_default(),
            eape: t.take("EAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            mpep1: t.take("MPEP1").and_then(|c| c.into_float()).unwrap_or_default(),
            hpe: t.take("HPE").and_then(|c| c.into_float()).unwrap_or_default(),
            wpe: t.take("WPE").and_then(|c| c.into_float()).unwrap_or_default(),
            mpep2: t.take("MPEP2").and_then(|c| c.into_float()).unwrap_or_default(),
            ce0: t.take("CE0").and_then(|c| c.into_float()).unwrap_or_default(),
            ae: t.take("AE").and_then(|c| c.into_float()).unwrap_or_default(),
            eae: t.take("EAE").and_then(|c| c.into_float()).unwrap_or_default(),
            me: t.take("ME").and_then(|c| c.into_float()).unwrap_or_default(),
            he: t.take("HE").and_then(|c| c.into_float()).unwrap_or_default(),
            we: t.take("WE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_GENERATION_SET {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub wblcid: Vec<i64>,
    pub cblcid: Vec<i64>,
    pub tblcid: Vec<i64>,
}
impl LOAD_HEAT_GENERATION_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_GENERATION_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            wblcid: t.take("WBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cblcid: t.take("CBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_GENERATION_SET_SHELL {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub wblcid: Vec<i64>,
    pub cblcid: Vec<i64>,
    pub tblcid: Vec<i64>,
}
impl LOAD_HEAT_GENERATION_SET_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_GENERATION_SET_SHELL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            wblcid: t.take("WBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cblcid: t.take("CBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_GENERATION_SET_SOLID {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub wblcid: Vec<i64>,
    pub cblcid: Vec<i64>,
    pub tblcid: Vec<i64>,
}
impl LOAD_HEAT_GENERATION_SET_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_GENERATION_SET_SOLID").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            wblcid: t.take("WBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cblcid: t.take("CBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_GENERATION_SHELL {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub wblcid: Vec<i64>,
    pub cblcid: Vec<i64>,
    pub tblcid: Vec<i64>,
}
impl LOAD_HEAT_GENERATION_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_GENERATION_SHELL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            wblcid: t.take("WBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cblcid: t.take("CBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_HEAT_GENERATION_SOLID {
    pub sid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cmult: Vec<f64>,
    pub wblcid: Vec<i64>,
    pub cblcid: Vec<i64>,
    pub tblcid: Vec<i64>,
}
impl LOAD_HEAT_GENERATION_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_HEAT_GENERATION_SOLID").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmult: t.take("CMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            wblcid: t.take("WBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cblcid: t.take("CBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tblcid: t.take("TBLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_MASK {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub vid1: Vec<i64>,
    pub off: Vec<f64>,
    pub boxid: Vec<i64>,
    pub lcidm: Vec<i64>,
    pub vid2: Vec<i64>,
    pub inout: Vec<i64>,
    pub icycle: Vec<i64>,
}
impl LOAD_MASK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_MASK").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid1: t.take("VID1").and_then(|c| c.into_int()).unwrap_or_default(),
            off: t.take("OFF").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidm: t.take("LCIDM").and_then(|c| c.into_int()).unwrap_or_default(),
            vid2: t.take("VID2").and_then(|c| c.into_int()).unwrap_or_default(),
            inout: t.take("INOUT").and_then(|c| c.into_int()).unwrap_or_default(),
            icycle: t.take("ICYCLE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_MOTION_NODE {
    pub node1: Vec<i64>,
    pub dof1: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid1: Vec<i64>,
    pub node2: Vec<i64>,
    pub dof2: Vec<i64>,
    pub cid2: Vec<i64>,
}
impl LOAD_MOTION_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_MOTION_NODE").unwrap());
        Self {
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            dof1: t.take("DOF1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid1: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            dof2: t.take("DOF2").and_then(|c| c.into_int()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_MOVING_PRESSURE {
    pub loadid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub lcid: Vec<i64>,
    pub cutoff: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub lcidd: Vec<i64>,
    pub idir: Vec<i64>,
    pub lsflg: Vec<i64>,
    pub id: Vec<i64>,
    pub idtype: Vec<i64>,
    pub nip: Vec<i64>,
}
impl LOAD_MOVING_PRESSURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_MOVING_PRESSURE").unwrap());
        Self {
            loadid: t.take("LOADID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cutoff: t.take("CUTOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidd: t.take("LCIDD").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            lsflg: t.take("LSFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            idtype: t.take("IDTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_NODE {
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub m1: Vec<i64>,
    pub m2: Vec<i64>,
    pub m3: Vec<i64>,
}
impl LOAD_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_int()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_int()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_NODE_POINT {
    pub nid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub m1: Vec<i64>,
    pub m2: Vec<i64>,
    pub m3: Vec<i64>,
}
impl LOAD_NODE_POINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_NODE_POINT").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_int()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_int()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_NODE_SET {
    pub nsid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub m1: Vec<i64>,
    pub m2: Vec<i64>,
    pub m3: Vec<i64>,
}
impl LOAD_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_NODE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_int()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_int()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_NODE_SET_ONCE {
    pub nsid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub m1: Vec<i64>,
    pub m2: Vec<i64>,
    pub m3: Vec<i64>,
    pub once: Vec<i64>,
    pub lcid1: Vec<i64>,
}
impl LOAD_NODE_SET_ONCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_NODE_SET_ONCE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_int()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_int()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_int()).unwrap_or_default(),
            once: t.take("ONCE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_NURBS_SHELL {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub dt: Vec<f64>,
    pub ltype: Vec<String>,
    pub regdef: Vec<String>,
    pub cid: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub rmin: Vec<f64>,
    pub smin: Vec<f64>,
    pub rmax: Vec<f64>,
    pub smax: Vec<f64>,
    pub ne1: Vec<i64>,
    pub ne2: Vec<i64>,
    pub ne3: Vec<i64>,
    pub ne4: Vec<i64>,
    pub ne5: Vec<i64>,
    pub ne6: Vec<i64>,
    pub ne7: Vec<i64>,
    pub ne8: Vec<i64>,
    pub nt1: Vec<i64>,
    pub nt2: Vec<i64>,
    pub nt3: Vec<i64>,
    pub nt4: Vec<i64>,
    pub nte: Vec<i64>,
    pub r1: Vec<f64>,
    pub s1: Vec<f64>,
    pub r2: Vec<f64>,
    pub s2: Vec<f64>,
    pub nc1: Vec<i64>,
    pub nc2: Vec<i64>,
    pub nce: Vec<i64>,
}
impl LOAD_NURBS_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_NURBS_SHELL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            ltype: t.take("LTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            regdef: t.take("REGDEF").and_then(|c| c.into_str()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            rmin: t.take("RMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            smin: t.take("SMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            rmax: t.take("RMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            smax: t.take("SMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ne1: t.take("NE1").and_then(|c| c.into_int()).unwrap_or_default(),
            ne2: t.take("NE2").and_then(|c| c.into_int()).unwrap_or_default(),
            ne3: t.take("NE3").and_then(|c| c.into_int()).unwrap_or_default(),
            ne4: t.take("NE4").and_then(|c| c.into_int()).unwrap_or_default(),
            ne5: t.take("NE5").and_then(|c| c.into_int()).unwrap_or_default(),
            ne6: t.take("NE6").and_then(|c| c.into_int()).unwrap_or_default(),
            ne7: t.take("NE7").and_then(|c| c.into_int()).unwrap_or_default(),
            ne8: t.take("NE8").and_then(|c| c.into_int()).unwrap_or_default(),
            nt1: t.take("NT1").and_then(|c| c.into_int()).unwrap_or_default(),
            nt2: t.take("NT2").and_then(|c| c.into_int()).unwrap_or_default(),
            nt3: t.take("NT3").and_then(|c| c.into_int()).unwrap_or_default(),
            nt4: t.take("NT4").and_then(|c| c.into_int()).unwrap_or_default(),
            nte: t.take("NTE").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            nc1: t.take("NC1").and_then(|c| c.into_int()).unwrap_or_default(),
            nc2: t.take("NC2").and_then(|c| c.into_int()).unwrap_or_default(),
            nce: t.take("NCE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_POINT_UVW {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl LOAD_POINT_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_POINT_UVW").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_POINT_UVW_SET {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
}
impl LOAD_POINT_UVW_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_POINT_UVW_SET").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_PYRO_ACTUATOR {
    pub id: Vec<i64>,
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub csa: Vec<f64>,
    pub vol: Vec<f64>,
    pub prs: Vec<f64>,
    pub dens: Vec<f64>,
    pub atime: Vec<f64>,
    pub mcid: Vec<i64>,
    pub cv: Vec<f64>,
    pub cp: Vec<f64>,
    pub temp: Vec<f64>,
}
impl LOAD_PYRO_ACTUATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_PYRO_ACTUATOR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            csa: t.take("CSA").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            prs: t.take("PRS").and_then(|c| c.into_float()).unwrap_or_default(),
            dens: t.take("DENS").and_then(|c| c.into_float()).unwrap_or_default(),
            atime: t.take("ATIME").and_then(|c| c.into_float()).unwrap_or_default(),
            mcid: t.take("MCID").and_then(|c| c.into_int()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_PZE {
    pub setid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub setyp: Vec<String>,
}
impl LOAD_PZE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_PZE").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_REMOVE_PART {
    pub pid: Vec<i64>,
    pub time0: Vec<f64>,
    pub time1: Vec<f64>,
    pub stgr: Vec<i64>,
}
impl LOAD_REMOVE_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_REMOVE_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            time0: t.take("TIME0").and_then(|c| c.into_float()).unwrap_or_default(),
            time1: t.take("TIME1").and_then(|c| c.into_float()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_REMOVE_PART_SET {
    pub psid: Vec<i64>,
    pub time0: Vec<f64>,
    pub time1: Vec<f64>,
    pub stgr: Vec<i64>,
}
impl LOAD_REMOVE_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_REMOVE_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            time0: t.take("TIME0").and_then(|c| c.into_float()).unwrap_or_default(),
            time1: t.take("TIME1").and_then(|c| c.into_float()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_RIGID_BODY {
    pub pid: Vec<i64>,
    pub dof: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub m1: Vec<i64>,
    pub m2: Vec<i64>,
    pub m3: Vec<i64>,
}
impl LOAD_RIGID_BODY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_RIGID_BODY").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_int()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_int()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
}
impl LOAD_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_CONTACT_MASK {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub lsid: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub cid1: Vec<i64>,
    pub cid2: Vec<i64>,
    pub cid3: Vec<i64>,
    pub cid4: Vec<i64>,
    pub cid5: Vec<i64>,
    pub cid1_2: Vec<i64>,
    pub cid2_2: Vec<i64>,
    pub cid3_2: Vec<i64>,
    pub cid4_2: Vec<i64>,
    pub cid5_2: Vec<i64>,
    pub cid6: Vec<i64>,
    pub cid7: Vec<i64>,
    pub cid8: Vec<i64>,
}
impl LOAD_SEGMENT_CONTACT_MASK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_CONTACT_MASK").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            lsid: t.take("LSID").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            cid1: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cid3: t.take("CID3").and_then(|c| c.into_int()).unwrap_or_default(),
            cid4: t.take("CID4").and_then(|c| c.into_int()).unwrap_or_default(),
            cid5: t.take("CID5").and_then(|c| c.into_int()).unwrap_or_default(),
            cid1_2: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            cid2_2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
            cid3_2: t.take("CID3").and_then(|c| c.into_int()).unwrap_or_default(),
            cid4_2: t.take("CID4").and_then(|c| c.into_int()).unwrap_or_default(),
            cid5_2: t.take("CID5").and_then(|c| c.into_int()).unwrap_or_default(),
            cid6: t.take("CID6").and_then(|c| c.into_int()).unwrap_or_default(),
            cid7: t.take("CID7").and_then(|c| c.into_int()).unwrap_or_default(),
            cid8: t.take("CID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_FILE {
    pub filename: Vec<String>,
    pub lcid: Vec<i64>,
}
impl LOAD_SEGMENT_FILE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_FILE").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_FSILNK {
    pub filename: Vec<String>,
    pub nint: Vec<i64>,
    pub lcid: Vec<i64>,
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl LOAD_SEGMENT_FSILNK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_FSILNK").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            nint: t.take("NINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_NONUNIFORM {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub dt: Vec<f64>,
    pub cid: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub n7: Vec<i64>,
    pub n8: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl LOAD_SEGMENT_NONUNIFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_NONUNIFORM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_int()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_SET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_SET_ANGLE {
    pub id: Vec<i64>,
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub ioptp: Vec<i64>,
    pub ioptd: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub na: Vec<i64>,
    pub ni: Vec<i64>,
}
impl LOAD_SEGMENT_SET_ANGLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_SET_ANGLE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            ioptp: t.take("IOPTP").and_then(|c| c.into_int()).unwrap_or_default(),
            ioptd: t.take("IOPTD").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            na: t.take("NA").and_then(|c| c.into_int()).unwrap_or_default(),
            ni: t.take("NI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEGMENT_SET_NONUNIFORM {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
    pub dt: Vec<f64>,
    pub eltype: Vec<String>,
    pub cid: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl LOAD_SEGMENT_SET_NONUNIFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEGMENT_SET_NONUNIFORM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            eltype: t.take("ELTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_AUX {
    pub filename: Vec<String>,
    pub ssid: Vec<i64>,
    pub gmset: Vec<i64>,
    pub sf: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub memgm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_AUX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_AUX").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            memgm: t.take("MEMGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_AUX_ID {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub filename: Vec<String>,
    pub ssid: Vec<i64>,
    pub gmset: Vec<i64>,
    pub sf: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub memgm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_AUX_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_AUX_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmset: t.take("GMSET").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            memgm: t.take("MEMGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_DECONV {
    pub ssid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
    pub pset: Vec<i64>,
    pub vdir: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_DECONV {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_DECONV").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
            vdir: t.take("VDIR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_DECONV_ID {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
    pub pset: Vec<i64>,
    pub vdir: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_DECONV_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_DECONV_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
            pset: t.take("PSET").and_then(|c| c.into_int()).unwrap_or_default(),
            vdir: t.take("VDIR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_NODE {
    pub ssid: Vec<i64>,
    pub typeid: Vec<i64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_NODE").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_NODE_ID {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub typeid: Vec<i64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_NODE_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_NODE_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_POINT {
    pub ssid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_POINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_POINT").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_POINT_ID {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_POINT_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_POINT_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_SET {
    pub ssid: Vec<i64>,
    pub typeid: Vec<i64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_SET").unwrap());
        Self {
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SEISMIC_SSI_SET_ID {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub ssid: Vec<i64>,
    pub typeid: Vec<i64>,
    pub gmx: Vec<i64>,
    pub gmy: Vec<i64>,
    pub gmz: Vec<i64>,
    pub sf: Vec<f64>,
    pub cid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub isg: Vec<i64>,
    pub igm: Vec<i64>,
}
impl LOAD_SEISMIC_SSI_SET_ID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SEISMIC_SSI_SET_ID").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            typeid: t.take("typeID").and_then(|c| c.into_int()).unwrap_or_default(),
            gmx: t.take("GMX").and_then(|c| c.into_int()).unwrap_or_default(),
            gmy: t.take("GMY").and_then(|c| c.into_int()).unwrap_or_default(),
            gmz: t.take("GMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            isg: t.take("ISG").and_then(|c| c.into_int()).unwrap_or_default(),
            igm: t.take("IGM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SHELL_ELEMENT {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub eid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_SHELL_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SHELL_ELEMENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SHELL_SET {
    pub id: Vec<i64>,
    pub heading: Vec<String>,
    pub esid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub sf: Vec<f64>,
    pub at: Vec<f64>,
}
impl LOAD_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SHELL_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("Heading").and_then(|c| c.into_str()).unwrap_or_default(),
            esid: t.take("ESID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SPCFORC {
}
impl LOAD_SPCFORC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SPCFORC").unwrap());
        Self {
        }
    }
}

pub struct LOAD_SSA {
    pub vs: Vec<f64>,
    pub ds: Vec<f64>,
    pub refl: Vec<f64>,
    pub zb: Vec<f64>,
    pub zsurf: Vec<f64>,
    pub fpsid: Vec<i64>,
    pub psid: Vec<i64>,
    pub a: Vec<f64>,
    pub alpha: Vec<f64>,
    pub gamma: Vec<f64>,
    pub ktheta: Vec<f64>,
    pub kappa: Vec<f64>,
    pub xs: Vec<f64>,
    pub ys: Vec<f64>,
    pub zs: Vec<f64>,
    pub w: Vec<f64>,
    pub tdely: Vec<f64>,
    pub rad: Vec<f64>,
    pub cz: Vec<f64>,
}
impl LOAD_SSA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SSA").unwrap());
        Self {
            vs: t.take("VS").and_then(|c| c.into_float()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            refl: t.take("REFL").and_then(|c| c.into_float()).unwrap_or_default(),
            zb: t.take("ZB").and_then(|c| c.into_float()).unwrap_or_default(),
            zsurf: t.take("ZSURF").and_then(|c| c.into_float()).unwrap_or_default(),
            fpsid: t.take("FPSID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            ktheta: t.take("KTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            xs: t.take("XS").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            zs: t.take("ZS").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            tdely: t.take("TDELY").and_then(|c| c.into_float()).unwrap_or_default(),
            rad: t.take("RAD").and_then(|c| c.into_float()).unwrap_or_default(),
            cz: t.take("CZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_STEADY_STATE_ROLLING {
    pub id: Vec<i64>,
    pub psid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub lcd1: Vec<i64>,
    pub lcd1r: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub lcd2: Vec<i64>,
    pub lcd2r: Vec<i64>,
    pub n5: Vec<i64>,
    pub n6: Vec<i64>,
    pub lcd3: Vec<i64>,
    pub lcd3r: Vec<i64>,
}
impl LOAD_STEADY_STATE_ROLLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_STEADY_STATE_ROLLING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd1: t.take("LCD1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd1r: t.take("LCD1R").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd2: t.take("LCD2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd2r: t.take("LCD2R").and_then(|c| c.into_int()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_int()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd3: t.take("LCD3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd3r: t.take("LCD3R").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_STIFFEN_PART {
    pub pid: Vec<i64>,
    pub lc: Vec<i64>,
    pub null: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl LOAD_STIFFEN_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_STIFFEN_PART").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_STIFFEN_PART_SET {
    pub psid: Vec<i64>,
    pub lc: Vec<i64>,
    pub null: Vec<i64>,
    pub stga: Vec<i64>,
    pub stgr: Vec<i64>,
}
impl LOAD_STIFFEN_PART_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_STIFFEN_PART_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_int()).unwrap_or_default(),
            stga: t.take("STGA").and_then(|c| c.into_int()).unwrap_or_default(),
            stgr: t.take("STGR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SUPERPLASTIC_FORMING {
    pub lcp1: Vec<i64>,
    pub csp1: Vec<i64>,
    pub ncp1: Vec<f64>,
    pub lcp2: Vec<i64>,
    pub csp2: Vec<i64>,
    pub ncp2: Vec<f64>,
    pub erate: Vec<f64>,
    pub scmin: Vec<f64>,
    pub scmax: Vec<f64>,
    pub ncyl: Vec<i64>,
}
impl LOAD_SUPERPLASTIC_FORMING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SUPERPLASTIC_FORMING").unwrap());
        Self {
            lcp1: t.take("LCP1").and_then(|c| c.into_int()).unwrap_or_default(),
            csp1: t.take("CSP1").and_then(|c| c.into_int()).unwrap_or_default(),
            ncp1: t.take("NCP1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp2: t.take("LCP2").and_then(|c| c.into_int()).unwrap_or_default(),
            csp2: t.take("CSP2").and_then(|c| c.into_int()).unwrap_or_default(),
            ncp2: t.take("NCP2").and_then(|c| c.into_float()).unwrap_or_default(),
            erate: t.take("ERATE").and_then(|c| c.into_float()).unwrap_or_default(),
            scmin: t.take("SCMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            scmax: t.take("SCMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyl: t.take("NCYL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SURFACE_STRESS {
    pub pid: Vec<i64>,
    pub lscid1: Vec<i64>,
    pub lscid2: Vec<i64>,
    pub lscid3: Vec<i64>,
    pub lscid4: Vec<i64>,
    pub lscid5: Vec<i64>,
    pub lscid6: Vec<i64>,
    pub lscid7: Vec<i64>,
    pub lscid8: Vec<i64>,
    pub uscid1: Vec<i64>,
    pub uscid2: Vec<i64>,
    pub uscid3: Vec<i64>,
    pub uscid4: Vec<i64>,
    pub uscid5: Vec<i64>,
    pub uscid6: Vec<i64>,
    pub uscid7: Vec<i64>,
    pub uscid8: Vec<i64>,
}
impl LOAD_SURFACE_STRESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SURFACE_STRESS").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid1: t.take("LSCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid2: t.take("LSCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid3: t.take("LSCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid4: t.take("LSCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid5: t.take("LSCID5").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid6: t.take("LSCID6").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid7: t.take("LSCID7").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid8: t.take("LSCID8").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid1: t.take("USCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid2: t.take("USCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid3: t.take("USCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid4: t.take("USCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid5: t.take("USCID5").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid6: t.take("USCID6").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid7: t.take("USCID7").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid8: t.take("USCID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_SURFACE_STRESS_SET {
    pub psid: Vec<i64>,
    pub lscid1: Vec<i64>,
    pub lscid2: Vec<i64>,
    pub lscid3: Vec<i64>,
    pub lscid4: Vec<i64>,
    pub lscid5: Vec<i64>,
    pub lscid6: Vec<i64>,
    pub lscid7: Vec<i64>,
    pub lscid8: Vec<i64>,
    pub uscid1: Vec<i64>,
    pub uscid2: Vec<i64>,
    pub uscid3: Vec<i64>,
    pub uscid4: Vec<i64>,
    pub uscid5: Vec<i64>,
    pub uscid6: Vec<i64>,
    pub uscid7: Vec<i64>,
    pub uscid8: Vec<i64>,
}
impl LOAD_SURFACE_STRESS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_SURFACE_STRESS_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid1: t.take("LSCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid2: t.take("LSCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid3: t.take("LSCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid4: t.take("LSCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid5: t.take("LSCID5").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid6: t.take("LSCID6").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid7: t.take("LSCID7").and_then(|c| c.into_int()).unwrap_or_default(),
            lscid8: t.take("LSCID8").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid1: t.take("USCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid2: t.take("USCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid3: t.take("USCID3").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid4: t.take("USCID4").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid5: t.take("USCID5").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid6: t.take("USCID6").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid7: t.take("USCID7").and_then(|c| c.into_int()).unwrap_or_default(),
            uscid8: t.take("USCID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_BINOUT {
    pub deftemp: Vec<f64>,
    pub filename: Vec<String>,
    pub startt: Vec<f64>,
    pub tsf: Vec<f64>,
}
impl LOAD_THERMAL_BINOUT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_BINOUT").unwrap());
        Self {
            deftemp: t.take("DEFTEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            startt: t.take("STARTT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsf: t.take("TSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT {
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub t: Vec<f64>,
    pub te: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            te: t.take("TE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT_ELEMENT_BEAM {
    pub eid: Vec<i64>,
    pub t: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT_ELEMENT_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT_ELEMENT_BEAM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT_ELEMENT_SHELL {
    pub eid: Vec<i64>,
    pub t: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT_ELEMENT_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT_ELEMENT_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT_ELEMENT_SOLID {
    pub eid: Vec<i64>,
    pub t: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT_ELEMENT_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT_ELEMENT_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT_ELEMENT_TSHELL {
    pub eid: Vec<i64>,
    pub t: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT_ELEMENT_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT_ELEMENT_TSHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_CONSTANT_NODE {
    pub nid: Vec<i64>,
    pub t: Vec<f64>,
}
impl LOAD_THERMAL_CONSTANT_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_CONSTANT_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_D3PLOT {
}
impl LOAD_THERMAL_D3PLOT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_D3PLOT").unwrap());
        Self {
        }
    }
}

pub struct LOAD_THERMAL_LOAD_CURVE {
    pub lcid: Vec<i64>,
    pub lciddr: Vec<i64>,
}
impl LOAD_THERMAL_LOAD_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_LOAD_CURVE").unwrap());
        Self {
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lciddr: t.take("LCIDDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_RSW {
    pub deftemp: Vec<i64>,
    pub sid: Vec<i64>,
    pub option: Vec<i64>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub tdeath: Vec<f64>,
    pub tbirth: Vec<f64>,
    pub loc: Vec<i64>,
    pub geoup: Vec<i64>,
    pub dist: Vec<f64>,
    pub h1: Vec<f64>,
    pub h2: Vec<f64>,
    pub r: Vec<f64>,
    pub tempc: Vec<f64>,
    pub tempb: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub f: Vec<i64>,
    pub hz1: Vec<f64>,
    pub hz2: Vec<f64>,
    pub rz: Vec<f64>,
    pub tempzb: Vec<f64>,
}
impl LOAD_THERMAL_RSW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_RSW").unwrap());
        Self {
            deftemp: t.take("DEFTEMP").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_int()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            loc: t.take("LOC").and_then(|c| c.into_int()).unwrap_or_default(),
            geoup: t.take("GEOUP").and_then(|c| c.into_int()).unwrap_or_default(),
            dist: t.take("DIST").and_then(|c| c.into_float()).unwrap_or_default(),
            h1: t.take("H1").and_then(|c| c.into_float()).unwrap_or_default(),
            h2: t.take("H2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            tempc: t.take("TEMPC").and_then(|c| c.into_float()).unwrap_or_default(),
            tempb: t.take("TEMPB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            hz1: t.take("HZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            hz2: t.take("HZ2").and_then(|c| c.into_float()).unwrap_or_default(),
            rz: t.take("RZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tempzb: t.take("TEMPZB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_TOPAZ {
}
impl LOAD_THERMAL_TOPAZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_TOPAZ").unwrap());
        Self {
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE {
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tse: Vec<f64>,
    pub tbe: Vec<f64>,
    pub lcide: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcidedr: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tse: t.take("TSE").and_then(|c| c.into_float()).unwrap_or_default(),
            tbe: t.take("TBE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcide: t.take("LCIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidedr: t.take("LCIDEDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_BEAM {
    pub id: Vec<i64>,
    pub eid: Vec<i64>,
    pub ipolar: Vec<i64>,
    pub tbase: Vec<f64>,
    pub tscale: Vec<f64>,
    pub tcurve: Vec<i64>,
    pub tcurdr: Vec<i64>,
    pub scoor: Vec<f64>,
    pub tcoor: Vec<f64>,
}
impl LOAD_THERMAL_VARIABLE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_BEAM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipolar: t.take("IPOLAR").and_then(|c| c.into_int()).unwrap_or_default(),
            tbase: t.take("TBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tcurve: t.take("TCURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcurdr: t.take("TCURDR").and_then(|c| c.into_int()).unwrap_or_default(),
            scoor: t.take("SCOOR").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoor: t.take("TCOOR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_BEAM_SET {
    pub id: Vec<i64>,
    pub sid: Vec<i64>,
    pub ipolar: Vec<i64>,
    pub tbase: Vec<f64>,
    pub tscale: Vec<f64>,
    pub tcurve: Vec<i64>,
    pub tcurdr: Vec<i64>,
    pub scoor: Vec<f64>,
    pub tcoor: Vec<f64>,
}
impl LOAD_THERMAL_VARIABLE_BEAM_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_BEAM_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ipolar: t.take("IPOLAR").and_then(|c| c.into_int()).unwrap_or_default(),
            tbase: t.take("TBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tcurve: t.take("TCURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcurdr: t.take("TCURDR").and_then(|c| c.into_int()).unwrap_or_default(),
            scoor: t.take("SCOOR").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoor: t.take("TCOOR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_ELEMENT_BEAM {
    pub eid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE_ELEMENT_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_ELEMENT_BEAM").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_ELEMENT_SHELL {
    pub eid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE_ELEMENT_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_ELEMENT_SHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_ELEMENT_SOLID {
    pub eid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE_ELEMENT_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_ELEMENT_SOLID").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_ELEMENT_TSHELL {
    pub eid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE_ELEMENT_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_ELEMENT_TSHELL").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_NODE {
    pub nid: Vec<i64>,
    pub ts: Vec<f64>,
    pub tb: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl LOAD_THERMAL_VARIABLE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_float()).unwrap_or_default(),
            tb: t.take("TB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_SHELL {
    pub id: Vec<i64>,
    pub eid: Vec<i64>,
    pub tbase: Vec<f64>,
    pub tscale: Vec<f64>,
    pub tcurve: Vec<i64>,
    pub tcurdr: Vec<i64>,
    pub zco: Vec<f64>,
}
impl LOAD_THERMAL_VARIABLE_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_SHELL").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbase: t.take("TBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tcurve: t.take("TCURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcurdr: t.take("TCURDR").and_then(|c| c.into_int()).unwrap_or_default(),
            zco: t.take("ZCO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_THERMAL_VARIABLE_SHELL_SET {
    pub id: Vec<i64>,
    pub sid: Vec<i64>,
    pub tbase: Vec<f64>,
    pub tscale: Vec<f64>,
    pub tcurve: Vec<i64>,
    pub tcurdr: Vec<i64>,
    pub zco: Vec<f64>,
}
impl LOAD_THERMAL_VARIABLE_SHELL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_THERMAL_VARIABLE_SHELL_SET").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbase: t.take("TBASE").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            tcurve: t.take("TCURVE").and_then(|c| c.into_int()).unwrap_or_default(),
            tcurdr: t.take("TCURDR").and_then(|c| c.into_int()).unwrap_or_default(),
            zco: t.take("ZCO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_VIBRO_ACOUSTIC {
    pub nmode: Vec<f64>,
    pub texpos: Vec<f64>,
    pub tscale: Vec<f64>,
    pub temper: Vec<f64>,
    pub dampro: Vec<f64>,
    pub damptype: Vec<f64>,
    pub spltype: Vec<f64>,
    pub lddamp: Vec<i64>,
    pub ldspl: Vec<i64>,
    pub ldvel: Vec<i64>,
    pub ldflw: Vec<i64>,
    pub ldspn: Vec<i64>,
}
impl LOAD_VIBRO_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_VIBRO_ACOUSTIC").unwrap());
        Self {
            nmode: t.take("NMODE").and_then(|c| c.into_float()).unwrap_or_default(),
            texpos: t.take("TEXPOS").and_then(|c| c.into_float()).unwrap_or_default(),
            tscale: t.take("TSCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            temper: t.take("TEMPER").and_then(|c| c.into_float()).unwrap_or_default(),
            dampro: t.take("DAMPRO").and_then(|c| c.into_float()).unwrap_or_default(),
            damptype: t.take("DAMPTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            spltype: t.take("SPLTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            lddamp: t.take("LDDAMP").and_then(|c| c.into_int()).unwrap_or_default(),
            ldspl: t.take("LDSPL").and_then(|c| c.into_int()).unwrap_or_default(),
            ldvel: t.take("LDVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            ldflw: t.take("LDFLW").and_then(|c| c.into_int()).unwrap_or_default(),
            ldspn: t.take("LDSPN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LOAD_VOLUME_LOSS {
    pub psid: Vec<i64>,
    pub coord: Vec<i64>,
    pub lcur: Vec<i64>,
    pub fx: Vec<f64>,
    pub fy: Vec<f64>,
    pub fz: Vec<f64>,
    pub pmin: Vec<f64>,
    pub factor: Vec<f64>,
}
impl LOAD_VOLUME_LOSS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LOAD_VOLUME_LOSS").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            coord: t.take("COORD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            fx: t.take("FX").and_then(|c| c.into_float()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_float()).unwrap_or_default(),
            fz: t.take("FZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmin: t.take("PMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            factor: t.take("FACTOR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LSO_DOMAIN {
    pub domain_type: Vec<String>,
    pub solver_name: Vec<String>,
    pub outid: Vec<i64>,
    pub refid: Vec<i64>,
    pub unused: Vec<i64>,
    pub reduct: Vec<i64>,
    pub outid_2: Vec<i64>,
    pub refid_2: Vec<i64>,
    pub override_: Vec<i64>,
    pub reduct_2: Vec<i64>,
    pub variable_name: Vec<String>,
}
impl LSO_DOMAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LSO_DOMAIN").unwrap());
        Self {
            domain_type: t.take("DOMAIN_TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            solver_name: t.take("SOLVER_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            outid: t.take("OUTID").and_then(|c| c.into_int()).unwrap_or_default(),
            refid: t.take("REFID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            reduct: t.take("REDUCT").and_then(|c| c.into_int()).unwrap_or_default(),
            outid_2: t.take("OUTID").and_then(|c| c.into_int()).unwrap_or_default(),
            refid_2: t.take("REFID").and_then(|c| c.into_int()).unwrap_or_default(),
            override_: t.take("OVERRIDE").and_then(|c| c.into_int()).unwrap_or_default(),
            reduct_2: t.take("REDUCT").and_then(|c| c.into_int()).unwrap_or_default(),
            variable_name: t.take("VARIABLE_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LSO_ID_SET {
    pub setid: Vec<i64>,
    pub type_: Vec<String>,
    pub solver: Vec<String>,
    pub id1: Vec<i64>,
    pub id2: Vec<i64>,
    pub id3: Vec<i64>,
    pub id4: Vec<i64>,
    pub id5: Vec<i64>,
    pub id6: Vec<i64>,
    pub id7: Vec<i64>,
    pub id8: Vec<i64>,
}
impl LSO_ID_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LSO_ID_SET").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            id3: t.take("ID3").and_then(|c| c.into_int()).unwrap_or_default(),
            id4: t.take("ID4").and_then(|c| c.into_int()).unwrap_or_default(),
            id5: t.take("ID5").and_then(|c| c.into_int()).unwrap_or_default(),
            id6: t.take("ID6").and_then(|c| c.into_int()).unwrap_or_default(),
            id7: t.take("ID7").and_then(|c| c.into_int()).unwrap_or_default(),
            id8: t.take("ID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct LSO_POINT_SET {
    pub setid: Vec<i64>,
    pub use_: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl LSO_POINT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LSO_POINT_SET").unwrap());
        Self {
            setid: t.take("SETID").and_then(|c| c.into_int()).unwrap_or_default(),
            use_: t.take("USE").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct LSO_TIME_SEQUENCE {
    pub solver_name: Vec<String>,
    pub dt: Vec<f64>,
    pub lcdt: Vec<i64>,
    pub lcopt: Vec<i64>,
    pub npltc: Vec<i64>,
    pub tbeg: Vec<f64>,
    pub tend: Vec<f64>,
    pub domid1: Vec<i64>,
    pub domid2: Vec<i64>,
    pub domid3: Vec<i64>,
    pub domid4: Vec<i64>,
    pub domid5: Vec<i64>,
    pub domid6: Vec<i64>,
    pub domid7: Vec<i64>,
    pub domid8: Vec<i64>,
    pub global_var: Vec<String>,
}
impl LSO_TIME_SEQUENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LSO_TIME_SEQUENCE").unwrap());
        Self {
            solver_name: t.take("SOLVER_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdt: t.take("LCDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcopt: t.take("LCOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            npltc: t.take("NPLTC").and_then(|c| c.into_int()).unwrap_or_default(),
            tbeg: t.take("TBEG").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            domid1: t.take("DOMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            domid2: t.take("DOMID2").and_then(|c| c.into_int()).unwrap_or_default(),
            domid3: t.take("DOMID3").and_then(|c| c.into_int()).unwrap_or_default(),
            domid4: t.take("DOMID4").and_then(|c| c.into_int()).unwrap_or_default(),
            domid5: t.take("DOMID5").and_then(|c| c.into_int()).unwrap_or_default(),
            domid6: t.take("DOMID6").and_then(|c| c.into_int()).unwrap_or_default(),
            domid7: t.take("DOMID7").and_then(|c| c.into_int()).unwrap_or_default(),
            domid8: t.take("DOMID8").and_then(|c| c.into_int()).unwrap_or_default(),
            global_var: t.take("GLOBAL_VAR").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct LSO_VARIABLE_GROUP {
    pub solver_name: Vec<String>,
    pub domain_type: Vec<String>,
    pub group_name: Vec<String>,
    pub var_name: Vec<String>,
}
impl LSO_VARIABLE_GROUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("LSO_VARIABLE_GROUP").unwrap());
        Self {
            solver_name: t.take("SOLVER_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            domain_type: t.take("DOMAIN_TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            group_name: t.take("GROUP_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
            var_name: t.take("VAR_NAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct MAT_000 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub em: Vec<f64>,
    pub vfm: Vec<f64>,
    pub gfm: Vec<f64>,
    pub fopt: Vec<f64>,
    pub fcf: Vec<f64>,
    pub fcm: Vec<f64>,
    pub fcfc: Vec<f64>,
    pub fcmc: Vec<f64>,
    pub fcd: Vec<f64>,
}
impl MAT_000 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_000").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            vfm: t.take("VFM").and_then(|c| c.into_float()).unwrap_or_default(),
            gfm: t.take("GFM").and_then(|c| c.into_float()).unwrap_or_default(),
            fopt: t.take("FOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            fcf: t.take("FCF").and_then(|c| c.into_float()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_float()).unwrap_or_default(),
            fcfc: t.take("FCFC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcmc: t.take("FCMC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcd: t.take("FCD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_001 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub da: Vec<f64>,
    pub db: Vec<f64>,
    pub not_used: Vec<f64>,
}
impl MAT_001 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_001").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            da: t.take("DA").and_then(|c| c.into_float()).unwrap_or_default(),
            db: t.take("DB").and_then(|c| c.into_float()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_001_FLUID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub da: Vec<f64>,
    pub db: Vec<f64>,
    pub k: Vec<f64>,
    pub vc: Vec<f64>,
    pub cp: Vec<f64>,
}
impl MAT_001_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_001_FLUID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            da: t.take("DA").and_then(|c| c.into_float()).unwrap_or_default(),
            db: t.take("DB").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_002 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_002 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_002").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_002_ANIS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_002_ANIS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_002_ANIS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_002_SUNIL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub f60: Vec<i64>,
    pub mfparm: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
    pub t1fail: Vec<f64>,
    pub c1fail: Vec<f64>,
    pub t2fail: Vec<f64>,
    pub c2fail: Vec<f64>,
    pub t3fail: Vec<f64>,
    pub c3fail: Vec<f64>,
    pub s12fail: Vec<f64>,
    pub s23fail: Vec<f64>,
    pub s31fail: Vec<f64>,
}
impl MAT_002_SUNIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_002_SUNIL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            mfparm: t.take("MFPARM").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            t1fail: t.take("T1FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c1fail: t.take("C1FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            t2fail: t.take("T2FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c2fail: t.take("C2FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            t3fail: t.take("T3FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c3fail: t.take("C3FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s12fail: t.take("S12FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s23fail: t.take("S23FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s31fail: t.take("S31FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_003 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub beta: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub fs: Vec<f64>,
    pub vp: Vec<f64>,
}
impl MAT_003 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_003").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_004 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub pr1: Vec<f64>,
    pub pr2: Vec<f64>,
    pub pr3: Vec<f64>,
    pub pr4: Vec<f64>,
    pub pr5: Vec<f64>,
    pub pr6: Vec<f64>,
    pub pr7: Vec<f64>,
    pub pr8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub sigy1: Vec<f64>,
    pub sigy2: Vec<f64>,
    pub sigy3: Vec<f64>,
    pub sigy4: Vec<f64>,
    pub sigy5: Vec<f64>,
    pub sigy6: Vec<f64>,
    pub sigy7: Vec<f64>,
    pub sigy8: Vec<f64>,
    pub etan1: Vec<f64>,
    pub etan2: Vec<f64>,
    pub etan3: Vec<f64>,
    pub etan4: Vec<f64>,
    pub etan5: Vec<f64>,
    pub etan6: Vec<f64>,
    pub etan7: Vec<f64>,
    pub etan8: Vec<f64>,
}
impl MAT_004 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_004").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr3: t.take("PR3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr4: t.take("PR4").and_then(|c| c.into_float()).unwrap_or_default(),
            pr5: t.take("PR5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr6: t.take("PR6").and_then(|c| c.into_float()).unwrap_or_default(),
            pr7: t.take("PR7").and_then(|c| c.into_float()).unwrap_or_default(),
            pr8: t.take("PR8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy1: t.take("SIGY1").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy2: t.take("SIGY2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy3: t.take("SIGY3").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy4: t.take("SIGY4").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy5: t.take("SIGY5").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy6: t.take("SIGY6").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy7: t.take("SIGY7").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy8: t.take("SIGY8").and_then(|c| c.into_float()).unwrap_or_default(),
            etan1: t.take("ETAN1").and_then(|c| c.into_float()).unwrap_or_default(),
            etan2: t.take("ETAN2").and_then(|c| c.into_float()).unwrap_or_default(),
            etan3: t.take("ETAN3").and_then(|c| c.into_float()).unwrap_or_default(),
            etan4: t.take("ETAN4").and_then(|c| c.into_float()).unwrap_or_default(),
            etan5: t.take("ETAN5").and_then(|c| c.into_float()).unwrap_or_default(),
            etan6: t.take("ETAN6").and_then(|c| c.into_float()).unwrap_or_default(),
            etan7: t.take("ETAN7").and_then(|c| c.into_float()).unwrap_or_default(),
            etan8: t.take("ETAN8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_005 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub bulk: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub pc: Vec<f64>,
    pub vcr: Vec<f64>,
    pub ref_: Vec<f64>,
    pub lcid: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p9: Vec<f64>,
    pub p10: Vec<f64>,
}
impl MAT_005 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_005").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vcr: t.take("VCR").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p9: t.take("P9").and_then(|c| c.into_float()).unwrap_or_default(),
            p10: t.take("P10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_006 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_006 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_006").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_007 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_007 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_007").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_008 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub d: Vec<f64>,
    pub pcj: Vec<f64>,
    pub beta: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
}
impl MAT_008 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_008").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            pcj: t.take("PCJ").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_010 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub pc: Vec<f64>,
    pub fs: Vec<f64>,
    pub charl: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub eps11: Vec<f64>,
    pub eps12: Vec<f64>,
    pub eps13: Vec<f64>,
    pub eps14: Vec<f64>,
    pub eps15: Vec<f64>,
    pub eps16: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub es9: Vec<f64>,
    pub es10: Vec<f64>,
    pub es11: Vec<f64>,
    pub es12: Vec<f64>,
    pub es13: Vec<f64>,
    pub es14: Vec<f64>,
    pub es15: Vec<f64>,
    pub es16: Vec<f64>,
}
impl MAT_010 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_010").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            charl: t.take("CHARL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            eps11: t.take("EPS11").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12: t.take("EPS12").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13: t.take("EPS13").and_then(|c| c.into_float()).unwrap_or_default(),
            eps14: t.take("EPS14").and_then(|c| c.into_float()).unwrap_or_default(),
            eps15: t.take("EPS15").and_then(|c| c.into_float()).unwrap_or_default(),
            eps16: t.take("EPS16").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            es9: t.take("ES9").and_then(|c| c.into_float()).unwrap_or_default(),
            es10: t.take("ES10").and_then(|c| c.into_float()).unwrap_or_default(),
            es11: t.take("ES11").and_then(|c| c.into_float()).unwrap_or_default(),
            es12: t.take("ES12").and_then(|c| c.into_float()).unwrap_or_default(),
            es13: t.take("ES13").and_then(|c| c.into_float()).unwrap_or_default(),
            es14: t.take("ES14").and_then(|c| c.into_float()).unwrap_or_default(),
            es15: t.take("ES15").and_then(|c| c.into_float()).unwrap_or_default(),
            es16: t.take("ES16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_010_SPALL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sig0: Vec<f64>,
    pub eh: Vec<f64>,
    pub pc: Vec<f64>,
    pub fs: Vec<f64>,
    pub charl: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub spall: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub eps11: Vec<f64>,
    pub eps12: Vec<f64>,
    pub eps13: Vec<f64>,
    pub eps14: Vec<f64>,
    pub eps15: Vec<f64>,
    pub eps16: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub es9: Vec<f64>,
    pub es10: Vec<f64>,
    pub es11: Vec<f64>,
    pub es12: Vec<f64>,
    pub es13: Vec<f64>,
    pub es14: Vec<f64>,
    pub es15: Vec<f64>,
    pub es16: Vec<f64>,
}
impl MAT_010_SPALL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_010_SPALL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            charl: t.take("CHARL").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            eps11: t.take("EPS11").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12: t.take("EPS12").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13: t.take("EPS13").and_then(|c| c.into_float()).unwrap_or_default(),
            eps14: t.take("EPS14").and_then(|c| c.into_float()).unwrap_or_default(),
            eps15: t.take("EPS15").and_then(|c| c.into_float()).unwrap_or_default(),
            eps16: t.take("EPS16").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            es9: t.take("ES9").and_then(|c| c.into_float()).unwrap_or_default(),
            es10: t.take("ES10").and_then(|c| c.into_float()).unwrap_or_default(),
            es11: t.take("ES11").and_then(|c| c.into_float()).unwrap_or_default(),
            es12: t.take("ES12").and_then(|c| c.into_float()).unwrap_or_default(),
            es13: t.take("ES13").and_then(|c| c.into_float()).unwrap_or_default(),
            es14: t.take("ES14").and_then(|c| c.into_float()).unwrap_or_default(),
            es15: t.take("ES15").and_then(|c| c.into_float()).unwrap_or_default(),
            es16: t.take("ES16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_011 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g0: Vec<f64>,
    pub sigo: Vec<f64>,
    pub beta: Vec<f64>,
    pub n: Vec<f64>,
    pub gama: Vec<f64>,
    pub sigm: Vec<f64>,
    pub b: Vec<f64>,
    pub bp: Vec<f64>,
    pub h: Vec<f64>,
    pub f: Vec<f64>,
    pub a: Vec<f64>,
    pub tmo: Vec<f64>,
    pub gamo: Vec<f64>,
    pub sa: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub rp: Vec<f64>,
    pub flag: Vec<f64>,
    pub mmn: Vec<f64>,
    pub mmx: Vec<f64>,
    pub eco: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec2: Vec<f64>,
    pub ec3: Vec<f64>,
    pub ec4: Vec<f64>,
    pub ec5: Vec<f64>,
    pub ec6: Vec<f64>,
    pub ec7: Vec<f64>,
    pub ec8: Vec<f64>,
    pub ec9: Vec<f64>,
}
impl MAT_011 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_011").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigo: t.take("SIGO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigm: t.take("SIGM").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            bp: t.take("BP").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            tmo: t.take("TMO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamo: t.take("GAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            sa: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            mmn: t.take("MMN").and_then(|c| c.into_float()).unwrap_or_default(),
            mmx: t.take("MMX").and_then(|c| c.into_float()).unwrap_or_default(),
            eco: t.take("ECO").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec3: t.take("EC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ec4: t.take("EC4").and_then(|c| c.into_float()).unwrap_or_default(),
            ec5: t.take("EC5").and_then(|c| c.into_float()).unwrap_or_default(),
            ec6: t.take("EC6").and_then(|c| c.into_float()).unwrap_or_default(),
            ec7: t.take("EC7").and_then(|c| c.into_float()).unwrap_or_default(),
            ec8: t.take("EC8").and_then(|c| c.into_float()).unwrap_or_default(),
            ec9: t.take("EC9").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_011_LUND {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g0: Vec<f64>,
    pub sigo: Vec<f64>,
    pub beta: Vec<f64>,
    pub n: Vec<f64>,
    pub gama: Vec<f64>,
    pub sigm: Vec<f64>,
    pub b: Vec<f64>,
    pub bp: Vec<f64>,
    pub h: Vec<f64>,
    pub f: Vec<f64>,
    pub a: Vec<f64>,
    pub tmo: Vec<f64>,
    pub gamo: Vec<f64>,
    pub sa: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub rp: Vec<f64>,
    pub flag: Vec<f64>,
    pub mmn: Vec<f64>,
    pub mmx: Vec<f64>,
    pub eco: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec2: Vec<f64>,
    pub ec3: Vec<f64>,
    pub ec4: Vec<f64>,
    pub ec5: Vec<f64>,
    pub ec6: Vec<f64>,
    pub ec7: Vec<f64>,
    pub ec8: Vec<f64>,
    pub ec9: Vec<f64>,
    pub uk: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub yp: Vec<f64>,
    pub ya: Vec<f64>,
    pub ym: Vec<f64>,
}
impl MAT_011_LUND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_011_LUND").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigo: t.take("SIGO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigm: t.take("SIGM").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            bp: t.take("BP").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            tmo: t.take("TMO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamo: t.take("GAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            sa: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            mmn: t.take("MMN").and_then(|c| c.into_float()).unwrap_or_default(),
            mmx: t.take("MMX").and_then(|c| c.into_float()).unwrap_or_default(),
            eco: t.take("ECO").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec3: t.take("EC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ec4: t.take("EC4").and_then(|c| c.into_float()).unwrap_or_default(),
            ec5: t.take("EC5").and_then(|c| c.into_float()).unwrap_or_default(),
            ec6: t.take("EC6").and_then(|c| c.into_float()).unwrap_or_default(),
            ec7: t.take("EC7").and_then(|c| c.into_float()).unwrap_or_default(),
            ec8: t.take("EC8").and_then(|c| c.into_float()).unwrap_or_default(),
            ec9: t.take("EC9").and_then(|c| c.into_float()).unwrap_or_default(),
            uk: t.take("UK").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            ya: t.take("YA").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_012 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub bulk: Vec<f64>,
}
impl MAT_012 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_012").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_013 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub bulk: Vec<f64>,
    pub epf: Vec<f64>,
    pub prf: Vec<f64>,
    pub rem: Vec<f64>,
    pub trem: Vec<f64>,
}
impl MAT_013 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_013").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            epf: t.take("EPF").and_then(|c| c.into_float()).unwrap_or_default(),
            prf: t.take("PRF").and_then(|c| c.into_float()).unwrap_or_default(),
            rem: t.take("REM").and_then(|c| c.into_float()).unwrap_or_default(),
            trem: t.take("TREM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_014 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub bulk: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub pc: Vec<f64>,
    pub vcr: Vec<f64>,
    pub ref_: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p9: Vec<f64>,
    pub p10: Vec<f64>,
}
impl MAT_014 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_014").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vcr: t.take("VCR").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p9: t.take("P9").and_then(|c| c.into_float()).unwrap_or_default(),
            p10: t.take("P10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_015 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub dtf: Vec<f64>,
    pub vp: Vec<f64>,
    pub rateop: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub tm: Vec<f64>,
    pub tr: Vec<f64>,
    pub epso: Vec<f64>,
    pub cp: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub it: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub c2_p_xnp_d: Vec<f64>,
    pub erod: Vec<f64>,
    pub efmin: Vec<f64>,
    pub numint: Vec<f64>,
    pub k: Vec<f64>,
    pub eps1: Vec<f64>,
}
impl MAT_015 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_015").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            dtf: t.take("DTF").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            rateop: t.take("RATEOP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            c2_p_xnp_d: t.take("C2/P/XNP/D").and_then(|c| c.into_float()).unwrap_or_default(),
            erod: t.take("EROD").and_then(|c| c.into_float()).unwrap_or_default(),
            efmin: t.take("EFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_016 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigf: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a0f: Vec<f64>,
    pub a1f: Vec<f64>,
    pub b1: Vec<f64>,
    pub per: Vec<f64>,
    pub er: Vec<f64>,
    pub prr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub lcp: Vec<i64>,
    pub lcr: Vec<i64>,
    pub x1: Vec<f64>,
    pub x2: Vec<f64>,
    pub x3: Vec<f64>,
    pub x4: Vec<f64>,
    pub x5: Vec<f64>,
    pub x6: Vec<f64>,
    pub x7: Vec<f64>,
    pub x8: Vec<f64>,
    pub x9: Vec<f64>,
    pub x10: Vec<f64>,
    pub x11: Vec<f64>,
    pub x12: Vec<f64>,
    pub x13: Vec<f64>,
    pub x14: Vec<f64>,
    pub x15: Vec<f64>,
    pub x16: Vec<f64>,
    pub ys1: Vec<f64>,
    pub ys2: Vec<f64>,
    pub ys3: Vec<f64>,
    pub ys4: Vec<f64>,
    pub ys5: Vec<f64>,
    pub ys6: Vec<f64>,
    pub ys7: Vec<f64>,
    pub ys8: Vec<f64>,
    pub ys9: Vec<f64>,
    pub ys10: Vec<f64>,
    pub ys11: Vec<f64>,
    pub ys12: Vec<f64>,
    pub ys13: Vec<f64>,
    pub ys14: Vec<f64>,
    pub ys15: Vec<f64>,
    pub ys16: Vec<f64>,
}
impl MAT_016 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_016").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a0f: t.take("A0F").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            per: t.take("PER").and_then(|c| c.into_float()).unwrap_or_default(),
            er: t.take("ER").and_then(|c| c.into_float()).unwrap_or_default(),
            prr: t.take("PRR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            x7: t.take("X7").and_then(|c| c.into_float()).unwrap_or_default(),
            x8: t.take("X8").and_then(|c| c.into_float()).unwrap_or_default(),
            x9: t.take("X9").and_then(|c| c.into_float()).unwrap_or_default(),
            x10: t.take("X10").and_then(|c| c.into_float()).unwrap_or_default(),
            x11: t.take("X11").and_then(|c| c.into_float()).unwrap_or_default(),
            x12: t.take("X12").and_then(|c| c.into_float()).unwrap_or_default(),
            x13: t.take("X13").and_then(|c| c.into_float()).unwrap_or_default(),
            x14: t.take("X14").and_then(|c| c.into_float()).unwrap_or_default(),
            x15: t.take("X15").and_then(|c| c.into_float()).unwrap_or_default(),
            x16: t.take("X16").and_then(|c| c.into_float()).unwrap_or_default(),
            ys1: t.take("YS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ys2: t.take("YS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ys3: t.take("YS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ys4: t.take("YS4").and_then(|c| c.into_float()).unwrap_or_default(),
            ys5: t.take("YS5").and_then(|c| c.into_float()).unwrap_or_default(),
            ys6: t.take("YS6").and_then(|c| c.into_float()).unwrap_or_default(),
            ys7: t.take("YS7").and_then(|c| c.into_float()).unwrap_or_default(),
            ys8: t.take("YS8").and_then(|c| c.into_float()).unwrap_or_default(),
            ys9: t.take("YS9").and_then(|c| c.into_float()).unwrap_or_default(),
            ys10: t.take("YS10").and_then(|c| c.into_float()).unwrap_or_default(),
            ys11: t.take("YS11").and_then(|c| c.into_float()).unwrap_or_default(),
            ys12: t.take("YS12").and_then(|c| c.into_float()).unwrap_or_default(),
            ys13: t.take("YS13").and_then(|c| c.into_float()).unwrap_or_default(),
            ys14: t.take("YS14").and_then(|c| c.into_float()).unwrap_or_default(),
            ys15: t.take("YS15").and_then(|c| c.into_float()).unwrap_or_default(),
            ys16: t.take("YS16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_017 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fs: Vec<f64>,
    pub prf: Vec<f64>,
    pub soft: Vec<f64>,
    pub cvelo: Vec<f64>,
}
impl MAT_017 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_017").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            prf: t.take("PRF").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            cvelo: t.take("CVELO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_018 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vp: Vec<f64>,
    pub epsf: Vec<f64>,
}
impl MAT_018 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_018").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_019 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lc1: Vec<i64>,
    pub etan: Vec<f64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub tdel: Vec<f64>,
    pub rdef: Vec<f64>,
}
impl MAT_019 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_019").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            rdef: t.take("RDEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_020 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<f64>,
    pub couple: Vec<f64>,
    pub m: Vec<f64>,
    pub alias: Vec<String>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub lco_or_a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_020 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_020").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            couple: t.take("COUPLE").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alias: t.take("ALIAS").and_then(|c| c.into_str()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            lco_or_a1: t.take("LCO_OR_A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_021 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aa: Vec<f64>,
    pub ab: Vec<f64>,
    pub ac: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<i64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub r: Vec<f64>,
    pub lccha: Vec<i64>,
    pub lcchb: Vec<i64>,
    pub lcchc: Vec<i64>,
    pub lcaa: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcac: Vec<i64>,
}
impl MAT_021 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_021").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aa: t.take("AA").and_then(|c| c.into_float()).unwrap_or_default(),
            ab: t.take("AB").and_then(|c| c.into_float()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            lccha: t.take("LCCHA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcchb: t.take("LCCHB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcchc: t.take("LCCHC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcaa: t.take("LCAA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_022 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kfail: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub atrack: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub alph: Vec<f64>,
    pub sn: Vec<f64>,
    pub syz: Vec<f64>,
    pub szx: Vec<f64>,
}
impl MAT_022 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_022").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kfail: t.take("KFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            atrack: t.take("ATRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            szx: t.take("SZX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_023 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ref_: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub eai: Vec<f64>,
    pub ebi: Vec<f64>,
    pub eci: Vec<f64>,
    pub prabi: Vec<f64>,
    pub prcai: Vec<f64>,
    pub prcbi: Vec<f64>,
    pub aai: Vec<f64>,
    pub abi: Vec<f64>,
    pub aci: Vec<f64>,
    pub gabi: Vec<f64>,
    pub gbci: Vec<f64>,
    pub gcai: Vec<f64>,
    pub ti: Vec<f64>,
}
impl MAT_023 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_023").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            eai: t.take("EAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ebi: t.take("EBI").and_then(|c| c.into_float()).unwrap_or_default(),
            eci: t.take("ECI").and_then(|c| c.into_float()).unwrap_or_default(),
            prabi: t.take("PRABI").and_then(|c| c.into_float()).unwrap_or_default(),
            prcai: t.take("PRCAI").and_then(|c| c.into_float()).unwrap_or_default(),
            prcbi: t.take("PRCBI").and_then(|c| c.into_float()).unwrap_or_default(),
            aai: t.take("AAi").and_then(|c| c.into_float()).unwrap_or_default(),
            abi: t.take("ABi").and_then(|c| c.into_float()).unwrap_or_default(),
            aci: t.take("ACi").and_then(|c| c.into_float()).unwrap_or_default(),
            gabi: t.take("GABi").and_then(|c| c.into_float()).unwrap_or_default(),
            gbci: t.take("GBCi").and_then(|c| c.into_float()).unwrap_or_default(),
            gcai: t.take("GCAi").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_024 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_024 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_024").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_025 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub r: Vec<f64>,
    pub d: Vec<f64>,
    pub w: Vec<f64>,
    pub x0: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub plot: Vec<f64>,
    pub ftype: Vec<f64>,
    pub vec: Vec<f64>,
    pub toff: Vec<f64>,
}
impl MAT_025 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_025").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            plot: t.take("PLOT").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            vec: t.take("VEC").and_then(|c| c.into_float()).unwrap_or_default(),
            toff: t.take("TOFF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_026 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vf: Vec<f64>,
    pub mu: Vec<f64>,
    pub bulk: Vec<f64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcc: Vec<i64>,
    pub lcs: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcbc: Vec<i64>,
    pub lcca: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eaau: Vec<f64>,
    pub ebbu: Vec<f64>,
    pub eccu: Vec<f64>,
    pub gabu: Vec<f64>,
    pub gbcu: Vec<f64>,
    pub gcau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub tsef: Vec<f64>,
    pub ssef: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_026 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_026").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcs: t.take("LCS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbc: t.take("LCBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcca: t.take("LCCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eaau: t.take("EAAU").and_then(|c| c.into_float()).unwrap_or_default(),
            ebbu: t.take("EBBU").and_then(|c| c.into_float()).unwrap_or_default(),
            eccu: t.take("ECCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gabu: t.take("GABU").and_then(|c| c.into_float()).unwrap_or_default(),
            gbcu: t.take("GBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gcau: t.take("GCAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            tsef: t.take("TSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            ssef: t.take("SSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_027 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_027 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_027").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_028 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
}
impl MAT_028 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_028").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_029 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub df: Vec<f64>,
    pub iaflc: Vec<i64>,
    pub ytflag: Vec<f64>,
    pub asoft: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub m6: Vec<f64>,
    pub m7: Vec<f64>,
    pub m8: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub lc5: Vec<i64>,
    pub lc6: Vec<i64>,
    pub lc7: Vec<i64>,
    pub lc8: Vec<i64>,
    pub lps1: Vec<i64>,
    pub sfs1: Vec<f64>,
    pub lps2: Vec<i64>,
    pub sfs2: Vec<f64>,
    pub yms1: Vec<f64>,
    pub yms2: Vec<f64>,
    pub lpt1: Vec<i64>,
    pub sft1: Vec<f64>,
    pub lpt2: Vec<i64>,
    pub sft2: Vec<f64>,
    pub ymt1: Vec<f64>,
    pub ymt2: Vec<f64>,
    pub lpr: Vec<i64>,
    pub sfr: Vec<f64>,
    pub ymr: Vec<f64>,
}
impl MAT_029 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_029").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            iaflc: t.take("IAFLC").and_then(|c| c.into_int()).unwrap_or_default(),
            ytflag: t.take("YTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            asoft: t.take("ASOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            m6: t.take("M6").and_then(|c| c.into_float()).unwrap_or_default(),
            m7: t.take("M7").and_then(|c| c.into_float()).unwrap_or_default(),
            m8: t.take("M8").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            lc5: t.take("LC5").and_then(|c| c.into_int()).unwrap_or_default(),
            lc6: t.take("LC6").and_then(|c| c.into_int()).unwrap_or_default(),
            lc7: t.take("LC7").and_then(|c| c.into_int()).unwrap_or_default(),
            lc8: t.take("LC8").and_then(|c| c.into_int()).unwrap_or_default(),
            lps1: t.take("LPS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs1: t.take("SFS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lps2: t.take("LPS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs2: t.take("SFS2").and_then(|c| c.into_float()).unwrap_or_default(),
            yms1: t.take("YMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            yms2: t.take("YMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt1: t.take("LPT1").and_then(|c| c.into_int()).unwrap_or_default(),
            sft1: t.take("SFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt2: t.take("LPT2").and_then(|c| c.into_int()).unwrap_or_default(),
            sft2: t.take("SFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt1: t.take("YMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt2: t.take("YMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpr: t.take("LPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymr: t.take("YMR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_030 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lcss: Vec<f64>,
    pub sig_ass: Vec<f64>,
    pub sig_asf: Vec<f64>,
    pub sig_sas: Vec<f64>,
    pub sig_saf: Vec<f64>,
    pub epsl: Vec<f64>,
    pub alpha: Vec<f64>,
    pub ymrt: Vec<f64>,
    pub lcid_as: Vec<i64>,
    pub lcid_sa: Vec<i64>,
}
impl MAT_030 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_030").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_ass: t.take("SIG_ASS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_asf: t.take("SIG_ASF").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_sas: t.take("SIG_SAS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_saf: t.take("SIG_SAF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsl: t.take("EPSL").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ymrt: t.take("YMRT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_as: t.take("LCID_AS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_sa: t.take("LCID_SA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_031 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub c100: Vec<f64>,
    pub c200: Vec<f64>,
    pub c300: Vec<f64>,
    pub c400: Vec<f64>,
    pub c110: Vec<f64>,
    pub c210: Vec<f64>,
    pub c010: Vec<f64>,
    pub c020: Vec<f64>,
    pub exit: Vec<f64>,
    pub emax: Vec<f64>,
    pub emin: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_031 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_031").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c100: t.take("C100").and_then(|c| c.into_float()).unwrap_or_default(),
            c200: t.take("C200").and_then(|c| c.into_float()).unwrap_or_default(),
            c300: t.take("C300").and_then(|c| c.into_float()).unwrap_or_default(),
            c400: t.take("C400").and_then(|c| c.into_float()).unwrap_or_default(),
            c110: t.take("C110").and_then(|c| c.into_float()).unwrap_or_default(),
            c210: t.take("C210").and_then(|c| c.into_float()).unwrap_or_default(),
            c010: t.take("C010").and_then(|c| c.into_float()).unwrap_or_default(),
            c020: t.take("C020").and_then(|c| c.into_float()).unwrap_or_default(),
            exit: t.take("EXIT").and_then(|c| c.into_float()).unwrap_or_default(),
            emax: t.take("EMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            emin: t.take("EMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_032 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub eg: Vec<f64>,
    pub prg: Vec<f64>,
    pub syg: Vec<f64>,
    pub etg: Vec<f64>,
    pub efg: Vec<f64>,
    pub ep: Vec<f64>,
    pub prp: Vec<f64>,
    pub syp: Vec<f64>,
    pub etp: Vec<f64>,
    pub f1: Vec<f64>,
    pub f2: Vec<f64>,
    pub f3: Vec<f64>,
    pub f4: Vec<f64>,
    pub f5: Vec<f64>,
    pub f6: Vec<f64>,
    pub f7: Vec<f64>,
    pub f8: Vec<f64>,
}
impl MAT_032 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_032").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            eg: t.take("EG").and_then(|c| c.into_float()).unwrap_or_default(),
            prg: t.take("PRG").and_then(|c| c.into_float()).unwrap_or_default(),
            syg: t.take("SYG").and_then(|c| c.into_float()).unwrap_or_default(),
            etg: t.take("ETG").and_then(|c| c.into_float()).unwrap_or_default(),
            efg: t.take("EFG").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
            prp: t.take("PRP").and_then(|c| c.into_float()).unwrap_or_default(),
            syp: t.take("SYP").and_then(|c| c.into_float()).unwrap_or_default(),
            etp: t.take("ETP").and_then(|c| c.into_float()).unwrap_or_default(),
            f1: t.take("F1").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            f3: t.take("F3").and_then(|c| c.into_float()).unwrap_or_default(),
            f4: t.take("F4").and_then(|c| c.into_float()).unwrap_or_default(),
            f5: t.take("F5").and_then(|c| c.into_float()).unwrap_or_default(),
            f6: t.take("F6").and_then(|c| c.into_float()).unwrap_or_default(),
            f7: t.take("F7").and_then(|c| c.into_float()).unwrap_or_default(),
            f8: t.take("F8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_033 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub f: Vec<f64>,
    pub g: Vec<f64>,
    pub h: Vec<f64>,
    pub lcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_033 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_033").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_033_96 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub esr0: Vec<f64>,
    pub m: Vec<f64>,
    pub hard: Vec<i64>,
    pub a: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az0: Vec<f64>,
    pub az1: Vec<f64>,
    pub aopt: Vec<f64>,
    pub offang: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_033_96 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_033_96").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            esr0: t.take("ESR0").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            hard: t.take("HARD").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az0: t.take("AZ0").and_then(|c| c.into_float()).unwrap_or_default(),
            az1: t.take("AZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            offang: t.take("OFFANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_034 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub f: Vec<f64>,
    pub prba: Vec<f64>,
    pub prab: Vec<f64>,
    pub f_2: Vec<f64>,
    pub gab: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub cse: Vec<f64>,
    pub el: Vec<f64>,
    pub prl: Vec<f64>,
    pub lratio: Vec<f64>,
    pub damp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub flc: Vec<f64>,
    pub fac: Vec<f64>,
    pub ela: Vec<f64>,
    pub lnrc: Vec<f64>,
    pub form: Vec<i64>,
    pub fvopt: Vec<i64>,
    pub tsrfac: Vec<f64>,
    pub unused: Vec<i64>,
    pub rgbrth: Vec<f64>,
    pub a0ref: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub x0: Vec<f64>,
    pub x1: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_2_3: Vec<f64>,
    pub f_3_2: Vec<f64>,
    pub beta: Vec<f64>,
    pub isrefg: Vec<i64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcua: Vec<i64>,
    pub lcub: Vec<i64>,
    pub lcuab: Vec<i64>,
    pub rl: Vec<f64>,
    pub lcaa: Vec<i64>,
    pub lcbb: Vec<i64>,
    pub h: Vec<f64>,
    pub dt: Vec<i64>,
    pub f40: Vec<i64>,
    pub ecoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub tcoat: Vec<f64>,
}
impl MAT_034 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_034").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prab: t.take("PRAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            prl: t.take("PRL").and_then(|c| c.into_float()).unwrap_or_default(),
            lratio: t.take("LRATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            flc: t.take("FLC").and_then(|c| c.into_float()).unwrap_or_default(),
            fac: t.take("FAC").and_then(|c| c.into_float()).unwrap_or_default(),
            ela: t.take("ELA").and_then(|c| c.into_float()).unwrap_or_default(),
            lnrc: t.take("LNRC").and_then(|c| c.into_float()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            fvopt: t.take("FVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tsrfac: t.take("TSRFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            rgbrth: t.take("RGBRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            a0ref: t.take("A0REF").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            isrefg: t.take("ISREFG").and_then(|c| c.into_int()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcua: t.take("LCUA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcub: t.take("LCUB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuab: t.take("LCUAB").and_then(|c| c.into_int()).unwrap_or_default(),
            rl: t.take("RL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaa: t.take("LCAA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbb: t.take("LCBB").and_then(|c| c.into_int()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_034M {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pxx: Vec<f64>,
    pub pyy: Vec<f64>,
    pub sxy: Vec<f64>,
    pub damp: Vec<f64>,
    pub th: Vec<f64>,
    pub fvopt: Vec<f64>,
    pub x0: Vec<f64>,
    pub x1: Vec<f64>,
    pub flc_x2: Vec<f64>,
    pub fac_x3: Vec<f64>,
    pub isrefg: Vec<f64>,
    pub cse: Vec<f64>,
    pub srfac: Vec<i64>,
    pub bulkc: Vec<f64>,
    pub jacc: Vec<f64>,
    pub fxx: Vec<i64>,
    pub fyy: Vec<i64>,
    pub dt: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ecoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub tcoat: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_034M {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_034M").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pxx: t.take("PXX").and_then(|c| c.into_float()).unwrap_or_default(),
            pyy: t.take("PYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            th: t.take("TH").and_then(|c| c.into_float()).unwrap_or_default(),
            fvopt: t.take("FVOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            flc_x2: t.take("FLC/X2").and_then(|c| c.into_float()).unwrap_or_default(),
            fac_x3: t.take("FAC/X3").and_then(|c| c.into_float()).unwrap_or_default(),
            isrefg: t.take("ISREFG").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            srfac: t.take("SRFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            bulkc: t.take("BULKC").and_then(|c| c.into_float()).unwrap_or_default(),
            jacc: t.take("JACC").and_then(|c| c.into_float()).unwrap_or_default(),
            fxx: t.take("FXX").and_then(|c| c.into_int()).unwrap_or_default(),
            fyy: t.take("FYY").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_035 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_035 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_035").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_036 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub f40: Vec<i64>,
    pub pb: Vec<f64>,
    pub nlp: Vec<i64>,
    pub htb: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub htflag: Vec<f64>,
    pub usrfail: Vec<f64>,
    pub lcbi: Vec<f64>,
    pub lcsh: Vec<f64>,
}
impl MAT_036 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_036").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pb: t.take("PB").and_then(|c| c.into_float()).unwrap_or_default(),
            nlp: t.take("NLP").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbi: t.take("LCBI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsh: t.take("LCSH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_036E {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lch00: Vec<i64>,
    pub lch45: Vec<i64>,
    pub lch90: Vec<i64>,
    pub lchbi: Vec<i64>,
    pub lchsh: Vec<i64>,
    pub lcr00: Vec<i64>,
    pub lcr45: Vec<i64>,
    pub lcr90: Vec<i64>,
    pub lcrbi: Vec<i64>,
    pub lcrsh: Vec<i64>,
    pub m: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_036E {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_036E").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lch00: t.take("LCH00").and_then(|c| c.into_int()).unwrap_or_default(),
            lch45: t.take("LCH45").and_then(|c| c.into_int()).unwrap_or_default(),
            lch90: t.take("LCH90").and_then(|c| c.into_int()).unwrap_or_default(),
            lchbi: t.take("LCHBI").and_then(|c| c.into_int()).unwrap_or_default(),
            lchsh: t.take("LCHSH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr00: t.take("LCR00").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr45: t.take("LCR45").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr90: t.take("LCR90").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrbi: t.take("LCRBI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrsh: t.take("LCRSH").and_then(|c| c.into_int()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_037 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub idscale: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub icfld: Vec<i64>,
    pub unused: Vec<i64>,
    pub strainlt: Vec<f64>,
}
impl MAT_037 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_037").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            idscale: t.take("IDSCALE").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            icfld: t.take("ICFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            strainlt: t.take("STRAINLT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_038 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_038 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_038").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_039 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub lcfld: Vec<i64>,
}
impl MAT_039 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_039").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfld: t.take("LCFLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_040 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub dt: Vec<f64>,
    pub tramp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcida: Vec<f64>,
    pub lcidb: Vec<f64>,
    pub efail: Vec<f64>,
    pub dtfail: Vec<f64>,
    pub cdamp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub atrack: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidab: Vec<i64>,
    pub lcidbc: Vec<i64>,
    pub lcidca: Vec<i64>,
}
impl MAT_040 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_040").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtfail: t.take("DTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            cdamp: t.take("CDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            atrack: t.take("ATRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidab: t.take("LCIDAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidbc: t.take("LCIDBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidca: t.take("LCIDCA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_041_050 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mt: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub iortho: Vec<i64>,
    pub ibulk: Vec<i64>,
    pub ig: Vec<i64>,
    pub ivect: Vec<i64>,
    pub ifail: Vec<i64>,
    pub itherm: Vec<i64>,
    pub ihyper: Vec<i64>,
    pub ieos: Vec<i64>,
    pub lmca: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ievts: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p1_2: Vec<f64>,
    pub p2_2: Vec<f64>,
    pub p3_2: Vec<f64>,
    pub p4_2: Vec<f64>,
    pub p5_2: Vec<f64>,
    pub p6_2: Vec<f64>,
    pub p7_2: Vec<f64>,
    pub p8_2: Vec<f64>,
}
impl MAT_041_050 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_041_050").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mt: t.take("MT").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            ibulk: t.take("IBULK").and_then(|c| c.into_int()).unwrap_or_default(),
            ig: t.take("IG").and_then(|c| c.into_int()).unwrap_or_default(),
            ivect: t.take("IVECT").and_then(|c| c.into_int()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            itherm: t.take("ITHERM").and_then(|c| c.into_int()).unwrap_or_default(),
            ihyper: t.take("IHYPER").and_then(|c| c.into_int()).unwrap_or_default(),
            ieos: t.take("IEOS").and_then(|c| c.into_int()).unwrap_or_default(),
            lmca: t.take("LMCA").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ievts: t.take("IEVTS").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p1_2: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2_2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3_2: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4_2: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5_2: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6_2: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7_2: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8_2: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_051 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c17: Vec<f64>,
    pub c18: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
}
impl MAT_051 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_051").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c17: t.take("C17").and_then(|c| c.into_float()).unwrap_or_default(),
            c18: t.take("C18").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_052 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c17: Vec<f64>,
    pub c18: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub n: Vec<f64>,
    pub d0: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_052 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_052").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c17: t.take("C17").and_then(|c| c.into_float()).unwrap_or_default(),
            c18: t.take("C18").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_053 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub p0: Vec<f64>,
    pub phi: Vec<f64>,
    pub gama0: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_053 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_053").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            gama0: t.take("GAMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_054_055 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f_2way: Vec<f64>,
    pub ti: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub dfailm: Vec<f64>,
    pub dfails: Vec<f64>,
    pub tfail: Vec<f64>,
    pub alph: Vec<f64>,
    pub soft: Vec<f64>,
    pub fbrt: Vec<f64>,
    pub ycfac: Vec<f64>,
    pub dfailt: Vec<f64>,
    pub dfailc: Vec<f64>,
    pub efs: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub crit: Vec<f64>,
    pub beta: Vec<f64>,
    pub pel: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub soft2: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub ncyred: Vec<f64>,
    pub softg: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub dt: Vec<f64>,
}
impl MAT_054_055 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_054/055").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("(EC)").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("(PRCA)").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("(PRCB)").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("(KF)").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2way: t.take("2WAY").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("TI").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailm: t.take("DFAILM").and_then(|c| c.into_float()).unwrap_or_default(),
            dfails: t.take("DFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbrt: t.take("FBRT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycfac: t.take("YCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailt: t.take("DFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailc: t.take("DFAILC").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            crit: t.take("CRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pel: t.take("PEL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            soft2: t.take("SOFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyred: t.take("NCYRED").and_then(|c| c.into_float()).unwrap_or_default(),
            softg: t.take("SOFTG").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_057 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tc: Vec<f64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_057 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_057").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_058 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub fs: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcdfail: Vec<i64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub lctau: Vec<i64>,
    pub lcgam: Vec<i64>,
    pub dt: Vec<f64>,
    pub lce11c: Vec<i64>,
    pub lce11t: Vec<i64>,
    pub lce22c: Vec<i64>,
    pub lce22t: Vec<i64>,
    pub lcgms: Vec<i64>,
    pub lcefs: Vec<i64>,
}
impl MAT_058 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_058").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdfail: t.take("LCDFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam: t.take("LCGAM").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lce11c: t.take("LCE11C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce11t: t.take("LCE11T").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22c: t.take("LCE22C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22t: t.take("LCE22T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms: t.take("LCGMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefs: t.take("LCEFS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_058_SOLID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub fs: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcdfail: Vec<i64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub e33c: Vec<f64>,
    pub e33t: Vec<f64>,
    pub gm23: Vec<f64>,
    pub gm31: Vec<f64>,
    pub zc: Vec<f64>,
    pub zt: Vec<f64>,
    pub sc23: Vec<f64>,
    pub sc31: Vec<f64>,
    pub slimt3: Vec<f64>,
    pub slimc3: Vec<f64>,
    pub slims23: Vec<f64>,
    pub lsims31: Vec<f64>,
    pub tau2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub tau3: Vec<f64>,
    pub gamma3: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub lctau: Vec<i64>,
    pub lcgam: Vec<i64>,
    pub dt: Vec<f64>,
    pub lce11c: Vec<i64>,
    pub lce11t: Vec<i64>,
    pub lce22c: Vec<i64>,
    pub lce22t: Vec<i64>,
    pub lcgms: Vec<i64>,
    pub lcefs: Vec<i64>,
    pub lczc: Vec<i64>,
    pub lczt: Vec<i64>,
    pub lcsc23: Vec<i64>,
    pub lcsc31: Vec<i64>,
    pub lctau2: Vec<i64>,
    pub lcgam2: Vec<i64>,
    pub lctau3: Vec<i64>,
    pub lcgam3: Vec<i64>,
    pub lce33c: Vec<i64>,
    pub lce33t: Vec<i64>,
    pub lcgms23: Vec<i64>,
    pub lcgms31: Vec<i64>,
}
impl MAT_058_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_058_SOLID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdfail: t.take("LCDFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            e33c: t.take("E33C").and_then(|c| c.into_float()).unwrap_or_default(),
            e33t: t.take("E33T").and_then(|c| c.into_float()).unwrap_or_default(),
            gm23: t.take("GM23").and_then(|c| c.into_float()).unwrap_or_default(),
            gm31: t.take("GM31").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc23: t.take("SC23").and_then(|c| c.into_float()).unwrap_or_default(),
            sc31: t.take("SC31").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt3: t.take("SLIMT3").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc3: t.take("SLIMC3").and_then(|c| c.into_float()).unwrap_or_default(),
            slims23: t.take("SLIMS23").and_then(|c| c.into_float()).unwrap_or_default(),
            lsims31: t.take("LSIMS31").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tau3: t.take("TAU3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam: t.take("LCGAM").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lce11c: t.take("LCE11C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce11t: t.take("LCE11T").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22c: t.take("LCE22C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22t: t.take("LCE22T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms: t.take("LCGMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefs: t.take("LCEFS").and_then(|c| c.into_int()).unwrap_or_default(),
            lczc: t.take("LCZC").and_then(|c| c.into_int()).unwrap_or_default(),
            lczt: t.take("LCZT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc23: t.take("LCSC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc31: t.take("LCSC31").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau2: t.take("LCTAU2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam2: t.take("LCGAM2").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau3: t.take("LCTAU3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam3: t.take("LCGAM3").and_then(|c| c.into_int()).unwrap_or_default(),
            lce33c: t.take("LCE33C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce33t: t.take("LCE33T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms23: t.take("LCGMS23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms31: t.take("LCGMS31").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_059_SHELL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub tsize: Vec<f64>,
    pub alp: Vec<f64>,
    pub soft: Vec<f64>,
    pub fbrt: Vec<f64>,
    pub sr: Vec<f64>,
    pub sf: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
}
impl MAT_059_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_059_SHELL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            alp: t.take("ALP").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbrt: t.take("FBRT").and_then(|c| c.into_float()).unwrap_or_default(),
            sr: t.take("SR").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_059_SOLID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sba: Vec<f64>,
    pub sca: Vec<f64>,
    pub scb: Vec<f64>,
    pub xxc: Vec<f64>,
    pub yyc: Vec<f64>,
    pub zzc: Vec<f64>,
    pub xxt: Vec<f64>,
    pub yyt: Vec<f64>,
    pub zzt: Vec<f64>,
}
impl MAT_059_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_059_SOLID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sba: t.take("SBA").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            scb: t.take("SCB").and_then(|c| c.into_float()).unwrap_or_default(),
            xxc: t.take("XXC").and_then(|c| c.into_float()).unwrap_or_default(),
            yyc: t.take("YYC").and_then(|c| c.into_float()).unwrap_or_default(),
            zzc: t.take("ZZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xxt: t.take("XXT").and_then(|c| c.into_float()).unwrap_or_default(),
            yyt: t.take("YYT").and_then(|c| c.into_float()).unwrap_or_default(),
            zzt: t.take("ZZT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_059_SPH {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sba: Vec<f64>,
    pub sca: Vec<f64>,
    pub scb: Vec<f64>,
    pub xxc: Vec<f64>,
    pub yyc: Vec<f64>,
    pub zzc: Vec<f64>,
    pub xxt: Vec<f64>,
    pub yyt: Vec<f64>,
    pub zzt: Vec<f64>,
}
impl MAT_059_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_059_SPH").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sba: t.take("SBA").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            scb: t.take("SCB").and_then(|c| c.into_float()).unwrap_or_default(),
            xxc: t.take("XXC").and_then(|c| c.into_float()).unwrap_or_default(),
            yyc: t.take("YYC").and_then(|c| c.into_float()).unwrap_or_default(),
            zzc: t.take("ZZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xxt: t.take("XXT").and_then(|c| c.into_float()).unwrap_or_default(),
            yyt: t.take("YYT").and_then(|c| c.into_float()).unwrap_or_default(),
            zzt: t.take("ZZT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_060 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub v0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub lcid: Vec<f64>,
    pub pr1: Vec<f64>,
    pub pr2: Vec<f64>,
    pub pr3: Vec<f64>,
    pub pr4: Vec<f64>,
    pub pr5: Vec<f64>,
    pub pr6: Vec<f64>,
    pub pr7: Vec<f64>,
    pub pr8: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub v4: Vec<f64>,
    pub v5: Vec<f64>,
    pub v6: Vec<f64>,
    pub v7: Vec<f64>,
    pub v8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
}
impl MAT_060 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_060").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr3: t.take("PR3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr4: t.take("PR4").and_then(|c| c.into_float()).unwrap_or_default(),
            pr5: t.take("PR5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr6: t.take("PR6").and_then(|c| c.into_float()).unwrap_or_default(),
            pr7: t.take("PR7").and_then(|c| c.into_float()).unwrap_or_default(),
            pr8: t.take("PR8").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            v4: t.take("V4").and_then(|c| c.into_float()).unwrap_or_default(),
            v5: t.take("V5").and_then(|c| c.into_float()).unwrap_or_default(),
            v6: t.take("V6").and_then(|c| c.into_float()).unwrap_or_default(),
            v7: t.take("V7").and_then(|c| c.into_float()).unwrap_or_default(),
            v8: t.take("V8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_060C {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub v0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub lcid: Vec<f64>,
    pub pr_lc: Vec<i64>,
    pub ym_lc: Vec<i64>,
    pub a_lc: Vec<i64>,
    pub v_lc: Vec<i64>,
    pub v_log: Vec<f64>,
}
impl MAT_060C {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_060C").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            pr_lc: t.take("PR_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            ym_lc: t.take("YM_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            a_lc: t.take("A_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            v_lc: t.take("V_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            v_log: t.take("V_LOG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_061 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub dc: Vec<f64>,
    pub fo: Vec<f64>,
    pub so: Vec<f64>,
}
impl MAT_061 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_061").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            fo: t.take("FO").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_062 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub n1: Vec<f64>,
    pub v2: Vec<f64>,
    pub e2: Vec<f64>,
    pub n2: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_062 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_062").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_063 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tsc: Vec<f64>,
    pub damp: Vec<f64>,
    pub model: Vec<i64>,
}
impl MAT_063 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_063").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_064 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub e0: Vec<f64>,
    pub vp: Vec<f64>,
    pub eps0: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_064 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_064").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_065 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub troom: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub efail: Vec<f64>,
    pub vp: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub g1: Vec<f64>,
    pub g2: Vec<f64>,
    pub g3: Vec<f64>,
    pub g4: Vec<f64>,
    pub bulk: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_065 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_065").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            troom: t.take("TROOM").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_066 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tkr: Vec<f64>,
    pub tks: Vec<f64>,
    pub tkt: Vec<f64>,
    pub rkr: Vec<f64>,
    pub rks: Vec<f64>,
    pub rkt: Vec<f64>,
    pub tdr: Vec<f64>,
    pub tds: Vec<f64>,
    pub tdt: Vec<f64>,
    pub rdr: Vec<f64>,
    pub rds: Vec<f64>,
    pub rdt: Vec<f64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
}
impl MAT_066 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_066").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tkr: t.take("TKR").and_then(|c| c.into_float()).unwrap_or_default(),
            tks: t.take("TKS").and_then(|c| c.into_float()).unwrap_or_default(),
            tkt: t.take("TKT").and_then(|c| c.into_float()).unwrap_or_default(),
            rkr: t.take("RKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rks: t.take("RKS").and_then(|c| c.into_float()).unwrap_or_default(),
            rkt: t.take("RKT").and_then(|c| c.into_float()).unwrap_or_default(),
            tdr: t.take("TDR").and_then(|c| c.into_float()).unwrap_or_default(),
            tds: t.take("TDS").and_then(|c| c.into_float()).unwrap_or_default(),
            tdt: t.take("TDT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdr: t.take("RDR").and_then(|c| c.into_float()).unwrap_or_default(),
            rds: t.take("RDS").and_then(|c| c.into_float()).unwrap_or_default(),
            rdt: t.take("RDT").and_then(|c| c.into_float()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_067 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub lcidtr: Vec<i64>,
    pub lcidts: Vec<i64>,
    pub lcidtt: Vec<i64>,
    pub lcidrr: Vec<i64>,
    pub lcidrs: Vec<i64>,
    pub lcidrt: Vec<i64>,
    pub lcidtdr: Vec<i64>,
    pub lcidtds: Vec<i64>,
    pub lcidtdt: Vec<i64>,
    pub lcidrdr: Vec<i64>,
    pub lcidrds: Vec<i64>,
    pub lcidrdt: Vec<i64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
    pub ffailr: Vec<f64>,
    pub ffails: Vec<f64>,
    pub ffailt: Vec<f64>,
    pub mfailr: Vec<f64>,
    pub mfails: Vec<f64>,
    pub mfailt: Vec<f64>,
    pub ufailr: Vec<f64>,
    pub ufails: Vec<f64>,
    pub ufailt: Vec<f64>,
    pub tfailr: Vec<f64>,
    pub tfails: Vec<f64>,
    pub tfailt: Vec<f64>,
}
impl MAT_067 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_067").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidtr: t.take("LCIDTR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidts: t.take("LCIDTS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtt: t.take("LCIDTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrs: t.take("LCIDRS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrt: t.take("LCIDRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdr: t.take("LCIDTDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtds: t.take("LCIDTDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdt: t.take("LCIDTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdr: t.take("LCIDRDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrds: t.take("LCIDRDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdt: t.take("LCIDRDT").and_then(|c| c.into_int()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailr: t.take("FFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ffails: t.take("FFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailt: t.take("FFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailr: t.take("MFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            mfails: t.take("MFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailt: t.take("MFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailr: t.take("UFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ufails: t.take("UFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailt: t.take("UFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailr: t.take("TFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            tfails: t.take("TFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailt: t.take("TFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_068 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tkr: Vec<f64>,
    pub tks: Vec<f64>,
    pub tkt: Vec<f64>,
    pub rkr: Vec<f64>,
    pub rks: Vec<f64>,
    pub rkt: Vec<f64>,
    pub tdr: Vec<f64>,
    pub tds: Vec<f64>,
    pub tdt: Vec<f64>,
    pub rdr: Vec<f64>,
    pub rds: Vec<f64>,
    pub rdt: Vec<f64>,
    pub lcpdr: Vec<i64>,
    pub lcpds: Vec<i64>,
    pub lcpdt: Vec<i64>,
    pub lcpmr: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub lcpmt: Vec<i64>,
    pub ffailr: Vec<f64>,
    pub ffails: Vec<f64>,
    pub ffailt: Vec<f64>,
    pub mfailr: Vec<f64>,
    pub mfails: Vec<f64>,
    pub mfailt: Vec<f64>,
    pub ufailr: Vec<f64>,
    pub ufails: Vec<f64>,
    pub ufailt: Vec<f64>,
    pub tfailr: Vec<f64>,
    pub tfails: Vec<f64>,
    pub tfailt: Vec<f64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
}
impl MAT_068 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_068").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tkr: t.take("TKR").and_then(|c| c.into_float()).unwrap_or_default(),
            tks: t.take("TKS").and_then(|c| c.into_float()).unwrap_or_default(),
            tkt: t.take("TKT").and_then(|c| c.into_float()).unwrap_or_default(),
            rkr: t.take("RKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rks: t.take("RKS").and_then(|c| c.into_float()).unwrap_or_default(),
            rkt: t.take("RKT").and_then(|c| c.into_float()).unwrap_or_default(),
            tdr: t.take("TDR").and_then(|c| c.into_float()).unwrap_or_default(),
            tds: t.take("TDS").and_then(|c| c.into_float()).unwrap_or_default(),
            tdt: t.take("TDT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdr: t.take("RDR").and_then(|c| c.into_float()).unwrap_or_default(),
            rds: t.take("RDS").and_then(|c| c.into_float()).unwrap_or_default(),
            rdt: t.take("RDT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpdr: t.take("LCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpds: t.take("LCPDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpdt: t.take("LCPDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpmr: t.take("LCPMR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            ffailr: t.take("FFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ffails: t.take("FFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailt: t.take("FFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailr: t.take("MFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            mfails: t.take("MFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailt: t.take("MFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailr: t.take("UFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ufails: t.take("UFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailt: t.take("UFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailr: t.take("TFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            tfails: t.take("TFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailt: t.take("TFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_069 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub st: Vec<f64>,
    pub d: Vec<f64>,
    pub r: Vec<f64>,
    pub h: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub c3: Vec<f64>,
    pub stf: Vec<f64>,
    pub rhof: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub lcidf: Vec<f64>,
    pub lcidd: Vec<f64>,
    pub s0: Vec<f64>,
    pub orfloc: Vec<f64>,
    pub orfrad: Vec<f64>,
    pub sf: Vec<f64>,
    pub dc: Vec<f64>,
}
impl MAT_069 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_069").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            stf: t.take("STF").and_then(|c| c.into_float()).unwrap_or_default(),
            rhof: t.take("RHOF").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidf: t.take("LCIDF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidd: t.take("LCIDD").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            orfloc: t.take("ORFLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            orfrad: t.take("ORFRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_070 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub co: Vec<f64>,
    pub n: Vec<f64>,
    pub p0: Vec<f64>,
    pub pa: Vec<f64>,
    pub ap: Vec<f64>,
    pub kh: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fr: Vec<f64>,
    pub sclf: Vec<f64>,
    pub clear: Vec<f64>,
}
impl MAT_070 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_070").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("CO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            pa: t.take("PA").and_then(|c| c.into_float()).unwrap_or_default(),
            ap: t.take("AP").and_then(|c| c.into_float()).unwrap_or_default(),
            kh: t.take("KH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fr: t.take("FR").and_then(|c| c.into_float()).unwrap_or_default(),
            sclf: t.take("SCLF").and_then(|c| c.into_float()).unwrap_or_default(),
            clear: t.take("CLEAR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_071 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub f0: Vec<f64>,
    pub tmaxf0: Vec<f64>,
    pub tramp: Vec<f64>,
    pub iread: Vec<i64>,
    pub output: Vec<i64>,
    pub tstart: Vec<f64>,
    pub fracl0: Vec<f64>,
    pub mxeps: Vec<f64>,
    pub mxfrc: Vec<f64>,
}
impl MAT_071 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_071").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            tmaxf0: t.take("TMAXF0").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            iread: t.take("IREAD").and_then(|c| c.into_int()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            fracl0: t.take("FRACL0").and_then(|c| c.into_float()).unwrap_or_default(),
            mxeps: t.take("MXEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            mxfrc: t.take("MXFRC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_072 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigf: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a0y: Vec<f64>,
    pub a1y: Vec<f64>,
    pub a2y: Vec<f64>,
    pub a1f: Vec<f64>,
    pub a2f: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub per: Vec<f64>,
    pub er: Vec<f64>,
    pub prr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub lcp: Vec<i64>,
    pub lcr: Vec<i64>,
    pub lambda_1: Vec<f64>,
    pub lambda_2: Vec<f64>,
    pub lambda_3: Vec<f64>,
    pub lambda_4: Vec<f64>,
    pub lambda_5: Vec<f64>,
    pub lambda_6: Vec<f64>,
    pub lambda_7: Vec<f64>,
    pub lambda_8: Vec<f64>,
    pub lambda_9: Vec<f64>,
    pub lambda_10: Vec<f64>,
    pub lambda_11: Vec<f64>,
    pub lambda_12: Vec<f64>,
    pub lambda_13: Vec<f64>,
    pub nu_1: Vec<f64>,
    pub nu_2: Vec<f64>,
    pub nu_3: Vec<f64>,
    pub nu_4: Vec<f64>,
    pub nu_5: Vec<f64>,
    pub nu_6: Vec<f64>,
    pub nu_7: Vec<f64>,
    pub nu_8: Vec<f64>,
}
impl MAT_072 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_072").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a0y: t.take("A0Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1y: t.take("A1Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a2y: t.take("A2Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            a2f: t.take("A2F").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            per: t.take("PER").and_then(|c| c.into_float()).unwrap_or_default(),
            er: t.take("ER").and_then(|c| c.into_float()).unwrap_or_default(),
            prr: t.take("PRR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
            lambda_1: t.take("LAMBDA-1").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_2: t.take("LAMBDA-2").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_3: t.take("LAMBDA-3").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_4: t.take("LAMBDA-4").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_5: t.take("LAMBDA-5").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_6: t.take("LAMBDA-6").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_7: t.take("LAMBDA-7").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_8: t.take("LAMBDA-8").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_9: t.take("LAMBDA-9").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_10: t.take("LAMBDA-10").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_11: t.take("LAMBDA-11").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_12: t.take("LAMBDA-12").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_13: t.take("LAMBDA-13").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_1: t.take("NU-1").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_2: t.take("NU-2").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_3: t.take("NU-3").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_4: t.take("NU-4").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_5: t.take("NU-5").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_6: t.take("NU-6").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_7: t.take("NU-7").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_8: t.take("NU-8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_072R3 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub ft: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub b1: Vec<f64>,
    pub omega: Vec<f64>,
    pub a1f: Vec<f64>,
    pub slambda: Vec<f64>,
    pub nout: Vec<f64>,
    pub edrop: Vec<f64>,
    pub rsize: Vec<f64>,
    pub ucf: Vec<f64>,
    pub lcrate: Vec<i64>,
    pub locwidth: Vec<f64>,
    pub npts: Vec<f64>,
    pub lambda1: Vec<f64>,
    pub lambda2: Vec<f64>,
    pub lambda3: Vec<f64>,
    pub lambda4: Vec<f64>,
    pub lambda5: Vec<f64>,
    pub lambda6: Vec<f64>,
    pub lambda7: Vec<f64>,
    pub lambda8: Vec<f64>,
    pub lambda09: Vec<f64>,
    pub lambda10: Vec<f64>,
    pub lambda11: Vec<f64>,
    pub lambda12: Vec<f64>,
    pub lambda13: Vec<f64>,
    pub b3: Vec<f64>,
    pub a0y: Vec<f64>,
    pub a1y: Vec<f64>,
    pub eta1: Vec<f64>,
    pub eta2: Vec<f64>,
    pub eta3: Vec<f64>,
    pub eta4: Vec<f64>,
    pub eta5: Vec<f64>,
    pub eta6: Vec<f64>,
    pub eta7: Vec<f64>,
    pub eta8: Vec<f64>,
    pub eta09: Vec<f64>,
    pub eta10: Vec<f64>,
    pub eta11: Vec<f64>,
    pub eta12: Vec<f64>,
    pub eta13: Vec<f64>,
    pub b2: Vec<f64>,
    pub a2f: Vec<f64>,
    pub a2y: Vec<f64>,
}
impl MAT_072R3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_072R3").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            slambda: t.take("Slambda").and_then(|c| c.into_float()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            edrop: t.take("EDROP").and_then(|c| c.into_float()).unwrap_or_default(),
            rsize: t.take("RSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            ucf: t.take("UCF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrate: t.take("LCRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            locwidth: t.take("LOCWIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            npts: t.take("NPTS").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda1: t.take("lambda1").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda2: t.take("lambda2").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda3: t.take("lambda3").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda4: t.take("lambda4").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda5: t.take("lambda5").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda6: t.take("lambda6").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda7: t.take("lambda7").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda8: t.take("lambda8").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda09: t.take("lambda09").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda10: t.take("lambda10").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda11: t.take("lambda11").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda12: t.take("lambda12").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda13: t.take("lambda13").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            a0y: t.take("A0Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1y: t.take("A1Y").and_then(|c| c.into_float()).unwrap_or_default(),
            eta1: t.take("eta1").and_then(|c| c.into_float()).unwrap_or_default(),
            eta2: t.take("eta2").and_then(|c| c.into_float()).unwrap_or_default(),
            eta3: t.take("eta3").and_then(|c| c.into_float()).unwrap_or_default(),
            eta4: t.take("eta4").and_then(|c| c.into_float()).unwrap_or_default(),
            eta5: t.take("eta5").and_then(|c| c.into_float()).unwrap_or_default(),
            eta6: t.take("eta6").and_then(|c| c.into_float()).unwrap_or_default(),
            eta7: t.take("eta7").and_then(|c| c.into_float()).unwrap_or_default(),
            eta8: t.take("eta8").and_then(|c| c.into_float()).unwrap_or_default(),
            eta09: t.take("eta09").and_then(|c| c.into_float()).unwrap_or_default(),
            eta10: t.take("eta10").and_then(|c| c.into_float()).unwrap_or_default(),
            eta11: t.take("eta11").and_then(|c| c.into_float()).unwrap_or_default(),
            eta12: t.take("eta12").and_then(|c| c.into_float()).unwrap_or_default(),
            eta13: t.take("eta13").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            a2f: t.take("A2F").and_then(|c| c.into_float()).unwrap_or_default(),
            a2y: t.take("A2Y").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_073 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tc: Vec<f64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub kcon: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nv: Vec<i64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_073 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_073").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_074 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub f0: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_074 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_074").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_075 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub lcpy: Vec<i64>,
    pub lcuys: Vec<i64>,
    pub vc: Vec<f64>,
    pub pc: Vec<f64>,
    pub vpc: Vec<f64>,
    pub tsc: Vec<f64>,
    pub vtsc: Vec<f64>,
    pub lcrate: Vec<i64>,
    pub pr: Vec<f64>,
    pub kcon: Vec<f64>,
    pub isflg: Vec<i64>,
    pub ncycle: Vec<f64>,
}
impl MAT_075 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_075").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpy: t.take("LCPY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuys: t.take("LCUYS").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vpc: t.take("VPC").and_then(|c| c.into_float()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            vtsc: t.take("VTSC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrate: t.take("LCRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            isflg: t.take("ISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_076 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub mo: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub mst: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_076 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_076").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("MO").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            mst: t.take("MST").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_077_H {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub nv: Vec<i64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tbhys: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub data: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub c10: Vec<f64>,
    pub c01: Vec<f64>,
    pub c11: Vec<f64>,
    pub c20: Vec<f64>,
    pub c02: Vec<f64>,
    pub c30: Vec<f64>,
    pub therml: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub gj: Vec<f64>,
    pub sigfj: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_077_H {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_077_H").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tbhys: t.take("TBHYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c20: t.take("C20").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            c30: t.take("C30").and_then(|c| c.into_float()).unwrap_or_default(),
            therml: t.take("THERML").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            gj: t.take("GJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigfj: t.take("SIGFJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_077_O {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub nv: Vec<i64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tbhys: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub data: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub mu3: Vec<f64>,
    pub mu4: Vec<f64>,
    pub mu5: Vec<f64>,
    pub mu6: Vec<f64>,
    pub mu7: Vec<f64>,
    pub mu8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_077_O {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_077_O").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tbhys: t.take("TBHYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            mu3: t.take("MU3").and_then(|c| c.into_float()).unwrap_or_default(),
            mu4: t.take("MU4").and_then(|c| c.into_float()).unwrap_or_default(),
            mu5: t.take("MU5").and_then(|c| c.into_float()).unwrap_or_default(),
            mu6: t.take("MU6").and_then(|c| c.into_float()).unwrap_or_default(),
            mu7: t.take("MU7").and_then(|c| c.into_float()).unwrap_or_default(),
            mu8: t.take("MU8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_078 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub lcpv: Vec<i64>,
    pub lcyp: Vec<i64>,
    pub lcfp: Vec<i64>,
    pub lcrp: Vec<i64>,
    pub pc: Vec<f64>,
    pub out: Vec<f64>,
    pub b: Vec<f64>,
    pub fail: Vec<f64>,
}
impl MAT_078 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_078").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpv: t.take("LCPV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyp: t.take("LCYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfp: t.take("LCFP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrp: t.take("LCRP").and_then(|c| c.into_int()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            out: t.take("OUT").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_079 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k0: Vec<f64>,
    pub p0: Vec<f64>,
    pub b: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub df: Vec<f64>,
    pub rp: Vec<f64>,
    pub lcid: Vec<i64>,
    pub sflc: Vec<f64>,
    pub dil_a: Vec<f64>,
    pub dil_b: Vec<f64>,
    pub dil_c: Vec<f64>,
    pub dil_d: Vec<f64>,
    pub gam1: Vec<f64>,
    pub gam2: Vec<f64>,
    pub gam3: Vec<f64>,
    pub gam4: Vec<f64>,
    pub gam5: Vec<f64>,
    pub lcd: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub pinit: Vec<i64>,
    pub tau1: Vec<f64>,
    pub tau2: Vec<f64>,
    pub tau3: Vec<f64>,
    pub tau4: Vec<f64>,
    pub tau5: Vec<f64>,
    pub flag5: Vec<i64>,
    pub sigth: Vec<f64>,
    pub sigr: Vec<f64>,
    pub chi: Vec<f64>,
}
impl MAT_079 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_079").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sflc: t.take("SFLC").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_a: t.take("DIL_A").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_b: t.take("DIL_B").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_c: t.take("DIL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_d: t.take("DIL_D").and_then(|c| c.into_float()).unwrap_or_default(),
            gam1: t.take("GAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            gam2: t.take("GAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            gam3: t.take("GAM3").and_then(|c| c.into_float()).unwrap_or_default(),
            gam4: t.take("GAM4").and_then(|c| c.into_float()).unwrap_or_default(),
            gam5: t.take("GAM5").and_then(|c| c.into_float()).unwrap_or_default(),
            lcd: t.take("LCD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            pinit: t.take("PINIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            tau3: t.take("TAU3").and_then(|c| c.into_float()).unwrap_or_default(),
            tau4: t.take("TAU4").and_then(|c| c.into_float()).unwrap_or_default(),
            tau5: t.take("TAU5").and_then(|c| c.into_float()).unwrap_or_default(),
            flag5: t.take("FLAG5").and_then(|c| c.into_int()).unwrap_or_default(),
            sigth: t.take("SIGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            sigr: t.take("SIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            chi: t.take("CHI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_080 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gamy: Vec<f64>,
    pub tauy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub r: Vec<f64>,
    pub bulk: Vec<f64>,
}
impl MAT_080 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_080").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamy: t.take("GAMY").and_then(|c| c.into_float()).unwrap_or_default(),
            tauy: t.take("TAUY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_081 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_081 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_081").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_082 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub d0: Vec<f64>,
    pub b: Vec<f64>,
    pub lambda: Vec<f64>,
    pub ds: Vec<f64>,
    pub l: Vec<f64>,
}
impl MAT_082 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_082").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_083 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub d0: Vec<f64>,
    pub n0: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub aij: Vec<f64>,
    pub sij: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_083 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_083").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            n0: t.take("N0").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            aij: t.take("AIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sij: t.take("SIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_083_1 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub betat: Vec<f64>,
    pub betac: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_083_1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_083_1").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            betat: t.take("BETAT").and_then(|c| c.into_float()).unwrap_or_default(),
            betac: t.take("BETAC").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_083_2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub d0: Vec<f64>,
    pub n0: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub aij: Vec<f64>,
    pub sij: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_083_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_083_2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            n0: t.take("N0").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            aij: t.take("AIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sij: t.take("SIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_084 {
    pub eid1_unused: Vec<i64>,
    pub eid2_pid: Vec<i64>,
    pub inc_axis: Vec<i64>,
    pub xr_coor: Vec<f64>,
    pub yr_rqa: Vec<f64>,
    pub zr_rqb: Vec<f64>,
}
impl MAT_084 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_084").unwrap());
        Self {
            eid1_unused: t.take("EID1/UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2_pid: t.take("EID2/PID").and_then(|c| c.into_int()).unwrap_or_default(),
            inc_axis: t.take("INC/AXIS").and_then(|c| c.into_int()).unwrap_or_default(),
            xr_coor: t.take("XR/COOR").and_then(|c| c.into_float()).unwrap_or_default(),
            yr_rqa: t.take("YR/RQA").and_then(|c| c.into_float()).unwrap_or_default(),
            zr_rqb: t.take("ZR/RQB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_084_085 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tm: Vec<f64>,
    pub pr: Vec<f64>,
    pub ucs: Vec<f64>,
    pub uts: Vec<f64>,
    pub fe: Vec<f64>,
    pub asize: Vec<f64>,
    pub e: Vec<f64>,
    pub ys: Vec<f64>,
    pub eh: Vec<f64>,
    pub uelong: Vec<f64>,
    pub rate: Vec<f64>,
    pub conm: Vec<f64>,
    pub conl: Vec<f64>,
    pub cont: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl MAT_084_085 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_084/085").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ucs: t.take("UCS").and_then(|c| c.into_float()).unwrap_or_default(),
            uts: t.take("UTS").and_then(|c| c.into_float()).unwrap_or_default(),
            fe: t.take("FE").and_then(|c| c.into_float()).unwrap_or_default(),
            asize: t.take("ASIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            uelong: t.take("UELONG").and_then(|c| c.into_float()).unwrap_or_default(),
            rate: t.take("RATE").and_then(|c| c.into_float()).unwrap_or_default(),
            conm: t.take("CONM").and_then(|c| c.into_float()).unwrap_or_default(),
            conl: t.take("CONL").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_086 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub vf: Vec<f64>,
    pub k: Vec<f64>,
    pub g0: Vec<f64>,
    pub ginf: Vec<f64>,
    pub beta: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub mangle: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_086 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_086").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            ginf: t.take("GINF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_087 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
    pub c10: Vec<f64>,
    pub c01: Vec<f64>,
    pub c11: Vec<f64>,
    pub c20: Vec<f64>,
    pub c02: Vec<f64>,
    pub p0: Vec<f64>,
    pub phi: Vec<f64>,
    pub ivs: Vec<f64>,
    pub g: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_087 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_087").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c20: t.take("C20").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            ivs: t.take("IVS").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_088 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub siga: Vec<f64>,
    pub sigi: Vec<f64>,
    pub sigs: Vec<f64>,
    pub sig0: Vec<f64>,
    pub bulk: Vec<f64>,
    pub hf0: Vec<f64>,
    pub hf1: Vec<f64>,
    pub hf2: Vec<f64>,
    pub sigs0: Vec<f64>,
    pub edots0: Vec<f64>,
    pub burg: Vec<f64>,
    pub capa: Vec<f64>,
    pub boltz: Vec<f64>,
    pub sm0: Vec<f64>,
    pub sm1: Vec<f64>,
    pub sm2: Vec<f64>,
    pub edot0: Vec<f64>,
    pub go: Vec<f64>,
    pub pinv: Vec<f64>,
    pub qinv: Vec<f64>,
    pub edoti: Vec<f64>,
    pub g0i: Vec<f64>,
    pub pinvi: Vec<f64>,
    pub qinvi: Vec<f64>,
    pub edots: Vec<f64>,
    pub g0s: Vec<f64>,
    pub pinvs: Vec<f64>,
    pub qinvs: Vec<f64>,
    pub rhocpr: Vec<f64>,
    pub temprf: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
}
impl MAT_088 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_088").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            siga: t.take("SIGA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigi: t.take("SIGI").and_then(|c| c.into_float()).unwrap_or_default(),
            sigs: t.take("SIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            hf0: t.take("HF0").and_then(|c| c.into_float()).unwrap_or_default(),
            hf1: t.take("HF1").and_then(|c| c.into_float()).unwrap_or_default(),
            hf2: t.take("HF2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigs0: t.take("SIGS0").and_then(|c| c.into_float()).unwrap_or_default(),
            edots0: t.take("EDOTS0").and_then(|c| c.into_float()).unwrap_or_default(),
            burg: t.take("BURG").and_then(|c| c.into_float()).unwrap_or_default(),
            capa: t.take("CAPA").and_then(|c| c.into_float()).unwrap_or_default(),
            boltz: t.take("BOLTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sm0: t.take("SM0").and_then(|c| c.into_float()).unwrap_or_default(),
            sm1: t.take("SM1").and_then(|c| c.into_float()).unwrap_or_default(),
            sm2: t.take("SM2").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            go: t.take("GO").and_then(|c| c.into_float()).unwrap_or_default(),
            pinv: t.take("PINV").and_then(|c| c.into_float()).unwrap_or_default(),
            qinv: t.take("QINV").and_then(|c| c.into_float()).unwrap_or_default(),
            edoti: t.take("EDOTI").and_then(|c| c.into_float()).unwrap_or_default(),
            g0i: t.take("G0I").and_then(|c| c.into_float()).unwrap_or_default(),
            pinvi: t.take("PINVI").and_then(|c| c.into_float()).unwrap_or_default(),
            qinvi: t.take("QINVI").and_then(|c| c.into_float()).unwrap_or_default(),
            edots: t.take("EDOTS").and_then(|c| c.into_float()).unwrap_or_default(),
            g0s: t.take("G0S").and_then(|c| c.into_float()).unwrap_or_default(),
            pinvs: t.take("PINVS").and_then(|c| c.into_float()).unwrap_or_default(),
            qinvs: t.take("QINVS").and_then(|c| c.into_float()).unwrap_or_default(),
            rhocpr: t.take("RHOCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            temprf: t.take("TEMPRF").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_089 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eftx: Vec<f64>,
    pub damp: Vec<f64>,
    pub ratefac: Vec<f64>,
    pub lcfail: Vec<i64>,
    pub numint: Vec<f64>,
}
impl MAT_089 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_089").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eftx: t.take("EFTX").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            ratefac: t.take("RATEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_090 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub beta: Vec<f64>,
    pub cf: Vec<f64>,
    pub atmos: Vec<f64>,
    pub grav: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub xn: Vec<f64>,
    pub yn: Vec<f64>,
    pub zn: Vec<f64>,
}
impl MAT_090 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_090").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmos: t.take("ATMOS").and_then(|c| c.into_float()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            xn: t.take("XN").and_then(|c| c.into_float()).unwrap_or_default(),
            yn: t.take("YN").and_then(|c| c.into_float()).unwrap_or_default(),
            zn: t.take("ZN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_090_COMPLEX {
    pub mid: Vec<i64>,
    pub rhor: Vec<f64>,
    pub bulkr: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub bulki: Vec<f64>,
    pub lcidrr: Vec<i64>,
    pub lcidkr: Vec<i64>,
    pub lcidri: Vec<i64>,
    pub lcidki: Vec<i64>,
}
impl MAT_090_COMPLEX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_090_COMPLEX").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rhor: t.take("RHOR").and_then(|c| c.into_float()).unwrap_or_default(),
            bulkr: t.take("BULKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOI").and_then(|c| c.into_float()).unwrap_or_default(),
            bulki: t.take("BULKI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidkr: t.take("LCIDKR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidri: t.take("LCIDRI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidki: t.take("LCIDKI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_090_DAMP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub cee: Vec<f64>,
    pub beta: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_6: Vec<i64>,
    pub vdc: Vec<f64>,
    pub beta2: Vec<f64>,
}
impl MAT_090_DAMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_090_DAMP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            cee: t.take("CEE").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_int()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_090_POROUS_DB {
    pub mid: Vec<i64>,
    pub rho0: Vec<f64>,
    pub cee0: Vec<f64>,
    pub sigma: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_090_POROUS_DB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_090_POROUS_DB").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho0: t.take("RHO0").and_then(|c| c.into_float()).unwrap_or_default(),
            cee0: t.take("CEE0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_091 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub xk: Vec<f64>,
    pub xlam: Vec<f64>,
    pub fang: Vec<f64>,
    pub xlam0: Vec<f64>,
    pub failsf: Vec<f64>,
    pub failsm: Vec<f64>,
    pub failshr: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub la1: Vec<f64>,
    pub la2: Vec<f64>,
    pub la3: Vec<f64>,
    pub macf: Vec<i64>,
}
impl MAT_091 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_091").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            xk: t.take("XK").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam: t.take("XLAM").and_then(|c| c.into_float()).unwrap_or_default(),
            fang: t.take("FANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam0: t.take("XLAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            failsf: t.take("FAILSF").and_then(|c| c.into_float()).unwrap_or_default(),
            failsm: t.take("FAILSM").and_then(|c| c.into_float()).unwrap_or_default(),
            failshr: t.take("FAILSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            la1: t.take("LA1").and_then(|c| c.into_float()).unwrap_or_default(),
            la2: t.take("LA2").and_then(|c| c.into_float()).unwrap_or_default(),
            la3: t.take("LA3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_092 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub xk: Vec<f64>,
    pub xlam: Vec<f64>,
    pub fang: Vec<f64>,
    pub xlam0: Vec<f64>,
    pub failsf: Vec<f64>,
    pub failsm: Vec<f64>,
    pub failshr: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub la1: Vec<f64>,
    pub la2: Vec<f64>,
    pub la3: Vec<f64>,
    pub macf: Vec<i64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub s4: Vec<f64>,
    pub s5: Vec<f64>,
    pub s6: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
}
impl MAT_092 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_092").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            xk: t.take("XK").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam: t.take("XLAM").and_then(|c| c.into_float()).unwrap_or_default(),
            fang: t.take("FANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam0: t.take("XLAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            failsf: t.take("FAILSF").and_then(|c| c.into_float()).unwrap_or_default(),
            failsm: t.take("FAILSM").and_then(|c| c.into_float()).unwrap_or_default(),
            failshr: t.take("FAILSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            la1: t.take("LA1").and_then(|c| c.into_float()).unwrap_or_default(),
            la2: t.take("LA2").and_then(|c| c.into_float()).unwrap_or_default(),
            la3: t.take("LA3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_float()).unwrap_or_default(),
            s5: t.take("S5").and_then(|c| c.into_float()).unwrap_or_default(),
            s6: t.take("S6").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_093 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tpidr: Vec<i64>,
    pub tpids: Vec<i64>,
    pub tpidt: Vec<i64>,
    pub rpidr: Vec<i64>,
    pub rpids: Vec<i64>,
    pub rpidt: Vec<i64>,
}
impl MAT_093 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_093").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tpidr: t.take("TPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tpids: t.take("TPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            tpidt: t.take("TPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidr: t.take("RPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            rpids: t.take("RPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidt: t.take("RPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_094 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub f0: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_094 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_094").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_095 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tpidr: Vec<i64>,
    pub tpids: Vec<i64>,
    pub tpidt: Vec<i64>,
    pub rpidr: Vec<i64>,
    pub rpids: Vec<i64>,
    pub rpidt: Vec<i64>,
}
impl MAT_095 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_095").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tpidr: t.take("TPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tpids: t.take("TPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            tpidt: t.take("TPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidr: t.take("RPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            rpids: t.take("RPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidt: t.take("RPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_096 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tlimit: Vec<f64>,
    pub slimit: Vec<f64>,
    pub ftough: Vec<f64>,
    pub sreten: Vec<f64>,
    pub visc: Vec<f64>,
    pub fra_rf: Vec<f64>,
    pub e_rf: Vec<f64>,
    pub ys_rf: Vec<f64>,
    pub eh_rf: Vec<f64>,
    pub fs_rf: Vec<f64>,
    pub sigy: Vec<f64>,
}
impl MAT_096 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_096").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tlimit: t.take("TLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            slimit: t.take("SLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ftough: t.take("FTOUGH").and_then(|c| c.into_float()).unwrap_or_default(),
            sreten: t.take("SRETEN").and_then(|c| c.into_float()).unwrap_or_default(),
            visc: t.take("VISC").and_then(|c| c.into_float()).unwrap_or_default(),
            fra_rf: t.take("FRA_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            e_rf: t.take("E_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            ys_rf: t.take("YS_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            eh_rf: t.take("EH_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            fs_rf: t.take("FS_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_097 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tr: Vec<i64>,
    pub ts: Vec<i64>,
    pub tt: Vec<i64>,
    pub rr: Vec<i64>,
    pub rs: Vec<i64>,
    pub rt: Vec<i64>,
    pub rpst: Vec<f64>,
    pub rpsr: Vec<f64>,
}
impl MAT_097 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_097").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_int()).unwrap_or_default(),
            tt: t.take("TT").and_then(|c| c.into_int()).unwrap_or_default(),
            rr: t.take("RR").and_then(|c| c.into_int()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_int()).unwrap_or_default(),
            rt: t.take("RT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpst: t.take("RPST").and_then(|c| c.into_float()).unwrap_or_default(),
            rpsr: t.take("RPSR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_098 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub psfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub sigsat: Vec<f64>,
    pub epso: Vec<f64>,
}
impl MAT_098 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_098").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigsat: t.take("SIGSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_099 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub eppfr: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub psfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub sigsat: Vec<f64>,
    pub epso: Vec<f64>,
}
impl MAT_099 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_099").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigsat: t.take("SIGSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_100 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
}
impl MAT_100 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_100").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_100_DA {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub unused_3_2: Vec<f64>,
    pub unused_4: Vec<f64>,
    pub unused_5: Vec<f64>,
    pub unused_6: Vec<f64>,
    pub nf: Vec<f64>,
    pub rs: Vec<f64>,
    pub asff: Vec<i64>,
    pub f: Vec<f64>,
    pub true_t: Vec<f64>,
    pub con_id: Vec<i64>,
    pub rfiltf: Vec<f64>,
    pub jtol: Vec<f64>,
}
impl MAT_100_DA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_100_DA").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            asff: t.take("ASFF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("_").and_then(|c| c.into_float()).unwrap_or_default(),
            true_t: t.take("TRUE_T").and_then(|c| c.into_float()).unwrap_or_default(),
            con_id: t.take("CON_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
            jtol: t.take("JTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_100_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
    pub efail_2: Vec<f64>,
    pub sigax: Vec<f64>,
    pub sigtu: Vec<f64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub f60: Vec<i64>,
    pub nf_2: Vec<f64>,
}
impl MAT_100_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_100_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            efail_2: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigax: t.take("SIGAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigtu: t.take("SIGTU").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            nf_2: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_101 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ratesf: Vec<f64>,
    pub edot0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcfeps: Vec<i64>,
    pub lcfsig: Vec<i64>,
    pub lce: Vec<i64>,
}
impl MAT_101 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_101").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ratesf: t.take("RATESF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfeps: t.take("LCFEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsig: t.take("LCFSIG").and_then(|c| c.into_int()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_102 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub vp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub n: Vec<f64>,
    pub a: Vec<f64>,
    pub q: Vec<f64>,
    pub g: Vec<f64>,
    pub epso: Vec<f64>,
    pub lcq: Vec<f64>,
}
impl MAT_102 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_102").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcq: t.take("LCQ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_102_T {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub alpha: Vec<f64>,
    pub n: Vec<f64>,
    pub a: Vec<f64>,
    pub q: Vec<f64>,
    pub g: Vec<f64>,
    pub epso: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lccte: Vec<f64>,
}
impl MAT_102_T {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_102_T").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lccte: t.take("LCCTE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_103 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub flag: Vec<i64>,
    pub lcss: Vec<i64>,
    pub alpha: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub vk: Vec<f64>,
    pub vm: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub aopt: Vec<f64>,
    pub fail: Vec<f64>,
    pub numint: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_103 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_103").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            vk: t.take("VK").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_103_P {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub s11: Vec<f64>,
    pub s22: Vec<f64>,
    pub s33: Vec<f64>,
    pub s12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_103_P {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_103_P").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            s11: t.take("S11").and_then(|c| c.into_float()).unwrap_or_default(),
            s22: t.take("S22").and_then(|c| c.into_float()).unwrap_or_default(),
            s33: t.take("S33").and_then(|c| c.into_float()).unwrap_or_default(),
            s12: t.take("S12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_104 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcds: Vec<i64>,
    pub q1: Vec<f64>,
    pub c1: Vec<f64>,
    pub q2: Vec<f64>,
    pub c2: Vec<f64>,
    pub epsd: Vec<f64>,
    pub espr_s: Vec<f64>,
    pub dc: Vec<f64>,
    pub flag: Vec<i64>,
    pub vk: Vec<f64>,
    pub vm: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<i64>,
    pub f20: Vec<i64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_104 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_104").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcds: t.take("LCDS").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            espr_s: t.take("ESPR/S").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vk: t.take("VK").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_105 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub epsd: Vec<f64>,
    pub s: Vec<f64>,
    pub dc: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_105 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_105").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_106 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub fail: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lcsigy: Vec<f64>,
    pub lcr: Vec<f64>,
    pub lcx: Vec<f64>,
    pub lcalph: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lcp: Vec<i64>,
    pub tref: Vec<f64>,
    pub lcfail: Vec<f64>,
    pub nuhis: Vec<i64>,
    pub t1phas: Vec<f64>,
    pub t2phas: Vec<f64>,
    pub tol: Vec<f64>,
    pub fushi1: Vec<i64>,
    pub fushi2: Vec<i64>,
    pub fushi3: Vec<i64>,
    pub fushi4: Vec<i64>,
    pub fushi5: Vec<i64>,
    pub fushi6: Vec<i64>,
    pub fushi7: Vec<i64>,
    pub fushi8: Vec<i64>,
}
impl MAT_106 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_106").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalph: t.take("LCALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nuhis: t.take("NUHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            t1phas: t.take("T1PHAS").and_then(|c| c.into_float()).unwrap_or_default(),
            t2phas: t.take("T2PHAS").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            fushi1: t.take("FUSHI1").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi2: t.take("FUSHI2").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi3: t.take("FUSHI3").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi4: t.take("FUSHI4").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi5: t.take("FUSHI5").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi6: t.take("FUSHI6").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi7: t.take("FUSHI7").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi8: t.take("FUSHI8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_107 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsi: Vec<f64>,
    pub cp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub e0dot: Vec<f64>,
    pub tr: Vec<f64>,
    pub tm: Vec<f64>,
    pub t0: Vec<f64>,
    pub flag1: Vec<f64>,
    pub flag2: Vec<f64>,
    pub a_siga: Vec<f64>,
    pub b_b: Vec<f64>,
    pub n_beta0: Vec<f64>,
    pub c_beta1: Vec<f64>,
    pub m_na: Vec<f64>,
}
impl MAT_107 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_107").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsi: t.take("XSI").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0dot: t.take("E0DOT").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("Tr").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("Tm").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            flag1: t.take("FLAG1").and_then(|c| c.into_float()).unwrap_or_default(),
            flag2: t.take("FLAG2").and_then(|c| c.into_float()).unwrap_or_default(),
            a_siga: t.take("A/SIGA").and_then(|c| c.into_float()).unwrap_or_default(),
            b_b: t.take("B/B").and_then(|c| c.into_float()).unwrap_or_default(),
            n_beta0: t.take("N/BETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            c_beta1: t.take("C/BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_na: t.take("m/NA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_108 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11: Vec<f64>,
    pub e22: Vec<f64>,
    pub g12: Vec<f64>,
    pub pr12: Vec<f64>,
    pub pr23: Vec<f64>,
    pub pr31: Vec<f64>,
    pub sigma0: Vec<f64>,
    pub lc: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub r11: Vec<f64>,
    pub r22: Vec<f64>,
    pub r33: Vec<f64>,
    pub r12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_108 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_108").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11: t.take("E11").and_then(|c| c.into_float()).unwrap_or_default(),
            e22: t.take("E22").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            pr12: t.take("PR12").and_then(|c| c.into_float()).unwrap_or_default(),
            pr23: t.take("PR23").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma0: t.take("SIGMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_110 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub epsi: Vec<f64>,
    pub t: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub hel: Vec<f64>,
    pub phel: Vec<f64>,
    pub beta: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_110 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_110").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            epsi: t.take("EPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hel: t.take("HEL").and_then(|c| c.into_float()).unwrap_or_default(),
            phel: t.take("PHEL").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_111 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub fc: Vec<f64>,
    pub t: Vec<f64>,
    pub eps0: Vec<f64>,
    pub efmin: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub pc: Vec<f64>,
    pub uc: Vec<f64>,
    pub pl: Vec<f64>,
    pub ul: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_111 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_111").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            efmin: t.take("EFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            uc: t.take("UC").and_then(|c| c.into_float()).unwrap_or_default(),
            pl: t.take("PL").and_then(|c| c.into_float()).unwrap_or_default(),
            ul: t.take("UL").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_112 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_112 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_112").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_113 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub t0: Vec<f64>,
    pub tref: Vec<f64>,
    pub ta0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub p: Vec<f64>,
    pub q: Vec<f64>,
    pub e0mart: Vec<f64>,
    pub vm0: Vec<f64>,
    pub ahs: Vec<f64>,
    pub bhs: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub eps0: Vec<f64>,
    pub hmart: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
}
impl MAT_113 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_113").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            ta0: t.take("TA0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            e0mart: t.take("E0MART").and_then(|c| c.into_float()).unwrap_or_default(),
            vm0: t.take("VM0").and_then(|c| c.into_float()).unwrap_or_default(),
            ahs: t.take("AHS").and_then(|c| c.into_float()).unwrap_or_default(),
            bhs: t.take("BHS").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            hmart: t.take("HMART").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_114 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_114 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_114").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_115 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub a: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_115 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_115").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_115_O {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr21: Vec<f64>,
    pub pr31: Vec<f64>,
    pub pr32: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub g13: Vec<f64>,
    pub a: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_115_O {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_115_o").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr21: t.take("PR21").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            pr32: t.take("PR32").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            g13: t.take("G13").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_116 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_116 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_116").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_117 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_117 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_117").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_118 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
}
impl MAT_118 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_118").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_119 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub kt: Vec<f64>,
    pub kr: Vec<f64>,
    pub iunld: Vec<i64>,
    pub offset: Vec<f64>,
    pub dampf: Vec<f64>,
    pub iflag: Vec<i64>,
    pub lcidtr: Vec<i64>,
    pub lcidts: Vec<i64>,
    pub lcidtt: Vec<i64>,
    pub lcidrr: Vec<i64>,
    pub lcidrs: Vec<i64>,
    pub lcidrt: Vec<i64>,
    pub lcidtur: Vec<i64>,
    pub lcidtus: Vec<i64>,
    pub lcidtut: Vec<i64>,
    pub lcidrur: Vec<i64>,
    pub lcidrus: Vec<i64>,
    pub lcidrut: Vec<i64>,
    pub lcidtdr: Vec<i64>,
    pub lcidtds: Vec<i64>,
    pub lcidtdt: Vec<i64>,
    pub lcidrdr: Vec<i64>,
    pub lcidrds: Vec<i64>,
    pub lcidrdt: Vec<i64>,
    pub lcidter: Vec<i64>,
    pub lcidtes: Vec<i64>,
    pub lcidtet: Vec<i64>,
    pub lcidrer: Vec<i64>,
    pub lcidres: Vec<i64>,
    pub lcidret: Vec<i64>,
    pub utfailr: Vec<f64>,
    pub utfails: Vec<f64>,
    pub utfailt: Vec<f64>,
    pub wtfailr: Vec<f64>,
    pub wtfails: Vec<f64>,
    pub wtfailt: Vec<f64>,
    pub fcrit: Vec<f64>,
    pub ucfailr: Vec<f64>,
    pub ucfails: Vec<f64>,
    pub ucfailt: Vec<f64>,
    pub wcfailr: Vec<f64>,
    pub wcfails: Vec<f64>,
    pub wcfailt: Vec<f64>,
    pub iur: Vec<f64>,
    pub ius: Vec<f64>,
    pub iut: Vec<f64>,
    pub iwr: Vec<f64>,
    pub iws: Vec<f64>,
    pub iwt: Vec<f64>,
}
impl MAT_119 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_119").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            kt: t.take("KT").and_then(|c| c.into_float()).unwrap_or_default(),
            kr: t.take("KR").and_then(|c| c.into_float()).unwrap_or_default(),
            iunld: t.take("IUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtr: t.take("LCIDTR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidts: t.take("LCIDTS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtt: t.take("LCIDTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrs: t.take("LCIDRS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrt: t.take("LCIDRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtur: t.take("LCIDTUR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtus: t.take("LCIDTUS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtut: t.take("LCIDTUT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrur: t.take("LCIDRUR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrus: t.take("LCIDRUS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrut: t.take("LCIDRUT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdr: t.take("LCIDTDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtds: t.take("LCIDTDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdt: t.take("LCIDTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdr: t.take("LCIDRDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrds: t.take("LCIDRDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdt: t.take("LCIDRDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidter: t.take("LCIDTER").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtes: t.take("LCIDTES").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtet: t.take("LCIDTET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrer: t.take("LCIDRER").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidres: t.take("LCIDRES").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidret: t.take("LCIDRET").and_then(|c| c.into_int()).unwrap_or_default(),
            utfailr: t.take("UTFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            utfails: t.take("UTFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            utfailt: t.take("UTFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfailr: t.take("WTFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfails: t.take("WTFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfailt: t.take("WTFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            fcrit: t.take("FCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfailr: t.take("UCFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfails: t.take("UCFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfailt: t.take("UCFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfailr: t.take("WCFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfails: t.take("WCFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfailt: t.take("WCFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            iur: t.take("IUR").and_then(|c| c.into_float()).unwrap_or_default(),
            ius: t.take("IUS").and_then(|c| c.into_float()).unwrap_or_default(),
            iut: t.take("IUT").and_then(|c| c.into_float()).unwrap_or_default(),
            iwr: t.take("IWR").and_then(|c| c.into_float()).unwrap_or_default(),
            iws: t.take("IWS").and_then(|c| c.into_float()).unwrap_or_default(),
            iwt: t.take("IWT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_120 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub l3: Vec<f64>,
    pub l4: Vec<f64>,
    pub ff1: Vec<f64>,
    pub ff2: Vec<f64>,
    pub ff3: Vec<f64>,
    pub ff4: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub lcf0: Vec<i64>,
    pub lcfc: Vec<i64>,
    pub lcfn: Vec<i64>,
    pub vgtyp: Vec<f64>,
}
impl MAT_120 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_120").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            l3: t.take("L3").and_then(|c| c.into_float()).unwrap_or_default(),
            l4: t.take("L4").and_then(|c| c.into_float()).unwrap_or_default(),
            ff1: t.take("FF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ff2: t.take("FF2").and_then(|c| c.into_float()).unwrap_or_default(),
            ff3: t.take("FF3").and_then(|c| c.into_float()).unwrap_or_default(),
            ff4: t.take("FF4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf0: t.take("LCF0").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfc: t.take("LCFC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfn: t.take("LCFN").and_then(|c| c.into_int()).unwrap_or_default(),
            vgtyp: t.take("VGTYP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_120_JC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub lcjc: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub lcf0: Vec<i64>,
    pub lcfc: Vec<i64>,
    pub lcfn: Vec<i64>,
    pub vgtyp: Vec<f64>,
    pub kw: Vec<i64>,
    pub beta: Vec<i64>,
    pub m: Vec<i64>,
}
impl MAT_120_JC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_120_JC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjc: t.take("LCJC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf0: t.take("LCF0").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfc: t.take("LCFC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfn: t.take("LCFN").and_then(|c| c.into_int()).unwrap_or_default(),
            vgtyp: t.take("VGTYP").and_then(|c| c.into_float()).unwrap_or_default(),
            kw: t.take("KW").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_int()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_120_RCDC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub l3: Vec<f64>,
    pub l4: Vec<f64>,
    pub ff1: Vec<f64>,
    pub ff2: Vec<f64>,
    pub ff3: Vec<f64>,
    pub ff4: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub d0: Vec<f64>,
    pub b: Vec<f64>,
    pub lambda: Vec<f64>,
    pub ds: Vec<f64>,
    pub l: Vec<f64>,
}
impl MAT_120_RCDC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_120_RCDC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            l3: t.take("L3").and_then(|c| c.into_float()).unwrap_or_default(),
            l4: t.take("L4").and_then(|c| c.into_float()).unwrap_or_default(),
            ff1: t.take("FF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ff2: t.take("FF2").and_then(|c| c.into_float()).unwrap_or_default(),
            ff3: t.take("FF3").and_then(|c| c.into_float()).unwrap_or_default(),
            ff4: t.take("FF4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_121 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub iunld: Vec<i64>,
    pub offset: Vec<f64>,
    pub dampf: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub lcidtu: Vec<i64>,
    pub lcidtd: Vec<i64>,
    pub lcidte: Vec<i64>,
    pub utfail: Vec<f64>,
    pub ucfail: Vec<f64>,
    pub iu: Vec<f64>,
}
impl MAT_121 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_121").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            iunld: t.take("IUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtu: t.take("LCIDTU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtd: t.take("LCIDTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidte: t.take("LCIDTE").and_then(|c| c.into_int()).unwrap_or_default(),
            utfail: t.take("UTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfail: t.take("UCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            iu: t.take("IU").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_122 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lc00: Vec<i64>,
    pub iconv: Vec<i64>,
    pub lc90: Vec<i64>,
    pub lc45: Vec<i64>,
    pub lcex: Vec<i64>,
    pub aopt: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_122 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_122").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lc00: t.take("LC00").and_then(|c| c.into_int()).unwrap_or_default(),
            iconv: t.take("ICONV").and_then(|c| c.into_int()).unwrap_or_default(),
            lc90: t.take("LC90").and_then(|c| c.into_int()).unwrap_or_default(),
            lc45: t.take("LC45").and_then(|c| c.into_int()).unwrap_or_default(),
            lcex: t.take("LCEX").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_122_2D {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ex: Vec<f64>,
    pub ey: Vec<f64>,
    pub ez: Vec<f64>,
    pub prxy: Vec<f64>,
    pub pryz: Vec<f64>,
    pub prxz: Vec<f64>,
    pub gxy: Vec<f64>,
    pub gyz: Vec<f64>,
    pub gxz: Vec<f64>,
    pub f: Vec<f64>,
    pub g: Vec<f64>,
    pub h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub hr: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_122_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_122_2D").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ex: t.take("EX").and_then(|c| c.into_float()).unwrap_or_default(),
            ey: t.take("EY").and_then(|c| c.into_float()).unwrap_or_default(),
            ez: t.take("EZ").and_then(|c| c.into_float()).unwrap_or_default(),
            prxy: t.take("PRXY").and_then(|c| c.into_float()).unwrap_or_default(),
            pryz: t.take("PRYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            prxz: t.take("PRXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gxy: t.take("GXY").and_then(|c| c.into_float()).unwrap_or_default(),
            gyz: t.take("GYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gxz: t.take("GXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_123 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_123 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_123").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_124 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcsrc: Vec<i64>,
    pub lcsrt: Vec<i64>,
    pub srflag: Vec<f64>,
    pub lcfail: Vec<i64>,
    pub ec: Vec<f64>,
    pub rpct: Vec<f64>,
    pub pc: Vec<f64>,
    pub pt: Vec<f64>,
    pub pcutc: Vec<f64>,
    pub pcutt: Vec<f64>,
    pub pcutf: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub srfilt: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_124 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_124").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrc: t.take("LCSRC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrt: t.take("LCSRT").and_then(|c| c.into_int()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            rpct: t.take("RPCT").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutc: t.take("PCUTC").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutt: t.take("PCUTT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutf: t.take("PCUTF").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            srfilt: t.take("SRFILT").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_125 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r: Vec<f64>,
    pub hclid: Vec<i64>,
    pub opt: Vec<i64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub sc2: Vec<f64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub ifld: Vec<i64>,
}
impl MAT_125 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_125").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hclid: t.take("HCLID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            sc2: t.take("SC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            ifld: t.take("IFLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_126 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vf: Vec<f64>,
    pub mu: Vec<f64>,
    pub bulk: Vec<f64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcc: Vec<i64>,
    pub lcs: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcbc: Vec<i64>,
    pub lcca: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eaau: Vec<f64>,
    pub ebbu: Vec<f64>,
    pub eccu: Vec<f64>,
    pub gabu: Vec<f64>,
    pub gbcu: Vec<f64>,
    pub gcau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub tsef: Vec<f64>,
    pub ssef: Vec<f64>,
    pub vref: Vec<f64>,
    pub tref: Vec<f64>,
    pub shdflg: Vec<f64>,
}
impl MAT_126 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_126").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcs: t.take("LCS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbc: t.take("LCBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcca: t.take("LCCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eaau: t.take("EAAU").and_then(|c| c.into_float()).unwrap_or_default(),
            ebbu: t.take("EBBU").and_then(|c| c.into_float()).unwrap_or_default(),
            eccu: t.take("ECCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gabu: t.take("GABU").and_then(|c| c.into_float()).unwrap_or_default(),
            gbcu: t.take("GBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gcau: t.take("GCAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            tsef: t.take("TSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            ssef: t.take("SSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            vref: t.take("VREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            shdflg: t.take("SHDFLG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_127 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub n: Vec<i64>,
    pub lcid: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nt: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_127 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_127").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_128 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub p: Vec<f64>,
    pub b: Vec<f64>,
    pub l0: Vec<f64>,
    pub ca0max: Vec<i64>,
    pub lr: Vec<f64>,
    pub mm: Vec<f64>,
    pub bb: Vec<f64>,
    pub ca0: Vec<f64>,
    pub tmax: Vec<f64>,
    pub tact: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_128 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_128").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca0max: t.take("CA0MAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lr: t.take("LR").and_then(|c| c.into_float()).unwrap_or_default(),
            mm: t.take("MM").and_then(|c| c.into_float()).unwrap_or_default(),
            bb: t.take("BB").and_then(|c| c.into_float()).unwrap_or_default(),
            ca0: t.take("CA0").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            tact: t.take("TACT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_129 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub delta: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub lcid: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nt: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_129 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_129").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            delta: t.take("DELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_130 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ys: Vec<f64>,
    pub ep: Vec<f64>,
    pub e11p: Vec<f64>,
    pub e22p: Vec<f64>,
    pub v12p: Vec<f64>,
    pub v21p: Vec<f64>,
    pub g12p: Vec<f64>,
    pub g23p: Vec<f64>,
    pub g31p: Vec<f64>,
    pub e11b: Vec<f64>,
    pub e22b: Vec<f64>,
    pub v12b: Vec<f64>,
    pub v21b: Vec<f64>,
    pub g12b: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_130 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_130").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
            e11p: t.take("E11P").and_then(|c| c.into_float()).unwrap_or_default(),
            e22p: t.take("E22P").and_then(|c| c.into_float()).unwrap_or_default(),
            v12p: t.take("V12P").and_then(|c| c.into_float()).unwrap_or_default(),
            v21p: t.take("V21P").and_then(|c| c.into_float()).unwrap_or_default(),
            g12p: t.take("G12P").and_then(|c| c.into_float()).unwrap_or_default(),
            g23p: t.take("G23P").and_then(|c| c.into_float()).unwrap_or_default(),
            g31p: t.take("G31P").and_then(|c| c.into_float()).unwrap_or_default(),
            e11b: t.take("E11B").and_then(|c| c.into_float()).unwrap_or_default(),
            e22b: t.take("E22B").and_then(|c| c.into_float()).unwrap_or_default(),
            v12b: t.take("V12B").and_then(|c| c.into_float()).unwrap_or_default(),
            v21b: t.take("V21B").and_then(|c| c.into_float()).unwrap_or_default(),
            g12b: t.take("G12B").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_131 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ispl: Vec<i64>,
    pub sigy: Vec<f64>,
    pub gk: Vec<f64>,
    pub sr: Vec<f64>,
}
impl MAT_131 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_131").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ispl: t.take("ISPL").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            gk: t.take("GK").and_then(|c| c.into_float()).unwrap_or_default(),
            sr: t.take("SR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_132 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub uins: Vec<f64>,
    pub uiss: Vec<f64>,
    pub cerrmi: Vec<f64>,
    pub cerrmii: Vec<f64>,
    pub ind: Vec<f64>,
    pub isd: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_132 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_132").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            uins: t.take("UINS").and_then(|c| c.into_float()).unwrap_or_default(),
            uiss: t.take("UISS").and_then(|c| c.into_float()).unwrap_or_default(),
            cerrmi: t.take("CERRMI").and_then(|c| c.into_float()).unwrap_or_default(),
            cerrmii: t.take("CERRMII").and_then(|c| c.into_float()).unwrap_or_default(),
            ind: t.take("IND").and_then(|c| c.into_float()).unwrap_or_default(),
            isd: t.take("ISD").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_133 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub fit: Vec<f64>,
    pub beta: Vec<f64>,
    pub iter: Vec<f64>,
    pub iscale: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub hard: Vec<f64>,
    pub a: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub sig00: Vec<f64>,
    pub sig45: Vec<f64>,
    pub sig90: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub dxx: Vec<f64>,
    pub dyy: Vec<f64>,
    pub dxy: Vec<f64>,
    pub aopt: Vec<f64>,
    pub offang: Vec<f64>,
    pub p4: Vec<f64>,
    pub htflag: Vec<i64>,
    pub hta: Vec<i64>,
    pub htb: Vec<i64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub null: Vec<f64>,
    pub null_2: Vec<f64>,
    pub null_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub usrfail: Vec<i64>,
    pub cp: Vec<f64>,
    pub t0: Vec<f64>,
    pub tref: Vec<f64>,
    pub ta0: Vec<f64>,
    pub a_2: Vec<f64>,
    pub b: Vec<f64>,
    pub c_2: Vec<f64>,
    pub d: Vec<f64>,
    pub p_2: Vec<f64>,
    pub q: Vec<f64>,
    pub e0mart: Vec<f64>,
    pub vm0: Vec<f64>,
    pub ahs: Vec<f64>,
    pub bhs: Vec<f64>,
    pub m: Vec<f64>,
    pub n_2: Vec<f64>,
    pub eps0: Vec<f64>,
    pub hmart: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
}
impl MAT_133 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_133").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            fit: t.take("FIT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            iscale: t.take("ISCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            hard: t.take("HARD").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            sig00: t.take("SIG00").and_then(|c| c.into_float()).unwrap_or_default(),
            sig45: t.take("SIG45").and_then(|c| c.into_float()).unwrap_or_default(),
            sig90: t.take("SIG90").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            dxx: t.take("DXX").and_then(|c| c.into_float()).unwrap_or_default(),
            dyy: t.take("DYY").and_then(|c| c.into_float()).unwrap_or_default(),
            dxy: t.take("DXY").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            offang: t.take("OFFANG").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hta: t.take("HTA").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_int()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_float()).unwrap_or_default(),
            null_2: t.take("NULL_2").and_then(|c| c.into_float()).unwrap_or_default(),
            null_3: t.take("NULL_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            ta0: t.take("TA0").and_then(|c| c.into_float()).unwrap_or_default(),
            a_2: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c_2: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            p_2: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            e0mart: t.take("E0MART").and_then(|c| c.into_float()).unwrap_or_default(),
            vm0: t.take("VM0").and_then(|c| c.into_float()).unwrap_or_default(),
            ahs: t.take("AHS").and_then(|c| c.into_float()).unwrap_or_default(),
            bhs: t.take("BHS").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n_2: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            hmart: t.take("HMART").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_134 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub cse: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_134 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_134").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_135 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub numfi: Vec<f64>,
    pub npsc: Vec<f64>,
    pub wc: Vec<f64>,
    pub tauc: Vec<f64>,
    pub sigma0: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub k: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub a7: Vec<f64>,
    pub a8: Vec<f64>,
    pub s: Vec<f64>,
    pub h: Vec<f64>,
    pub omega: Vec<f64>,
    pub td: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub a2_2: Vec<f64>,
    pub a3_2: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_135 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_135").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            numfi: t.take("NUMFI").and_then(|c| c.into_float()).unwrap_or_default(),
            npsc: t.take("NPSC").and_then(|c| c.into_float()).unwrap_or_default(),
            wc: t.take("WC").and_then(|c| c.into_float()).unwrap_or_default(),
            tauc: t.take("TAUC").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma0: t.take("SIGMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            a7: t.take("A7").and_then(|c| c.into_float()).unwrap_or_default(),
            a8: t.take("A8").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            td: t.take("TD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2_2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3_2: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_136 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fun_i: Vec<f64>,
    pub run_i: Vec<f64>,
    pub fps1_i: Vec<f64>,
    pub fps2_i: Vec<f64>,
    pub fsh_i: Vec<f64>,
}
impl MAT_136 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_136").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fun_i: t.take("FUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            run_i: t.take("RUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps1_i: t.take("FPS1-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps2_i: t.take("FPS2-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fsh_i: t.take("FSH-I").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_136_2017 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub unused: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub unused_2: Vec<i64>,
    pub dys: Vec<f64>,
    pub raten: Vec<f64>,
    pub srn0: Vec<f64>,
    pub exsr: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub rm_0: Vec<f64>,
    pub rm_45: Vec<f64>,
    pub rm_90: Vec<f64>,
    pub ag_0: Vec<f64>,
    pub ag_45: Vec<f64>,
    pub ag_90: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
}
impl MAT_136_2017 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_136_2017").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            dys: t.take("DYS").and_then(|c| c.into_float()).unwrap_or_default(),
            raten: t.take("RATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            srn0: t.take("SRN0").and_then(|c| c.into_float()).unwrap_or_default(),
            exsr: t.take("EXSR").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_0: t.take("RM-0").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_45: t.take("RM-45").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_90: t.take("RM-90").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_0: t.take("AG-0").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_45: t.take("AG-45").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_90: t.take("AG-90").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_136_STD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub unused: Vec<i64>,
    pub dys: Vec<f64>,
    pub raten: Vec<f64>,
    pub srn0: Vec<f64>,
    pub exsr: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fun_i: Vec<f64>,
    pub run_i: Vec<f64>,
    pub fps1_i: Vec<f64>,
    pub alps_i: Vec<f64>,
    pub fsh_i: Vec<f64>,
}
impl MAT_136_STD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_136_STD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            dys: t.take("DYS").and_then(|c| c.into_float()).unwrap_or_default(),
            raten: t.take("RATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            srn0: t.take("SRN0").and_then(|c| c.into_float()).unwrap_or_default(),
            exsr: t.take("EXSR").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fun_i: t.take("FUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            run_i: t.take("RUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps1_i: t.take("FPS1-I").and_then(|c| c.into_float()).unwrap_or_default(),
            alps_i: t.take("ALPS-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fsh_i: t.take("FSH-I").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_138 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<f64>,
    pub intfail: Vec<f64>,
    pub en: Vec<f64>,
    pub et: Vec<f64>,
    pub gic: Vec<f64>,
    pub giic: Vec<f64>,
    pub xmu: Vec<f64>,
    pub t: Vec<f64>,
    pub s: Vec<f64>,
    pub und: Vec<f64>,
    pub utd: Vec<f64>,
    pub gamma: Vec<f64>,
}
impl MAT_138 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_138").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            gic: t.take("GIC").and_then(|c| c.into_float()).unwrap_or_default(),
            giic: t.take("GIIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xmu: t.take("XMU").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            und: t.take("UND").and_then(|c| c.into_float()).unwrap_or_default(),
            utd: t.take("UTD").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_139 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub df: Vec<f64>,
    pub iaflc: Vec<i64>,
    pub ytflag: Vec<f64>,
    pub asoft: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub m6: Vec<f64>,
    pub m7: Vec<f64>,
    pub m8: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub lc5: Vec<i64>,
    pub lc6: Vec<i64>,
    pub lc7: Vec<i64>,
    pub lc8: Vec<i64>,
    pub lps1: Vec<i64>,
    pub sfs1: Vec<f64>,
    pub lps2: Vec<i64>,
    pub sfs2: Vec<f64>,
    pub yms1: Vec<f64>,
    pub yms2: Vec<f64>,
    pub lpt1: Vec<i64>,
    pub sft1: Vec<f64>,
    pub lpt2: Vec<i64>,
    pub sft2: Vec<f64>,
    pub ymt1: Vec<f64>,
    pub ymt2: Vec<f64>,
    pub lpr: Vec<i64>,
    pub sfr: Vec<f64>,
    pub ymr: Vec<f64>,
    pub lys1: Vec<i64>,
    pub sys1: Vec<f64>,
    pub lys2: Vec<i64>,
    pub sys2: Vec<f64>,
    pub lyt1: Vec<i64>,
    pub syt1: Vec<f64>,
    pub lyt2: Vec<i64>,
    pub syt2: Vec<f64>,
    pub lyr: Vec<i64>,
    pub syr: Vec<f64>,
    pub hms1_1: Vec<f64>,
    pub hms1_2: Vec<f64>,
    pub hms1_3: Vec<f64>,
    pub hms1_4: Vec<f64>,
    pub hms1_5: Vec<f64>,
    pub hms1_6: Vec<f64>,
    pub hms1_7: Vec<f64>,
    pub hms1_8: Vec<f64>,
    pub lpms1_1: Vec<i64>,
    pub lpms1_2: Vec<i64>,
    pub lpms1_3: Vec<i64>,
    pub lpms1_4: Vec<i64>,
    pub lpms1_5: Vec<i64>,
    pub lpms1_6: Vec<i64>,
    pub lpms1_7: Vec<i64>,
    pub lpms1_8: Vec<i64>,
    pub hms2_1: Vec<f64>,
    pub hms2_2: Vec<f64>,
    pub hms2_3: Vec<f64>,
    pub hms2_4: Vec<f64>,
    pub hms2_5: Vec<f64>,
    pub hms2_6: Vec<f64>,
    pub hms2_7: Vec<f64>,
    pub hms2_8: Vec<f64>,
    pub lpms2_1: Vec<i64>,
    pub lpms2_2: Vec<i64>,
    pub lpms2_3: Vec<i64>,
    pub lpms2_4: Vec<i64>,
    pub lpms2_5: Vec<i64>,
    pub lpms2_6: Vec<i64>,
    pub lpms2_7: Vec<i64>,
    pub lpms2_8: Vec<i64>,
    pub hmt1_1: Vec<f64>,
    pub hmt1_2: Vec<f64>,
    pub hmt1_3: Vec<f64>,
    pub hmt1_4: Vec<f64>,
    pub hmt1_5: Vec<f64>,
    pub hmt1_6: Vec<f64>,
    pub hmt1_7: Vec<f64>,
    pub hmt1_8: Vec<f64>,
    pub lpmt1_1: Vec<i64>,
    pub lpmt1_2: Vec<i64>,
    pub lpmt1_3: Vec<i64>,
    pub lpmt1_4: Vec<i64>,
    pub lpmt1_5: Vec<i64>,
    pub lpmt1_6: Vec<i64>,
    pub lpmt1_7: Vec<i64>,
    pub lpmt1_8: Vec<i64>,
    pub hmt2_1: Vec<f64>,
    pub hmt2_2: Vec<f64>,
    pub hmt2_3: Vec<f64>,
    pub hmt2_4: Vec<f64>,
    pub hmt2_5: Vec<f64>,
    pub hmt2_6: Vec<f64>,
    pub hmt2_7: Vec<f64>,
    pub hmt2_8: Vec<f64>,
    pub lpmt2_1: Vec<i64>,
    pub lpmt2_2: Vec<i64>,
    pub lpmt2_3: Vec<i64>,
    pub lpmt2_4: Vec<i64>,
    pub lpmt2_5: Vec<i64>,
    pub lpmt2_6: Vec<i64>,
    pub lpmt2_7: Vec<i64>,
    pub lpmt2_8: Vec<i64>,
    pub hmr_1: Vec<f64>,
    pub hmr_2: Vec<f64>,
    pub hmr_3: Vec<f64>,
    pub hmr_4: Vec<f64>,
    pub hmr_5: Vec<f64>,
    pub hmr_6: Vec<f64>,
    pub hmr_7: Vec<f64>,
    pub hmr_8: Vec<f64>,
    pub lpmr_1: Vec<i64>,
    pub lpmr_2: Vec<i64>,
    pub lpmr_3: Vec<i64>,
    pub lpmr_4: Vec<i64>,
    pub lpmr_5: Vec<i64>,
    pub lpmr_6: Vec<i64>,
    pub lpmr_7: Vec<i64>,
    pub lpmr_8: Vec<i64>,
}
impl MAT_139 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_139").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            iaflc: t.take("IAFLC").and_then(|c| c.into_int()).unwrap_or_default(),
            ytflag: t.take("YTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            asoft: t.take("ASOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            m6: t.take("M6").and_then(|c| c.into_float()).unwrap_or_default(),
            m7: t.take("M7").and_then(|c| c.into_float()).unwrap_or_default(),
            m8: t.take("M8").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            lc5: t.take("LC5").and_then(|c| c.into_int()).unwrap_or_default(),
            lc6: t.take("LC6").and_then(|c| c.into_int()).unwrap_or_default(),
            lc7: t.take("LC7").and_then(|c| c.into_int()).unwrap_or_default(),
            lc8: t.take("LC8").and_then(|c| c.into_int()).unwrap_or_default(),
            lps1: t.take("LPS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs1: t.take("SFS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lps2: t.take("LPS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs2: t.take("SFS2").and_then(|c| c.into_float()).unwrap_or_default(),
            yms1: t.take("YMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            yms2: t.take("YMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt1: t.take("LPT1").and_then(|c| c.into_int()).unwrap_or_default(),
            sft1: t.take("SFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt2: t.take("LPT2").and_then(|c| c.into_int()).unwrap_or_default(),
            sft2: t.take("SFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt1: t.take("YMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt2: t.take("YMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpr: t.take("LPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymr: t.take("YMR").and_then(|c| c.into_float()).unwrap_or_default(),
            lys1: t.take("LYS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sys1: t.take("SYS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lys2: t.take("LYS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sys2: t.take("SYS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lyt1: t.take("LYT1").and_then(|c| c.into_int()).unwrap_or_default(),
            syt1: t.take("SYT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lyt2: t.take("LYT2").and_then(|c| c.into_int()).unwrap_or_default(),
            syt2: t.take("SYT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lyr: t.take("LYR").and_then(|c| c.into_int()).unwrap_or_default(),
            syr: t.take("SYR").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_1: t.take("HMS1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_2: t.take("HMS1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_3: t.take("HMS1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_4: t.take("HMS1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_5: t.take("HMS1_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_6: t.take("HMS1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_7: t.take("HMS1_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_8: t.take("HMS1_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpms1_1: t.take("LPMS1_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_2: t.take("LPMS1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_3: t.take("LPMS1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_4: t.take("LPMS1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_5: t.take("LPMS1_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_6: t.take("LPMS1_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_7: t.take("LPMS1_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_8: t.take("LPMS1_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hms2_1: t.take("HMS2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_2: t.take("HMS2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_3: t.take("HMS2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_4: t.take("HMS2_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_5: t.take("HMS2_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_6: t.take("HMS2_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_7: t.take("HMS2_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_8: t.take("HMS2_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpms2_1: t.take("LPMS2_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_2: t.take("LPMS2_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_3: t.take("LPMS2_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_4: t.take("LPMS2_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_5: t.take("LPMS2_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_6: t.take("LPMS2_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_7: t.take("LPMS2_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_8: t.take("LPMS2_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmt1_1: t.take("HMT1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_2: t.take("HMT1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_3: t.take("HMT1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_4: t.take("HMT1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_5: t.take("HMT1_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_6: t.take("HMT1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_7: t.take("HMT1_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_8: t.take("HMT1_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmt1_1: t.take("LPMT1_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_2: t.take("LPMT1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_3: t.take("LPMT1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_4: t.take("LPMT1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_5: t.take("LPMT1_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_6: t.take("LPMT1_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_7: t.take("LPMT1_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_8: t.take("LPMT1_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmt2_1: t.take("HMT2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_2: t.take("HMT2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_3: t.take("HMT2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_4: t.take("HMT2_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_5: t.take("HMT2_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_6: t.take("HMT2_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_7: t.take("HMT2_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_8: t.take("HMT2_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmt2_1: t.take("LPMT2_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_2: t.take("LPMT2_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_3: t.take("LPMT2_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_4: t.take("LPMT2_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_5: t.take("LPMT2_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_6: t.take("LPMT2_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_7: t.take("LPMT2_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_8: t.take("LPMT2_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmr_1: t.take("HMR_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_2: t.take("HMR_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_3: t.take("HMR_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_4: t.take("HMR_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_5: t.take("HMR_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_6: t.take("HMR_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_7: t.take("HMR_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_8: t.take("HMR_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmr_1: t.take("LPMR_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_2: t.take("LPMR_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_3: t.take("LPMR_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_4: t.take("LPMR_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_5: t.take("LPMR_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_6: t.take("LPMR_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_7: t.take("LPMR_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_8: t.take("LPMR_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_140 {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
}
impl MAT_140 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_140").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_141 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub do_: Vec<f64>,
    pub n: Vec<f64>,
    pub zo: Vec<f64>,
    pub q: Vec<f64>,
    pub omega: Vec<f64>,
}
impl MAT_141 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_141").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            do_: t.take("Do").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("Zo").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("q").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("Omega").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_142 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11: Vec<f64>,
    pub e22: Vec<f64>,
    pub e12: Vec<f64>,
    pub e23: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub i11: Vec<i64>,
    pub i22: Vec<i64>,
    pub i12: Vec<i64>,
    pub i23: Vec<i64>,
    pub iaa: Vec<i64>,
    pub nsym: Vec<i64>,
    pub ang: Vec<f64>,
    pub mu: Vec<f64>,
    pub aopt: Vec<f64>,
    pub iscl: Vec<i64>,
    pub beta: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_142 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_142").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11: t.take("E11").and_then(|c| c.into_float()).unwrap_or_default(),
            e22: t.take("E22").and_then(|c| c.into_float()).unwrap_or_default(),
            e12: t.take("E12").and_then(|c| c.into_float()).unwrap_or_default(),
            e23: t.take("E23").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            i11: t.take("I11").and_then(|c| c.into_int()).unwrap_or_default(),
            i22: t.take("I22").and_then(|c| c.into_int()).unwrap_or_default(),
            i12: t.take("I12").and_then(|c| c.into_int()).unwrap_or_default(),
            i23: t.take("I23").and_then(|c| c.into_int()).unwrap_or_default(),
            iaa: t.take("IAA").and_then(|c| c.into_int()).unwrap_or_default(),
            nsym: t.take("NSYM").and_then(|c| c.into_int()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iscl: t.take("ISCL").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_143 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub el: Vec<f64>,
    pub et: Vec<f64>,
    pub glt: Vec<f64>,
    pub gtr: Vec<f64>,
    pub pr: Vec<f64>,
    pub xt: Vec<f64>,
    pub xc: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub sxy: Vec<f64>,
    pub syz: Vec<f64>,
    pub gf1: Vec<f64>,
    pub gf2: Vec<f64>,
    pub bfit: Vec<f64>,
    pub dmax: Vec<f64>,
    pub gf1p: Vec<f64>,
    pub gf2p: Vec<f64>,
    pub dfit: Vec<f64>,
    pub dmaxp: Vec<f64>,
    pub flpar: Vec<f64>,
    pub flparc: Vec<f64>,
    pub powpar: Vec<f64>,
    pub flper: Vec<f64>,
    pub flperc: Vec<f64>,
    pub powper: Vec<f64>,
    pub npar: Vec<f64>,
    pub cpar: Vec<f64>,
    pub nper: Vec<f64>,
    pub cper: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_143 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_143").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            glt: t.take("GLT").and_then(|c| c.into_float()).unwrap_or_default(),
            gtr: t.take("GTR").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gf1: t.take("GF1||").and_then(|c| c.into_float()).unwrap_or_default(),
            gf2: t.take("GF2||").and_then(|c| c.into_float()).unwrap_or_default(),
            bfit: t.take("BFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmax: t.take("DMAX||").and_then(|c| c.into_float()).unwrap_or_default(),
            gf1p: t.take("GF1P").and_then(|c| c.into_float()).unwrap_or_default(),
            gf2p: t.take("GF2P").and_then(|c| c.into_float()).unwrap_or_default(),
            dfit: t.take("DFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmaxp: t.take("DMAXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flpar: t.take("FLPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            flparc: t.take("FLPARC").and_then(|c| c.into_float()).unwrap_or_default(),
            powpar: t.take("POWPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            flper: t.take("FLPER").and_then(|c| c.into_float()).unwrap_or_default(),
            flperc: t.take("FLPERC").and_then(|c| c.into_float()).unwrap_or_default(),
            powper: t.take("POWPER").and_then(|c| c.into_float()).unwrap_or_default(),
            npar: t.take("NPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            cpar: t.take("CPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            nper: t.take("NPER").and_then(|c| c.into_float()).unwrap_or_default(),
            cper: t.take("CPER").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_143_FIR {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub mois: Vec<f64>,
    pub temp: Vec<f64>,
    pub qual_t: Vec<f64>,
    pub qual_c: Vec<f64>,
    pub units: Vec<i64>,
    pub iqual: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_143_FIR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_143_FIR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mois: t.take("MOIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_t: t.take("QUAL_T").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_c: t.take("QUAL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            iqual: t.take("IQUAL").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_143_PINE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub mois: Vec<f64>,
    pub temp: Vec<f64>,
    pub qual_t: Vec<f64>,
    pub qual_c: Vec<f64>,
    pub units: Vec<i64>,
    pub iqual: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_143_PINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_143_PINE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mois: t.take("MOIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_t: t.take("QUAL_T").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_c: t.take("QUAL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            iqual: t.take("IQUAL").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_144 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub pr: Vec<f64>,
    pub ty: Vec<f64>,
    pub srtv: Vec<f64>,
    pub lcpy: Vec<i64>,
    pub lcuys: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vc: Vec<f64>,
    pub dflg: Vec<f64>,
}
impl MAT_144 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_144").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            srtv: t.take("SRTV").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpy: t.take("LCPY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuys: t.take("LCUYS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            dflg: t.take("DFLG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_145 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub shear: Vec<f64>,
    pub bulk: Vec<f64>,
    pub grun: Vec<f64>,
    pub shock: Vec<f64>,
    pub pore: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub efit: Vec<f64>,
    pub ffit: Vec<f64>,
    pub alphan: Vec<f64>,
    pub calpha: Vec<f64>,
    pub ro_2: Vec<f64>,
    pub xo: Vec<f64>,
    pub irock: Vec<f64>,
    pub secp: Vec<f64>,
    pub afit: Vec<f64>,
    pub bfit: Vec<f64>,
    pub rdamo: Vec<f64>,
    pub w: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub nplot: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub cfit: Vec<f64>,
    pub dfit: Vec<f64>,
    pub tfail: Vec<f64>,
    pub failfg: Vec<f64>,
    pub dbeta: Vec<f64>,
    pub ddelta: Vec<f64>,
    pub vptau: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub theta1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub theta2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub beta2: Vec<f64>,
}
impl MAT_145 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_145").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            grun: t.take("GRUN").and_then(|c| c.into_float()).unwrap_or_default(),
            shock: t.take("SHOCK").and_then(|c| c.into_float()).unwrap_or_default(),
            pore: t.take("PORE").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            efit: t.take("EFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ffit: t.take("FFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            alphan: t.take("ALPHAN").and_then(|c| c.into_float()).unwrap_or_default(),
            calpha: t.take("CALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ro_2: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            irock: t.take("IROCK").and_then(|c| c.into_float()).unwrap_or_default(),
            secp: t.take("SECP").and_then(|c| c.into_float()).unwrap_or_default(),
            afit: t.take("AFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            bfit: t.take("BFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdamo: t.take("RDAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            cfit: t.take("CFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfit: t.take("DFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            failfg: t.take("FAILFG").and_then(|c| c.into_float()).unwrap_or_default(),
            dbeta: t.take("DBETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ddelta: t.take("DDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            vptau: t.take("VPTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_146 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub scln1: Vec<f64>,
    pub scln2: Vec<f64>,
    pub dofn1: Vec<i64>,
    pub dofn2: Vec<i64>,
    pub cid1: Vec<i64>,
    pub cid2: Vec<i64>,
}
impl MAT_146 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_146").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            scln1: t.take("SCLN1").and_then(|c| c.into_float()).unwrap_or_default(),
            scln2: t.take("SCLN2").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_int()).unwrap_or_default(),
            cid1: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_147 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub spgrav: Vec<f64>,
    pub rhowat: Vec<f64>,
    pub vn: Vec<f64>,
    pub gammar: Vec<f64>,
    pub intrmx: Vec<i64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub phimax: Vec<f64>,
    pub ahyp: Vec<f64>,
    pub coh: Vec<f64>,
    pub eccen: Vec<f64>,
    pub an: Vec<f64>,
    pub et: Vec<f64>,
    pub mcont: Vec<f64>,
    pub pwd1: Vec<f64>,
    pub pwksk: Vec<f64>,
    pub pwd2: Vec<f64>,
    pub phires: Vec<f64>,
    pub dint: Vec<f64>,
    pub vdfm: Vec<f64>,
    pub damlev: Vec<f64>,
    pub epsmax: Vec<f64>,
}
impl MAT_147 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_147").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            spgrav: t.take("SPGRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            rhowat: t.take("RHOWAT").and_then(|c| c.into_float()).unwrap_or_default(),
            vn: t.take("VN").and_then(|c| c.into_float()).unwrap_or_default(),
            gammar: t.take("GAMMAR").and_then(|c| c.into_float()).unwrap_or_default(),
            intrmx: t.take("INTRMX").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            phimax: t.take("PHIMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ahyp: t.take("AHYP").and_then(|c| c.into_float()).unwrap_or_default(),
            coh: t.take("COH").and_then(|c| c.into_float()).unwrap_or_default(),
            eccen: t.take("ECCEN").and_then(|c| c.into_float()).unwrap_or_default(),
            an: t.take("AN").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            mcont: t.take("MCONT").and_then(|c| c.into_float()).unwrap_or_default(),
            pwd1: t.take("PWD1").and_then(|c| c.into_float()).unwrap_or_default(),
            pwksk: t.take("PWKSK").and_then(|c| c.into_float()).unwrap_or_default(),
            pwd2: t.take("PWD2").and_then(|c| c.into_float()).unwrap_or_default(),
            phires: t.take("PHIRES").and_then(|c| c.into_float()).unwrap_or_default(),
            dint: t.take("DINT").and_then(|c| c.into_float()).unwrap_or_default(),
            vdfm: t.take("VDFM").and_then(|c| c.into_float()).unwrap_or_default(),
            damlev: t.take("DAMLEV").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_147_N {
    pub mid: Vec<i64>,
    pub fctim: Vec<f64>,
    pub fctmas: Vec<f64>,
    pub fctlen: Vec<f64>,
}
impl MAT_147_N {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_147_N").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            fctim: t.take("FCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            fctmas: t.take("FCTMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            fctlen: t.take("FCTLEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_148 {
    pub mid: Vec<i64>,
    pub iadiab: Vec<i64>,
    pub runiv: Vec<f64>,
    pub cvmass1: Vec<f64>,
    pub cvmass2: Vec<f64>,
    pub cvmass3: Vec<f64>,
    pub cvmass4: Vec<f64>,
    pub cvmass5: Vec<f64>,
    pub cvmass6: Vec<f64>,
    pub cvmass7: Vec<f64>,
    pub cvmass8: Vec<f64>,
    pub cpmass1: Vec<f64>,
    pub cpmass2: Vec<f64>,
    pub cpmass3: Vec<f64>,
    pub cpmass4: Vec<f64>,
    pub cpmass5: Vec<f64>,
    pub cpmass6: Vec<f64>,
    pub cpmass7: Vec<f64>,
    pub cpmass8: Vec<f64>,
    pub molwt1: Vec<f64>,
    pub molwt2: Vec<f64>,
    pub molwt3: Vec<f64>,
    pub molwt4: Vec<f64>,
    pub molwt5: Vec<f64>,
    pub molwt6: Vec<f64>,
    pub molwt7: Vec<f64>,
    pub molwt8: Vec<f64>,
    pub cpmole1: Vec<f64>,
    pub cpmole2: Vec<f64>,
    pub cpmole3: Vec<f64>,
    pub cpmole4: Vec<f64>,
    pub cpmole5: Vec<f64>,
    pub cpmole6: Vec<f64>,
    pub cpmole7: Vec<f64>,
    pub cpmole8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_148 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_148").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            iadiab: t.take("IADIAB").and_then(|c| c.into_int()).unwrap_or_default(),
            runiv: t.take("RUNIV").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass1: t.take("CVmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass2: t.take("CVmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass3: t.take("CVmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass4: t.take("CVmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass5: t.take("CVmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass6: t.take("CVmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass7: t.take("CVmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass8: t.take("CVmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass1: t.take("CPmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass2: t.take("CPmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass3: t.take("CPmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass4: t.take("CPmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass5: t.take("CPmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass6: t.take("CPmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass7: t.take("CPmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass8: t.take("CPmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt1: t.take("MOLWT1").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt2: t.take("MOLWT2").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt3: t.take("MOLWT3").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt4: t.take("MOLWT4").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt5: t.take("MOLWT5").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt6: t.take("MOLWT6").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt7: t.take("MOLWT7").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt8: t.take("MOLWT8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole1: t.take("CPmole1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole2: t.take("CPmole2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole3: t.take("CPmole3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole4: t.take("CPmole4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole5: t.take("CPmole5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole6: t.take("CPmole6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole7: t.take("CPmole7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole8: t.take("CPmole8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_151 {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub rgas: Vec<f64>,
    pub bvect: Vec<f64>,
    pub d0: Vec<f64>,
    pub qd: Vec<f64>,
    pub cv: Vec<f64>,
    pub adrag: Vec<f64>,
    pub bdrag: Vec<f64>,
    pub dmtheta: Vec<f64>,
    pub dmphi: Vec<f64>,
    pub dntheta: Vec<f64>,
    pub dnphi: Vec<f64>,
    pub theta0: Vec<f64>,
    pub thetam: Vec<f64>,
    pub beta0: Vec<f64>,
    pub btheta: Vec<f64>,
    pub dmr: Vec<f64>,
    pub dnuc1: Vec<f64>,
    pub dnuc2: Vec<f64>,
    pub dnuc3: Vec<f64>,
    pub dnuc4: Vec<f64>,
    pub dm1: Vec<f64>,
    pub dm2: Vec<f64>,
    pub dm3: Vec<f64>,
    pub dm4: Vec<f64>,
    pub dm5: Vec<f64>,
    pub qind: Vec<f64>,
    pub q2nd: Vec<f64>,
    pub q3nd: Vec<f64>,
    pub q4nd: Vec<f64>,
    pub calpha: Vec<f64>,
    pub ckappa: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2nd: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7nd: Vec<f64>,
    pub c8nd: Vec<f64>,
    pub c9nd: Vec<f64>,
    pub c10: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a_xx: Vec<f64>,
    pub a_yy: Vec<f64>,
    pub a_zz: Vec<f64>,
    pub a_xy: Vec<f64>,
    pub a_yz: Vec<f64>,
    pub a_xz: Vec<f64>,
    pub alphxx: Vec<f64>,
    pub alphyy: Vec<f64>,
    pub alphzz: Vec<f64>,
    pub alphxy: Vec<f64>,
    pub alphyz: Vec<f64>,
    pub alphxz: Vec<f64>,
    pub dkappa: Vec<f64>,
    pub phi0: Vec<f64>,
    pub phicr: Vec<f64>,
    pub dlbdag: Vec<f64>,
    pub factor: Vec<f64>,
    pub rswtch: Vec<f64>,
    pub dmgopt: Vec<f64>,
    pub delaso: Vec<f64>,
    pub dimplo: Vec<f64>,
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub dniter: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_5: Vec<f64>,
    pub f_6: Vec<f64>,
    pub f_7: Vec<f64>,
    pub f_8: Vec<f64>,
}
impl MAT_151 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_151").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            rgas: t.take("RGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            bvect: t.take("BVECT").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            qd: t.take("QD").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            adrag: t.take("ADRAG").and_then(|c| c.into_float()).unwrap_or_default(),
            bdrag: t.take("BDRAG").and_then(|c| c.into_float()).unwrap_or_default(),
            dmtheta: t.take("DMTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dmphi: t.take("DMPHI").and_then(|c| c.into_float()).unwrap_or_default(),
            dntheta: t.take("DNTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dnphi: t.take("DNPHI").and_then(|c| c.into_float()).unwrap_or_default(),
            theta0: t.take("THETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            thetam: t.take("THETAM").and_then(|c| c.into_float()).unwrap_or_default(),
            beta0: t.take("BETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            btheta: t.take("BTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dmr: t.take("DMR").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc1: t.take("DNUC1").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc2: t.take("DNUC2").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc3: t.take("DNUC3").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc4: t.take("DNUC4").and_then(|c| c.into_float()).unwrap_or_default(),
            dm1: t.take("DM1").and_then(|c| c.into_float()).unwrap_or_default(),
            dm2: t.take("DM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dm3: t.take("DM3").and_then(|c| c.into_float()).unwrap_or_default(),
            dm4: t.take("DM4").and_then(|c| c.into_float()).unwrap_or_default(),
            dm5: t.take("DM5").and_then(|c| c.into_float()).unwrap_or_default(),
            qind: t.take("QIND").and_then(|c| c.into_float()).unwrap_or_default(),
            q2nd: t.take("Q2ND").and_then(|c| c.into_float()).unwrap_or_default(),
            q3nd: t.take("Q3ND").and_then(|c| c.into_float()).unwrap_or_default(),
            q4nd: t.take("Q4ND").and_then(|c| c.into_float()).unwrap_or_default(),
            calpha: t.take("CALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ckappa: t.take("CKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2nd: t.take("C2ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7nd: t.take("C7ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c8nd: t.take("C8ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c9nd: t.take("C9ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xx: t.take("A_XX").and_then(|c| c.into_float()).unwrap_or_default(),
            a_yy: t.take("A_YY").and_then(|c| c.into_float()).unwrap_or_default(),
            a_zz: t.take("A_ZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xy: t.take("A_XY").and_then(|c| c.into_float()).unwrap_or_default(),
            a_yz: t.take("A_YZ").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xz: t.take("A_XZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxx: t.take("ALPHXX").and_then(|c| c.into_float()).unwrap_or_default(),
            alphyy: t.take("ALPHYY").and_then(|c| c.into_float()).unwrap_or_default(),
            alphzz: t.take("ALPHZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxy: t.take("ALPHXY").and_then(|c| c.into_float()).unwrap_or_default(),
            alphyz: t.take("ALPHYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxz: t.take("ALPHXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dkappa: t.take("DKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            phi0: t.take("PHI0").and_then(|c| c.into_float()).unwrap_or_default(),
            phicr: t.take("PHICR").and_then(|c| c.into_float()).unwrap_or_default(),
            dlbdag: t.take("DLBDAG").and_then(|c| c.into_float()).unwrap_or_default(),
            factor: t.take("FACTOR").and_then(|c| c.into_float()).unwrap_or_default(),
            rswtch: t.take("RSWTCH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgopt: t.take("DMGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            delaso: t.take("DELASO").and_then(|c| c.into_float()).unwrap_or_default(),
            dimplo: t.take("DIMPLO").and_then(|c| c.into_float()).unwrap_or_default(),
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dniter: t.take("DNITER").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_float()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_float()).unwrap_or_default(),
            f_7: t.take("-_7").and_then(|c| c.into_float()).unwrap_or_default(),
            f_8: t.take("-_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_153 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub hardi: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcss: Vec<i64>,
    pub hardk1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub hardk2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub hardk3: Vec<f64>,
    pub gamma3: Vec<f64>,
    pub idamage: Vec<i64>,
    pub ids: Vec<i64>,
    pub idep: Vec<i64>,
    pub epsd: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub dc: Vec<f64>,
    pub khflg: Vec<i64>,
}
impl MAT_153 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_153").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            hardi: t.take("HARDI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            hardk1: t.take("HARDK1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hardk2: t.take("HARDK2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            hardk3: t.take("HARDK3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
            idamage: t.take("IDAMAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            ids: t.take("IDS").and_then(|c| c.into_int()).unwrap_or_default(),
            idep: t.take("IDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            khflg: t.take("KHFLG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_154 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub alpha: Vec<f64>,
    pub gamma: Vec<f64>,
    pub epsd: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub beta: Vec<f64>,
    pub sigp: Vec<f64>,
    pub derfi: Vec<f64>,
    pub cfail: Vec<f64>,
    pub pfail: Vec<f64>,
    pub num: Vec<i64>,
}
impl MAT_154 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_154").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigp: t.take("SIGP").and_then(|c| c.into_float()).unwrap_or_default(),
            derfi: t.take("DERFI").and_then(|c| c.into_float()).unwrap_or_default(),
            cfail: t.take("CFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            pfail: t.take("PFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            num: t.take("NUM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_155 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcsrc: Vec<i64>,
    pub lcsrt: Vec<i64>,
    pub srflag: Vec<f64>,
    pub pc: Vec<f64>,
    pub pt: Vec<f64>,
    pub pcutc: Vec<f64>,
    pub pcutt: Vec<f64>,
    pub pcutf: Vec<f64>,
    pub scalep: Vec<f64>,
    pub scalee: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_155 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_155").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrc: t.take("LCSRC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrt: t.take("LCSRT").and_then(|c| c.into_int()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutc: t.take("PCUTC").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutt: t.take("PCUTT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutf: t.take("PCUTF").and_then(|c| c.into_float()).unwrap_or_default(),
            scalep: t.take("SCALEP").and_then(|c| c.into_float()).unwrap_or_default(),
            scalee: t.take("SCALEE").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_156 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub sno: Vec<f64>,
    pub srm: Vec<f64>,
    pub pis: Vec<f64>,
    pub ssm: Vec<f64>,
    pub cer: Vec<f64>,
    pub dmp: Vec<f64>,
    pub alm: Vec<f64>,
    pub sfr: Vec<f64>,
    pub svs: Vec<f64>,
    pub svr: Vec<f64>,
    pub ssp: Vec<f64>,
}
impl MAT_156 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_156").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            sno: t.take("SNO").and_then(|c| c.into_float()).unwrap_or_default(),
            srm: t.take("SRM").and_then(|c| c.into_float()).unwrap_or_default(),
            pis: t.take("PIS").and_then(|c| c.into_float()).unwrap_or_default(),
            ssm: t.take("SSM").and_then(|c| c.into_float()).unwrap_or_default(),
            cer: t.take("CER").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp: t.take("DMP").and_then(|c| c.into_float()).unwrap_or_default(),
            alm: t.take("ALM").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            svs: t.take("SVS").and_then(|c| c.into_float()).unwrap_or_default(),
            svr: t.take("SVR").and_then(|c| c.into_float()).unwrap_or_default(),
            ssp: t.take("SSP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_157 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c22: Vec<f64>,
    pub c23: Vec<f64>,
    pub c24: Vec<f64>,
    pub c25: Vec<f64>,
    pub c26: Vec<f64>,
    pub c33: Vec<f64>,
    pub c34: Vec<f64>,
    pub c35: Vec<f64>,
    pub c36: Vec<f64>,
    pub c44: Vec<f64>,
    pub c45: Vec<f64>,
    pub c46: Vec<f64>,
    pub c55: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub s11_l: Vec<f64>,
    pub s22_m: Vec<f64>,
    pub s33_n: Vec<f64>,
    pub s12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub vp: Vec<f64>,
    pub unused: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub id3upd: Vec<f64>,
    pub extra: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ihis: Vec<f64>,
    pub xt: Vec<f64>,
    pub xc: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub sxy: Vec<f64>,
    pub ff12: Vec<f64>,
    pub f60: Vec<f64>,
    pub ncfail: Vec<f64>,
    pub zt: Vec<f64>,
    pub zc: Vec<f64>,
    pub syz: Vec<f64>,
    pub szx: Vec<f64>,
    pub ff23: Vec<f64>,
    pub ff31: Vec<f64>,
}
impl MAT_157 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_157").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            s11_l: t.take("S11/L").and_then(|c| c.into_float()).unwrap_or_default(),
            s22_m: t.take("S22/M").and_then(|c| c.into_float()).unwrap_or_default(),
            s33_n: t.take("S33/N").and_then(|c| c.into_float()).unwrap_or_default(),
            s12: t.take("S12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            id3upd: t.take("ID3UPD").and_then(|c| c.into_float()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ff12: t.take("FF12").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_float()).unwrap_or_default(),
            ncfail: t.take("NCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            szx: t.take("SZX").and_then(|c| c.into_float()).unwrap_or_default(),
            ff23: t.take("FF23").and_then(|c| c.into_float()).unwrap_or_default(),
            ff31: t.take("FF31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_158 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub taui: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub sf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_158 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_158").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            taui: t.take("TAUI").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMs").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_159 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub incre: Vec<f64>,
    pub irate: Vec<i64>,
    pub erode: Vec<f64>,
    pub recov: Vec<f64>,
    pub itretrc: Vec<i64>,
    pub pred: Vec<f64>,
    pub fpc: Vec<f64>,
    pub dagg: Vec<f64>,
    pub units: Vec<i64>,
}
impl MAT_159 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_159").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            incre: t.take("INCRE").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_float()).unwrap_or_default(),
            recov: t.take("RECOV").and_then(|c| c.into_float()).unwrap_or_default(),
            itretrc: t.take("ITRETRC").and_then(|c| c.into_int()).unwrap_or_default(),
            pred: t.take("PRED").and_then(|c| c.into_float()).unwrap_or_default(),
            fpc: t.take("FPC").and_then(|c| c.into_float()).unwrap_or_default(),
            dagg: t.take("DAGG").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_160 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mu: Vec<f64>,
    pub tol: Vec<f64>,
    pub dtout: Vec<f64>,
    pub ncg: Vec<i64>,
    pub meth: Vec<i64>,
}
impl MAT_160 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_160").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            ncg: t.take("NCG").and_then(|c| c.into_int()).unwrap_or_default(),
            meth: t.take("METH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_161 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sat: Vec<f64>,
    pub sac: Vec<f64>,
    pub sbt: Vec<f64>,
    pub sbc: Vec<f64>,
    pub sct: Vec<f64>,
    pub sfc: Vec<f64>,
    pub sfs: Vec<f64>,
    pub sab: Vec<f64>,
    pub sbc_2: Vec<f64>,
    pub sca: Vec<f64>,
    pub sffc: Vec<f64>,
    pub amodel: Vec<i64>,
    pub phic: Vec<f64>,
    pub e_limt: Vec<f64>,
    pub s_delm: Vec<f64>,
    pub omgmx: Vec<f64>,
    pub ecrsh: Vec<f64>,
    pub eexpn: Vec<f64>,
    pub cerate1: Vec<f64>,
    pub am1: Vec<f64>,
}
impl MAT_161 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_161").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sat: t.take("SAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sac: t.take("SAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sbt: t.take("SBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sct: t.take("SCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfc: t.take("SFC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            sab: t.take("SAB").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc_2: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            sffc: t.take("SFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            amodel: t.take("AMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            phic: t.take("PHIC").and_then(|c| c.into_float()).unwrap_or_default(),
            e_limt: t.take("E_LIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            s_delm: t.take("S_DELM").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmx: t.take("OMGMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrsh: t.take("ECRSH").and_then(|c| c.into_float()).unwrap_or_default(),
            eexpn: t.take("EEXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate1: t.take("CERATE1").and_then(|c| c.into_float()).unwrap_or_default(),
            am1: t.take("AM1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_162 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sat: Vec<f64>,
    pub sac: Vec<f64>,
    pub sbt: Vec<f64>,
    pub sbc: Vec<f64>,
    pub sct: Vec<f64>,
    pub sfc: Vec<f64>,
    pub sfs: Vec<f64>,
    pub sab: Vec<f64>,
    pub sbc_2: Vec<f64>,
    pub sca: Vec<f64>,
    pub sffc: Vec<f64>,
    pub amodel: Vec<i64>,
    pub phic: Vec<f64>,
    pub e_limt: Vec<f64>,
    pub s_delm: Vec<f64>,
    pub omgmx: Vec<f64>,
    pub ecrsh: Vec<f64>,
    pub eexpn: Vec<f64>,
    pub cerate1: Vec<f64>,
    pub am1: Vec<f64>,
    pub am2: Vec<f64>,
    pub am3: Vec<f64>,
    pub am4: Vec<f64>,
    pub cerate2: Vec<f64>,
    pub cerate3: Vec<f64>,
    pub cerate4: Vec<f64>,
}
impl MAT_162 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_162").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sat: t.take("SAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sac: t.take("SAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sbt: t.take("SBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sct: t.take("SCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfc: t.take("SFC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            sab: t.take("SAB").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc_2: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            sffc: t.take("SFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            amodel: t.take("AMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            phic: t.take("PHIC").and_then(|c| c.into_float()).unwrap_or_default(),
            e_limt: t.take("E_LIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            s_delm: t.take("S_DELM").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmx: t.take("OMGMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrsh: t.take("ECRSH").and_then(|c| c.into_float()).unwrap_or_default(),
            eexpn: t.take("EEXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate1: t.take("CERATE1").and_then(|c| c.into_float()).unwrap_or_default(),
            am1: t.take("AM1").and_then(|c| c.into_float()).unwrap_or_default(),
            am2: t.take("AM2").and_then(|c| c.into_float()).unwrap_or_default(),
            am3: t.take("AM3").and_then(|c| c.into_float()).unwrap_or_default(),
            am4: t.take("AM4").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate2: t.take("CERATE2").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate3: t.take("CERATE3").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate4: t.take("CERATE4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_163 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tid: Vec<i64>,
    pub tsc: Vec<f64>,
    pub damp: Vec<f64>,
    pub ncycle: Vec<f64>,
    pub srclmt: Vec<f64>,
    pub srflag: Vec<i64>,
}
impl MAT_163 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_163").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tid: t.take("TID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
            srclmt: t.take("SRCLMT").and_then(|c| c.into_float()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_164 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub dc: Vec<f64>,
    pub fo: Vec<f64>,
    pub so: Vec<f64>,
}
impl MAT_164 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_164").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            fo: t.take("FO").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_165 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub h: Vec<f64>,
    pub c: Vec<f64>,
    pub gamma: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_165 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_165").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_165B {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub re: Vec<f64>,
    pub b: Vec<f64>,
    pub q: Vec<f64>,
    pub c1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub c2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub c3: Vec<f64>,
    pub gamma3: Vec<f64>,
}
impl MAT_165B {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_165B").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            re: t.take("RE").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_166 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub elaf: Vec<i64>,
    pub epflg: Vec<i64>,
    pub cta: Vec<f64>,
    pub ctb: Vec<f64>,
    pub ctt: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub n4: Vec<f64>,
    pub n5: Vec<f64>,
    pub n6: Vec<f64>,
    pub n7: Vec<f64>,
    pub n8: Vec<f64>,
    pub lcms1: Vec<i64>,
    pub lcms2: Vec<i64>,
    pub lcms3: Vec<i64>,
    pub lcms4: Vec<i64>,
    pub lcms5: Vec<i64>,
    pub lcms6: Vec<i64>,
    pub lcms7: Vec<i64>,
    pub lcms8: Vec<i64>,
    pub lcmt1: Vec<i64>,
    pub lcmt2: Vec<i64>,
    pub lcmt3: Vec<i64>,
    pub lcmt4: Vec<i64>,
    pub lcmt5: Vec<i64>,
    pub lcmt6: Vec<i64>,
    pub lcmt7: Vec<i64>,
    pub lcmt8: Vec<i64>,
    pub lct1: Vec<i64>,
    pub lct2: Vec<i64>,
    pub lct3: Vec<i64>,
    pub lct4: Vec<i64>,
    pub lct5: Vec<i64>,
    pub lct6: Vec<i64>,
    pub lct7: Vec<i64>,
    pub lct8: Vec<i64>,
    pub cfa: Vec<f64>,
    pub cfb: Vec<f64>,
    pub cft: Vec<f64>,
    pub hrule: Vec<f64>,
    pub reps: Vec<f64>,
    pub rbeta: Vec<f64>,
    pub rcapay: Vec<f64>,
    pub rcapaz: Vec<f64>,
}
impl MAT_166 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_166").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            elaf: t.take("ELAF").and_then(|c| c.into_int()).unwrap_or_default(),
            epflg: t.take("EPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            cta: t.take("CTA").and_then(|c| c.into_float()).unwrap_or_default(),
            ctb: t.take("CTB").and_then(|c| c.into_float()).unwrap_or_default(),
            ctt: t.take("CTT").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_float()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_float()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_float()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_float()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_float()).unwrap_or_default(),
            lcms1: t.take("LCMS1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms2: t.take("LCMS2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms3: t.take("LCMS3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms4: t.take("LCMS4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms5: t.take("LCMS5").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms6: t.take("LCMS6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms7: t.take("LCMS7").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms8: t.take("LCMS8").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt1: t.take("LCMT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt2: t.take("LCMT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt3: t.take("LCMT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt4: t.take("LCMT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt5: t.take("LCMT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt6: t.take("LCMT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt7: t.take("LCMT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt8: t.take("LCMT8").and_then(|c| c.into_int()).unwrap_or_default(),
            lct1: t.take("LCT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lct2: t.take("LCT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lct3: t.take("LCT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lct4: t.take("LCT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lct5: t.take("LCT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lct6: t.take("LCT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lct7: t.take("LCT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lct8: t.take("LCT8").and_then(|c| c.into_int()).unwrap_or_default(),
            cfa: t.take("CFA").and_then(|c| c.into_float()).unwrap_or_default(),
            cfb: t.take("CFB").and_then(|c| c.into_float()).unwrap_or_default(),
            cft: t.take("CFT").and_then(|c| c.into_float()).unwrap_or_default(),
            hrule: t.take("HRULE").and_then(|c| c.into_float()).unwrap_or_default(),
            reps: t.take("REPS").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta: t.take("RBETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rcapay: t.take("RCAPAY").and_then(|c| c.into_float()).unwrap_or_default(),
            rcapaz: t.take("RCAPAZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_167 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub q1: Vec<f64>,
    pub c1: Vec<f64>,
    pub q2: Vec<f64>,
    pub c2: Vec<f64>,
    pub s: Vec<f64>,
    pub h: Vec<f64>,
    pub omega: Vec<f64>,
    pub td: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
}
impl MAT_167 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_167").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            td: t.take("TD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_168 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub gamma0: Vec<f64>,
    pub dg: Vec<f64>,
    pub sc: Vec<f64>,
    pub st: Vec<f64>,
    pub temp: Vec<f64>,
    pub k: Vec<f64>,
    pub cr: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
}
impl MAT_168 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_168").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma0: t.take("GAMMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            dg: t.take("DG").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("CR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_169 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tenmax: Vec<f64>,
    pub gcten: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub gcshr: Vec<f64>,
    pub pwrt: Vec<f64>,
    pub pwrs: Vec<f64>,
    pub shrp: Vec<f64>,
    pub sht_sl: Vec<f64>,
    pub edot0: Vec<f64>,
    pub edot2: Vec<f64>,
    pub thkdir: Vec<f64>,
    pub extra: Vec<f64>,
    pub tmaxe: Vec<f64>,
    pub gcte: Vec<f64>,
    pub smaxe: Vec<f64>,
    pub gcse: Vec<f64>,
    pub pwrte: Vec<f64>,
    pub pwrse: Vec<f64>,
    pub facet: Vec<f64>,
    pub facct: Vec<f64>,
    pub faces: Vec<f64>,
    pub faccs: Vec<f64>,
    pub softt: Vec<f64>,
    pub softs: Vec<f64>,
    pub sdfac: Vec<f64>,
    pub sgfac: Vec<f64>,
    pub sdefac: Vec<f64>,
    pub sgefac: Vec<f64>,
    pub bthk: Vec<f64>,
    pub outfail: Vec<f64>,
    pub fsip: Vec<f64>,
    pub fbr713: Vec<f64>,
    pub ele2ns: Vec<f64>,
}
impl MAT_169 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_169").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tenmax: t.take("TENMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gcten: t.take("GCTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gcshr: t.take("GCSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrt: t.take("PWRT").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrs: t.take("PWRS").and_then(|c| c.into_float()).unwrap_or_default(),
            shrp: t.take("SHRP").and_then(|c| c.into_float()).unwrap_or_default(),
            sht_sl: t.take("SHT_SL").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            edot2: t.take("EDOT2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkdir: t.take("THKDIR").and_then(|c| c.into_float()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_float()).unwrap_or_default(),
            tmaxe: t.take("TMAXE").and_then(|c| c.into_float()).unwrap_or_default(),
            gcte: t.take("GCTE").and_then(|c| c.into_float()).unwrap_or_default(),
            smaxe: t.take("SMAXE").and_then(|c| c.into_float()).unwrap_or_default(),
            gcse: t.take("GCSE").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrte: t.take("PWRTE").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrse: t.take("PWRSE").and_then(|c| c.into_float()).unwrap_or_default(),
            facet: t.take("FACET").and_then(|c| c.into_float()).unwrap_or_default(),
            facct: t.take("FACCT").and_then(|c| c.into_float()).unwrap_or_default(),
            faces: t.take("FACES").and_then(|c| c.into_float()).unwrap_or_default(),
            faccs: t.take("FACCS").and_then(|c| c.into_float()).unwrap_or_default(),
            softt: t.take("SOFTT").and_then(|c| c.into_float()).unwrap_or_default(),
            softs: t.take("SOFTS").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfac: t.take("SDFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sgfac: t.take("SGFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdefac: t.take("SDEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sgefac: t.take("SGEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bthk: t.take("BTHK").and_then(|c| c.into_float()).unwrap_or_default(),
            outfail: t.take("OUTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            fsip: t.take("FSIP").and_then(|c| c.into_float()).unwrap_or_default(),
            fbr713: t.take("FBR713").and_then(|c| c.into_float()).unwrap_or_default(),
            ele2ns: t.take("ELE2NS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_170 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11p: Vec<f64>,
    pub e22p: Vec<f64>,
    pub v12p: Vec<f64>,
    pub v21p: Vec<f64>,
    pub g12p: Vec<f64>,
    pub g23p: Vec<f64>,
    pub g31p: Vec<f64>,
    pub e11b: Vec<f64>,
    pub e22b: Vec<f64>,
    pub v12b: Vec<f64>,
    pub v21b: Vec<f64>,
    pub g12b: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ln11: Vec<f64>,
    pub ln22: Vec<f64>,
    pub ln12: Vec<f64>,
    pub lq1: Vec<f64>,
    pub lq2: Vec<f64>,
    pub lm11: Vec<f64>,
    pub lm22: Vec<f64>,
    pub lm12: Vec<f64>,
    pub null: Vec<f64>,
    pub null_2: Vec<f64>,
    pub null_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_170 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_170").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11p: t.take("E11P").and_then(|c| c.into_float()).unwrap_or_default(),
            e22p: t.take("E22P").and_then(|c| c.into_float()).unwrap_or_default(),
            v12p: t.take("V12P").and_then(|c| c.into_float()).unwrap_or_default(),
            v21p: t.take("V21P").and_then(|c| c.into_float()).unwrap_or_default(),
            g12p: t.take("G12P").and_then(|c| c.into_float()).unwrap_or_default(),
            g23p: t.take("G23P").and_then(|c| c.into_float()).unwrap_or_default(),
            g31p: t.take("G31P").and_then(|c| c.into_float()).unwrap_or_default(),
            e11b: t.take("E11B").and_then(|c| c.into_float()).unwrap_or_default(),
            e22b: t.take("E22B").and_then(|c| c.into_float()).unwrap_or_default(),
            v12b: t.take("V12B").and_then(|c| c.into_float()).unwrap_or_default(),
            v21b: t.take("V21B").and_then(|c| c.into_float()).unwrap_or_default(),
            g12b: t.take("G12B").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ln11: t.take("LN11").and_then(|c| c.into_float()).unwrap_or_default(),
            ln22: t.take("LN22").and_then(|c| c.into_float()).unwrap_or_default(),
            ln12: t.take("LN12").and_then(|c| c.into_float()).unwrap_or_default(),
            lq1: t.take("LQ1").and_then(|c| c.into_float()).unwrap_or_default(),
            lq2: t.take("LQ2").and_then(|c| c.into_float()).unwrap_or_default(),
            lm11: t.take("LM11").and_then(|c| c.into_float()).unwrap_or_default(),
            lm22: t.take("LM22").and_then(|c| c.into_float()).unwrap_or_default(),
            lm12: t.take("LM12").and_then(|c| c.into_float()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_float()).unwrap_or_default(),
            null_2: t.take("NULL_2").and_then(|c| c.into_float()).unwrap_or_default(),
            null_3: t.take("NULL_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_171 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lamda: Vec<f64>,
    pub fbuck: Vec<f64>,
    pub fbuck2: Vec<f64>,
    pub ccbrf: Vec<f64>,
    pub bcur: Vec<f64>,
    pub ts1: Vec<f64>,
    pub ts2: Vec<f64>,
    pub ts3: Vec<f64>,
    pub ts4: Vec<f64>,
    pub cs1: Vec<f64>,
    pub cs2: Vec<f64>,
    pub cs3: Vec<f64>,
    pub cs4: Vec<f64>,
}
impl MAT_171 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_171").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda: t.take("LAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            fbuck: t.take("FBUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            fbuck2: t.take("FBUCK2").and_then(|c| c.into_float()).unwrap_or_default(),
            ccbrf: t.take("CCBRF").and_then(|c| c.into_float()).unwrap_or_default(),
            bcur: t.take("BCUR").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts3: t.take("TS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ts4: t.take("TS4").and_then(|c| c.into_float()).unwrap_or_default(),
            cs1: t.take("CS1").and_then(|c| c.into_float()).unwrap_or_default(),
            cs2: t.take("CS2").and_then(|c| c.into_float()).unwrap_or_default(),
            cs3: t.take("CS3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs4: t.take("CS4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_172 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub fc: Vec<f64>,
    pub ft: Vec<f64>,
    pub typec: Vec<f64>,
    pub unitc: Vec<f64>,
    pub ecuten: Vec<f64>,
    pub fcc: Vec<f64>,
    pub esoft: Vec<f64>,
    pub lchar: Vec<f64>,
    pub mu: Vec<f64>,
    pub taumxf: Vec<f64>,
    pub taumxc: Vec<f64>,
    pub ecragg: Vec<f64>,
    pub aggsz: Vec<f64>,
    pub unitl: Vec<f64>,
    pub ymreinf: Vec<f64>,
    pub prrinf: Vec<f64>,
    pub sureinf: Vec<f64>,
    pub typer: Vec<f64>,
    pub fracrx: Vec<f64>,
    pub fracry: Vec<f64>,
    pub lcrsu: Vec<i64>,
    pub lcalps: Vec<i64>,
    pub aopt: Vec<f64>,
    pub et36: Vec<f64>,
    pub prt36: Vec<f64>,
    pub ecut36: Vec<f64>,
    pub lcalpc: Vec<i64>,
    pub degrad: Vec<f64>,
    pub ishchk: Vec<i64>,
    pub unlfac: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub typesc: Vec<f64>,
    pub p_or_f: Vec<f64>,
    pub effd: Vec<f64>,
    pub gamsc: Vec<f64>,
    pub erodet: Vec<f64>,
    pub erodec: Vec<f64>,
    pub eroder: Vec<f64>,
    pub tmpoff: Vec<f64>,
    pub ec1_6: Vec<f64>,
    pub ecsp69: Vec<f64>,
    pub gamce9: Vec<f64>,
    pub phief9: Vec<f64>,
}
impl MAT_172 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_172").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            typec: t.take("TYPEC").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            ecuten: t.take("ECUTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fcc: t.take("FCC").and_then(|c| c.into_float()).unwrap_or_default(),
            esoft: t.take("ESOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchar: t.take("LCHAR").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            taumxf: t.take("TAUMXF").and_then(|c| c.into_float()).unwrap_or_default(),
            taumxc: t.take("TAUMXC").and_then(|c| c.into_float()).unwrap_or_default(),
            ecragg: t.take("ECRAGG").and_then(|c| c.into_float()).unwrap_or_default(),
            aggsz: t.take("AGGSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            unitl: t.take("UNITL").and_then(|c| c.into_float()).unwrap_or_default(),
            ymreinf: t.take("YMREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            prrinf: t.take("PRRINF").and_then(|c| c.into_float()).unwrap_or_default(),
            sureinf: t.take("SUREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            typer: t.take("TYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            fracrx: t.take("FRACRX").and_then(|c| c.into_float()).unwrap_or_default(),
            fracry: t.take("FRACRY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrsu: t.take("LCRSU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcalps: t.take("LCALPS").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            et36: t.take("ET36").and_then(|c| c.into_float()).unwrap_or_default(),
            prt36: t.take("PRT36").and_then(|c| c.into_float()).unwrap_or_default(),
            ecut36: t.take("ECUT36").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalpc: t.take("LCALPC").and_then(|c| c.into_int()).unwrap_or_default(),
            degrad: t.take("DEGRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            ishchk: t.take("ISHCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            unlfac: t.take("UNLFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            typesc: t.take("TYPESC").and_then(|c| c.into_float()).unwrap_or_default(),
            p_or_f: t.take("P_OR_F").and_then(|c| c.into_float()).unwrap_or_default(),
            effd: t.take("EFFD").and_then(|c| c.into_float()).unwrap_or_default(),
            gamsc: t.take("GAMSC").and_then(|c| c.into_float()).unwrap_or_default(),
            erodet: t.take("ERODET").and_then(|c| c.into_float()).unwrap_or_default(),
            erodec: t.take("ERODEC").and_then(|c| c.into_float()).unwrap_or_default(),
            eroder: t.take("ERODER").and_then(|c| c.into_float()).unwrap_or_default(),
            tmpoff: t.take("TMPOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1_6: t.take("EC1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            ecsp69: t.take("ECSP69").and_then(|c| c.into_float()).unwrap_or_default(),
            gamce9: t.take("GAMCE9").and_then(|c| c.into_float()).unwrap_or_default(),
            phief9: t.take("PHIEF9").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_173 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub f40: Vec<i64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub novoid: Vec<i64>,
    pub nplanes: Vec<i64>,
    pub extra: Vec<i64>,
    pub lccpdr: Vec<i64>,
    pub lccpt: Vec<i64>,
    pub lccjdr: Vec<i64>,
    pub lccjt: Vec<i64>,
    pub lcsfac: Vec<i64>,
    pub gmoddp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub lcgmep: Vec<i64>,
    pub lcphiep: Vec<i64>,
    pub lcpsiep: Vec<i64>,
    pub lcgmst: Vec<i64>,
    pub cvalgr: Vec<f64>,
    pub aniso: Vec<f64>,
    pub lcgmt: Vec<f64>,
    pub lccvt: Vec<f64>,
    pub lcpht: Vec<f64>,
    pub epdam1: Vec<f64>,
    pub epdam2: Vec<f64>,
    pub dip: Vec<f64>,
    pub dipang: Vec<f64>,
    pub cplane: Vec<f64>,
    pub frplane: Vec<f64>,
    pub tplane: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub local: Vec<i64>,
}
impl MAT_173 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_173").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            novoid: t.take("NOVOID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplanes: t.take("NPLANES").and_then(|c| c.into_int()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpdr: t.take("LCCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpt: t.take("LCCPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjdr: t.take("LCCJDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjt: t.take("LCCJT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsfac: t.take("LCSFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcgmep: t.take("LCGMEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcphiep: t.take("LCPHIEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsiep: t.take("LCPSIEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgmst: t.take("LCGMST").and_then(|c| c.into_int()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            aniso: t.take("ANISO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcgmt: t.take("LCGMT").and_then(|c| c.into_float()).unwrap_or_default(),
            lccvt: t.take("LCCVT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpht: t.take("LCPHT").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam1: t.take("EPDAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam2: t.take("EPDAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dip: t.take("DIP").and_then(|c| c.into_float()).unwrap_or_default(),
            dipang: t.take("DIPANG").and_then(|c| c.into_float()).unwrap_or_default(),
            cplane: t.take("CPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            frplane: t.take("FRPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            tplane: t.take("TPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_174 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub eunl: Vec<f64>,
    pub pr: Vec<f64>,
    pub fc: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec50: Vec<f64>,
    pub resid: Vec<f64>,
    pub ft: Vec<f64>,
    pub unitc: Vec<f64>,
    pub f20: Vec<f64>,
    pub f30: Vec<f64>,
    pub f40: Vec<f64>,
    pub esoft: Vec<f64>,
    pub lchar: Vec<f64>,
    pub output: Vec<f64>,
    pub fracr: Vec<f64>,
    pub ymreinf: Vec<f64>,
    pub prreinf: Vec<f64>,
    pub syreinf: Vec<f64>,
    pub sureinf: Vec<f64>,
    pub eshr: Vec<f64>,
    pub eur: Vec<f64>,
    pub rreinf: Vec<f64>,
}
impl MAT_174 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_174").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            eunl: t.take("EUNL").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec50: t.take("EC50").and_then(|c| c.into_float()).unwrap_or_default(),
            resid: t.take("RESID").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_float()).unwrap_or_default(),
            esoft: t.take("ESOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchar: t.take("LCHAR").and_then(|c| c.into_float()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_float()).unwrap_or_default(),
            fracr: t.take("FRACR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymreinf: t.take("YMREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            prreinf: t.take("PRREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            syreinf: t.take("SYREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            sureinf: t.take("SUREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            eshr: t.take("ESHR").and_then(|c| c.into_float()).unwrap_or_default(),
            eur: t.take("EUR").and_then(|c| c.into_float()).unwrap_or_default(),
            rreinf: t.take("RREINF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_175 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_175 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_175").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_176 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub n: Vec<f64>,
    pub gstart: Vec<f64>,
    pub m: Vec<f64>,
    pub so: Vec<f64>,
    pub e_min: Vec<f64>,
    pub e_max: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub k_2: Vec<f64>,
    pub eh: Vec<f64>,
    pub form: Vec<i64>,
    pub g1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub g2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub g3: Vec<f64>,
    pub beta3: Vec<f64>,
    pub g4: Vec<f64>,
    pub beta4: Vec<f64>,
    pub g5: Vec<f64>,
    pub beta5: Vec<f64>,
    pub g6: Vec<f64>,
    pub beta6: Vec<f64>,
    pub g7: Vec<f64>,
    pub beta7: Vec<f64>,
    pub g8: Vec<f64>,
    pub beta8: Vec<f64>,
    pub g9: Vec<f64>,
    pub beta9: Vec<f64>,
    pub g10: Vec<f64>,
    pub beta10: Vec<f64>,
    pub g11: Vec<f64>,
    pub beta11: Vec<f64>,
    pub g12: Vec<f64>,
    pub beta12: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
}
impl MAT_176 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_176").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gstart: t.take("GSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
            e_min: t.take("E_MIN").and_then(|c| c.into_float()).unwrap_or_default(),
            e_max: t.take("E_MAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            k_2: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta3: t.take("BETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta4: t.take("BETA4").and_then(|c| c.into_float()).unwrap_or_default(),
            g5: t.take("G5").and_then(|c| c.into_float()).unwrap_or_default(),
            beta5: t.take("BETA5").and_then(|c| c.into_float()).unwrap_or_default(),
            g6: t.take("G6").and_then(|c| c.into_float()).unwrap_or_default(),
            beta6: t.take("BETA6").and_then(|c| c.into_float()).unwrap_or_default(),
            g7: t.take("G7").and_then(|c| c.into_float()).unwrap_or_default(),
            beta7: t.take("BETA7").and_then(|c| c.into_float()).unwrap_or_default(),
            g8: t.take("G8").and_then(|c| c.into_float()).unwrap_or_default(),
            beta8: t.take("BETA8").and_then(|c| c.into_float()).unwrap_or_default(),
            g9: t.take("G9").and_then(|c| c.into_float()).unwrap_or_default(),
            beta9: t.take("BETA9").and_then(|c| c.into_float()).unwrap_or_default(),
            g10: t.take("G10").and_then(|c| c.into_float()).unwrap_or_default(),
            beta10: t.take("BETA10").and_then(|c| c.into_float()).unwrap_or_default(),
            g11: t.take("G11").and_then(|c| c.into_float()).unwrap_or_default(),
            beta11: t.take("BETA11").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            beta12: t.take("BETA12").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_177 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub nu: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fittype: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub r: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_177 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_177").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fittype: t.take("FITTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_178 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub nu: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fittype: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub lcve: Vec<i64>,
    pub nt: Vec<f64>,
    pub gstart: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_178 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_178").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fittype: t.take("FITTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcve: t.take("LCVE").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gstart: t.take("GSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_179 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tc: Vec<f64>,
    pub rflag: Vec<f64>,
    pub dtrt: Vec<f64>,
}
impl MAT_179 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_179").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            dtrt: t.take("DTRT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_180 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_180 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_180").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_181 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub k: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub eh: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_181 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_181").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_183 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<f64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub lcunld: Vec<i64>,
}
impl MAT_183 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_183").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_float()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_184 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub et: Vec<f64>,
    pub en: Vec<f64>,
    pub fn_fail: Vec<f64>,
    pub ft_fail: Vec<f64>,
}
impl MAT_184 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_184").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_fail: t.take("FN_FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ft_fail: t.take("FT_FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_185 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<f64>,
    pub intfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub nls: Vec<f64>,
    pub tls: Vec<f64>,
    pub tls2: Vec<f64>,
    pub lamda1: Vec<f64>,
    pub lamda2: Vec<f64>,
    pub lamdaf: Vec<f64>,
    pub stfsf: Vec<f64>,
    pub isw: Vec<i64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
}
impl MAT_185 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_185").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nls: t.take("NLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tls: t.take("TLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tls2: t.take("TLS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda1: t.take("LAMDA1").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda2: t.take("LAMDA2").and_then(|c| c.into_float()).unwrap_or_default(),
            lamdaf: t.take("LAMDAF").and_then(|c| c.into_float()).unwrap_or_default(),
            stfsf: t.take("STFSF").and_then(|c| c.into_float()).unwrap_or_default(),
            isw: t.take("ISW").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_186 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub tes: Vec<f64>,
    pub tslc: Vec<i64>,
    pub gic: Vec<f64>,
    pub giic: Vec<f64>,
    pub xmu: Vec<f64>,
    pub t: Vec<f64>,
    pub s: Vec<f64>,
    pub stfsf: Vec<f64>,
    pub tslc2: Vec<f64>,
}
impl MAT_186 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_186").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tes: t.take("TES").and_then(|c| c.into_float()).unwrap_or_default(),
            tslc: t.take("TSLC").and_then(|c| c.into_int()).unwrap_or_default(),
            gic: t.take("GIC").and_then(|c| c.into_float()).unwrap_or_default(),
            giic: t.take("GIIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xmu: t.take("XMU").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            stfsf: t.take("STFSF").and_then(|c| c.into_float()).unwrap_or_default(),
            tslc2: t.take("TSLC2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_187 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub gmod: Vec<f64>,
    pub emod: Vec<f64>,
    pub nue: Vec<f64>,
    pub rbcfac: Vec<f64>,
    pub numint: Vec<i64>,
    pub lcid_t: Vec<i64>,
    pub lcid_c: Vec<i64>,
    pub lcid_s: Vec<i64>,
    pub lcid_b: Vec<i64>,
    pub nuep: Vec<f64>,
    pub lcid_p: Vec<i64>,
    pub unused: Vec<i64>,
    pub incdam: Vec<i64>,
    pub lcid_d: Vec<i64>,
    pub epfail: Vec<f64>,
    pub deprpt: Vec<f64>,
    pub lcid_tri: Vec<i64>,
    pub lcid_lc: Vec<i64>,
    pub miter: Vec<i64>,
    pub mipds: Vec<i64>,
    pub f20: Vec<i64>,
    pub incfail: Vec<i64>,
    pub iconv: Vec<i64>,
    pub asaf: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub filt: Vec<f64>,
}
impl MAT_187 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_187").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            nue: t.take("NUE").and_then(|c| c.into_float()).unwrap_or_default(),
            rbcfac: t.take("RBCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_t: t.take("LCID-T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_c: t.take("LCID-C").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_s: t.take("LCID-S").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_b: t.take("LCID-B").and_then(|c| c.into_int()).unwrap_or_default(),
            nuep: t.take("NUEP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_p: t.take("LCID-P").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            incdam: t.take("INCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_d: t.take("LCID_D").and_then(|c| c.into_int()).unwrap_or_default(),
            epfail: t.take("EPFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            deprpt: t.take("DEPRPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_tri: t.take("LCID-TRI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_lc: t.take("LCID_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            miter: t.take("MITER").and_then(|c| c.into_int()).unwrap_or_default(),
            mipds: t.take("MIPDS").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            incfail: t.take("INCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            iconv: t.take("ICONV").and_then(|c| c.into_int()).unwrap_or_default(),
            asaf: t.take("ASAF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            filt: t.take("FILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_187L {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub emod: Vec<f64>,
    pub nue: Vec<f64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub lcid_t: Vec<i64>,
    pub lcid_c: Vec<i64>,
    pub ctflg: Vec<i64>,
    pub rateop: Vec<i64>,
    pub nuep: Vec<f64>,
    pub lcid_p: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_187L {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_187L").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            nue: t.take("NUE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_t: t.take("LCID_T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_c: t.take("LCID_C").and_then(|c| c.into_int()).unwrap_or_default(),
            ctflg: t.take("CTFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            rateop: t.take("RATEOP").and_then(|c| c.into_int()).unwrap_or_default(),
            nuep: t.take("NUEP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_p: t.take("LCID-P").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_188 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub reftem: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lcsigy: Vec<f64>,
    pub lcqr: Vec<f64>,
    pub lcqx: Vec<f64>,
    pub lcalph: Vec<f64>,
    pub lcc: Vec<f64>,
    pub lcp: Vec<f64>,
    pub lccr: Vec<f64>,
    pub lccx: Vec<f64>,
    pub crpa: Vec<f64>,
    pub crpb: Vec<f64>,
    pub crpq: Vec<f64>,
    pub crpm: Vec<f64>,
    pub crplaw: Vec<f64>,
}
impl MAT_188 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_188").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            reftem: t.take("REFTEM").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcqr: t.take("LCQR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcqx: t.take("LCQX").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalph: t.take("LCALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_float()).unwrap_or_default(),
            lccr: t.take("LCCR").and_then(|c| c.into_float()).unwrap_or_default(),
            lccx: t.take("LCCX").and_then(|c| c.into_float()).unwrap_or_default(),
            crpa: t.take("CRPA").and_then(|c| c.into_float()).unwrap_or_default(),
            crpb: t.take("CRPB").and_then(|c| c.into_float()).unwrap_or_default(),
            crpq: t.take("CRPQ").and_then(|c| c.into_float()).unwrap_or_default(),
            crpm: t.take("CRPM").and_then(|c| c.into_float()).unwrap_or_default(),
            crplaw: t.take("CRPLAW").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_189 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ta1: Vec<i64>,
    pub ta2: Vec<i64>,
    pub ta3: Vec<i64>,
    pub ta4: Vec<i64>,
    pub ta5: Vec<i64>,
    pub ta6: Vec<i64>,
    pub c11: Vec<i64>,
    pub c12: Vec<i64>,
    pub c13: Vec<i64>,
    pub c14: Vec<i64>,
    pub c15: Vec<i64>,
    pub c16: Vec<i64>,
    pub c22: Vec<i64>,
    pub c23: Vec<i64>,
    pub c24: Vec<i64>,
    pub c25: Vec<i64>,
    pub c26: Vec<i64>,
    pub c33: Vec<i64>,
    pub c34: Vec<i64>,
    pub c35: Vec<i64>,
    pub c36: Vec<i64>,
    pub c44: Vec<i64>,
    pub c45: Vec<i64>,
    pub c46: Vec<i64>,
    pub c55: Vec<i64>,
    pub c56: Vec<i64>,
    pub c66: Vec<i64>,
    pub tge: Vec<i64>,
    pub tref: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_189 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_189").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ta1: t.take("TA1").and_then(|c| c.into_int()).unwrap_or_default(),
            ta2: t.take("TA2").and_then(|c| c.into_int()).unwrap_or_default(),
            ta3: t.take("TA3").and_then(|c| c.into_int()).unwrap_or_default(),
            ta4: t.take("TA4").and_then(|c| c.into_int()).unwrap_or_default(),
            ta5: t.take("TA5").and_then(|c| c.into_int()).unwrap_or_default(),
            ta6: t.take("TA6").and_then(|c| c.into_int()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_int()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_int()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_int()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_int()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_int()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_int()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_int()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_int()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_int()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_int()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_int()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_int()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_int()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_int()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_int()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_int()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_int()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_int()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_int()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_int()).unwrap_or_default(),
            tge: t.take("TGE").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_190 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fldcid: Vec<i64>,
    pub rn: Vec<f64>,
    pub rt: Vec<f64>,
    pub fldsafe: Vec<f64>,
    pub fldnipf: Vec<i64>,
    pub f0: Vec<i64>,
    pub f10: Vec<i64>,
    pub f20: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_190 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_190").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fldcid: t.take("FLDCID").and_then(|c| c.into_int()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            rt: t.take("RT").and_then(|c| c.into_float()).unwrap_or_default(),
            fldsafe: t.take("FLDSAFE").and_then(|c| c.into_float()).unwrap_or_default(),
            fldnipf: t.take("FLDNIPF").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_191 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub asflag: Vec<f64>,
    pub ftype: Vec<i64>,
    pub degrad: Vec<i64>,
    pub ifema: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub sfs: Vec<f64>,
    pub lcpmt: Vec<i64>,
    pub sft: Vec<f64>,
    pub lcat: Vec<i64>,
    pub sfat: Vec<f64>,
    pub lcac: Vec<i64>,
    pub sfac: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub delta: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub foffs: Vec<f64>,
    pub f: Vec<f64>,
    pub sigy: Vec<f64>,
    pub d: Vec<f64>,
    pub w: Vec<f64>,
    pub tf: Vec<f64>,
    pub tw: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub phi_t: Vec<f64>,
    pub phi_c: Vec<f64>,
    pub phi_b: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_2_3: Vec<f64>,
    pub f_3_2: Vec<f64>,
    pub f_4_2: Vec<f64>,
    pub f_5: Vec<f64>,
}
impl MAT_191 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_191").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            asflag: t.take("ASFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            degrad: t.take("DEGRAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ifema: t.take("IFEMA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfat: t.take("SFAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
            sfac: t.take("SFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            delta: t.take("DELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            foffs: t.take("FOFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
            tw: t.take("TW").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_t: t.take("PHI_T").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_c: t.take("PHI_C").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_b: t.take("PHI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4_2: t.take("-_4").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_192 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub rlamda: Vec<f64>,
    pub rkappa: Vec<f64>,
    pub riota: Vec<f64>,
    pub rbeta1: Vec<f64>,
    pub rbeta2: Vec<f64>,
    pub rmu: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rlcid: Vec<f64>,
    pub tol: Vec<f64>,
    pub pgcl: Vec<f64>,
    pub sub_inc: Vec<f64>,
    pub blk: Vec<f64>,
    pub grav: Vec<f64>,
    pub theory: Vec<i64>,
}
impl MAT_192 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_192").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            rlamda: t.take("RLAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rkappa: t.take("RKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            riota: t.take("RIOTA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta1: t.take("RBETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta2: t.take("RBETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            rmu: t.take("RMU").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rlcid: t.take("RLCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            pgcl: t.take("PGCL").and_then(|c| c.into_float()).unwrap_or_default(),
            sub_inc: t.take("SUB-INC").and_then(|c| c.into_float()).unwrap_or_default(),
            blk: t.take("BLK").and_then(|c| c.into_float()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            theory: t.take("THEORY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_193 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rkf: Vec<f64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub str_lim: Vec<f64>,
    pub gmoddp: Vec<f64>,
    pub phidp: Vec<f64>,
    pub cvaldp: Vec<f64>,
    pub psidp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub phigr: Vec<f64>,
    pub cvalgr: Vec<f64>,
    pub psigr: Vec<f64>,
}
impl MAT_193 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_193").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rkf: t.take("RKF").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            str_lim: t.take("STR_LIM").and_then(|c| c.into_float()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            phidp: t.take("PHIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvaldp: t.take("CVALDP").and_then(|c| c.into_float()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            phigr: t.take("PHIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            psigr: t.take("PSIGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_194 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tmax: Vec<f64>,
    pub fc: Vec<f64>,
    pub pref: Vec<f64>,
    pub fyield: Vec<f64>,
    pub sig0: Vec<f64>,
    pub unconv: Vec<f64>,
    pub alpha: Vec<f64>,
    pub ft: Vec<f64>,
    pub erienf: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub e_2: Vec<f64>,
    pub f: Vec<f64>,
    pub y1: Vec<f64>,
    pub y2: Vec<f64>,
    pub y3: Vec<f64>,
    pub y4: Vec<f64>,
    pub y5: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_194 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_194").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC'").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            fyield: t.take("FYIELD").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            unconv: t.take("UNCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            erienf: t.take("ERIENF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            e_2: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_195 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub noten: Vec<i64>,
    pub tencut: Vec<f64>,
    pub sdr: Vec<f64>,
}
impl MAT_195 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_195").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            noten: t.take("NOTEN").and_then(|c| c.into_int()).unwrap_or_default(),
            tencut: t.take("TENCUT").and_then(|c| c.into_float()).unwrap_or_default(),
            sdr: t.take("SDR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_196 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub dospot: Vec<i64>,
    pub dof: Vec<i64>,
    pub type_: Vec<i64>,
    pub k: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_196 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_196").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            dospot: t.take("DOSPOT").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_197 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub a: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub dispy: Vec<f64>,
    pub stiffv: Vec<f64>,
    pub itype: Vec<i64>,
    pub preload: Vec<f64>,
    pub damp: Vec<f64>,
    pub mx1: Vec<f64>,
    pub mx2: Vec<f64>,
    pub my1: Vec<f64>,
    pub my2: Vec<f64>,
    pub cde: Vec<f64>,
    pub iextra: Vec<i64>,
    pub fmax: Vec<f64>,
    pub delf: Vec<f64>,
    pub afric: Vec<f64>,
    pub radx: Vec<f64>,
    pub rady: Vec<f64>,
    pub radb: Vec<f64>,
    pub stiffl: Vec<f64>,
    pub stiffts: Vec<f64>,
    pub forcey: Vec<f64>,
    pub alpha: Vec<f64>,
    pub stifft: Vec<f64>,
    pub dfail: Vec<f64>,
    pub fmaxyc: Vec<f64>,
    pub fmaxxt: Vec<f64>,
    pub fmaxyt: Vec<f64>,
    pub ylock: Vec<f64>,
}
impl MAT_197 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_197").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            dispy: t.take("DISPY").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffv: t.take("STIFFV").and_then(|c| c.into_float()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            preload: t.take("PRELOAD").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            mx1: t.take("MX1").and_then(|c| c.into_float()).unwrap_or_default(),
            mx2: t.take("MX2").and_then(|c| c.into_float()).unwrap_or_default(),
            my1: t.take("MY1").and_then(|c| c.into_float()).unwrap_or_default(),
            my2: t.take("MY2").and_then(|c| c.into_float()).unwrap_or_default(),
            cde: t.take("CDE").and_then(|c| c.into_float()).unwrap_or_default(),
            iextra: t.take("IEXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            delf: t.take("DELF").and_then(|c| c.into_float()).unwrap_or_default(),
            afric: t.take("AFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radx: t.take("RADX").and_then(|c| c.into_float()).unwrap_or_default(),
            rady: t.take("RADY").and_then(|c| c.into_float()).unwrap_or_default(),
            radb: t.take("RADB").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffl: t.take("STIFFL").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffts: t.take("STIFFTS").and_then(|c| c.into_float()).unwrap_or_default(),
            forcey: t.take("FORCEY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            stifft: t.take("STIFFT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfail: t.take("DFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxyc: t.take("FMAXYC").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxxt: t.take("FMAXXT").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxyt: t.take("FMAXYT").and_then(|c| c.into_float()).unwrap_or_default(),
            ylock: t.take("YLOCK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_198 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rkf: Vec<f64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub str_lim: Vec<f64>,
    pub nplanes: Vec<i64>,
    pub elastic: Vec<i64>,
    pub lccpdr: Vec<i64>,
    pub lccpt: Vec<i64>,
    pub lccjdr: Vec<i64>,
    pub lccjt: Vec<i64>,
    pub lcsfac: Vec<i64>,
    pub gmoddp: Vec<f64>,
    pub phidp: Vec<f64>,
    pub cvaldp: Vec<f64>,
    pub psidp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub phigr: Vec<f64>,
    pub cvalgr: Vec<f64>,
    pub psigr: Vec<f64>,
    pub dip: Vec<f64>,
    pub strike: Vec<f64>,
    pub cplane: Vec<f64>,
    pub frplane: Vec<f64>,
    pub tplane: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub local: Vec<f64>,
}
impl MAT_198 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_198").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rkf: t.take("RKF").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            str_lim: t.take("STR_LIM").and_then(|c| c.into_float()).unwrap_or_default(),
            nplanes: t.take("NPLANES").and_then(|c| c.into_int()).unwrap_or_default(),
            elastic: t.take("ELASTIC").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpdr: t.take("LCCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpt: t.take("LCCPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjdr: t.take("LCCJDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjt: t.take("LCCJT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsfac: t.take("LCSFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            phidp: t.take("PHIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvaldp: t.take("CVALDP").and_then(|c| c.into_float()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            phigr: t.take("PHIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            psigr: t.take("PSIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            dip: t.take("DIP").and_then(|c| c.into_float()).unwrap_or_default(),
            strike: t.take("STRIKE").and_then(|c| c.into_float()).unwrap_or_default(),
            cplane: t.take("CPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            frplane: t.take("FRPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            tplane: t.take("TPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_199 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp12: Vec<f64>,
    pub cp13: Vec<f64>,
    pub cp21: Vec<f64>,
    pub cp23: Vec<f64>,
    pub cp31: Vec<f64>,
    pub cp32: Vec<f64>,
    pub cpp12: Vec<f64>,
    pub cpp13: Vec<f64>,
    pub cpp21: Vec<f64>,
    pub cpp23: Vec<f64>,
    pub cpp31: Vec<f64>,
    pub cpp32: Vec<f64>,
    pub cp44: Vec<f64>,
    pub cp55: Vec<f64>,
    pub cp66: Vec<f64>,
    pub cpp44: Vec<f64>,
    pub cpp55: Vec<f64>,
    pub cpp66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub a: Vec<f64>,
    pub lcss: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_199 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_199").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp12: t.take("CP12").and_then(|c| c.into_float()).unwrap_or_default(),
            cp13: t.take("CP13").and_then(|c| c.into_float()).unwrap_or_default(),
            cp21: t.take("CP21").and_then(|c| c.into_float()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            cp31: t.take("CP31").and_then(|c| c.into_float()).unwrap_or_default(),
            cp32: t.take("CP32").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp12: t.take("CPP12").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp13: t.take("CPP13").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp21: t.take("CPP21").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp23: t.take("CPP23").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp31: t.take("CPP31").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp32: t.take("CPP32").and_then(|c| c.into_float()).unwrap_or_default(),
            cp44: t.take("CP44").and_then(|c| c.into_float()).unwrap_or_default(),
            cp55: t.take("CP55").and_then(|c| c.into_float()).unwrap_or_default(),
            cp66: t.take("CP66").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp44: t.take("CPP44").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp55: t.take("CPP55").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp66: t.take("CPP66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_1DOF_GENERALIZED_SPRING {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub scln1: Vec<f64>,
    pub scln2: Vec<f64>,
    pub dofn1: Vec<i64>,
    pub dofn2: Vec<i64>,
    pub cid1: Vec<i64>,
    pub cid2: Vec<i64>,
}
impl MAT_1DOF_GENERALIZED_SPRING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_1DOF_GENERALIZED_SPRING").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            scln1: t.take("SCLN1").and_then(|c| c.into_float()).unwrap_or_default(),
            scln2: t.take("SCLN2").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_int()).unwrap_or_default(),
            cid1: t.take("CID1").and_then(|c| c.into_int()).unwrap_or_default(),
            cid2: t.take("CID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_202 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sgy: Vec<f64>,
    pub lc_e: Vec<i64>,
    pub lc_pr: Vec<i64>,
    pub lc_al: Vec<i64>,
    pub tbl_ss: Vec<i64>,
    pub lc_fs: Vec<i64>,
}
impl MAT_202 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_202").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sgy: t.take("SGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_e: t.take("LC_E").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_pr: t.take("LC_PR").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_al: t.take("LC_AL").and_then(|c| c.into_int()).unwrap_or_default(),
            tbl_ss: t.take("TBL_SS").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_fs: t.take("LC_FS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_203 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lamda: Vec<f64>,
    pub sbuck: Vec<f64>,
    pub power: Vec<f64>,
    pub fracx: Vec<f64>,
    pub fracy: Vec<f64>,
    pub lcten: Vec<f64>,
    pub lccomp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ebu: Vec<f64>,
    pub downsl: Vec<f64>,
    pub dbar: Vec<f64>,
    pub fcdow: Vec<f64>,
    pub lchard: Vec<f64>,
    pub unitc: Vec<f64>,
    pub unitl: Vec<f64>,
    pub epdam1: Vec<f64>,
    pub epdam2: Vec<f64>,
    pub dresid: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_203 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_203").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda: t.take("LAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            sbuck: t.take("SBUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            power: t.take("POWER").and_then(|c| c.into_float()).unwrap_or_default(),
            fracx: t.take("FRACX").and_then(|c| c.into_float()).unwrap_or_default(),
            fracy: t.take("FRACY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcten: t.take("LCTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            lccomp: t.take("LCCOMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ebu: t.take("EBU").and_then(|c| c.into_float()).unwrap_or_default(),
            downsl: t.take("DOWNSL").and_then(|c| c.into_float()).unwrap_or_default(),
            dbar: t.take("DBAR").and_then(|c| c.into_float()).unwrap_or_default(),
            fcdow: t.take("FCDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            lchard: t.take("LCHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            unitl: t.take("UNITL").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam1: t.take("EPDAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam2: t.take("EPDAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dresid: t.take("DRESID").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_205 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub stiff: Vec<f64>,
    pub fric: Vec<f64>,
    pub damp: Vec<f64>,
    pub dmxpz: Vec<f64>,
    pub limpz: Vec<f64>,
    pub dmxpx: Vec<f64>,
    pub dmxnx: Vec<f64>,
    pub dmxpy: Vec<f64>,
    pub dmxny: Vec<f64>,
    pub limpx: Vec<f64>,
    pub limnx: Vec<f64>,
    pub limpy: Vec<f64>,
    pub limny: Vec<f64>,
    pub krotx: Vec<f64>,
    pub kroty: Vec<f64>,
    pub krotz: Vec<f64>,
    pub tkrot: Vec<f64>,
    pub fbondh: Vec<f64>,
    pub fbondt: Vec<f64>,
    pub dbondh: Vec<f64>,
    pub dbondt: Vec<f64>,
    pub lcz: Vec<i64>,
    pub dampz: Vec<f64>,
    pub stiffh: Vec<f64>,
    pub frmax: Vec<f64>,
    pub damph: Vec<f64>,
    pub gap0: Vec<f64>,
    pub afac: Vec<f64>,
}
impl MAT_205 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_205").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpz: t.take("DMXPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            limpz: t.take("LIMPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpx: t.take("DMXPX").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxnx: t.take("DMXNX").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpy: t.take("DMXPY").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxny: t.take("DMXNY").and_then(|c| c.into_float()).unwrap_or_default(),
            limpx: t.take("LIMPX").and_then(|c| c.into_float()).unwrap_or_default(),
            limnx: t.take("LIMNX").and_then(|c| c.into_float()).unwrap_or_default(),
            limpy: t.take("LIMPY").and_then(|c| c.into_float()).unwrap_or_default(),
            limny: t.take("LIMNY").and_then(|c| c.into_float()).unwrap_or_default(),
            krotx: t.take("KROTX").and_then(|c| c.into_float()).unwrap_or_default(),
            kroty: t.take("KROTY").and_then(|c| c.into_float()).unwrap_or_default(),
            krotz: t.take("KROTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tkrot: t.take("TKROT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbondh: t.take("FBONDH").and_then(|c| c.into_float()).unwrap_or_default(),
            fbondt: t.take("FBONDT").and_then(|c| c.into_float()).unwrap_or_default(),
            dbondh: t.take("DBONDH").and_then(|c| c.into_float()).unwrap_or_default(),
            dbondt: t.take("DBONDT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcz: t.take("LCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dampz: t.take("DAMPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffh: t.take("STIFFH").and_then(|c| c.into_float()).unwrap_or_default(),
            frmax: t.take("FRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            damph: t.take("DAMPH").and_then(|c| c.into_float()).unwrap_or_default(),
            gap0: t.take("GAP0").and_then(|c| c.into_float()).unwrap_or_default(),
            afac: t.take("AFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_208 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub kax: Vec<f64>,
    pub kshr: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub fpre: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcax: Vec<i64>,
    pub lcshr: Vec<i64>,
    pub fric: Vec<f64>,
    pub clear: Vec<f64>,
    pub dafail: Vec<f64>,
    pub drfail: Vec<f64>,
    pub damag: Vec<f64>,
    pub t0pre: Vec<f64>,
    pub dacfail: Vec<f64>,
    pub axshel: Vec<i64>,
    pub holshr: Vec<i64>,
}
impl MAT_208 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_208").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            kax: t.take("KAX").and_then(|c| c.into_float()).unwrap_or_default(),
            kshr: t.take("KSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            fpre: t.take("FPRE").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcax: t.take("LCAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcshr: t.take("LCSHR").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            clear: t.take("CLEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            dafail: t.take("DAFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            drfail: t.take("DRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damag: t.take("DAMAG").and_then(|c| c.into_float()).unwrap_or_default(),
            t0pre: t.take("T0PRE").and_then(|c| c.into_float()).unwrap_or_default(),
            dacfail: t.take("DACFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            axshel: t.take("AXSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            holshr: t.take("HOLSHR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_209 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub iax: Vec<i64>,
    pub isurf: Vec<i64>,
    pub ihard: Vec<i64>,
    pub ifema: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub sfs: Vec<f64>,
    pub lcpmt: Vec<i64>,
    pub sft: Vec<f64>,
    pub lcat: Vec<i64>,
    pub sfat: Vec<f64>,
    pub lcac: Vec<i64>,
    pub sfac: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub f0: Vec<f64>,
    pub pinm: Vec<f64>,
    pub pins: Vec<f64>,
    pub hloc1: Vec<f64>,
    pub hloc2: Vec<f64>,
    pub deltas: Vec<f64>,
    pub kappas: Vec<f64>,
    pub deltat: Vec<f64>,
    pub kappat: Vec<f64>,
    pub lcshs: Vec<i64>,
    pub sfshs: Vec<f64>,
    pub lcsht: Vec<i64>,
    pub sfsht: Vec<f64>,
    pub hardms: Vec<f64>,
    pub gamms: Vec<f64>,
    pub hardmt: Vec<f64>,
    pub gammt: Vec<f64>,
    pub hardat: Vec<f64>,
    pub gamat: Vec<f64>,
    pub hardac: Vec<f64>,
    pub gamac: Vec<f64>,
    pub omgms1: Vec<f64>,
    pub omgms2: Vec<f64>,
    pub omgmt1: Vec<f64>,
    pub omgmt2: Vec<f64>,
    pub omgat1: Vec<f64>,
    pub omgat2: Vec<f64>,
    pub omgac1: Vec<f64>,
    pub omgac2: Vec<f64>,
    pub rums: Vec<f64>,
    pub rumt: Vec<f64>,
    pub duat: Vec<f64>,
    pub duac: Vec<f64>,
    pub lam1: Vec<f64>,
    pub lam2: Vec<f64>,
    pub soft1: Vec<f64>,
    pub soft2: Vec<f64>,
    pub prs1: Vec<f64>,
    pub prs2: Vec<f64>,
    pub prs3: Vec<f64>,
    pub prs4: Vec<f64>,
    pub prt1: Vec<f64>,
    pub prt2: Vec<f64>,
    pub prt3: Vec<f64>,
    pub prt4: Vec<f64>,
    pub ts1: Vec<f64>,
    pub ts2: Vec<f64>,
    pub ts3: Vec<f64>,
    pub ts4: Vec<f64>,
    pub cs1: Vec<f64>,
    pub cs2: Vec<f64>,
    pub cs3: Vec<f64>,
    pub cs4: Vec<f64>,
    pub ss1: Vec<f64>,
    pub ss2: Vec<f64>,
    pub ss3: Vec<f64>,
    pub ss4: Vec<f64>,
    pub st1: Vec<f64>,
    pub st2: Vec<f64>,
    pub st3: Vec<f64>,
    pub st4: Vec<f64>,
}
impl MAT_209 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_209").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            iax: t.take("IAX").and_then(|c| c.into_int()).unwrap_or_default(),
            isurf: t.take("ISURF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihard: t.take("IHARD").and_then(|c| c.into_int()).unwrap_or_default(),
            ifema: t.take("IFEMA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfat: t.take("SFAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
            sfac: t.take("SFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            pinm: t.take("PINM").and_then(|c| c.into_float()).unwrap_or_default(),
            pins: t.take("PINS").and_then(|c| c.into_float()).unwrap_or_default(),
            hloc1: t.take("HLOC1").and_then(|c| c.into_float()).unwrap_or_default(),
            hloc2: t.take("HLOC2").and_then(|c| c.into_float()).unwrap_or_default(),
            deltas: t.take("DELTAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kappas: t.take("KAPPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            deltat: t.take("DELTAT").and_then(|c| c.into_float()).unwrap_or_default(),
            kappat: t.take("KAPPAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcshs: t.take("LCSHS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfshs: t.take("SFSHS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsht: t.take("LCSHT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfsht: t.take("SFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardms: t.take("HARDMS").and_then(|c| c.into_float()).unwrap_or_default(),
            gamms: t.take("GAMMS").and_then(|c| c.into_float()).unwrap_or_default(),
            hardmt: t.take("HARDMT").and_then(|c| c.into_float()).unwrap_or_default(),
            gammt: t.take("GAMMT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardat: t.take("HARDAT").and_then(|c| c.into_float()).unwrap_or_default(),
            gamat: t.take("GAMAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardac: t.take("HARDAC").and_then(|c| c.into_float()).unwrap_or_default(),
            gamac: t.take("GAMAC").and_then(|c| c.into_float()).unwrap_or_default(),
            omgms1: t.take("OMGMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgms2: t.take("OMGMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmt1: t.take("OMGMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmt2: t.take("OMGMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgat1: t.take("OMGAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgat2: t.take("OMGAT2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgac1: t.take("OMGAC1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgac2: t.take("OMGAC2").and_then(|c| c.into_float()).unwrap_or_default(),
            rums: t.take("RUMS").and_then(|c| c.into_float()).unwrap_or_default(),
            rumt: t.take("RUMT").and_then(|c| c.into_float()).unwrap_or_default(),
            duat: t.take("DUAT").and_then(|c| c.into_float()).unwrap_or_default(),
            duac: t.take("DUAC").and_then(|c| c.into_float()).unwrap_or_default(),
            lam1: t.take("LAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            lam2: t.take("LAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            soft1: t.take("SOFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            soft2: t.take("SOFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            prs1: t.take("PRS1").and_then(|c| c.into_float()).unwrap_or_default(),
            prs2: t.take("PRS2").and_then(|c| c.into_float()).unwrap_or_default(),
            prs3: t.take("PRS3").and_then(|c| c.into_float()).unwrap_or_default(),
            prs4: t.take("PRS4").and_then(|c| c.into_float()).unwrap_or_default(),
            prt1: t.take("PRT1").and_then(|c| c.into_float()).unwrap_or_default(),
            prt2: t.take("PRT2").and_then(|c| c.into_float()).unwrap_or_default(),
            prt3: t.take("PRT3").and_then(|c| c.into_float()).unwrap_or_default(),
            prt4: t.take("PRT4").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts3: t.take("TS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ts4: t.take("TS4").and_then(|c| c.into_float()).unwrap_or_default(),
            cs1: t.take("CS1").and_then(|c| c.into_float()).unwrap_or_default(),
            cs2: t.take("CS2").and_then(|c| c.into_float()).unwrap_or_default(),
            cs3: t.take("CS3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs4: t.take("CS4").and_then(|c| c.into_float()).unwrap_or_default(),
            ss1: t.take("SS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ss2: t.take("SS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ss3: t.take("SS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ss4: t.take("SS4").and_then(|c| c.into_float()).unwrap_or_default(),
            st1: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            st2: t.take("ST2").and_then(|c| c.into_float()).unwrap_or_default(),
            st3: t.take("ST3").and_then(|c| c.into_float()).unwrap_or_default(),
            st4: t.take("ST4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_211 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub helas: Vec<f64>,
    pub telas: Vec<f64>,
    pub lcaxh: Vec<i64>,
    pub lcshh: Vec<i64>,
    pub lcbmh: Vec<i64>,
    pub sfaxh: Vec<f64>,
    pub sfshh: Vec<f64>,
    pub sfbmh: Vec<f64>,
    pub dfakh: Vec<f64>,
    pub dfshh: Vec<f64>,
    pub rfbmh: Vec<f64>,
    pub dmfaxh: Vec<f64>,
    pub dmfshh: Vec<f64>,
    pub dmfbmh: Vec<f64>,
    pub lcaxt: Vec<i64>,
    pub lcsht: Vec<i64>,
    pub lcbmt: Vec<i64>,
    pub sfaxt: Vec<f64>,
    pub sfsht: Vec<f64>,
    pub sbfmt: Vec<f64>,
    pub dfaxt: Vec<f64>,
    pub dfsht: Vec<f64>,
    pub rfbmt: Vec<f64>,
    pub dfmaxt: Vec<f64>,
    pub dmfsht: Vec<f64>,
    pub dmfbmt: Vec<f64>,
}
impl MAT_211 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_211").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            helas: t.take("HELAS").and_then(|c| c.into_float()).unwrap_or_default(),
            telas: t.take("TELAS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaxh: t.take("LCAXH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcshh: t.take("LCSHH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbmh: t.take("LCBMH").and_then(|c| c.into_int()).unwrap_or_default(),
            sfaxh: t.take("SFAXH").and_then(|c| c.into_float()).unwrap_or_default(),
            sfshh: t.take("SFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            sfbmh: t.take("SFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfakh: t.take("DFAKH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfshh: t.take("DFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            rfbmh: t.take("RFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfaxh: t.take("DMFAXH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfshh: t.take("DMFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfbmh: t.take("DMFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaxt: t.take("LCAXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsht: t.take("LCSHT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbmt: t.take("LCBMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfaxt: t.take("SFAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsht: t.take("SFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbfmt: t.take("SBFMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfaxt: t.take("DFAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfsht: t.take("DFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            rfbmt: t.take("RFBMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfmaxt: t.take("DFMAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfsht: t.take("DMFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfbmt: t.take("DMFBMT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_213 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub ptol: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub filt: Vec<f64>,
    pub vevp: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub tcsym: Vec<i64>,
    pub h11: Vec<f64>,
    pub h22: Vec<f64>,
    pub h33: Vec<f64>,
    pub h12: Vec<f64>,
    pub h23: Vec<f64>,
    pub h13: Vec<f64>,
    pub h44: Vec<f64>,
    pub h55: Vec<f64>,
    pub h66: Vec<f64>,
    pub lt1: Vec<i64>,
    pub lt2: Vec<i64>,
    pub lt3: Vec<i64>,
    pub lt4: Vec<i64>,
    pub lt5: Vec<i64>,
    pub lt6: Vec<i64>,
    pub lt7: Vec<i64>,
    pub lt8: Vec<i64>,
    pub lt9: Vec<i64>,
    pub lt10: Vec<i64>,
    pub lt11: Vec<i64>,
    pub lt12: Vec<i64>,
    pub ysc: Vec<i64>,
    pub dflag: Vec<i64>,
    pub dc: Vec<i64>,
    pub ftype: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub unused_6: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub f: Vec<i64>,
    pub unused_8: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5_2: Vec<i64>,
    pub unused_6_2: Vec<i64>,
    pub unused_7_2: Vec<i64>,
    pub beta11: Vec<f64>,
    pub beta22: Vec<f64>,
    pub beta33: Vec<f64>,
    pub beta44: Vec<f64>,
    pub beta55: Vec<f64>,
    pub beta66: Vec<f64>,
    pub beta12: Vec<f64>,
    pub beta23: Vec<f64>,
    pub beta13: Vec<f64>,
    pub cp: Vec<f64>,
    pub tqc: Vec<f64>,
    pub temp: Vec<f64>,
    pub pmacc: Vec<f64>,
}
impl MAT_213 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_213").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            ptol: t.take("PTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            filt: t.take("FILT").and_then(|c| c.into_float()).unwrap_or_default(),
            vevp: t.take("VEVP").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tcsym: t.take("TCSYM").and_then(|c| c.into_int()).unwrap_or_default(),
            h11: t.take("H11").and_then(|c| c.into_float()).unwrap_or_default(),
            h22: t.take("H22").and_then(|c| c.into_float()).unwrap_or_default(),
            h33: t.take("H33").and_then(|c| c.into_float()).unwrap_or_default(),
            h12: t.take("H12").and_then(|c| c.into_float()).unwrap_or_default(),
            h23: t.take("H23").and_then(|c| c.into_float()).unwrap_or_default(),
            h13: t.take("H13").and_then(|c| c.into_float()).unwrap_or_default(),
            h44: t.take("H44").and_then(|c| c.into_float()).unwrap_or_default(),
            h55: t.take("H55").and_then(|c| c.into_float()).unwrap_or_default(),
            h66: t.take("H66").and_then(|c| c.into_float()).unwrap_or_default(),
            lt1: t.take("LT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lt2: t.take("LT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lt3: t.take("LT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lt4: t.take("LT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lt5: t.take("LT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lt6: t.take("LT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lt7: t.take("LT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lt8: t.take("LT8").and_then(|c| c.into_int()).unwrap_or_default(),
            lt9: t.take("LT9").and_then(|c| c.into_int()).unwrap_or_default(),
            lt10: t.take("LT10").and_then(|c| c.into_int()).unwrap_or_default(),
            lt11: t.take("LT11").and_then(|c| c.into_int()).unwrap_or_default(),
            lt12: t.take("LT12").and_then(|c| c.into_int()).unwrap_or_default(),
            ysc: t.take("YSC").and_then(|c| c.into_int()).unwrap_or_default(),
            dflag: t.take("DFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED_7").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_8: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5_2: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6_2: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7_2: t.take("UNUSED_7").and_then(|c| c.into_int()).unwrap_or_default(),
            beta11: t.take("BETA11").and_then(|c| c.into_float()).unwrap_or_default(),
            beta22: t.take("BETA22").and_then(|c| c.into_float()).unwrap_or_default(),
            beta33: t.take("BETA33").and_then(|c| c.into_float()).unwrap_or_default(),
            beta44: t.take("BETA44").and_then(|c| c.into_float()).unwrap_or_default(),
            beta55: t.take("BETA55").and_then(|c| c.into_float()).unwrap_or_default(),
            beta66: t.take("BETA66").and_then(|c| c.into_float()).unwrap_or_default(),
            beta12: t.take("BETA12").and_then(|c| c.into_float()).unwrap_or_default(),
            beta23: t.take("BETA23").and_then(|c| c.into_float()).unwrap_or_default(),
            beta13: t.take("BETA13").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tqc: t.take("TQC").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            pmacc: t.take("PMACC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_214 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub gab1: Vec<f64>,
    pub gab2: Vec<f64>,
    pub gab3: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub gamab1: Vec<f64>,
    pub gamab2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub eacrf: Vec<f64>,
    pub ebcrf: Vec<f64>,
    pub eacrp: Vec<f64>,
    pub ebcrp: Vec<f64>,
    pub easf: Vec<f64>,
    pub ebsf: Vec<f64>,
    pub eunlf: Vec<f64>,
    pub ecomf: Vec<f64>,
    pub eamax: Vec<f64>,
    pub ebmax: Vec<f64>,
    pub sigpost: Vec<f64>,
    pub cce: Vec<f64>,
    pub pce: Vec<f64>,
    pub cse: Vec<f64>,
    pub pse: Vec<f64>,
    pub dfac: Vec<f64>,
    pub emax: Vec<f64>,
    pub eafail: Vec<f64>,
    pub ebfail: Vec<f64>,
}
impl MAT_214 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_214").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab1: t.take("GAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab2: t.take("GAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab3: t.take("GAB3").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamab1: t.take("GAMAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamab2: t.take("GAMAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            eacrf: t.take("EACRF").and_then(|c| c.into_float()).unwrap_or_default(),
            ebcrf: t.take("EBCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            eacrp: t.take("EACRP").and_then(|c| c.into_float()).unwrap_or_default(),
            ebcrp: t.take("EBCRP").and_then(|c| c.into_float()).unwrap_or_default(),
            easf: t.take("EASF").and_then(|c| c.into_float()).unwrap_or_default(),
            ebsf: t.take("EBSF").and_then(|c| c.into_float()).unwrap_or_default(),
            eunlf: t.take("EUNLF").and_then(|c| c.into_float()).unwrap_or_default(),
            ecomf: t.take("ECOMF").and_then(|c| c.into_float()).unwrap_or_default(),
            eamax: t.take("EAMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ebmax: t.take("EBMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigpost: t.take("SIGPOST").and_then(|c| c.into_float()).unwrap_or_default(),
            cce: t.take("CCE").and_then(|c| c.into_float()).unwrap_or_default(),
            pce: t.take("PCE").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            pse: t.take("PSE").and_then(|c| c.into_float()).unwrap_or_default(),
            dfac: t.take("DFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            emax: t.take("EMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            eafail: t.take("EAFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ebfail: t.take("EBFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_215 {
    pub mid: Vec<i64>,
    pub mmopt: Vec<f64>,
    pub bupd: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub failm: Vec<f64>,
    pub failf: Vec<i64>,
    pub numint: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fvf: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub fl: Vec<f64>,
    pub fd: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub a11: Vec<f64>,
    pub a22: Vec<f64>,
    pub rof: Vec<f64>,
    pub el: Vec<f64>,
    pub et: Vec<f64>,
    pub glt: Vec<f64>,
    pub prtl: Vec<f64>,
    pub prtt: Vec<f64>,
    pub xt: Vec<f64>,
    pub unused_4: Vec<i64>,
    pub unused_2_3: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub slimxt: Vec<f64>,
    pub ncyred: Vec<f64>,
    pub rom: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigyt: Vec<f64>,
    pub etant: Vec<f64>,
    pub unused_6: Vec<i64>,
    pub unused_2_4: Vec<i64>,
    pub eps0: Vec<f64>,
    pub c: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub unused_2_5: Vec<i64>,
    pub unused_3_3: Vec<i64>,
    pub lcdi: Vec<i64>,
    pub upf: Vec<f64>,
    pub unused_4_3: Vec<i64>,
    pub ncyred2: Vec<f64>,
}
impl MAT_215 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_215").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mmopt: t.take("MMOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            bupd: t.take("BUPD").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            failm: t.take("FAILM").and_then(|c| c.into_float()).unwrap_or_default(),
            failf: t.take("FAILF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fvf: t.take("FVF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            fl: t.take("FL").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            rof: t.take("ROF").and_then(|c| c.into_float()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            glt: t.take("GLT").and_then(|c| c.into_float()).unwrap_or_default(),
            prtl: t.take("PRTL").and_then(|c| c.into_float()).unwrap_or_default(),
            prtt: t.take("PRTT").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_3: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            slimxt: t.take("SLIMXT").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyred: t.take("NCYRED").and_then(|c| c.into_float()).unwrap_or_default(),
            rom: t.take("ROM").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyt: t.take("SIGYT").and_then(|c| c.into_float()).unwrap_or_default(),
            etant: t.take("ETANT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_4: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_5: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdi: t.take("LCDI").and_then(|c| c.into_int()).unwrap_or_default(),
            upf: t.take("UPF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4_3: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyred2: t.take("NCYRED2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_216 {
    pub mid: Vec<i64>,
    pub ro1: Vec<f64>,
    pub e1: Vec<f64>,
    pub pr1: Vec<f64>,
    pub f0: Vec<i64>,
    pub ro2: Vec<f64>,
    pub e2: Vec<f64>,
    pub pr2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_216 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_216").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro1: t.take("RO1").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            ro2: t.take("RO2").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_217 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea1: Vec<f64>,
    pub eb1: Vec<f64>,
    pub ec1: Vec<f64>,
    pub prba1: Vec<f64>,
    pub prca1: Vec<f64>,
    pub prcb1: Vec<f64>,
    pub gab1: Vec<f64>,
    pub gbc1: Vec<f64>,
    pub gca1: Vec<f64>,
    pub aopt1: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a11: Vec<f64>,
    pub a21: Vec<f64>,
    pub a31: Vec<f64>,
    pub v11: Vec<f64>,
    pub v21: Vec<f64>,
    pub v31: Vec<f64>,
    pub d11: Vec<f64>,
    pub d21: Vec<f64>,
    pub d31: Vec<f64>,
    pub beta1: Vec<f64>,
    pub f_4: Vec<i64>,
    pub r02: Vec<f64>,
    pub ea2: Vec<f64>,
    pub eb2: Vec<f64>,
    pub ec2: Vec<f64>,
    pub prba2: Vec<f64>,
    pub prca2: Vec<f64>,
    pub prcb2: Vec<f64>,
    pub gab2: Vec<f64>,
    pub gbc2: Vec<f64>,
    pub gca2: Vec<f64>,
    pub f_5: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub f_3_2: Vec<i64>,
    pub a12: Vec<f64>,
    pub a22: Vec<f64>,
    pub a32: Vec<f64>,
    pub v12: Vec<f64>,
    pub v22: Vec<f64>,
    pub v32: Vec<f64>,
    pub d12: Vec<f64>,
    pub d22: Vec<f64>,
    pub d32: Vec<f64>,
    pub beta2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_217 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_217").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea1: t.take("EA1").and_then(|c| c.into_float()).unwrap_or_default(),
            eb1: t.take("EB1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            prba1: t.take("PRBA1").and_then(|c| c.into_float()).unwrap_or_default(),
            prca1: t.take("PRCA1").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb1: t.take("PRCB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab1: t.take("GAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc1: t.take("GBC1").and_then(|c| c.into_float()).unwrap_or_default(),
            gca1: t.take("GCA1").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt1: t.take("AOPT1").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            v11: t.take("V11").and_then(|c| c.into_float()).unwrap_or_default(),
            v21: t.take("V21").and_then(|c| c.into_float()).unwrap_or_default(),
            v31: t.take("V31").and_then(|c| c.into_float()).unwrap_or_default(),
            d11: t.take("D11").and_then(|c| c.into_float()).unwrap_or_default(),
            d21: t.take("D21").and_then(|c| c.into_float()).unwrap_or_default(),
            d31: t.take("D31").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            r02: t.take("R02").and_then(|c| c.into_float()).unwrap_or_default(),
            ea2: t.take("EA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eb2: t.take("EB2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            prba2: t.take("PRBA2").and_then(|c| c.into_float()).unwrap_or_default(),
            prca2: t.take("PRCA2").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb2: t.take("PRCB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab2: t.take("GAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc2: t.take("GBC2").and_then(|c| c.into_float()).unwrap_or_default(),
            gca2: t.take("GCA2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v22: t.take("V22").and_then(|c| c.into_float()).unwrap_or_default(),
            v32: t.take("V32").and_then(|c| c.into_float()).unwrap_or_default(),
            d12: t.take("D12").and_then(|c| c.into_float()).unwrap_or_default(),
            d22: t.take("D22").and_then(|c| c.into_float()).unwrap_or_default(),
            d32: t.take("D32").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_217_ANIS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c111: Vec<f64>,
    pub c121: Vec<f64>,
    pub c221: Vec<f64>,
    pub c131: Vec<f64>,
    pub c231: Vec<f64>,
    pub c331: Vec<f64>,
    pub c141: Vec<f64>,
    pub c241: Vec<f64>,
    pub c341: Vec<f64>,
    pub c441: Vec<f64>,
    pub c151: Vec<f64>,
    pub c251: Vec<f64>,
    pub c351: Vec<f64>,
    pub c451: Vec<f64>,
    pub c551: Vec<f64>,
    pub c161: Vec<f64>,
    pub c261: Vec<f64>,
    pub c361: Vec<f64>,
    pub c461: Vec<f64>,
    pub c561: Vec<f64>,
    pub c661: Vec<f64>,
    pub aopt1: Vec<f64>,
    pub xp1: Vec<f64>,
    pub yp1: Vec<f64>,
    pub zp1: Vec<f64>,
    pub a11: Vec<f64>,
    pub a21: Vec<f64>,
    pub a31: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v11: Vec<f64>,
    pub v21: Vec<f64>,
    pub v31: Vec<f64>,
    pub d11: Vec<f64>,
    pub d21: Vec<f64>,
    pub d31: Vec<f64>,
    pub beta1: Vec<f64>,
    pub ref_: Vec<f64>,
    pub f0: Vec<f64>,
    pub f10: Vec<f64>,
    pub c112: Vec<f64>,
    pub c122: Vec<f64>,
    pub c222: Vec<f64>,
    pub c132: Vec<f64>,
    pub c232: Vec<f64>,
    pub c332: Vec<f64>,
    pub c142: Vec<f64>,
    pub c242: Vec<f64>,
    pub c342: Vec<f64>,
    pub c442: Vec<f64>,
    pub c152: Vec<f64>,
    pub c252: Vec<f64>,
    pub c352: Vec<f64>,
    pub c452: Vec<f64>,
    pub c552: Vec<f64>,
    pub c162: Vec<f64>,
    pub c262: Vec<f64>,
    pub c362: Vec<f64>,
    pub c462: Vec<f64>,
    pub c562: Vec<f64>,
    pub c662: Vec<f64>,
    pub xp2: Vec<f64>,
    pub yp2: Vec<f64>,
    pub zp2: Vec<f64>,
    pub a12: Vec<f64>,
    pub a22: Vec<f64>,
    pub a32: Vec<f64>,
    pub xp2_2: Vec<f64>,
    pub v12: Vec<f64>,
    pub v22: Vec<f64>,
    pub v32: Vec<f64>,
    pub d12: Vec<f64>,
    pub d22: Vec<f64>,
    pub d32: Vec<f64>,
    pub beta2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_217_ANIS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_217_ANIS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c111: t.take("C111").and_then(|c| c.into_float()).unwrap_or_default(),
            c121: t.take("C121").and_then(|c| c.into_float()).unwrap_or_default(),
            c221: t.take("C221").and_then(|c| c.into_float()).unwrap_or_default(),
            c131: t.take("C131").and_then(|c| c.into_float()).unwrap_or_default(),
            c231: t.take("C231").and_then(|c| c.into_float()).unwrap_or_default(),
            c331: t.take("C331").and_then(|c| c.into_float()).unwrap_or_default(),
            c141: t.take("C141").and_then(|c| c.into_float()).unwrap_or_default(),
            c241: t.take("C241").and_then(|c| c.into_float()).unwrap_or_default(),
            c341: t.take("C341").and_then(|c| c.into_float()).unwrap_or_default(),
            c441: t.take("C441").and_then(|c| c.into_float()).unwrap_or_default(),
            c151: t.take("C151").and_then(|c| c.into_float()).unwrap_or_default(),
            c251: t.take("C251").and_then(|c| c.into_float()).unwrap_or_default(),
            c351: t.take("C351").and_then(|c| c.into_float()).unwrap_or_default(),
            c451: t.take("C451").and_then(|c| c.into_float()).unwrap_or_default(),
            c551: t.take("C551").and_then(|c| c.into_float()).unwrap_or_default(),
            c161: t.take("C161").and_then(|c| c.into_float()).unwrap_or_default(),
            c261: t.take("C261").and_then(|c| c.into_float()).unwrap_or_default(),
            c361: t.take("C361").and_then(|c| c.into_float()).unwrap_or_default(),
            c461: t.take("C461").and_then(|c| c.into_float()).unwrap_or_default(),
            c561: t.take("C561").and_then(|c| c.into_float()).unwrap_or_default(),
            c661: t.take("C661").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt1: t.take("AOPT1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            yp1: t.take("YP1").and_then(|c| c.into_float()).unwrap_or_default(),
            zp1: t.take("ZP1").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v11: t.take("V11").and_then(|c| c.into_float()).unwrap_or_default(),
            v21: t.take("V21").and_then(|c| c.into_float()).unwrap_or_default(),
            v31: t.take("V31").and_then(|c| c.into_float()).unwrap_or_default(),
            d11: t.take("D11").and_then(|c| c.into_float()).unwrap_or_default(),
            d21: t.take("D21").and_then(|c| c.into_float()).unwrap_or_default(),
            d31: t.take("D31").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            c112: t.take("C112").and_then(|c| c.into_float()).unwrap_or_default(),
            c122: t.take("C122").and_then(|c| c.into_float()).unwrap_or_default(),
            c222: t.take("C222").and_then(|c| c.into_float()).unwrap_or_default(),
            c132: t.take("C132").and_then(|c| c.into_float()).unwrap_or_default(),
            c232: t.take("C232").and_then(|c| c.into_float()).unwrap_or_default(),
            c332: t.take("C332").and_then(|c| c.into_float()).unwrap_or_default(),
            c142: t.take("C142").and_then(|c| c.into_float()).unwrap_or_default(),
            c242: t.take("C242").and_then(|c| c.into_float()).unwrap_or_default(),
            c342: t.take("C342").and_then(|c| c.into_float()).unwrap_or_default(),
            c442: t.take("C442").and_then(|c| c.into_float()).unwrap_or_default(),
            c152: t.take("C152").and_then(|c| c.into_float()).unwrap_or_default(),
            c252: t.take("C252").and_then(|c| c.into_float()).unwrap_or_default(),
            c352: t.take("C352").and_then(|c| c.into_float()).unwrap_or_default(),
            c452: t.take("C452").and_then(|c| c.into_float()).unwrap_or_default(),
            c552: t.take("C552").and_then(|c| c.into_float()).unwrap_or_default(),
            c162: t.take("C162").and_then(|c| c.into_float()).unwrap_or_default(),
            c262: t.take("C262").and_then(|c| c.into_float()).unwrap_or_default(),
            c362: t.take("C362").and_then(|c| c.into_float()).unwrap_or_default(),
            c462: t.take("C462").and_then(|c| c.into_float()).unwrap_or_default(),
            c562: t.take("C562").and_then(|c| c.into_float()).unwrap_or_default(),
            c662: t.take("C662").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            yp2: t.take("YP2").and_then(|c| c.into_float()).unwrap_or_default(),
            zp2: t.take("ZP2").and_then(|c| c.into_float()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2_2: t.take("XP2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v22: t.take("V22").and_then(|c| c.into_float()).unwrap_or_default(),
            v32: t.take("V32").and_then(|c| c.into_float()).unwrap_or_default(),
            d12: t.take("D12").and_then(|c| c.into_float()).unwrap_or_default(),
            d22: t.take("D22").and_then(|c| c.into_float()).unwrap_or_default(),
            d32: t.take("D32").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_218 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr1: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl1: Vec<f64>,
    pub sw1: Vec<f64>,
    pub st1: Vec<f64>,
    pub lcid1: Vec<f64>,
    pub f0: Vec<f64>,
    pub f10: Vec<f64>,
    pub pr2: Vec<f64>,
    pub a2: Vec<f64>,
    pub b2: Vec<f64>,
    pub sgl1_2: Vec<f64>,
    pub sw1_2: Vec<f64>,
    pub st1_2: Vec<f64>,
    pub lcid1_2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_218 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_218").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1_2: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1_2: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1_2: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1_2: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_219 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub f: Vec<f64>,
    pub prba: Vec<f64>,
    pub f_2: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub nlayer: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub macf: Vec<f64>,
    pub angle1: Vec<f64>,
    pub angle2: Vec<f64>,
    pub angle3: Vec<f64>,
    pub angle4: Vec<f64>,
    pub angle5: Vec<f64>,
    pub angle6: Vec<f64>,
    pub angle7: Vec<f64>,
    pub angle8: Vec<f64>,
    pub imatt: Vec<f64>,
    pub ifibt: Vec<f64>,
    pub iloct: Vec<f64>,
    pub idelt: Vec<f64>,
    pub smatt: Vec<f64>,
    pub sfibt: Vec<f64>,
    pub sloct: Vec<f64>,
    pub sdelt: Vec<f64>,
    pub imatc: Vec<f64>,
    pub ifibc: Vec<f64>,
    pub ilocc: Vec<f64>,
    pub idelc: Vec<f64>,
    pub smatc: Vec<f64>,
    pub sfibc: Vec<f64>,
    pub slocc: Vec<f64>,
    pub sdelc: Vec<f64>,
    pub erode: Vec<i64>,
    pub erpar1: Vec<f64>,
    pub erpar2: Vec<f64>,
    pub resids: Vec<f64>,
}
impl MAT_219 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_219").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            nlayer: t.take("NLAYER").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            angle1: t.take("ANGLE1").and_then(|c| c.into_float()).unwrap_or_default(),
            angle2: t.take("ANGLE2").and_then(|c| c.into_float()).unwrap_or_default(),
            angle3: t.take("ANGLE3").and_then(|c| c.into_float()).unwrap_or_default(),
            angle4: t.take("ANGLE4").and_then(|c| c.into_float()).unwrap_or_default(),
            angle5: t.take("ANGLE5").and_then(|c| c.into_float()).unwrap_or_default(),
            angle6: t.take("ANGLE6").and_then(|c| c.into_float()).unwrap_or_default(),
            angle7: t.take("ANGLE7").and_then(|c| c.into_float()).unwrap_or_default(),
            angle8: t.take("ANGLE8").and_then(|c| c.into_float()).unwrap_or_default(),
            imatt: t.take("IMATT").and_then(|c| c.into_float()).unwrap_or_default(),
            ifibt: t.take("IFIBT").and_then(|c| c.into_float()).unwrap_or_default(),
            iloct: t.take("ILOCT").and_then(|c| c.into_float()).unwrap_or_default(),
            idelt: t.take("IDELT").and_then(|c| c.into_float()).unwrap_or_default(),
            smatt: t.take("SMATT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfibt: t.take("SFIBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sloct: t.take("SLOCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sdelt: t.take("SDELT").and_then(|c| c.into_float()).unwrap_or_default(),
            imatc: t.take("IMATC").and_then(|c| c.into_float()).unwrap_or_default(),
            ifibc: t.take("IFIBC").and_then(|c| c.into_float()).unwrap_or_default(),
            ilocc: t.take("ILOCC").and_then(|c| c.into_float()).unwrap_or_default(),
            idelc: t.take("IDELC").and_then(|c| c.into_float()).unwrap_or_default(),
            smatc: t.take("SMATC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfibc: t.take("SFIBC").and_then(|c| c.into_float()).unwrap_or_default(),
            slocc: t.take("SLOCC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdelc: t.take("SDELC").and_then(|c| c.into_float()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            erpar1: t.take("ERPAR1").and_then(|c| c.into_float()).unwrap_or_default(),
            erpar2: t.take("ERPAR2").and_then(|c| c.into_float()).unwrap_or_default(),
            resids: t.take("RESIDS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_220 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_220 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_220").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_221 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub unused: Vec<i64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub nerode: Vec<i64>,
    pub ndam: Vec<i64>,
    pub eps1tf: Vec<f64>,
    pub eps2tf: Vec<f64>,
    pub eps3tf: Vec<f64>,
    pub eps1cf: Vec<f64>,
    pub eps2cf: Vec<f64>,
    pub eps3cf: Vec<f64>,
    pub eps12f: Vec<f64>,
    pub eps23f: Vec<f64>,
    pub eps13f: Vec<f64>,
    pub epsd1t: Vec<f64>,
    pub epsc1t: Vec<f64>,
    pub cdam1t: Vec<f64>,
    pub epsd2t: Vec<f64>,
    pub epsc2t: Vec<f64>,
    pub cdam2t: Vec<f64>,
    pub epsd3t: Vec<f64>,
    pub epsc3t: Vec<f64>,
    pub cdam3t: Vec<f64>,
    pub epsd1c: Vec<f64>,
    pub epsc1c: Vec<f64>,
    pub cdam1c: Vec<f64>,
    pub epsd2c: Vec<f64>,
    pub epsc2c: Vec<f64>,
    pub cdam2c: Vec<f64>,
    pub epsd3c: Vec<f64>,
    pub epsc3c: Vec<f64>,
    pub cdam3c: Vec<f64>,
    pub epsd12: Vec<f64>,
    pub epsc12: Vec<f64>,
    pub cdam12: Vec<f64>,
    pub epsd23: Vec<f64>,
    pub epsc23: Vec<f64>,
    pub cdam23: Vec<f64>,
    pub epsd31: Vec<f64>,
    pub epsc31: Vec<f64>,
    pub cdam31: Vec<f64>,
}
impl MAT_221 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_221").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nerode: t.take("NERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            ndam: t.take("NDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1tf: t.take("EPS1TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2tf: t.take("EPS2TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3tf: t.take("EPS3TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1cf: t.take("EPS1CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2cf: t.take("EPS2CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3cf: t.take("EPS3CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12f: t.take("EPS12F").and_then(|c| c.into_float()).unwrap_or_default(),
            eps23f: t.take("EPS23F").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13f: t.take("EPS13F").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd1t: t.take("EPSD1T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc1t: t.take("EPSC1T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam1t: t.take("CDAM1T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd2t: t.take("EPSD2T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc2t: t.take("EPSC2T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam2t: t.take("CDAM2T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd3t: t.take("EPSD3T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc3t: t.take("EPSC3T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam3t: t.take("CDAM3T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd1c: t.take("EPSD1C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc1c: t.take("EPSC1C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam1c: t.take("CDAM1C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd2c: t.take("EPSD2C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc2c: t.take("EPSC2C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam2c: t.take("CDAM2C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd3c: t.take("EPSD3C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc3c: t.take("EPSC3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam3c: t.take("CDAM3C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd12: t.take("EPSD12").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc12: t.take("EPSC12").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam12: t.take("CDAM12").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd23: t.take("EPSD23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc23: t.take("EPSC23").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam23: t.take("CDAM23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd31: t.take("EPSD31").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc31: t.take("EPSC31").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam31: t.take("CDAM31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_224 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub tabk1: Vec<i64>,
    pub tabkt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub failopt: Vec<i64>,
    pub numavg: Vec<i64>,
    pub ncyfail: Vec<i64>,
    pub erode: Vec<i64>,
    pub lcps: Vec<i64>,
}
impl MAT_224 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_224").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            tabk1: t.take("TABK1").and_then(|c| c.into_int()).unwrap_or_default(),
            tabkt: t.take("TABKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            failopt: t.take("FAILOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            numavg: t.take("NUMAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyfail: t.take("NCYFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcps: t.take("LCPS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_224_GYS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub lck1: Vec<i64>,
    pub lckt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub lccr: Vec<i64>,
    pub lcct: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub lcst: Vec<i64>,
    pub iflag: Vec<i64>,
    pub sfiepm: Vec<f64>,
    pub niter: Vec<i64>,
}
impl MAT_224_GYS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_224_GYS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lck1: t.take("LCK1").and_then(|c| c.into_int()).unwrap_or_default(),
            lckt: t.take("LCKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            lccr: t.take("LCCR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcct: t.take("LCCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcst: t.take("LCST").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfiepm: t.take("SFIEPM").and_then(|c| c.into_float()).unwrap_or_default(),
            niter: t.take("NITER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_226 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub f: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_226 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_226").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_226_NLP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub ifld: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_226_NLP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_226_NLP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            ifld: t.take("IFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_230 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub vc: Vec<f64>,
}
impl MAT_230 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_230").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_231 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
}
impl MAT_231 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_231").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_232 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub zt: Vec<f64>,
    pub fd: Vec<f64>,
}
impl MAT_232 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_232").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_233 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub a: Vec<f64>,
    pub c11: Vec<f64>,
    pub c22: Vec<f64>,
    pub c33: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub k: Vec<f64>,
    pub p3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<f64>,
    pub f20: Vec<f64>,
    pub f30: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c44: Vec<f64>,
    pub xp: Vec<i64>,
    pub yp: Vec<i64>,
    pub zp: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fit: Vec<i64>,
    pub lc1id: Vec<i64>,
    pub lc2id: Vec<i64>,
    pub numint: Vec<i64>,
    pub lccid: Vec<i64>,
    pub icflag: Vec<i64>,
    pub idflag: Vec<i64>,
    pub lc3id: Vec<i64>,
    pub epsfg: Vec<f64>,
}
impl MAT_233 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_233").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_int()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_int()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fit: t.take("FIT").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1id: t.take("LC1ID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2id: t.take("LC2ID").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccid: t.take("LCCID").and_then(|c| c.into_int()).unwrap_or_default(),
            icflag: t.take("ICFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            idflag: t.take("IDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3id: t.take("LC3ID").and_then(|c| c.into_int()).unwrap_or_default(),
            epsfg: t.take("EPSFG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_234 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub eu: Vec<f64>,
    pub thl: Vec<f64>,
    pub thi: Vec<f64>,
    pub ta: Vec<f64>,
    pub w: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub h: Vec<f64>,
    pub s_2: Vec<f64>,
    pub eka: Vec<f64>,
    pub eua: Vec<f64>,
    pub vmb: Vec<f64>,
    pub c: Vec<f64>,
    pub g23: Vec<f64>,
    pub ekb: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_234 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_234").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            eu: t.take("EU").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            thi: t.take("THI").and_then(|c| c.into_float()).unwrap_or_default(),
            ta: t.take("TA").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("s").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            s_2: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            eka: t.take("EKA").and_then(|c| c.into_float()).unwrap_or_default(),
            eua: t.take("EUA").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            ekb: t.take("EKB").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("Xp").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("Yp").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("Zp").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_235 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub v12: Vec<f64>,
    pub v23: Vec<f64>,
    pub xt: Vec<f64>,
    pub thi: Vec<f64>,
    pub thl: Vec<f64>,
    pub bfi: Vec<f64>,
    pub bwi: Vec<f64>,
    pub dscf: Vec<f64>,
    pub cnst: Vec<f64>,
    pub atlr: Vec<f64>,
    pub vmb: Vec<f64>,
    pub vme: Vec<f64>,
    pub trs: Vec<f64>,
    pub fflg: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_235 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_235").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v23: t.take("V23").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("Xt").and_then(|c| c.into_float()).unwrap_or_default(),
            thi: t.take("THI").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            bfi: t.take("BFI").and_then(|c| c.into_float()).unwrap_or_default(),
            bwi: t.take("BWI").and_then(|c| c.into_float()).unwrap_or_default(),
            dscf: t.take("DSCF").and_then(|c| c.into_float()).unwrap_or_default(),
            cnst: t.take("CNST").and_then(|c| c.into_float()).unwrap_or_default(),
            atlr: t.take("ATLR").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            vme: t.take("VME").and_then(|c| c.into_float()).unwrap_or_default(),
            trs: t.take("TRS").and_then(|c| c.into_float()).unwrap_or_default(),
            fflg: t.take("FFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_236 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e0: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub pr: Vec<f64>,
    pub g: Vec<f64>,
    pub g_scl: Vec<f64>,
    pub tsl: Vec<f64>,
    pub eps_tan: Vec<f64>,
}
impl MAT_236 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_236").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            g_scl: t.take("G_SCL").and_then(|c| c.into_float()).unwrap_or_default(),
            tsl: t.take("TSL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps_tan: t.take("EPS_TAN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_237 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub zt: Vec<f64>,
    pub fd: Vec<f64>,
}
impl MAT_237 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_237").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_238 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_238 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_238").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<f64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<f64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<f64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240_FUNCTIONS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<i64>,
    pub gmod: Vec<i64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<i64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<i64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<i64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<i64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<i64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<i64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240_FUNCTIONS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240_FUNCTIONS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240_FUNCTIONS_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<i64>,
    pub gmod: Vec<i64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<i64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<i64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<i64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<i64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<i64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<i64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<i64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<i64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<i64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240_FUNCTIONS_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240_FUNCTIONS_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_240_THERMAL_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<f64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<f64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<f64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_240_THERMAL_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_240_THERMAL_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_241 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub p1: Vec<f64>,
    pub s1: Vec<f64>,
    pub p2: Vec<f64>,
    pub s2: Vec<f64>,
    pub c: Vec<f64>,
    pub epsi: Vec<f64>,
    pub t: Vec<f64>,
    pub unused: Vec<f64>,
    pub alpha: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub beta: Vec<f64>,
    pub dp1: Vec<f64>,
    pub epfmin: Vec<f64>,
    pub epfmax: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_241 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_241").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsi: t.take("EPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dp1: t.take("DP1").and_then(|c| c.into_float()).unwrap_or_default(),
            epfmin: t.take("EPFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epfmax: t.take("EPFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_242 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc1: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub aopt: Vec<i64>,
    pub unused: Vec<i64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_242 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_242").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc1: t.take("SC1").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_243 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub unused: Vec<i64>,
    pub flag: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub usrfail: Vec<f64>,
}
impl MAT_243 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_243").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_244 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tunit: Vec<f64>,
    pub crsh: Vec<i64>,
    pub phase: Vec<i64>,
    pub heat: Vec<i64>,
    pub lcy1: Vec<i64>,
    pub lcy2: Vec<i64>,
    pub lcy3: Vec<i64>,
    pub lcy4: Vec<i64>,
    pub lcy5: Vec<i64>,
    pub kfer: Vec<f64>,
    pub kper: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub co: Vec<f64>,
    pub mo: Vec<f64>,
    pub cr: Vec<f64>,
    pub ni: Vec<f64>,
    pub mn: Vec<f64>,
    pub si: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub cu: Vec<f64>,
    pub p: Vec<f64>,
    pub al: Vec<f64>,
    pub as_: Vec<f64>,
    pub ti: Vec<f64>,
    pub cwm: Vec<i64>,
    pub lctre: Vec<i64>,
    pub thexp1: Vec<f64>,
    pub thexp5: Vec<f64>,
    pub lcth1: Vec<i64>,
    pub lcth5: Vec<i64>,
    pub tref: Vec<f64>,
    pub lat1: Vec<f64>,
    pub lat5: Vec<f64>,
    pub tabth: Vec<i64>,
    pub qr2: Vec<f64>,
    pub qr3: Vec<f64>,
    pub qr4: Vec<f64>,
    pub alpha: Vec<f64>,
    pub grain: Vec<f64>,
    pub toffe: Vec<f64>,
    pub tofpe: Vec<f64>,
    pub tofba: Vec<f64>,
    pub plmem2: Vec<f64>,
    pub plmem3: Vec<f64>,
    pub plmem4: Vec<f64>,
    pub plmem5: Vec<f64>,
    pub strc: Vec<f64>,
    pub strp: Vec<f64>,
    pub react: Vec<i64>,
    pub temper: Vec<i64>,
}
impl MAT_244 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_244").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tunit: t.take("TUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            crsh: t.take("CRSH").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            heat: t.take("HEAT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy1: t.take("LCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy2: t.take("LCY2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy3: t.take("LCY3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy4: t.take("LCY4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy5: t.take("LCY5").and_then(|c| c.into_int()).unwrap_or_default(),
            kfer: t.take("KFER").and_then(|c| c.into_float()).unwrap_or_default(),
            kper: t.take("KPER").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("Co").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("Mo").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("Cr").and_then(|c| c.into_float()).unwrap_or_default(),
            ni: t.take("Ni").and_then(|c| c.into_float()).unwrap_or_default(),
            mn: t.take("Mn").and_then(|c| c.into_float()).unwrap_or_default(),
            si: t.take("Si").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            cu: t.take("Cu").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            al: t.take("Al").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("As").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
            cwm: t.take("CWM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctre: t.take("LCTRE").and_then(|c| c.into_int()).unwrap_or_default(),
            thexp1: t.take("THEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            thexp5: t.take("THEXP5").and_then(|c| c.into_float()).unwrap_or_default(),
            lcth1: t.take("LCTH1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcth5: t.take("LCTH5").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lat1: t.take("LAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lat5: t.take("LAT5").and_then(|c| c.into_float()).unwrap_or_default(),
            tabth: t.take("TABTH").and_then(|c| c.into_int()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qr3: t.take("QR3").and_then(|c| c.into_float()).unwrap_or_default(),
            qr4: t.take("QR4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            grain: t.take("GRAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            toffe: t.take("TOFFE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofpe: t.take("TOFPE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofba: t.take("TOFBA").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem2: t.take("PLMEM2").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem3: t.take("PLMEM3").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem4: t.take("PLMEM4").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem5: t.take("PLMEM5").and_then(|c| c.into_float()).unwrap_or_default(),
            strc: t.take("STRC").and_then(|c| c.into_float()).unwrap_or_default(),
            strp: t.take("STRP").and_then(|c| c.into_float()).unwrap_or_default(),
            react: t.take("REACT").and_then(|c| c.into_int()).unwrap_or_default(),
            temper: t.take("TEMPER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_245 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_245 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_245").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_246 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mu: Vec<f64>,
}
impl MAT_246 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_246").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_248 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tunit: Vec<f64>,
    pub trip: Vec<i64>,
    pub phase: Vec<i64>,
    pub heat: Vec<i64>,
    pub lcy1: Vec<i64>,
    pub lcy2: Vec<i64>,
    pub lcy3: Vec<i64>,
    pub lcy4: Vec<i64>,
    pub lcy5: Vec<i64>,
    pub c_f: Vec<f64>,
    pub c_p: Vec<f64>,
    pub c_b: Vec<f64>,
    pub c: Vec<f64>,
    pub co: Vec<f64>,
    pub mo: Vec<f64>,
    pub cr: Vec<f64>,
    pub ni: Vec<f64>,
    pub mn: Vec<f64>,
    pub si: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub cu: Vec<f64>,
    pub p: Vec<f64>,
    pub ai: Vec<f64>,
    pub as_: Vec<f64>,
    pub ti: Vec<f64>,
    pub b: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub tabrho: Vec<i64>,
    pub f_3: Vec<i64>,
    pub tref: Vec<f64>,
    pub lat1: Vec<f64>,
    pub lat5: Vec<f64>,
    pub tabth: Vec<i64>,
    pub qr2: Vec<f64>,
    pub qr3: Vec<f64>,
    pub qr4: Vec<f64>,
    pub alpha: Vec<f64>,
    pub grain: Vec<f64>,
    pub toffe: Vec<f64>,
    pub tofpe: Vec<f64>,
    pub tofba: Vec<f64>,
    pub plmem2: Vec<f64>,
    pub plmem3: Vec<f64>,
    pub plmem4: Vec<f64>,
    pub plmem5: Vec<f64>,
    pub strc: Vec<f64>,
    pub strp: Vec<f64>,
    pub f_4: Vec<i64>,
    pub f_2_3: Vec<i64>,
    pub fs: Vec<f64>,
    pub ps: Vec<f64>,
    pub bs: Vec<f64>,
    pub ms: Vec<f64>,
    pub msig: Vec<f64>,
    pub lceps23: Vec<i64>,
    pub lceps4: Vec<i64>,
    pub lceps5: Vec<i64>,
    pub lch4: Vec<i64>,
    pub lch5: Vec<i64>,
    pub dtcrit: Vec<f64>,
    pub tsamp: Vec<f64>,
    pub islc: Vec<i64>,
    pub iextra: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_2_4: Vec<i64>,
    pub alph_m: Vec<f64>,
    pub n_m: Vec<f64>,
    pub phi_m: Vec<f64>,
    pub psi_m: Vec<f64>,
    pub omg_f: Vec<f64>,
    pub phi_f: Vec<f64>,
    pub psi_f: Vec<f64>,
    pub cr_f: Vec<f64>,
    pub omg_p: Vec<f64>,
    pub phi_p: Vec<f64>,
    pub psi_p: Vec<f64>,
    pub cr_p: Vec<f64>,
    pub omg_b: Vec<f64>,
    pub phi_b: Vec<f64>,
    pub psi_b: Vec<f64>,
    pub cr_b: Vec<f64>,
    pub aust: Vec<f64>,
    pub ferr: Vec<f64>,
    pub pear: Vec<f64>,
    pub bain: Vec<f64>,
    pub mart: Vec<f64>,
    pub grk: Vec<f64>,
    pub grqr: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gra: Vec<f64>,
    pub grb: Vec<f64>,
    pub expa: Vec<f64>,
    pub expb: Vec<f64>,
    pub grcc: Vec<f64>,
    pub grcm: Vec<f64>,
    pub heatn: Vec<f64>,
    pub tau2: Vec<f64>,
    pub funca: Vec<i64>,
    pub funcb: Vec<i64>,
    pub funcm: Vec<i64>,
    pub tcvup: Vec<f64>,
    pub tcvlo: Vec<f64>,
    pub cvcrit: Vec<f64>,
    pub tcvsl: Vec<f64>,
    pub epsp: Vec<f64>,
    pub expon: Vec<f64>,
}
impl MAT_248 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_248").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tunit: t.take("TUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            trip: t.take("TRIP").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            heat: t.take("HEAT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy1: t.take("LCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy2: t.take("LCY2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy3: t.take("LCY3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy4: t.take("LCY4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy5: t.take("LCY5").and_then(|c| c.into_int()).unwrap_or_default(),
            c_f: t.take("C_F").and_then(|c| c.into_float()).unwrap_or_default(),
            c_p: t.take("C_P").and_then(|c| c.into_float()).unwrap_or_default(),
            c_b: t.take("C_B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("Co").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("Mo").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("Cr").and_then(|c| c.into_float()).unwrap_or_default(),
            ni: t.take("Ni").and_then(|c| c.into_float()).unwrap_or_default(),
            mn: t.take("Mn").and_then(|c| c.into_float()).unwrap_or_default(),
            si: t.take("Si").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            cu: t.take("Cu").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("AI").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("As").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            tabrho: t.take("TABRHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lat1: t.take("LAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lat5: t.take("LAT5").and_then(|c| c.into_float()).unwrap_or_default(),
            tabth: t.take("TABTH").and_then(|c| c.into_int()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qr3: t.take("QR3").and_then(|c| c.into_float()).unwrap_or_default(),
            qr4: t.take("QR4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            grain: t.take("GRAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            toffe: t.take("TOFFE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofpe: t.take("TOFPE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofba: t.take("TOFBA").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem2: t.take("PLMEM2").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem3: t.take("PLMEM3").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem4: t.take("PLMEM4").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem5: t.take("PLMEM5").and_then(|c| c.into_float()).unwrap_or_default(),
            strc: t.take("STRC").and_then(|c| c.into_float()).unwrap_or_default(),
            strp: t.take("STRP").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            msig: t.take("MSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps23: t.take("LCEPS23").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps4: t.take("LCEPS4").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps5: t.take("LCEPS5").and_then(|c| c.into_int()).unwrap_or_default(),
            lch4: t.take("LCH4").and_then(|c| c.into_int()).unwrap_or_default(),
            lch5: t.take("LCH5").and_then(|c| c.into_int()).unwrap_or_default(),
            dtcrit: t.take("DTCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsamp: t.take("TSAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            islc: t.take("ISLC").and_then(|c| c.into_int()).unwrap_or_default(),
            iextra: t.take("IEXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_4: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph_m: t.take("ALPH_M").and_then(|c| c.into_float()).unwrap_or_default(),
            n_m: t.take("N_M").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_m: t.take("PHI_M").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_m: t.take("PSI_M").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_f: t.take("OMG_F").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_f: t.take("PHI_F").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_f: t.take("PSI_F").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_f: t.take("CR_F").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_p: t.take("OMG_P").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_p: t.take("PHI_P").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_p: t.take("PSI_P").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_p: t.take("CR_P").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_b: t.take("OMG_B").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_b: t.take("PHI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_b: t.take("PSI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_b: t.take("CR_B").and_then(|c| c.into_float()).unwrap_or_default(),
            aust: t.take("AUST").and_then(|c| c.into_float()).unwrap_or_default(),
            ferr: t.take("FERR").and_then(|c| c.into_float()).unwrap_or_default(),
            pear: t.take("PEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bain: t.take("BAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            mart: t.take("MART").and_then(|c| c.into_float()).unwrap_or_default(),
            grk: t.take("GRK").and_then(|c| c.into_float()).unwrap_or_default(),
            grqr: t.take("GRQR").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gra: t.take("GRA").and_then(|c| c.into_float()).unwrap_or_default(),
            grb: t.take("GRB").and_then(|c| c.into_float()).unwrap_or_default(),
            expa: t.take("EXPA").and_then(|c| c.into_float()).unwrap_or_default(),
            expb: t.take("EXPB").and_then(|c| c.into_float()).unwrap_or_default(),
            grcc: t.take("GRCC").and_then(|c| c.into_float()).unwrap_or_default(),
            grcm: t.take("GRCM").and_then(|c| c.into_float()).unwrap_or_default(),
            heatn: t.take("HEATN").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            funca: t.take("FUNCA").and_then(|c| c.into_int()).unwrap_or_default(),
            funcb: t.take("FUNCB").and_then(|c| c.into_int()).unwrap_or_default(),
            funcm: t.take("FUNCM").and_then(|c| c.into_int()).unwrap_or_default(),
            tcvup: t.take("TCVUP").and_then(|c| c.into_float()).unwrap_or_default(),
            tcvlo: t.take("TCVLO").and_then(|c| c.into_float()).unwrap_or_default(),
            cvcrit: t.take("CVCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcvsl: t.take("TCVSL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsp: t.take("EPSP").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_249 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub lcem: Vec<i64>,
    pub prm: Vec<f64>,
    pub lcprm: Vec<i64>,
    pub lcsigy: Vec<i64>,
    pub beta: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub thick: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<f64>,
    pub ef1: Vec<f64>,
    pub lcef1: Vec<i64>,
    pub g23_1: Vec<f64>,
    pub g31_1: Vec<f64>,
    pub g12: Vec<f64>,
    pub lcg12: Vec<i64>,
    pub aloc12: Vec<f64>,
    pub gloc12: Vec<f64>,
    pub meth12: Vec<i64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub lcef2: Vec<i64>,
    pub g23_2: Vec<f64>,
    pub g31_2: Vec<f64>,
    pub g23: Vec<f64>,
    pub lcg23: Vec<i64>,
    pub aloc23: Vec<f64>,
    pub gloc23: Vec<f64>,
    pub meth23: Vec<i64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub lcef3: Vec<i64>,
    pub g23_3: Vec<f64>,
    pub g31_3: Vec<f64>,
}
impl MAT_249 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_249").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcem: t.take("LCEM").and_then(|c| c.into_int()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcprm: t.take("LCPRM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_float()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef1: t.take("LCEF1").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_1: t.take("G23_1").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_1: t.take("G31_1").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg12: t.take("LCG12").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc12: t.take("ALOC12").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc12: t.take("GLOC12").and_then(|c| c.into_float()).unwrap_or_default(),
            meth12: t.take("METH12").and_then(|c| c.into_int()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef2: t.take("LCEF2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_2: t.take("G23_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_2: t.take("G31_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg23: t.take("LCG23").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc23: t.take("ALOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc23: t.take("GLOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            meth23: t.take("METH23").and_then(|c| c.into_int()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef3: t.take("LCEF3").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_3: t.take("G23_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_3: t.take("G31_3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_249_CRASH {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub prm: Vec<f64>,
    pub lcsigy: Vec<i64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub visc: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub thick: Vec<f64>,
    pub vg1: Vec<f64>,
    pub vb1: Vec<f64>,
    pub vg2: Vec<f64>,
    pub vb2: Vec<f64>,
    pub vg3: Vec<f64>,
    pub vb3: Vec<f64>,
    pub vg4: Vec<f64>,
    pub vb4: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<i64>,
    pub ef1: Vec<i64>,
    pub lcef1: Vec<i64>,
    pub g23_1: Vec<i64>,
    pub g31_1: Vec<i64>,
    pub daf1: Vec<i64>,
    pub dam1: Vec<i64>,
    pub g12: Vec<i64>,
    pub lcg12: Vec<i64>,
    pub aloc12: Vec<i64>,
    pub gloc12: Vec<i64>,
    pub meth12: Vec<i64>,
    pub dam12: Vec<i64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub lcef2: Vec<i64>,
    pub g23_2: Vec<f64>,
    pub g31_2: Vec<f64>,
    pub daf2: Vec<i64>,
    pub dam2: Vec<i64>,
    pub g23: Vec<f64>,
    pub lcg23: Vec<i64>,
    pub aloc23: Vec<f64>,
    pub gloc23: Vec<f64>,
    pub meth23: Vec<i64>,
    pub dam23: Vec<i64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub lcef3: Vec<i64>,
    pub g23_3: Vec<f64>,
    pub g31_3: Vec<f64>,
    pub daf3: Vec<i64>,
    pub dam3: Vec<i64>,
    pub postv: Vec<f64>,
    pub viscs: Vec<f64>,
    pub ihis: Vec<f64>,
}
impl MAT_249_CRASH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_249_CRASH").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            visc: t.take("VISC").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            vg1: t.take("VG1").and_then(|c| c.into_float()).unwrap_or_default(),
            vb1: t.take("VB1").and_then(|c| c.into_float()).unwrap_or_default(),
            vg2: t.take("VG2").and_then(|c| c.into_float()).unwrap_or_default(),
            vb2: t.take("VB2").and_then(|c| c.into_float()).unwrap_or_default(),
            vg3: t.take("VG3").and_then(|c| c.into_float()).unwrap_or_default(),
            vb3: t.take("VB3").and_then(|c| c.into_float()).unwrap_or_default(),
            vg4: t.take("VG4").and_then(|c| c.into_float()).unwrap_or_default(),
            vb4: t.take("VB4").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_int()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcef1: t.take("LCEF1").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_1: t.take("G23_1").and_then(|c| c.into_int()).unwrap_or_default(),
            g31_1: t.take("G31_1").and_then(|c| c.into_int()).unwrap_or_default(),
            daf1: t.take("DAF1").and_then(|c| c.into_int()).unwrap_or_default(),
            dam1: t.take("DAM1").and_then(|c| c.into_int()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg12: t.take("LCG12").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc12: t.take("ALOC12").and_then(|c| c.into_int()).unwrap_or_default(),
            gloc12: t.take("GLOC12").and_then(|c| c.into_int()).unwrap_or_default(),
            meth12: t.take("METH12").and_then(|c| c.into_int()).unwrap_or_default(),
            dam12: t.take("DAM12").and_then(|c| c.into_int()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef2: t.take("LCEF2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_2: t.take("G23_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_2: t.take("G31_2").and_then(|c| c.into_float()).unwrap_or_default(),
            daf2: t.take("DAF2").and_then(|c| c.into_int()).unwrap_or_default(),
            dam2: t.take("DAM2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg23: t.take("LCG23").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc23: t.take("ALOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc23: t.take("GLOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            meth23: t.take("METH23").and_then(|c| c.into_int()).unwrap_or_default(),
            dam23: t.take("DAM23").and_then(|c| c.into_int()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef3: t.take("LCEF3").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_3: t.take("G23_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_3: t.take("G31_3").and_then(|c| c.into_float()).unwrap_or_default(),
            daf3: t.take("DAF3").and_then(|c| c.into_int()).unwrap_or_default(),
            dam3: t.take("DAM3").and_then(|c| c.into_int()).unwrap_or_default(),
            postv: t.take("POSTV").and_then(|c| c.into_float()).unwrap_or_default(),
            viscs: t.take("VISCS").and_then(|c| c.into_float()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_249_UDFIBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub prm: Vec<f64>,
    pub g: Vec<f64>,
    pub ezdef: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<f64>,
    pub ef1: Vec<f64>,
    pub kap1: Vec<f64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub kap2: Vec<f64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub kap3: Vec<f64>,
}
impl MAT_249_UDFIBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_249_UDfiber").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ezdef: t.take("EZDEF").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_float()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_float()).unwrap_or_default(),
            kap1: t.take("KAP1").and_then(|c| c.into_float()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            kap2: t.take("KAP2").and_then(|c| c.into_float()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            kap3: t.take("KAP3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_251 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub f_3: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub lcss: Vec<i64>,
    pub f_3_2: Vec<i64>,
    pub vp: Vec<f64>,
    pub hisvn: Vec<i64>,
    pub phase: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_251 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_251").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            hisvn: t.take("HISVN").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_252 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub flg: Vec<i64>,
    pub jcfl: Vec<i64>,
    pub dopt: Vec<i64>,
    pub lcss: Vec<i64>,
    pub tau0: Vec<f64>,
    pub q: Vec<f64>,
    pub b: Vec<f64>,
    pub h: Vec<f64>,
    pub c: Vec<f64>,
    pub gam0: Vec<f64>,
    pub gamm: Vec<f64>,
    pub a10: Vec<f64>,
    pub a20: Vec<f64>,
    pub a1h: Vec<f64>,
    pub a2h: Vec<f64>,
    pub a2s: Vec<f64>,
    pub pow: Vec<f64>,
    pub f: Vec<i64>,
    pub srfilt: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d1c: Vec<f64>,
    pub d2c: Vec<f64>,
}
impl MAT_252 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_252").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            flg: t.take("FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            jcfl: t.take("JCFL").and_then(|c| c.into_int()).unwrap_or_default(),
            dopt: t.take("DOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau0: t.take("TAU0").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            gam0: t.take("GAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamm: t.take("GAMM").and_then(|c| c.into_float()).unwrap_or_default(),
            a10: t.take("A10").and_then(|c| c.into_float()).unwrap_or_default(),
            a20: t.take("A20").and_then(|c| c.into_float()).unwrap_or_default(),
            a1h: t.take("A1H").and_then(|c| c.into_float()).unwrap_or_default(),
            a2h: t.take("A2H").and_then(|c| c.into_float()).unwrap_or_default(),
            a2s: t.take("A2S").and_then(|c| c.into_float()).unwrap_or_default(),
            pow: t.take("POW").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            srfilt: t.take("SRFILT").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d1c: t.take("D1C").and_then(|c| c.into_float()).unwrap_or_default(),
            d2c: t.take("D2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_254 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub n: Vec<i64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub mix: Vec<i64>,
    pub mixr: Vec<i64>,
    pub tastrt: Vec<f64>,
    pub tend: Vec<f64>,
    pub cte: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub epsini: Vec<i64>,
    pub dtemp: Vec<f64>,
    pub xastr: Vec<f64>,
    pub xaend: Vec<f64>,
    pub xa1pa1: Vec<i64>,
    pub xa1pa2: Vec<i64>,
    pub xa1pa3: Vec<i64>,
    pub xafpa: Vec<f64>,
    pub cteann: Vec<f64>,
    pub ptlaw: Vec<i64>,
    pub ptstr: Vec<i64>,
    pub ptend: Vec<i64>,
    pub ptx1: Vec<i64>,
    pub ptx2: Vec<i64>,
    pub ptx3: Vec<i64>,
    pub ptx4: Vec<i64>,
    pub ptx5: Vec<i64>,
    pub pttab1: Vec<i64>,
    pub pttab2: Vec<i64>,
    pub pttab3: Vec<i64>,
    pub pttab4: Vec<i64>,
    pub pttab5: Vec<i64>,
    pub pttab6: Vec<i64>,
    pub pttab7: Vec<i64>,
    pub pteps: Vec<i64>,
    pub ptrip: Vec<i64>,
    pub ptlat: Vec<i64>,
    pub postv: Vec<i64>,
    pub nushis: Vec<i64>,
    pub grai: Vec<f64>,
    pub t1phas: Vec<i64>,
    pub t2phas: Vec<i64>,
    pub fushi1: Vec<i64>,
    pub fushi2: Vec<i64>,
    pub fushi3: Vec<i64>,
    pub fushi4: Vec<i64>,
    pub fushi5: Vec<i64>,
    pub fushi6: Vec<i64>,
    pub fushi7: Vec<i64>,
    pub fushi8: Vec<i64>,
    pub sigy1: Vec<i64>,
    pub sigy2: Vec<i64>,
    pub sigy3: Vec<i64>,
    pub sigy4: Vec<i64>,
    pub sigy5: Vec<i64>,
    pub sigy6: Vec<i64>,
    pub sigy7: Vec<i64>,
    pub sigy8: Vec<i64>,
}
impl MAT_254 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_254").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            mix: t.take("MIX").and_then(|c| c.into_int()).unwrap_or_default(),
            mixr: t.take("MIXR").and_then(|c| c.into_int()).unwrap_or_default(),
            tastrt: t.take("TASTRT").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            cte: t.take("CTE").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            epsini: t.take("EPSINI").and_then(|c| c.into_int()).unwrap_or_default(),
            dtemp: t.take("DTEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            xastr: t.take("XASTR").and_then(|c| c.into_float()).unwrap_or_default(),
            xaend: t.take("XAEND").and_then(|c| c.into_float()).unwrap_or_default(),
            xa1pa1: t.take("XA1PA1").and_then(|c| c.into_int()).unwrap_or_default(),
            xa1pa2: t.take("XA1PA2").and_then(|c| c.into_int()).unwrap_or_default(),
            xa1pa3: t.take("XA1PA3").and_then(|c| c.into_int()).unwrap_or_default(),
            xafpa: t.take("XAFPA").and_then(|c| c.into_float()).unwrap_or_default(),
            cteann: t.take("CTEANN").and_then(|c| c.into_float()).unwrap_or_default(),
            ptlaw: t.take("PTLAW").and_then(|c| c.into_int()).unwrap_or_default(),
            ptstr: t.take("PTSTR").and_then(|c| c.into_int()).unwrap_or_default(),
            ptend: t.take("PTEND").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx1: t.take("PTX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx2: t.take("PTX2").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx3: t.take("PTX3").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx4: t.take("PTX4").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx5: t.take("PTX5").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab1: t.take("PTTAB1").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab2: t.take("PTTAB2").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab3: t.take("PTTAB3").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab4: t.take("PTTAB4").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab5: t.take("PTTAB5").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab6: t.take("PTTAB6").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab7: t.take("PTTAB7").and_then(|c| c.into_int()).unwrap_or_default(),
            pteps: t.take("PTEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ptrip: t.take("PTRIP").and_then(|c| c.into_int()).unwrap_or_default(),
            ptlat: t.take("PTLAT").and_then(|c| c.into_int()).unwrap_or_default(),
            postv: t.take("POSTV").and_then(|c| c.into_int()).unwrap_or_default(),
            nushis: t.take("NUSHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            grai: t.take("GRAI").and_then(|c| c.into_float()).unwrap_or_default(),
            t1phas: t.take("T1PHAS").and_then(|c| c.into_int()).unwrap_or_default(),
            t2phas: t.take("T2PHAS").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi1: t.take("FUSHI1").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi2: t.take("FUSHI2").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi3: t.take("FUSHI3").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi4: t.take("FUSHI4").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi5: t.take("FUSHI5").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi6: t.take("FUSHI6").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi7: t.take("FUSHI7").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi8: t.take("FUSHI8").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy1: t.take("SIGY1").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy2: t.take("SIGY2").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy3: t.take("SIGY3").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy4: t.take("SIGY4").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy5: t.take("SIGY5").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy6: t.take("SIGY6").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy7: t.take("SIGY7").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy8: t.take("SIGY8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_255 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub tabidc: Vec<i64>,
    pub tabidt: Vec<i64>,
    pub lalpha: Vec<i64>,
    pub alpha: Vec<f64>,
    pub tref: Vec<f64>,
}
impl MAT_255 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_255").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            tabidc: t.take("TABIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            tabidt: t.take("TABIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lalpha: t.take("LALPHA").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_256 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub mr: Vec<f64>,
    pub ll: Vec<f64>,
    pub nu0: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha: Vec<f64>,
    pub h0: Vec<f64>,
    pub scv: Vec<f64>,
    pub b: Vec<f64>,
    pub ecv: Vec<f64>,
    pub g0: Vec<f64>,
    pub s0: Vec<f64>,
}
impl MAT_256 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_256").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            mr: t.take("MR").and_then(|c| c.into_float()).unwrap_or_default(),
            ll: t.take("LL").and_then(|c| c.into_float()).unwrap_or_default(),
            nu0: t.take("NU0").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            scv: t.take("SCV").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ecv: t.take("ECV").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_258 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub a: Vec<f64>,
    pub ksi: Vec<f64>,
    pub theta1: Vec<f64>,
    pub q1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub q2: Vec<f64>,
    pub theta3: Vec<f64>,
    pub q3: Vec<f64>,
    pub cs: Vec<f64>,
    pub pdots: Vec<f64>,
    pub dcrit: Vec<f64>,
    pub wcb: Vec<f64>,
    pub wcl: Vec<f64>,
    pub wcs: Vec<f64>,
    pub cc: Vec<f64>,
    pub phi: Vec<f64>,
    pub gamma: Vec<f64>,
    pub thick: Vec<f64>,
}
impl MAT_258 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_258").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            ksi: t.take("KSI").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta3: t.take("THETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            pdots: t.take("PDOTS").and_then(|c| c.into_float()).unwrap_or_default(),
            dcrit: t.take("DCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            wcb: t.take("WCB").and_then(|c| c.into_float()).unwrap_or_default(),
            wcl: t.take("WCL").and_then(|c| c.into_float()).unwrap_or_default(),
            wcs: t.take("WCS").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_260A {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub sig00: Vec<f64>,
    pub sig45: Vec<f64>,
    pub sig90: Vec<f64>,
    pub sig_b: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub scale: Vec<f64>,
    pub ef0: Vec<f64>,
    pub plim: Vec<f64>,
    pub q: Vec<f64>,
    pub gama: Vec<f64>,
    pub m: Vec<f64>,
    pub beta: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_260A {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_260A").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig00: t.take("SIG00").and_then(|c| c.into_float()).unwrap_or_default(),
            sig45: t.take("SIG45").and_then(|c| c.into_float()).unwrap_or_default(),
            sig90: t.take("SIG90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_b: t.take("SIG_B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            ef0: t.take("EF0").and_then(|c| c.into_float()).unwrap_or_default(),
            plim: t.take("PLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_260B {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub p12: Vec<f64>,
    pub p22: Vec<f64>,
    pub p33: Vec<f64>,
    pub g12: Vec<f64>,
    pub g22: Vec<f64>,
    pub g33: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lfld: Vec<i64>,
    pub lfrac: Vec<i64>,
    pub w0: Vec<f64>,
    pub a: Vec<f64>,
    pub b0: Vec<f64>,
    pub gamma: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub scale: Vec<f64>,
    pub size0: Vec<f64>,
    pub tref: Vec<f64>,
    pub tmelt: Vec<f64>,
    pub m: Vec<f64>,
    pub eta: Vec<f64>,
    pub cp: Vec<f64>,
    pub tini: Vec<f64>,
    pub depso: Vec<f64>,
    pub depsad: Vec<f64>,
    pub ef0: Vec<f64>,
    pub plim: Vec<f64>,
    pub q: Vec<f64>,
    pub gama: Vec<f64>,
    pub m_2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_260B {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_260B").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            p12: t.take("P12").and_then(|c| c.into_float()).unwrap_or_default(),
            p22: t.take("P22").and_then(|c| c.into_float()).unwrap_or_default(),
            p33: t.take("P33").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g22: t.take("G22").and_then(|c| c.into_float()).unwrap_or_default(),
            g33: t.take("G33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lfld: t.take("LFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            lfrac: t.take("LFRAC").and_then(|c| c.into_int()).unwrap_or_default(),
            w0: t.take("W0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            size0: t.take("SIZE0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tmelt: t.take("TMELT").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tini: t.take("TINI").and_then(|c| c.into_float()).unwrap_or_default(),
            depso: t.take("DEPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            depsad: t.take("DEPSAD").and_then(|c| c.into_float()).unwrap_or_default(),
            ef0: t.take("EF0").and_then(|c| c.into_float()).unwrap_or_default(),
            plim: t.take("PLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            m_2: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_261 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub daf: Vec<f64>,
    pub dkf: Vec<f64>,
    pub dmf: Vec<f64>,
    pub efs: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub enkink: Vec<f64>,
    pub ena: Vec<f64>,
    pub enb: Vec<f64>,
    pub ent: Vec<f64>,
    pub enl: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sl: Vec<f64>,
    pub fio: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub puck: Vec<f64>,
    pub soft: Vec<f64>,
}
impl MAT_261 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_261").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            daf: t.take("DAF").and_then(|c| c.into_float()).unwrap_or_default(),
            dkf: t.take("DKF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmf: t.take("DMF").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            enkink: t.take("ENKINK").and_then(|c| c.into_float()).unwrap_or_default(),
            ena: t.take("ENA").and_then(|c| c.into_float()).unwrap_or_default(),
            enb: t.take("ENB").and_then(|c| c.into_float()).unwrap_or_default(),
            ent: t.take("ENT").and_then(|c| c.into_float()).unwrap_or_default(),
            enl: t.take("ENL").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            fio: t.take("FIO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            puck: t.take("PUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_262 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub daf: Vec<f64>,
    pub dkf: Vec<f64>,
    pub dmf: Vec<f64>,
    pub efs: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub dsf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub msg: Vec<f64>,
    pub gxc: Vec<f64>,
    pub gxt: Vec<f64>,
    pub gyc: Vec<f64>,
    pub gyt: Vec<f64>,
    pub gsl: Vec<f64>,
    pub gxco: Vec<f64>,
    pub gxto: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sl: Vec<f64>,
    pub xco: Vec<f64>,
    pub xto: Vec<f64>,
    pub fio: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub puck: Vec<f64>,
    pub soft: Vec<f64>,
    pub dt: Vec<f64>,
    pub epsf23: Vec<f64>,
    pub epsr23: Vec<f64>,
    pub tsmd23: Vec<f64>,
    pub epsf31: Vec<f64>,
    pub epsr31: Vec<f64>,
    pub tsmd31: Vec<f64>,
}
impl MAT_262 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_262").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            daf: t.take("DAF").and_then(|c| c.into_float()).unwrap_or_default(),
            dkf: t.take("DKF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmf: t.take("DMF").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            dsf: t.take("DSF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            msg: t.take("MSG").and_then(|c| c.into_float()).unwrap_or_default(),
            gxc: t.take("GXC").and_then(|c| c.into_float()).unwrap_or_default(),
            gxt: t.take("GXT").and_then(|c| c.into_float()).unwrap_or_default(),
            gyc: t.take("GYC").and_then(|c| c.into_float()).unwrap_or_default(),
            gyt: t.take("GYT").and_then(|c| c.into_float()).unwrap_or_default(),
            gsl: t.take("GSL").and_then(|c| c.into_float()).unwrap_or_default(),
            gxco: t.take("GXCO").and_then(|c| c.into_float()).unwrap_or_default(),
            gxto: t.take("GXTO").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            xco: t.take("XCO").and_then(|c| c.into_float()).unwrap_or_default(),
            xto: t.take("XTO").and_then(|c| c.into_float()).unwrap_or_default(),
            fio: t.take("FIO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            puck: t.take("PUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf23: t.take("EPSF23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr23: t.take("EPSR23").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd23: t.take("TSMD23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf31: t.take("EPSF31").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr31: t.take("EPSR31").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd31: t.take("TSMD31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_263 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub afr: Vec<i64>,
    pub nfunc: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub lcf: Vec<i64>,
    pub p3: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f_6: Vec<i64>,
    pub f_2_3: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub cc: Vec<f64>,
    pub f_7: Vec<i64>,
    pub pc1: Vec<f64>,
    pub pc2: Vec<f64>,
    pub pc3: Vec<f64>,
    pub pc4: Vec<f64>,
    pub pc5: Vec<f64>,
    pub pc6: Vec<f64>,
    pub pcc: Vec<f64>,
    pub f_8: Vec<i64>,
    pub vf1: Vec<f64>,
    pub vf2: Vec<f64>,
    pub vf3: Vec<f64>,
    pub vf4: Vec<f64>,
    pub vf5: Vec<f64>,
    pub f_9: Vec<i64>,
    pub f_2_4: Vec<i64>,
    pub f_3_2: Vec<i64>,
}
impl MAT_263 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_263").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            afr: t.take("AFR").and_then(|c| c.into_int()).unwrap_or_default(),
            nfunc: t.take("NFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_6: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            f_7: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pc1: t.take("PC1").and_then(|c| c.into_float()).unwrap_or_default(),
            pc2: t.take("PC2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc3: t.take("PC3").and_then(|c| c.into_float()).unwrap_or_default(),
            pc4: t.take("PC4").and_then(|c| c.into_float()).unwrap_or_default(),
            pc5: t.take("PC5").and_then(|c| c.into_float()).unwrap_or_default(),
            pc6: t.take("PC6").and_then(|c| c.into_float()).unwrap_or_default(),
            pcc: t.take("PCC").and_then(|c| c.into_float()).unwrap_or_default(),
            f_8: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            vf1: t.take("VF1").and_then(|c| c.into_float()).unwrap_or_default(),
            vf2: t.take("VF2").and_then(|c| c.into_float()).unwrap_or_default(),
            vf3: t.take("VF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf4: t.take("VF4").and_then(|c| c.into_float()).unwrap_or_default(),
            vf5: t.take("VF5").and_then(|c| c.into_float()).unwrap_or_default(),
            f_9: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_4: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_264 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub lct00r: Vec<i64>,
    pub lct00t: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub lcc00r: Vec<f64>,
    pub lcc00t: Vec<f64>,
    pub lcs45r: Vec<f64>,
    pub lcs45t: Vec<f64>,
    pub iflag: Vec<i64>,
    pub sfiepm: Vec<i64>,
    pub niter: Vec<i64>,
    pub aopt: Vec<f64>,
    pub lct90r: Vec<i64>,
    pub lct45r: Vec<i64>,
    pub lctthr: Vec<i64>,
    pub lcc90r: Vec<i64>,
    pub lcc45r: Vec<i64>,
    pub lccth: Vec<i64>,
    pub lct90t: Vec<i64>,
    pub lct45t: Vec<i64>,
    pub lcttht: Vec<i64>,
    pub lcc90t: Vec<i64>,
    pub lcc45t: Vec<i64>,
    pub lcctht: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta_2: Vec<f64>,
}
impl MAT_264 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_264").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lct00r: t.take("LCT00R").and_then(|c| c.into_int()).unwrap_or_default(),
            lct00t: t.take("LCT00T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc00r: t.take("LCC00R").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc00t: t.take("LCC00T").and_then(|c| c.into_float()).unwrap_or_default(),
            lcs45r: t.take("LCS45R").and_then(|c| c.into_float()).unwrap_or_default(),
            lcs45t: t.take("LCS45T").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfiepm: t.take("SFIEPM").and_then(|c| c.into_int()).unwrap_or_default(),
            niter: t.take("NITER").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lct90r: t.take("LCT90R").and_then(|c| c.into_int()).unwrap_or_default(),
            lct45r: t.take("LCT45R").and_then(|c| c.into_int()).unwrap_or_default(),
            lctthr: t.take("LCTTHR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc90r: t.take("LCC90R").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc45r: t.take("LCC45R").and_then(|c| c.into_int()).unwrap_or_default(),
            lccth: t.take("LCCTH").and_then(|c| c.into_int()).unwrap_or_default(),
            lct90t: t.take("LCT90T").and_then(|c| c.into_int()).unwrap_or_default(),
            lct45t: t.take("LCT45T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcttht: t.take("LCTTHT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc90t: t.take("LCC90T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc45t: t.take("LCC45T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcctht: t.take("LCCTHT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_2: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_265 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub model: Vec<i64>,
    pub stiff: Vec<f64>,
    pub rn: Vec<f64>,
    pub rs: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcf: Vec<i64>,
    pub lcupf: Vec<i64>,
    pub lcupr: Vec<i64>,
    pub upfn: Vec<f64>,
    pub upfs: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub uprn: Vec<f64>,
    pub uprs: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub beta3: Vec<f64>,
}
impl MAT_265 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_265").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupf: t.take("LCUPF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupr: t.take("LCUPR").and_then(|c| c.into_int()).unwrap_or_default(),
            upfn: t.take("UPFN").and_then(|c| c.into_float()).unwrap_or_default(),
            upfs: t.take("UPFS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            uprn: t.take("UPRN").and_then(|c| c.into_float()).unwrap_or_default(),
            uprs: t.take("UPRS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta3: t.take("BETA3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_266 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<f64>,
    pub sigma: Vec<f64>,
    pub mu: Vec<f64>,
    pub kappa: Vec<f64>,
    pub act: Vec<i64>,
    pub init: Vec<i64>,
    pub fid: Vec<i64>,
    pub orth: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub theta: Vec<f64>,
    pub act1: Vec<f64>,
    pub act2: Vec<f64>,
    pub act3: Vec<f64>,
    pub act4: Vec<f64>,
    pub act5: Vec<f64>,
    pub act6: Vec<f64>,
    pub act7: Vec<f64>,
    pub act8: Vec<f64>,
    pub act9: Vec<f64>,
    pub act10: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_266 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_266").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            act: t.take("ACT").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            orth: t.take("ORTH").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            act1: t.take("ACT1").and_then(|c| c.into_float()).unwrap_or_default(),
            act2: t.take("ACT2").and_then(|c| c.into_float()).unwrap_or_default(),
            act3: t.take("ACT3").and_then(|c| c.into_float()).unwrap_or_default(),
            act4: t.take("ACT4").and_then(|c| c.into_float()).unwrap_or_default(),
            act5: t.take("ACT5").and_then(|c| c.into_float()).unwrap_or_default(),
            act6: t.take("ACT6").and_then(|c| c.into_float()).unwrap_or_default(),
            act7: t.take("ACT7").and_then(|c| c.into_float()).unwrap_or_default(),
            act8: t.take("ACT8").and_then(|c| c.into_float()).unwrap_or_default(),
            act9: t.take("ACT9").and_then(|c| c.into_float()).unwrap_or_default(),
            act10: t.take("ACT10").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_267 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub mu: Vec<f64>,
    pub n: Vec<i64>,
    pub mull: Vec<i64>,
    pub vispl: Vec<i64>,
    pub visel: Vec<i64>,
    pub yld0: Vec<f64>,
    pub fp: Vec<f64>,
    pub gp: Vec<f64>,
    pub hp: Vec<f64>,
    pub lp: Vec<f64>,
    pub mp: Vec<f64>,
    pub np: Vec<f64>,
    pub pmu: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub time: Vec<f64>,
    pub vcon: Vec<f64>,
    pub q1: Vec<f64>,
    pub b1: Vec<f64>,
    pub q2: Vec<f64>,
    pub b2: Vec<f64>,
    pub q3: Vec<f64>,
    pub b3: Vec<f64>,
    pub q4: Vec<f64>,
    pub b4: Vec<f64>,
    pub k1: Vec<f64>,
    pub s1: Vec<f64>,
    pub k2: Vec<f64>,
    pub s2: Vec<f64>,
    pub k3: Vec<f64>,
    pub s3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub taui: Vec<f64>,
    pub betai_gammai: Vec<f64>,
    pub taui_2: Vec<f64>,
    pub betai_gammai_2: Vec<f64>,
    pub taui_3: Vec<f64>,
    pub betai_gammai_3: Vec<f64>,
    pub taui_4: Vec<f64>,
    pub betai_gammai_4: Vec<f64>,
    pub taui_5: Vec<f64>,
    pub betai_gammai_5: Vec<f64>,
    pub taui_6: Vec<f64>,
    pub betai_gammai_6: Vec<f64>,
    pub taui_7: Vec<f64>,
    pub betai_gammai_7: Vec<f64>,
}
impl MAT_267 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_267").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            mull: t.take("MULL").and_then(|c| c.into_int()).unwrap_or_default(),
            vispl: t.take("VISPL").and_then(|c| c.into_int()).unwrap_or_default(),
            visel: t.take("VISEL").and_then(|c| c.into_int()).unwrap_or_default(),
            yld0: t.take("YLD0").and_then(|c| c.into_float()).unwrap_or_default(),
            fp: t.take("FP").and_then(|c| c.into_float()).unwrap_or_default(),
            gp: t.take("GP").and_then(|c| c.into_float()).unwrap_or_default(),
            hp: t.take("HP").and_then(|c| c.into_float()).unwrap_or_default(),
            lp: t.take("LP").and_then(|c| c.into_float()).unwrap_or_default(),
            mp: t.take("MP").and_then(|c| c.into_float()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_float()).unwrap_or_default(),
            pmu: t.take("PMU").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            vcon: t.take("VCON").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            q4: t.take("Q4").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            taui: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_2: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_2: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_3: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_3: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_4: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_4: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_5: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_5: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_6: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_6: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_7: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_7: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_269 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub gv: Vec<f64>,
    pub n: Vec<f64>,
    pub nv: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub gam0: Vec<f64>,
    pub tauh: Vec<f64>,
}
impl MAT_269 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_269").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            gv: t.take("GV").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            gam0: t.take("GAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            tauh: t.take("TAUH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_270 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub lcem: Vec<i64>,
    pub lcpr: Vec<i64>,
    pub lcsy: Vec<i64>,
    pub lchr: Vec<i64>,
    pub lcat: Vec<i64>,
    pub beta: Vec<f64>,
    pub tastart: Vec<f64>,
    pub taend: Vec<f64>,
    pub tlstart: Vec<f64>,
    pub tlend: Vec<f64>,
    pub eghost: Vec<f64>,
    pub pghost: Vec<f64>,
    pub aghost: Vec<f64>,
}
impl MAT_270 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_270").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcem: t.take("LCEM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsy: t.take("LCSY").and_then(|c| c.into_int()).unwrap_or_default(),
            lchr: t.take("LCHR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tastart: t.take("TASTART").and_then(|c| c.into_float()).unwrap_or_default(),
            taend: t.take("TAEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tlstart: t.take("TLSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tlend: t.take("TLEND").and_then(|c| c.into_float()).unwrap_or_default(),
            eghost: t.take("EGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            pghost: t.take("PGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            aghost: t.take("AGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_271 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub p11: Vec<f64>,
    pub p22: Vec<f64>,
    pub p33: Vec<f64>,
    pub p12: Vec<f64>,
    pub p23: Vec<f64>,
    pub p13: Vec<f64>,
    pub e0: Vec<f64>,
    pub lck: Vec<i64>,
    pub pr: Vec<f64>,
    pub lcx: Vec<i64>,
    pub lcy: Vec<i64>,
    pub lcc: Vec<i64>,
    pub l: Vec<f64>,
    pub r: Vec<f64>,
    pub ca: Vec<f64>,
    pub cd: Vec<f64>,
    pub cv: Vec<f64>,
    pub p: Vec<f64>,
    pub lch: Vec<i64>,
    pub lcfi: Vec<i64>,
    pub sint: Vec<f64>,
    pub tzro: Vec<f64>,
    pub lcfk: Vec<i64>,
    pub lcfs2: Vec<i64>,
    pub dv1: Vec<f64>,
    pub dv2: Vec<f64>,
    pub ds1: Vec<f64>,
    pub ds2: Vec<f64>,
    pub omega: Vec<f64>,
    pub rgas: Vec<f64>,
    pub lcpr: Vec<i64>,
    pub lcfs3: Vec<i64>,
    pub lctau: Vec<i64>,
    pub alpha: Vec<f64>,
    pub lcfs1: Vec<i64>,
    pub gamma: Vec<f64>,
    pub l0: Vec<f64>,
    pub lcfks: Vec<i64>,
}
impl MAT_271 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_271").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            p11: t.take("P11").and_then(|c| c.into_float()).unwrap_or_default(),
            p22: t.take("P22").and_then(|c| c.into_float()).unwrap_or_default(),
            p33: t.take("P33").and_then(|c| c.into_float()).unwrap_or_default(),
            p12: t.take("P12").and_then(|c| c.into_float()).unwrap_or_default(),
            p23: t.take("P23").and_then(|c| c.into_float()).unwrap_or_default(),
            p13: t.take("P13").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            lck: t.take("LCK").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy: t.take("LCY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            cd: t.take("CD").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfi: t.take("LCFI").and_then(|c| c.into_int()).unwrap_or_default(),
            sint: t.take("SINT").and_then(|c| c.into_float()).unwrap_or_default(),
            tzro: t.take("TZRO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfk: t.take("LCFK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfs2: t.take("LCFS2").and_then(|c| c.into_int()).unwrap_or_default(),
            dv1: t.take("DV1").and_then(|c| c.into_float()).unwrap_or_default(),
            dv2: t.take("DV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ds1: t.take("DS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ds2: t.take("DS2").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            rgas: t.take("RGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfs3: t.take("LCFS3").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfs1: t.take("LCFS1").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfks: t.take("LCFKS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_273 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ecc: Vec<f64>,
    pub qh0: Vec<f64>,
    pub ft: Vec<f64>,
    pub fc: Vec<f64>,
    pub hp: Vec<f64>,
    pub ah: Vec<f64>,
    pub bh: Vec<f64>,
    pub ch: Vec<f64>,
    pub dh: Vec<f64>,
    pub as_: Vec<f64>,
    pub df: Vec<f64>,
    pub fc0: Vec<f64>,
    pub type_: Vec<f64>,
    pub bs: Vec<f64>,
    pub wf: Vec<f64>,
    pub wf1: Vec<f64>,
    pub ft1: Vec<f64>,
    pub strflg: Vec<f64>,
    pub failflg: Vec<f64>,
    pub efc: Vec<f64>,
}
impl MAT_273 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_273").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ecc: t.take("ECC").and_then(|c| c.into_float()).unwrap_or_default(),
            qh0: t.take("QH0").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            hp: t.take("HP").and_then(|c| c.into_float()).unwrap_or_default(),
            ah: t.take("AH").and_then(|c| c.into_float()).unwrap_or_default(),
            bh: t.take("BH").and_then(|c| c.into_float()).unwrap_or_default(),
            ch: t.take("CH").and_then(|c| c.into_float()).unwrap_or_default(),
            dh: t.take("DH").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            fc0: t.take("FC0").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            wf: t.take("WF").and_then(|c| c.into_float()).unwrap_or_default(),
            wf1: t.take("WF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ft1: t.take("FT1").and_then(|c| c.into_float()).unwrap_or_default(),
            strflg: t.take("STRFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            failflg: t.take("FAILFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            efc: t.take("EFC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_274 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr21: Vec<f64>,
    pub pr32: Vec<f64>,
    pub pr31: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub g13: Vec<f64>,
    pub e3c: Vec<f64>,
    pub cc: Vec<f64>,
    pub twok: Vec<f64>,
    pub s01: Vec<f64>,
    pub a01: Vec<f64>,
    pub b01: Vec<f64>,
    pub c01: Vec<f64>,
    pub s02: Vec<f64>,
    pub a02: Vec<f64>,
    pub b02: Vec<f64>,
    pub c02: Vec<f64>,
    pub s03: Vec<f64>,
    pub a03: Vec<f64>,
    pub b03: Vec<f64>,
    pub c03: Vec<f64>,
    pub s04: Vec<f64>,
    pub a04: Vec<f64>,
    pub b04: Vec<f64>,
    pub c04: Vec<f64>,
    pub s05: Vec<f64>,
    pub a05: Vec<f64>,
    pub b05: Vec<f64>,
    pub c05: Vec<f64>,
    pub prp1: Vec<f64>,
    pub prp2: Vec<f64>,
    pub prp4: Vec<f64>,
    pub prp5: Vec<f64>,
    pub asig: Vec<f64>,
    pub bsig: Vec<f64>,
    pub csig: Vec<f64>,
    pub tau0: Vec<f64>,
    pub atau: Vec<f64>,
    pub btau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_274 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_274").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr21: t.take("PR21").and_then(|c| c.into_float()).unwrap_or_default(),
            pr32: t.take("PR32").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            g13: t.take("G13").and_then(|c| c.into_float()).unwrap_or_default(),
            e3c: t.take("E3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            twok: t.take("TWOK").and_then(|c| c.into_float()).unwrap_or_default(),
            s01: t.take("S01").and_then(|c| c.into_float()).unwrap_or_default(),
            a01: t.take("A01").and_then(|c| c.into_float()).unwrap_or_default(),
            b01: t.take("B01").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            s02: t.take("S02").and_then(|c| c.into_float()).unwrap_or_default(),
            a02: t.take("A02").and_then(|c| c.into_float()).unwrap_or_default(),
            b02: t.take("B02").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            s03: t.take("S03").and_then(|c| c.into_float()).unwrap_or_default(),
            a03: t.take("A03").and_then(|c| c.into_float()).unwrap_or_default(),
            b03: t.take("B03").and_then(|c| c.into_float()).unwrap_or_default(),
            c03: t.take("C03").and_then(|c| c.into_float()).unwrap_or_default(),
            s04: t.take("S04").and_then(|c| c.into_float()).unwrap_or_default(),
            a04: t.take("A04").and_then(|c| c.into_float()).unwrap_or_default(),
            b04: t.take("B04").and_then(|c| c.into_float()).unwrap_or_default(),
            c04: t.take("C04").and_then(|c| c.into_float()).unwrap_or_default(),
            s05: t.take("S05").and_then(|c| c.into_float()).unwrap_or_default(),
            a05: t.take("A05").and_then(|c| c.into_float()).unwrap_or_default(),
            b05: t.take("B05").and_then(|c| c.into_float()).unwrap_or_default(),
            c05: t.take("C05").and_then(|c| c.into_float()).unwrap_or_default(),
            prp1: t.take("PRP1").and_then(|c| c.into_float()).unwrap_or_default(),
            prp2: t.take("PRP2").and_then(|c| c.into_float()).unwrap_or_default(),
            prp4: t.take("PRP4").and_then(|c| c.into_float()).unwrap_or_default(),
            prp5: t.take("PRP5").and_then(|c| c.into_float()).unwrap_or_default(),
            asig: t.take("ASIG").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig: t.take("BSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            csig: t.take("CSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            tau0: t.take("TAU0").and_then(|c| c.into_float()).unwrap_or_default(),
            atau: t.take("ATAU").and_then(|c| c.into_float()).unwrap_or_default(),
            btau: t.take("BTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_275 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub a0: Vec<f64>,
    pub b0: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub m: Vec<f64>,
    pub kapas: Vec<f64>,
    pub kapa0: Vec<f64>,
    pub shear: Vec<f64>,
    pub a0_2: Vec<f64>,
    pub b0_2: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub b1_2: Vec<f64>,
    pub m_2: Vec<f64>,
    pub kapas_2: Vec<f64>,
    pub kapa0_2: Vec<f64>,
    pub shear_2: Vec<f64>,
    pub a0_3: Vec<f64>,
    pub b0_3: Vec<f64>,
    pub a1_3: Vec<f64>,
    pub b1_3: Vec<f64>,
    pub m_3: Vec<f64>,
    pub kapas_3: Vec<f64>,
    pub kapa0_3: Vec<f64>,
    pub shear_3: Vec<f64>,
    pub a0_4: Vec<f64>,
    pub b0_4: Vec<f64>,
    pub a1_4: Vec<f64>,
    pub b1_4: Vec<f64>,
    pub m_4: Vec<f64>,
    pub kapas_4: Vec<f64>,
    pub kapa0_4: Vec<f64>,
    pub shear_4: Vec<f64>,
    pub a0_5: Vec<f64>,
    pub b0_5: Vec<f64>,
    pub a1_5: Vec<f64>,
    pub b1_5: Vec<f64>,
    pub m_5: Vec<f64>,
    pub kapas_5: Vec<f64>,
    pub kapa0_5: Vec<f64>,
    pub shear_5: Vec<f64>,
    pub a0_6: Vec<f64>,
    pub b0_6: Vec<f64>,
    pub a1_6: Vec<f64>,
    pub b1_6: Vec<f64>,
    pub m_6: Vec<f64>,
    pub kapas_6: Vec<f64>,
    pub kapa0_6: Vec<f64>,
    pub shear_6: Vec<f64>,
}
impl MAT_275 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_275").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_2: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_2: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_2: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_2: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_2: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_2: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_2: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_3: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_3: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_3: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_3: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_3: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_3: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_3: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_3: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_4: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_4: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_4: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_4: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_4: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_4: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_4: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_4: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_5: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_5: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_5: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_5: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_5: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_5: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_5: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_5: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_6: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_6: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_6: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_6: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_6: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_6: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_6: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_6: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_276 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_276 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_276").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_277 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub chexp1: Vec<f64>,
    pub chexp2: Vec<f64>,
    pub chexp3: Vec<f64>,
    pub lcchexp: Vec<i64>,
    pub lcthexp: Vec<i64>,
    pub r: Vec<f64>,
    pub trefexp: Vec<f64>,
    pub docrefexp: Vec<f64>,
    pub wlftref: Vec<f64>,
    pub wlfa: Vec<f64>,
    pub wlfb: Vec<f64>,
    pub lcg0: Vec<i64>,
    pub lck0: Vec<i64>,
    pub idoc: Vec<f64>,
    pub incr: Vec<i64>,
    pub qcure: Vec<f64>,
    pub gi: Vec<f64>,
    pub betagi: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_277 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_277").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp1: t.take("CHEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp2: t.take("CHEXP2").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp3: t.take("CHEXP3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcchexp: t.take("LCCHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcthexp: t.take("LCTHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            trefexp: t.take("TREFEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            docrefexp: t.take("DOCREFEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            wlftref: t.take("WLFTREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfa: t.take("WLFA").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfb: t.take("WLFB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg0: t.take("LCG0").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
            idoc: t.take("IDOC").and_then(|c| c.into_float()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
            qcure: t.take("QCURE").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betagi: t.take("BETAGI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_278 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub eu: Vec<f64>,
    pub c: Vec<f64>,
    pub eka: Vec<f64>,
    pub eua: Vec<f64>,
    pub vmb: Vec<f64>,
    pub ekb: Vec<f64>,
    pub thl: Vec<f64>,
    pub ta: Vec<f64>,
    pub thi1: Vec<f64>,
    pub thi2: Vec<f64>,
    pub w: Vec<f64>,
    pub span: Vec<f64>,
    pub thick: Vec<f64>,
    pub h: Vec<f64>,
    pub area: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr13: Vec<f64>,
    pub pr23: Vec<f64>,
    pub aopt: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub vyarn: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub chexp1: Vec<f64>,
    pub chexp2: Vec<f64>,
    pub chexp3: Vec<f64>,
    pub lcchexp: Vec<i64>,
    pub lcthexp: Vec<i64>,
    pub r: Vec<f64>,
    pub tref: Vec<f64>,
    pub docref: Vec<f64>,
    pub wlftref: Vec<f64>,
    pub wlfa: Vec<f64>,
    pub wlfb: Vec<f64>,
    pub lcg0: Vec<i64>,
    pub lck0: Vec<i64>,
    pub idoc: Vec<f64>,
    pub incr: Vec<i64>,
    pub qcure: Vec<f64>,
    pub gi: Vec<f64>,
    pub betagi: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_278 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_278").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            eu: t.take("EU").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            eka: t.take("EKA").and_then(|c| c.into_float()).unwrap_or_default(),
            eua: t.take("EUA").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            ekb: t.take("EKB").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            ta: t.take("TA").and_then(|c| c.into_float()).unwrap_or_default(),
            thi1: t.take("THI1").and_then(|c| c.into_float()).unwrap_or_default(),
            thi2: t.take("THI2").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            span: t.take("SPAN").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr13: t.take("PR13").and_then(|c| c.into_float()).unwrap_or_default(),
            pr23: t.take("PR23").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            vyarn: t.take("VYARN").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp1: t.take("CHEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp2: t.take("CHEXP2").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp3: t.take("CHEXP3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcchexp: t.take("LCCHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcthexp: t.take("LCTHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            docref: t.take("DOCREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlftref: t.take("WLFTREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfa: t.take("WLFA").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfb: t.take("WLFB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg0: t.take("LCG0").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
            idoc: t.take("IDOC").and_then(|c| c.into_float()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
            qcure: t.take("QCURE").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betagi: t.take("BETAGi").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("Ki").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKi").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_279 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub en0: Vec<f64>,
    pub et0: Vec<f64>,
    pub en1: Vec<f64>,
    pub et1: Vec<f64>,
    pub t0n: Vec<f64>,
    pub dn: Vec<f64>,
    pub t1n: Vec<f64>,
    pub t0t: Vec<f64>,
    pub dt: Vec<f64>,
    pub t1t: Vec<f64>,
    pub e3c: Vec<f64>,
    pub cc: Vec<f64>,
    pub asig: Vec<f64>,
    pub bsig: Vec<f64>,
    pub csig: Vec<f64>,
    pub failn: Vec<f64>,
    pub failt: Vec<f64>,
}
impl MAT_279 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_279").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            en0: t.take("EN0").and_then(|c| c.into_float()).unwrap_or_default(),
            et0: t.take("ET0").and_then(|c| c.into_float()).unwrap_or_default(),
            en1: t.take("EN1").and_then(|c| c.into_float()).unwrap_or_default(),
            et1: t.take("ET1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0n: t.take("T0N").and_then(|c| c.into_float()).unwrap_or_default(),
            dn: t.take("DN").and_then(|c| c.into_float()).unwrap_or_default(),
            t1n: t.take("T1N").and_then(|c| c.into_float()).unwrap_or_default(),
            t0t: t.take("T0T").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1t: t.take("T1T").and_then(|c| c.into_float()).unwrap_or_default(),
            e3c: t.take("E3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            asig: t.take("ASIG").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig: t.take("BSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            csig: t.take("CSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            failn: t.take("FAILN").and_then(|c| c.into_float()).unwrap_or_default(),
            failt: t.take("FAILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_280 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub imod: Vec<f64>,
    pub ilaw: Vec<f64>,
    pub fmod: Vec<f64>,
    pub ft: Vec<f64>,
    pub fc: Vec<f64>,
    pub at: Vec<f64>,
    pub bt: Vec<i64>,
    pub ac: Vec<i64>,
    pub bc: Vec<f64>,
    pub ftscl: Vec<f64>,
    pub sfsti: Vec<f64>,
    pub sfstr: Vec<f64>,
    pub crin: Vec<f64>,
    pub ecrcl: Vec<f64>,
    pub ncycr: Vec<f64>,
    pub nipf: Vec<f64>,
    pub epscr: Vec<f64>,
    pub engcrt: Vec<f64>,
    pub radcrt: Vec<f64>,
    pub ratenl: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_280 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_280").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            imod: t.take("IMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            ilaw: t.take("ILAW").and_then(|c| c.into_float()).unwrap_or_default(),
            fmod: t.take("FMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_int()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_int()).unwrap_or_default(),
            bc: t.take("BC").and_then(|c| c.into_float()).unwrap_or_default(),
            ftscl: t.take("FTSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsti: t.take("SFSTI").and_then(|c| c.into_float()).unwrap_or_default(),
            sfstr: t.take("SFSTR").and_then(|c| c.into_float()).unwrap_or_default(),
            crin: t.take("CRIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrcl: t.take("ECRCL").and_then(|c| c.into_float()).unwrap_or_default(),
            ncycr: t.take("NCYCR").and_then(|c| c.into_float()).unwrap_or_default(),
            nipf: t.take("NIPF").and_then(|c| c.into_float()).unwrap_or_default(),
            epscr: t.take("EPSCR").and_then(|c| c.into_float()).unwrap_or_default(),
            engcrt: t.take("ENGCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            radcrt: t.take("RADCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            ratenl: t.take("RATENL").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_291 {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub em: Vec<f64>,
    pub ea: Vec<f64>,
    pub prm: Vec<f64>,
    pub pra: Vec<f64>,
    pub aopt: Vec<i64>,
    pub stype: Vec<i64>,
    pub cpm: Vec<f64>,
    pub cpa: Vec<f64>,
    pub lh: Vec<f64>,
    pub tc: Vec<f64>,
    pub tmf: Vec<f64>,
    pub tms: Vec<f64>,
    pub tas: Vec<f64>,
    pub taf: Vec<f64>,
    pub a1i: Vec<f64>,
    pub a2i: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub ki: Vec<f64>,
    pub mi: Vec<f64>,
    pub kl: Vec<f64>,
    pub ml: Vec<f64>,
    pub a1s: Vec<f64>,
    pub a2s: Vec<f64>,
    pub bs: Vec<f64>,
    pub cs: Vec<f64>,
    pub ks: Vec<f64>,
    pub ms: Vec<f64>,
    pub d0l: Vec<f64>,
    pub d0m: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
    pub n11: Vec<f64>,
    pub n22: Vec<f64>,
    pub n33: Vec<f64>,
    pub n44: Vec<f64>,
    pub n55: Vec<f64>,
    pub n66: Vec<f64>,
    pub n12: Vec<f64>,
    pub n23: Vec<f64>,
    pub n34: Vec<f64>,
    pub n45: Vec<f64>,
    pub n56: Vec<f64>,
    pub n13: Vec<f64>,
    pub n24: Vec<f64>,
    pub n35: Vec<f64>,
    pub n46: Vec<f64>,
    pub n14: Vec<f64>,
    pub n25: Vec<f64>,
    pub n36: Vec<f64>,
    pub n15: Vec<f64>,
    pub n26: Vec<f64>,
    pub n16: Vec<f64>,
}
impl MAT_291 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_291").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cpm: t.take("CPM").and_then(|c| c.into_float()).unwrap_or_default(),
            cpa: t.take("CPA").and_then(|c| c.into_float()).unwrap_or_default(),
            lh: t.take("LH").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            tmf: t.take("TMF").and_then(|c| c.into_float()).unwrap_or_default(),
            tms: t.take("TMS").and_then(|c| c.into_float()).unwrap_or_default(),
            tas: t.take("TAS").and_then(|c| c.into_float()).unwrap_or_default(),
            taf: t.take("TAF").and_then(|c| c.into_float()).unwrap_or_default(),
            a1i: t.take("A1I").and_then(|c| c.into_float()).unwrap_or_default(),
            a2i: t.take("A2I").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("BI").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("CI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            mi: t.take("MI").and_then(|c| c.into_float()).unwrap_or_default(),
            kl: t.take("KL").and_then(|c| c.into_float()).unwrap_or_default(),
            ml: t.take("ML").and_then(|c| c.into_float()).unwrap_or_default(),
            a1s: t.take("A1S").and_then(|c| c.into_float()).unwrap_or_default(),
            a2s: t.take("A2S").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            ks: t.take("KS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            d0l: t.take("D0L").and_then(|c| c.into_float()).unwrap_or_default(),
            d0m: t.take("D0M").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_float()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_float()).unwrap_or_default(),
            n33: t.take("N33").and_then(|c| c.into_float()).unwrap_or_default(),
            n44: t.take("N44").and_then(|c| c.into_float()).unwrap_or_default(),
            n55: t.take("N55").and_then(|c| c.into_float()).unwrap_or_default(),
            n66: t.take("N66").and_then(|c| c.into_float()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_float()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_float()).unwrap_or_default(),
            n34: t.take("N34").and_then(|c| c.into_float()).unwrap_or_default(),
            n45: t.take("N45").and_then(|c| c.into_float()).unwrap_or_default(),
            n56: t.take("N56").and_then(|c| c.into_float()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_float()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_float()).unwrap_or_default(),
            n35: t.take("N35").and_then(|c| c.into_float()).unwrap_or_default(),
            n46: t.take("N46").and_then(|c| c.into_float()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_float()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_float()).unwrap_or_default(),
            n36: t.take("N36").and_then(|c| c.into_float()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_float()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_float()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_292 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub gt: Vec<f64>,
    pub gs: Vec<f64>,
}
impl MAT_292 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_292").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            gt: t.take("Gt").and_then(|c| c.into_float()).unwrap_or_default(),
            gs: t.take("Gs").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_292A {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub em: Vec<f64>,
    pub vfm: Vec<f64>,
    pub gfm: Vec<f64>,
    pub fopt: Vec<f64>,
    pub fcf: Vec<f64>,
    pub fcm: Vec<f64>,
    pub fcfc: Vec<f64>,
    pub fcmc: Vec<f64>,
    pub fcd: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_292A {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_292A").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            vfm: t.take("VFM").and_then(|c| c.into_float()).unwrap_or_default(),
            gfm: t.take("GFM").and_then(|c| c.into_float()).unwrap_or_default(),
            fopt: t.take("FOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            fcf: t.take("FCF").and_then(|c| c.into_float()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_float()).unwrap_or_default(),
            fcfc: t.take("FCFC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcmc: t.take("FCMC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcd: t.take("FCD").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_293 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub et: Vec<f64>,
    pub ec: Vec<f64>,
    pub pr: Vec<f64>,
    pub g121: Vec<f64>,
    pub g122: Vec<f64>,
    pub g123: Vec<f64>,
    pub g124: Vec<f64>,
    pub g125: Vec<f64>,
    pub g126: Vec<f64>,
    pub gammal: Vec<f64>,
    pub vf: Vec<f64>,
    pub ef3: Vec<f64>,
    pub vf23: Vec<f64>,
    pub em: Vec<f64>,
    pub vm: Vec<f64>,
    pub epsilon: Vec<f64>,
    pub theta: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g: Vec<f64>,
}
impl MAT_293 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_293").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            g121: t.take("G121").and_then(|c| c.into_float()).unwrap_or_default(),
            g122: t.take("G122").and_then(|c| c.into_float()).unwrap_or_default(),
            g123: t.take("G123").and_then(|c| c.into_float()).unwrap_or_default(),
            g124: t.take("G124").and_then(|c| c.into_float()).unwrap_or_default(),
            g125: t.take("G125").and_then(|c| c.into_float()).unwrap_or_default(),
            g126: t.take("G126").and_then(|c| c.into_float()).unwrap_or_default(),
            gammal: t.take("GAMMAL").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf23: t.take("VF23").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            epsilon: t.take("EPSILON").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_295 {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub aopt: Vec<f64>,
    pub title: Vec<String>,
    pub itype: Vec<i64>,
    pub beta: Vec<f64>,
    pub nu: Vec<f64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub mu3: Vec<f64>,
    pub mu4: Vec<f64>,
    pub mu5: Vec<f64>,
    pub mu6: Vec<f64>,
    pub mu7: Vec<f64>,
    pub mu8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub title_2: Vec<String>,
    pub atype: Vec<i64>,
    pub intype: Vec<i64>,
    pub nf: Vec<i64>,
    pub theta: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub ftype: Vec<i64>,
    pub fcid: Vec<i64>,
    pub k1_2: Vec<f64>,
    pub k2_2: Vec<f64>,
    pub ftype_2: Vec<i64>,
    pub flcid: Vec<i64>,
    pub e: Vec<f64>,
    pub r0norm: Vec<f64>,
    pub h0norm: Vec<f64>,
    pub k1_3: Vec<f64>,
    pub k2_3: Vec<f64>,
    pub title_3: Vec<String>,
    pub actype: Vec<i64>,
    pub acdir: Vec<i64>,
    pub acid: Vec<i64>,
    pub acthr: Vec<f64>,
    pub sf: Vec<f64>,
    pub ss: Vec<f64>,
    pub sn: Vec<f64>,
    pub t0: Vec<f64>,
    pub ca2ion: Vec<f64>,
    pub ca2ionm: Vec<f64>,
    pub n: Vec<f64>,
    pub taumax: Vec<f64>,
    pub stf: Vec<f64>,
    pub b_2: Vec<f64>,
    pub l0: Vec<f64>,
    pub l: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub mr: Vec<f64>,
    pub tr: Vec<f64>,
    pub l_2: Vec<f64>,
    pub eta: Vec<f64>,
    pub t0_2: Vec<f64>,
    pub ca2ion_2: Vec<f64>,
    pub ca2ion50: Vec<f64>,
    pub n_2: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub f: Vec<f64>,
    pub l_3: Vec<f64>,
    pub eta_2: Vec<f64>,
    pub t0_3: Vec<f64>,
    pub ca2ion50_2: Vec<f64>,
    pub ca2ionmax: Vec<f64>,
    pub n_3: Vec<f64>,
    pub sigmax_2: Vec<f64>,
    pub f_2: Vec<f64>,
    pub ca2ion0: Vec<f64>,
    pub tca: Vec<f64>,
    pub l_4: Vec<f64>,
    pub eta_3: Vec<f64>,
    pub fseid: Vec<i64>,
    pub flid: Vec<i64>,
    pub fvid: Vec<i64>,
    pub alphaid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub f_3: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta_2: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_295 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_295").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            mu3: t.take("MU3").and_then(|c| c.into_float()).unwrap_or_default(),
            mu4: t.take("MU4").and_then(|c| c.into_float()).unwrap_or_default(),
            mu5: t.take("MU5").and_then(|c| c.into_float()).unwrap_or_default(),
            mu6: t.take("MU6").and_then(|c| c.into_float()).unwrap_or_default(),
            mu7: t.take("MU7").and_then(|c| c.into_float()).unwrap_or_default(),
            mu8: t.take("MU8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            title_2: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            intype: t.take("INTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_int()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            fcid: t.take("FCID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1_2: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype_2: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            r0norm: t.take("R0NORM").and_then(|c| c.into_float()).unwrap_or_default(),
            h0norm: t.take("H0NORM").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_3: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_3: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            title_3: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            actype: t.take("ACTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            acdir: t.take("ACDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            acid: t.take("ACID").and_then(|c| c.into_int()).unwrap_or_default(),
            acthr: t.take("ACTHR").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion: t.take("CA2ION").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ionm: t.take("CA2IONM").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            taumax: t.take("TAUMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            stf: t.take("STF").and_then(|c| c.into_float()).unwrap_or_default(),
            b_2: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            mr: t.take("MR").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            l_2: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            t0_2: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion_2: t.take("CA2ION").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion50: t.take("CA2ION50").and_then(|c| c.into_float()).unwrap_or_default(),
            n_2: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            l_3: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta_2: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            t0_3: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion50_2: t.take("CA2ION50").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ionmax: t.take("CA2IONMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n_3: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax_2: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion0: t.take("CA2ION0").and_then(|c| c.into_float()).unwrap_or_default(),
            tca: t.take("TCA").and_then(|c| c.into_float()).unwrap_or_default(),
            l_4: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta_3: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fseid: t.take("FSEID").and_then(|c| c.into_int()).unwrap_or_default(),
            flid: t.take("FLID").and_then(|c| c.into_int()).unwrap_or_default(),
            fvid: t.take("FVID").and_then(|c| c.into_int()).unwrap_or_default(),
            alphaid: t.take("ALPHAID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_2: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_296 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub alpha: Vec<f64>,
    pub a1: Vec<f64>,
    pub ratioqr: Vec<f64>,
    pub xi: Vec<f64>,
    pub m: Vec<f64>,
    pub s0: Vec<f64>,
    pub h0: Vec<f64>,
    pub a2: Vec<f64>,
    pub sbar: Vec<f64>,
    pub n: Vec<f64>,
    pub lcym: Vec<i64>,
    pub tref: Vec<f64>,
}
impl MAT_296 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_296").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            ratioqr: t.take("RATIOQR").and_then(|c| c.into_float()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            sbar: t.take("SBAR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            lcym: t.take("LCYM").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_3_PARAMETER_BARLAT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub f40: Vec<i64>,
    pub pb: Vec<f64>,
    pub hta: Vec<i64>,
    pub htb: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub htflag: Vec<f64>,
    pub usrfail: Vec<f64>,
    pub lcbi: Vec<f64>,
    pub lcsh: Vec<f64>,
}
impl MAT_3_PARAMETER_BARLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_3-PARAMETER_BARLAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pb: t.take("PB").and_then(|c| c.into_float()).unwrap_or_default(),
            hta: t.take("HTA").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbi: t.take("LCBI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsh: t.take("LCSH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_3_PARAMETER_BARLAT_NLP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub f40: Vec<i64>,
    pub pb: Vec<f64>,
    pub nlp: Vec<i64>,
    pub htb: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub htflag: Vec<f64>,
    pub usrfail: Vec<f64>,
    pub lcbi: Vec<f64>,
    pub lcsh: Vec<f64>,
}
impl MAT_3_PARAMETER_BARLAT_NLP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_3-PARAMETER_BARLAT_NLP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pb: t.take("PB").and_then(|c| c.into_float()).unwrap_or_default(),
            nlp: t.take("NLP").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcbi: t.take("LCBI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsh: t.take("LCSH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_305 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub alphat: Vec<f64>,
    pub beta: Vec<f64>,
    pub vp: Vec<f64>,
    pub tol: Vec<f64>,
    pub yb: Vec<f64>,
    pub qdef: Vec<f64>,
    pub r: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub minrt: Vec<f64>,
    pub post: Vec<f64>,
    pub odesol: Vec<f64>,
    pub asig0: Vec<f64>,
    pub bsig0: Vec<f64>,
    pub asigs: Vec<f64>,
    pub bsigs: Vec<f64>,
    pub asigss: Vec<f64>,
    pub bsigss: Vec<f64>,
    pub aeps: Vec<f64>,
    pub beps: Vec<f64>,
    pub thres: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha: Vec<f64>,
    pub nud: Vec<f64>,
    pub u0: Vec<f64>,
    pub k: Vec<f64>,
    pub nu: Vec<f64>,
    pub bnu: Vec<f64>,
    pub t50: Vec<f64>,
    pub n: Vec<f64>,
    pub a50: Vec<f64>,
    pub d: Vec<f64>,
    pub gsf: Vec<f64>,
    pub p: Vec<f64>,
    pub q: Vec<f64>,
    pub qrex: Vec<f64>,
}
impl MAT_305 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_305").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alphat: t.take("ALPHAT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            yb: t.take("YB").and_then(|c| c.into_float()).unwrap_or_default(),
            qdef: t.take("QDEF").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            minrt: t.take("MINRT").and_then(|c| c.into_float()).unwrap_or_default(),
            post: t.take("POST").and_then(|c| c.into_float()).unwrap_or_default(),
            odesol: t.take("ODESOL").and_then(|c| c.into_float()).unwrap_or_default(),
            asig0: t.take("ASIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig0: t.take("BSIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            asigs: t.take("ASIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            bsigs: t.take("BSIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            asigss: t.take("ASIGSS").and_then(|c| c.into_float()).unwrap_or_default(),
            bsigss: t.take("BSIGSS").and_then(|c| c.into_float()).unwrap_or_default(),
            aeps: t.take("AEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            beps: t.take("BEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            thres: t.take("THRES").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            nud: t.take("NUD").and_then(|c| c.into_float()).unwrap_or_default(),
            u0: t.take("U0").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            bnu: t.take("BNU").and_then(|c| c.into_float()).unwrap_or_default(),
            t50: t.take("T50").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a50: t.take("A50").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            gsf: t.take("GSF").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            qrex: t.take("QREX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_318 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub mua: Vec<f64>,
    pub thetah: Vec<f64>,
    pub lambl: Vec<f64>,
    pub kappa: Vec<f64>,
    pub tauha: Vec<f64>,
    pub a: Vec<f64>,
    pub ma: Vec<f64>,
    pub n: Vec<f64>,
    pub mubi: Vec<f64>,
    pub mubf: Vec<f64>,
    pub beta: Vec<f64>,
    pub tauhb: Vec<f64>,
    pub mb: Vec<f64>,
    pub muc: Vec<f64>,
    pub q: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theata0: Vec<f64>,
    pub ibulk: Vec<f64>,
    pub ig: Vec<f64>,
    pub tsstif: Vec<f64>,
    pub gamma0: Vec<f64>,
}
impl MAT_318 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_318").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            mua: t.take("MUA").and_then(|c| c.into_float()).unwrap_or_default(),
            thetah: t.take("THETAH").and_then(|c| c.into_float()).unwrap_or_default(),
            lambl: t.take("LAMBL").and_then(|c| c.into_float()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            tauha: t.take("TAUHA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            ma: t.take("MA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            mubi: t.take("MUBI").and_then(|c| c.into_float()).unwrap_or_default(),
            mubf: t.take("MUBF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tauhb: t.take("TAUHB").and_then(|c| c.into_float()).unwrap_or_default(),
            mb: t.take("MB").and_then(|c| c.into_float()).unwrap_or_default(),
            muc: t.take("MUC").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theata0: t.take("THEATA0").and_then(|c| c.into_float()).unwrap_or_default(),
            ibulk: t.take("IBULK").and_then(|c| c.into_float()).unwrap_or_default(),
            ig: t.take("IG").and_then(|c| c.into_float()).unwrap_or_default(),
            tsstif: t.take("TSSTIF").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma0: t.take("GAMMA0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_319 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub dynvis: Vec<f64>,
    pub sften: Vec<f64>,
}
impl MAT_319 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_319").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            dynvis: t.take("DYNVIS").and_then(|c| c.into_float()).unwrap_or_default(),
            sften: t.take("SFTEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_326 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<i64>,
    pub lc: Vec<i64>,
    pub uc: Vec<i64>,
    pub eten: Vec<f64>,
    pub etsr: Vec<f64>,
    pub emem: Vec<f64>,
    pub pr: Vec<f64>,
    pub ps: Vec<i64>,
}
impl MAT_326 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_326").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            uc: t.take("UC").and_then(|c| c.into_int()).unwrap_or_default(),
            eten: t.take("ETEN").and_then(|c| c.into_float()).unwrap_or_default(),
            etsr: t.take("ETSR").and_then(|c| c.into_float()).unwrap_or_default(),
            emem: t.take("EMEM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_3PARAMETER_BARLAT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub f40: Vec<i64>,
    pub pb: Vec<f64>,
    pub hta: Vec<i64>,
    pub htb: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub htflag: Vec<f64>,
}
impl MAT_3PARAMETER_BARLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_3PARAMETER_BARLAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            pb: t.take("PB").and_then(|c| c.into_float()).unwrap_or_default(),
            hta: t.take("HTA").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_4A_MICROMEC {
    pub mid: Vec<i64>,
    pub mmopt: Vec<f64>,
    pub bupd: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub failm: Vec<f64>,
    pub failf: Vec<i64>,
    pub numint: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fvf: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub fl: Vec<f64>,
    pub fd: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub a11: Vec<f64>,
    pub a22: Vec<f64>,
    pub rof: Vec<f64>,
    pub el: Vec<f64>,
    pub et: Vec<f64>,
    pub glt: Vec<f64>,
    pub prtl: Vec<f64>,
    pub prtt: Vec<f64>,
    pub xt: Vec<f64>,
    pub unused_4: Vec<i64>,
    pub unused_2_3: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub slimxt: Vec<f64>,
    pub ncyred: Vec<f64>,
    pub rom: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigyt: Vec<f64>,
    pub etant: Vec<f64>,
    pub unused_6: Vec<i64>,
    pub unused_2_4: Vec<i64>,
    pub eps0: Vec<f64>,
    pub c: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub unused_2_5: Vec<i64>,
    pub unused_3_3: Vec<i64>,
    pub lcdi: Vec<i64>,
    pub upf: Vec<f64>,
    pub unused_4_3: Vec<i64>,
    pub ncyred2: Vec<f64>,
}
impl MAT_4A_MICROMEC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_4A_MICROMEC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mmopt: t.take("MMOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            bupd: t.take("BUPD").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            failm: t.take("FAILM").and_then(|c| c.into_float()).unwrap_or_default(),
            failf: t.take("FAILF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fvf: t.take("FVF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            fl: t.take("FL").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            rof: t.take("ROF").and_then(|c| c.into_float()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            glt: t.take("GLT").and_then(|c| c.into_float()).unwrap_or_default(),
            prtl: t.take("PRTL").and_then(|c| c.into_float()).unwrap_or_default(),
            prtt: t.take("PRTT").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_3: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            slimxt: t.take("SLIMXT").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyred: t.take("NCYRED").and_then(|c| c.into_float()).unwrap_or_default(),
            rom: t.take("ROM").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyt: t.take("SIGYT").and_then(|c| c.into_float()).unwrap_or_default(),
            etant: t.take("ETANT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_4: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_5: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdi: t.take("LCDI").and_then(|c| c.into_int()).unwrap_or_default(),
            upf: t.take("UPF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4_3: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyred2: t.take("NCYRED2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ACOUSTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub beta: Vec<f64>,
    pub cf: Vec<f64>,
    pub atmos: Vec<f64>,
    pub grav: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub xn: Vec<f64>,
    pub yn: Vec<f64>,
    pub zn: Vec<f64>,
}
impl MAT_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ACOUSTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            cf: t.take("CF").and_then(|c| c.into_float()).unwrap_or_default(),
            atmos: t.take("ATMOS").and_then(|c| c.into_float()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            xn: t.take("XN").and_then(|c| c.into_float()).unwrap_or_default(),
            yn: t.take("YN").and_then(|c| c.into_float()).unwrap_or_default(),
            zn: t.take("ZN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ACOUSTIC_COMPLEX {
    pub mid: Vec<i64>,
    pub rhor: Vec<f64>,
    pub bulkr: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub bulki: Vec<f64>,
    pub lcidrr: Vec<i64>,
    pub lcidkr: Vec<i64>,
    pub lcidri: Vec<i64>,
    pub lcidki: Vec<i64>,
}
impl MAT_ACOUSTIC_COMPLEX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ACOUSTIC_COMPLEX").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rhor: t.take("RHOR").and_then(|c| c.into_float()).unwrap_or_default(),
            bulkr: t.take("BULKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOI").and_then(|c| c.into_float()).unwrap_or_default(),
            bulki: t.take("BULKI").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidkr: t.take("LCIDKR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidri: t.take("LCIDRI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidki: t.take("LCIDKI").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ACOUSTIC_DAMP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub cee: Vec<f64>,
    pub beta: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_6: Vec<i64>,
    pub vdc: Vec<f64>,
    pub beta2: Vec<f64>,
}
impl MAT_ACOUSTIC_DAMP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ACOUSTIC_DAMP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            cee: t.take("CEE").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_int()).unwrap_or_default(),
            vdc: t.take("VDC").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ACOUSTIC_POROUS_DB {
    pub mid: Vec<i64>,
    pub rho0: Vec<f64>,
    pub cee0: Vec<f64>,
    pub sigma: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_ACOUSTIC_POROUS_DB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ACOUSTIC_POROUS_DB").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho0: t.take("RHO0").and_then(|c| c.into_float()).unwrap_or_default(),
            cee0: t.take("CEE0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_AIRBAG_POROSITY_LEAKAGE {
    pub mid: Vec<i64>,
    pub x2_flc: Vec<f64>,
    pub x3_fac: Vec<f64>,
    pub ela: Vec<f64>,
    pub fvopt: Vec<f64>,
    pub x0: Vec<f64>,
    pub x1: Vec<f64>,
    pub f: Vec<i64>,
}
impl MAT_ADD_AIRBAG_POROSITY_LEAKAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_AIRBAG_POROSITY_LEAKAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            x2_flc: t.take("X2/FLC").and_then(|c| c.into_float()).unwrap_or_default(),
            x3_fac: t.take("X3/FAC").and_then(|c| c.into_float()).unwrap_or_default(),
            ela: t.take("ELA").and_then(|c| c.into_float()).unwrap_or_default(),
            fvopt: t.take("FVOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_CHEM_SHRINKAGE {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
}
impl MAT_ADD_CHEM_SHRINKAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_CHEM_SHRINKAGE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_COHESIVE {
    pub pid: Vec<i64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub thick: Vec<f64>,
}
impl MAT_ADD_COHESIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_COHESIVE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_DAMAGE_DIEM {
    pub mid: Vec<i64>,
    pub ndiemc: Vec<f64>,
    pub dinit: Vec<i64>,
    pub deps: Vec<f64>,
    pub numfip: Vec<f64>,
    pub dityp: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub detyp: Vec<f64>,
    pub dctyp: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub q4: Vec<f64>,
}
impl MAT_ADD_DAMAGE_DIEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_DAMAGE_DIEM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndiemc: t.take("NDIEMC").and_then(|c| c.into_float()).unwrap_or_default(),
            dinit: t.take("DINIT").and_then(|c| c.into_int()).unwrap_or_default(),
            deps: t.take("DEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            dityp: t.take("DITYP").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            detyp: t.take("DETYP").and_then(|c| c.into_float()).unwrap_or_default(),
            dctyp: t.take("DCTYP").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            q4: t.take("Q4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_DAMAGE_GISSMO {
    pub mid: Vec<i64>,
    pub f: Vec<i64>,
    pub dtyp: Vec<f64>,
    pub refsz: Vec<f64>,
    pub numfip: Vec<f64>,
    pub lcsdg: Vec<i64>,
    pub ecrit: Vec<f64>,
    pub dmgexp: Vec<f64>,
    pub dcrit: Vec<f64>,
    pub fadexp: Vec<f64>,
    pub lcregd: Vec<i64>,
    pub instf: Vec<i64>,
    pub lcsrs: Vec<i64>,
    pub shrf: Vec<f64>,
    pub biaxf: Vec<f64>,
    pub lcdlim: Vec<i64>,
    pub midfail: Vec<f64>,
    pub hisvn: Vec<f64>,
    pub soft: Vec<f64>,
    pub lp2bi: Vec<f64>,
}
impl MAT_ADD_DAMAGE_GISSMO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_DAMAGE_GISSMO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            dtyp: t.take("DTYP").and_then(|c| c.into_float()).unwrap_or_default(),
            refsz: t.take("REFSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            ecrit: t.take("ECRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            dcrit: t.take("DCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            fadexp: t.take("FADEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcregd: t.take("LCREGD").and_then(|c| c.into_int()).unwrap_or_default(),
            instf: t.take("INSTF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrs: t.take("LCSRS").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            biaxf: t.take("BIAXF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdlim: t.take("LCDLIM").and_then(|c| c.into_int()).unwrap_or_default(),
            midfail: t.take("MIDFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            hisvn: t.take("HISVN").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lp2bi: t.take("LP2BI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_DAMAGE_GISSMO_STOCHASTIC {
    pub mid: Vec<i64>,
    pub f: Vec<i64>,
    pub dtyp: Vec<f64>,
    pub refsz: Vec<f64>,
    pub numfip: Vec<f64>,
    pub lcsdg: Vec<i64>,
    pub ecrit: Vec<f64>,
    pub dmgexp: Vec<f64>,
    pub dcrit: Vec<f64>,
    pub fadexp: Vec<f64>,
    pub lcregd: Vec<i64>,
    pub instf: Vec<i64>,
    pub lcsrs: Vec<i64>,
    pub shrf: Vec<f64>,
    pub biaxf: Vec<f64>,
    pub lcdlim: Vec<i64>,
    pub midfail: Vec<f64>,
    pub hisvn: Vec<f64>,
    pub soft: Vec<f64>,
    pub lp2bi: Vec<f64>,
}
impl MAT_ADD_DAMAGE_GISSMO_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_DAMAGE_GISSMO_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            dtyp: t.take("DTYP").and_then(|c| c.into_float()).unwrap_or_default(),
            refsz: t.take("REFSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            ecrit: t.take("ECRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            dcrit: t.take("DCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            fadexp: t.take("FADEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcregd: t.take("LCREGD").and_then(|c| c.into_int()).unwrap_or_default(),
            instf: t.take("INSTF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrs: t.take("LCSRS").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            biaxf: t.take("BIAXF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdlim: t.take("LCDLIM").and_then(|c| c.into_int()).unwrap_or_default(),
            midfail: t.take("MIDFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            hisvn: t.take("HISVN").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lp2bi: t.take("LP2BI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_EROSION {
    pub mid: Vec<i64>,
    pub excl: Vec<f64>,
    pub mxpres: Vec<f64>,
    pub mneps: Vec<f64>,
    pub effeps: Vec<f64>,
    pub voleps: Vec<f64>,
    pub numfip: Vec<f64>,
    pub ncs: Vec<f64>,
    pub mnpres: Vec<f64>,
    pub sigp1: Vec<f64>,
    pub sigvm: Vec<f64>,
    pub mxeps: Vec<f64>,
    pub epssh: Vec<f64>,
    pub sigth: Vec<f64>,
    pub impulse: Vec<f64>,
    pub failtm: Vec<f64>,
    pub idam: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_6: Vec<i64>,
    pub lcregd: Vec<i64>,
    pub lcfld: Vec<i64>,
    pub nsff: Vec<i64>,
    pub epsthin: Vec<f64>,
    pub engcrt: Vec<f64>,
    pub radcrt: Vec<f64>,
    pub lceps12: Vec<i64>,
    pub lceps13: Vec<i64>,
    pub lcepsmx: Vec<i64>,
    pub dteflt: Vec<f64>,
    pub volfrac: Vec<f64>,
    pub mxtmp: Vec<f64>,
    pub dtmin: Vec<f64>,
}
impl MAT_ADD_EROSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_EROSION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            excl: t.take("EXCL").and_then(|c| c.into_float()).unwrap_or_default(),
            mxpres: t.take("MXPRES").and_then(|c| c.into_float()).unwrap_or_default(),
            mneps: t.take("MNEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            effeps: t.take("EFFEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            voleps: t.take("VOLEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ncs: t.take("NCS").and_then(|c| c.into_float()).unwrap_or_default(),
            mnpres: t.take("MNPRES").and_then(|c| c.into_float()).unwrap_or_default(),
            sigp1: t.take("SIGP1").and_then(|c| c.into_float()).unwrap_or_default(),
            sigvm: t.take("SIGVM").and_then(|c| c.into_float()).unwrap_or_default(),
            mxeps: t.take("MXEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            epssh: t.take("EPSSH").and_then(|c| c.into_float()).unwrap_or_default(),
            sigth: t.take("SIGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            impulse: t.take("IMPULSE").and_then(|c| c.into_float()).unwrap_or_default(),
            failtm: t.take("FAILTM").and_then(|c| c.into_float()).unwrap_or_default(),
            idam: t.take("IDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcregd: t.take("LCREGD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfld: t.take("LCFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            nsff: t.take("NSFF").and_then(|c| c.into_int()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            engcrt: t.take("ENGCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            radcrt: t.take("RADCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps12: t.take("LCEPS12").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps13: t.take("LCEPS13").and_then(|c| c.into_int()).unwrap_or_default(),
            lcepsmx: t.take("LCEPSMX").and_then(|c| c.into_int()).unwrap_or_default(),
            dteflt: t.take("DTEFLT").and_then(|c| c.into_float()).unwrap_or_default(),
            volfrac: t.take("VOLFRAC").and_then(|c| c.into_float()).unwrap_or_default(),
            mxtmp: t.take("MXTMP").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmin: t.take("DTMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_FATIGUE {
    pub mid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub ltype: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub sthres: Vec<f64>,
    pub snlimt: Vec<i64>,
    pub sntype: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub ai: Vec<f64>,
    pub bi: Vec<f64>,
    pub sthresi: Vec<f64>,
}
impl MAT_ADD_FATIGUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_FATIGUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ltype: t.take("LTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            sthres: t.take("STHRES").and_then(|c| c.into_float()).unwrap_or_default(),
            snlimt: t.take("SNLIMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sntype: t.take("SNTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            ai: t.take("Ai").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            sthresi: t.take("STHRESi").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_FATIGUE_EN {
    pub mid: Vec<i64>,
    pub kp: Vec<f64>,
    pub np: Vec<f64>,
    pub sigmaf: Vec<f64>,
    pub epsp: Vec<f64>,
    pub bp: Vec<f64>,
    pub cp: Vec<f64>,
    pub e: Vec<i64>,
    pub pr: Vec<f64>,
}
impl MAT_ADD_FATIGUE_EN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_FATIGUE_EN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmaf: t.take("SIGMAF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsp: t.take("EPSP").and_then(|c| c.into_float()).unwrap_or_default(),
            bp: t.take("BP").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_GENERALIZED_DAMAGE {
    pub mid: Vec<i64>,
    pub idam: Vec<i64>,
    pub dtyp: Vec<i64>,
    pub refsz: Vec<f64>,
    pub numfip: Vec<f64>,
    pub unused: Vec<i64>,
    pub pddt: Vec<i64>,
    pub nhis: Vec<i64>,
    pub his1: Vec<i64>,
    pub his2: Vec<i64>,
    pub his3: Vec<i64>,
    pub iflg1: Vec<i64>,
    pub iflg2: Vec<i64>,
    pub iflg3: Vec<i64>,
    pub d11: Vec<i64>,
    pub d22: Vec<i64>,
    pub d33: Vec<i64>,
    pub d44: Vec<i64>,
    pub d55: Vec<i64>,
    pub d66: Vec<i64>,
    pub d12: Vec<i64>,
    pub d21: Vec<i64>,
    pub d24: Vec<i64>,
    pub d42: Vec<i64>,
    pub d14: Vec<i64>,
    pub d41: Vec<i64>,
    pub lcsdg: Vec<i64>,
    pub ecrit: Vec<f64>,
    pub dmgexp: Vec<f64>,
    pub dcrit: Vec<f64>,
    pub fadexp: Vec<f64>,
    pub lcreg: Vec<i64>,
    pub lcsrs: Vec<i64>,
    pub shrf: Vec<f64>,
    pub biaxf: Vec<f64>,
    pub lcdlim: Vec<i64>,
    pub midfail: Vec<f64>,
    pub nfloc: Vec<f64>,
}
impl MAT_ADD_GENERALIZED_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_GENERALIZED_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            idam: t.take("IDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            dtyp: t.take("DTYP").and_then(|c| c.into_int()).unwrap_or_default(),
            refsz: t.take("REFSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            numfip: t.take("NUMFIP").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            pddt: t.take("PDDT").and_then(|c| c.into_int()).unwrap_or_default(),
            nhis: t.take("NHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            his1: t.take("HIS1").and_then(|c| c.into_int()).unwrap_or_default(),
            his2: t.take("HIS2").and_then(|c| c.into_int()).unwrap_or_default(),
            his3: t.take("HIS3").and_then(|c| c.into_int()).unwrap_or_default(),
            iflg1: t.take("IFLG1").and_then(|c| c.into_int()).unwrap_or_default(),
            iflg2: t.take("IFLG2").and_then(|c| c.into_int()).unwrap_or_default(),
            iflg3: t.take("IFLG3").and_then(|c| c.into_int()).unwrap_or_default(),
            d11: t.take("D11").and_then(|c| c.into_int()).unwrap_or_default(),
            d22: t.take("D22").and_then(|c| c.into_int()).unwrap_or_default(),
            d33: t.take("D33").and_then(|c| c.into_int()).unwrap_or_default(),
            d44: t.take("D44").and_then(|c| c.into_int()).unwrap_or_default(),
            d55: t.take("D55").and_then(|c| c.into_int()).unwrap_or_default(),
            d66: t.take("D66").and_then(|c| c.into_int()).unwrap_or_default(),
            d12: t.take("D12").and_then(|c| c.into_int()).unwrap_or_default(),
            d21: t.take("D21").and_then(|c| c.into_int()).unwrap_or_default(),
            d24: t.take("D24").and_then(|c| c.into_int()).unwrap_or_default(),
            d42: t.take("D42").and_then(|c| c.into_int()).unwrap_or_default(),
            d14: t.take("D14").and_then(|c| c.into_int()).unwrap_or_default(),
            d41: t.take("D41").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsdg: t.take("LCSDG").and_then(|c| c.into_int()).unwrap_or_default(),
            ecrit: t.take("ECRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgexp: t.take("DMGEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            dcrit: t.take("DCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            fadexp: t.take("FADEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcreg: t.take("LCREG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrs: t.take("LCSRS").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            biaxf: t.take("BIAXF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdlim: t.take("LCDLIM").and_then(|c| c.into_int()).unwrap_or_default(),
            midfail: t.take("MIDFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nfloc: t.take("NFLOC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_INELASTICITY {
    pub mid: Vec<i64>,
    pub nielinks: Vec<i64>,
    pub unused: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub nielaws: Vec<i64>,
    pub weight: Vec<f64>,
    pub law: Vec<i64>,
    pub model: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_5: Vec<f64>,
}
impl MAT_ADD_INELASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_INELASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            nielinks: t.take("NIELINKS").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            nielaws: t.take("NIELAWS").and_then(|c| c.into_int()).unwrap_or_default(),
            weight: t.take("WEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            law: t.take("LAW").and_then(|c| c.into_int()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PERMEABILITY {
    pub mid: Vec<i64>,
    pub perm: Vec<f64>,
    pub permy: Vec<i64>,
    pub permz: Vec<i64>,
    pub thexp: Vec<f64>,
    pub lckz: Vec<i64>,
    pub pmtyp: Vec<i64>,
}
impl MAT_ADD_PERMEABILITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PERMEABILITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            perm: t.take("PERM").and_then(|c| c.into_float()).unwrap_or_default(),
            permy: t.take("PERMY").and_then(|c| c.into_int()).unwrap_or_default(),
            permz: t.take("PERMZ").and_then(|c| c.into_int()).unwrap_or_default(),
            thexp: t.take("THEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            lckz: t.take("LCKZ").and_then(|c| c.into_int()).unwrap_or_default(),
            pmtyp: t.take("PMTYP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PORE_AIR {
    pub mid: Vec<i64>,
    pub pa_rho: Vec<f64>,
    pub pa_pre: Vec<f64>,
    pub pore: Vec<f64>,
    pub dvimin: Vec<f64>,
    pub perm1: Vec<f64>,
    pub perm2: Vec<f64>,
    pub perm3: Vec<f64>,
    pub cdarcy: Vec<f64>,
    pub cdf: Vec<f64>,
    pub lcpgd1: Vec<i64>,
    pub lcpgd2: Vec<i64>,
    pub lcpgd3: Vec<i64>,
}
impl MAT_ADD_PORE_AIR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PORE_AIR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            pa_rho: t.take("PA_RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            pa_pre: t.take("PA_PRE").and_then(|c| c.into_float()).unwrap_or_default(),
            pore: t.take("PORE").and_then(|c| c.into_float()).unwrap_or_default(),
            dvimin: t.take("DVIMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            perm1: t.take("PERM1").and_then(|c| c.into_float()).unwrap_or_default(),
            perm2: t.take("PERM2").and_then(|c| c.into_float()).unwrap_or_default(),
            perm3: t.take("PERM3").and_then(|c| c.into_float()).unwrap_or_default(),
            cdarcy: t.take("CDARCY").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpgd1: t.take("LCPGD1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpgd2: t.take("LCPGD2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpgd3: t.take("LCPGD3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PROPERTY_DEPENDENCE {
    pub mid: Vec<i64>,
    pub prop: Vec<String>,
    pub lcid: Vec<i64>,
}
impl MAT_ADD_PROPERTY_DEPENDENCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PROPERTY_DEPENDENCE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            prop: t.take("PROP").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PROPERTY_DEPENDENCE_FREQ {
    pub mid: Vec<i64>,
    pub prop: Vec<String>,
    pub lcid: Vec<i64>,
}
impl MAT_ADD_PROPERTY_DEPENDENCE_FREQ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PROPERTY_DEPENDENCE_FREQ").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            prop: t.take("PROP").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PROPERTY_DEPENDENCE_TIME {
    pub mid: Vec<i64>,
    pub prop: Vec<String>,
    pub lcid: Vec<i64>,
}
impl MAT_ADD_PROPERTY_DEPENDENCE_TIME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PROPERTY_DEPENDENCE_TIME").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            prop: t.take("PROP").and_then(|c| c.into_str()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_PZELECTRIC {
    pub mid: Vec<String>,
    pub dtype: Vec<String>,
    pub gpt: Vec<i64>,
    pub aopt: Vec<i64>,
    pub dxx: Vec<f64>,
    pub dyy: Vec<f64>,
    pub dzz: Vec<f64>,
    pub dxy: Vec<f64>,
    pub dxz: Vec<f64>,
    pub dyz: Vec<f64>,
    pub px11: Vec<f64>,
    pub px22: Vec<f64>,
    pub px33: Vec<f64>,
    pub px12: Vec<f64>,
    pub px13: Vec<f64>,
    pub px23: Vec<f64>,
    pub py11: Vec<f64>,
    pub py22: Vec<f64>,
    pub py33: Vec<f64>,
    pub py12: Vec<f64>,
    pub px13_2: Vec<f64>,
    pub py23: Vec<f64>,
    pub pz11: Vec<f64>,
    pub pz22: Vec<f64>,
    pub pz33: Vec<f64>,
    pub pz12: Vec<f64>,
    pub pz13: Vec<f64>,
    pub pz23: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_ADD_PZELECTRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_PZELECTRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_str()).unwrap_or_default(),
            dtype: t.take("DTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            gpt: t.take("GPT").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            dxx: t.take("DXX").and_then(|c| c.into_float()).unwrap_or_default(),
            dyy: t.take("DYY").and_then(|c| c.into_float()).unwrap_or_default(),
            dzz: t.take("DZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dxy: t.take("DXY").and_then(|c| c.into_float()).unwrap_or_default(),
            dxz: t.take("DXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dyz: t.take("DYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            px11: t.take("PX11").and_then(|c| c.into_float()).unwrap_or_default(),
            px22: t.take("PX22").and_then(|c| c.into_float()).unwrap_or_default(),
            px33: t.take("PX33").and_then(|c| c.into_float()).unwrap_or_default(),
            px12: t.take("PX12").and_then(|c| c.into_float()).unwrap_or_default(),
            px13: t.take("PX13").and_then(|c| c.into_float()).unwrap_or_default(),
            px23: t.take("PX23").and_then(|c| c.into_float()).unwrap_or_default(),
            py11: t.take("PY11").and_then(|c| c.into_float()).unwrap_or_default(),
            py22: t.take("PY22").and_then(|c| c.into_float()).unwrap_or_default(),
            py33: t.take("PY33").and_then(|c| c.into_float()).unwrap_or_default(),
            py12: t.take("PY12").and_then(|c| c.into_float()).unwrap_or_default(),
            px13_2: t.take("PX13").and_then(|c| c.into_float()).unwrap_or_default(),
            py23: t.take("PY23").and_then(|c| c.into_float()).unwrap_or_default(),
            pz11: t.take("PZ11").and_then(|c| c.into_float()).unwrap_or_default(),
            pz22: t.take("PZ22").and_then(|c| c.into_float()).unwrap_or_default(),
            pz33: t.take("PZ33").and_then(|c| c.into_float()).unwrap_or_default(),
            pz12: t.take("PZ12").and_then(|c| c.into_float()).unwrap_or_default(),
            pz13: t.take("PZ13").and_then(|c| c.into_float()).unwrap_or_default(),
            pz23: t.take("PZ23").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("_").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("__2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("__3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_SOC_EXPANSION {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mult: Vec<f64>,
    pub lcidy: Vec<i64>,
    pub multy: Vec<f64>,
    pub lcidz: Vec<i64>,
    pub multz: Vec<f64>,
}
impl MAT_ADD_SOC_EXPANSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_SOC_EXPANSION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mult: t.take("MULT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidy: t.take("LCIDY").and_then(|c| c.into_int()).unwrap_or_default(),
            multy: t.take("MULTY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidz: t.take("LCIDZ").and_then(|c| c.into_int()).unwrap_or_default(),
            multz: t.take("MULTZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADD_THERMAL_EXPANSION {
    pub pid: Vec<i64>,
    pub lcid: Vec<i64>,
    pub mult: Vec<f64>,
    pub lcid_2: Vec<i64>,
    pub multy: Vec<f64>,
    pub lcid_3: Vec<i64>,
    pub multz: Vec<f64>,
}
impl MAT_ADD_THERMAL_EXPANSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADD_THERMAL_EXPANSION").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            mult: t.take("MULT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_2: t.take("LCID_2").and_then(|c| c.into_int()).unwrap_or_default(),
            multy: t.take("MULTY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_3: t.take("LCID_3").and_then(|c| c.into_int()).unwrap_or_default(),
            multz: t.take("MULTZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ADHESIVE_CURING_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub chexp1: Vec<f64>,
    pub chexp2: Vec<f64>,
    pub chexp3: Vec<f64>,
    pub lcchexp: Vec<i64>,
    pub lcthexp: Vec<i64>,
    pub r: Vec<f64>,
    pub trefexp: Vec<f64>,
    pub docrefexp: Vec<f64>,
    pub wlftref: Vec<f64>,
    pub wlfa: Vec<f64>,
    pub wlfb: Vec<f64>,
    pub lcg0: Vec<i64>,
    pub lck0: Vec<i64>,
    pub idoc: Vec<f64>,
    pub incr: Vec<i64>,
    pub qcure: Vec<f64>,
    pub gi: Vec<f64>,
    pub betagi: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_ADHESIVE_CURING_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ADHESIVE_CURING_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp1: t.take("CHEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp2: t.take("CHEXP2").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp3: t.take("CHEXP3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcchexp: t.take("LCCHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcthexp: t.take("LCTHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            trefexp: t.take("TREFEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            docrefexp: t.take("DOCREFEXP").and_then(|c| c.into_float()).unwrap_or_default(),
            wlftref: t.take("WLFTREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfa: t.take("WLFA").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfb: t.take("WLFB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg0: t.take("LCG0").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
            idoc: t.take("IDOC").and_then(|c| c.into_float()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
            qcure: t.take("QCURE").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betagi: t.take("BETAGI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_01 {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
}
impl MAT_ALE_01 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_01").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_02 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub f60: Vec<f64>,
    pub rn: Vec<f64>,
}
impl MAT_ALE_02 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_02").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_03 {
    pub mid: Vec<i64>,
    pub iadiab: Vec<i64>,
    pub runiv: Vec<f64>,
    pub cvmass1: Vec<f64>,
    pub cvmass2: Vec<f64>,
    pub cvmass3: Vec<f64>,
    pub cvmass4: Vec<f64>,
    pub cvmass5: Vec<f64>,
    pub cvmass6: Vec<f64>,
    pub cvmass7: Vec<f64>,
    pub cvmass8: Vec<f64>,
    pub cpmass1: Vec<f64>,
    pub cpmass2: Vec<f64>,
    pub cpmass3: Vec<f64>,
    pub cpmass4: Vec<f64>,
    pub cpmass5: Vec<f64>,
    pub cpmass6: Vec<f64>,
    pub cpmass7: Vec<f64>,
    pub cpmass8: Vec<f64>,
    pub molwt1: Vec<f64>,
    pub molwt2: Vec<f64>,
    pub molwt3: Vec<f64>,
    pub molwt4: Vec<f64>,
    pub molwt5: Vec<f64>,
    pub molwt6: Vec<f64>,
    pub molwt7: Vec<f64>,
    pub molwt8: Vec<f64>,
    pub cpmole1: Vec<f64>,
    pub cpmole2: Vec<f64>,
    pub cpmole3: Vec<f64>,
    pub cpmole4: Vec<f64>,
    pub cpmole5: Vec<f64>,
    pub cpmole6: Vec<f64>,
    pub cpmole7: Vec<f64>,
    pub cpmole8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_ALE_03 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_03").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            iadiab: t.take("IADIAB").and_then(|c| c.into_int()).unwrap_or_default(),
            runiv: t.take("RUNIV").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass1: t.take("CVmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass2: t.take("CVmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass3: t.take("CVmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass4: t.take("CVmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass5: t.take("CVmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass6: t.take("CVmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass7: t.take("CVmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass8: t.take("CVmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass1: t.take("CPmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass2: t.take("CPmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass3: t.take("CPmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass4: t.take("CPmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass5: t.take("CPmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass6: t.take("CPmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass7: t.take("CPmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass8: t.take("CPmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt1: t.take("MOLWT1").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt2: t.take("MOLWT2").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt3: t.take("MOLWT3").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt4: t.take("MOLWT4").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt5: t.take("MOLWT5").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt6: t.take("MOLWT6").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt7: t.take("MOLWT7").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt8: t.take("MOLWT8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole1: t.take("CPmole1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole2: t.take("CPmole2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole3: t.take("CPmole3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole4: t.take("CPmole4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole5: t.take("CPmole5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole6: t.take("CPmole6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole7: t.take("CPmole7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole8: t.take("CPmole8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_GAS_MIXTURE {
    pub mid: Vec<i64>,
    pub iadiab: Vec<i64>,
    pub runiv: Vec<f64>,
    pub cvmass1: Vec<f64>,
    pub cvmass2: Vec<f64>,
    pub cvmass3: Vec<f64>,
    pub cvmass4: Vec<f64>,
    pub cvmass5: Vec<f64>,
    pub cvmass6: Vec<f64>,
    pub cvmass7: Vec<f64>,
    pub cvmass8: Vec<f64>,
    pub cpmass1: Vec<f64>,
    pub cpmass2: Vec<f64>,
    pub cpmass3: Vec<f64>,
    pub cpmass4: Vec<f64>,
    pub cpmass5: Vec<f64>,
    pub cpmass6: Vec<f64>,
    pub cpmass7: Vec<f64>,
    pub cpmass8: Vec<f64>,
    pub molwt1: Vec<f64>,
    pub molwt2: Vec<f64>,
    pub molwt3: Vec<f64>,
    pub molwt4: Vec<f64>,
    pub molwt5: Vec<f64>,
    pub molwt6: Vec<f64>,
    pub molwt7: Vec<f64>,
    pub molwt8: Vec<f64>,
    pub cpmole1: Vec<f64>,
    pub cpmole2: Vec<f64>,
    pub cpmole3: Vec<f64>,
    pub cpmole4: Vec<f64>,
    pub cpmole5: Vec<f64>,
    pub cpmole6: Vec<f64>,
    pub cpmole7: Vec<f64>,
    pub cpmole8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_ALE_GAS_MIXTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_GAS_MIXTURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            iadiab: t.take("IADIAB").and_then(|c| c.into_int()).unwrap_or_default(),
            runiv: t.take("RUNIV").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass1: t.take("CVmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass2: t.take("CVmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass3: t.take("CVmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass4: t.take("CVmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass5: t.take("CVmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass6: t.take("CVmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass7: t.take("CVmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass8: t.take("CVmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass1: t.take("CPmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass2: t.take("CPmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass3: t.take("CPmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass4: t.take("CPmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass5: t.take("CPmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass6: t.take("CPmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass7: t.take("CPmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass8: t.take("CPmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt1: t.take("MOLWT1").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt2: t.take("MOLWT2").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt3: t.take("MOLWT3").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt4: t.take("MOLWT4").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt5: t.take("MOLWT5").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt6: t.take("MOLWT6").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt7: t.take("MOLWT7").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt8: t.take("MOLWT8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole1: t.take("CPmole1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole2: t.take("CPmole2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole3: t.take("CPmole3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole4: t.take("CPmole4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole5: t.take("CPmole5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole6: t.take("CPmole6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole7: t.take("CPmole7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole8: t.take("CPmole8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_HERSCHEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub f: Vec<f64>,
    pub rn: Vec<f64>,
    pub gdotc: Vec<f64>,
    pub tao0: Vec<f64>,
}
impl MAT_ALE_HERSCHEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_HERSCHEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            gdotc: t.take("GDOTC").and_then(|c| c.into_float()).unwrap_or_default(),
            tao0: t.take("TAO0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_INCOMPRESSIBLE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mu: Vec<f64>,
    pub tol: Vec<f64>,
    pub dtout: Vec<f64>,
    pub ncg: Vec<i64>,
    pub meth: Vec<i64>,
}
impl MAT_ALE_INCOMPRESSIBLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_INCOMPRESSIBLE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtout: t.take("DTOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            ncg: t.take("NCG").and_then(|c| c.into_int()).unwrap_or_default(),
            meth: t.take("METH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_MIXING_LENGTH {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub f: Vec<f64>,
    pub rn: Vec<f64>,
    pub lci: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub lcx: Vec<f64>,
    pub d0: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub e0: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
}
impl MAT_ALE_MIXING_LENGTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_MIXING_LENGTH").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_VACUUM {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
}
impl MAT_ALE_VACUUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_VACUUM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ALE_VISCOUS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub f60: Vec<f64>,
    pub rn: Vec<f64>,
}
impl MAT_ALE_VISCOUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ALE_VISCOUS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_AMORPHOUS_SOLIDS_FINITE_STRAIN {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub mr: Vec<f64>,
    pub ll: Vec<f64>,
    pub nu0: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha: Vec<f64>,
    pub h0: Vec<f64>,
    pub scv: Vec<f64>,
    pub b: Vec<f64>,
    pub ecv: Vec<f64>,
    pub g0: Vec<f64>,
    pub s0: Vec<f64>,
}
impl MAT_AMORPHOUS_SOLIDS_FINITE_STRAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_AMORPHOUS_SOLIDS_FINITE_STRAIN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            mr: t.take("MR").and_then(|c| c.into_float()).unwrap_or_default(),
            ll: t.take("LL").and_then(|c| c.into_float()).unwrap_or_default(),
            nu0: t.take("NU0").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            scv: t.take("SCV").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ecv: t.take("ECV").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANAND_VISCOPLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub alpha: Vec<f64>,
    pub a1: Vec<f64>,
    pub ratioqr: Vec<f64>,
    pub xi: Vec<f64>,
    pub m: Vec<f64>,
    pub s0: Vec<f64>,
    pub h0: Vec<f64>,
    pub a2: Vec<f64>,
    pub sbar: Vec<f64>,
    pub n: Vec<f64>,
    pub lcym: Vec<i64>,
    pub tref: Vec<f64>,
}
impl MAT_ANAND_VISCOPLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANAND_VISCOPLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            ratioqr: t.take("RATIOQR").and_then(|c| c.into_float()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            h0: t.take("H0").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            sbar: t.take("SBAR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            lcym: t.take("LCYM").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_ANISOTROPIC_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_ELASTIC_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c111: Vec<f64>,
    pub c121: Vec<f64>,
    pub c221: Vec<f64>,
    pub c131: Vec<f64>,
    pub c231: Vec<f64>,
    pub c331: Vec<f64>,
    pub c141: Vec<f64>,
    pub c241: Vec<f64>,
    pub c341: Vec<f64>,
    pub c441: Vec<f64>,
    pub c151: Vec<f64>,
    pub c251: Vec<f64>,
    pub c351: Vec<f64>,
    pub c451: Vec<f64>,
    pub c551: Vec<f64>,
    pub c161: Vec<f64>,
    pub c261: Vec<f64>,
    pub c361: Vec<f64>,
    pub c461: Vec<f64>,
    pub c561: Vec<f64>,
    pub c661: Vec<f64>,
    pub aopt1: Vec<f64>,
    pub xp1: Vec<f64>,
    pub yp1: Vec<f64>,
    pub zp1: Vec<f64>,
    pub a11: Vec<f64>,
    pub a21: Vec<f64>,
    pub a31: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v11: Vec<f64>,
    pub v21: Vec<f64>,
    pub v31: Vec<f64>,
    pub d11: Vec<f64>,
    pub d21: Vec<f64>,
    pub d31: Vec<f64>,
    pub beta1: Vec<f64>,
    pub ref_: Vec<f64>,
    pub f0: Vec<f64>,
    pub f10: Vec<f64>,
    pub c112: Vec<f64>,
    pub c122: Vec<f64>,
    pub c222: Vec<f64>,
    pub c132: Vec<f64>,
    pub c232: Vec<f64>,
    pub c332: Vec<f64>,
    pub c142: Vec<f64>,
    pub c242: Vec<f64>,
    pub c342: Vec<f64>,
    pub c442: Vec<f64>,
    pub c152: Vec<f64>,
    pub c252: Vec<f64>,
    pub c352: Vec<f64>,
    pub c452: Vec<f64>,
    pub c552: Vec<f64>,
    pub c162: Vec<f64>,
    pub c262: Vec<f64>,
    pub c362: Vec<f64>,
    pub c462: Vec<f64>,
    pub c562: Vec<f64>,
    pub c662: Vec<f64>,
    pub xp2: Vec<f64>,
    pub yp2: Vec<f64>,
    pub zp2: Vec<f64>,
    pub a12: Vec<f64>,
    pub a22: Vec<f64>,
    pub a32: Vec<f64>,
    pub xp2_2: Vec<f64>,
    pub v12: Vec<f64>,
    pub v22: Vec<f64>,
    pub v32: Vec<f64>,
    pub d12: Vec<f64>,
    pub d22: Vec<f64>,
    pub d32: Vec<f64>,
    pub beta2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_ANISOTROPIC_ELASTIC_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_ELASTIC_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c111: t.take("C111").and_then(|c| c.into_float()).unwrap_or_default(),
            c121: t.take("C121").and_then(|c| c.into_float()).unwrap_or_default(),
            c221: t.take("C221").and_then(|c| c.into_float()).unwrap_or_default(),
            c131: t.take("C131").and_then(|c| c.into_float()).unwrap_or_default(),
            c231: t.take("C231").and_then(|c| c.into_float()).unwrap_or_default(),
            c331: t.take("C331").and_then(|c| c.into_float()).unwrap_or_default(),
            c141: t.take("C141").and_then(|c| c.into_float()).unwrap_or_default(),
            c241: t.take("C241").and_then(|c| c.into_float()).unwrap_or_default(),
            c341: t.take("C341").and_then(|c| c.into_float()).unwrap_or_default(),
            c441: t.take("C441").and_then(|c| c.into_float()).unwrap_or_default(),
            c151: t.take("C151").and_then(|c| c.into_float()).unwrap_or_default(),
            c251: t.take("C251").and_then(|c| c.into_float()).unwrap_or_default(),
            c351: t.take("C351").and_then(|c| c.into_float()).unwrap_or_default(),
            c451: t.take("C451").and_then(|c| c.into_float()).unwrap_or_default(),
            c551: t.take("C551").and_then(|c| c.into_float()).unwrap_or_default(),
            c161: t.take("C161").and_then(|c| c.into_float()).unwrap_or_default(),
            c261: t.take("C261").and_then(|c| c.into_float()).unwrap_or_default(),
            c361: t.take("C361").and_then(|c| c.into_float()).unwrap_or_default(),
            c461: t.take("C461").and_then(|c| c.into_float()).unwrap_or_default(),
            c561: t.take("C561").and_then(|c| c.into_float()).unwrap_or_default(),
            c661: t.take("C661").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt1: t.take("AOPT1").and_then(|c| c.into_float()).unwrap_or_default(),
            xp1: t.take("XP1").and_then(|c| c.into_float()).unwrap_or_default(),
            yp1: t.take("YP1").and_then(|c| c.into_float()).unwrap_or_default(),
            zp1: t.take("ZP1").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v11: t.take("V11").and_then(|c| c.into_float()).unwrap_or_default(),
            v21: t.take("V21").and_then(|c| c.into_float()).unwrap_or_default(),
            v31: t.take("V31").and_then(|c| c.into_float()).unwrap_or_default(),
            d11: t.take("D11").and_then(|c| c.into_float()).unwrap_or_default(),
            d21: t.take("D21").and_then(|c| c.into_float()).unwrap_or_default(),
            d31: t.take("D31").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            c112: t.take("C112").and_then(|c| c.into_float()).unwrap_or_default(),
            c122: t.take("C122").and_then(|c| c.into_float()).unwrap_or_default(),
            c222: t.take("C222").and_then(|c| c.into_float()).unwrap_or_default(),
            c132: t.take("C132").and_then(|c| c.into_float()).unwrap_or_default(),
            c232: t.take("C232").and_then(|c| c.into_float()).unwrap_or_default(),
            c332: t.take("C332").and_then(|c| c.into_float()).unwrap_or_default(),
            c142: t.take("C142").and_then(|c| c.into_float()).unwrap_or_default(),
            c242: t.take("C242").and_then(|c| c.into_float()).unwrap_or_default(),
            c342: t.take("C342").and_then(|c| c.into_float()).unwrap_or_default(),
            c442: t.take("C442").and_then(|c| c.into_float()).unwrap_or_default(),
            c152: t.take("C152").and_then(|c| c.into_float()).unwrap_or_default(),
            c252: t.take("C252").and_then(|c| c.into_float()).unwrap_or_default(),
            c352: t.take("C352").and_then(|c| c.into_float()).unwrap_or_default(),
            c452: t.take("C452").and_then(|c| c.into_float()).unwrap_or_default(),
            c552: t.take("C552").and_then(|c| c.into_float()).unwrap_or_default(),
            c162: t.take("C162").and_then(|c| c.into_float()).unwrap_or_default(),
            c262: t.take("C262").and_then(|c| c.into_float()).unwrap_or_default(),
            c362: t.take("C362").and_then(|c| c.into_float()).unwrap_or_default(),
            c462: t.take("C462").and_then(|c| c.into_float()).unwrap_or_default(),
            c562: t.take("C562").and_then(|c| c.into_float()).unwrap_or_default(),
            c662: t.take("C662").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2: t.take("XP2").and_then(|c| c.into_float()).unwrap_or_default(),
            yp2: t.take("YP2").and_then(|c| c.into_float()).unwrap_or_default(),
            zp2: t.take("ZP2").and_then(|c| c.into_float()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            xp2_2: t.take("XP2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v22: t.take("V22").and_then(|c| c.into_float()).unwrap_or_default(),
            v32: t.take("V32").and_then(|c| c.into_float()).unwrap_or_default(),
            d12: t.take("D12").and_then(|c| c.into_float()).unwrap_or_default(),
            d22: t.take("D22").and_then(|c| c.into_float()).unwrap_or_default(),
            d32: t.take("D32").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_ELASTIC_PLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c22: Vec<f64>,
    pub c23: Vec<f64>,
    pub c24: Vec<f64>,
    pub c25: Vec<f64>,
    pub c26: Vec<f64>,
    pub c33: Vec<f64>,
    pub c34: Vec<f64>,
    pub c35: Vec<f64>,
    pub c36: Vec<f64>,
    pub c44: Vec<f64>,
    pub c45: Vec<f64>,
    pub c46: Vec<f64>,
    pub c55: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub s11_l: Vec<f64>,
    pub s22_m: Vec<f64>,
    pub s33_n: Vec<f64>,
    pub s12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub vp: Vec<f64>,
    pub unused: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub id3upd: Vec<f64>,
    pub extra: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ihis: Vec<f64>,
    pub xt: Vec<f64>,
    pub xc: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub sxy: Vec<f64>,
    pub ff12: Vec<f64>,
    pub f60: Vec<f64>,
    pub ncfail: Vec<f64>,
    pub zt: Vec<f64>,
    pub zc: Vec<f64>,
    pub syz: Vec<f64>,
    pub szx: Vec<f64>,
    pub ff23: Vec<f64>,
    pub ff31: Vec<f64>,
}
impl MAT_ANISOTROPIC_ELASTIC_PLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_ELASTIC_PLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            s11_l: t.take("S11/L").and_then(|c| c.into_float()).unwrap_or_default(),
            s22_m: t.take("S22/M").and_then(|c| c.into_float()).unwrap_or_default(),
            s33_n: t.take("S33/N").and_then(|c| c.into_float()).unwrap_or_default(),
            s12: t.take("S12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            id3upd: t.take("ID3UPD").and_then(|c| c.into_float()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ff12: t.take("FF12").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_float()).unwrap_or_default(),
            ncfail: t.take("NCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            szx: t.take("SZX").and_then(|c| c.into_float()).unwrap_or_default(),
            ff23: t.take("FF23").and_then(|c| c.into_float()).unwrap_or_default(),
            ff31: t.take("FF31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_HYPERELASTIC {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub aopt: Vec<f64>,
    pub title: Vec<String>,
    pub itype: Vec<i64>,
    pub beta: Vec<f64>,
    pub nu: Vec<f64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub mu3: Vec<f64>,
    pub mu4: Vec<f64>,
    pub mu5: Vec<f64>,
    pub mu6: Vec<f64>,
    pub mu7: Vec<f64>,
    pub mu8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub title_2: Vec<String>,
    pub atype: Vec<i64>,
    pub intype: Vec<i64>,
    pub nf: Vec<i64>,
    pub theta: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub ftype: Vec<i64>,
    pub fcid: Vec<i64>,
    pub k1_2: Vec<f64>,
    pub k2_2: Vec<f64>,
    pub ftype_2: Vec<i64>,
    pub flcid: Vec<i64>,
    pub e: Vec<f64>,
    pub r0norm: Vec<f64>,
    pub h0norm: Vec<f64>,
    pub k1_3: Vec<f64>,
    pub k2_3: Vec<f64>,
    pub title_3: Vec<String>,
    pub actype: Vec<i64>,
    pub acdir: Vec<i64>,
    pub acid: Vec<i64>,
    pub acthr: Vec<f64>,
    pub sf: Vec<f64>,
    pub ss: Vec<f64>,
    pub sn: Vec<f64>,
    pub t0: Vec<f64>,
    pub ca2ion: Vec<f64>,
    pub ca2ionm: Vec<f64>,
    pub n: Vec<f64>,
    pub taumax: Vec<f64>,
    pub stf: Vec<f64>,
    pub b_2: Vec<f64>,
    pub l0: Vec<f64>,
    pub l: Vec<f64>,
    pub dtmax: Vec<f64>,
    pub mr: Vec<f64>,
    pub tr: Vec<f64>,
    pub l_2: Vec<f64>,
    pub eta: Vec<f64>,
    pub t0_2: Vec<f64>,
    pub ca2ion_2: Vec<f64>,
    pub ca2ion50: Vec<f64>,
    pub n_2: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub f: Vec<f64>,
    pub l_3: Vec<f64>,
    pub eta_2: Vec<f64>,
    pub t0_3: Vec<f64>,
    pub ca2ion50_2: Vec<f64>,
    pub ca2ionmax: Vec<f64>,
    pub n_3: Vec<f64>,
    pub sigmax_2: Vec<f64>,
    pub f_2: Vec<f64>,
    pub ca2ion0: Vec<f64>,
    pub tca: Vec<f64>,
    pub l_4: Vec<f64>,
    pub eta_3: Vec<f64>,
    pub fseid: Vec<i64>,
    pub flid: Vec<i64>,
    pub fvid: Vec<i64>,
    pub alphaid: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub f_3: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta_2: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_ANISOTROPIC_HYPERELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_HYPERELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            mu3: t.take("MU3").and_then(|c| c.into_float()).unwrap_or_default(),
            mu4: t.take("MU4").and_then(|c| c.into_float()).unwrap_or_default(),
            mu5: t.take("MU5").and_then(|c| c.into_float()).unwrap_or_default(),
            mu6: t.take("MU6").and_then(|c| c.into_float()).unwrap_or_default(),
            mu7: t.take("MU7").and_then(|c| c.into_float()).unwrap_or_default(),
            mu8: t.take("MU8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            title_2: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            atype: t.take("ATYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            intype: t.take("INTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_int()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            fcid: t.take("FCID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1_2: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype_2: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            r0norm: t.take("R0NORM").and_then(|c| c.into_float()).unwrap_or_default(),
            h0norm: t.take("H0NORM").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_3: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_3: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            title_3: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            actype: t.take("ACTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            acdir: t.take("ACDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            acid: t.take("ACID").and_then(|c| c.into_int()).unwrap_or_default(),
            acthr: t.take("ACTHR").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            ss: t.take("SS").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion: t.take("CA2ION").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ionm: t.take("CA2IONM").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            taumax: t.take("TAUMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            stf: t.take("STF").and_then(|c| c.into_float()).unwrap_or_default(),
            b_2: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            mr: t.take("MR").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            l_2: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            t0_2: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion_2: t.take("CA2ION").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion50: t.take("CA2ION50").and_then(|c| c.into_float()).unwrap_or_default(),
            n_2: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            l_3: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta_2: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            t0_3: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion50_2: t.take("CA2ION50").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ionmax: t.take("CA2IONMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            n_3: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax_2: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            ca2ion0: t.take("CA2ION0").and_then(|c| c.into_float()).unwrap_or_default(),
            tca: t.take("TCA").and_then(|c| c.into_float()).unwrap_or_default(),
            l_4: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            eta_3: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fseid: t.take("FSEID").and_then(|c| c.into_int()).unwrap_or_default(),
            flid: t.take("FLID").and_then(|c| c.into_int()).unwrap_or_default(),
            fvid: t.take("FVID").and_then(|c| c.into_int()).unwrap_or_default(),
            alphaid: t.take("ALPHAID").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_2: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_PLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub s11: Vec<f64>,
    pub s22: Vec<f64>,
    pub s33: Vec<f64>,
    pub s12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_ANISOTROPIC_PLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_PLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            s11: t.take("S11").and_then(|c| c.into_float()).unwrap_or_default(),
            s22: t.take("S22").and_then(|c| c.into_float()).unwrap_or_default(),
            s33: t.take("S33").and_then(|c| c.into_float()).unwrap_or_default(),
            s12: t.take("S12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_THERMOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ta1: Vec<i64>,
    pub ta2: Vec<i64>,
    pub ta3: Vec<i64>,
    pub ta4: Vec<i64>,
    pub ta5: Vec<i64>,
    pub ta6: Vec<i64>,
    pub c11: Vec<i64>,
    pub c12: Vec<i64>,
    pub c13: Vec<i64>,
    pub c14: Vec<i64>,
    pub c15: Vec<i64>,
    pub c16: Vec<i64>,
    pub c22: Vec<i64>,
    pub c23: Vec<i64>,
    pub c24: Vec<i64>,
    pub c25: Vec<i64>,
    pub c26: Vec<i64>,
    pub c33: Vec<i64>,
    pub c34: Vec<i64>,
    pub c35: Vec<i64>,
    pub c36: Vec<i64>,
    pub c44: Vec<i64>,
    pub c45: Vec<i64>,
    pub c46: Vec<i64>,
    pub c55: Vec<i64>,
    pub c56: Vec<i64>,
    pub c66: Vec<i64>,
    pub tge: Vec<i64>,
    pub tref: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_ANISOTROPIC_THERMOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_THERMOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ta1: t.take("TA1").and_then(|c| c.into_int()).unwrap_or_default(),
            ta2: t.take("TA2").and_then(|c| c.into_int()).unwrap_or_default(),
            ta3: t.take("TA3").and_then(|c| c.into_int()).unwrap_or_default(),
            ta4: t.take("TA4").and_then(|c| c.into_int()).unwrap_or_default(),
            ta5: t.take("TA5").and_then(|c| c.into_int()).unwrap_or_default(),
            ta6: t.take("TA6").and_then(|c| c.into_int()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_int()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_int()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_int()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_int()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_int()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_int()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_int()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_int()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_int()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_int()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_int()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_int()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_int()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_int()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_int()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_int()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_int()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_int()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_int()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_int()).unwrap_or_default(),
            tge: t.take("TGE").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ANISOTROPIC_VISCOPLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub flag: Vec<i64>,
    pub lcss: Vec<i64>,
    pub alpha: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub vk: Vec<f64>,
    pub vm: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub aopt: Vec<f64>,
    pub fail: Vec<f64>,
    pub numint: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_ANISOTROPIC_VISCOPLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ANISOTROPIC_VISCOPLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            vk: t.take("VK").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ARRUDA_BOYCE_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub n: Vec<i64>,
    pub lcid: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nt: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_ARRUDA_BOYCE_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ARRUDA_BOYCE_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ARUP_ADHESIVE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tenmax: Vec<f64>,
    pub gcten: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub gcshr: Vec<f64>,
    pub pwrt: Vec<f64>,
    pub pwrs: Vec<f64>,
    pub shrp: Vec<f64>,
    pub sht_sl: Vec<f64>,
    pub edot0: Vec<f64>,
    pub edot2: Vec<f64>,
    pub thkdir: Vec<f64>,
    pub extra: Vec<f64>,
    pub tmaxe: Vec<f64>,
    pub gcte: Vec<f64>,
    pub smaxe: Vec<f64>,
    pub gcse: Vec<f64>,
    pub pwrte: Vec<f64>,
    pub pwrse: Vec<f64>,
    pub facet: Vec<f64>,
    pub facct: Vec<f64>,
    pub faces: Vec<f64>,
    pub faccs: Vec<f64>,
    pub softt: Vec<f64>,
    pub softs: Vec<f64>,
    pub sdfac: Vec<f64>,
    pub sgfac: Vec<f64>,
    pub sdefac: Vec<f64>,
    pub sgefac: Vec<f64>,
    pub bthk: Vec<f64>,
    pub outfail: Vec<f64>,
    pub fsip: Vec<f64>,
    pub fbr713: Vec<f64>,
    pub ele2ns: Vec<f64>,
}
impl MAT_ARUP_ADHESIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ARUP_ADHESIVE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tenmax: t.take("TENMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gcten: t.take("GCTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gcshr: t.take("GCSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrt: t.take("PWRT").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrs: t.take("PWRS").and_then(|c| c.into_float()).unwrap_or_default(),
            shrp: t.take("SHRP").and_then(|c| c.into_float()).unwrap_or_default(),
            sht_sl: t.take("SHT_SL").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            edot2: t.take("EDOT2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkdir: t.take("THKDIR").and_then(|c| c.into_float()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_float()).unwrap_or_default(),
            tmaxe: t.take("TMAXE").and_then(|c| c.into_float()).unwrap_or_default(),
            gcte: t.take("GCTE").and_then(|c| c.into_float()).unwrap_or_default(),
            smaxe: t.take("SMAXE").and_then(|c| c.into_float()).unwrap_or_default(),
            gcse: t.take("GCSE").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrte: t.take("PWRTE").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrse: t.take("PWRSE").and_then(|c| c.into_float()).unwrap_or_default(),
            facet: t.take("FACET").and_then(|c| c.into_float()).unwrap_or_default(),
            facct: t.take("FACCT").and_then(|c| c.into_float()).unwrap_or_default(),
            faces: t.take("FACES").and_then(|c| c.into_float()).unwrap_or_default(),
            faccs: t.take("FACCS").and_then(|c| c.into_float()).unwrap_or_default(),
            softt: t.take("SOFTT").and_then(|c| c.into_float()).unwrap_or_default(),
            softs: t.take("SOFTS").and_then(|c| c.into_float()).unwrap_or_default(),
            sdfac: t.take("SDFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sgfac: t.take("SGFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdefac: t.take("SDEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sgefac: t.take("SGEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            bthk: t.take("BTHK").and_then(|c| c.into_float()).unwrap_or_default(),
            outfail: t.take("OUTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            fsip: t.take("FSIP").and_then(|c| c.into_float()).unwrap_or_default(),
            fbr713: t.take("FBR713").and_then(|c| c.into_float()).unwrap_or_default(),
            ele2ns: t.take("ELE2NS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_B01 {
    pub mid: Vec<i64>,
    pub mpul: Vec<f64>,
    pub llcid: Vec<i64>,
    pub ulcid: Vec<i64>,
    pub lmin: Vec<f64>,
    pub cse: Vec<f64>,
    pub damp: Vec<f64>,
    pub e: Vec<f64>,
    pub a: Vec<f64>,
    pub i: Vec<f64>,
    pub j: Vec<f64>,
    pub as_: Vec<f64>,
    pub f: Vec<f64>,
    pub m: Vec<f64>,
    pub r: Vec<f64>,
    pub p1doff: Vec<i64>,
    pub form: Vec<i64>,
    pub ecoat: Vec<f64>,
    pub tcoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub eb: Vec<f64>,
    pub prba: Vec<f64>,
    pub prba_2: Vec<f64>,
    pub gab: Vec<f64>,
}
impl MAT_B01 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_B01").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mpul: t.take("MPUL").and_then(|c| c.into_float()).unwrap_or_default(),
            llcid: t.take("LLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ulcid: t.take("ULCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            i: t.take("I").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            p1doff: t.take("P1DOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prba_2: t.take("PRBA_2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BAMMAN {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c17: Vec<f64>,
    pub c18: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
}
impl MAT_BAMMAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BAMMAN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c17: t.take("C17").and_then(|c| c.into_float()).unwrap_or_default(),
            c18: t.take("C18").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BAMMAN_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub c9: Vec<f64>,
    pub c10: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c14: Vec<f64>,
    pub c15: Vec<f64>,
    pub c16: Vec<f64>,
    pub c17: Vec<f64>,
    pub c18: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub n: Vec<f64>,
    pub d0: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_BAMMAN_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BAMMAN_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            c9: t.take("C9").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c17: t.take("C17").and_then(|c| c.into_float()).unwrap_or_default(),
            c18: t.take("C18").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BARLAT_ANISOTROPIC_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub f: Vec<f64>,
    pub g: Vec<f64>,
    pub h: Vec<f64>,
    pub lcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_BARLAT_ANISOTROPIC_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BARLAT_ANISOTROPIC_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BARLAT_YLD2000 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub fit: Vec<f64>,
    pub beta: Vec<f64>,
    pub iter: Vec<f64>,
    pub iscale: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub hard: Vec<f64>,
    pub a: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub sig00: Vec<f64>,
    pub sig45: Vec<f64>,
    pub sig90: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub sigxx: Vec<f64>,
    pub sigyy: Vec<f64>,
    pub sigxy: Vec<f64>,
    pub dxx: Vec<f64>,
    pub dyy: Vec<f64>,
    pub dxy: Vec<f64>,
    pub aopt: Vec<f64>,
    pub offang: Vec<f64>,
    pub p4: Vec<f64>,
    pub htflag: Vec<i64>,
    pub hta: Vec<i64>,
    pub htb: Vec<i64>,
    pub htc: Vec<i64>,
    pub htd: Vec<i64>,
    pub null: Vec<f64>,
    pub null_2: Vec<f64>,
    pub null_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub usrfail: Vec<i64>,
    pub cp: Vec<f64>,
    pub t0: Vec<f64>,
    pub tref: Vec<f64>,
    pub ta0: Vec<f64>,
    pub a_2: Vec<f64>,
    pub b: Vec<f64>,
    pub c_2: Vec<f64>,
    pub d: Vec<f64>,
    pub p_2: Vec<f64>,
    pub q: Vec<f64>,
    pub e0mart: Vec<f64>,
    pub vm0: Vec<f64>,
    pub ahs: Vec<f64>,
    pub bhs: Vec<f64>,
    pub m: Vec<f64>,
    pub n_2: Vec<f64>,
    pub eps0: Vec<f64>,
    pub hmart: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
}
impl MAT_BARLAT_YLD2000 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BARLAT_YLD2000").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            fit: t.take("FIT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            iscale: t.take("ISCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            hard: t.take("HARD").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            sig00: t.take("SIG00").and_then(|c| c.into_float()).unwrap_or_default(),
            sig45: t.take("SIG45").and_then(|c| c.into_float()).unwrap_or_default(),
            sig90: t.take("SIG90").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxx: t.take("SIGXX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigyy: t.take("SIGYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sigxy: t.take("SIGXY").and_then(|c| c.into_float()).unwrap_or_default(),
            dxx: t.take("DXX").and_then(|c| c.into_float()).unwrap_or_default(),
            dyy: t.take("DYY").and_then(|c| c.into_float()).unwrap_or_default(),
            dxy: t.take("DXY").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            offang: t.take("OFFANG").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            htflag: t.take("HTFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            hta: t.take("HTA").and_then(|c| c.into_int()).unwrap_or_default(),
            htb: t.take("HTB").and_then(|c| c.into_int()).unwrap_or_default(),
            htc: t.take("HTC").and_then(|c| c.into_int()).unwrap_or_default(),
            htd: t.take("HTD").and_then(|c| c.into_int()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_float()).unwrap_or_default(),
            null_2: t.take("NULL_2").and_then(|c| c.into_float()).unwrap_or_default(),
            null_3: t.take("NULL_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            ta0: t.take("TA0").and_then(|c| c.into_float()).unwrap_or_default(),
            a_2: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c_2: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            p_2: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            e0mart: t.take("E0MART").and_then(|c| c.into_float()).unwrap_or_default(),
            vm0: t.take("VM0").and_then(|c| c.into_float()).unwrap_or_default(),
            ahs: t.take("AHS").and_then(|c| c.into_float()).unwrap_or_default(),
            bhs: t.take("BHS").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n_2: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            hmart: t.take("HMART").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BARLAT_YLD2004 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp12: Vec<f64>,
    pub cp13: Vec<f64>,
    pub cp21: Vec<f64>,
    pub cp23: Vec<f64>,
    pub cp31: Vec<f64>,
    pub cp32: Vec<f64>,
    pub cpp12: Vec<f64>,
    pub cpp13: Vec<f64>,
    pub cpp21: Vec<f64>,
    pub cpp23: Vec<f64>,
    pub cpp31: Vec<f64>,
    pub cpp32: Vec<f64>,
    pub cp44: Vec<f64>,
    pub cp55: Vec<f64>,
    pub cp66: Vec<f64>,
    pub cpp44: Vec<f64>,
    pub cpp55: Vec<f64>,
    pub cpp66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub a: Vec<f64>,
    pub lcss: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_BARLAT_YLD2004 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BARLAT_YLD2004").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp12: t.take("CP12").and_then(|c| c.into_float()).unwrap_or_default(),
            cp13: t.take("CP13").and_then(|c| c.into_float()).unwrap_or_default(),
            cp21: t.take("CP21").and_then(|c| c.into_float()).unwrap_or_default(),
            cp23: t.take("CP23").and_then(|c| c.into_float()).unwrap_or_default(),
            cp31: t.take("CP31").and_then(|c| c.into_float()).unwrap_or_default(),
            cp32: t.take("CP32").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp12: t.take("CPP12").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp13: t.take("CPP13").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp21: t.take("CPP21").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp23: t.take("CPP23").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp31: t.take("CPP31").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp32: t.take("CPP32").and_then(|c| c.into_float()).unwrap_or_default(),
            cp44: t.take("CP44").and_then(|c| c.into_float()).unwrap_or_default(),
            cp55: t.take("CP55").and_then(|c| c.into_float()).unwrap_or_default(),
            cp66: t.take("CP66").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp44: t.take("CPP44").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp55: t.take("CPP55").and_then(|c| c.into_float()).unwrap_or_default(),
            cpp66: t.take("CPP66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BARLAT_YLD96 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub esr0: Vec<f64>,
    pub m: Vec<f64>,
    pub hard: Vec<i64>,
    pub a: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az0: Vec<f64>,
    pub az1: Vec<f64>,
    pub aopt: Vec<f64>,
    pub offang: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_BARLAT_YLD96 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BARLAT_YLD96").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            esr0: t.take("ESR0").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            hard: t.take("HARD").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az0: t.take("AZ0").and_then(|c| c.into_float()).unwrap_or_default(),
            az1: t.take("AZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            offang: t.take("OFFANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BERGSTROM_BOYCE_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub gv: Vec<f64>,
    pub n: Vec<f64>,
    pub nv: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub gam0: Vec<f64>,
    pub tauh: Vec<f64>,
}
impl MAT_BERGSTROM_BOYCE_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BERGSTROM_BOYCE_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            gv: t.take("GV").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            gam0: t.take("GAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            tauh: t.take("TAUH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BILKHU_DUBOIS_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub lcpy: Vec<i64>,
    pub lcuys: Vec<i64>,
    pub vc: Vec<f64>,
    pub pc: Vec<f64>,
    pub vpc: Vec<f64>,
    pub tsc: Vec<f64>,
    pub vtsc: Vec<f64>,
    pub lcrate: Vec<i64>,
    pub pr: Vec<f64>,
    pub kcon: Vec<f64>,
    pub isflg: Vec<i64>,
    pub ncycle: Vec<f64>,
}
impl MAT_BILKHU_DUBOIS_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BILKHU/DUBOIS_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpy: t.take("LCPY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuys: t.take("LCUYS").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vpc: t.take("VPC").and_then(|c| c.into_float()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            vtsc: t.take("VTSC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrate: t.take("LCRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            isflg: t.take("ISFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BIOT_HYSTERETIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub zt: Vec<f64>,
    pub fd: Vec<f64>,
}
impl MAT_BIOT_HYSTERETIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BIOT_HYSTERETIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BLATZ_KO_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_BLATZ_KO_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BLATZ-KO_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BLATZ_KO_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_BLATZ_KO_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BLATZ-KO_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BOLT_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub kax: Vec<f64>,
    pub kshr: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub fpre: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcax: Vec<i64>,
    pub lcshr: Vec<i64>,
    pub fric: Vec<f64>,
    pub clear: Vec<f64>,
    pub dafail: Vec<f64>,
    pub drfail: Vec<f64>,
    pub damag: Vec<f64>,
    pub t0pre: Vec<f64>,
    pub dacfail: Vec<f64>,
    pub axshel: Vec<i64>,
    pub holshr: Vec<i64>,
}
impl MAT_BOLT_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BOLT_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            kax: t.take("KAX").and_then(|c| c.into_float()).unwrap_or_default(),
            kshr: t.take("KSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            fpre: t.take("FPRE").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcax: t.take("LCAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcshr: t.take("LCSHR").and_then(|c| c.into_int()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            clear: t.take("CLEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            dafail: t.take("DAFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            drfail: t.take("DRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damag: t.take("DAMAG").and_then(|c| c.into_float()).unwrap_or_default(),
            t0pre: t.take("T0PRE").and_then(|c| c.into_float()).unwrap_or_default(),
            dacfail: t.take("DACFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            axshel: t.take("AXSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            holshr: t.take("HOLSHR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BRAIN_LINEAR_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub dc: Vec<f64>,
    pub fo: Vec<f64>,
    pub so: Vec<f64>,
}
impl MAT_BRAIN_LINEAR_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BRAIN_LINEAR_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            fo: t.take("FO").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_BRITTLE_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tlimit: Vec<f64>,
    pub slimit: Vec<f64>,
    pub ftough: Vec<f64>,
    pub sreten: Vec<f64>,
    pub visc: Vec<f64>,
    pub fra_rf: Vec<f64>,
    pub e_rf: Vec<f64>,
    pub ys_rf: Vec<f64>,
    pub eh_rf: Vec<f64>,
    pub fs_rf: Vec<f64>,
    pub sigy: Vec<f64>,
}
impl MAT_BRITTLE_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_BRITTLE_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tlimit: t.take("TLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            slimit: t.take("SLIMIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ftough: t.take("FTOUGH").and_then(|c| c.into_float()).unwrap_or_default(),
            sreten: t.take("SRETEN").and_then(|c| c.into_float()).unwrap_or_default(),
            visc: t.take("VISC").and_then(|c| c.into_float()).unwrap_or_default(),
            fra_rf: t.take("FRA_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            e_rf: t.take("E_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            ys_rf: t.take("YS_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            eh_rf: t.take("EH_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            fs_rf: t.take("FS_RF").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CABLE_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub f0: Vec<f64>,
    pub tmaxf0: Vec<f64>,
    pub tramp: Vec<f64>,
    pub iread: Vec<i64>,
    pub output: Vec<i64>,
    pub tstart: Vec<f64>,
    pub fracl0: Vec<f64>,
    pub mxeps: Vec<f64>,
    pub mxfrc: Vec<f64>,
}
impl MAT_CABLE_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CABLE_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            tmaxf0: t.take("TMAXF0").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            iread: t.take("IREAD").and_then(|c| c.into_int()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_int()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            fracl0: t.take("FRACL0").and_then(|c| c.into_float()).unwrap_or_default(),
            mxeps: t.take("MXEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            mxfrc: t.take("MXFRC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CAZACU_BARLAT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub a: Vec<f64>,
    pub c11: Vec<f64>,
    pub c22: Vec<f64>,
    pub c33: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub k: Vec<f64>,
    pub p3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<f64>,
    pub f20: Vec<f64>,
    pub f30: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c44: Vec<f64>,
    pub xp: Vec<i64>,
    pub yp: Vec<i64>,
    pub zp: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fit: Vec<i64>,
}
impl MAT_CAZACU_BARLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CAZACU_BARLAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_int()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_int()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fit: t.take("FIT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CAZACU_BARLAT_MAGNESIUM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub a: Vec<f64>,
    pub c11: Vec<f64>,
    pub c22: Vec<f64>,
    pub c33: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub k: Vec<f64>,
    pub p3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<f64>,
    pub f20: Vec<f64>,
    pub f30: Vec<f64>,
    pub c12: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c44: Vec<f64>,
    pub xp: Vec<i64>,
    pub yp: Vec<i64>,
    pub zp: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fit: Vec<i64>,
    pub lc1id: Vec<i64>,
    pub lc2id: Vec<i64>,
    pub numint: Vec<i64>,
    pub lccid: Vec<i64>,
    pub icflag: Vec<i64>,
    pub idflag: Vec<i64>,
    pub lc3id: Vec<i64>,
    pub epsfg: Vec<f64>,
}
impl MAT_CAZACU_BARLAT_MAGNESIUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CAZACU_BARLAT_MAGNESIUM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_int()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_int()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fit: t.take("FIT").and_then(|c| c.into_int()).unwrap_or_default(),
            lc1id: t.take("LC1ID").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2id: t.take("LC2ID").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccid: t.take("LCCID").and_then(|c| c.into_int()).unwrap_or_default(),
            icflag: t.take("ICFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            idflag: t.take("IDFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3id: t.take("LC3ID").and_then(|c| c.into_int()).unwrap_or_default(),
            epsfg: t.take("EPSFG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CDPM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ecc: Vec<f64>,
    pub qh0: Vec<f64>,
    pub ft: Vec<f64>,
    pub fc: Vec<f64>,
    pub hp: Vec<f64>,
    pub ah: Vec<f64>,
    pub bh: Vec<f64>,
    pub ch: Vec<f64>,
    pub dh: Vec<f64>,
    pub as_: Vec<f64>,
    pub df: Vec<f64>,
    pub fc0: Vec<f64>,
    pub type_: Vec<f64>,
    pub bs: Vec<f64>,
    pub wf: Vec<f64>,
    pub wf1: Vec<f64>,
    pub ft1: Vec<f64>,
    pub strflg: Vec<f64>,
    pub failflg: Vec<f64>,
    pub efc: Vec<f64>,
}
impl MAT_CDPM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CDPM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ecc: t.take("ECC").and_then(|c| c.into_float()).unwrap_or_default(),
            qh0: t.take("QH0").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            hp: t.take("HP").and_then(|c| c.into_float()).unwrap_or_default(),
            ah: t.take("AH").and_then(|c| c.into_float()).unwrap_or_default(),
            bh: t.take("BH").and_then(|c| c.into_float()).unwrap_or_default(),
            ch: t.take("CH").and_then(|c| c.into_float()).unwrap_or_default(),
            dh: t.take("DH").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            fc0: t.take("FC0").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            wf: t.take("WF").and_then(|c| c.into_float()).unwrap_or_default(),
            wf1: t.take("WF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ft1: t.take("FT1").and_then(|c| c.into_float()).unwrap_or_default(),
            strflg: t.take("STRFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            failflg: t.take("FAILFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            efc: t.take("EFC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CELLULAR_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
    pub c10: Vec<f64>,
    pub c01: Vec<f64>,
    pub c11: Vec<f64>,
    pub c20: Vec<f64>,
    pub c02: Vec<f64>,
    pub p0: Vec<f64>,
    pub phi: Vec<f64>,
    pub ivs: Vec<f64>,
    pub g: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_CELLULAR_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CELLULAR_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c20: t.take("C20").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            ivs: t.take("IVS").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CF_MICROMECHANICS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub eu: Vec<f64>,
    pub c: Vec<f64>,
    pub eka: Vec<f64>,
    pub eua: Vec<f64>,
    pub vmb: Vec<f64>,
    pub ekb: Vec<f64>,
    pub thl: Vec<f64>,
    pub ta: Vec<f64>,
    pub thi1: Vec<f64>,
    pub thi2: Vec<f64>,
    pub w: Vec<f64>,
    pub span: Vec<f64>,
    pub thick: Vec<f64>,
    pub h: Vec<f64>,
    pub area: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr13: Vec<f64>,
    pub pr23: Vec<f64>,
    pub aopt: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub vyarn: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub chexp1: Vec<f64>,
    pub chexp2: Vec<f64>,
    pub chexp3: Vec<f64>,
    pub lcchexp: Vec<i64>,
    pub lcthexp: Vec<i64>,
    pub r: Vec<f64>,
    pub tref: Vec<f64>,
    pub docref: Vec<f64>,
    pub wlftref: Vec<f64>,
    pub wlfa: Vec<f64>,
    pub wlfb: Vec<f64>,
    pub lcg0: Vec<i64>,
    pub lck0: Vec<i64>,
    pub idoc: Vec<f64>,
    pub incr: Vec<i64>,
    pub qcure: Vec<f64>,
    pub gi: Vec<f64>,
    pub betagi: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_CF_MICROMECHANICS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CF_MICROMECHANICS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            eu: t.take("EU").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            eka: t.take("EKA").and_then(|c| c.into_float()).unwrap_or_default(),
            eua: t.take("EUA").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            ekb: t.take("EKB").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            ta: t.take("TA").and_then(|c| c.into_float()).unwrap_or_default(),
            thi1: t.take("THI1").and_then(|c| c.into_float()).unwrap_or_default(),
            thi2: t.take("THI2").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            span: t.take("SPAN").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr13: t.take("PR13").and_then(|c| c.into_float()).unwrap_or_default(),
            pr23: t.take("PR23").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            vyarn: t.take("VYARN").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp1: t.take("CHEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp2: t.take("CHEXP2").and_then(|c| c.into_float()).unwrap_or_default(),
            chexp3: t.take("CHEXP3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcchexp: t.take("LCCHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcthexp: t.take("LCTHEXP").and_then(|c| c.into_int()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            docref: t.take("DOCREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlftref: t.take("WLFTREF").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfa: t.take("WLFA").and_then(|c| c.into_float()).unwrap_or_default(),
            wlfb: t.take("WLFB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg0: t.take("LCG0").and_then(|c| c.into_int()).unwrap_or_default(),
            lck0: t.take("LCK0").and_then(|c| c.into_int()).unwrap_or_default(),
            idoc: t.take("IDOC").and_then(|c| c.into_float()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
            qcure: t.take("QCURE").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betagi: t.take("BETAGi").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("Ki").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKi").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CHRONOLOGICAL_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_CHRONOLOGICAL_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CHRONOLOGICAL_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CLOSED_CELL_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub p0: Vec<f64>,
    pub phi: Vec<f64>,
    pub gama0: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_CLOSED_CELL_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CLOSED_CELL_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            gama0: t.take("GAMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CODAM2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub f: Vec<f64>,
    pub prba: Vec<f64>,
    pub f_2: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub nlayer: Vec<i64>,
    pub r1: Vec<f64>,
    pub r2: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub macf: Vec<f64>,
    pub angle1: Vec<f64>,
    pub angle2: Vec<f64>,
    pub angle3: Vec<f64>,
    pub angle4: Vec<f64>,
    pub angle5: Vec<f64>,
    pub angle6: Vec<f64>,
    pub angle7: Vec<f64>,
    pub angle8: Vec<f64>,
    pub imatt: Vec<f64>,
    pub ifibt: Vec<f64>,
    pub iloct: Vec<f64>,
    pub idelt: Vec<f64>,
    pub smatt: Vec<f64>,
    pub sfibt: Vec<f64>,
    pub sloct: Vec<f64>,
    pub sdelt: Vec<f64>,
    pub imatc: Vec<f64>,
    pub ifibc: Vec<f64>,
    pub ilocc: Vec<f64>,
    pub idelc: Vec<f64>,
    pub smatc: Vec<f64>,
    pub sfibc: Vec<f64>,
    pub slocc: Vec<f64>,
    pub sdelc: Vec<f64>,
    pub erode: Vec<i64>,
    pub erpar1: Vec<f64>,
    pub erpar2: Vec<f64>,
    pub resids: Vec<f64>,
}
impl MAT_CODAM2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CODAM2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            nlayer: t.take("NLAYER").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            r2: t.take("R2").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            angle1: t.take("ANGLE1").and_then(|c| c.into_float()).unwrap_or_default(),
            angle2: t.take("ANGLE2").and_then(|c| c.into_float()).unwrap_or_default(),
            angle3: t.take("ANGLE3").and_then(|c| c.into_float()).unwrap_or_default(),
            angle4: t.take("ANGLE4").and_then(|c| c.into_float()).unwrap_or_default(),
            angle5: t.take("ANGLE5").and_then(|c| c.into_float()).unwrap_or_default(),
            angle6: t.take("ANGLE6").and_then(|c| c.into_float()).unwrap_or_default(),
            angle7: t.take("ANGLE7").and_then(|c| c.into_float()).unwrap_or_default(),
            angle8: t.take("ANGLE8").and_then(|c| c.into_float()).unwrap_or_default(),
            imatt: t.take("IMATT").and_then(|c| c.into_float()).unwrap_or_default(),
            ifibt: t.take("IFIBT").and_then(|c| c.into_float()).unwrap_or_default(),
            iloct: t.take("ILOCT").and_then(|c| c.into_float()).unwrap_or_default(),
            idelt: t.take("IDELT").and_then(|c| c.into_float()).unwrap_or_default(),
            smatt: t.take("SMATT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfibt: t.take("SFIBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sloct: t.take("SLOCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sdelt: t.take("SDELT").and_then(|c| c.into_float()).unwrap_or_default(),
            imatc: t.take("IMATC").and_then(|c| c.into_float()).unwrap_or_default(),
            ifibc: t.take("IFIBC").and_then(|c| c.into_float()).unwrap_or_default(),
            ilocc: t.take("ILOCC").and_then(|c| c.into_float()).unwrap_or_default(),
            idelc: t.take("IDELC").and_then(|c| c.into_float()).unwrap_or_default(),
            smatc: t.take("SMATC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfibc: t.take("SFIBC").and_then(|c| c.into_float()).unwrap_or_default(),
            slocc: t.take("SLOCC").and_then(|c| c.into_float()).unwrap_or_default(),
            sdelc: t.take("SDELC").and_then(|c| c.into_float()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            erpar1: t.take("ERPAR1").and_then(|c| c.into_float()).unwrap_or_default(),
            erpar2: t.take("ERPAR2").and_then(|c| c.into_float()).unwrap_or_default(),
            resids: t.take("RESIDS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub et: Vec<f64>,
    pub en: Vec<f64>,
    pub fn_fail: Vec<f64>,
    pub ft_fail: Vec<f64>,
}
impl MAT_COHESIVE_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_fail: t.take("FN_FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ft_fail: t.take("FT_FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_GASKET {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<i64>,
    pub lc: Vec<i64>,
    pub uc: Vec<i64>,
    pub eten: Vec<f64>,
    pub etsr: Vec<f64>,
    pub emem: Vec<f64>,
    pub pr: Vec<f64>,
    pub ps: Vec<i64>,
}
impl MAT_COHESIVE_GASKET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_GASKET").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            uc: t.take("UC").and_then(|c| c.into_int()).unwrap_or_default(),
            eten: t.take("ETEN").and_then(|c| c.into_float()).unwrap_or_default(),
            etsr: t.take("ETSR").and_then(|c| c.into_float()).unwrap_or_default(),
            emem: t.take("EMEM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_GENERAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub tes: Vec<f64>,
    pub tslc: Vec<i64>,
    pub gic: Vec<f64>,
    pub giic: Vec<f64>,
    pub xmu: Vec<f64>,
    pub t: Vec<f64>,
    pub s: Vec<f64>,
    pub stfsf: Vec<f64>,
    pub tslc2: Vec<f64>,
}
impl MAT_COHESIVE_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_GENERAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tes: t.take("TES").and_then(|c| c.into_float()).unwrap_or_default(),
            tslc: t.take("TSLC").and_then(|c| c.into_int()).unwrap_or_default(),
            gic: t.take("GIC").and_then(|c| c.into_float()).unwrap_or_default(),
            giic: t.take("GIIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xmu: t.take("XMU").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            stfsf: t.take("STFSF").and_then(|c| c.into_float()).unwrap_or_default(),
            tslc2: t.take("TSLC2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<f64>,
    pub intfail: Vec<f64>,
    pub en: Vec<f64>,
    pub et: Vec<f64>,
    pub gic: Vec<f64>,
    pub giic: Vec<f64>,
    pub xmu: Vec<f64>,
    pub t: Vec<f64>,
    pub s: Vec<f64>,
    pub und: Vec<f64>,
    pub utd: Vec<f64>,
    pub gamma: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            gic: t.take("GIC").and_then(|c| c.into_float()).unwrap_or_default(),
            giic: t.take("GIIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xmu: t.take("XMU").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            und: t.take("UND").and_then(|c| c.into_float()).unwrap_or_default(),
            utd: t.take("UTD").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<f64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<f64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<f64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<i64>,
    pub gmod: Vec<i64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<i64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<i64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<i64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<i64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<i64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<i64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<i64>,
    pub gmod: Vec<i64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<i64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<i64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<i64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<i64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<i64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<i64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<i64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<i64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<i64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_FUNCTIONS_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_int()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL_3MODES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub emod: Vec<f64>,
    pub gmod: Vec<f64>,
    pub thick: Vec<f64>,
    pub inicrt: Vec<f64>,
    pub g1c_0: Vec<f64>,
    pub g1c_inf: Vec<f64>,
    pub edot_g1: Vec<f64>,
    pub t0: Vec<f64>,
    pub t1: Vec<f64>,
    pub edot_t: Vec<f64>,
    pub fg1: Vec<f64>,
    pub lcg1c: Vec<i64>,
    pub g2c_0: Vec<f64>,
    pub g2c_inf: Vec<f64>,
    pub edot_g2: Vec<f64>,
    pub s0: Vec<f64>,
    pub s1: Vec<f64>,
    pub edot_s: Vec<f64>,
    pub fg2: Vec<f64>,
    pub lcg2c: Vec<i64>,
    pub g3c_0: Vec<f64>,
    pub g3c_inf: Vec<f64>,
    pub edot_g3: Vec<f64>,
    pub r0: Vec<f64>,
    pub r1: Vec<f64>,
    pub edot_r: Vec<f64>,
    pub fg3: Vec<f64>,
    pub lcg3c: Vec<i64>,
    pub gmod3: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL_3MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_MIXED_MODE_ELASTOPLASTIC_RATE_THERMAL_3MODES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            inicrt: t.take("INICRT").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_0: t.take("G1C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g1c_inf: t.take("G1C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g1: t.take("EDOT_G1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_t: t.take("EDOT_T").and_then(|c| c.into_float()).unwrap_or_default(),
            fg1: t.take("FG1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg1c: t.take("LCG1C").and_then(|c| c.into_int()).unwrap_or_default(),
            g2c_0: t.take("G2C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g2c_inf: t.take("G2C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g2: t.take("EDOT_G2").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_s: t.take("EDOT_S").and_then(|c| c.into_float()).unwrap_or_default(),
            fg2: t.take("FG2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg2c: t.take("LCG2C").and_then(|c| c.into_int()).unwrap_or_default(),
            g3c_0: t.take("G3C_0").and_then(|c| c.into_float()).unwrap_or_default(),
            g3c_inf: t.take("G3C_INF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_g3: t.take("EDOT_G3").and_then(|c| c.into_float()).unwrap_or_default(),
            r0: t.take("R0").and_then(|c| c.into_float()).unwrap_or_default(),
            r1: t.take("R1").and_then(|c| c.into_float()).unwrap_or_default(),
            edot_r: t.take("EDOT_R").and_then(|c| c.into_float()).unwrap_or_default(),
            fg3: t.take("FG3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg3c: t.take("LCG3C").and_then(|c| c.into_int()).unwrap_or_default(),
            gmod3: t.take("GMOD3").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_PAPER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub intfail: Vec<f64>,
    pub en0: Vec<f64>,
    pub et0: Vec<f64>,
    pub en1: Vec<f64>,
    pub et1: Vec<f64>,
    pub t0n: Vec<f64>,
    pub dn: Vec<f64>,
    pub t1n: Vec<f64>,
    pub t0t: Vec<f64>,
    pub dt: Vec<f64>,
    pub t1t: Vec<f64>,
    pub e3c: Vec<f64>,
    pub cc: Vec<f64>,
    pub asig: Vec<f64>,
    pub bsig: Vec<f64>,
    pub csig: Vec<f64>,
    pub failn: Vec<f64>,
    pub failt: Vec<f64>,
}
impl MAT_COHESIVE_PAPER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_PAPER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            en0: t.take("EN0").and_then(|c| c.into_float()).unwrap_or_default(),
            et0: t.take("ET0").and_then(|c| c.into_float()).unwrap_or_default(),
            en1: t.take("EN1").and_then(|c| c.into_float()).unwrap_or_default(),
            et1: t.take("ET1").and_then(|c| c.into_float()).unwrap_or_default(),
            t0n: t.take("T0N").and_then(|c| c.into_float()).unwrap_or_default(),
            dn: t.take("DN").and_then(|c| c.into_float()).unwrap_or_default(),
            t1n: t.take("T1N").and_then(|c| c.into_float()).unwrap_or_default(),
            t0t: t.take("T0T").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1t: t.take("T1T").and_then(|c| c.into_float()).unwrap_or_default(),
            e3c: t.take("E3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            asig: t.take("ASIG").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig: t.take("BSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            csig: t.take("CSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            failn: t.take("FAILN").and_then(|c| c.into_float()).unwrap_or_default(),
            failt: t.take("FAILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COHESIVE_TH {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<f64>,
    pub intfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub nls: Vec<f64>,
    pub tls: Vec<f64>,
    pub tls2: Vec<f64>,
    pub lamda1: Vec<f64>,
    pub lamda2: Vec<f64>,
    pub lamdaf: Vec<f64>,
    pub stfsf: Vec<f64>,
    pub isw: Vec<i64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub dr: Vec<f64>,
    pub alpha3: Vec<f64>,
}
impl MAT_COHESIVE_TH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COHESIVE_TH").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            intfail: t.take("INTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            nls: t.take("NLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tls: t.take("TLS").and_then(|c| c.into_float()).unwrap_or_default(),
            tls2: t.take("TLS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda1: t.take("LAMDA1").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda2: t.take("LAMDA2").and_then(|c| c.into_float()).unwrap_or_default(),
            lamdaf: t.take("LAMDAF").and_then(|c| c.into_float()).unwrap_or_default(),
            stfsf: t.take("STFSF").and_then(|c| c.into_float()).unwrap_or_default(),
            isw: t.take("ISW").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            dr: t.take("DR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kfail: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub atrack: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub alph: Vec<f64>,
    pub sn: Vec<f64>,
    pub syz: Vec<f64>,
    pub szx: Vec<f64>,
}
impl MAT_COMPOSITE_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kfail: t.take("KFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            atrack: t.take("ATRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            szx: t.take("SZX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_DIRECT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
}
impl MAT_COMPOSITE_DIRECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_DIRECT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_FAILURE_SHELL_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub tsize: Vec<f64>,
    pub alp: Vec<f64>,
    pub soft: Vec<f64>,
    pub fbrt: Vec<f64>,
    pub sr: Vec<f64>,
    pub sf: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
}
impl MAT_COMPOSITE_FAILURE_SHELL_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_FAILURE_SHELL_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            alp: t.take("ALP").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbrt: t.take("FBRT").and_then(|c| c.into_float()).unwrap_or_default(),
            sr: t.take("SR").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_FAILURE_SOLID_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sba: Vec<f64>,
    pub sca: Vec<f64>,
    pub scb: Vec<f64>,
    pub xxc: Vec<f64>,
    pub yyc: Vec<f64>,
    pub zzc: Vec<f64>,
    pub xxt: Vec<f64>,
    pub yyt: Vec<f64>,
    pub zzt: Vec<f64>,
}
impl MAT_COMPOSITE_FAILURE_SOLID_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_FAILURE_SOLID_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sba: t.take("SBA").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            scb: t.take("SCB").and_then(|c| c.into_float()).unwrap_or_default(),
            xxc: t.take("XXC").and_then(|c| c.into_float()).unwrap_or_default(),
            yyc: t.take("YYC").and_then(|c| c.into_float()).unwrap_or_default(),
            zzc: t.take("ZZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xxt: t.take("XXT").and_then(|c| c.into_float()).unwrap_or_default(),
            yyt: t.take("YYT").and_then(|c| c.into_float()).unwrap_or_default(),
            zzt: t.take("ZZT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_FAILURE_SPH_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sba: Vec<f64>,
    pub sca: Vec<f64>,
    pub scb: Vec<f64>,
    pub xxc: Vec<f64>,
    pub yyc: Vec<f64>,
    pub zzc: Vec<f64>,
    pub xxt: Vec<f64>,
    pub yyt: Vec<f64>,
    pub zzt: Vec<f64>,
}
impl MAT_COMPOSITE_FAILURE_SPH_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_FAILURE_SPH_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("KF").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sba: t.take("SBA").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            scb: t.take("SCB").and_then(|c| c.into_float()).unwrap_or_default(),
            xxc: t.take("XXC").and_then(|c| c.into_float()).unwrap_or_default(),
            yyc: t.take("YYC").and_then(|c| c.into_float()).unwrap_or_default(),
            zzc: t.take("ZZC").and_then(|c| c.into_float()).unwrap_or_default(),
            xxt: t.take("XXT").and_then(|c| c.into_float()).unwrap_or_default(),
            yyt: t.take("YYT").and_then(|c| c.into_float()).unwrap_or_default(),
            zzt: t.take("ZZT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_FAILURE_SUNIL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub f60: Vec<i64>,
    pub mfparm: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
    pub t1fail: Vec<f64>,
    pub c1fail: Vec<f64>,
    pub t2fail: Vec<f64>,
    pub c2fail: Vec<f64>,
    pub t3fail: Vec<f64>,
    pub c3fail: Vec<f64>,
    pub s12fail: Vec<f64>,
    pub s23fail: Vec<f64>,
    pub s31fail: Vec<f64>,
}
impl MAT_COMPOSITE_FAILURE_SUNIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_FAILURE_SUNIL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            mfparm: t.take("MFPARM").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            t1fail: t.take("T1FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c1fail: t.take("C1FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            t2fail: t.take("T2FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c2fail: t.take("C2FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            t3fail: t.take("T3FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            c3fail: t.take("C3FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s12fail: t.take("S12FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s23fail: t.take("S23FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            s31fail: t.take("S31FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_LAYUP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_COMPOSITE_LAYUP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_LAYUP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_MATRIX {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_COMPOSITE_MATRIX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_MATRIX").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_MSC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sat: Vec<f64>,
    pub sac: Vec<f64>,
    pub sbt: Vec<f64>,
    pub sbc: Vec<f64>,
    pub sct: Vec<f64>,
    pub sfc: Vec<f64>,
    pub sfs: Vec<f64>,
    pub sab: Vec<f64>,
    pub sbc_2: Vec<f64>,
    pub sca: Vec<f64>,
    pub sffc: Vec<f64>,
    pub amodel: Vec<i64>,
    pub phic: Vec<f64>,
    pub e_limt: Vec<f64>,
    pub s_delm: Vec<f64>,
    pub omgmx: Vec<f64>,
    pub ecrsh: Vec<f64>,
    pub eexpn: Vec<f64>,
    pub cerate1: Vec<f64>,
    pub am1: Vec<f64>,
}
impl MAT_COMPOSITE_MSC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_MSC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sat: t.take("SAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sac: t.take("SAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sbt: t.take("SBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sct: t.take("SCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfc: t.take("SFC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            sab: t.take("SAB").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc_2: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            sffc: t.take("SFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            amodel: t.take("AMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            phic: t.take("PHIC").and_then(|c| c.into_float()).unwrap_or_default(),
            e_limt: t.take("E_LIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            s_delm: t.take("S_DELM").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmx: t.take("OMGMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrsh: t.take("ECRSH").and_then(|c| c.into_float()).unwrap_or_default(),
            eexpn: t.take("EEXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate1: t.take("CERATE1").and_then(|c| c.into_float()).unwrap_or_default(),
            am1: t.take("AM1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_MSC_DMG {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub sat: Vec<f64>,
    pub sac: Vec<f64>,
    pub sbt: Vec<f64>,
    pub sbc: Vec<f64>,
    pub sct: Vec<f64>,
    pub sfc: Vec<f64>,
    pub sfs: Vec<f64>,
    pub sab: Vec<f64>,
    pub sbc_2: Vec<f64>,
    pub sca: Vec<f64>,
    pub sffc: Vec<f64>,
    pub amodel: Vec<i64>,
    pub phic: Vec<f64>,
    pub e_limt: Vec<f64>,
    pub s_delm: Vec<f64>,
    pub omgmx: Vec<f64>,
    pub ecrsh: Vec<f64>,
    pub eexpn: Vec<f64>,
    pub cerate1: Vec<f64>,
    pub am1: Vec<f64>,
    pub am2: Vec<f64>,
    pub am3: Vec<f64>,
    pub am4: Vec<f64>,
    pub cerate2: Vec<f64>,
    pub cerate3: Vec<f64>,
    pub cerate4: Vec<f64>,
}
impl MAT_COMPOSITE_MSC_DMG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_MSC_DMG").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sat: t.take("SAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sac: t.take("SAC").and_then(|c| c.into_float()).unwrap_or_default(),
            sbt: t.take("SBT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sct: t.take("SCT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfc: t.take("SFC").and_then(|c| c.into_float()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            sab: t.take("SAB").and_then(|c| c.into_float()).unwrap_or_default(),
            sbc_2: t.take("SBC").and_then(|c| c.into_float()).unwrap_or_default(),
            sca: t.take("SCA").and_then(|c| c.into_float()).unwrap_or_default(),
            sffc: t.take("SFFC").and_then(|c| c.into_float()).unwrap_or_default(),
            amodel: t.take("AMODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            phic: t.take("PHIC").and_then(|c| c.into_float()).unwrap_or_default(),
            e_limt: t.take("E_LIMT").and_then(|c| c.into_float()).unwrap_or_default(),
            s_delm: t.take("S_DELM").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmx: t.take("OMGMX").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrsh: t.take("ECRSH").and_then(|c| c.into_float()).unwrap_or_default(),
            eexpn: t.take("EEXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate1: t.take("CERATE1").and_then(|c| c.into_float()).unwrap_or_default(),
            am1: t.take("AM1").and_then(|c| c.into_float()).unwrap_or_default(),
            am2: t.take("AM2").and_then(|c| c.into_float()).unwrap_or_default(),
            am3: t.take("AM3").and_then(|c| c.into_float()).unwrap_or_default(),
            am4: t.take("AM4").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate2: t.take("CERATE2").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate3: t.take("CERATE3").and_then(|c| c.into_float()).unwrap_or_default(),
            cerate4: t.take("CERATE4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPOSITE_TABULATED_PLASTICITY_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub ptol: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub filt: Vec<f64>,
    pub vevp: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub tcsym: Vec<i64>,
    pub h11: Vec<f64>,
    pub h22: Vec<f64>,
    pub h33: Vec<f64>,
    pub h12: Vec<f64>,
    pub h23: Vec<f64>,
    pub h13: Vec<f64>,
    pub h44: Vec<f64>,
    pub h55: Vec<f64>,
    pub h66: Vec<f64>,
    pub lt1: Vec<i64>,
    pub lt2: Vec<i64>,
    pub lt3: Vec<i64>,
    pub lt4: Vec<i64>,
    pub lt5: Vec<i64>,
    pub lt6: Vec<i64>,
    pub lt7: Vec<i64>,
    pub lt8: Vec<i64>,
    pub lt9: Vec<i64>,
    pub lt10: Vec<i64>,
    pub lt11: Vec<i64>,
    pub lt12: Vec<i64>,
    pub ysc: Vec<i64>,
    pub dflag: Vec<i64>,
    pub dc: Vec<i64>,
    pub ftype: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub unused_4: Vec<i64>,
    pub unused_5: Vec<i64>,
    pub unused_6: Vec<i64>,
    pub unused_7: Vec<i64>,
    pub f: Vec<i64>,
    pub unused_8: Vec<i64>,
    pub unused_2_2: Vec<i64>,
    pub unused_3_2: Vec<i64>,
    pub unused_4_2: Vec<i64>,
    pub unused_5_2: Vec<i64>,
    pub unused_6_2: Vec<i64>,
    pub unused_7_2: Vec<i64>,
    pub beta11: Vec<f64>,
    pub beta22: Vec<f64>,
    pub beta33: Vec<f64>,
    pub beta44: Vec<f64>,
    pub beta55: Vec<f64>,
    pub beta66: Vec<f64>,
    pub beta12: Vec<f64>,
    pub beta23: Vec<f64>,
    pub beta13: Vec<f64>,
    pub cp: Vec<f64>,
    pub tqc: Vec<f64>,
    pub temp: Vec<f64>,
    pub pmacc: Vec<f64>,
}
impl MAT_COMPOSITE_TABULATED_PLASTICITY_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPOSITE_TABULATED_PLASTICITY_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            ptol: t.take("PTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            filt: t.take("FILT").and_then(|c| c.into_float()).unwrap_or_default(),
            vevp: t.take("VEVP").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tcsym: t.take("TCSYM").and_then(|c| c.into_int()).unwrap_or_default(),
            h11: t.take("H11").and_then(|c| c.into_float()).unwrap_or_default(),
            h22: t.take("H22").and_then(|c| c.into_float()).unwrap_or_default(),
            h33: t.take("H33").and_then(|c| c.into_float()).unwrap_or_default(),
            h12: t.take("H12").and_then(|c| c.into_float()).unwrap_or_default(),
            h23: t.take("H23").and_then(|c| c.into_float()).unwrap_or_default(),
            h13: t.take("H13").and_then(|c| c.into_float()).unwrap_or_default(),
            h44: t.take("H44").and_then(|c| c.into_float()).unwrap_or_default(),
            h55: t.take("H55").and_then(|c| c.into_float()).unwrap_or_default(),
            h66: t.take("H66").and_then(|c| c.into_float()).unwrap_or_default(),
            lt1: t.take("LT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lt2: t.take("LT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lt3: t.take("LT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lt4: t.take("LT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lt5: t.take("LT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lt6: t.take("LT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lt7: t.take("LT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lt8: t.take("LT8").and_then(|c| c.into_int()).unwrap_or_default(),
            lt9: t.take("LT9").and_then(|c| c.into_int()).unwrap_or_default(),
            lt10: t.take("LT10").and_then(|c| c.into_int()).unwrap_or_default(),
            lt11: t.take("LT11").and_then(|c| c.into_int()).unwrap_or_default(),
            lt12: t.take("LT12").and_then(|c| c.into_int()).unwrap_or_default(),
            ysc: t.take("YSC").and_then(|c| c.into_int()).unwrap_or_default(),
            dflag: t.take("DFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7: t.take("UNUSED_7").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_8: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_4_2: t.take("UNUSED_4").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_5_2: t.take("UNUSED_5").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_6_2: t.take("UNUSED_6").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_7_2: t.take("UNUSED_7").and_then(|c| c.into_int()).unwrap_or_default(),
            beta11: t.take("BETA11").and_then(|c| c.into_float()).unwrap_or_default(),
            beta22: t.take("BETA22").and_then(|c| c.into_float()).unwrap_or_default(),
            beta33: t.take("BETA33").and_then(|c| c.into_float()).unwrap_or_default(),
            beta44: t.take("BETA44").and_then(|c| c.into_float()).unwrap_or_default(),
            beta55: t.take("BETA55").and_then(|c| c.into_float()).unwrap_or_default(),
            beta66: t.take("BETA66").and_then(|c| c.into_float()).unwrap_or_default(),
            beta12: t.take("BETA12").and_then(|c| c.into_float()).unwrap_or_default(),
            beta23: t.take("BETA23").and_then(|c| c.into_float()).unwrap_or_default(),
            beta13: t.take("BETA13").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tqc: t.take("TQC").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            pmacc: t.take("PMACC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_COMPRF {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub et: Vec<f64>,
    pub ec: Vec<f64>,
    pub pr: Vec<f64>,
    pub g121: Vec<f64>,
    pub g122: Vec<f64>,
    pub g123: Vec<f64>,
    pub g124: Vec<f64>,
    pub g125: Vec<f64>,
    pub g126: Vec<f64>,
    pub gammal: Vec<f64>,
    pub vf: Vec<f64>,
    pub ef3: Vec<f64>,
    pub vf23: Vec<f64>,
    pub em: Vec<f64>,
    pub vm: Vec<f64>,
    pub epsilon: Vec<f64>,
    pub theta: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g: Vec<f64>,
}
impl MAT_COMPRF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_COMPRF").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            g121: t.take("G121").and_then(|c| c.into_float()).unwrap_or_default(),
            g122: t.take("G122").and_then(|c| c.into_float()).unwrap_or_default(),
            g123: t.take("G123").and_then(|c| c.into_float()).unwrap_or_default(),
            g124: t.take("G124").and_then(|c| c.into_float()).unwrap_or_default(),
            g125: t.take("G125").and_then(|c| c.into_float()).unwrap_or_default(),
            g126: t.take("G126").and_then(|c| c.into_float()).unwrap_or_default(),
            gammal: t.take("GAMMAL").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf23: t.take("VF23").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            epsilon: t.take("EPSILON").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub noten: Vec<i64>,
    pub tencut: Vec<f64>,
    pub sdr: Vec<f64>,
}
impl MAT_CONCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            noten: t.take("NOTEN").and_then(|c| c.into_int()).unwrap_or_default(),
            tencut: t.take("TENCUT").and_then(|c| c.into_float()).unwrap_or_default(),
            sdr: t.take("SDR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONCRETE_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigf: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a0y: Vec<f64>,
    pub a1y: Vec<f64>,
    pub a2y: Vec<f64>,
    pub a1f: Vec<f64>,
    pub a2f: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub per: Vec<f64>,
    pub er: Vec<f64>,
    pub prr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub lcp: Vec<i64>,
    pub lcr: Vec<i64>,
    pub lambda_1: Vec<f64>,
    pub lambda_2: Vec<f64>,
    pub lambda_3: Vec<f64>,
    pub lambda_4: Vec<f64>,
    pub lambda_5: Vec<f64>,
    pub lambda_6: Vec<f64>,
    pub lambda_7: Vec<f64>,
    pub lambda_8: Vec<f64>,
    pub lambda_9: Vec<f64>,
    pub lambda_10: Vec<f64>,
    pub lambda_11: Vec<f64>,
    pub lambda_12: Vec<f64>,
    pub lambda_13: Vec<f64>,
    pub nu_1: Vec<f64>,
    pub nu_2: Vec<f64>,
    pub nu_3: Vec<f64>,
    pub nu_4: Vec<f64>,
    pub nu_5: Vec<f64>,
    pub nu_6: Vec<f64>,
    pub nu_7: Vec<f64>,
    pub nu_8: Vec<f64>,
    pub nu_9: Vec<f64>,
    pub nu_10: Vec<f64>,
    pub nu_11: Vec<f64>,
    pub nu_12: Vec<f64>,
    pub nu_13: Vec<f64>,
}
impl MAT_CONCRETE_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONCRETE_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a0y: t.take("A0Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1y: t.take("A1Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a2y: t.take("A2Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            a2f: t.take("A2F").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            per: t.take("PER").and_then(|c| c.into_float()).unwrap_or_default(),
            er: t.take("ER").and_then(|c| c.into_float()).unwrap_or_default(),
            prr: t.take("PRR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
            lambda_1: t.take("LAMBDA-1").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_2: t.take("LAMBDA-2").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_3: t.take("LAMBDA-3").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_4: t.take("LAMBDA-4").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_5: t.take("LAMBDA-5").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_6: t.take("LAMBDA-6").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_7: t.take("LAMBDA-7").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_8: t.take("LAMBDA-8").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_9: t.take("LAMBDA-9").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_10: t.take("LAMBDA-10").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_11: t.take("LAMBDA-11").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_12: t.take("LAMBDA-12").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda_13: t.take("LAMBDA-13").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_1: t.take("NU-1").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_2: t.take("NU-2").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_3: t.take("NU-3").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_4: t.take("NU-4").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_5: t.take("NU-5").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_6: t.take("NU-6").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_7: t.take("NU-7").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_8: t.take("NU-8").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_9: t.take("NU-9").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_10: t.take("NU-10").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_11: t.take("NU-11").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_12: t.take("NU-12").and_then(|c| c.into_float()).unwrap_or_default(),
            nu_13: t.take("NU-13").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONCRETE_DAMAGE_PLASTIC_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ecc: Vec<f64>,
    pub qh0: Vec<f64>,
    pub ft: Vec<f64>,
    pub fc: Vec<f64>,
    pub hp: Vec<f64>,
    pub ah: Vec<f64>,
    pub bh: Vec<f64>,
    pub ch: Vec<f64>,
    pub dh: Vec<f64>,
    pub as_: Vec<f64>,
    pub df: Vec<f64>,
    pub fc0: Vec<f64>,
    pub type_: Vec<f64>,
    pub bs: Vec<f64>,
    pub wf: Vec<f64>,
    pub wf1: Vec<f64>,
    pub ft1: Vec<f64>,
    pub strflg: Vec<f64>,
    pub failflg: Vec<f64>,
    pub efc: Vec<f64>,
}
impl MAT_CONCRETE_DAMAGE_PLASTIC_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONCRETE_DAMAGE_PLASTIC_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ecc: t.take("ECC").and_then(|c| c.into_float()).unwrap_or_default(),
            qh0: t.take("QH0").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            hp: t.take("HP").and_then(|c| c.into_float()).unwrap_or_default(),
            ah: t.take("AH").and_then(|c| c.into_float()).unwrap_or_default(),
            bh: t.take("BH").and_then(|c| c.into_float()).unwrap_or_default(),
            ch: t.take("CH").and_then(|c| c.into_float()).unwrap_or_default(),
            dh: t.take("DH").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            fc0: t.take("FC0").and_then(|c| c.into_float()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            wf: t.take("WF").and_then(|c| c.into_float()).unwrap_or_default(),
            wf1: t.take("WF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ft1: t.take("FT1").and_then(|c| c.into_float()).unwrap_or_default(),
            strflg: t.take("STRFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            failflg: t.take("FAILFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            efc: t.take("EFC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONCRETE_DAMAGE_REL3 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub ft: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub b1: Vec<f64>,
    pub omega: Vec<f64>,
    pub a1f: Vec<f64>,
    pub slambda: Vec<f64>,
    pub nout: Vec<f64>,
    pub edrop: Vec<f64>,
    pub rsize: Vec<f64>,
    pub ucf: Vec<f64>,
    pub lcrate: Vec<i64>,
    pub locwidth: Vec<f64>,
    pub npts: Vec<f64>,
    pub lambda1: Vec<f64>,
    pub lambda2: Vec<f64>,
    pub lambda3: Vec<f64>,
    pub lambda4: Vec<f64>,
    pub lambda5: Vec<f64>,
    pub lambda6: Vec<f64>,
    pub lambda7: Vec<f64>,
    pub lambda8: Vec<f64>,
    pub lambda09: Vec<f64>,
    pub lambda10: Vec<f64>,
    pub lambda11: Vec<f64>,
    pub lambda12: Vec<f64>,
    pub lambda13: Vec<f64>,
    pub b3: Vec<f64>,
    pub a0y: Vec<f64>,
    pub a1y: Vec<f64>,
    pub eta1: Vec<f64>,
    pub eta2: Vec<f64>,
    pub eta3: Vec<f64>,
    pub eta4: Vec<f64>,
    pub eta5: Vec<f64>,
    pub eta6: Vec<f64>,
    pub eta7: Vec<f64>,
    pub eta8: Vec<f64>,
    pub eta09: Vec<f64>,
    pub eta10: Vec<f64>,
    pub eta11: Vec<f64>,
    pub eta12: Vec<f64>,
    pub eta13: Vec<f64>,
    pub b2: Vec<f64>,
    pub a2f: Vec<f64>,
    pub a2y: Vec<f64>,
}
impl MAT_CONCRETE_DAMAGE_REL3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONCRETE_DAMAGE_REL3").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            slambda: t.take("Slambda").and_then(|c| c.into_float()).unwrap_or_default(),
            nout: t.take("NOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            edrop: t.take("EDROP").and_then(|c| c.into_float()).unwrap_or_default(),
            rsize: t.take("RSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            ucf: t.take("UCF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrate: t.take("LCRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            locwidth: t.take("LOCWIDTH").and_then(|c| c.into_float()).unwrap_or_default(),
            npts: t.take("NPTS").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda1: t.take("lambda1").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda2: t.take("lambda2").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda3: t.take("lambda3").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda4: t.take("lambda4").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda5: t.take("lambda5").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda6: t.take("lambda6").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda7: t.take("lambda7").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda8: t.take("lambda8").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda09: t.take("lambda09").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda10: t.take("lambda10").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda11: t.take("lambda11").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda12: t.take("lambda12").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda13: t.take("lambda13").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            a0y: t.take("A0Y").and_then(|c| c.into_float()).unwrap_or_default(),
            a1y: t.take("A1Y").and_then(|c| c.into_float()).unwrap_or_default(),
            eta1: t.take("eta1").and_then(|c| c.into_float()).unwrap_or_default(),
            eta2: t.take("eta2").and_then(|c| c.into_float()).unwrap_or_default(),
            eta3: t.take("eta3").and_then(|c| c.into_float()).unwrap_or_default(),
            eta4: t.take("eta4").and_then(|c| c.into_float()).unwrap_or_default(),
            eta5: t.take("eta5").and_then(|c| c.into_float()).unwrap_or_default(),
            eta6: t.take("eta6").and_then(|c| c.into_float()).unwrap_or_default(),
            eta7: t.take("eta7").and_then(|c| c.into_float()).unwrap_or_default(),
            eta8: t.take("eta8").and_then(|c| c.into_float()).unwrap_or_default(),
            eta09: t.take("eta09").and_then(|c| c.into_float()).unwrap_or_default(),
            eta10: t.take("eta10").and_then(|c| c.into_float()).unwrap_or_default(),
            eta11: t.take("eta11").and_then(|c| c.into_float()).unwrap_or_default(),
            eta12: t.take("eta12").and_then(|c| c.into_float()).unwrap_or_default(),
            eta13: t.take("eta13").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            a2f: t.take("A2F").and_then(|c| c.into_float()).unwrap_or_default(),
            a2y: t.take("A2Y").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONCRETE_EC2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub fc: Vec<f64>,
    pub ft: Vec<f64>,
    pub typec: Vec<f64>,
    pub unitc: Vec<f64>,
    pub ecuten: Vec<f64>,
    pub fcc: Vec<f64>,
    pub esoft: Vec<f64>,
    pub lchar: Vec<f64>,
    pub mu: Vec<f64>,
    pub taumxf: Vec<f64>,
    pub taumxc: Vec<f64>,
    pub ecragg: Vec<f64>,
    pub aggsz: Vec<f64>,
    pub unitl: Vec<f64>,
    pub ymreinf: Vec<f64>,
    pub prrinf: Vec<f64>,
    pub sureinf: Vec<f64>,
    pub typer: Vec<f64>,
    pub fracrx: Vec<f64>,
    pub fracry: Vec<f64>,
    pub lcrsu: Vec<i64>,
    pub lcalps: Vec<i64>,
    pub aopt: Vec<f64>,
    pub et36: Vec<f64>,
    pub prt36: Vec<f64>,
    pub ecut36: Vec<f64>,
    pub lcalpc: Vec<i64>,
    pub degrad: Vec<f64>,
    pub ishchk: Vec<i64>,
    pub unlfac: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub typesc: Vec<f64>,
    pub p_or_f: Vec<f64>,
    pub effd: Vec<f64>,
    pub gamsc: Vec<f64>,
    pub erodet: Vec<f64>,
    pub erodec: Vec<f64>,
    pub eroder: Vec<f64>,
    pub tmpoff: Vec<f64>,
    pub ec1_6: Vec<f64>,
    pub ecsp69: Vec<f64>,
    pub gamce9: Vec<f64>,
    pub phief9: Vec<f64>,
    pub ft2: Vec<f64>,
    pub ftshr: Vec<f64>,
    pub lcftt: Vec<i64>,
    pub wro_g: Vec<f64>,
    pub zsurf: Vec<f64>,
}
impl MAT_CONCRETE_EC2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONCRETE_EC2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            typec: t.take("TYPEC").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            ecuten: t.take("ECUTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            fcc: t.take("FCC").and_then(|c| c.into_float()).unwrap_or_default(),
            esoft: t.take("ESOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchar: t.take("LCHAR").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            taumxf: t.take("TAUMXF").and_then(|c| c.into_float()).unwrap_or_default(),
            taumxc: t.take("TAUMXC").and_then(|c| c.into_float()).unwrap_or_default(),
            ecragg: t.take("ECRAGG").and_then(|c| c.into_float()).unwrap_or_default(),
            aggsz: t.take("AGGSZ").and_then(|c| c.into_float()).unwrap_or_default(),
            unitl: t.take("UNITL").and_then(|c| c.into_float()).unwrap_or_default(),
            ymreinf: t.take("YMREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            prrinf: t.take("PRRINF").and_then(|c| c.into_float()).unwrap_or_default(),
            sureinf: t.take("SUREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            typer: t.take("TYPER").and_then(|c| c.into_float()).unwrap_or_default(),
            fracrx: t.take("FRACRX").and_then(|c| c.into_float()).unwrap_or_default(),
            fracry: t.take("FRACRY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcrsu: t.take("LCRSU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcalps: t.take("LCALPS").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            et36: t.take("ET36").and_then(|c| c.into_float()).unwrap_or_default(),
            prt36: t.take("PRT36").and_then(|c| c.into_float()).unwrap_or_default(),
            ecut36: t.take("ECUT36").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalpc: t.take("LCALPC").and_then(|c| c.into_int()).unwrap_or_default(),
            degrad: t.take("DEGRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            ishchk: t.take("ISHCHK").and_then(|c| c.into_int()).unwrap_or_default(),
            unlfac: t.take("UNLFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            typesc: t.take("TYPESC").and_then(|c| c.into_float()).unwrap_or_default(),
            p_or_f: t.take("P_OR_F").and_then(|c| c.into_float()).unwrap_or_default(),
            effd: t.take("EFFD").and_then(|c| c.into_float()).unwrap_or_default(),
            gamsc: t.take("GAMSC").and_then(|c| c.into_float()).unwrap_or_default(),
            erodet: t.take("ERODET").and_then(|c| c.into_float()).unwrap_or_default(),
            erodec: t.take("ERODEC").and_then(|c| c.into_float()).unwrap_or_default(),
            eroder: t.take("ERODER").and_then(|c| c.into_float()).unwrap_or_default(),
            tmpoff: t.take("TMPOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1_6: t.take("EC1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            ecsp69: t.take("ECSP69").and_then(|c| c.into_float()).unwrap_or_default(),
            gamce9: t.take("GAMCE9").and_then(|c| c.into_float()).unwrap_or_default(),
            phief9: t.take("PHIEF9").and_then(|c| c.into_float()).unwrap_or_default(),
            ft2: t.take("FT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ftshr: t.take("FTSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcftt: t.take("LCFTT").and_then(|c| c.into_int()).unwrap_or_default(),
            wro_g: t.take("WRO_G").and_then(|c| c.into_float()).unwrap_or_default(),
            zsurf: t.take("ZSURF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONSTRAINED_SPR2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub fn_: Vec<f64>,
    pub ft: Vec<f64>,
    pub dn: Vec<f64>,
    pub dt: Vec<f64>,
    pub xin: Vec<f64>,
    pub xit: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub expn: Vec<f64>,
    pub expt: Vec<f64>,
}
impl MAT_CONSTRAINED_SPR2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONSTRAINED_SPR2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            dn: t.take("DN").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            xin: t.take("XIN").and_then(|c| c.into_float()).unwrap_or_default(),
            xit: t.take("XIT").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            expn: t.take("EXPN").and_then(|c| c.into_float()).unwrap_or_default(),
            expt: t.take("EXPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CONSTRAINED_SPR3 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub model: Vec<i64>,
    pub stiff: Vec<f64>,
    pub rn: Vec<f64>,
    pub rs: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcf: Vec<i64>,
    pub lcupf: Vec<i64>,
    pub lcupr: Vec<i64>,
    pub upfn: Vec<f64>,
    pub upfs: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub uprn: Vec<f64>,
    pub uprs: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub beta3: Vec<f64>,
    pub mrn: Vec<f64>,
    pub mrs: Vec<f64>,
}
impl MAT_CONSTRAINED_SPR3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CONSTRAINED_SPR3").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupf: t.take("LCUPF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcupr: t.take("LCUPR").and_then(|c| c.into_int()).unwrap_or_default(),
            upfn: t.take("UPFN").and_then(|c| c.into_float()).unwrap_or_default(),
            upfs: t.take("UPFS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            uprn: t.take("UPRN").and_then(|c| c.into_float()).unwrap_or_default(),
            uprs: t.take("UPRS").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta3: t.take("BETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            mrn: t.take("MRN").and_then(|c| c.into_float()).unwrap_or_default(),
            mrs: t.take("MRS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CORUS_VEGTER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fun_i: Vec<f64>,
    pub run_i: Vec<f64>,
    pub fps1_i: Vec<f64>,
    pub fps2_i: Vec<f64>,
    pub fsh_i: Vec<f64>,
}
impl MAT_CORUS_VEGTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CORUS_VEGTER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fun_i: t.take("FUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            run_i: t.take("RUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps1_i: t.take("FPS1-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps2_i: t.take("FPS2-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fsh_i: t.take("FSH-I").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CRUSHABLE_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tsc: Vec<f64>,
    pub damp: Vec<f64>,
    pub model: Vec<i64>,
}
impl MAT_CRUSHABLE_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CRUSHABLE_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            model: t.take("MODEL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CSCM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub incre: Vec<f64>,
    pub irate: Vec<i64>,
    pub erode: Vec<f64>,
    pub recov: Vec<f64>,
    pub itretrc: Vec<i64>,
    pub pred: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub lamda: Vec<f64>,
    pub beta: Vec<f64>,
    pub nh: Vec<f64>,
    pub ch: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub theta1: Vec<f64>,
    pub lamda1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub theta2: Vec<f64>,
    pub lamda2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub r: Vec<f64>,
    pub xd: Vec<f64>,
    pub w: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub b: Vec<f64>,
    pub gfc: Vec<f64>,
    pub d: Vec<f64>,
    pub gft: Vec<f64>,
    pub gfs: Vec<f64>,
    pub pwrc: Vec<f64>,
    pub pwrt: Vec<f64>,
    pub pmod: Vec<f64>,
    pub eta0c: Vec<f64>,
    pub nc: Vec<f64>,
    pub etaot: Vec<f64>,
    pub nt: Vec<f64>,
    pub overc: Vec<f64>,
    pub overt: Vec<f64>,
    pub srate: Vec<f64>,
    pub rep0w: Vec<f64>,
}
impl MAT_CSCM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CSCM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            incre: t.take("INCRE").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_float()).unwrap_or_default(),
            recov: t.take("RECOV").and_then(|c| c.into_float()).unwrap_or_default(),
            itretrc: t.take("ITRETRC").and_then(|c| c.into_int()).unwrap_or_default(),
            pred: t.take("PRED").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda: t.take("LAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nh: t.take("NH").and_then(|c| c.into_float()).unwrap_or_default(),
            ch: t.take("CH").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda1: t.take("LAMDA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda2: t.take("LAMDA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            xd: t.take("XD").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            gfc: t.take("GFC").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            gft: t.take("GFT").and_then(|c| c.into_float()).unwrap_or_default(),
            gfs: t.take("GFS").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrc: t.take("PWRC").and_then(|c| c.into_float()).unwrap_or_default(),
            pwrt: t.take("PWRT").and_then(|c| c.into_float()).unwrap_or_default(),
            pmod: t.take("PMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            eta0c: t.take("ETA0C").and_then(|c| c.into_float()).unwrap_or_default(),
            nc: t.take("NC").and_then(|c| c.into_float()).unwrap_or_default(),
            etaot: t.take("ETAOT").and_then(|c| c.into_float()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            overc: t.take("OVERC").and_then(|c| c.into_float()).unwrap_or_default(),
            overt: t.take("OVERT").and_then(|c| c.into_float()).unwrap_or_default(),
            srate: t.take("SRATE").and_then(|c| c.into_float()).unwrap_or_default(),
            rep0w: t.take("REP0W").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CSCM_CONCRETE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub incre: Vec<f64>,
    pub irate: Vec<i64>,
    pub erode: Vec<f64>,
    pub recov: Vec<f64>,
    pub itretrc: Vec<i64>,
    pub pred: Vec<f64>,
    pub fpc: Vec<f64>,
    pub dagg: Vec<f64>,
    pub units: Vec<i64>,
}
impl MAT_CSCM_CONCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CSCM_CONCRETE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            incre: t.take("INCRE").and_then(|c| c.into_float()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_float()).unwrap_or_default(),
            recov: t.take("RECOV").and_then(|c| c.into_float()).unwrap_or_default(),
            itretrc: t.take("ITRETRC").and_then(|c| c.into_int()).unwrap_or_default(),
            pred: t.take("PRED").and_then(|c| c.into_float()).unwrap_or_default(),
            fpc: t.take("FPC").and_then(|c| c.into_float()).unwrap_or_default(),
            dagg: t.take("DAGG").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_CWM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub lcem: Vec<i64>,
    pub lcpr: Vec<i64>,
    pub lcsy: Vec<i64>,
    pub lchr: Vec<i64>,
    pub lcat: Vec<i64>,
    pub beta: Vec<f64>,
    pub tastart: Vec<f64>,
    pub taend: Vec<f64>,
    pub tlstart: Vec<f64>,
    pub tlend: Vec<f64>,
    pub eghost: Vec<f64>,
    pub pghost: Vec<f64>,
    pub aghost: Vec<f64>,
    pub t2phase: Vec<f64>,
    pub t1phase: Vec<f64>,
}
impl MAT_CWM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_CWM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcem: t.take("LCEM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsy: t.take("LCSY").and_then(|c| c.into_int()).unwrap_or_default(),
            lchr: t.take("LCHR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tastart: t.take("TASTART").and_then(|c| c.into_float()).unwrap_or_default(),
            taend: t.take("TAEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tlstart: t.take("TLSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tlend: t.take("TLEND").and_then(|c| c.into_float()).unwrap_or_default(),
            eghost: t.take("EGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            pghost: t.take("PGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            aghost: t.take("AGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            t2phase: t.take("T2PHASE").and_then(|c| c.into_float()).unwrap_or_default(),
            t1phase: t.take("T1PHASE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DAMAGE_1 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcds: Vec<i64>,
    pub q1: Vec<f64>,
    pub c1: Vec<f64>,
    pub q2: Vec<f64>,
    pub c2: Vec<f64>,
    pub epsd: Vec<f64>,
    pub espr_s: Vec<f64>,
    pub dc: Vec<f64>,
    pub flag: Vec<i64>,
    pub vk: Vec<f64>,
    pub vm: Vec<f64>,
    pub r00_f: Vec<f64>,
    pub r45_g: Vec<f64>,
    pub r90_h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<i64>,
    pub f20: Vec<i64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_DAMAGE_1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DAMAGE_1").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcds: t.take("LCDS").and_then(|c| c.into_int()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            espr_s: t.take("ESPR/S").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            vk: t.take("VK").and_then(|c| c.into_float()).unwrap_or_default(),
            vm: t.take("VM").and_then(|c| c.into_float()).unwrap_or_default(),
            r00_f: t.take("R00/F").and_then(|c| c.into_float()).unwrap_or_default(),
            r45_g: t.take("R45/G").and_then(|c| c.into_float()).unwrap_or_default(),
            r90_h: t.take("R90/H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DAMAGE_2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub epsd: Vec<f64>,
    pub s: Vec<f64>,
    pub dc: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_DAMAGE_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DAMAGE_2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DAMAGE_3 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub hardi: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcss: Vec<i64>,
    pub hardk1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub hardk2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub hardk3: Vec<f64>,
    pub gamma3: Vec<f64>,
    pub idamage: Vec<i64>,
    pub ids: Vec<i64>,
    pub idep: Vec<i64>,
    pub epsd: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub dc: Vec<f64>,
    pub khflg: Vec<i64>,
    pub hardk4: Vec<f64>,
    pub gamma5: Vec<f64>,
    pub lckh: Vec<i64>,
    pub nkh: Vec<i64>,
}
impl MAT_DAMAGE_3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DAMAGE_3").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            hardi: t.take("HARDI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            hardk1: t.take("HARDK1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            hardk2: t.take("HARDK2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            hardk3: t.take("HARDK3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
            idamage: t.take("IDAMAGE").and_then(|c| c.into_int()).unwrap_or_default(),
            ids: t.take("IDS").and_then(|c| c.into_int()).unwrap_or_default(),
            idep: t.take("IDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            khflg: t.take("KHFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            hardk4: t.take("HARDK4").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma5: t.take("GAMMA5").and_then(|c| c.into_float()).unwrap_or_default(),
            lckh: t.take("LCKH").and_then(|c| c.into_int()).unwrap_or_default(),
            nkh: t.take("NKH").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DAMPER_NONLINEAR_VISCOUS {
    pub mid: Vec<i64>,
    pub lcdr: Vec<i64>,
}
impl MAT_DAMPER_NONLINEAR_VISCOUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DAMPER_NONLINEAR_VISCOUS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DAMPER_VISCOUS {
    pub mid: Vec<i64>,
    pub dc: Vec<f64>,
}
impl MAT_DAMPER_VISCOUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DAMPER_VISCOUS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DESHPANDE_FLECK_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub alpha: Vec<f64>,
    pub gamma: Vec<f64>,
    pub epsd: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub beta: Vec<f64>,
    pub sigp: Vec<f64>,
    pub derfi: Vec<f64>,
    pub cfail: Vec<f64>,
    pub pfail: Vec<f64>,
    pub num: Vec<i64>,
}
impl MAT_DESHPANDE_FLECK_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DESHPANDE_FLECK_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd: t.take("EPSD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigp: t.take("SIGP").and_then(|c| c.into_float()).unwrap_or_default(),
            derfi: t.take("DERFI").and_then(|c| c.into_float()).unwrap_or_default(),
            cfail: t.take("CFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            pfail: t.take("PFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            num: t.take("NUM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DISCRETE_BEAM_POINT_CONTACT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub stiff: Vec<f64>,
    pub fric: Vec<f64>,
    pub damp: Vec<f64>,
    pub dmxpz: Vec<f64>,
    pub limpz: Vec<f64>,
    pub dmxpx: Vec<f64>,
    pub dmxnx: Vec<f64>,
    pub dmxpy: Vec<f64>,
    pub dmxny: Vec<f64>,
    pub limpx: Vec<f64>,
    pub limnx: Vec<f64>,
    pub limpy: Vec<f64>,
    pub limny: Vec<f64>,
    pub krotx: Vec<f64>,
    pub kroty: Vec<f64>,
    pub krotz: Vec<f64>,
    pub tkrot: Vec<f64>,
    pub fbondh: Vec<f64>,
    pub fbondt: Vec<f64>,
    pub dbondh: Vec<f64>,
    pub dbondt: Vec<f64>,
    pub lcz: Vec<i64>,
    pub dampz: Vec<f64>,
    pub stiffh: Vec<f64>,
    pub frmax: Vec<f64>,
    pub damph: Vec<f64>,
    pub gap0: Vec<f64>,
    pub afac: Vec<f64>,
}
impl MAT_DISCRETE_BEAM_POINT_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DISCRETE_BEAM_POINT_CONTACT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            stiff: t.take("STIFF").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpz: t.take("DMXPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            limpz: t.take("LIMPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpx: t.take("DMXPX").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxnx: t.take("DMXNX").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxpy: t.take("DMXPY").and_then(|c| c.into_float()).unwrap_or_default(),
            dmxny: t.take("DMXNY").and_then(|c| c.into_float()).unwrap_or_default(),
            limpx: t.take("LIMPX").and_then(|c| c.into_float()).unwrap_or_default(),
            limnx: t.take("LIMNX").and_then(|c| c.into_float()).unwrap_or_default(),
            limpy: t.take("LIMPY").and_then(|c| c.into_float()).unwrap_or_default(),
            limny: t.take("LIMNY").and_then(|c| c.into_float()).unwrap_or_default(),
            krotx: t.take("KROTX").and_then(|c| c.into_float()).unwrap_or_default(),
            kroty: t.take("KROTY").and_then(|c| c.into_float()).unwrap_or_default(),
            krotz: t.take("KROTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tkrot: t.take("TKROT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbondh: t.take("FBONDH").and_then(|c| c.into_float()).unwrap_or_default(),
            fbondt: t.take("FBONDT").and_then(|c| c.into_float()).unwrap_or_default(),
            dbondh: t.take("DBONDH").and_then(|c| c.into_float()).unwrap_or_default(),
            dbondt: t.take("DBONDT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcz: t.take("LCZ").and_then(|c| c.into_int()).unwrap_or_default(),
            dampz: t.take("DAMPZ").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffh: t.take("STIFFH").and_then(|c| c.into_float()).unwrap_or_default(),
            frmax: t.take("FRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            damph: t.take("DAMPH").and_then(|c| c.into_float()).unwrap_or_default(),
            gap0: t.take("GAP0").and_then(|c| c.into_float()).unwrap_or_default(),
            afac: t.take("AFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DRUCKER_PRAGER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rkf: Vec<f64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub str_lim: Vec<f64>,
    pub gmoddp: Vec<f64>,
    pub phidp: Vec<f64>,
    pub cvaldp: Vec<f64>,
    pub psidp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub phigr: Vec<f64>,
    pub cvalgr: Vec<f64>,
    pub psigr: Vec<f64>,
}
impl MAT_DRUCKER_PRAGER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DRUCKER_PRAGER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rkf: t.take("RKF").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            str_lim: t.take("STR_LIM").and_then(|c| c.into_float()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            phidp: t.take("PHIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvaldp: t.take("CVALDP").and_then(|c| c.into_float()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            phigr: t.take("PHIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            psigr: t.take("PSIGR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_DRY_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub gab1: Vec<f64>,
    pub gab2: Vec<f64>,
    pub gab3: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub gamab1: Vec<f64>,
    pub gamab2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f10: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub eacrf: Vec<f64>,
    pub ebcrf: Vec<f64>,
    pub eacrp: Vec<f64>,
    pub ebcrp: Vec<f64>,
    pub easf: Vec<f64>,
    pub ebsf: Vec<f64>,
    pub eunlf: Vec<f64>,
    pub ecomf: Vec<f64>,
    pub eamax: Vec<f64>,
    pub ebmax: Vec<f64>,
    pub sigpost: Vec<f64>,
    pub cce: Vec<f64>,
    pub pce: Vec<f64>,
    pub cse: Vec<f64>,
    pub pse: Vec<f64>,
    pub dfac: Vec<f64>,
    pub emax: Vec<f64>,
    pub eafail: Vec<f64>,
    pub ebfail: Vec<f64>,
}
impl MAT_DRY_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_DRY_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab1: t.take("GAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab2: t.take("GAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab3: t.take("GAB3").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamab1: t.take("GAMAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamab2: t.take("GAMAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            eacrf: t.take("EACRF").and_then(|c| c.into_float()).unwrap_or_default(),
            ebcrf: t.take("EBCRF").and_then(|c| c.into_float()).unwrap_or_default(),
            eacrp: t.take("EACRP").and_then(|c| c.into_float()).unwrap_or_default(),
            ebcrp: t.take("EBCRP").and_then(|c| c.into_float()).unwrap_or_default(),
            easf: t.take("EASF").and_then(|c| c.into_float()).unwrap_or_default(),
            ebsf: t.take("EBSF").and_then(|c| c.into_float()).unwrap_or_default(),
            eunlf: t.take("EUNLF").and_then(|c| c.into_float()).unwrap_or_default(),
            ecomf: t.take("ECOMF").and_then(|c| c.into_float()).unwrap_or_default(),
            eamax: t.take("EAMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ebmax: t.take("EBMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigpost: t.take("SIGPOST").and_then(|c| c.into_float()).unwrap_or_default(),
            cce: t.take("CCE").and_then(|c| c.into_float()).unwrap_or_default(),
            pce: t.take("PCE").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            pse: t.take("PSE").and_then(|c| c.into_float()).unwrap_or_default(),
            dfac: t.take("DFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            emax: t.take("EMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            eafail: t.take("EAFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ebfail: t.take("EBFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_EIGHT_CHAIN_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub mu: Vec<f64>,
    pub n: Vec<i64>,
    pub mull: Vec<i64>,
    pub vispl: Vec<i64>,
    pub visel: Vec<i64>,
    pub yld0: Vec<f64>,
    pub fp: Vec<f64>,
    pub gp: Vec<f64>,
    pub hp: Vec<f64>,
    pub lp: Vec<f64>,
    pub mp: Vec<f64>,
    pub np: Vec<f64>,
    pub pmu: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub time: Vec<f64>,
    pub vcon: Vec<f64>,
    pub q1: Vec<f64>,
    pub b1: Vec<f64>,
    pub q2: Vec<f64>,
    pub b2: Vec<f64>,
    pub q3: Vec<f64>,
    pub b3: Vec<f64>,
    pub q4: Vec<f64>,
    pub b4: Vec<f64>,
    pub k1: Vec<f64>,
    pub s1: Vec<f64>,
    pub k2: Vec<f64>,
    pub s2: Vec<f64>,
    pub k3: Vec<f64>,
    pub s3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub taui: Vec<f64>,
    pub betai_gammai: Vec<f64>,
    pub taui_2: Vec<f64>,
    pub betai_gammai_2: Vec<f64>,
    pub taui_3: Vec<f64>,
    pub betai_gammai_3: Vec<f64>,
    pub taui_4: Vec<f64>,
    pub betai_gammai_4: Vec<f64>,
    pub taui_5: Vec<f64>,
    pub betai_gammai_5: Vec<f64>,
    pub taui_6: Vec<f64>,
    pub betai_gammai_6: Vec<f64>,
    pub taui_7: Vec<f64>,
    pub betai_gammai_7: Vec<f64>,
}
impl MAT_EIGHT_CHAIN_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_EIGHT_CHAIN_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            mull: t.take("MULL").and_then(|c| c.into_int()).unwrap_or_default(),
            vispl: t.take("VISPL").and_then(|c| c.into_int()).unwrap_or_default(),
            visel: t.take("VISEL").and_then(|c| c.into_int()).unwrap_or_default(),
            yld0: t.take("YLD0").and_then(|c| c.into_float()).unwrap_or_default(),
            fp: t.take("FP").and_then(|c| c.into_float()).unwrap_or_default(),
            gp: t.take("GP").and_then(|c| c.into_float()).unwrap_or_default(),
            hp: t.take("HP").and_then(|c| c.into_float()).unwrap_or_default(),
            lp: t.take("LP").and_then(|c| c.into_float()).unwrap_or_default(),
            mp: t.take("MP").and_then(|c| c.into_float()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_float()).unwrap_or_default(),
            pmu: t.take("PMU").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
            vcon: t.take("VCON").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            q4: t.take("Q4").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            taui: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_2: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_2: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_3: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_3: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_4: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_4: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_5: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_5: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_6: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_6: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
            taui_7: t.take("TAUi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai_gammai_7: t.take("BETAi/GAMMAi").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub da: Vec<f64>,
    pub db: Vec<f64>,
    pub not_used: Vec<f64>,
}
impl MAT_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            da: t.take("DA").and_then(|c| c.into_float()).unwrap_or_default(),
            db: t.take("DB").and_then(|c| c.into_float()).unwrap_or_default(),
            not_used: t.take("NOT_USED").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_6DOF_SPRING_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tpidr: Vec<i64>,
    pub tpids: Vec<i64>,
    pub tpidt: Vec<i64>,
    pub rpidr: Vec<i64>,
    pub rpids: Vec<i64>,
    pub rpidt: Vec<i64>,
}
impl MAT_ELASTIC_6DOF_SPRING_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_6DOF_SPRING_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tpidr: t.take("TPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tpids: t.take("TPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            tpidt: t.take("TPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidr: t.take("RPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            rpids: t.take("RPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidt: t.take("RPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_FLUID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub da: Vec<f64>,
    pub db: Vec<f64>,
    pub k: Vec<f64>,
    pub vc: Vec<f64>,
    pub cp: Vec<f64>,
}
impl MAT_ELASTIC_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_FLUID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            da: t.take("DA").and_then(|c| c.into_float()).unwrap_or_default(),
            db: t.take("DB").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PERI {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub gt: Vec<f64>,
    pub gs: Vec<f64>,
}
impl MAT_ELASTIC_PERI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PERI").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            gt: t.take("Gt").and_then(|c| c.into_float()).unwrap_or_default(),
            gs: t.take("Gs").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PERI_LAMINATE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub em: Vec<f64>,
    pub vfm: Vec<f64>,
    pub gfm: Vec<f64>,
    pub fopt: Vec<f64>,
    pub fcf: Vec<f64>,
    pub fcm: Vec<f64>,
    pub fcfc: Vec<f64>,
    pub fcmc: Vec<f64>,
    pub fcd: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_ELASTIC_PERI_LAMINATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PERI_LAMINATE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            vfm: t.take("VFM").and_then(|c| c.into_float()).unwrap_or_default(),
            gfm: t.take("GFM").and_then(|c| c.into_float()).unwrap_or_default(),
            fopt: t.take("FOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            fcf: t.take("FCF").and_then(|c| c.into_float()).unwrap_or_default(),
            fcm: t.take("FCM").and_then(|c| c.into_float()).unwrap_or_default(),
            fcfc: t.take("FCFC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcmc: t.take("FCMC").and_then(|c| c.into_float()).unwrap_or_default(),
            fcd: t.take("FCD").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro1: Vec<f64>,
    pub e1: Vec<f64>,
    pub pr1: Vec<f64>,
    pub f0: Vec<i64>,
    pub ro2: Vec<f64>,
    pub e2: Vec<f64>,
    pub pr2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_ELASTIC_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro1: t.take("RO1").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            ro2: t.take("RO2").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PLASTIC_HYDRO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub pc: Vec<f64>,
    pub fs: Vec<f64>,
    pub charl: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub eps11: Vec<f64>,
    pub eps12: Vec<f64>,
    pub eps13: Vec<f64>,
    pub eps14: Vec<f64>,
    pub eps15: Vec<f64>,
    pub eps16: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub es9: Vec<f64>,
    pub es10: Vec<f64>,
    pub es11: Vec<f64>,
    pub es12: Vec<f64>,
    pub es13: Vec<f64>,
    pub es14: Vec<f64>,
    pub es15: Vec<f64>,
    pub es16: Vec<f64>,
}
impl MAT_ELASTIC_PLASTIC_HYDRO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PLASTIC_HYDRO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            charl: t.take("CHARL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            eps11: t.take("EPS11").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12: t.take("EPS12").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13: t.take("EPS13").and_then(|c| c.into_float()).unwrap_or_default(),
            eps14: t.take("EPS14").and_then(|c| c.into_float()).unwrap_or_default(),
            eps15: t.take("EPS15").and_then(|c| c.into_float()).unwrap_or_default(),
            eps16: t.take("EPS16").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            es9: t.take("ES9").and_then(|c| c.into_float()).unwrap_or_default(),
            es10: t.take("ES10").and_then(|c| c.into_float()).unwrap_or_default(),
            es11: t.take("ES11").and_then(|c| c.into_float()).unwrap_or_default(),
            es12: t.take("ES12").and_then(|c| c.into_float()).unwrap_or_default(),
            es13: t.take("ES13").and_then(|c| c.into_float()).unwrap_or_default(),
            es14: t.take("ES14").and_then(|c| c.into_float()).unwrap_or_default(),
            es15: t.take("ES15").and_then(|c| c.into_float()).unwrap_or_default(),
            es16: t.take("ES16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PLASTIC_HYDRO_SPALL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sig0: Vec<f64>,
    pub eh: Vec<f64>,
    pub pc: Vec<f64>,
    pub fs: Vec<f64>,
    pub charl: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub spall: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub eps11: Vec<f64>,
    pub eps12: Vec<f64>,
    pub eps13: Vec<f64>,
    pub eps14: Vec<f64>,
    pub eps15: Vec<f64>,
    pub eps16: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub es9: Vec<f64>,
    pub es10: Vec<f64>,
    pub es11: Vec<f64>,
    pub es12: Vec<f64>,
    pub es13: Vec<f64>,
    pub es14: Vec<f64>,
    pub es15: Vec<f64>,
    pub es16: Vec<f64>,
}
impl MAT_ELASTIC_PLASTIC_HYDRO_SPALL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PLASTIC_HYDRO_SPALL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            charl: t.take("CHARL").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            eps11: t.take("EPS11").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12: t.take("EPS12").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13: t.take("EPS13").and_then(|c| c.into_float()).unwrap_or_default(),
            eps14: t.take("EPS14").and_then(|c| c.into_float()).unwrap_or_default(),
            eps15: t.take("EPS15").and_then(|c| c.into_float()).unwrap_or_default(),
            eps16: t.take("EPS16").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            es9: t.take("ES9").and_then(|c| c.into_float()).unwrap_or_default(),
            es10: t.take("ES10").and_then(|c| c.into_float()).unwrap_or_default(),
            es11: t.take("ES11").and_then(|c| c.into_float()).unwrap_or_default(),
            es12: t.take("ES12").and_then(|c| c.into_float()).unwrap_or_default(),
            es13: t.take("ES13").and_then(|c| c.into_float()).unwrap_or_default(),
            es14: t.take("ES14").and_then(|c| c.into_float()).unwrap_or_default(),
            es15: t.take("ES15").and_then(|c| c.into_float()).unwrap_or_default(),
            es16: t.take("ES16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PLASTIC_HYDRO_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub pc: Vec<f64>,
    pub fs: Vec<f64>,
    pub charl: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub eps11: Vec<f64>,
    pub eps12: Vec<f64>,
    pub eps13: Vec<f64>,
    pub eps14: Vec<f64>,
    pub eps15: Vec<f64>,
    pub eps16: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub es9: Vec<f64>,
    pub es10: Vec<f64>,
    pub es11: Vec<f64>,
    pub es12: Vec<f64>,
    pub es13: Vec<f64>,
    pub es14: Vec<f64>,
    pub es15: Vec<f64>,
    pub es16: Vec<f64>,
}
impl MAT_ELASTIC_PLASTIC_HYDRO_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PLASTIC_HYDRO_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            charl: t.take("CHARL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            eps11: t.take("EPS11").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12: t.take("EPS12").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13: t.take("EPS13").and_then(|c| c.into_float()).unwrap_or_default(),
            eps14: t.take("EPS14").and_then(|c| c.into_float()).unwrap_or_default(),
            eps15: t.take("EPS15").and_then(|c| c.into_float()).unwrap_or_default(),
            eps16: t.take("EPS16").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            es9: t.take("ES9").and_then(|c| c.into_float()).unwrap_or_default(),
            es10: t.take("ES10").and_then(|c| c.into_float()).unwrap_or_default(),
            es11: t.take("ES11").and_then(|c| c.into_float()).unwrap_or_default(),
            es12: t.take("ES12").and_then(|c| c.into_float()).unwrap_or_default(),
            es13: t.take("ES13").and_then(|c| c.into_float()).unwrap_or_default(),
            es14: t.take("ES14").and_then(|c| c.into_float()).unwrap_or_default(),
            es15: t.take("ES15").and_then(|c| c.into_float()).unwrap_or_default(),
            es16: t.take("ES16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_PLASTIC_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub pr1: Vec<f64>,
    pub pr2: Vec<f64>,
    pub pr3: Vec<f64>,
    pub pr4: Vec<f64>,
    pub pr5: Vec<f64>,
    pub pr6: Vec<f64>,
    pub pr7: Vec<f64>,
    pub pr8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub sigy1: Vec<f64>,
    pub sigy2: Vec<f64>,
    pub sigy3: Vec<f64>,
    pub sigy4: Vec<f64>,
    pub sigy5: Vec<f64>,
    pub sigy6: Vec<f64>,
    pub sigy7: Vec<f64>,
    pub sigy8: Vec<f64>,
    pub etan1: Vec<f64>,
    pub etan2: Vec<f64>,
    pub etan3: Vec<f64>,
    pub etan4: Vec<f64>,
    pub etan5: Vec<f64>,
    pub etan6: Vec<f64>,
    pub etan7: Vec<f64>,
    pub etan8: Vec<f64>,
}
impl MAT_ELASTIC_PLASTIC_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_PLASTIC_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr3: t.take("PR3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr4: t.take("PR4").and_then(|c| c.into_float()).unwrap_or_default(),
            pr5: t.take("PR5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr6: t.take("PR6").and_then(|c| c.into_float()).unwrap_or_default(),
            pr7: t.take("PR7").and_then(|c| c.into_float()).unwrap_or_default(),
            pr8: t.take("PR8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy1: t.take("SIGY1").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy2: t.take("SIGY2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy3: t.take("SIGY3").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy4: t.take("SIGY4").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy5: t.take("SIGY5").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy6: t.take("SIGY6").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy7: t.take("SIGY7").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy8: t.take("SIGY8").and_then(|c| c.into_float()).unwrap_or_default(),
            etan1: t.take("ETAN1").and_then(|c| c.into_float()).unwrap_or_default(),
            etan2: t.take("ETAN2").and_then(|c| c.into_float()).unwrap_or_default(),
            etan3: t.take("ETAN3").and_then(|c| c.into_float()).unwrap_or_default(),
            etan4: t.take("ETAN4").and_then(|c| c.into_float()).unwrap_or_default(),
            etan5: t.take("ETAN5").and_then(|c| c.into_float()).unwrap_or_default(),
            etan6: t.take("ETAN6").and_then(|c| c.into_float()).unwrap_or_default(),
            etan7: t.take("ETAN7").and_then(|c| c.into_float()).unwrap_or_default(),
            etan8: t.take("ETAN8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_SPRING_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub f0: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_ELASTIC_SPRING_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_SPRING_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_VISCOPLASTIC_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub fail: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lcsigy: Vec<f64>,
    pub lcr: Vec<f64>,
    pub lcx: Vec<f64>,
    pub lcalph: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lcp: Vec<i64>,
    pub tref: Vec<f64>,
    pub lcfail: Vec<f64>,
    pub nuhis: Vec<i64>,
    pub t1phas: Vec<f64>,
    pub t2phas: Vec<f64>,
    pub tol: Vec<f64>,
    pub fushi1: Vec<i64>,
    pub fushi2: Vec<i64>,
    pub fushi3: Vec<i64>,
    pub fushi4: Vec<i64>,
    pub fushi5: Vec<i64>,
    pub fushi6: Vec<i64>,
    pub fushi7: Vec<i64>,
    pub fushi8: Vec<i64>,
}
impl MAT_ELASTIC_VISCOPLASTIC_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_VISCOPLASTIC_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalph: t.take("LCALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nuhis: t.take("NUHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            t1phas: t.take("T1PHAS").and_then(|c| c.into_float()).unwrap_or_default(),
            t2phas: t.take("T2PHAS").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            fushi1: t.take("FUSHI1").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi2: t.take("FUSHI2").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi3: t.take("FUSHI3").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi4: t.take("FUSHI4").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi5: t.take("FUSHI5").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi6: t.take("FUSHI6").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi7: t.take("FUSHI7").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi8: t.take("FUSHI8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_WITH_VISCOSITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub v0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub lcid: Vec<f64>,
    pub pr1: Vec<f64>,
    pub pr2: Vec<f64>,
    pub pr3: Vec<f64>,
    pub pr4: Vec<f64>,
    pub pr5: Vec<f64>,
    pub pr6: Vec<f64>,
    pub pr7: Vec<f64>,
    pub pr8: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub v4: Vec<f64>,
    pub v5: Vec<f64>,
    pub v6: Vec<f64>,
    pub v7: Vec<f64>,
    pub v8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
}
impl MAT_ELASTIC_WITH_VISCOSITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_WITH_VISCOSITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr3: t.take("PR3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr4: t.take("PR4").and_then(|c| c.into_float()).unwrap_or_default(),
            pr5: t.take("PR5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr6: t.take("PR6").and_then(|c| c.into_float()).unwrap_or_default(),
            pr7: t.take("PR7").and_then(|c| c.into_float()).unwrap_or_default(),
            pr8: t.take("PR8").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            v4: t.take("V4").and_then(|c| c.into_float()).unwrap_or_default(),
            v5: t.take("V5").and_then(|c| c.into_float()).unwrap_or_default(),
            v6: t.take("V6").and_then(|c| c.into_float()).unwrap_or_default(),
            v7: t.take("V7").and_then(|c| c.into_float()).unwrap_or_default(),
            v8: t.take("V8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ELASTIC_WITH_VISCOSITY_CURVE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub v0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub lcid: Vec<f64>,
    pub pr_lc: Vec<i64>,
    pub ym_lc: Vec<i64>,
    pub a_lc: Vec<i64>,
    pub v_lc: Vec<i64>,
    pub v_log: Vec<f64>,
}
impl MAT_ELASTIC_WITH_VISCOSITY_CURVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ELASTIC_WITH_VISCOSITY_CURVE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            pr_lc: t.take("PR_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            ym_lc: t.take("YM_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            a_lc: t.take("A_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            v_lc: t.take("V_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            v_log: t.take("V_LOG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_EMMI {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub rgas: Vec<f64>,
    pub bvect: Vec<f64>,
    pub d0: Vec<f64>,
    pub qd: Vec<f64>,
    pub cv: Vec<f64>,
    pub adrag: Vec<f64>,
    pub bdrag: Vec<f64>,
    pub dmtheta: Vec<f64>,
    pub dmphi: Vec<f64>,
    pub dntheta: Vec<f64>,
    pub dnphi: Vec<f64>,
    pub theta0: Vec<f64>,
    pub thetam: Vec<f64>,
    pub beta0: Vec<f64>,
    pub btheta: Vec<f64>,
    pub dmr: Vec<f64>,
    pub dnuc1: Vec<f64>,
    pub dnuc2: Vec<f64>,
    pub dnuc3: Vec<f64>,
    pub dnuc4: Vec<f64>,
    pub dm1: Vec<f64>,
    pub dm2: Vec<f64>,
    pub dm3: Vec<f64>,
    pub dm4: Vec<f64>,
    pub dm5: Vec<f64>,
    pub qind: Vec<f64>,
    pub q2nd: Vec<f64>,
    pub q3nd: Vec<f64>,
    pub q4nd: Vec<f64>,
    pub calpha: Vec<f64>,
    pub ckappa: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2nd: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7nd: Vec<f64>,
    pub c8nd: Vec<f64>,
    pub c9nd: Vec<f64>,
    pub c10: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a_xx: Vec<f64>,
    pub a_yy: Vec<f64>,
    pub a_zz: Vec<f64>,
    pub a_xy: Vec<f64>,
    pub a_yz: Vec<f64>,
    pub a_xz: Vec<f64>,
    pub alphxx: Vec<f64>,
    pub alphyy: Vec<f64>,
    pub alphzz: Vec<f64>,
    pub alphxy: Vec<f64>,
    pub alphyz: Vec<f64>,
    pub alphxz: Vec<f64>,
    pub dkappa: Vec<f64>,
    pub phi0: Vec<f64>,
    pub phicr: Vec<f64>,
    pub dlbdag: Vec<f64>,
    pub factor: Vec<f64>,
    pub rswtch: Vec<f64>,
    pub dmgopt: Vec<f64>,
    pub delaso: Vec<f64>,
    pub dimplo: Vec<f64>,
    pub atol: Vec<f64>,
    pub rtol: Vec<f64>,
    pub dniter: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_5: Vec<f64>,
    pub f_6: Vec<f64>,
    pub f_7: Vec<f64>,
    pub f_8: Vec<f64>,
}
impl MAT_EMMI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_EMMI").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            rgas: t.take("RGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            bvect: t.take("BVECT").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            qd: t.take("QD").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            adrag: t.take("ADRAG").and_then(|c| c.into_float()).unwrap_or_default(),
            bdrag: t.take("BDRAG").and_then(|c| c.into_float()).unwrap_or_default(),
            dmtheta: t.take("DMTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dmphi: t.take("DMPHI").and_then(|c| c.into_float()).unwrap_or_default(),
            dntheta: t.take("DNTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dnphi: t.take("DNPHI").and_then(|c| c.into_float()).unwrap_or_default(),
            theta0: t.take("THETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            thetam: t.take("THETAM").and_then(|c| c.into_float()).unwrap_or_default(),
            beta0: t.take("BETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            btheta: t.take("BTHETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dmr: t.take("DMR").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc1: t.take("DNUC1").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc2: t.take("DNUC2").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc3: t.take("DNUC3").and_then(|c| c.into_float()).unwrap_or_default(),
            dnuc4: t.take("DNUC4").and_then(|c| c.into_float()).unwrap_or_default(),
            dm1: t.take("DM1").and_then(|c| c.into_float()).unwrap_or_default(),
            dm2: t.take("DM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dm3: t.take("DM3").and_then(|c| c.into_float()).unwrap_or_default(),
            dm4: t.take("DM4").and_then(|c| c.into_float()).unwrap_or_default(),
            dm5: t.take("DM5").and_then(|c| c.into_float()).unwrap_or_default(),
            qind: t.take("QIND").and_then(|c| c.into_float()).unwrap_or_default(),
            q2nd: t.take("Q2ND").and_then(|c| c.into_float()).unwrap_or_default(),
            q3nd: t.take("Q3ND").and_then(|c| c.into_float()).unwrap_or_default(),
            q4nd: t.take("Q4ND").and_then(|c| c.into_float()).unwrap_or_default(),
            calpha: t.take("CALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ckappa: t.take("CKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2nd: t.take("C2ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7nd: t.take("C7ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c8nd: t.take("C8ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c9nd: t.take("C9ND").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xx: t.take("A_XX").and_then(|c| c.into_float()).unwrap_or_default(),
            a_yy: t.take("A_YY").and_then(|c| c.into_float()).unwrap_or_default(),
            a_zz: t.take("A_ZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xy: t.take("A_XY").and_then(|c| c.into_float()).unwrap_or_default(),
            a_yz: t.take("A_YZ").and_then(|c| c.into_float()).unwrap_or_default(),
            a_xz: t.take("A_XZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxx: t.take("ALPHXX").and_then(|c| c.into_float()).unwrap_or_default(),
            alphyy: t.take("ALPHYY").and_then(|c| c.into_float()).unwrap_or_default(),
            alphzz: t.take("ALPHZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxy: t.take("ALPHXY").and_then(|c| c.into_float()).unwrap_or_default(),
            alphyz: t.take("ALPHYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            alphxz: t.take("ALPHXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            dkappa: t.take("DKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            phi0: t.take("PHI0").and_then(|c| c.into_float()).unwrap_or_default(),
            phicr: t.take("PHICR").and_then(|c| c.into_float()).unwrap_or_default(),
            dlbdag: t.take("DLBDAG").and_then(|c| c.into_float()).unwrap_or_default(),
            factor: t.take("FACTOR").and_then(|c| c.into_float()).unwrap_or_default(),
            rswtch: t.take("RSWTCH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmgopt: t.take("DMGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            delaso: t.take("DELASO").and_then(|c| c.into_float()).unwrap_or_default(),
            dimplo: t.take("DIMPLO").and_then(|c| c.into_float()).unwrap_or_default(),
            atol: t.take("ATOL").and_then(|c| c.into_float()).unwrap_or_default(),
            rtol: t.take("RTOL").and_then(|c| c.into_float()).unwrap_or_default(),
            dniter: t.take("DNITER").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_float()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_float()).unwrap_or_default(),
            f_7: t.take("-_7").and_then(|c| c.into_float()).unwrap_or_default(),
            f_8: t.take("-_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ENHANCED_COMPOSITE_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f_2way: Vec<f64>,
    pub ti: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub dfailm: Vec<f64>,
    pub dfails: Vec<f64>,
    pub tfail: Vec<f64>,
    pub alph: Vec<f64>,
    pub soft: Vec<f64>,
    pub fbrt: Vec<f64>,
    pub ycfac: Vec<f64>,
    pub dfailt: Vec<f64>,
    pub dfailc: Vec<f64>,
    pub efs: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub crit: Vec<f64>,
    pub beta: Vec<f64>,
    pub pel: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub soft2: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub ncyred: Vec<f64>,
    pub softg: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub dt: Vec<f64>,
}
impl MAT_ENHANCED_COMPOSITE_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ENHANCED_COMPOSITE_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("(EC)").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("(PRCA)").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("(PRCB)").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("(KF)").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2way: t.take("2WAY").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("TI").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailm: t.take("DFAILM").and_then(|c| c.into_float()).unwrap_or_default(),
            dfails: t.take("DFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbrt: t.take("FBRT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycfac: t.take("YCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailt: t.take("DFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailc: t.take("DFAILC").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            crit: t.take("CRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pel: t.take("PEL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            soft2: t.take("SOFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            ncyred: t.take("NCYRED").and_then(|c| c.into_float()).unwrap_or_default(),
            softg: t.take("SOFTG").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ENHANCED_COMPOSITE_DAMAGE_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub kf: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f_2way: Vec<f64>,
    pub ti: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub dfailm: Vec<f64>,
    pub dfails: Vec<f64>,
    pub tfail: Vec<f64>,
    pub alph: Vec<f64>,
    pub soft: Vec<f64>,
    pub fbrt: Vec<f64>,
    pub ycfac: Vec<f64>,
    pub dfailt: Vec<f64>,
    pub dfailc: Vec<f64>,
    pub efs: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub crit: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_ENHANCED_COMPOSITE_DAMAGE_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ENHANCED_COMPOSITE_DAMAGE_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("(EC)").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("(PRCA)").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("(PRCB)").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            kf: t.take("(KF)").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2way: t.take("2WAY").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("TI").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailm: t.take("DFAILM").and_then(|c| c.into_float()).unwrap_or_default(),
            dfails: t.take("DFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            alph: t.take("ALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fbrt: t.take("FBRT").and_then(|c| c.into_float()).unwrap_or_default(),
            ycfac: t.take("YCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailt: t.take("DFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfailc: t.take("DFAILC").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            crit: t.take("CRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_EXTENDED_3_PARAMETER_BARLAT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lch00: Vec<i64>,
    pub lch45: Vec<i64>,
    pub lch90: Vec<i64>,
    pub lchbi: Vec<i64>,
    pub lchsh: Vec<i64>,
    pub lcr00: Vec<i64>,
    pub lcr45: Vec<i64>,
    pub lcr90: Vec<i64>,
    pub lcrbi: Vec<i64>,
    pub lcrsh: Vec<i64>,
    pub m: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_EXTENDED_3_PARAMETER_BARLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_EXTENDED_3-PARAMETER_BARLAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lch00: t.take("LCH00").and_then(|c| c.into_int()).unwrap_or_default(),
            lch45: t.take("LCH45").and_then(|c| c.into_int()).unwrap_or_default(),
            lch90: t.take("LCH90").and_then(|c| c.into_int()).unwrap_or_default(),
            lchbi: t.take("LCHBI").and_then(|c| c.into_int()).unwrap_or_default(),
            lchsh: t.take("LCHSH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr00: t.take("LCR00").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr45: t.take("LCR45").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr90: t.take("LCR90").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrbi: t.take("LCRBI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrsh: t.take("LCRSH").and_then(|c| c.into_int()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub f: Vec<f64>,
    pub prba: Vec<f64>,
    pub prab: Vec<f64>,
    pub f_2: Vec<f64>,
    pub gab: Vec<f64>,
    pub f_3: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub cse: Vec<f64>,
    pub el: Vec<f64>,
    pub prl: Vec<f64>,
    pub lratio: Vec<f64>,
    pub damp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub flc: Vec<f64>,
    pub fac: Vec<f64>,
    pub ela: Vec<f64>,
    pub lnrc: Vec<f64>,
    pub form: Vec<i64>,
    pub fvopt: Vec<i64>,
    pub tsrfac: Vec<f64>,
    pub unused: Vec<i64>,
    pub rgbrth: Vec<f64>,
    pub a0ref: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub x0: Vec<f64>,
    pub x1: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_2_3: Vec<f64>,
    pub f_3_2: Vec<f64>,
    pub beta: Vec<f64>,
    pub isrefg: Vec<i64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcua: Vec<i64>,
    pub lcub: Vec<i64>,
    pub lcuab: Vec<i64>,
    pub rl: Vec<f64>,
    pub lcaa: Vec<i64>,
    pub lcbb: Vec<i64>,
    pub h: Vec<f64>,
    pub dt: Vec<i64>,
    pub f40: Vec<i64>,
    pub ecoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub tcoat: Vec<f64>,
}
impl MAT_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prab: t.take("PRAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            prl: t.take("PRL").and_then(|c| c.into_float()).unwrap_or_default(),
            lratio: t.take("LRATIO").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            flc: t.take("FLC").and_then(|c| c.into_float()).unwrap_or_default(),
            fac: t.take("FAC").and_then(|c| c.into_float()).unwrap_or_default(),
            ela: t.take("ELA").and_then(|c| c.into_float()).unwrap_or_default(),
            lnrc: t.take("LNRC").and_then(|c| c.into_float()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            fvopt: t.take("FVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tsrfac: t.take("TSRFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            rgbrth: t.take("RGBRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            a0ref: t.take("A0REF").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            isrefg: t.take("ISREFG").and_then(|c| c.into_int()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcua: t.take("LCUA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcub: t.take("LCUB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuab: t.take("LCUAB").and_then(|c| c.into_int()).unwrap_or_default(),
            rl: t.take("RL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaa: t.take("LCAA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbb: t.take("LCBB").and_then(|c| c.into_int()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FABRIC_MAP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pxx: Vec<f64>,
    pub pyy: Vec<f64>,
    pub sxy: Vec<f64>,
    pub damp: Vec<f64>,
    pub th: Vec<f64>,
    pub fvopt: Vec<f64>,
    pub x0: Vec<f64>,
    pub x1: Vec<f64>,
    pub flc_x2: Vec<f64>,
    pub fac_x3: Vec<f64>,
    pub isrefg: Vec<f64>,
    pub cse: Vec<f64>,
    pub srfac: Vec<i64>,
    pub bulkc: Vec<f64>,
    pub jacc: Vec<f64>,
    pub fxx: Vec<i64>,
    pub fyy: Vec<i64>,
    pub dt: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ecoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub tcoat: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_FABRIC_MAP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FABRIC_MAP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pxx: t.take("PXX").and_then(|c| c.into_float()).unwrap_or_default(),
            pyy: t.take("PYY").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            th: t.take("TH").and_then(|c| c.into_float()).unwrap_or_default(),
            fvopt: t.take("FVOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            flc_x2: t.take("FLC/X2").and_then(|c| c.into_float()).unwrap_or_default(),
            fac_x3: t.take("FAC/X3").and_then(|c| c.into_float()).unwrap_or_default(),
            isrefg: t.take("ISREFG").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            srfac: t.take("SRFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            bulkc: t.take("BULKC").and_then(|c| c.into_float()).unwrap_or_default(),
            jacc: t.take("JACC").and_then(|c| c.into_float()).unwrap_or_default(),
            fxx: t.take("FXX").and_then(|c| c.into_int()).unwrap_or_default(),
            fyy: t.take("FYY").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FHWA_SOIL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub spgrav: Vec<f64>,
    pub rhowat: Vec<f64>,
    pub vn: Vec<f64>,
    pub gammar: Vec<f64>,
    pub intrmx: Vec<i64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub phimax: Vec<f64>,
    pub ahyp: Vec<f64>,
    pub coh: Vec<f64>,
    pub eccen: Vec<f64>,
    pub an: Vec<f64>,
    pub et: Vec<f64>,
    pub mcont: Vec<f64>,
    pub pwd1: Vec<f64>,
    pub pwksk: Vec<f64>,
    pub pwd2: Vec<f64>,
    pub phires: Vec<f64>,
    pub dint: Vec<f64>,
    pub vdfm: Vec<f64>,
    pub damlev: Vec<f64>,
    pub epsmax: Vec<f64>,
}
impl MAT_FHWA_SOIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FHWA_SOIL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            spgrav: t.take("SPGRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            rhowat: t.take("RHOWAT").and_then(|c| c.into_float()).unwrap_or_default(),
            vn: t.take("VN").and_then(|c| c.into_float()).unwrap_or_default(),
            gammar: t.take("GAMMAR").and_then(|c| c.into_float()).unwrap_or_default(),
            intrmx: t.take("INTRMX").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            phimax: t.take("PHIMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ahyp: t.take("AHYP").and_then(|c| c.into_float()).unwrap_or_default(),
            coh: t.take("COH").and_then(|c| c.into_float()).unwrap_or_default(),
            eccen: t.take("ECCEN").and_then(|c| c.into_float()).unwrap_or_default(),
            an: t.take("AN").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            mcont: t.take("MCONT").and_then(|c| c.into_float()).unwrap_or_default(),
            pwd1: t.take("PWD1").and_then(|c| c.into_float()).unwrap_or_default(),
            pwksk: t.take("PWKSK").and_then(|c| c.into_float()).unwrap_or_default(),
            pwd2: t.take("PWD2").and_then(|c| c.into_float()).unwrap_or_default(),
            phires: t.take("PHIRES").and_then(|c| c.into_float()).unwrap_or_default(),
            dint: t.take("DINT").and_then(|c| c.into_float()).unwrap_or_default(),
            vdfm: t.take("VDFM").and_then(|c| c.into_float()).unwrap_or_default(),
            damlev: t.take("DAMLEV").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FHWA_SOIL_NEBRASKA {
    pub mid: Vec<i64>,
    pub fctim: Vec<f64>,
    pub fctmas: Vec<f64>,
    pub fctlen: Vec<f64>,
}
impl MAT_FHWA_SOIL_NEBRASKA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FHWA_SOIL_NEBRASKA").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            fctim: t.take("FCTIM").and_then(|c| c.into_float()).unwrap_or_default(),
            fctmas: t.take("FCTMAS").and_then(|c| c.into_float()).unwrap_or_default(),
            fctlen: t.take("FCTLEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FINITE_ELASTIC_STRAIN_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_FINITE_ELASTIC_STRAIN_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FINITE_ELASTIC_STRAIN_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FLD_3_PARAMETER_BARLAT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fldcid: Vec<i64>,
    pub rn: Vec<f64>,
    pub rt: Vec<f64>,
    pub fldsafe: Vec<f64>,
    pub fldnipf: Vec<i64>,
    pub f0: Vec<i64>,
    pub f10: Vec<i64>,
    pub f20: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_FLD_3_PARAMETER_BARLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FLD_3-PARAMETER_BARLAT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fldcid: t.take("FLDCID").and_then(|c| c.into_int()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
            rt: t.take("RT").and_then(|c| c.into_float()).unwrap_or_default(),
            fldsafe: t.take("FLDSAFE").and_then(|c| c.into_float()).unwrap_or_default(),
            fldnipf: t.take("FLDNIPF").and_then(|c| c.into_int()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_int()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FLD_TRANSVERSELY_ANISOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub lcfld: Vec<i64>,
}
impl MAT_FLD_TRANSVERSELY_ANISOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FLD_TRANSVERSELY_ANISOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfld: t.take("LCFLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FORCE_LIMITED {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub df: Vec<f64>,
    pub iaflc: Vec<i64>,
    pub ytflag: Vec<f64>,
    pub asoft: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub m6: Vec<f64>,
    pub m7: Vec<f64>,
    pub m8: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub lc5: Vec<i64>,
    pub lc6: Vec<i64>,
    pub lc7: Vec<i64>,
    pub lc8: Vec<i64>,
    pub lps1: Vec<i64>,
    pub sfs1: Vec<f64>,
    pub lps2: Vec<i64>,
    pub sfs2: Vec<f64>,
    pub yms1: Vec<f64>,
    pub yms2: Vec<f64>,
    pub lpt1: Vec<i64>,
    pub sft1: Vec<f64>,
    pub lpt2: Vec<i64>,
    pub sft2: Vec<f64>,
    pub ymt1: Vec<f64>,
    pub ymt2: Vec<f64>,
    pub lpr: Vec<i64>,
    pub sfr: Vec<f64>,
    pub ymr: Vec<f64>,
}
impl MAT_FORCE_LIMITED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FORCE_LIMITED").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            iaflc: t.take("IAFLC").and_then(|c| c.into_int()).unwrap_or_default(),
            ytflag: t.take("YTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            asoft: t.take("ASOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            m6: t.take("M6").and_then(|c| c.into_float()).unwrap_or_default(),
            m7: t.take("M7").and_then(|c| c.into_float()).unwrap_or_default(),
            m8: t.take("M8").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            lc5: t.take("LC5").and_then(|c| c.into_int()).unwrap_or_default(),
            lc6: t.take("LC6").and_then(|c| c.into_int()).unwrap_or_default(),
            lc7: t.take("LC7").and_then(|c| c.into_int()).unwrap_or_default(),
            lc8: t.take("LC8").and_then(|c| c.into_int()).unwrap_or_default(),
            lps1: t.take("LPS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs1: t.take("SFS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lps2: t.take("LPS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs2: t.take("SFS2").and_then(|c| c.into_float()).unwrap_or_default(),
            yms1: t.take("YMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            yms2: t.take("YMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt1: t.take("LPT1").and_then(|c| c.into_int()).unwrap_or_default(),
            sft1: t.take("SFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt2: t.take("LPT2").and_then(|c| c.into_int()).unwrap_or_default(),
            sft2: t.take("SFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt1: t.take("YMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt2: t.take("YMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpr: t.take("LPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymr: t.take("YMR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FRAZER_NASH_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub c100: Vec<f64>,
    pub c200: Vec<f64>,
    pub c300: Vec<f64>,
    pub c400: Vec<f64>,
    pub c110: Vec<f64>,
    pub c210: Vec<f64>,
    pub c010: Vec<f64>,
    pub c020: Vec<f64>,
    pub exit: Vec<f64>,
    pub emax: Vec<f64>,
    pub emin: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_FRAZER_NASH_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FRAZER-NASH_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c100: t.take("C100").and_then(|c| c.into_float()).unwrap_or_default(),
            c200: t.take("C200").and_then(|c| c.into_float()).unwrap_or_default(),
            c300: t.take("C300").and_then(|c| c.into_float()).unwrap_or_default(),
            c400: t.take("C400").and_then(|c| c.into_float()).unwrap_or_default(),
            c110: t.take("C110").and_then(|c| c.into_float()).unwrap_or_default(),
            c210: t.take("C210").and_then(|c| c.into_float()).unwrap_or_default(),
            c010: t.take("C010").and_then(|c| c.into_float()).unwrap_or_default(),
            c020: t.take("C020").and_then(|c| c.into_float()).unwrap_or_default(),
            exit: t.take("EXIT").and_then(|c| c.into_float()).unwrap_or_default(),
            emax: t.take("EMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            emin: t.take("EMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FRAZER_NASH_RUBBER_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub c100: Vec<f64>,
    pub c200: Vec<f64>,
    pub c300: Vec<f64>,
    pub c400: Vec<f64>,
    pub c110: Vec<f64>,
    pub c210: Vec<f64>,
    pub c010: Vec<f64>,
    pub c020: Vec<f64>,
    pub exit: Vec<f64>,
    pub emax: Vec<f64>,
    pub emin: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_FRAZER_NASH_RUBBER_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FRAZER_NASH_RUBBER_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c100: t.take("C100").and_then(|c| c.into_float()).unwrap_or_default(),
            c200: t.take("C200").and_then(|c| c.into_float()).unwrap_or_default(),
            c300: t.take("C300").and_then(|c| c.into_float()).unwrap_or_default(),
            c400: t.take("C400").and_then(|c| c.into_float()).unwrap_or_default(),
            c110: t.take("C110").and_then(|c| c.into_float()).unwrap_or_default(),
            c210: t.take("C210").and_then(|c| c.into_float()).unwrap_or_default(),
            c010: t.take("C010").and_then(|c| c.into_float()).unwrap_or_default(),
            c020: t.take("C020").and_then(|c| c.into_float()).unwrap_or_default(),
            exit: t.take("EXIT").and_then(|c| c.into_float()).unwrap_or_default(),
            emax: t.take("EMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            emin: t.take("EMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FU_CHANG_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub d0: Vec<f64>,
    pub n0: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub aij: Vec<f64>,
    pub sij: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_FU_CHANG_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FU_CHANG_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            n0: t.take("N0").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            aij: t.take("AIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sij: t.take("SIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FU_CHANG_FOAM_DAMAGE_DECAY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub betat: Vec<f64>,
    pub betac: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_FU_CHANG_FOAM_DAMAGE_DECAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FU_CHANG_FOAM_DAMAGE_DECAY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            betat: t.take("BETAT").and_then(|c| c.into_float()).unwrap_or_default(),
            betac: t.take("BETAC").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_FU_CHANG_FOAM_LOG_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub kcon: Vec<f64>,
    pub tc: Vec<f64>,
    pub fail: Vec<f64>,
    pub damp: Vec<f64>,
    pub tbid: Vec<i64>,
    pub bvflag: Vec<f64>,
    pub sflag: Vec<f64>,
    pub rflag: Vec<f64>,
    pub tflag: Vec<f64>,
    pub pvid: Vec<i64>,
    pub sraf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub hu: Vec<f64>,
    pub d0: Vec<f64>,
    pub n0: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub c0: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub aij: Vec<f64>,
    pub sij: Vec<f64>,
    pub minr: Vec<f64>,
    pub maxr: Vec<f64>,
    pub shape: Vec<f64>,
    pub expon: Vec<f64>,
    pub riuld: Vec<f64>,
}
impl MAT_FU_CHANG_FOAM_LOG_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_FU_CHANG_FOAM_LOG_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tbid: t.take("TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            sflag: t.take("SFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            tflag: t.take("TFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pvid: t.take("PVID").and_then(|c| c.into_int()).unwrap_or_default(),
            sraf: t.take("SRAF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            n0: t.take("N0").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            c0: t.take("C0").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            aij: t.take("AIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sij: t.take("SIJ").and_then(|c| c.into_float()).unwrap_or_default(),
            minr: t.take("MINR").and_then(|c| c.into_float()).unwrap_or_default(),
            maxr: t.take("MAXR").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
            riuld: t.take("RIULD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GAS_MIXTURE {
    pub mid: Vec<i64>,
    pub iadiab: Vec<i64>,
    pub runiv: Vec<f64>,
    pub cvmass1: Vec<f64>,
    pub cvmass2: Vec<f64>,
    pub cvmass3: Vec<f64>,
    pub cvmass4: Vec<f64>,
    pub cvmass5: Vec<f64>,
    pub cvmass6: Vec<f64>,
    pub cvmass7: Vec<f64>,
    pub cvmass8: Vec<f64>,
    pub cpmass1: Vec<f64>,
    pub cpmass2: Vec<f64>,
    pub cpmass3: Vec<f64>,
    pub cpmass4: Vec<f64>,
    pub cpmass5: Vec<f64>,
    pub cpmass6: Vec<f64>,
    pub cpmass7: Vec<f64>,
    pub cpmass8: Vec<f64>,
    pub molwt1: Vec<f64>,
    pub molwt2: Vec<f64>,
    pub molwt3: Vec<f64>,
    pub molwt4: Vec<f64>,
    pub molwt5: Vec<f64>,
    pub molwt6: Vec<f64>,
    pub molwt7: Vec<f64>,
    pub molwt8: Vec<f64>,
    pub cpmole1: Vec<f64>,
    pub cpmole2: Vec<f64>,
    pub cpmole3: Vec<f64>,
    pub cpmole4: Vec<f64>,
    pub cpmole5: Vec<f64>,
    pub cpmole6: Vec<f64>,
    pub cpmole7: Vec<f64>,
    pub cpmole8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
}
impl MAT_GAS_MIXTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GAS_MIXTURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            iadiab: t.take("IADIAB").and_then(|c| c.into_int()).unwrap_or_default(),
            runiv: t.take("RUNIV").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass1: t.take("CVmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass2: t.take("CVmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass3: t.take("CVmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass4: t.take("CVmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass5: t.take("CVmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass6: t.take("CVmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass7: t.take("CVmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cvmass8: t.take("CVmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass1: t.take("CPmass1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass2: t.take("CPmass2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass3: t.take("CPmass3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass4: t.take("CPmass4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass5: t.take("CPmass5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass6: t.take("CPmass6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass7: t.take("CPmass7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmass8: t.take("CPmass8").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt1: t.take("MOLWT1").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt2: t.take("MOLWT2").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt3: t.take("MOLWT3").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt4: t.take("MOLWT4").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt5: t.take("MOLWT5").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt6: t.take("MOLWT6").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt7: t.take("MOLWT7").and_then(|c| c.into_float()).unwrap_or_default(),
            molwt8: t.take("MOLWT8").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole1: t.take("CPmole1").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole2: t.take("CPmole2").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole3: t.take("CPmole3").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole4: t.take("CPmole4").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole5: t.take("CPmole5").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole6: t.take("CPmole6").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole7: t.take("CPmole7").and_then(|c| c.into_float()).unwrap_or_default(),
            cpmole8: t.take("CPmole8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERALIZED_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub n: Vec<i64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub mix: Vec<i64>,
    pub mixr: Vec<i64>,
    pub tastrt: Vec<f64>,
    pub tend: Vec<f64>,
    pub cte: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub epsini: Vec<i64>,
    pub dtemp: Vec<f64>,
    pub xastr: Vec<f64>,
    pub xaend: Vec<f64>,
    pub xa1pa1: Vec<i64>,
    pub xa1pa2: Vec<i64>,
    pub xa1pa3: Vec<i64>,
    pub xafpa: Vec<f64>,
    pub cteann: Vec<f64>,
    pub ptlaw: Vec<i64>,
    pub ptstr: Vec<i64>,
    pub ptend: Vec<i64>,
    pub ptx1: Vec<i64>,
    pub ptx2: Vec<i64>,
    pub ptx3: Vec<i64>,
    pub ptx4: Vec<i64>,
    pub ptx5: Vec<i64>,
    pub pttab1: Vec<i64>,
    pub pttab2: Vec<i64>,
    pub pttab3: Vec<i64>,
    pub pttab4: Vec<i64>,
    pub pttab5: Vec<i64>,
    pub pttab6: Vec<i64>,
    pub pttab7: Vec<i64>,
    pub pteps: Vec<i64>,
    pub ptrip: Vec<i64>,
    pub ptlat: Vec<i64>,
    pub postv: Vec<i64>,
    pub nushis: Vec<i64>,
    pub grai: Vec<f64>,
    pub t1phas: Vec<i64>,
    pub t2phas: Vec<i64>,
    pub fushi1: Vec<i64>,
    pub fushi2: Vec<i64>,
    pub fushi3: Vec<i64>,
    pub fushi4: Vec<i64>,
    pub fushi5: Vec<i64>,
    pub fushi6: Vec<i64>,
    pub fushi7: Vec<i64>,
    pub fushi8: Vec<i64>,
    pub sigy1: Vec<i64>,
    pub sigy2: Vec<i64>,
    pub sigy3: Vec<i64>,
    pub sigy4: Vec<i64>,
    pub sigy5: Vec<i64>,
    pub sigy6: Vec<i64>,
    pub sigy7: Vec<i64>,
    pub sigy8: Vec<i64>,
}
impl MAT_GENERALIZED_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERALIZED_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            mix: t.take("MIX").and_then(|c| c.into_int()).unwrap_or_default(),
            mixr: t.take("MIXR").and_then(|c| c.into_int()).unwrap_or_default(),
            tastrt: t.take("TASTRT").and_then(|c| c.into_float()).unwrap_or_default(),
            tend: t.take("TEND").and_then(|c| c.into_float()).unwrap_or_default(),
            cte: t.take("CTE").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            epsini: t.take("EPSINI").and_then(|c| c.into_int()).unwrap_or_default(),
            dtemp: t.take("DTEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            xastr: t.take("XASTR").and_then(|c| c.into_float()).unwrap_or_default(),
            xaend: t.take("XAEND").and_then(|c| c.into_float()).unwrap_or_default(),
            xa1pa1: t.take("XA1PA1").and_then(|c| c.into_int()).unwrap_or_default(),
            xa1pa2: t.take("XA1PA2").and_then(|c| c.into_int()).unwrap_or_default(),
            xa1pa3: t.take("XA1PA3").and_then(|c| c.into_int()).unwrap_or_default(),
            xafpa: t.take("XAFPA").and_then(|c| c.into_float()).unwrap_or_default(),
            cteann: t.take("CTEANN").and_then(|c| c.into_float()).unwrap_or_default(),
            ptlaw: t.take("PTLAW").and_then(|c| c.into_int()).unwrap_or_default(),
            ptstr: t.take("PTSTR").and_then(|c| c.into_int()).unwrap_or_default(),
            ptend: t.take("PTEND").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx1: t.take("PTX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx2: t.take("PTX2").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx3: t.take("PTX3").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx4: t.take("PTX4").and_then(|c| c.into_int()).unwrap_or_default(),
            ptx5: t.take("PTX5").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab1: t.take("PTTAB1").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab2: t.take("PTTAB2").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab3: t.take("PTTAB3").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab4: t.take("PTTAB4").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab5: t.take("PTTAB5").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab6: t.take("PTTAB6").and_then(|c| c.into_int()).unwrap_or_default(),
            pttab7: t.take("PTTAB7").and_then(|c| c.into_int()).unwrap_or_default(),
            pteps: t.take("PTEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            ptrip: t.take("PTRIP").and_then(|c| c.into_int()).unwrap_or_default(),
            ptlat: t.take("PTLAT").and_then(|c| c.into_int()).unwrap_or_default(),
            postv: t.take("POSTV").and_then(|c| c.into_int()).unwrap_or_default(),
            nushis: t.take("NUSHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            grai: t.take("GRAI").and_then(|c| c.into_float()).unwrap_or_default(),
            t1phas: t.take("T1PHAS").and_then(|c| c.into_int()).unwrap_or_default(),
            t2phas: t.take("T2PHAS").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi1: t.take("FUSHI1").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi2: t.take("FUSHI2").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi3: t.take("FUSHI3").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi4: t.take("FUSHI4").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi5: t.take("FUSHI5").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi6: t.take("FUSHI6").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi7: t.take("FUSHI7").and_then(|c| c.into_int()).unwrap_or_default(),
            fushi8: t.take("FUSHI8").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy1: t.take("SIGY1").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy2: t.take("SIGY2").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy3: t.take("SIGY3").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy4: t.take("SIGY4").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy5: t.take("SIGY5").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy6: t.take("SIGY6").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy7: t.take("SIGY7").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy8: t.take("SIGY8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_JOINT_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tr: Vec<i64>,
    pub ts: Vec<i64>,
    pub tt: Vec<i64>,
    pub rr: Vec<i64>,
    pub rs: Vec<i64>,
    pub rt: Vec<i64>,
    pub rpst: Vec<f64>,
    pub rpsr: Vec<f64>,
}
impl MAT_GENERAL_JOINT_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_JOINT_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_int()).unwrap_or_default(),
            ts: t.take("TS").and_then(|c| c.into_int()).unwrap_or_default(),
            tt: t.take("TT").and_then(|c| c.into_int()).unwrap_or_default(),
            rr: t.take("RR").and_then(|c| c.into_int()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_int()).unwrap_or_default(),
            rt: t.take("RT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpst: t.take("RPST").and_then(|c| c.into_float()).unwrap_or_default(),
            rpsr: t.take("RPSR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_NONLINEAR_1DOF_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub iunld: Vec<i64>,
    pub offset: Vec<f64>,
    pub dampf: Vec<f64>,
    pub lcidt: Vec<i64>,
    pub lcidtu: Vec<i64>,
    pub lcidtd: Vec<i64>,
    pub lcidte: Vec<i64>,
    pub utfail: Vec<f64>,
    pub ucfail: Vec<f64>,
    pub iu: Vec<f64>,
}
impl MAT_GENERAL_NONLINEAR_1DOF_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_NONLINEAR_1DOF_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            iunld: t.take("IUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtu: t.take("LCIDTU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtd: t.take("LCIDTD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidte: t.take("LCIDTE").and_then(|c| c.into_int()).unwrap_or_default(),
            utfail: t.take("UTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfail: t.take("UCFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            iu: t.take("IU").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_NONLINEAR_6DOF_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub kt: Vec<f64>,
    pub kr: Vec<f64>,
    pub iunld: Vec<i64>,
    pub offset: Vec<f64>,
    pub dampf: Vec<f64>,
    pub iflag: Vec<i64>,
    pub lcidtr: Vec<i64>,
    pub lcidts: Vec<i64>,
    pub lcidtt: Vec<i64>,
    pub lcidrr: Vec<i64>,
    pub lcidrs: Vec<i64>,
    pub lcidrt: Vec<i64>,
    pub lcidtur: Vec<i64>,
    pub lcidtus: Vec<i64>,
    pub lcidtut: Vec<i64>,
    pub lcidrur: Vec<i64>,
    pub lcidrus: Vec<i64>,
    pub lcidrut: Vec<i64>,
    pub lcidtdr: Vec<i64>,
    pub lcidtds: Vec<i64>,
    pub lcidtdt: Vec<i64>,
    pub lcidrdr: Vec<i64>,
    pub lcidrds: Vec<i64>,
    pub lcidrdt: Vec<i64>,
    pub lcidter: Vec<i64>,
    pub lcidtes: Vec<i64>,
    pub lcidtet: Vec<i64>,
    pub lcidrer: Vec<i64>,
    pub lcidres: Vec<i64>,
    pub lcidret: Vec<i64>,
    pub utfailr: Vec<f64>,
    pub utfails: Vec<f64>,
    pub utfailt: Vec<f64>,
    pub wtfailr: Vec<f64>,
    pub wtfails: Vec<f64>,
    pub wtfailt: Vec<f64>,
    pub fcrit: Vec<f64>,
    pub ucfailr: Vec<f64>,
    pub ucfails: Vec<f64>,
    pub ucfailt: Vec<f64>,
    pub wcfailr: Vec<f64>,
    pub wcfails: Vec<f64>,
    pub wcfailt: Vec<f64>,
    pub iur: Vec<f64>,
    pub ius: Vec<f64>,
    pub iut: Vec<f64>,
    pub iwr: Vec<f64>,
    pub iws: Vec<f64>,
    pub iwt: Vec<f64>,
}
impl MAT_GENERAL_NONLINEAR_6DOF_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_NONLINEAR_6DOF_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            kt: t.take("KT").and_then(|c| c.into_float()).unwrap_or_default(),
            kr: t.take("KR").and_then(|c| c.into_float()).unwrap_or_default(),
            iunld: t.take("IUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtr: t.take("LCIDTR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidts: t.take("LCIDTS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtt: t.take("LCIDTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrs: t.take("LCIDRS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrt: t.take("LCIDRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtur: t.take("LCIDTUR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtus: t.take("LCIDTUS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtut: t.take("LCIDTUT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrur: t.take("LCIDRUR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrus: t.take("LCIDRUS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrut: t.take("LCIDRUT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdr: t.take("LCIDTDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtds: t.take("LCIDTDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdt: t.take("LCIDTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdr: t.take("LCIDRDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrds: t.take("LCIDRDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdt: t.take("LCIDRDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidter: t.take("LCIDTER").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtes: t.take("LCIDTES").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtet: t.take("LCIDTET").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrer: t.take("LCIDRER").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidres: t.take("LCIDRES").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidret: t.take("LCIDRET").and_then(|c| c.into_int()).unwrap_or_default(),
            utfailr: t.take("UTFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            utfails: t.take("UTFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            utfailt: t.take("UTFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfailr: t.take("WTFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfails: t.take("WTFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            wtfailt: t.take("WTFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            fcrit: t.take("FCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfailr: t.take("UCFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfails: t.take("UCFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ucfailt: t.take("UCFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfailr: t.take("WCFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfails: t.take("WCFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            wcfailt: t.take("WCFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            iur: t.take("IUR").and_then(|c| c.into_float()).unwrap_or_default(),
            ius: t.take("IUS").and_then(|c| c.into_float()).unwrap_or_default(),
            iut: t.take("IUT").and_then(|c| c.into_float()).unwrap_or_default(),
            iwr: t.take("IWR").and_then(|c| c.into_float()).unwrap_or_default(),
            iws: t.take("IWS").and_then(|c| c.into_float()).unwrap_or_default(),
            iwt: t.take("IWT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_SPRING_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub dospot: Vec<i64>,
    pub dof: Vec<i64>,
    pub type_: Vec<i64>,
    pub k: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_GENERAL_SPRING_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_SPRING_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            dospot: t.take("DOSPOT").and_then(|c| c.into_int()).unwrap_or_default(),
            dof: t.take("DOF").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_GENERAL_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GENERAL_VISCOELASTIC_MOISTURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub mo: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub mst: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_GENERAL_VISCOELASTIC_MOISTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GENERAL_VISCOELASTIC_MOISTURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("MO").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            mst: t.take("MST").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GEOLOGIC_CAP_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub r: Vec<f64>,
    pub d: Vec<f64>,
    pub w: Vec<f64>,
    pub x0: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub plot: Vec<f64>,
    pub ftype: Vec<f64>,
    pub vec: Vec<f64>,
    pub toff: Vec<f64>,
}
impl MAT_GEOLOGIC_CAP_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GEOLOGIC_CAP_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            x0: t.take("X0").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            plot: t.take("PLOT").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            vec: t.take("VEC").and_then(|c| c.into_float()).unwrap_or_default(),
            toff: t.take("TOFF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GEPLASTIC_SRATE_2000A {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ratesf: Vec<f64>,
    pub edot0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcfeps: Vec<i64>,
    pub lcfsig: Vec<i64>,
    pub lce: Vec<i64>,
}
impl MAT_GEPLASTIC_SRATE_2000A {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GEPLASTIC_SRATE_2000A").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ratesf: t.take("RATESF").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfeps: t.take("LCFEPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfsig: t.take("LCFSIG").and_then(|c| c.into_int()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GLASS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub imod: Vec<f64>,
    pub ilaw: Vec<f64>,
    pub fmod: Vec<f64>,
    pub ft: Vec<f64>,
    pub fc: Vec<f64>,
    pub at: Vec<f64>,
    pub bt: Vec<i64>,
    pub ac: Vec<i64>,
    pub bc: Vec<f64>,
    pub ftscl: Vec<f64>,
    pub sfsti: Vec<f64>,
    pub sfstr: Vec<f64>,
    pub crin: Vec<f64>,
    pub ecrcl: Vec<f64>,
    pub ncycr: Vec<f64>,
    pub nipf: Vec<f64>,
    pub epscr: Vec<f64>,
    pub engcrt: Vec<f64>,
    pub radcrt: Vec<f64>,
    pub ratenl: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_GLASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GLASS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            imod: t.take("IMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            ilaw: t.take("ILAW").and_then(|c| c.into_float()).unwrap_or_default(),
            fmod: t.take("FMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            at: t.take("AT").and_then(|c| c.into_float()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_int()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_int()).unwrap_or_default(),
            bc: t.take("BC").and_then(|c| c.into_float()).unwrap_or_default(),
            ftscl: t.take("FTSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsti: t.take("SFSTI").and_then(|c| c.into_float()).unwrap_or_default(),
            sfstr: t.take("SFSTR").and_then(|c| c.into_float()).unwrap_or_default(),
            crin: t.take("CRIN").and_then(|c| c.into_float()).unwrap_or_default(),
            ecrcl: t.take("ECRCL").and_then(|c| c.into_float()).unwrap_or_default(),
            ncycr: t.take("NCYCR").and_then(|c| c.into_float()).unwrap_or_default(),
            nipf: t.take("NIPF").and_then(|c| c.into_float()).unwrap_or_default(),
            epscr: t.take("EPSCR").and_then(|c| c.into_float()).unwrap_or_default(),
            engcrt: t.take("ENGCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            radcrt: t.take("RADCRT").and_then(|c| c.into_float()).unwrap_or_default(),
            ratenl: t.take("RATENL").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GURSON {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub l3: Vec<f64>,
    pub l4: Vec<f64>,
    pub ff1: Vec<f64>,
    pub ff2: Vec<f64>,
    pub ff3: Vec<f64>,
    pub ff4: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub lcf0: Vec<i64>,
    pub lcfc: Vec<i64>,
    pub lcfn: Vec<i64>,
    pub vgtyp: Vec<f64>,
}
impl MAT_GURSON {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GURSON").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            l3: t.take("L3").and_then(|c| c.into_float()).unwrap_or_default(),
            l4: t.take("L4").and_then(|c| c.into_float()).unwrap_or_default(),
            ff1: t.take("FF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ff2: t.take("FF2").and_then(|c| c.into_float()).unwrap_or_default(),
            ff3: t.take("FF3").and_then(|c| c.into_float()).unwrap_or_default(),
            ff4: t.take("FF4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf0: t.take("LCF0").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfc: t.take("LCFC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfn: t.take("LCFN").and_then(|c| c.into_int()).unwrap_or_default(),
            vgtyp: t.take("VGTYP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GURSON_JC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub lcdam: Vec<i64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub lcjc: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub lcf0: Vec<i64>,
    pub lcfc: Vec<i64>,
    pub lcfn: Vec<i64>,
    pub vgtyp: Vec<f64>,
    pub kw: Vec<i64>,
    pub beta: Vec<i64>,
    pub m: Vec<i64>,
}
impl MAT_GURSON_JC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GURSON_JC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdam: t.take("LCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcjc: t.take("LCJC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf0: t.take("LCF0").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfc: t.take("LCFC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfn: t.take("LCFN").and_then(|c| c.into_int()).unwrap_or_default(),
            vgtyp: t.take("VGTYP").and_then(|c| c.into_float()).unwrap_or_default(),
            kw: t.take("KW").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_int()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_GURSON_RCDC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub n: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub fc: Vec<f64>,
    pub f0: Vec<f64>,
    pub en: Vec<f64>,
    pub sn: Vec<f64>,
    pub fn_: Vec<f64>,
    pub etan: Vec<f64>,
    pub atyp: Vec<f64>,
    pub ff0: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub l1: Vec<f64>,
    pub l2: Vec<f64>,
    pub l3: Vec<f64>,
    pub l4: Vec<f64>,
    pub ff1: Vec<f64>,
    pub ff2: Vec<f64>,
    pub ff3: Vec<f64>,
    pub ff4: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lclf: Vec<i64>,
    pub numint: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub d0: Vec<f64>,
    pub b: Vec<f64>,
    pub lambda: Vec<f64>,
    pub ds: Vec<f64>,
    pub l: Vec<f64>,
}
impl MAT_GURSON_RCDC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_GURSON_RCDC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            en: t.take("EN").and_then(|c| c.into_float()).unwrap_or_default(),
            sn: t.take("SN").and_then(|c| c.into_float()).unwrap_or_default(),
            fn_: t.take("FN").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            atyp: t.take("ATYP").and_then(|c| c.into_float()).unwrap_or_default(),
            ff0: t.take("FF0").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            l1: t.take("L1").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            l3: t.take("L3").and_then(|c| c.into_float()).unwrap_or_default(),
            l4: t.take("L4").and_then(|c| c.into_float()).unwrap_or_default(),
            ff1: t.take("FF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ff2: t.take("FF2").and_then(|c| c.into_float()).unwrap_or_default(),
            ff3: t.take("FF3").and_then(|c| c.into_float()).unwrap_or_default(),
            ff4: t.take("FF4").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lclf: t.take("LCLF").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HEART_TISSUE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub p: Vec<f64>,
    pub b: Vec<f64>,
    pub l0: Vec<f64>,
    pub ca0max: Vec<i64>,
    pub lr: Vec<f64>,
    pub mm: Vec<f64>,
    pub bb: Vec<f64>,
    pub ca0: Vec<f64>,
    pub tmax: Vec<f64>,
    pub tact: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_HEART_TISSUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HEART_TISSUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            ca0max: t.take("CA0MAX").and_then(|c| c.into_int()).unwrap_or_default(),
            lr: t.take("LR").and_then(|c| c.into_float()).unwrap_or_default(),
            mm: t.take("MM").and_then(|c| c.into_float()).unwrap_or_default(),
            bb: t.take("BB").and_then(|c| c.into_float()).unwrap_or_default(),
            ca0: t.take("CA0").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            tact: t.take("TACT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HIGH_EXPLOSIVE_BURN {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub d: Vec<f64>,
    pub pcj: Vec<f64>,
    pub beta: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
}
impl MAT_HIGH_EXPLOSIVE_BURN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HIGH_EXPLOSIVE_BURN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            pcj: t.take("PCJ").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HILL_3R {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub aopt: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_HILL_3R {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HILL_3R").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HILL_3R_3D {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ex: Vec<f64>,
    pub ey: Vec<f64>,
    pub ez: Vec<f64>,
    pub prxy: Vec<f64>,
    pub pryz: Vec<f64>,
    pub prxz: Vec<f64>,
    pub gxy: Vec<f64>,
    pub gyz: Vec<f64>,
    pub gxz: Vec<f64>,
    pub f: Vec<f64>,
    pub g: Vec<f64>,
    pub h: Vec<f64>,
    pub l: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub hr: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_HILL_3R_3D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HILL_3R_3D").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ex: t.take("EX").and_then(|c| c.into_float()).unwrap_or_default(),
            ey: t.take("EY").and_then(|c| c.into_float()).unwrap_or_default(),
            ez: t.take("EZ").and_then(|c| c.into_float()).unwrap_or_default(),
            prxy: t.take("PRXY").and_then(|c| c.into_float()).unwrap_or_default(),
            pryz: t.take("PRYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            prxz: t.take("PRXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gxy: t.take("GXY").and_then(|c| c.into_float()).unwrap_or_default(),
            gyz: t.take("GYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gxz: t.take("GXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HILL_3R_TABULATED {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lc00: Vec<i64>,
    pub iconv: Vec<i64>,
    pub lc90: Vec<i64>,
    pub lc45: Vec<i64>,
    pub lcex: Vec<i64>,
    pub aopt: Vec<i64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_HILL_3R_TABULATED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HILL_3R_TABULATED").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lc00: t.take("LC00").and_then(|c| c.into_int()).unwrap_or_default(),
            iconv: t.take("ICONV").and_then(|c| c.into_int()).unwrap_or_default(),
            lc90: t.take("LC90").and_then(|c| c.into_int()).unwrap_or_default(),
            lc45: t.take("LC45").and_then(|c| c.into_int()).unwrap_or_default(),
            lcex: t.take("LCEX").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HILL_90 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub spi: Vec<f64>,
    pub p3: Vec<f64>,
    pub crc1: Vec<f64>,
    pub cra1: Vec<f64>,
    pub crc2: Vec<f64>,
    pub cra2: Vec<f64>,
    pub crc3: Vec<f64>,
    pub cra3: Vec<f64>,
    pub crc4: Vec<f64>,
    pub cra4: Vec<f64>,
    pub aopt: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub vlcid: Vec<i64>,
    pub unused: Vec<i64>,
    pub flag: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub usrfail: Vec<f64>,
}
impl MAT_HILL_90 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HILL_90").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            spi: t.take("SPI").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc1: t.take("CRC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cra1: t.take("CRA1").and_then(|c| c.into_float()).unwrap_or_default(),
            crc2: t.take("CRC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cra2: t.take("CRA2").and_then(|c| c.into_float()).unwrap_or_default(),
            crc3: t.take("CRC3").and_then(|c| c.into_float()).unwrap_or_default(),
            cra3: t.take("CRA3").and_then(|c| c.into_float()).unwrap_or_default(),
            crc4: t.take("CRC4").and_then(|c| c.into_float()).unwrap_or_default(),
            cra4: t.take("CRA4").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            vlcid: t.take("VLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            usrfail: t.take("USRFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HILL_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub nu: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fittype: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub r: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_HILL_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HILL_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fittype: t.take("FITTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HONEYCOMB {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vf: Vec<f64>,
    pub mu: Vec<f64>,
    pub bulk: Vec<f64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcc: Vec<i64>,
    pub lcs: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcbc: Vec<i64>,
    pub lcca: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eaau: Vec<f64>,
    pub ebbu: Vec<f64>,
    pub eccu: Vec<f64>,
    pub gabu: Vec<f64>,
    pub gbcu: Vec<f64>,
    pub gcau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub tsef: Vec<f64>,
    pub ssef: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_HONEYCOMB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HONEYCOMB").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcs: t.take("LCS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbc: t.take("LCBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcca: t.take("LCCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eaau: t.take("EAAU").and_then(|c| c.into_float()).unwrap_or_default(),
            ebbu: t.take("EBBU").and_then(|c| c.into_float()).unwrap_or_default(),
            eccu: t.take("ECCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gabu: t.take("GABU").and_then(|c| c.into_float()).unwrap_or_default(),
            gbcu: t.take("GBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gcau: t.take("GCAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            tsef: t.take("TSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            ssef: t.take("SSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HOT_PLATE_ROLLING {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub alphat: Vec<f64>,
    pub beta: Vec<f64>,
    pub vp: Vec<f64>,
    pub tol: Vec<f64>,
    pub yb: Vec<f64>,
    pub qdef: Vec<f64>,
    pub r: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub minrt: Vec<f64>,
    pub post: Vec<f64>,
    pub odesol: Vec<f64>,
    pub asig0: Vec<f64>,
    pub bsig0: Vec<f64>,
    pub asigs: Vec<f64>,
    pub bsigs: Vec<f64>,
    pub asigss: Vec<f64>,
    pub bsigss: Vec<f64>,
    pub aeps: Vec<f64>,
    pub beps: Vec<f64>,
    pub thres: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha: Vec<f64>,
    pub nud: Vec<f64>,
    pub u0: Vec<f64>,
    pub k: Vec<f64>,
    pub nu: Vec<f64>,
    pub bnu: Vec<f64>,
    pub t50: Vec<f64>,
    pub n: Vec<f64>,
    pub a50: Vec<f64>,
    pub d: Vec<f64>,
    pub gsf: Vec<f64>,
    pub p: Vec<f64>,
    pub q: Vec<f64>,
    pub qrex: Vec<f64>,
}
impl MAT_HOT_PLATE_ROLLING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HOT_PLATE_ROLLING").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            alphat: t.take("ALPHAT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            yb: t.take("YB").and_then(|c| c.into_float()).unwrap_or_default(),
            qdef: t.take("QDEF").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            minrt: t.take("MINRT").and_then(|c| c.into_float()).unwrap_or_default(),
            post: t.take("POST").and_then(|c| c.into_float()).unwrap_or_default(),
            odesol: t.take("ODESOL").and_then(|c| c.into_float()).unwrap_or_default(),
            asig0: t.take("ASIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig0: t.take("BSIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            asigs: t.take("ASIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            bsigs: t.take("BSIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            asigss: t.take("ASIGSS").and_then(|c| c.into_float()).unwrap_or_default(),
            bsigss: t.take("BSIGSS").and_then(|c| c.into_float()).unwrap_or_default(),
            aeps: t.take("AEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            beps: t.take("BEPS").and_then(|c| c.into_float()).unwrap_or_default(),
            thres: t.take("THRES").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            nud: t.take("NUD").and_then(|c| c.into_float()).unwrap_or_default(),
            u0: t.take("U0").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            bnu: t.take("BNU").and_then(|c| c.into_float()).unwrap_or_default(),
            t50: t.take("T50").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a50: t.take("A50").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            gsf: t.take("GSF").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            qrex: t.take("QREX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HYDRAULIC_GAS_DAMPER_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub co: Vec<f64>,
    pub n: Vec<f64>,
    pub p0: Vec<f64>,
    pub pa: Vec<f64>,
    pub ap: Vec<f64>,
    pub kh: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fr: Vec<f64>,
    pub sclf: Vec<f64>,
    pub clear: Vec<f64>,
}
impl MAT_HYDRAULIC_GAS_DAMPER_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HYDRAULIC_GAS_DAMPER_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("CO").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            pa: t.take("PA").and_then(|c| c.into_float()).unwrap_or_default(),
            ap: t.take("AP").and_then(|c| c.into_float()).unwrap_or_default(),
            kh: t.take("KH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fr: t.take("FR").and_then(|c| c.into_float()).unwrap_or_default(),
            sclf: t.take("SCLF").and_then(|c| c.into_float()).unwrap_or_default(),
            clear: t.take("CLEAR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HYPERELASTIC_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub nv: Vec<i64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tbhys: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub data: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub c10: Vec<f64>,
    pub c01: Vec<f64>,
    pub c11: Vec<f64>,
    pub c20: Vec<f64>,
    pub c02: Vec<f64>,
    pub c30: Vec<f64>,
    pub therml: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub gj: Vec<f64>,
    pub sigfj: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_HYPERELASTIC_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HYPERELASTIC_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tbhys: t.take("TBHYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            c10: t.take("C10").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c20: t.take("C20").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            c30: t.take("C30").and_then(|c| c.into_float()).unwrap_or_default(),
            therml: t.take("THERML").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            gj: t.take("GJ").and_then(|c| c.into_float()).unwrap_or_default(),
            sigfj: t.take("SIGFJ").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HYSTERETIC_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub iax: Vec<i64>,
    pub isurf: Vec<i64>,
    pub ihard: Vec<i64>,
    pub ifema: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub sfs: Vec<f64>,
    pub lcpmt: Vec<i64>,
    pub sft: Vec<f64>,
    pub lcat: Vec<i64>,
    pub sfat: Vec<f64>,
    pub lcac: Vec<i64>,
    pub sfac: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub f0: Vec<f64>,
    pub pinm: Vec<f64>,
    pub pins: Vec<f64>,
    pub hloc1: Vec<f64>,
    pub hloc2: Vec<f64>,
    pub deltas: Vec<f64>,
    pub kappas: Vec<f64>,
    pub deltat: Vec<f64>,
    pub kappat: Vec<f64>,
    pub lcshs: Vec<i64>,
    pub sfshs: Vec<f64>,
    pub lcsht: Vec<i64>,
    pub sfsht: Vec<f64>,
    pub hardms: Vec<f64>,
    pub gamms: Vec<f64>,
    pub hardmt: Vec<f64>,
    pub gammt: Vec<f64>,
    pub hardat: Vec<f64>,
    pub gamat: Vec<f64>,
    pub hardac: Vec<f64>,
    pub gamac: Vec<f64>,
    pub omgms1: Vec<f64>,
    pub omgms2: Vec<f64>,
    pub omgmt1: Vec<f64>,
    pub omgmt2: Vec<f64>,
    pub omgat1: Vec<f64>,
    pub omgat2: Vec<f64>,
    pub omgac1: Vec<f64>,
    pub omgac2: Vec<f64>,
    pub rums: Vec<f64>,
    pub rumt: Vec<f64>,
    pub duat: Vec<f64>,
    pub duac: Vec<f64>,
    pub lam1: Vec<f64>,
    pub lam2: Vec<f64>,
    pub soft1: Vec<f64>,
    pub soft2: Vec<f64>,
    pub prs1: Vec<f64>,
    pub prs2: Vec<f64>,
    pub prs3: Vec<f64>,
    pub prs4: Vec<f64>,
    pub prt1: Vec<f64>,
    pub prt2: Vec<f64>,
    pub prt3: Vec<f64>,
    pub prt4: Vec<f64>,
    pub ts1: Vec<f64>,
    pub ts2: Vec<f64>,
    pub ts3: Vec<f64>,
    pub ts4: Vec<f64>,
    pub cs1: Vec<f64>,
    pub cs2: Vec<f64>,
    pub cs3: Vec<f64>,
    pub cs4: Vec<f64>,
    pub ss1: Vec<f64>,
    pub ss2: Vec<f64>,
    pub ss3: Vec<f64>,
    pub ss4: Vec<f64>,
    pub st1: Vec<f64>,
    pub st2: Vec<f64>,
    pub st3: Vec<f64>,
    pub st4: Vec<f64>,
}
impl MAT_HYSTERETIC_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HYSTERETIC_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            iax: t.take("IAX").and_then(|c| c.into_int()).unwrap_or_default(),
            isurf: t.take("ISURF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihard: t.take("IHARD").and_then(|c| c.into_int()).unwrap_or_default(),
            ifema: t.take("IFEMA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfat: t.take("SFAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
            sfac: t.take("SFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            pinm: t.take("PINM").and_then(|c| c.into_float()).unwrap_or_default(),
            pins: t.take("PINS").and_then(|c| c.into_float()).unwrap_or_default(),
            hloc1: t.take("HLOC1").and_then(|c| c.into_float()).unwrap_or_default(),
            hloc2: t.take("HLOC2").and_then(|c| c.into_float()).unwrap_or_default(),
            deltas: t.take("DELTAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kappas: t.take("KAPPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            deltat: t.take("DELTAT").and_then(|c| c.into_float()).unwrap_or_default(),
            kappat: t.take("KAPPAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcshs: t.take("LCSHS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfshs: t.take("SFSHS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsht: t.take("LCSHT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfsht: t.take("SFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardms: t.take("HARDMS").and_then(|c| c.into_float()).unwrap_or_default(),
            gamms: t.take("GAMMS").and_then(|c| c.into_float()).unwrap_or_default(),
            hardmt: t.take("HARDMT").and_then(|c| c.into_float()).unwrap_or_default(),
            gammt: t.take("GAMMT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardat: t.take("HARDAT").and_then(|c| c.into_float()).unwrap_or_default(),
            gamat: t.take("GAMAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hardac: t.take("HARDAC").and_then(|c| c.into_float()).unwrap_or_default(),
            gamac: t.take("GAMAC").and_then(|c| c.into_float()).unwrap_or_default(),
            omgms1: t.take("OMGMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgms2: t.take("OMGMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmt1: t.take("OMGMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgmt2: t.take("OMGMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgat1: t.take("OMGAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgat2: t.take("OMGAT2").and_then(|c| c.into_float()).unwrap_or_default(),
            omgac1: t.take("OMGAC1").and_then(|c| c.into_float()).unwrap_or_default(),
            omgac2: t.take("OMGAC2").and_then(|c| c.into_float()).unwrap_or_default(),
            rums: t.take("RUMS").and_then(|c| c.into_float()).unwrap_or_default(),
            rumt: t.take("RUMT").and_then(|c| c.into_float()).unwrap_or_default(),
            duat: t.take("DUAT").and_then(|c| c.into_float()).unwrap_or_default(),
            duac: t.take("DUAC").and_then(|c| c.into_float()).unwrap_or_default(),
            lam1: t.take("LAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            lam2: t.take("LAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            soft1: t.take("SOFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            soft2: t.take("SOFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            prs1: t.take("PRS1").and_then(|c| c.into_float()).unwrap_or_default(),
            prs2: t.take("PRS2").and_then(|c| c.into_float()).unwrap_or_default(),
            prs3: t.take("PRS3").and_then(|c| c.into_float()).unwrap_or_default(),
            prs4: t.take("PRS4").and_then(|c| c.into_float()).unwrap_or_default(),
            prt1: t.take("PRT1").and_then(|c| c.into_float()).unwrap_or_default(),
            prt2: t.take("PRT2").and_then(|c| c.into_float()).unwrap_or_default(),
            prt3: t.take("PRT3").and_then(|c| c.into_float()).unwrap_or_default(),
            prt4: t.take("PRT4").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts3: t.take("TS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ts4: t.take("TS4").and_then(|c| c.into_float()).unwrap_or_default(),
            cs1: t.take("CS1").and_then(|c| c.into_float()).unwrap_or_default(),
            cs2: t.take("CS2").and_then(|c| c.into_float()).unwrap_or_default(),
            cs3: t.take("CS3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs4: t.take("CS4").and_then(|c| c.into_float()).unwrap_or_default(),
            ss1: t.take("SS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ss2: t.take("SS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ss3: t.take("SS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ss4: t.take("SS4").and_then(|c| c.into_float()).unwrap_or_default(),
            st1: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            st2: t.take("ST2").and_then(|c| c.into_float()).unwrap_or_default(),
            st3: t.take("ST3").and_then(|c| c.into_float()).unwrap_or_default(),
            st4: t.take("ST4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HYSTERETIC_REINFORCEMENT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lamda: Vec<f64>,
    pub sbuck: Vec<f64>,
    pub power: Vec<f64>,
    pub fracx: Vec<f64>,
    pub fracy: Vec<f64>,
    pub lcten: Vec<f64>,
    pub lccomp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ebu: Vec<f64>,
    pub downsl: Vec<f64>,
    pub dbar: Vec<f64>,
    pub fcdow: Vec<f64>,
    pub lchard: Vec<f64>,
    pub unitc: Vec<f64>,
    pub unitl: Vec<f64>,
    pub epdam1: Vec<f64>,
    pub epdam2: Vec<f64>,
    pub dresid: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_HYSTERETIC_REINFORCEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HYSTERETIC_REINFORCEMENT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda: t.take("LAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            sbuck: t.take("SBUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            power: t.take("POWER").and_then(|c| c.into_float()).unwrap_or_default(),
            fracx: t.take("FRACX").and_then(|c| c.into_float()).unwrap_or_default(),
            fracy: t.take("FRACY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcten: t.take("LCTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            lccomp: t.take("LCCOMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ebu: t.take("EBU").and_then(|c| c.into_float()).unwrap_or_default(),
            downsl: t.take("DOWNSL").and_then(|c| c.into_float()).unwrap_or_default(),
            dbar: t.take("DBAR").and_then(|c| c.into_float()).unwrap_or_default(),
            fcdow: t.take("FCDOW").and_then(|c| c.into_float()).unwrap_or_default(),
            lchard: t.take("LCHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            unitl: t.take("UNITL").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam1: t.take("EPDAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam2: t.take("EPDAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dresid: t.take("DRESID").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_HYSTERETIC_SOIL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k0: Vec<f64>,
    pub p0: Vec<f64>,
    pub b: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub df: Vec<f64>,
    pub rp: Vec<f64>,
    pub lcid: Vec<i64>,
    pub sflc: Vec<f64>,
    pub dil_a: Vec<f64>,
    pub dil_b: Vec<f64>,
    pub dil_c: Vec<f64>,
    pub dil_d: Vec<f64>,
    pub gam1: Vec<f64>,
    pub gam2: Vec<f64>,
    pub gam3: Vec<f64>,
    pub gam4: Vec<f64>,
    pub gam5: Vec<f64>,
    pub lcd: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub pinit: Vec<i64>,
    pub tau1: Vec<f64>,
    pub tau2: Vec<f64>,
    pub tau3: Vec<f64>,
    pub tau4: Vec<f64>,
    pub tau5: Vec<f64>,
    pub flag5: Vec<i64>,
    pub sigth: Vec<f64>,
    pub sigr: Vec<f64>,
    pub chi: Vec<f64>,
}
impl MAT_HYSTERETIC_SOIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_HYSTERETIC_SOIL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            p0: t.take("P0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            sflc: t.take("SFLC").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_a: t.take("DIL_A").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_b: t.take("DIL_B").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_c: t.take("DIL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            dil_d: t.take("DIL_D").and_then(|c| c.into_float()).unwrap_or_default(),
            gam1: t.take("GAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            gam2: t.take("GAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            gam3: t.take("GAM3").and_then(|c| c.into_float()).unwrap_or_default(),
            gam4: t.take("GAM4").and_then(|c| c.into_float()).unwrap_or_default(),
            gam5: t.take("GAM5").and_then(|c| c.into_float()).unwrap_or_default(),
            lcd: t.take("LCD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            pinit: t.take("PINIT").and_then(|c| c.into_int()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            tau3: t.take("TAU3").and_then(|c| c.into_float()).unwrap_or_default(),
            tau4: t.take("TAU4").and_then(|c| c.into_float()).unwrap_or_default(),
            tau5: t.take("TAU5").and_then(|c| c.into_float()).unwrap_or_default(),
            flag5: t.take("FLAG5").and_then(|c| c.into_int()).unwrap_or_default(),
            sigth: t.take("SIGTH").and_then(|c| c.into_float()).unwrap_or_default(),
            sigr: t.take("SIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            chi: t.take("CHI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_IFPD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub dynvis: Vec<f64>,
    pub sften: Vec<f64>,
}
impl MAT_IFPD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_IFPD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            dynvis: t.take("DYNVIS").and_then(|c| c.into_float()).unwrap_or_default(),
            sften: t.take("SFTEN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_INELASTIC_6DOF_SPRING_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tpidr: Vec<i64>,
    pub tpids: Vec<i64>,
    pub tpidt: Vec<i64>,
    pub rpidr: Vec<i64>,
    pub rpids: Vec<i64>,
    pub rpidt: Vec<i64>,
}
impl MAT_INELASTIC_6DOF_SPRING_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_INELASTIC_6DOF_SPRING_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tpidr: t.take("TPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            tpids: t.take("TPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            tpidt: t.take("TPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidr: t.take("RPIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            rpids: t.take("RPIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            rpidt: t.take("RPIDT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_INELASTIC_SPRING_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub f0: Vec<f64>,
    pub d: Vec<f64>,
    pub cdf: Vec<f64>,
    pub tdf: Vec<f64>,
    pub flcid: Vec<i64>,
    pub hlcid: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub dle: Vec<f64>,
    pub glcid: Vec<i64>,
}
impl MAT_INELASTIC_SPRING_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_INELASTIC_SPRING_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("F0").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            cdf: t.take("CDF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdf: t.take("TDF").and_then(|c| c.into_float()).unwrap_or_default(),
            flcid: t.take("FLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            dle: t.take("DLE").and_then(|c| c.into_float()).unwrap_or_default(),
            glcid: t.take("GLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_INV_HYPERBOLIC_SIN {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub t: Vec<f64>,
    pub hc: Vec<f64>,
    pub vp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub n: Vec<f64>,
    pub a: Vec<f64>,
    pub q: Vec<f64>,
    pub g: Vec<f64>,
    pub epso: Vec<f64>,
    pub lcq: Vec<f64>,
}
impl MAT_INV_HYPERBOLIC_SIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_INV_HYPERBOLIC_SIN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcq: t.take("LCQ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_INV_HYPERBOLIC_SIN_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub alpha: Vec<f64>,
    pub n: Vec<f64>,
    pub a: Vec<f64>,
    pub q: Vec<f64>,
    pub g: Vec<f64>,
    pub epso: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lccte: Vec<f64>,
}
impl MAT_INV_HYPERBOLIC_SIN_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_INV_HYPERBOLIC_SIN_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lccte: t.take("LCCTE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ISOTROPIC_ELASTIC_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub bulk: Vec<f64>,
    pub epf: Vec<f64>,
    pub prf: Vec<f64>,
    pub rem: Vec<f64>,
    pub trem: Vec<f64>,
}
impl MAT_ISOTROPIC_ELASTIC_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ISOTROPIC_ELASTIC_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            epf: t.take("EPF").and_then(|c| c.into_float()).unwrap_or_default(),
            prf: t.take("PRF").and_then(|c| c.into_float()).unwrap_or_default(),
            rem: t.take("REM").and_then(|c| c.into_float()).unwrap_or_default(),
            trem: t.take("TREM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ISOTROPIC_ELASTIC_PLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub bulk: Vec<f64>,
}
impl MAT_ISOTROPIC_ELASTIC_PLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ISOTROPIC_ELASTIC_PLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ISOTROPIC_SMEARED_CRACK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ispl: Vec<i64>,
    pub sigy: Vec<f64>,
    pub gk: Vec<f64>,
    pub sr: Vec<f64>,
}
impl MAT_ISOTROPIC_SMEARED_CRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ISOTROPIC_SMEARED_CRACK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ispl: t.take("ISPL").and_then(|c| c.into_int()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            gk: t.take("GK").and_then(|c| c.into_float()).unwrap_or_default(),
            sr: t.take("SR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOHNSON_COOK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub dtf: Vec<f64>,
    pub vp: Vec<f64>,
    pub rateop: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub tm: Vec<f64>,
    pub tr: Vec<f64>,
    pub epso: Vec<f64>,
    pub cp: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub it: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub c2_p_xnp_d: Vec<f64>,
    pub erod: Vec<f64>,
    pub efmin: Vec<f64>,
    pub numint: Vec<f64>,
    pub k: Vec<f64>,
    pub eps1: Vec<f64>,
}
impl MAT_JOHNSON_COOK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOHNSON_COOK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            dtf: t.take("DTF").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            rateop: t.take("RATEOP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            c2_p_xnp_d: t.take("C2/P/XNP/D").and_then(|c| c.into_float()).unwrap_or_default(),
            erod: t.take("EROD").and_then(|c| c.into_float()).unwrap_or_default(),
            efmin: t.take("EFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOHNSON_COOK_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub dtf: Vec<f64>,
    pub vp: Vec<f64>,
    pub rateop: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub tm: Vec<f64>,
    pub tr: Vec<f64>,
    pub epso: Vec<f64>,
    pub cp: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub it: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d5: Vec<f64>,
    pub c2_p_xnp_d: Vec<f64>,
    pub erod: Vec<f64>,
    pub efmin: Vec<f64>,
    pub numint: Vec<f64>,
    pub k: Vec<f64>,
    pub eps1: Vec<f64>,
}
impl MAT_JOHNSON_COOK_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOHNSON_COOK_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            dtf: t.take("DTF").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            rateop: t.take("RATEOP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            it: t.take("IT").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d5: t.take("D5").and_then(|c| c.into_float()).unwrap_or_default(),
            c2_p_xnp_d: t.take("C2/P/XNP/D").and_then(|c| c.into_float()).unwrap_or_default(),
            erod: t.take("EROD").and_then(|c| c.into_float()).unwrap_or_default(),
            efmin: t.take("EFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOHNSON_HOLMQUIST_CERAMICS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub epsi: Vec<f64>,
    pub t: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub hel: Vec<f64>,
    pub phel: Vec<f64>,
    pub beta: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_JOHNSON_HOLMQUIST_CERAMICS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOHNSON_HOLMQUIST_CERAMICS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            epsi: t.take("EPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            hel: t.take("HEL").and_then(|c| c.into_float()).unwrap_or_default(),
            phel: t.take("PHEL").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOHNSON_HOLMQUIST_CONCRETE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub fc: Vec<f64>,
    pub t: Vec<f64>,
    pub eps0: Vec<f64>,
    pub efmin: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub pc: Vec<f64>,
    pub uc: Vec<f64>,
    pub pl: Vec<f64>,
    pub ul: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_JOHNSON_HOLMQUIST_CONCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOHNSON_HOLMQUIST_CONCRETE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            efmin: t.take("EFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            uc: t.take("UC").and_then(|c| c.into_float()).unwrap_or_default(),
            pl: t.take("PL").and_then(|c| c.into_float()).unwrap_or_default(),
            ul: t.take("UL").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOHNSON_HOLMQUIST_JH1 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub p1: Vec<f64>,
    pub s1: Vec<f64>,
    pub p2: Vec<f64>,
    pub s2: Vec<f64>,
    pub c: Vec<f64>,
    pub epsi: Vec<f64>,
    pub t: Vec<f64>,
    pub unused: Vec<f64>,
    pub alpha: Vec<f64>,
    pub sfmax: Vec<f64>,
    pub beta: Vec<f64>,
    pub dp1: Vec<f64>,
    pub epfmin: Vec<f64>,
    pub epfmax: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_JOHNSON_HOLMQUIST_JH1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOHNSON_HOLMQUIST_JH1").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsi: t.take("EPSI").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfmax: t.take("SFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            dp1: t.take("DP1").and_then(|c| c.into_float()).unwrap_or_default(),
            epfmin: t.take("EPFMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epfmax: t.take("EPFMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_JOINTED_ROCK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rkf: Vec<f64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub str_lim: Vec<f64>,
    pub nplanes: Vec<i64>,
    pub elastic: Vec<i64>,
    pub lccpdr: Vec<i64>,
    pub lccpt: Vec<i64>,
    pub lccjdr: Vec<i64>,
    pub lccjt: Vec<i64>,
    pub lcsfac: Vec<i64>,
    pub gmoddp: Vec<f64>,
    pub phidp: Vec<f64>,
    pub cvaldp: Vec<f64>,
    pub psidp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub phigr: Vec<f64>,
    pub cvalgr: Vec<f64>,
    pub psigr: Vec<f64>,
    pub dip: Vec<f64>,
    pub strike: Vec<f64>,
    pub cplane: Vec<f64>,
    pub frplane: Vec<f64>,
    pub tplane: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub local: Vec<f64>,
}
impl MAT_JOINTED_ROCK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_JOINTED_ROCK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rkf: t.take("RKF").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            str_lim: t.take("STR_LIM").and_then(|c| c.into_float()).unwrap_or_default(),
            nplanes: t.take("NPLANES").and_then(|c| c.into_int()).unwrap_or_default(),
            elastic: t.take("ELASTIC").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpdr: t.take("LCCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpt: t.take("LCCPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjdr: t.take("LCCJDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjt: t.take("LCCJT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsfac: t.take("LCSFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            phidp: t.take("PHIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            cvaldp: t.take("CVALDP").and_then(|c| c.into_float()).unwrap_or_default(),
            psidp: t.take("PSIDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            phigr: t.take("PHIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            psigr: t.take("PSIGR").and_then(|c| c.into_float()).unwrap_or_default(),
            dip: t.take("DIP").and_then(|c| c.into_float()).unwrap_or_default(),
            strike: t.take("STRIKE").and_then(|c| c.into_float()).unwrap_or_default(),
            cplane: t.take("CPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            frplane: t.take("FRPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            tplane: t.take("TPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KELVIN_MAXWELL_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub dc: Vec<f64>,
    pub fo: Vec<f64>,
    pub so: Vec<f64>,
}
impl MAT_KELVIN_MAXWELL_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KELVIN-MAXWELL_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            fo: t.take("FO").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KINEMATIC_HARDENING_BARLAT2000 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub m: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc1: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub aopt: Vec<i64>,
    pub unused: Vec<i64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_KINEMATIC_HARDENING_BARLAT2000 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KINEMATIC_HARDENING_BARLAT2000").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc1: t.take("SC1").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KINEMATIC_HARDENING_BARLAT89 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub f: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_KINEMATIC_HARDENING_BARLAT89 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KINEMATIC_HARDENING_BARLAT89").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KINEMATIC_HARDENING_BARLAT89_NLP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub m: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub ifld: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_KINEMATIC_HARDENING_BARLAT89_NLP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KINEMATIC_HARDENING_BARLAT89_NLP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            ifld: t.take("IFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r: Vec<f64>,
    pub hclid: Vec<i64>,
    pub opt: Vec<i64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub sc2: Vec<f64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
}
impl MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hclid: t.take("HCLID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            sc2: t.take("SC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC_NLP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r: Vec<f64>,
    pub hclid: Vec<i64>,
    pub opt: Vec<i64>,
    pub cb: Vec<f64>,
    pub y: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub rsat: Vec<f64>,
    pub sb: Vec<f64>,
    pub h: Vec<f64>,
    pub sc2: Vec<f64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub iopt: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub ifld: Vec<i64>,
}
impl MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC_NLP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_KINEMATIC_HARDENING_TRANSVERSELY_ANISOTROPIC_NLP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hclid: t.take("HCLID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            cb: t.take("CB").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            rsat: t.take("RSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            sb: t.take("SB").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            sc2: t.take("SC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            iopt: t.take("IOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            ifld: t.take("IFLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAMINATED_COMPOSITE_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub fs: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcdfail: Vec<i64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub lctau: Vec<i64>,
    pub lcgam: Vec<i64>,
    pub dt: Vec<f64>,
    pub lce11c: Vec<i64>,
    pub lce11t: Vec<i64>,
    pub lce22c: Vec<i64>,
    pub lce22t: Vec<i64>,
    pub lcgms: Vec<i64>,
    pub lcefs: Vec<i64>,
}
impl MAT_LAMINATED_COMPOSITE_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAMINATED_COMPOSITE_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdfail: t.take("LCDFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam: t.take("LCGAM").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lce11c: t.take("LCE11C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce11t: t.take("LCE11T").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22c: t.take("LCE22C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22t: t.take("LCE22T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms: t.take("LCGMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefs: t.take("LCEFS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAMINATED_COMPOSITE_FABRIC_SOLID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub fs: Vec<f64>,
    pub epsf: Vec<f64>,
    pub epsr: Vec<f64>,
    pub tsmd: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcdfail: Vec<i64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub e33c: Vec<f64>,
    pub e33t: Vec<f64>,
    pub gm23: Vec<f64>,
    pub gm31: Vec<f64>,
    pub zc: Vec<f64>,
    pub zt: Vec<f64>,
    pub sc23: Vec<f64>,
    pub sc31: Vec<f64>,
    pub slimt3: Vec<f64>,
    pub slimc3: Vec<f64>,
    pub slims23: Vec<f64>,
    pub lsims31: Vec<f64>,
    pub tau2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub tau3: Vec<f64>,
    pub gamma3: Vec<f64>,
    pub lcxc: Vec<i64>,
    pub lcxt: Vec<i64>,
    pub lcyc: Vec<i64>,
    pub lcyt: Vec<i64>,
    pub lcsc: Vec<i64>,
    pub lctau: Vec<i64>,
    pub lcgam: Vec<i64>,
    pub dt: Vec<f64>,
    pub lce11c: Vec<i64>,
    pub lce11t: Vec<i64>,
    pub lce22c: Vec<i64>,
    pub lce22t: Vec<i64>,
    pub lcgms: Vec<i64>,
    pub lcefs: Vec<i64>,
    pub lczc: Vec<i64>,
    pub lczt: Vec<i64>,
    pub lcsc23: Vec<i64>,
    pub lcsc31: Vec<i64>,
    pub lctau2: Vec<i64>,
    pub lcgam2: Vec<i64>,
    pub lctau3: Vec<i64>,
    pub lcgam3: Vec<i64>,
    pub lce33c: Vec<i64>,
    pub lce33t: Vec<i64>,
    pub lcgms23: Vec<i64>,
    pub lcgms31: Vec<i64>,
}
impl MAT_LAMINATED_COMPOSITE_FABRIC_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAMINATED_COMPOSITE_FABRIC_SOLID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMS").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr: t.take("EPSR").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd: t.take("TSMD").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdfail: t.take("LCDFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            e33c: t.take("E33C").and_then(|c| c.into_float()).unwrap_or_default(),
            e33t: t.take("E33T").and_then(|c| c.into_float()).unwrap_or_default(),
            gm23: t.take("GM23").and_then(|c| c.into_float()).unwrap_or_default(),
            gm31: t.take("GM31").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc23: t.take("SC23").and_then(|c| c.into_float()).unwrap_or_default(),
            sc31: t.take("SC31").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt3: t.take("SLIMT3").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc3: t.take("SLIMC3").and_then(|c| c.into_float()).unwrap_or_default(),
            slims23: t.take("SLIMS23").and_then(|c| c.into_float()).unwrap_or_default(),
            lsims31: t.take("LSIMS31").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            tau3: t.take("TAU3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcxc: t.take("LCXC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcxt: t.take("LCXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyc: t.take("LCYC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyt: t.take("LCYT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc: t.take("LCSC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam: t.take("LCGAM").and_then(|c| c.into_int()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            lce11c: t.take("LCE11C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce11t: t.take("LCE11T").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22c: t.take("LCE22C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce22t: t.take("LCE22T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms: t.take("LCGMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcefs: t.take("LCEFS").and_then(|c| c.into_int()).unwrap_or_default(),
            lczc: t.take("LCZC").and_then(|c| c.into_int()).unwrap_or_default(),
            lczt: t.take("LCZT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc23: t.take("LCSC23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsc31: t.take("LCSC31").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau2: t.take("LCTAU2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam2: t.take("LCGAM2").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau3: t.take("LCTAU3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgam3: t.take("LCGAM3").and_then(|c| c.into_int()).unwrap_or_default(),
            lce33c: t.take("LCE33C").and_then(|c| c.into_int()).unwrap_or_default(),
            lce33t: t.take("LCE33T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms23: t.take("LCGMS23").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgms31: t.take("LCGMS31").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAMINATED_FRACTURE_DAIMLER_CAMANHO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub daf: Vec<f64>,
    pub dkf: Vec<f64>,
    pub dmf: Vec<f64>,
    pub efs: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub dsf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub msg: Vec<f64>,
    pub gxc: Vec<f64>,
    pub gxt: Vec<f64>,
    pub gyc: Vec<f64>,
    pub gyt: Vec<f64>,
    pub gsl: Vec<f64>,
    pub gxco: Vec<f64>,
    pub gxto: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sl: Vec<f64>,
    pub xco: Vec<f64>,
    pub xto: Vec<f64>,
    pub fio: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub puck: Vec<f64>,
    pub soft: Vec<f64>,
    pub dt: Vec<f64>,
    pub epsf23: Vec<f64>,
    pub epsr23: Vec<f64>,
    pub tsmd23: Vec<f64>,
    pub epsf31: Vec<f64>,
    pub epsr31: Vec<f64>,
    pub tsmd31: Vec<f64>,
}
impl MAT_LAMINATED_FRACTURE_DAIMLER_CAMANHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAMINATED_FRACTURE_DAIMLER_CAMANHO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            daf: t.take("DAF").and_then(|c| c.into_float()).unwrap_or_default(),
            dkf: t.take("DKF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmf: t.take("DMF").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            dsf: t.take("DSF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            msg: t.take("MSG").and_then(|c| c.into_float()).unwrap_or_default(),
            gxc: t.take("GXC").and_then(|c| c.into_float()).unwrap_or_default(),
            gxt: t.take("GXT").and_then(|c| c.into_float()).unwrap_or_default(),
            gyc: t.take("GYC").and_then(|c| c.into_float()).unwrap_or_default(),
            gyt: t.take("GYT").and_then(|c| c.into_float()).unwrap_or_default(),
            gsl: t.take("GSL").and_then(|c| c.into_float()).unwrap_or_default(),
            gxco: t.take("GXCO").and_then(|c| c.into_float()).unwrap_or_default(),
            gxto: t.take("GXTO").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            xco: t.take("XCO").and_then(|c| c.into_float()).unwrap_or_default(),
            xto: t.take("XTO").and_then(|c| c.into_float()).unwrap_or_default(),
            fio: t.take("FIO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            puck: t.take("PUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf23: t.take("EPSF23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr23: t.take("EPSR23").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd23: t.take("TSMD23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf31: t.take("EPSF31").and_then(|c| c.into_float()).unwrap_or_default(),
            epsr31: t.take("EPSR31").and_then(|c| c.into_float()).unwrap_or_default(),
            tsmd31: t.take("TSMD31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAMINATED_FRACTURE_DAIMLER_PINHO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub daf: Vec<f64>,
    pub dkf: Vec<f64>,
    pub dmf: Vec<f64>,
    pub efs: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangle: Vec<f64>,
    pub enkink: Vec<f64>,
    pub ena: Vec<f64>,
    pub enb: Vec<f64>,
    pub ent: Vec<f64>,
    pub enl: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sl: Vec<f64>,
    pub fio: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lcss: Vec<i64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub puck: Vec<f64>,
    pub soft: Vec<f64>,
}
impl MAT_LAMINATED_FRACTURE_DAIMLER_PINHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAMINATED_FRACTURE_DAIMLER_PINHO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            daf: t.take("DAF").and_then(|c| c.into_float()).unwrap_or_default(),
            dkf: t.take("DKF").and_then(|c| c.into_float()).unwrap_or_default(),
            dmf: t.take("DMF").and_then(|c| c.into_float()).unwrap_or_default(),
            efs: t.take("EFS").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            enkink: t.take("ENKINK").and_then(|c| c.into_float()).unwrap_or_default(),
            ena: t.take("ENA").and_then(|c| c.into_float()).unwrap_or_default(),
            enb: t.take("ENB").and_then(|c| c.into_float()).unwrap_or_default(),
            ent: t.take("ENT").and_then(|c| c.into_float()).unwrap_or_default(),
            enl: t.take("ENL").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sl: t.take("SL").and_then(|c| c.into_float()).unwrap_or_default(),
            fio: t.take("FIO").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            puck: t.take("PUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAMINATED_GLASS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub eg: Vec<f64>,
    pub prg: Vec<f64>,
    pub syg: Vec<f64>,
    pub etg: Vec<f64>,
    pub efg: Vec<f64>,
    pub ep: Vec<f64>,
    pub prp: Vec<f64>,
    pub syp: Vec<f64>,
    pub etp: Vec<f64>,
    pub f1: Vec<f64>,
    pub f2: Vec<f64>,
    pub f3: Vec<f64>,
    pub f4: Vec<f64>,
    pub f5: Vec<f64>,
    pub f6: Vec<f64>,
    pub f7: Vec<f64>,
    pub f8: Vec<f64>,
}
impl MAT_LAMINATED_GLASS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAMINATED_GLASS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            eg: t.take("EG").and_then(|c| c.into_float()).unwrap_or_default(),
            prg: t.take("PRG").and_then(|c| c.into_float()).unwrap_or_default(),
            syg: t.take("SYG").and_then(|c| c.into_float()).unwrap_or_default(),
            etg: t.take("ETG").and_then(|c| c.into_float()).unwrap_or_default(),
            efg: t.take("EFG").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
            prp: t.take("PRP").and_then(|c| c.into_float()).unwrap_or_default(),
            syp: t.take("SYP").and_then(|c| c.into_float()).unwrap_or_default(),
            etp: t.take("ETP").and_then(|c| c.into_float()).unwrap_or_default(),
            f1: t.take("F1").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            f3: t.take("F3").and_then(|c| c.into_float()).unwrap_or_default(),
            f4: t.take("F4").and_then(|c| c.into_float()).unwrap_or_default(),
            f5: t.take("F5").and_then(|c| c.into_float()).unwrap_or_default(),
            f6: t.take("F6").and_then(|c| c.into_float()).unwrap_or_default(),
            f7: t.take("F7").and_then(|c| c.into_float()).unwrap_or_default(),
            f8: t.take("F8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LAYERED_LINEAR_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_LAYERED_LINEAR_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LAYERED_LINEAR_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LINEAR_ELASTIC_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tkr: Vec<f64>,
    pub tks: Vec<f64>,
    pub tkt: Vec<f64>,
    pub rkr: Vec<f64>,
    pub rks: Vec<f64>,
    pub rkt: Vec<f64>,
    pub tdr: Vec<f64>,
    pub tds: Vec<f64>,
    pub tdt: Vec<f64>,
    pub rdr: Vec<f64>,
    pub rds: Vec<f64>,
    pub rdt: Vec<f64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
}
impl MAT_LINEAR_ELASTIC_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LINEAR_ELASTIC_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tkr: t.take("TKR").and_then(|c| c.into_float()).unwrap_or_default(),
            tks: t.take("TKS").and_then(|c| c.into_float()).unwrap_or_default(),
            tkt: t.take("TKT").and_then(|c| c.into_float()).unwrap_or_default(),
            rkr: t.take("RKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rks: t.take("RKS").and_then(|c| c.into_float()).unwrap_or_default(),
            rkt: t.take("RKT").and_then(|c| c.into_float()).unwrap_or_default(),
            tdr: t.take("TDR").and_then(|c| c.into_float()).unwrap_or_default(),
            tds: t.take("TDS").and_then(|c| c.into_float()).unwrap_or_default(),
            tdt: t.take("TDT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdr: t.take("RDR").and_then(|c| c.into_float()).unwrap_or_default(),
            rds: t.take("RDS").and_then(|c| c.into_float()).unwrap_or_default(),
            rdt: t.take("RDT").and_then(|c| c.into_float()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LOU_YOON_ANISOTROPIC_PLASTICTIY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub hr: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub iter: Vec<f64>,
    pub afr: Vec<i64>,
    pub nfunc: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub lcid: Vec<i64>,
    pub e0: Vec<f64>,
    pub lcf: Vec<i64>,
    pub p3: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub f_6: Vec<i64>,
    pub f_2_3: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub cc: Vec<f64>,
    pub f_7: Vec<i64>,
    pub pc1: Vec<f64>,
    pub pc2: Vec<f64>,
    pub pc3: Vec<f64>,
    pub pc4: Vec<f64>,
    pub pc5: Vec<f64>,
    pub pc6: Vec<f64>,
    pub pcc: Vec<f64>,
    pub f_8: Vec<i64>,
    pub vf1: Vec<f64>,
    pub vf2: Vec<f64>,
    pub vf3: Vec<f64>,
    pub vf4: Vec<f64>,
    pub vf5: Vec<f64>,
    pub f_9: Vec<i64>,
    pub f_2_4: Vec<i64>,
    pub f_3_2: Vec<i64>,
}
impl MAT_LOU_YOON_ANISOTROPIC_PLASTICTIY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LOU-YOON_ANISOTROPIC_PLASTICTIY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            hr: t.take("HR").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            iter: t.take("ITER").and_then(|c| c.into_float()).unwrap_or_default(),
            afr: t.take("AFR").and_then(|c| c.into_int()).unwrap_or_default(),
            nfunc: t.take("NFUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_6: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            f_7: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            pc1: t.take("PC1").and_then(|c| c.into_float()).unwrap_or_default(),
            pc2: t.take("PC2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc3: t.take("PC3").and_then(|c| c.into_float()).unwrap_or_default(),
            pc4: t.take("PC4").and_then(|c| c.into_float()).unwrap_or_default(),
            pc5: t.take("PC5").and_then(|c| c.into_float()).unwrap_or_default(),
            pc6: t.take("PC6").and_then(|c| c.into_float()).unwrap_or_default(),
            pcc: t.take("PCC").and_then(|c| c.into_float()).unwrap_or_default(),
            f_8: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            vf1: t.take("VF1").and_then(|c| c.into_float()).unwrap_or_default(),
            vf2: t.take("VF2").and_then(|c| c.into_float()).unwrap_or_default(),
            vf3: t.take("VF3").and_then(|c| c.into_float()).unwrap_or_default(),
            vf4: t.take("VF4").and_then(|c| c.into_float()).unwrap_or_default(),
            vf5: t.take("VF5").and_then(|c| c.into_float()).unwrap_or_default(),
            f_9: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_4: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LOW_DENSITY_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tc: Vec<f64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_LOW_DENSITY_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LOW_DENSITY_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LOW_DENSITY_SYNTHETIC_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tc: Vec<f64>,
    pub rflag: Vec<f64>,
    pub dtrt: Vec<f64>,
}
impl MAT_LOW_DENSITY_SYNTHETIC_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LOW_DENSITY_SYNTHETIC_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            rflag: t.take("RFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            dtrt: t.take("DTRT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LOW_DENSITY_SYNTHETIC_FOAM_ORTHO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub lcid2: Vec<i64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub ed: Vec<f64>,
    pub beta1: Vec<f64>,
    pub kcon: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_LOW_DENSITY_SYNTHETIC_FOAM_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LOW_DENSITY_SYNTHETIC_FOAM_ORTHO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ed: t.take("ED").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LOW_DENSITY_VISCOUS_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub lcid: Vec<i64>,
    pub tc: Vec<f64>,
    pub hu: Vec<f64>,
    pub beta: Vec<f64>,
    pub damp: Vec<f64>,
    pub shape: Vec<f64>,
    pub fail: Vec<f64>,
    pub bvflag: Vec<f64>,
    pub kcon: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nv: Vec<i64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_LOW_DENSITY_VISCOUS_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LOW_DENSITY_VISCOUS_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            bvflag: t.take("BVFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            kcon: t.take("KCON").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_LUNG_TISSUE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub delta: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub lcid: Vec<f64>,
    pub tramp: Vec<f64>,
    pub nt: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_LUNG_TISSUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_LUNG_TISSUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            delta: t.take("DELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MA_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_PRESTRAIN {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub lctsrf: Vec<i64>,
    pub eps0: Vec<f64>,
    pub triax: Vec<f64>,
    pub ips: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_MA_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_PRESTRAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MA_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_PRESTRAIN").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            lctsrf: t.take("LCTSRF").and_then(|c| c.into_int()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            triax: t.take("TRIAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ips: t.take("IPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MCCORMICK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub q1: Vec<f64>,
    pub c1: Vec<f64>,
    pub q2: Vec<f64>,
    pub c2: Vec<f64>,
    pub s: Vec<f64>,
    pub h: Vec<f64>,
    pub omega: Vec<f64>,
    pub td: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
}
impl MAT_MCCORMICK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MCCORMICK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            td: t.take("TD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MICROMECHANICS_DRY_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub v12: Vec<f64>,
    pub v23: Vec<f64>,
    pub xt: Vec<f64>,
    pub thi: Vec<f64>,
    pub thl: Vec<f64>,
    pub bfi: Vec<f64>,
    pub bwi: Vec<f64>,
    pub dscf: Vec<f64>,
    pub cnst: Vec<f64>,
    pub atlr: Vec<f64>,
    pub vmb: Vec<f64>,
    pub vme: Vec<f64>,
    pub trs: Vec<f64>,
    pub fflg: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_MICROMECHANICS_DRY_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MICROMECHANICS_DRY_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v23: t.take("V23").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("Xt").and_then(|c| c.into_float()).unwrap_or_default(),
            thi: t.take("THI").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            bfi: t.take("BFI").and_then(|c| c.into_float()).unwrap_or_default(),
            bwi: t.take("BWI").and_then(|c| c.into_float()).unwrap_or_default(),
            dscf: t.take("DSCF").and_then(|c| c.into_float()).unwrap_or_default(),
            cnst: t.take("CNST").and_then(|c| c.into_float()).unwrap_or_default(),
            atlr: t.take("ATLR").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            vme: t.take("VME").and_then(|c| c.into_float()).unwrap_or_default(),
            trs: t.take("TRS").and_then(|c| c.into_float()).unwrap_or_default(),
            fflg: t.take("FFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_CRUSHABLE_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tid: Vec<i64>,
    pub tsc: Vec<f64>,
    pub damp: Vec<f64>,
    pub ncycle: Vec<f64>,
    pub srclmt: Vec<f64>,
    pub srflag: Vec<i64>,
}
impl MAT_MODIFIED_CRUSHABLE_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_CRUSHABLE_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tid: t.take("TID").and_then(|c| c.into_int()).unwrap_or_default(),
            tsc: t.take("TSC").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            ncycle: t.take("NCYCLE").and_then(|c| c.into_float()).unwrap_or_default(),
            srclmt: t.take("SRCLMT").and_then(|c| c.into_float()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_FORCE_LIMITED {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub df: Vec<f64>,
    pub iaflc: Vec<i64>,
    pub ytflag: Vec<f64>,
    pub asoft: Vec<f64>,
    pub m1: Vec<f64>,
    pub m2: Vec<f64>,
    pub m3: Vec<f64>,
    pub m4: Vec<f64>,
    pub m5: Vec<f64>,
    pub m6: Vec<f64>,
    pub m7: Vec<f64>,
    pub m8: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub lc5: Vec<i64>,
    pub lc6: Vec<i64>,
    pub lc7: Vec<i64>,
    pub lc8: Vec<i64>,
    pub lps1: Vec<i64>,
    pub sfs1: Vec<f64>,
    pub lps2: Vec<i64>,
    pub sfs2: Vec<f64>,
    pub yms1: Vec<f64>,
    pub yms2: Vec<f64>,
    pub lpt1: Vec<i64>,
    pub sft1: Vec<f64>,
    pub lpt2: Vec<i64>,
    pub sft2: Vec<f64>,
    pub ymt1: Vec<f64>,
    pub ymt2: Vec<f64>,
    pub lpr: Vec<i64>,
    pub sfr: Vec<f64>,
    pub ymr: Vec<f64>,
    pub lys1: Vec<i64>,
    pub sys1: Vec<f64>,
    pub lys2: Vec<i64>,
    pub sys2: Vec<f64>,
    pub lyt1: Vec<i64>,
    pub syt1: Vec<f64>,
    pub lyt2: Vec<i64>,
    pub syt2: Vec<f64>,
    pub lyr: Vec<i64>,
    pub syr: Vec<f64>,
    pub hms1_1: Vec<f64>,
    pub hms1_2: Vec<f64>,
    pub hms1_3: Vec<f64>,
    pub hms1_4: Vec<f64>,
    pub hms1_5: Vec<f64>,
    pub hms1_6: Vec<f64>,
    pub hms1_7: Vec<f64>,
    pub hms1_8: Vec<f64>,
    pub lpms1_1: Vec<i64>,
    pub lpms1_2: Vec<i64>,
    pub lpms1_3: Vec<i64>,
    pub lpms1_4: Vec<i64>,
    pub lpms1_5: Vec<i64>,
    pub lpms1_6: Vec<i64>,
    pub lpms1_7: Vec<i64>,
    pub lpms1_8: Vec<i64>,
    pub hms2_1: Vec<f64>,
    pub hms2_2: Vec<f64>,
    pub hms2_3: Vec<f64>,
    pub hms2_4: Vec<f64>,
    pub hms2_5: Vec<f64>,
    pub hms2_6: Vec<f64>,
    pub hms2_7: Vec<f64>,
    pub hms2_8: Vec<f64>,
    pub lpms2_1: Vec<i64>,
    pub lpms2_2: Vec<i64>,
    pub lpms2_3: Vec<i64>,
    pub lpms2_4: Vec<i64>,
    pub lpms2_5: Vec<i64>,
    pub lpms2_6: Vec<i64>,
    pub lpms2_7: Vec<i64>,
    pub lpms2_8: Vec<i64>,
    pub hmt1_1: Vec<f64>,
    pub hmt1_2: Vec<f64>,
    pub hmt1_3: Vec<f64>,
    pub hmt1_4: Vec<f64>,
    pub hmt1_5: Vec<f64>,
    pub hmt1_6: Vec<f64>,
    pub hmt1_7: Vec<f64>,
    pub hmt1_8: Vec<f64>,
    pub lpmt1_1: Vec<i64>,
    pub lpmt1_2: Vec<i64>,
    pub lpmt1_3: Vec<i64>,
    pub lpmt1_4: Vec<i64>,
    pub lpmt1_5: Vec<i64>,
    pub lpmt1_6: Vec<i64>,
    pub lpmt1_7: Vec<i64>,
    pub lpmt1_8: Vec<i64>,
    pub hmt2_1: Vec<f64>,
    pub hmt2_2: Vec<f64>,
    pub hmt2_3: Vec<f64>,
    pub hmt2_4: Vec<f64>,
    pub hmt2_5: Vec<f64>,
    pub hmt2_6: Vec<f64>,
    pub hmt2_7: Vec<f64>,
    pub hmt2_8: Vec<f64>,
    pub lpmt2_1: Vec<i64>,
    pub lpmt2_2: Vec<i64>,
    pub lpmt2_3: Vec<i64>,
    pub lpmt2_4: Vec<i64>,
    pub lpmt2_5: Vec<i64>,
    pub lpmt2_6: Vec<i64>,
    pub lpmt2_7: Vec<i64>,
    pub lpmt2_8: Vec<i64>,
    pub hmr_1: Vec<f64>,
    pub hmr_2: Vec<f64>,
    pub hmr_3: Vec<f64>,
    pub hmr_4: Vec<f64>,
    pub hmr_5: Vec<f64>,
    pub hmr_6: Vec<f64>,
    pub hmr_7: Vec<f64>,
    pub hmr_8: Vec<f64>,
    pub lpmr_1: Vec<i64>,
    pub lpmr_2: Vec<i64>,
    pub lpmr_3: Vec<i64>,
    pub lpmr_4: Vec<i64>,
    pub lpmr_5: Vec<i64>,
    pub lpmr_6: Vec<i64>,
    pub lpmr_7: Vec<i64>,
    pub lpmr_8: Vec<i64>,
}
impl MAT_MODIFIED_FORCE_LIMITED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_FORCE_LIMITED").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            df: t.take("DF").and_then(|c| c.into_float()).unwrap_or_default(),
            iaflc: t.take("IAFLC").and_then(|c| c.into_int()).unwrap_or_default(),
            ytflag: t.take("YTFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            asoft: t.take("ASOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            m1: t.take("M1").and_then(|c| c.into_float()).unwrap_or_default(),
            m2: t.take("M2").and_then(|c| c.into_float()).unwrap_or_default(),
            m3: t.take("M3").and_then(|c| c.into_float()).unwrap_or_default(),
            m4: t.take("M4").and_then(|c| c.into_float()).unwrap_or_default(),
            m5: t.take("M5").and_then(|c| c.into_float()).unwrap_or_default(),
            m6: t.take("M6").and_then(|c| c.into_float()).unwrap_or_default(),
            m7: t.take("M7").and_then(|c| c.into_float()).unwrap_or_default(),
            m8: t.take("M8").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            lc5: t.take("LC5").and_then(|c| c.into_int()).unwrap_or_default(),
            lc6: t.take("LC6").and_then(|c| c.into_int()).unwrap_or_default(),
            lc7: t.take("LC7").and_then(|c| c.into_int()).unwrap_or_default(),
            lc8: t.take("LC8").and_then(|c| c.into_int()).unwrap_or_default(),
            lps1: t.take("LPS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs1: t.take("SFS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lps2: t.take("LPS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs2: t.take("SFS2").and_then(|c| c.into_float()).unwrap_or_default(),
            yms1: t.take("YMS1").and_then(|c| c.into_float()).unwrap_or_default(),
            yms2: t.take("YMS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt1: t.take("LPT1").and_then(|c| c.into_int()).unwrap_or_default(),
            sft1: t.take("SFT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lpt2: t.take("LPT2").and_then(|c| c.into_int()).unwrap_or_default(),
            sft2: t.take("SFT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt1: t.take("YMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            ymt2: t.take("YMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lpr: t.take("LPR").and_then(|c| c.into_int()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymr: t.take("YMR").and_then(|c| c.into_float()).unwrap_or_default(),
            lys1: t.take("LYS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sys1: t.take("SYS1").and_then(|c| c.into_float()).unwrap_or_default(),
            lys2: t.take("LYS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sys2: t.take("SYS2").and_then(|c| c.into_float()).unwrap_or_default(),
            lyt1: t.take("LYT1").and_then(|c| c.into_int()).unwrap_or_default(),
            syt1: t.take("SYT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lyt2: t.take("LYT2").and_then(|c| c.into_int()).unwrap_or_default(),
            syt2: t.take("SYT2").and_then(|c| c.into_float()).unwrap_or_default(),
            lyr: t.take("LYR").and_then(|c| c.into_int()).unwrap_or_default(),
            syr: t.take("SYR").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_1: t.take("HMS1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_2: t.take("HMS1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_3: t.take("HMS1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_4: t.take("HMS1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_5: t.take("HMS1_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_6: t.take("HMS1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_7: t.take("HMS1_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hms1_8: t.take("HMS1_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpms1_1: t.take("LPMS1_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_2: t.take("LPMS1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_3: t.take("LPMS1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_4: t.take("LPMS1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_5: t.take("LPMS1_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_6: t.take("LPMS1_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_7: t.take("LPMS1_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms1_8: t.take("LPMS1_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hms2_1: t.take("HMS2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_2: t.take("HMS2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_3: t.take("HMS2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_4: t.take("HMS2_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_5: t.take("HMS2_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_6: t.take("HMS2_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_7: t.take("HMS2_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hms2_8: t.take("HMS2_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpms2_1: t.take("LPMS2_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_2: t.take("LPMS2_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_3: t.take("LPMS2_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_4: t.take("LPMS2_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_5: t.take("LPMS2_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_6: t.take("LPMS2_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_7: t.take("LPMS2_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpms2_8: t.take("LPMS2_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmt1_1: t.take("HMT1_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_2: t.take("HMT1_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_3: t.take("HMT1_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_4: t.take("HMT1_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_5: t.take("HMT1_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_6: t.take("HMT1_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_7: t.take("HMT1_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt1_8: t.take("HMT1_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmt1_1: t.take("LPMT1_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_2: t.take("LPMT1_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_3: t.take("LPMT1_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_4: t.take("LPMT1_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_5: t.take("LPMT1_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_6: t.take("LPMT1_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_7: t.take("LPMT1_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt1_8: t.take("LPMT1_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmt2_1: t.take("HMT2_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_2: t.take("HMT2_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_3: t.take("HMT2_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_4: t.take("HMT2_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_5: t.take("HMT2_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_6: t.take("HMT2_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_7: t.take("HMT2_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmt2_8: t.take("HMT2_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmt2_1: t.take("LPMT2_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_2: t.take("LPMT2_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_3: t.take("LPMT2_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_4: t.take("LPMT2_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_5: t.take("LPMT2_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_6: t.take("LPMT2_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_7: t.take("LPMT2_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmt2_8: t.take("LPMT2_8").and_then(|c| c.into_int()).unwrap_or_default(),
            hmr_1: t.take("HMR_1").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_2: t.take("HMR_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_3: t.take("HMR_3").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_4: t.take("HMR_4").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_5: t.take("HMR_5").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_6: t.take("HMR_6").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_7: t.take("HMR_7").and_then(|c| c.into_float()).unwrap_or_default(),
            hmr_8: t.take("HMR_8").and_then(|c| c.into_float()).unwrap_or_default(),
            lpmr_1: t.take("LPMR_1").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_2: t.take("LPMR_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_3: t.take("LPMR_3").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_4: t.take("LPMR_4").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_5: t.take("LPMR_5").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_6: t.take("LPMR_6").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_7: t.take("LPMR_7").and_then(|c| c.into_int()).unwrap_or_default(),
            lpmr_8: t.take("LPMR_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_HONEYCOMB {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vf: Vec<f64>,
    pub mu: Vec<f64>,
    pub bulk: Vec<f64>,
    pub lca: Vec<i64>,
    pub lcb: Vec<i64>,
    pub lcc: Vec<i64>,
    pub lcs: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcbc: Vec<i64>,
    pub lcca: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eaau: Vec<f64>,
    pub ebbu: Vec<f64>,
    pub eccu: Vec<f64>,
    pub gabu: Vec<f64>,
    pub gbcu: Vec<f64>,
    pub gcau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub tsef: Vec<f64>,
    pub ssef: Vec<f64>,
    pub vref: Vec<f64>,
    pub tref: Vec<f64>,
    pub shdflg: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub lcsra: Vec<f64>,
    pub lcsrb: Vec<f64>,
    pub lcsrc: Vec<f64>,
    pub lcsrab: Vec<f64>,
    pub lcsrbc: Vec<f64>,
    pub lcsrca: Vec<f64>,
}
impl MAT_MODIFIED_HONEYCOMB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_HONEYCOMB").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            lca: t.take("LCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcb: t.take("LCB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcs: t.take("LCS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbc: t.take("LCBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcca: t.take("LCCA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eaau: t.take("EAAU").and_then(|c| c.into_float()).unwrap_or_default(),
            ebbu: t.take("EBBU").and_then(|c| c.into_float()).unwrap_or_default(),
            eccu: t.take("ECCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gabu: t.take("GABU").and_then(|c| c.into_float()).unwrap_or_default(),
            gbcu: t.take("GBCU").and_then(|c| c.into_float()).unwrap_or_default(),
            gcau: t.take("GCAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            tsef: t.take("TSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            ssef: t.take("SSEF").and_then(|c| c.into_float()).unwrap_or_default(),
            vref: t.take("VREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            shdflg: t.take("SHDFLG").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsra: t.take("LCSRA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsrb: t.take("LCSRB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsrc: t.take("LCSRC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsrab: t.take("LCSRAB").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsrbc: t.take("LCSRBC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsrca: t.take("LCSRCA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_JOHNSON_COOK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub beta: Vec<f64>,
    pub xsi: Vec<f64>,
    pub cp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub e0dot: Vec<f64>,
    pub tr: Vec<f64>,
    pub tm: Vec<f64>,
    pub t0: Vec<f64>,
    pub flag1: Vec<f64>,
    pub flag2: Vec<f64>,
    pub a_siga: Vec<f64>,
    pub b_b: Vec<f64>,
    pub n_beta0: Vec<f64>,
    pub c_beta1: Vec<f64>,
    pub m_na: Vec<f64>,
    pub q1_a: Vec<f64>,
    pub c1_n: Vec<f64>,
    pub q2_alpha0: Vec<f64>,
    pub c2_alpha1: Vec<f64>,
    pub dc_dc: Vec<f64>,
    pub pd_wc: Vec<f64>,
    pub d1_na: Vec<f64>,
    pub d2_na: Vec<f64>,
    pub d3_na: Vec<f64>,
    pub d4_na: Vec<f64>,
    pub d5_na: Vec<f64>,
    pub tc: Vec<f64>,
    pub tauc: Vec<f64>,
}
impl MAT_MODIFIED_JOHNSON_COOK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_JOHNSON_COOK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xsi: t.take("XSI").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            e0dot: t.take("E0DOT").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("Tr").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("Tm").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            flag1: t.take("FLAG1").and_then(|c| c.into_float()).unwrap_or_default(),
            flag2: t.take("FLAG2").and_then(|c| c.into_float()).unwrap_or_default(),
            a_siga: t.take("A/SIGA").and_then(|c| c.into_float()).unwrap_or_default(),
            b_b: t.take("B/B").and_then(|c| c.into_float()).unwrap_or_default(),
            n_beta0: t.take("N/BETA0").and_then(|c| c.into_float()).unwrap_or_default(),
            c_beta1: t.take("C/BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_na: t.take("m/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            q1_a: t.take("Q1/A").and_then(|c| c.into_float()).unwrap_or_default(),
            c1_n: t.take("C1/N").and_then(|c| c.into_float()).unwrap_or_default(),
            q2_alpha0: t.take("Q2/ALPHA0").and_then(|c| c.into_float()).unwrap_or_default(),
            c2_alpha1: t.take("C2/ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            dc_dc: t.take("DC/DC").and_then(|c| c.into_float()).unwrap_or_default(),
            pd_wc: t.take("PD/WC").and_then(|c| c.into_float()).unwrap_or_default(),
            d1_na: t.take("D1/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            d2_na: t.take("D2/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            d3_na: t.take("D3/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            d4_na: t.take("D4/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            d5_na: t.take("D5/NA").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            tauc: t.take("TAUC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RATE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub lctsrf: Vec<i64>,
    pub eps0: Vec<f64>,
    pub triax: Vec<f64>,
    pub ips: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RATE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            lctsrf: t.take("LCTSRF").and_then(|c| c.into_int()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            triax: t.take("TRIAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ips: t.take("IPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RTCL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub lctsrf: Vec<i64>,
    pub eps0: Vec<f64>,
    pub triax: Vec<f64>,
    pub ips: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RTCL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_RTCL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            lctsrf: t.take("LCTSRF").and_then(|c| c.into_int()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            triax: t.take("TRIAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ips: t.take("IPS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub epsthin: Vec<f64>,
    pub epsmaj: Vec<f64>,
    pub numint: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsthin: t.take("EPSTHIN").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmaj: t.take("EPSMAJ").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MODIFIED_ZERILLI_ARMSTRONG {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub e0: Vec<f64>,
    pub n: Vec<f64>,
    pub troom: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub efail: Vec<f64>,
    pub vp: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub g1: Vec<f64>,
    pub g2: Vec<f64>,
    pub g3: Vec<f64>,
    pub g4: Vec<f64>,
    pub bulk: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_MODIFIED_ZERILLI_ARMSTRONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MODIFIED_ZERILLI_ARMSTRONG").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            troom: t.take("TROOM").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOHR_COULOMB {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gmod: Vec<f64>,
    pub rnu: Vec<f64>,
    pub f40: Vec<i64>,
    pub phi: Vec<f64>,
    pub cval: Vec<f64>,
    pub psi: Vec<f64>,
    pub novoid: Vec<i64>,
    pub nplanes: Vec<i64>,
    pub extra: Vec<i64>,
    pub lccpdr: Vec<i64>,
    pub lccpt: Vec<i64>,
    pub lccjdr: Vec<i64>,
    pub lccjt: Vec<i64>,
    pub lcsfac: Vec<i64>,
    pub gmoddp: Vec<f64>,
    pub gmodgr: Vec<f64>,
    pub lcgmep: Vec<i64>,
    pub lcphiep: Vec<i64>,
    pub lcpsiep: Vec<i64>,
    pub lcgmst: Vec<i64>,
    pub cvalgr: Vec<f64>,
    pub aniso: Vec<f64>,
    pub lcgmt: Vec<f64>,
    pub lccvt: Vec<f64>,
    pub lcpht: Vec<f64>,
    pub epdam1: Vec<f64>,
    pub epdam2: Vec<f64>,
    pub dip: Vec<f64>,
    pub dipang: Vec<f64>,
    pub cplane: Vec<f64>,
    pub frplane: Vec<f64>,
    pub tplane: Vec<f64>,
    pub shrmax: Vec<f64>,
    pub local: Vec<i64>,
}
impl MAT_MOHR_COULOMB {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOHR_COULOMB").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            cval: t.take("CVAL").and_then(|c| c.into_float()).unwrap_or_default(),
            psi: t.take("PSI").and_then(|c| c.into_float()).unwrap_or_default(),
            novoid: t.take("NOVOID").and_then(|c| c.into_int()).unwrap_or_default(),
            nplanes: t.take("NPLANES").and_then(|c| c.into_int()).unwrap_or_default(),
            extra: t.take("EXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpdr: t.take("LCCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccpt: t.take("LCCPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjdr: t.take("LCCJDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lccjt: t.take("LCCJT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsfac: t.take("LCSFAC").and_then(|c| c.into_int()).unwrap_or_default(),
            gmoddp: t.take("GMODDP").and_then(|c| c.into_float()).unwrap_or_default(),
            gmodgr: t.take("GMODGR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcgmep: t.take("LCGMEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcphiep: t.take("LCPHIEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpsiep: t.take("LCPSIEP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcgmst: t.take("LCGMST").and_then(|c| c.into_int()).unwrap_or_default(),
            cvalgr: t.take("CVALGR").and_then(|c| c.into_float()).unwrap_or_default(),
            aniso: t.take("ANISO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcgmt: t.take("LCGMT").and_then(|c| c.into_float()).unwrap_or_default(),
            lccvt: t.take("LCCVT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpht: t.take("LCPHT").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam1: t.take("EPDAM1").and_then(|c| c.into_float()).unwrap_or_default(),
            epdam2: t.take("EPDAM2").and_then(|c| c.into_float()).unwrap_or_default(),
            dip: t.take("DIP").and_then(|c| c.into_float()).unwrap_or_default(),
            dipang: t.take("DIPANG").and_then(|c| c.into_float()).unwrap_or_default(),
            cplane: t.take("CPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            frplane: t.take("FRPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            tplane: t.take("TPLANE").and_then(|c| c.into_float()).unwrap_or_default(),
            shrmax: t.take("SHRMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOHR_NON_ASSOCIATED_FLOW {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub p12: Vec<f64>,
    pub p22: Vec<f64>,
    pub p33: Vec<f64>,
    pub g12: Vec<f64>,
    pub g22: Vec<f64>,
    pub g33: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lfld: Vec<i64>,
    pub lfrac: Vec<i64>,
    pub w0: Vec<f64>,
    pub a: Vec<f64>,
    pub b0: Vec<f64>,
    pub gamma: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub scale: Vec<f64>,
    pub size0: Vec<f64>,
    pub tref: Vec<f64>,
    pub tmelt: Vec<f64>,
    pub m: Vec<f64>,
    pub eta: Vec<f64>,
    pub cp: Vec<f64>,
    pub tini: Vec<f64>,
    pub depso: Vec<f64>,
    pub depsad: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_MOHR_NON_ASSOCIATED_FLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOHR_NON_ASSOCIATED_FLOW").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            p12: t.take("P12").and_then(|c| c.into_float()).unwrap_or_default(),
            p22: t.take("P22").and_then(|c| c.into_float()).unwrap_or_default(),
            p33: t.take("P33").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g22: t.take("G22").and_then(|c| c.into_float()).unwrap_or_default(),
            g33: t.take("G33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lfld: t.take("LFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            lfrac: t.take("LFRAC").and_then(|c| c.into_int()).unwrap_or_default(),
            w0: t.take("W0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            size0: t.take("SIZE0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tmelt: t.take("TMELT").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tini: t.take("TINI").and_then(|c| c.into_float()).unwrap_or_default(),
            depso: t.take("DEPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            depsad: t.take("DEPSAD").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOHR_NON_ASSOCIATED_FLOW_XUE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub p12: Vec<f64>,
    pub p22: Vec<f64>,
    pub p33: Vec<f64>,
    pub g12: Vec<f64>,
    pub g22: Vec<f64>,
    pub g33: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lfld: Vec<i64>,
    pub lfrac: Vec<i64>,
    pub w0: Vec<f64>,
    pub a: Vec<f64>,
    pub b0: Vec<f64>,
    pub gamma: Vec<f64>,
    pub c: Vec<f64>,
    pub n: Vec<f64>,
    pub scale: Vec<f64>,
    pub size0: Vec<f64>,
    pub tref: Vec<f64>,
    pub tmelt: Vec<f64>,
    pub m: Vec<f64>,
    pub eta: Vec<f64>,
    pub cp: Vec<f64>,
    pub tini: Vec<f64>,
    pub depso: Vec<f64>,
    pub depsad: Vec<f64>,
    pub ef0: Vec<f64>,
    pub plim: Vec<f64>,
    pub q: Vec<f64>,
    pub gama: Vec<f64>,
    pub m_2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_MOHR_NON_ASSOCIATED_FLOW_XUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOHR_NON_ASSOCIATED_FLOW_XUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            p12: t.take("P12").and_then(|c| c.into_float()).unwrap_or_default(),
            p22: t.take("P22").and_then(|c| c.into_float()).unwrap_or_default(),
            p33: t.take("P33").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g22: t.take("G22").and_then(|c| c.into_float()).unwrap_or_default(),
            g33: t.take("G33").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lfld: t.take("LFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            lfrac: t.take("LFRAC").and_then(|c| c.into_int()).unwrap_or_default(),
            w0: t.take("W0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            size0: t.take("SIZE0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            tmelt: t.take("TMELT").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tini: t.take("TINI").and_then(|c| c.into_float()).unwrap_or_default(),
            depso: t.take("DEPSO").and_then(|c| c.into_float()).unwrap_or_default(),
            depsad: t.take("DEPSAD").and_then(|c| c.into_float()).unwrap_or_default(),
            ef0: t.take("EF0").and_then(|c| c.into_float()).unwrap_or_default(),
            plim: t.take("PLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            m_2: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOMENT_CURVATURE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub elaf: Vec<i64>,
    pub epflg: Vec<i64>,
    pub cta: Vec<f64>,
    pub ctb: Vec<f64>,
    pub ctt: Vec<f64>,
    pub n1: Vec<f64>,
    pub n2: Vec<f64>,
    pub n3: Vec<f64>,
    pub n4: Vec<f64>,
    pub n5: Vec<f64>,
    pub n6: Vec<f64>,
    pub n7: Vec<f64>,
    pub n8: Vec<f64>,
    pub lcms1: Vec<i64>,
    pub lcms2: Vec<i64>,
    pub lcms3: Vec<i64>,
    pub lcms4: Vec<i64>,
    pub lcms5: Vec<i64>,
    pub lcms6: Vec<i64>,
    pub lcms7: Vec<i64>,
    pub lcms8: Vec<i64>,
    pub lcmt1: Vec<i64>,
    pub lcmt2: Vec<i64>,
    pub lcmt3: Vec<i64>,
    pub lcmt4: Vec<i64>,
    pub lcmt5: Vec<i64>,
    pub lcmt6: Vec<i64>,
    pub lcmt7: Vec<i64>,
    pub lcmt8: Vec<i64>,
    pub lct1: Vec<i64>,
    pub lct2: Vec<i64>,
    pub lct3: Vec<i64>,
    pub lct4: Vec<i64>,
    pub lct5: Vec<i64>,
    pub lct6: Vec<i64>,
    pub lct7: Vec<i64>,
    pub lct8: Vec<i64>,
    pub cfa: Vec<f64>,
    pub cfb: Vec<f64>,
    pub cft: Vec<f64>,
    pub hrule: Vec<f64>,
    pub reps: Vec<f64>,
    pub rbeta: Vec<f64>,
    pub rcapay: Vec<f64>,
    pub rcapaz: Vec<f64>,
}
impl MAT_MOMENT_CURVATURE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOMENT_CURVATURE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            elaf: t.take("ELAF").and_then(|c| c.into_int()).unwrap_or_default(),
            epflg: t.take("EPFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            cta: t.take("CTA").and_then(|c| c.into_float()).unwrap_or_default(),
            ctb: t.take("CTB").and_then(|c| c.into_float()).unwrap_or_default(),
            ctt: t.take("CTT").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_float()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_float()).unwrap_or_default(),
            n5: t.take("N5").and_then(|c| c.into_float()).unwrap_or_default(),
            n6: t.take("N6").and_then(|c| c.into_float()).unwrap_or_default(),
            n7: t.take("N7").and_then(|c| c.into_float()).unwrap_or_default(),
            n8: t.take("N8").and_then(|c| c.into_float()).unwrap_or_default(),
            lcms1: t.take("LCMS1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms2: t.take("LCMS2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms3: t.take("LCMS3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms4: t.take("LCMS4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms5: t.take("LCMS5").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms6: t.take("LCMS6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms7: t.take("LCMS7").and_then(|c| c.into_int()).unwrap_or_default(),
            lcms8: t.take("LCMS8").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt1: t.take("LCMT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt2: t.take("LCMT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt3: t.take("LCMT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt4: t.take("LCMT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt5: t.take("LCMT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt6: t.take("LCMT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt7: t.take("LCMT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmt8: t.take("LCMT8").and_then(|c| c.into_int()).unwrap_or_default(),
            lct1: t.take("LCT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lct2: t.take("LCT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lct3: t.take("LCT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lct4: t.take("LCT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lct5: t.take("LCT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lct6: t.take("LCT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lct7: t.take("LCT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lct8: t.take("LCT8").and_then(|c| c.into_int()).unwrap_or_default(),
            cfa: t.take("CFA").and_then(|c| c.into_float()).unwrap_or_default(),
            cfb: t.take("CFB").and_then(|c| c.into_float()).unwrap_or_default(),
            cft: t.take("CFT").and_then(|c| c.into_float()).unwrap_or_default(),
            hrule: t.take("HRULE").and_then(|c| c.into_float()).unwrap_or_default(),
            reps: t.take("REPS").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta: t.take("RBETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rcapay: t.take("RCAPAY").and_then(|c| c.into_float()).unwrap_or_default(),
            rcapaz: t.take("RCAPAZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOONEY_RIVLIN_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr1: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl1: Vec<f64>,
    pub sw1: Vec<f64>,
    pub st1: Vec<f64>,
    pub lcid1: Vec<f64>,
    pub f0: Vec<f64>,
    pub f10: Vec<f64>,
    pub pr2: Vec<f64>,
    pub a2: Vec<f64>,
    pub b2: Vec<f64>,
    pub sgl1_2: Vec<f64>,
    pub sw1_2: Vec<f64>,
    pub st1_2: Vec<f64>,
    pub lcid1_2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_MOONEY_RIVLIN_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOONEY-RIVLIN_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1_2: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1_2: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1_2: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1_2: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOONEY_RIVLIN_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid: Vec<i64>,
}
impl MAT_MOONEY_RIVLIN_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOONEY-RIVLIN_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MOONEY_RIVLIN_RUBBER_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr1: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub ref_: Vec<f64>,
    pub sgl1: Vec<f64>,
    pub sw1: Vec<f64>,
    pub st1: Vec<f64>,
    pub lcid1: Vec<f64>,
    pub f0: Vec<f64>,
    pub f10: Vec<f64>,
    pub pr2: Vec<f64>,
    pub a2: Vec<f64>,
    pub b2: Vec<f64>,
    pub sgl1_2: Vec<f64>,
    pub sw1_2: Vec<f64>,
    pub st1_2: Vec<f64>,
    pub lcid1_2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_MOONEY_RIVLIN_RUBBER_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MOONEY-RIVLIN_RUBBER_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr1: t.take("PR1").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            f0: t.take("f0").and_then(|c| c.into_float()).unwrap_or_default(),
            f10: t.take("f10").and_then(|c| c.into_float()).unwrap_or_default(),
            pr2: t.take("PR2").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl1_2: t.take("SGL1").and_then(|c| c.into_float()).unwrap_or_default(),
            sw1_2: t.take("SW1").and_then(|c| c.into_float()).unwrap_or_default(),
            st1_2: t.take("ST1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1_2: t.take("LCID1").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MTS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub siga: Vec<f64>,
    pub sigi: Vec<f64>,
    pub sigs: Vec<f64>,
    pub sig0: Vec<f64>,
    pub bulk: Vec<f64>,
    pub hf0: Vec<f64>,
    pub hf1: Vec<f64>,
    pub hf2: Vec<f64>,
    pub sigs0: Vec<f64>,
    pub edots0: Vec<f64>,
    pub burg: Vec<f64>,
    pub capa: Vec<f64>,
    pub boltz: Vec<f64>,
    pub sm0: Vec<f64>,
    pub sm1: Vec<f64>,
    pub sm2: Vec<f64>,
    pub edot0: Vec<f64>,
    pub go: Vec<f64>,
    pub pinv: Vec<f64>,
    pub qinv: Vec<f64>,
    pub edoti: Vec<f64>,
    pub g0i: Vec<f64>,
    pub pinvi: Vec<f64>,
    pub qinvi: Vec<f64>,
    pub edots: Vec<f64>,
    pub g0s: Vec<f64>,
    pub pinvs: Vec<f64>,
    pub qinvs: Vec<f64>,
    pub rhocpr: Vec<f64>,
    pub temprf: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
}
impl MAT_MTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MTS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            siga: t.take("SIGA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigi: t.take("SIGI").and_then(|c| c.into_float()).unwrap_or_default(),
            sigs: t.take("SIGS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            hf0: t.take("HF0").and_then(|c| c.into_float()).unwrap_or_default(),
            hf1: t.take("HF1").and_then(|c| c.into_float()).unwrap_or_default(),
            hf2: t.take("HF2").and_then(|c| c.into_float()).unwrap_or_default(),
            sigs0: t.take("SIGS0").and_then(|c| c.into_float()).unwrap_or_default(),
            edots0: t.take("EDOTS0").and_then(|c| c.into_float()).unwrap_or_default(),
            burg: t.take("BURG").and_then(|c| c.into_float()).unwrap_or_default(),
            capa: t.take("CAPA").and_then(|c| c.into_float()).unwrap_or_default(),
            boltz: t.take("BOLTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            sm0: t.take("SM0").and_then(|c| c.into_float()).unwrap_or_default(),
            sm1: t.take("SM1").and_then(|c| c.into_float()).unwrap_or_default(),
            sm2: t.take("SM2").and_then(|c| c.into_float()).unwrap_or_default(),
            edot0: t.take("EDOT0").and_then(|c| c.into_float()).unwrap_or_default(),
            go: t.take("GO").and_then(|c| c.into_float()).unwrap_or_default(),
            pinv: t.take("PINV").and_then(|c| c.into_float()).unwrap_or_default(),
            qinv: t.take("QINV").and_then(|c| c.into_float()).unwrap_or_default(),
            edoti: t.take("EDOTI").and_then(|c| c.into_float()).unwrap_or_default(),
            g0i: t.take("G0I").and_then(|c| c.into_float()).unwrap_or_default(),
            pinvi: t.take("PINVI").and_then(|c| c.into_float()).unwrap_or_default(),
            qinvi: t.take("QINVI").and_then(|c| c.into_float()).unwrap_or_default(),
            edots: t.take("EDOTS").and_then(|c| c.into_float()).unwrap_or_default(),
            g0s: t.take("G0S").and_then(|c| c.into_float()).unwrap_or_default(),
            pinvs: t.take("PINVS").and_then(|c| c.into_float()).unwrap_or_default(),
            qinvs: t.take("QINVS").and_then(|c| c.into_float()).unwrap_or_default(),
            rhocpr: t.take("RHOCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            temprf: t.take("TEMPRF").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_MUSCLE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub sno: Vec<f64>,
    pub srm: Vec<f64>,
    pub pis: Vec<f64>,
    pub ssm: Vec<f64>,
    pub cer: Vec<f64>,
    pub dmp: Vec<f64>,
    pub alm: Vec<f64>,
    pub sfr: Vec<f64>,
    pub svs: Vec<f64>,
    pub svr: Vec<f64>,
    pub ssp: Vec<f64>,
}
impl MAT_MUSCLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_MUSCLE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            sno: t.take("SNO").and_then(|c| c.into_float()).unwrap_or_default(),
            srm: t.take("SRM").and_then(|c| c.into_float()).unwrap_or_default(),
            pis: t.take("PIS").and_then(|c| c.into_float()).unwrap_or_default(),
            ssm: t.take("SSM").and_then(|c| c.into_float()).unwrap_or_default(),
            cer: t.take("CER").and_then(|c| c.into_float()).unwrap_or_default(),
            dmp: t.take("DMP").and_then(|c| c.into_float()).unwrap_or_default(),
            alm: t.take("ALM").and_then(|c| c.into_float()).unwrap_or_default(),
            sfr: t.take("SFR").and_then(|c| c.into_float()).unwrap_or_default(),
            svs: t.take("SVS").and_then(|c| c.into_float()).unwrap_or_default(),
            svr: t.take("SVR").and_then(|c| c.into_float()).unwrap_or_default(),
            ssp: t.take("SSP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NONLINEAR_ELASTIC_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub lcidtr: Vec<i64>,
    pub lcidts: Vec<i64>,
    pub lcidtt: Vec<i64>,
    pub lcidrr: Vec<i64>,
    pub lcidrs: Vec<i64>,
    pub lcidrt: Vec<i64>,
    pub lcidtdr: Vec<i64>,
    pub lcidtds: Vec<i64>,
    pub lcidtdt: Vec<i64>,
    pub lcidrdr: Vec<i64>,
    pub lcidrds: Vec<i64>,
    pub lcidrdt: Vec<i64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
    pub ffailr: Vec<f64>,
    pub ffails: Vec<f64>,
    pub ffailt: Vec<f64>,
    pub mfailr: Vec<f64>,
    pub mfails: Vec<f64>,
    pub mfailt: Vec<f64>,
    pub ufailr: Vec<f64>,
    pub ufails: Vec<f64>,
    pub ufailt: Vec<f64>,
    pub tfailr: Vec<f64>,
    pub tfails: Vec<f64>,
    pub tfailt: Vec<f64>,
}
impl MAT_NONLINEAR_ELASTIC_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NONLINEAR_ELASTIC_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidtr: t.take("LCIDTR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidts: t.take("LCIDTS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtt: t.take("LCIDTT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrr: t.take("LCIDRR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrs: t.take("LCIDRS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrt: t.take("LCIDRT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdr: t.take("LCIDTDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtds: t.take("LCIDTDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidtdt: t.take("LCIDTDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdr: t.take("LCIDRDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrds: t.take("LCIDRDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidrdt: t.take("LCIDRDT").and_then(|c| c.into_int()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailr: t.take("FFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ffails: t.take("FFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailt: t.take("FFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailr: t.take("MFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            mfails: t.take("MFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailt: t.take("MFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailr: t.take("UFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ufails: t.take("UFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailt: t.take("UFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailr: t.take("TFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            tfails: t.take("TFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailt: t.take("TFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NONLINEAR_ORTHOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub dt: Vec<f64>,
    pub tramp: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcida: Vec<f64>,
    pub lcidb: Vec<f64>,
    pub efail: Vec<f64>,
    pub dtfail: Vec<f64>,
    pub cdamp: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub atrack: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidab: Vec<i64>,
    pub lcidbc: Vec<i64>,
    pub lcidca: Vec<i64>,
}
impl MAT_NONLINEAR_ORTHOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NONLINEAR_ORTHOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            dtfail: t.take("DTFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            cdamp: t.take("CDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            atrack: t.take("ATRACK").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidab: t.take("LCIDAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidbc: t.take("LCIDBC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidca: t.take("LCIDCA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NONLINEAR_PLASTIC_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tkr: Vec<f64>,
    pub tks: Vec<f64>,
    pub tkt: Vec<f64>,
    pub rkr: Vec<f64>,
    pub rks: Vec<f64>,
    pub rkt: Vec<f64>,
    pub tdr: Vec<f64>,
    pub tds: Vec<f64>,
    pub tdt: Vec<f64>,
    pub rdr: Vec<f64>,
    pub rds: Vec<f64>,
    pub rdt: Vec<f64>,
    pub lcpdr: Vec<i64>,
    pub lcpds: Vec<i64>,
    pub lcpdt: Vec<i64>,
    pub lcpmr: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub lcpmt: Vec<i64>,
    pub ffailr: Vec<f64>,
    pub ffails: Vec<f64>,
    pub ffailt: Vec<f64>,
    pub mfailr: Vec<f64>,
    pub mfails: Vec<f64>,
    pub mfailt: Vec<f64>,
    pub ufailr: Vec<f64>,
    pub ufails: Vec<f64>,
    pub ufailt: Vec<f64>,
    pub tfailr: Vec<f64>,
    pub tfails: Vec<f64>,
    pub tfailt: Vec<f64>,
    pub for_: Vec<f64>,
    pub fos: Vec<f64>,
    pub fot: Vec<f64>,
    pub mor: Vec<f64>,
    pub mos: Vec<f64>,
    pub mot: Vec<f64>,
}
impl MAT_NONLINEAR_PLASTIC_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NONLINEAR_PLASTIC_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tkr: t.take("TKR").and_then(|c| c.into_float()).unwrap_or_default(),
            tks: t.take("TKS").and_then(|c| c.into_float()).unwrap_or_default(),
            tkt: t.take("TKT").and_then(|c| c.into_float()).unwrap_or_default(),
            rkr: t.take("RKR").and_then(|c| c.into_float()).unwrap_or_default(),
            rks: t.take("RKS").and_then(|c| c.into_float()).unwrap_or_default(),
            rkt: t.take("RKT").and_then(|c| c.into_float()).unwrap_or_default(),
            tdr: t.take("TDR").and_then(|c| c.into_float()).unwrap_or_default(),
            tds: t.take("TDS").and_then(|c| c.into_float()).unwrap_or_default(),
            tdt: t.take("TDT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdr: t.take("RDR").and_then(|c| c.into_float()).unwrap_or_default(),
            rds: t.take("RDS").and_then(|c| c.into_float()).unwrap_or_default(),
            rdt: t.take("RDT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpdr: t.take("LCPDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpds: t.take("LCPDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpdt: t.take("LCPDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpmr: t.take("LCPMR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            ffailr: t.take("FFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ffails: t.take("FFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ffailt: t.take("FFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailr: t.take("MFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            mfails: t.take("MFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            mfailt: t.take("MFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailr: t.take("UFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            ufails: t.take("UFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            ufailt: t.take("UFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailr: t.take("TFAILR").and_then(|c| c.into_float()).unwrap_or_default(),
            tfails: t.take("TFAILS").and_then(|c| c.into_float()).unwrap_or_default(),
            tfailt: t.take("TFAILT").and_then(|c| c.into_float()).unwrap_or_default(),
            for_: t.take("FOR").and_then(|c| c.into_float()).unwrap_or_default(),
            fos: t.take("FOS").and_then(|c| c.into_float()).unwrap_or_default(),
            fot: t.take("FOT").and_then(|c| c.into_float()).unwrap_or_default(),
            mor: t.take("MOR").and_then(|c| c.into_float()).unwrap_or_default(),
            mos: t.take("MOS").and_then(|c| c.into_float()).unwrap_or_default(),
            mot: t.take("MOT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NONLOCAL {
    pub idnl: Vec<i64>,
    pub pid: Vec<i64>,
    pub p: Vec<i64>,
    pub q: Vec<i64>,
    pub l: Vec<f64>,
    pub nfreq: Vec<i64>,
    pub nhv: Vec<i64>,
    pub nl1: Vec<i64>,
    pub nl2: Vec<i64>,
    pub nl3: Vec<i64>,
    pub nl4: Vec<i64>,
    pub nl5: Vec<i64>,
    pub nl6: Vec<i64>,
    pub nl7: Vec<i64>,
    pub nl8: Vec<i64>,
    pub xc1: Vec<f64>,
    pub yc1: Vec<f64>,
    pub zc1: Vec<f64>,
    pub xc2: Vec<f64>,
    pub yc2: Vec<f64>,
    pub zc2: Vec<f64>,
}
impl MAT_NONLOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NONLOCAL").unwrap());
        Self {
            idnl: t.take("IDNL").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_int()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            nfreq: t.take("NFREQ").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            nl1: t.take("NL1").and_then(|c| c.into_int()).unwrap_or_default(),
            nl2: t.take("NL2").and_then(|c| c.into_int()).unwrap_or_default(),
            nl3: t.take("NL3").and_then(|c| c.into_int()).unwrap_or_default(),
            nl4: t.take("NL4").and_then(|c| c.into_int()).unwrap_or_default(),
            nl5: t.take("NL5").and_then(|c| c.into_int()).unwrap_or_default(),
            nl6: t.take("NL6").and_then(|c| c.into_int()).unwrap_or_default(),
            nl7: t.take("NL7").and_then(|c| c.into_int()).unwrap_or_default(),
            nl8: t.take("NL8").and_then(|c| c.into_int()).unwrap_or_default(),
            xc1: t.take("XC1").and_then(|c| c.into_float()).unwrap_or_default(),
            yc1: t.take("YC1").and_then(|c| c.into_float()).unwrap_or_default(),
            zc1: t.take("ZC1").and_then(|c| c.into_float()).unwrap_or_default(),
            xc2: t.take("XC2").and_then(|c| c.into_float()).unwrap_or_default(),
            yc2: t.take("YC2").and_then(|c| c.into_float()).unwrap_or_default(),
            zc2: t.take("ZC2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NON_QUADRATIC_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub a: Vec<f64>,
    pub ksi: Vec<f64>,
    pub theta1: Vec<f64>,
    pub q1: Vec<f64>,
    pub theta2: Vec<f64>,
    pub q2: Vec<f64>,
    pub theta3: Vec<f64>,
    pub q3: Vec<f64>,
    pub cs: Vec<f64>,
    pub pdots: Vec<f64>,
    pub dcrit: Vec<f64>,
    pub wcb: Vec<f64>,
    pub wcl: Vec<f64>,
    pub wcs: Vec<f64>,
    pub cc: Vec<f64>,
    pub phi: Vec<f64>,
    pub gamma: Vec<f64>,
    pub thick: Vec<f64>,
}
impl MAT_NON_QUADRATIC_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NON_QUADRATIC_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            ksi: t.take("KSI").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta3: t.take("THETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            pdots: t.take("PDOTS").and_then(|c| c.into_float()).unwrap_or_default(),
            dcrit: t.take("DCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            wcb: t.take("WCB").and_then(|c| c.into_float()).unwrap_or_default(),
            wcl: t.take("WCL").and_then(|c| c.into_float()).unwrap_or_default(),
            wcs: t.take("WCS").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            phi: t.take("PHI").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_NULL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mu: Vec<f64>,
    pub terod: Vec<f64>,
    pub cerod: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_NULL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_NULL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            terod: t.take("TEROD").and_then(|c| c.into_float()).unwrap_or_default(),
            cerod: t.take("CEROD").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_OGDEN_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub nv: Vec<i64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub tbhys: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lcid1: Vec<i64>,
    pub data: Vec<f64>,
    pub lcid2: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub mu1: Vec<f64>,
    pub mu2: Vec<f64>,
    pub mu3: Vec<f64>,
    pub mu4: Vec<f64>,
    pub mu5: Vec<f64>,
    pub mu6: Vec<f64>,
    pub mu7: Vec<f64>,
    pub mu8: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub alpha3: Vec<f64>,
    pub alpha4: Vec<f64>,
    pub alpha5: Vec<f64>,
    pub alpha6: Vec<f64>,
    pub alpha7: Vec<f64>,
    pub alpha8: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_OGDEN_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_OGDEN_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            nv: t.take("NV").and_then(|c| c.into_int()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            tbhys: t.take("TBHYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid1: t.take("LCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            data: t.take("DATA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            mu1: t.take("MU1").and_then(|c| c.into_float()).unwrap_or_default(),
            mu2: t.take("MU2").and_then(|c| c.into_float()).unwrap_or_default(),
            mu3: t.take("MU3").and_then(|c| c.into_float()).unwrap_or_default(),
            mu4: t.take("MU4").and_then(|c| c.into_float()).unwrap_or_default(),
            mu5: t.take("MU5").and_then(|c| c.into_float()).unwrap_or_default(),
            mu6: t.take("MU6").and_then(|c| c.into_float()).unwrap_or_default(),
            mu7: t.take("MU7").and_then(|c| c.into_float()).unwrap_or_default(),
            mu8: t.take("MU8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha3: t.take("ALPHA3").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha4: t.take("ALPHA4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha5: t.take("ALPHA5").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha6: t.take("ALPHA6").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha7: t.take("ALPHA7").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha8: t.take("ALPHA8").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORIENTED_CRACK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fs: Vec<f64>,
    pub prf: Vec<f64>,
    pub soft: Vec<f64>,
    pub cvelo: Vec<f64>,
}
impl MAT_ORIENTED_CRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORIENTED_CRACK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            prf: t.take("PRF").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            cvelo: t.take("CVELO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub ihis: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_ORTHOTROPIC_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_ELASTIC_PHASE_CHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea1: Vec<f64>,
    pub eb1: Vec<f64>,
    pub ec1: Vec<f64>,
    pub prba1: Vec<f64>,
    pub prca1: Vec<f64>,
    pub prcb1: Vec<f64>,
    pub gab1: Vec<f64>,
    pub gbc1: Vec<f64>,
    pub gca1: Vec<f64>,
    pub aopt1: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a11: Vec<f64>,
    pub a21: Vec<f64>,
    pub a31: Vec<f64>,
    pub v11: Vec<f64>,
    pub v21: Vec<f64>,
    pub v31: Vec<f64>,
    pub d11: Vec<f64>,
    pub d21: Vec<f64>,
    pub d31: Vec<f64>,
    pub beta1: Vec<f64>,
    pub f_4: Vec<i64>,
    pub r02: Vec<f64>,
    pub ea2: Vec<f64>,
    pub eb2: Vec<f64>,
    pub ec2: Vec<f64>,
    pub prba2: Vec<f64>,
    pub prca2: Vec<f64>,
    pub prcb2: Vec<f64>,
    pub gab2: Vec<f64>,
    pub gbc2: Vec<f64>,
    pub gca2: Vec<f64>,
    pub f_5: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub f_3_2: Vec<i64>,
    pub a12: Vec<f64>,
    pub a22: Vec<f64>,
    pub a32: Vec<f64>,
    pub v12: Vec<f64>,
    pub v22: Vec<f64>,
    pub v32: Vec<f64>,
    pub d12: Vec<f64>,
    pub d22: Vec<f64>,
    pub d32: Vec<f64>,
    pub beta2: Vec<f64>,
    pub x1: Vec<f64>,
    pub y1: Vec<f64>,
    pub z1: Vec<f64>,
    pub x2: Vec<f64>,
    pub y2: Vec<f64>,
    pub z2: Vec<f64>,
    pub thkfac: Vec<f64>,
}
impl MAT_ORTHOTROPIC_ELASTIC_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_ELASTIC_PHASE_CHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea1: t.take("EA1").and_then(|c| c.into_float()).unwrap_or_default(),
            eb1: t.take("EB1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            prba1: t.take("PRBA1").and_then(|c| c.into_float()).unwrap_or_default(),
            prca1: t.take("PRCA1").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb1: t.take("PRCB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab1: t.take("GAB1").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc1: t.take("GBC1").and_then(|c| c.into_float()).unwrap_or_default(),
            gca1: t.take("GCA1").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt1: t.take("AOPT1").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a21: t.take("A21").and_then(|c| c.into_float()).unwrap_or_default(),
            a31: t.take("A31").and_then(|c| c.into_float()).unwrap_or_default(),
            v11: t.take("V11").and_then(|c| c.into_float()).unwrap_or_default(),
            v21: t.take("V21").and_then(|c| c.into_float()).unwrap_or_default(),
            v31: t.take("V31").and_then(|c| c.into_float()).unwrap_or_default(),
            d11: t.take("D11").and_then(|c| c.into_float()).unwrap_or_default(),
            d21: t.take("D21").and_then(|c| c.into_float()).unwrap_or_default(),
            d31: t.take("D31").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            r02: t.take("R02").and_then(|c| c.into_float()).unwrap_or_default(),
            ea2: t.take("EA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eb2: t.take("EB2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            prba2: t.take("PRBA2").and_then(|c| c.into_float()).unwrap_or_default(),
            prca2: t.take("PRCA2").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb2: t.take("PRCB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab2: t.take("GAB2").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc2: t.take("GBC2").and_then(|c| c.into_float()).unwrap_or_default(),
            gca2: t.take("GCA2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a12: t.take("A12").and_then(|c| c.into_float()).unwrap_or_default(),
            a22: t.take("A22").and_then(|c| c.into_float()).unwrap_or_default(),
            a32: t.take("A32").and_then(|c| c.into_float()).unwrap_or_default(),
            v12: t.take("V12").and_then(|c| c.into_float()).unwrap_or_default(),
            v22: t.take("V22").and_then(|c| c.into_float()).unwrap_or_default(),
            v32: t.take("V32").and_then(|c| c.into_float()).unwrap_or_default(),
            d12: t.take("D12").and_then(|c| c.into_float()).unwrap_or_default(),
            d22: t.take("D22").and_then(|c| c.into_float()).unwrap_or_default(),
            d32: t.take("D32").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            z1: t.take("Z1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            z2: t.take("Z2").and_then(|c| c.into_float()).unwrap_or_default(),
            thkfac: t.take("THKFAC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_SIMPLIFIED_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub unused: Vec<i64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub nerode: Vec<i64>,
    pub ndam: Vec<i64>,
    pub eps1tf: Vec<f64>,
    pub eps2tf: Vec<f64>,
    pub eps3tf: Vec<f64>,
    pub eps1cf: Vec<f64>,
    pub eps2cf: Vec<f64>,
    pub eps3cf: Vec<f64>,
    pub eps12f: Vec<f64>,
    pub eps23f: Vec<f64>,
    pub eps13f: Vec<f64>,
    pub epsd1t: Vec<f64>,
    pub epsc1t: Vec<f64>,
    pub cdam1t: Vec<f64>,
    pub epsd2t: Vec<f64>,
    pub epsc2t: Vec<f64>,
    pub cdam2t: Vec<f64>,
    pub epsd3t: Vec<f64>,
    pub epsc3t: Vec<f64>,
    pub cdam3t: Vec<f64>,
    pub epsd1c: Vec<f64>,
    pub epsc1c: Vec<f64>,
    pub cdam1c: Vec<f64>,
    pub epsd2c: Vec<f64>,
    pub epsc2c: Vec<f64>,
    pub cdam2c: Vec<f64>,
    pub epsd3c: Vec<f64>,
    pub epsc3c: Vec<f64>,
    pub cdam3c: Vec<f64>,
    pub epsd12: Vec<f64>,
    pub epsc12: Vec<f64>,
    pub cdam12: Vec<f64>,
    pub epsd23: Vec<f64>,
    pub epsc23: Vec<f64>,
    pub cdam23: Vec<f64>,
    pub epsd31: Vec<f64>,
    pub epsc31: Vec<f64>,
    pub cdam31: Vec<f64>,
}
impl MAT_ORTHOTROPIC_SIMPLIFIED_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_SIMPLIFIED_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nerode: t.take("NERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            ndam: t.take("NDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1tf: t.take("EPS1TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2tf: t.take("EPS2TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3tf: t.take("EPS3TF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1cf: t.take("EPS1CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2cf: t.take("EPS2CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3cf: t.take("EPS3CF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps12f: t.take("EPS12F").and_then(|c| c.into_float()).unwrap_or_default(),
            eps23f: t.take("EPS23F").and_then(|c| c.into_float()).unwrap_or_default(),
            eps13f: t.take("EPS13F").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd1t: t.take("EPSD1T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc1t: t.take("EPSC1T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam1t: t.take("CDAM1T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd2t: t.take("EPSD2T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc2t: t.take("EPSC2T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam2t: t.take("CDAM2T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd3t: t.take("EPSD3T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc3t: t.take("EPSC3T").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam3t: t.take("CDAM3T").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd1c: t.take("EPSD1C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc1c: t.take("EPSC1C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam1c: t.take("CDAM1C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd2c: t.take("EPSD2C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc2c: t.take("EPSC2C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam2c: t.take("CDAM2C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd3c: t.take("EPSD3C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc3c: t.take("EPSC3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam3c: t.take("CDAM3C").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd12: t.take("EPSD12").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc12: t.take("EPSC12").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam12: t.take("CDAM12").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd23: t.take("EPSD23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc23: t.take("EPSC23").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam23: t.take("CDAM23").and_then(|c| c.into_float()).unwrap_or_default(),
            epsd31: t.take("EPSD31").and_then(|c| c.into_float()).unwrap_or_default(),
            epsc31: t.take("EPSC31").and_then(|c| c.into_float()).unwrap_or_default(),
            cdam31: t.take("CDAM31").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_SMEARED_CRACK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub uins: Vec<f64>,
    pub uiss: Vec<f64>,
    pub cerrmi: Vec<f64>,
    pub cerrmii: Vec<f64>,
    pub ind: Vec<f64>,
    pub isd: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_ORTHOTROPIC_SMEARED_CRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_SMEARED_CRACK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            uins: t.take("UINS").and_then(|c| c.into_float()).unwrap_or_default(),
            uiss: t.take("UISS").and_then(|c| c.into_float()).unwrap_or_default(),
            cerrmi: t.take("CERRMI").and_then(|c| c.into_float()).unwrap_or_default(),
            cerrmii: t.take("CERRMII").and_then(|c| c.into_float()).unwrap_or_default(),
            ind: t.take("IND").and_then(|c| c.into_float()).unwrap_or_default(),
            isd: t.take("ISD").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aa: Vec<f64>,
    pub ab: Vec<f64>,
    pub ac: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<i64>,
}
impl MAT_ORTHOTROPIC_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aa: t.take("AA").and_then(|c| c.into_float()).unwrap_or_default(),
            ab: t.take("AB").and_then(|c| c.into_float()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_THERMAL_CURING {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aa: Vec<f64>,
    pub ab: Vec<f64>,
    pub ac: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<i64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub r: Vec<f64>,
    pub lccha: Vec<i64>,
    pub lcchb: Vec<i64>,
    pub lcchc: Vec<i64>,
    pub lcaa: Vec<i64>,
    pub lcab: Vec<i64>,
    pub lcac: Vec<i64>,
}
impl MAT_ORTHOTROPIC_THERMAL_CURING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_THERMAL_CURING").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aa: t.take("AA").and_then(|c| c.into_float()).unwrap_or_default(),
            ab: t.take("AB").and_then(|c| c.into_float()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            lccha: t.take("LCCHA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcchb: t.take("LCCHB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcchc: t.take("LCCHC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcaa: t.take("LCAA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcab: t.take("LCAB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_THERMAL_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aa: Vec<f64>,
    pub ab: Vec<f64>,
    pub ac: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<i64>,
    pub a1_2: Vec<f64>,
    pub a11: Vec<f64>,
    pub a2_2: Vec<f64>,
    pub a5: Vec<f64>,
    pub a55: Vec<f64>,
    pub a4: Vec<f64>,
    pub nip: Vec<i64>,
}
impl MAT_ORTHOTROPIC_THERMAL_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_THERMAL_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aa: t.take("AA").and_then(|c| c.into_float()).unwrap_or_default(),
            ab: t.take("AB").and_then(|c| c.into_float()).unwrap_or_default(),
            ac: t.take("AC").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_int()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a11: t.take("A11").and_then(|c| c.into_float()).unwrap_or_default(),
            a2_2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a55: t.take("A55").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHOTROPIC_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub vf: Vec<f64>,
    pub k: Vec<f64>,
    pub g0: Vec<f64>,
    pub ginf: Vec<f64>,
    pub beta: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub mangle: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_ORTHOTROPIC_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHOTROPIC_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            vf: t.take("VF").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            ginf: t.take("GINF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            mangle: t.take("MANGLE").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_ORTHO_ELASTIC_PLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11: Vec<f64>,
    pub e22: Vec<f64>,
    pub g12: Vec<f64>,
    pub pr12: Vec<f64>,
    pub pr23: Vec<f64>,
    pub pr31: Vec<f64>,
    pub sigma0: Vec<f64>,
    pub lc: Vec<i64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub r11: Vec<f64>,
    pub r22: Vec<f64>,
    pub r33: Vec<f64>,
    pub r12: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_ORTHO_ELASTIC_PLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_ORTHO_ELASTIC_PLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11: t.take("E11").and_then(|c| c.into_float()).unwrap_or_default(),
            e22: t.take("E22").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            pr12: t.take("PR12").and_then(|c| c.into_float()).unwrap_or_default(),
            pr23: t.take("PR23").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma0: t.take("SIGMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_int()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            r11: t.take("R11").and_then(|c| c.into_float()).unwrap_or_default(),
            r22: t.take("R22").and_then(|c| c.into_float()).unwrap_or_default(),
            r33: t.take("R33").and_then(|c| c.into_float()).unwrap_or_default(),
            r12: t.take("R12").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PAPER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr21: Vec<f64>,
    pub pr32: Vec<f64>,
    pub pr31: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub g13: Vec<f64>,
    pub e3c: Vec<f64>,
    pub cc: Vec<f64>,
    pub twok: Vec<f64>,
    pub s01: Vec<f64>,
    pub a01: Vec<f64>,
    pub b01: Vec<f64>,
    pub c01: Vec<f64>,
    pub s02: Vec<f64>,
    pub a02: Vec<f64>,
    pub b02: Vec<f64>,
    pub c02: Vec<f64>,
    pub s03: Vec<f64>,
    pub a03: Vec<f64>,
    pub b03: Vec<f64>,
    pub c03: Vec<f64>,
    pub s04: Vec<f64>,
    pub a04: Vec<f64>,
    pub b04: Vec<f64>,
    pub c04: Vec<f64>,
    pub s05: Vec<f64>,
    pub a05: Vec<f64>,
    pub b05: Vec<f64>,
    pub c05: Vec<f64>,
    pub prp1: Vec<f64>,
    pub prp2: Vec<f64>,
    pub prp4: Vec<f64>,
    pub prp5: Vec<f64>,
    pub asig: Vec<f64>,
    pub bsig: Vec<f64>,
    pub csig: Vec<f64>,
    pub tau0: Vec<f64>,
    pub atau: Vec<f64>,
    pub btau: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_PAPER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PAPER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr21: t.take("PR21").and_then(|c| c.into_float()).unwrap_or_default(),
            pr32: t.take("PR32").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            g13: t.take("G13").and_then(|c| c.into_float()).unwrap_or_default(),
            e3c: t.take("E3C").and_then(|c| c.into_float()).unwrap_or_default(),
            cc: t.take("CC").and_then(|c| c.into_float()).unwrap_or_default(),
            twok: t.take("TWOK").and_then(|c| c.into_float()).unwrap_or_default(),
            s01: t.take("S01").and_then(|c| c.into_float()).unwrap_or_default(),
            a01: t.take("A01").and_then(|c| c.into_float()).unwrap_or_default(),
            b01: t.take("B01").and_then(|c| c.into_float()).unwrap_or_default(),
            c01: t.take("C01").and_then(|c| c.into_float()).unwrap_or_default(),
            s02: t.take("S02").and_then(|c| c.into_float()).unwrap_or_default(),
            a02: t.take("A02").and_then(|c| c.into_float()).unwrap_or_default(),
            b02: t.take("B02").and_then(|c| c.into_float()).unwrap_or_default(),
            c02: t.take("C02").and_then(|c| c.into_float()).unwrap_or_default(),
            s03: t.take("S03").and_then(|c| c.into_float()).unwrap_or_default(),
            a03: t.take("A03").and_then(|c| c.into_float()).unwrap_or_default(),
            b03: t.take("B03").and_then(|c| c.into_float()).unwrap_or_default(),
            c03: t.take("C03").and_then(|c| c.into_float()).unwrap_or_default(),
            s04: t.take("S04").and_then(|c| c.into_float()).unwrap_or_default(),
            a04: t.take("A04").and_then(|c| c.into_float()).unwrap_or_default(),
            b04: t.take("B04").and_then(|c| c.into_float()).unwrap_or_default(),
            c04: t.take("C04").and_then(|c| c.into_float()).unwrap_or_default(),
            s05: t.take("S05").and_then(|c| c.into_float()).unwrap_or_default(),
            a05: t.take("A05").and_then(|c| c.into_float()).unwrap_or_default(),
            b05: t.take("B05").and_then(|c| c.into_float()).unwrap_or_default(),
            c05: t.take("C05").and_then(|c| c.into_float()).unwrap_or_default(),
            prp1: t.take("PRP1").and_then(|c| c.into_float()).unwrap_or_default(),
            prp2: t.take("PRP2").and_then(|c| c.into_float()).unwrap_or_default(),
            prp4: t.take("PRP4").and_then(|c| c.into_float()).unwrap_or_default(),
            prp5: t.take("PRP5").and_then(|c| c.into_float()).unwrap_or_default(),
            asig: t.take("ASIG").and_then(|c| c.into_float()).unwrap_or_default(),
            bsig: t.take("BSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            csig: t.take("CSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            tau0: t.take("TAU0").and_then(|c| c.into_float()).unwrap_or_default(),
            atau: t.take("ATAU").and_then(|c| c.into_float()).unwrap_or_default(),
            btau: t.take("BTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PERT_PIECEWISE_LINEAR_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PERT_PIECEWISE_LINEAR_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PERT_PIECEWISE_LINEAR_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PHS_BMW {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tunit: Vec<f64>,
    pub trip: Vec<i64>,
    pub phase: Vec<i64>,
    pub heat: Vec<i64>,
    pub lcy1: Vec<i64>,
    pub lcy2: Vec<i64>,
    pub lcy3: Vec<i64>,
    pub lcy4: Vec<i64>,
    pub lcy5: Vec<i64>,
    pub c_f: Vec<f64>,
    pub c_p: Vec<f64>,
    pub c_b: Vec<f64>,
    pub c: Vec<f64>,
    pub co: Vec<f64>,
    pub mo: Vec<f64>,
    pub cr: Vec<f64>,
    pub ni: Vec<f64>,
    pub mn: Vec<f64>,
    pub si: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub cu: Vec<f64>,
    pub p: Vec<f64>,
    pub ai: Vec<f64>,
    pub as_: Vec<f64>,
    pub ti: Vec<f64>,
    pub b: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub tabrho: Vec<i64>,
    pub f_3: Vec<i64>,
    pub tref: Vec<f64>,
    pub lat1: Vec<f64>,
    pub lat5: Vec<f64>,
    pub tabth: Vec<i64>,
    pub qr2: Vec<f64>,
    pub qr3: Vec<f64>,
    pub qr4: Vec<f64>,
    pub alpha: Vec<f64>,
    pub grain: Vec<f64>,
    pub toffe: Vec<f64>,
    pub tofpe: Vec<f64>,
    pub tofba: Vec<f64>,
    pub plmem2: Vec<f64>,
    pub plmem3: Vec<f64>,
    pub plmem4: Vec<f64>,
    pub plmem5: Vec<f64>,
    pub strc: Vec<f64>,
    pub strp: Vec<f64>,
    pub f_4: Vec<i64>,
    pub f_2_3: Vec<i64>,
    pub fs: Vec<f64>,
    pub ps: Vec<f64>,
    pub bs: Vec<f64>,
    pub ms: Vec<f64>,
    pub msig: Vec<f64>,
    pub lceps23: Vec<i64>,
    pub lceps4: Vec<i64>,
    pub lceps5: Vec<i64>,
    pub lch4: Vec<i64>,
    pub lch5: Vec<i64>,
    pub dtcrit: Vec<f64>,
    pub tsamp: Vec<f64>,
    pub islc: Vec<i64>,
    pub iextra: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_2_4: Vec<i64>,
    pub alph_m: Vec<f64>,
    pub n_m: Vec<f64>,
    pub phi_m: Vec<f64>,
    pub psi_m: Vec<f64>,
    pub omg_f: Vec<f64>,
    pub phi_f: Vec<f64>,
    pub psi_f: Vec<f64>,
    pub cr_f: Vec<f64>,
    pub omg_p: Vec<f64>,
    pub phi_p: Vec<f64>,
    pub psi_p: Vec<f64>,
    pub cr_p: Vec<f64>,
    pub omg_b: Vec<f64>,
    pub phi_b: Vec<f64>,
    pub psi_b: Vec<f64>,
    pub cr_b: Vec<f64>,
    pub aust: Vec<f64>,
    pub ferr: Vec<f64>,
    pub pear: Vec<f64>,
    pub bain: Vec<f64>,
    pub mart: Vec<f64>,
    pub grk: Vec<f64>,
    pub grqr: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gra: Vec<f64>,
    pub grb: Vec<f64>,
    pub expa: Vec<f64>,
    pub expb: Vec<f64>,
    pub grcc: Vec<f64>,
    pub grcm: Vec<f64>,
    pub heatn: Vec<f64>,
    pub tau2: Vec<f64>,
    pub funca: Vec<i64>,
    pub funcb: Vec<i64>,
    pub funcm: Vec<i64>,
    pub tcvup: Vec<f64>,
    pub tcvlo: Vec<f64>,
    pub cvcrit: Vec<f64>,
    pub tcvsl: Vec<f64>,
    pub epsp: Vec<f64>,
    pub expon: Vec<f64>,
}
impl MAT_PHS_BMW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PHS_BMW").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tunit: t.take("TUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            trip: t.take("TRIP").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            heat: t.take("HEAT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy1: t.take("LCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy2: t.take("LCY2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy3: t.take("LCY3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy4: t.take("LCY4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy5: t.take("LCY5").and_then(|c| c.into_int()).unwrap_or_default(),
            c_f: t.take("C_F").and_then(|c| c.into_float()).unwrap_or_default(),
            c_p: t.take("C_P").and_then(|c| c.into_float()).unwrap_or_default(),
            c_b: t.take("C_B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("Co").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("Mo").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("Cr").and_then(|c| c.into_float()).unwrap_or_default(),
            ni: t.take("Ni").and_then(|c| c.into_float()).unwrap_or_default(),
            mn: t.take("Mn").and_then(|c| c.into_float()).unwrap_or_default(),
            si: t.take("Si").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            cu: t.take("Cu").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            ai: t.take("AI").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("As").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            tabrho: t.take("TABRHO").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lat1: t.take("LAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lat5: t.take("LAT5").and_then(|c| c.into_float()).unwrap_or_default(),
            tabth: t.take("TABTH").and_then(|c| c.into_int()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qr3: t.take("QR3").and_then(|c| c.into_float()).unwrap_or_default(),
            qr4: t.take("QR4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            grain: t.take("GRAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            toffe: t.take("TOFFE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofpe: t.take("TOFPE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofba: t.take("TOFBA").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem2: t.take("PLMEM2").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem3: t.take("PLMEM3").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem4: t.take("PLMEM4").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem5: t.take("PLMEM5").and_then(|c| c.into_float()).unwrap_or_default(),
            strc: t.take("STRC").and_then(|c| c.into_float()).unwrap_or_default(),
            strp: t.take("STRP").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            msig: t.take("MSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps23: t.take("LCEPS23").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps4: t.take("LCEPS4").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps5: t.take("LCEPS5").and_then(|c| c.into_int()).unwrap_or_default(),
            lch4: t.take("LCH4").and_then(|c| c.into_int()).unwrap_or_default(),
            lch5: t.take("LCH5").and_then(|c| c.into_int()).unwrap_or_default(),
            dtcrit: t.take("DTCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsamp: t.take("TSAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            islc: t.take("ISLC").and_then(|c| c.into_int()).unwrap_or_default(),
            iextra: t.take("IEXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_4: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph_m: t.take("ALPH_M").and_then(|c| c.into_float()).unwrap_or_default(),
            n_m: t.take("N_M").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_m: t.take("PHI_M").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_m: t.take("PSI_M").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_f: t.take("OMG_F").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_f: t.take("PHI_F").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_f: t.take("PSI_F").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_f: t.take("CR_F").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_p: t.take("OMG_P").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_p: t.take("PHI_P").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_p: t.take("PSI_P").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_p: t.take("CR_P").and_then(|c| c.into_float()).unwrap_or_default(),
            omg_b: t.take("OMG_B").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_b: t.take("PHI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            psi_b: t.take("PSI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            cr_b: t.take("CR_B").and_then(|c| c.into_float()).unwrap_or_default(),
            aust: t.take("AUST").and_then(|c| c.into_float()).unwrap_or_default(),
            ferr: t.take("FERR").and_then(|c| c.into_float()).unwrap_or_default(),
            pear: t.take("PEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bain: t.take("BAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            mart: t.take("MART").and_then(|c| c.into_float()).unwrap_or_default(),
            grk: t.take("GRK").and_then(|c| c.into_float()).unwrap_or_default(),
            grqr: t.take("GRQR").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gra: t.take("GRA").and_then(|c| c.into_float()).unwrap_or_default(),
            grb: t.take("GRB").and_then(|c| c.into_float()).unwrap_or_default(),
            expa: t.take("EXPA").and_then(|c| c.into_float()).unwrap_or_default(),
            expb: t.take("EXPB").and_then(|c| c.into_float()).unwrap_or_default(),
            grcc: t.take("GRCC").and_then(|c| c.into_float()).unwrap_or_default(),
            grcm: t.take("GRCM").and_then(|c| c.into_float()).unwrap_or_default(),
            heatn: t.take("HEATN").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            funca: t.take("FUNCA").and_then(|c| c.into_int()).unwrap_or_default(),
            funcb: t.take("FUNCB").and_then(|c| c.into_int()).unwrap_or_default(),
            funcm: t.take("FUNCM").and_then(|c| c.into_int()).unwrap_or_default(),
            tcvup: t.take("TCVUP").and_then(|c| c.into_float()).unwrap_or_default(),
            tcvlo: t.take("TCVLO").and_then(|c| c.into_float()).unwrap_or_default(),
            cvcrit: t.take("CVCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcvsl: t.take("TCVSL").and_then(|c| c.into_float()).unwrap_or_default(),
            epsp: t.take("EPSP").and_then(|c| c.into_float()).unwrap_or_default(),
            expon: t.take("EXPON").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY_2D {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY_2D").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY_HAZ {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY_HAZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY_HAZ").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY_MIDFAIL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY_MIDFAIL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY_MIDFAIL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vp: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTICITY_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PIECEWISE_LINEAR_PLASTIC_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub tabidc: Vec<i64>,
    pub tabidt: Vec<i64>,
    pub lalpha: Vec<i64>,
    pub alpha: Vec<f64>,
    pub tref: Vec<f64>,
}
impl MAT_PIECEWISE_LINEAR_PLASTIC_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PIECEWISE_LINEAR_PLASTIC_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            tabidc: t.take("TABIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            tabidt: t.take("TABIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lalpha: t.take("LALPHA").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PITZER_CRUSHABLE_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub g: Vec<f64>,
    pub pr: Vec<f64>,
    pub ty: Vec<f64>,
    pub srtv: Vec<f64>,
    pub lcpy: Vec<i64>,
    pub lcuys: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub vc: Vec<f64>,
    pub dflg: Vec<f64>,
}
impl MAT_PITZER_CRUSHABLE_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PITZER_CRUSHABLE_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ty: t.take("TY").and_then(|c| c.into_float()).unwrap_or_default(),
            srtv: t.take("SRTV").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpy: t.take("LCPY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcuys: t.take("LCUYS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            dflg: t.take("DFLG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_COMPRESSION_TENSION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcsrc: Vec<i64>,
    pub lcsrt: Vec<i64>,
    pub srflag: Vec<f64>,
    pub lcfail: Vec<i64>,
    pub ec: Vec<f64>,
    pub rpct: Vec<f64>,
    pub pc: Vec<f64>,
    pub pt: Vec<f64>,
    pub pcutc: Vec<f64>,
    pub pcutt: Vec<f64>,
    pub pcutf: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub srfilt: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_PLASTICITY_COMPRESSION_TENSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_COMPRESSION_TENSION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrc: t.take("LCSRC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrt: t.take("LCSRT").and_then(|c| c.into_int()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            rpct: t.take("RPCT").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutc: t.take("PCUTC").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutt: t.take("PCUTT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutf: t.take("PCUTF").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            srfilt: t.take("SRFILT").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_COMPRESSION_TENSION_EOS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub lcidc: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcsrc: Vec<i64>,
    pub lcsrt: Vec<i64>,
    pub srflag: Vec<f64>,
    pub pc: Vec<f64>,
    pub pt: Vec<f64>,
    pub pcutc: Vec<f64>,
    pub pcutt: Vec<f64>,
    pub pcutf: Vec<f64>,
    pub scalep: Vec<f64>,
    pub scalee: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_PLASTICITY_COMPRESSION_TENSION_EOS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_COMPRESSION_TENSION_EOS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidc: t.take("LCIDC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrc: t.take("LCSRC").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsrt: t.take("LCSRT").and_then(|c| c.into_int()).unwrap_or_default(),
            srflag: t.take("SRFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            pt: t.take("PT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutc: t.take("PCUTC").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutt: t.take("PCUTT").and_then(|c| c.into_float()).unwrap_or_default(),
            pcutf: t.take("PCUTF").and_then(|c| c.into_float()).unwrap_or_default(),
            scalep: t.take("SCALEP").and_then(|c| c.into_float()).unwrap_or_default(),
            scalee: t.take("SCALEE").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_POLYMER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eftx: Vec<f64>,
    pub damp: Vec<f64>,
    pub ratefac: Vec<f64>,
    pub lcfail: Vec<i64>,
    pub numint: Vec<f64>,
}
impl MAT_PLASTICITY_POLYMER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_POLYMER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eftx: t.take("EFTX").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            ratefac: t.take("RATEFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfail: t.take("LCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_WITH_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PLASTICITY_WITH_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_WITH_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_WITH_DAMAGE_ORTHO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PLASTICITY_WITH_DAMAGE_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_WITH_DAMAGE_ORTHO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_WITH_DAMAGE_ORTHO_RCDC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub d0: Vec<f64>,
    pub b: Vec<f64>,
    pub lambda: Vec<f64>,
    pub ds: Vec<f64>,
    pub l: Vec<f64>,
}
impl MAT_PLASTICITY_WITH_DAMAGE_ORTHO_RCDC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_WITH_DAMAGE_ORTHO_RCDC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            d0: t.take("D0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTICITY_WITH_DAMAGE_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub eppf: Vec<f64>,
    pub tdel: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lcss: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub eppfr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_PLASTICITY_WITH_DAMAGE_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTICITY_WITH_DAMAGE_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            eppf: t.take("EPPF").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTIC_GREEN_NAGHDI_RATE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_PLASTIC_GREEN_NAGHDI_RATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTIC_GREEN-NAGHDI_RATE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTIC_KINEMATIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub beta: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub fs: Vec<f64>,
    pub vp: Vec<f64>,
}
impl MAT_PLASTIC_KINEMATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTIC_KINEMATIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTIC_NONLINEAR_KINEMATIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub h: Vec<f64>,
    pub c: Vec<f64>,
    pub gamma: Vec<f64>,
    pub fs: Vec<f64>,
}
impl MAT_PLASTIC_NONLINEAR_KINEMATIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTIC_NONLINEAR_KINEMATIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PLASTIC_NONLINEAR_KINEMATIC_B {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub re: Vec<f64>,
    pub b: Vec<f64>,
    pub q: Vec<f64>,
    pub c1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub c2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub c3: Vec<f64>,
    pub gamma3: Vec<f64>,
}
impl MAT_PLASTIC_NONLINEAR_KINEMATIC_B {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PLASTIC_NONLINEAR_KINEMATIC_B").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            re: t.take("RE").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma3: t.take("GAMMA3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_ACOUSTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c: Vec<f64>,
}
impl MAT_PML_ACOUSTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_ACOUSTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_ANISOTROPIC_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c11: Vec<f64>,
    pub c12: Vec<f64>,
    pub c22: Vec<f64>,
    pub c13: Vec<f64>,
    pub c23: Vec<f64>,
    pub c33: Vec<f64>,
    pub c14: Vec<f64>,
    pub c24: Vec<f64>,
    pub c34: Vec<f64>,
    pub c44: Vec<f64>,
    pub c15: Vec<f64>,
    pub c25: Vec<f64>,
    pub c35: Vec<f64>,
    pub c45: Vec<f64>,
    pub c55: Vec<f64>,
    pub c16: Vec<f64>,
    pub c26: Vec<f64>,
    pub c36: Vec<f64>,
    pub c46: Vec<f64>,
    pub c56: Vec<f64>,
    pub c66: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<i64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<i64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_PML_ANISOTROPIC_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_ANISOTROPIC_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c11: t.take("C11").and_then(|c| c.into_float()).unwrap_or_default(),
            c12: t.take("C12").and_then(|c| c.into_float()).unwrap_or_default(),
            c22: t.take("C22").and_then(|c| c.into_float()).unwrap_or_default(),
            c13: t.take("C13").and_then(|c| c.into_float()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            c33: t.take("C33").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            c24: t.take("C24").and_then(|c| c.into_float()).unwrap_or_default(),
            c34: t.take("C34").and_then(|c| c.into_float()).unwrap_or_default(),
            c44: t.take("C44").and_then(|c| c.into_float()).unwrap_or_default(),
            c15: t.take("C15").and_then(|c| c.into_float()).unwrap_or_default(),
            c25: t.take("C25").and_then(|c| c.into_float()).unwrap_or_default(),
            c35: t.take("C35").and_then(|c| c.into_float()).unwrap_or_default(),
            c45: t.take("C45").and_then(|c| c.into_float()).unwrap_or_default(),
            c55: t.take("C55").and_then(|c| c.into_float()).unwrap_or_default(),
            c16: t.take("C16").and_then(|c| c.into_float()).unwrap_or_default(),
            c26: t.take("C26").and_then(|c| c.into_float()).unwrap_or_default(),
            c36: t.take("C36").and_then(|c| c.into_float()).unwrap_or_default(),
            c46: t.take("C46").and_then(|c| c.into_float()).unwrap_or_default(),
            c56: t.take("C56").and_then(|c| c.into_float()).unwrap_or_default(),
            c66: t.take("C66").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_int()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_int()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_PML_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_ELASTIC_FLUID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub vc: Vec<f64>,
}
impl MAT_PML_ELASTIC_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_ELASTIC_FLUID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_HYSTERETIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub zt: Vec<f64>,
    pub fd: Vec<f64>,
}
impl MAT_PML_HYSTERETIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_HYSTERETIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_NULL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mu: Vec<f64>,
}
impl MAT_PML_NULL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_NULL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PML_ORTHOTROPIC_ELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub prca: Vec<f64>,
    pub prcb: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub aopt: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
}
impl MAT_PML_ORTHOTROPIC_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PML_ORTHOTROPIC_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prca: t.take("PRCA").and_then(|c| c.into_float()).unwrap_or_default(),
            prcb: t.take("PRCB").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_POLYMER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub gamma0: Vec<f64>,
    pub dg: Vec<f64>,
    pub sc: Vec<f64>,
    pub st: Vec<f64>,
    pub temp: Vec<f64>,
    pub k: Vec<f64>,
    pub cr: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
}
impl MAT_POLYMER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_POLYMER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma0: t.take("GAMMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            dg: t.take("DG").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("CR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_POWDER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub p11: Vec<f64>,
    pub p22: Vec<f64>,
    pub p33: Vec<f64>,
    pub p12: Vec<f64>,
    pub p23: Vec<f64>,
    pub p13: Vec<f64>,
    pub e0: Vec<f64>,
    pub lck: Vec<i64>,
    pub pr: Vec<f64>,
    pub lcx: Vec<i64>,
    pub lcy: Vec<i64>,
    pub lcc: Vec<i64>,
    pub l: Vec<f64>,
    pub r: Vec<f64>,
    pub ca: Vec<f64>,
    pub cd: Vec<f64>,
    pub cv: Vec<f64>,
    pub p: Vec<f64>,
    pub lch: Vec<i64>,
    pub lcfi: Vec<i64>,
    pub sint: Vec<f64>,
    pub tzro: Vec<f64>,
    pub lcfk: Vec<i64>,
    pub lcfs2: Vec<i64>,
    pub dv1: Vec<f64>,
    pub dv2: Vec<f64>,
    pub ds1: Vec<f64>,
    pub ds2: Vec<f64>,
    pub omega: Vec<f64>,
    pub rgas: Vec<f64>,
    pub lcpr: Vec<i64>,
    pub lcfs3: Vec<i64>,
    pub lctau: Vec<i64>,
    pub alpha: Vec<f64>,
    pub lcfs1: Vec<i64>,
    pub gamma: Vec<f64>,
    pub l0: Vec<f64>,
    pub lcfks: Vec<i64>,
}
impl MAT_POWDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_POWDER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            p11: t.take("P11").and_then(|c| c.into_float()).unwrap_or_default(),
            p22: t.take("P22").and_then(|c| c.into_float()).unwrap_or_default(),
            p33: t.take("P33").and_then(|c| c.into_float()).unwrap_or_default(),
            p12: t.take("P12").and_then(|c| c.into_float()).unwrap_or_default(),
            p23: t.take("P23").and_then(|c| c.into_float()).unwrap_or_default(),
            p13: t.take("P13").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            lck: t.take("LCK").and_then(|c| c.into_int()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcx: t.take("LCX").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy: t.take("LCY").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            l: t.take("L").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            cd: t.take("CD").and_then(|c| c.into_float()).unwrap_or_default(),
            cv: t.take("CV").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfi: t.take("LCFI").and_then(|c| c.into_int()).unwrap_or_default(),
            sint: t.take("SINT").and_then(|c| c.into_float()).unwrap_or_default(),
            tzro: t.take("TZRO").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfk: t.take("LCFK").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfs2: t.take("LCFS2").and_then(|c| c.into_int()).unwrap_or_default(),
            dv1: t.take("DV1").and_then(|c| c.into_float()).unwrap_or_default(),
            dv2: t.take("DV2").and_then(|c| c.into_float()).unwrap_or_default(),
            ds1: t.take("DS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ds2: t.take("DS2").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            rgas: t.take("RGAS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfs3: t.take("LCFS3").and_then(|c| c.into_int()).unwrap_or_default(),
            lctau: t.take("LCTAU").and_then(|c| c.into_int()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfs1: t.take("LCFS1").and_then(|c| c.into_int()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            l0: t.take("L0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcfks: t.take("LCFKS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_POWER_LAW_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub src: Vec<f64>,
    pub srp: Vec<f64>,
    pub sigy: Vec<f64>,
    pub vp: Vec<f64>,
    pub epsf: Vec<f64>,
}
impl MAT_POWER_LAW_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_POWER_LAW_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            src: t.take("SRC").and_then(|c| c.into_float()).unwrap_or_default(),
            srp: t.take("SRP").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_PSEUDO_TENSOR {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigf: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a0f: Vec<f64>,
    pub a1f: Vec<f64>,
    pub b1: Vec<f64>,
    pub per: Vec<f64>,
    pub er: Vec<f64>,
    pub prr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub lcp: Vec<i64>,
    pub lcr: Vec<i64>,
    pub x1: Vec<f64>,
    pub x2: Vec<f64>,
    pub x3: Vec<f64>,
    pub x4: Vec<f64>,
    pub x5: Vec<f64>,
    pub x6: Vec<f64>,
    pub x7: Vec<f64>,
    pub x8: Vec<f64>,
    pub x9: Vec<f64>,
    pub x10: Vec<f64>,
    pub x11: Vec<f64>,
    pub x12: Vec<f64>,
    pub x13: Vec<f64>,
    pub x14: Vec<f64>,
    pub x15: Vec<f64>,
    pub x16: Vec<f64>,
    pub ys1: Vec<f64>,
    pub ys2: Vec<f64>,
    pub ys3: Vec<f64>,
    pub ys4: Vec<f64>,
    pub ys5: Vec<f64>,
    pub ys6: Vec<f64>,
    pub ys7: Vec<f64>,
    pub ys8: Vec<f64>,
    pub ys9: Vec<f64>,
    pub ys10: Vec<f64>,
    pub ys11: Vec<f64>,
    pub ys12: Vec<f64>,
    pub ys13: Vec<f64>,
    pub ys14: Vec<f64>,
    pub ys15: Vec<f64>,
    pub ys16: Vec<f64>,
}
impl MAT_PSEUDO_TENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_PSEUDO_TENSOR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a0f: t.take("A0F").and_then(|c| c.into_float()).unwrap_or_default(),
            a1f: t.take("A1F").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            per: t.take("PER").and_then(|c| c.into_float()).unwrap_or_default(),
            er: t.take("ER").and_then(|c| c.into_float()).unwrap_or_default(),
            prr: t.take("PRR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            x4: t.take("X4").and_then(|c| c.into_float()).unwrap_or_default(),
            x5: t.take("X5").and_then(|c| c.into_float()).unwrap_or_default(),
            x6: t.take("X6").and_then(|c| c.into_float()).unwrap_or_default(),
            x7: t.take("X7").and_then(|c| c.into_float()).unwrap_or_default(),
            x8: t.take("X8").and_then(|c| c.into_float()).unwrap_or_default(),
            x9: t.take("X9").and_then(|c| c.into_float()).unwrap_or_default(),
            x10: t.take("X10").and_then(|c| c.into_float()).unwrap_or_default(),
            x11: t.take("X11").and_then(|c| c.into_float()).unwrap_or_default(),
            x12: t.take("X12").and_then(|c| c.into_float()).unwrap_or_default(),
            x13: t.take("X13").and_then(|c| c.into_float()).unwrap_or_default(),
            x14: t.take("X14").and_then(|c| c.into_float()).unwrap_or_default(),
            x15: t.take("X15").and_then(|c| c.into_float()).unwrap_or_default(),
            x16: t.take("X16").and_then(|c| c.into_float()).unwrap_or_default(),
            ys1: t.take("YS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ys2: t.take("YS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ys3: t.take("YS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ys4: t.take("YS4").and_then(|c| c.into_float()).unwrap_or_default(),
            ys5: t.take("YS5").and_then(|c| c.into_float()).unwrap_or_default(),
            ys6: t.take("YS6").and_then(|c| c.into_float()).unwrap_or_default(),
            ys7: t.take("YS7").and_then(|c| c.into_float()).unwrap_or_default(),
            ys8: t.take("YS8").and_then(|c| c.into_float()).unwrap_or_default(),
            ys9: t.take("YS9").and_then(|c| c.into_float()).unwrap_or_default(),
            ys10: t.take("YS10").and_then(|c| c.into_float()).unwrap_or_default(),
            ys11: t.take("YS11").and_then(|c| c.into_float()).unwrap_or_default(),
            ys12: t.take("YS12").and_then(|c| c.into_float()).unwrap_or_default(),
            ys13: t.take("YS13").and_then(|c| c.into_float()).unwrap_or_default(),
            ys14: t.take("YS14").and_then(|c| c.into_float()).unwrap_or_default(),
            ys15: t.take("YS15").and_then(|c| c.into_float()).unwrap_or_default(),
            ys16: t.take("YS16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_QUASILINEAR_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub lc1: Vec<i64>,
    pub lc2: Vec<i64>,
    pub n: Vec<f64>,
    pub gstart: Vec<f64>,
    pub m: Vec<f64>,
    pub so: Vec<f64>,
    pub e_min: Vec<f64>,
    pub e_max: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub k_2: Vec<f64>,
    pub eh: Vec<f64>,
    pub form: Vec<i64>,
    pub g1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub g2: Vec<f64>,
    pub beta2: Vec<f64>,
    pub g3: Vec<f64>,
    pub beta3: Vec<f64>,
    pub g4: Vec<f64>,
    pub beta4: Vec<f64>,
    pub g5: Vec<f64>,
    pub beta5: Vec<f64>,
    pub g6: Vec<f64>,
    pub beta6: Vec<f64>,
    pub g7: Vec<f64>,
    pub beta7: Vec<f64>,
    pub g8: Vec<f64>,
    pub beta8: Vec<f64>,
    pub g9: Vec<f64>,
    pub beta9: Vec<f64>,
    pub g10: Vec<f64>,
    pub beta10: Vec<f64>,
    pub g11: Vec<f64>,
    pub beta11: Vec<f64>,
    pub g12: Vec<f64>,
    pub beta12: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
}
impl MAT_QUASILINEAR_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_QUASILINEAR_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gstart: t.take("GSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            so: t.take("SO").and_then(|c| c.into_float()).unwrap_or_default(),
            e_min: t.take("E_MIN").and_then(|c| c.into_float()).unwrap_or_default(),
            e_max: t.take("E_MAX").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            k_2: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            g1: t.take("G1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            g2: t.take("G2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            g3: t.take("G3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta3: t.take("BETA3").and_then(|c| c.into_float()).unwrap_or_default(),
            g4: t.take("G4").and_then(|c| c.into_float()).unwrap_or_default(),
            beta4: t.take("BETA4").and_then(|c| c.into_float()).unwrap_or_default(),
            g5: t.take("G5").and_then(|c| c.into_float()).unwrap_or_default(),
            beta5: t.take("BETA5").and_then(|c| c.into_float()).unwrap_or_default(),
            g6: t.take("G6").and_then(|c| c.into_float()).unwrap_or_default(),
            beta6: t.take("BETA6").and_then(|c| c.into_float()).unwrap_or_default(),
            g7: t.take("G7").and_then(|c| c.into_float()).unwrap_or_default(),
            beta7: t.take("BETA7").and_then(|c| c.into_float()).unwrap_or_default(),
            g8: t.take("G8").and_then(|c| c.into_float()).unwrap_or_default(),
            beta8: t.take("BETA8").and_then(|c| c.into_float()).unwrap_or_default(),
            g9: t.take("G9").and_then(|c| c.into_float()).unwrap_or_default(),
            beta9: t.take("BETA9").and_then(|c| c.into_float()).unwrap_or_default(),
            g10: t.take("G10").and_then(|c| c.into_float()).unwrap_or_default(),
            beta10: t.take("BETA10").and_then(|c| c.into_float()).unwrap_or_default(),
            g11: t.take("G11").and_then(|c| c.into_float()).unwrap_or_default(),
            beta11: t.take("BETA11").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            beta12: t.take("BETA12").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RAMBERG_OSGOOD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gamy: Vec<f64>,
    pub tauy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub r: Vec<f64>,
    pub bulk: Vec<f64>,
}
impl MAT_RAMBERG_OSGOOD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RAMBERG-OSGOOD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamy: t.take("GAMY").and_then(|c| c.into_float()).unwrap_or_default(),
            tauy: t.take("TAUY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RAMBERG_OSGOOD_2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub gamy: Vec<f64>,
    pub tauy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub r: Vec<f64>,
    pub bulk: Vec<f64>,
}
impl MAT_RAMBERG_OSGOOD_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RAMBERG_OSGOOD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamy: t.take("GAMY").and_then(|c| c.into_float()).unwrap_or_default(),
            tauy: t.take("TAUY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RATE_SENSITIVE_COMPOSITE_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ea: Vec<f64>,
    pub eb: Vec<f64>,
    pub ec: Vec<f64>,
    pub prba: Vec<f64>,
    pub taui: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gab: Vec<f64>,
    pub gbc: Vec<f64>,
    pub gca: Vec<f64>,
    pub slimt1: Vec<f64>,
    pub slimc1: Vec<f64>,
    pub slimt2: Vec<f64>,
    pub slimc2: Vec<f64>,
    pub slims: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tsize: Vec<f64>,
    pub erods: Vec<f64>,
    pub soft: Vec<f64>,
    pub sf: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub e11c: Vec<f64>,
    pub e11t: Vec<f64>,
    pub e22c: Vec<f64>,
    pub e22t: Vec<f64>,
    pub gms: Vec<f64>,
    pub xc: Vec<f64>,
    pub xt: Vec<f64>,
    pub yc: Vec<f64>,
    pub yt: Vec<f64>,
    pub sc: Vec<f64>,
    pub k: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_RATE_SENSITIVE_COMPOSITE_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RATE_SENSITIVE_COMPOSITE_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            taui: t.take("TAUI").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
            gbc: t.take("GBC").and_then(|c| c.into_float()).unwrap_or_default(),
            gca: t.take("GCA").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt1: t.take("SLIMT1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc1: t.take("SLIMC1").and_then(|c| c.into_float()).unwrap_or_default(),
            slimt2: t.take("SLIMT2").and_then(|c| c.into_float()).unwrap_or_default(),
            slimc2: t.take("SLIMC2").and_then(|c| c.into_float()).unwrap_or_default(),
            slims: t.take("SLIMs").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsize: t.take("TSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            erods: t.take("ERODS").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            e11c: t.take("E11C").and_then(|c| c.into_float()).unwrap_or_default(),
            e11t: t.take("E11T").and_then(|c| c.into_float()).unwrap_or_default(),
            e22c: t.take("E22C").and_then(|c| c.into_float()).unwrap_or_default(),
            e22t: t.take("E22T").and_then(|c| c.into_float()).unwrap_or_default(),
            gms: t.take("GMS").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            sc: t.take("SC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RATE_SENSITIVE_POLYMER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub do_: Vec<f64>,
    pub n: Vec<f64>,
    pub zo: Vec<f64>,
    pub q: Vec<f64>,
    pub omega: Vec<f64>,
}
impl MAT_RATE_SENSITIVE_POLYMER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RATE_SENSITIVE_POLYMER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            do_: t.take("Do").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            zo: t.take("Zo").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("q").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("Omega").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RATE_SENSITIVE_POWERLAW_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub k: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub e0: Vec<f64>,
    pub vp: Vec<f64>,
    pub eps0: Vec<f64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_RATE_SENSITIVE_POWERLAW_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RATE_SENSITIVE_POWERLAW_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RC_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub eunl: Vec<f64>,
    pub pr: Vec<f64>,
    pub fc: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec50: Vec<f64>,
    pub resid: Vec<f64>,
    pub ft: Vec<f64>,
    pub unitc: Vec<f64>,
    pub f20: Vec<f64>,
    pub f30: Vec<f64>,
    pub f40: Vec<f64>,
    pub esoft: Vec<f64>,
    pub lchar: Vec<f64>,
    pub output: Vec<f64>,
    pub fracr: Vec<f64>,
    pub ymreinf: Vec<f64>,
    pub prreinf: Vec<f64>,
    pub syreinf: Vec<f64>,
    pub sureinf: Vec<f64>,
    pub eshr: Vec<f64>,
    pub eur: Vec<f64>,
    pub rreinf: Vec<f64>,
}
impl MAT_RC_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RC_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            eunl: t.take("EUNL").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec50: t.take("EC50").and_then(|c| c.into_float()).unwrap_or_default(),
            resid: t.take("RESID").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            unitc: t.take("UNITC").and_then(|c| c.into_float()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_float()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_float()).unwrap_or_default(),
            esoft: t.take("ESOFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchar: t.take("LCHAR").and_then(|c| c.into_float()).unwrap_or_default(),
            output: t.take("OUTPUT").and_then(|c| c.into_float()).unwrap_or_default(),
            fracr: t.take("FRACR").and_then(|c| c.into_float()).unwrap_or_default(),
            ymreinf: t.take("YMREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            prreinf: t.take("PRREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            syreinf: t.take("SYREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            sureinf: t.take("SUREINF").and_then(|c| c.into_float()).unwrap_or_default(),
            eshr: t.take("ESHR").and_then(|c| c.into_float()).unwrap_or_default(),
            eur: t.take("EUR").and_then(|c| c.into_float()).unwrap_or_default(),
            rreinf: t.take("RREINF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RC_SHEAR_WALL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tmax: Vec<f64>,
    pub fc: Vec<f64>,
    pub pref: Vec<f64>,
    pub fyield: Vec<f64>,
    pub sig0: Vec<f64>,
    pub unconv: Vec<f64>,
    pub alpha: Vec<f64>,
    pub ft: Vec<f64>,
    pub erienf: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub e_2: Vec<f64>,
    pub f: Vec<f64>,
    pub y1: Vec<f64>,
    pub y2: Vec<f64>,
    pub y3: Vec<f64>,
    pub y4: Vec<f64>,
    pub y5: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_RC_SHEAR_WALL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RC_SHEAR_WALL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tmax: t.take("TMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC'").and_then(|c| c.into_float()).unwrap_or_default(),
            pref: t.take("PREF").and_then(|c| c.into_float()).unwrap_or_default(),
            fyield: t.take("FYIELD").and_then(|c| c.into_float()).unwrap_or_default(),
            sig0: t.take("SIG0").and_then(|c| c.into_float()).unwrap_or_default(),
            unconv: t.take("UNCONV").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT").and_then(|c| c.into_float()).unwrap_or_default(),
            erienf: t.take("ERIENF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            e_2: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            y1: t.take("Y1").and_then(|c| c.into_float()).unwrap_or_default(),
            y2: t.take("Y2").and_then(|c| c.into_float()).unwrap_or_default(),
            y3: t.take("Y3").and_then(|c| c.into_float()).unwrap_or_default(),
            y4: t.take("Y4").and_then(|c| c.into_float()).unwrap_or_default(),
            y5: t.take("Y5").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_REINFORCED_THERMOPLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub lcem: Vec<i64>,
    pub prm: Vec<f64>,
    pub lcprm: Vec<i64>,
    pub lcsigy: Vec<i64>,
    pub beta: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub thick: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<f64>,
    pub ef1: Vec<f64>,
    pub lcef1: Vec<i64>,
    pub g23_1: Vec<f64>,
    pub g31_1: Vec<f64>,
    pub g12: Vec<f64>,
    pub lcg12: Vec<i64>,
    pub aloc12: Vec<f64>,
    pub gloc12: Vec<f64>,
    pub meth12: Vec<i64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub lcef2: Vec<i64>,
    pub g23_2: Vec<f64>,
    pub g31_2: Vec<f64>,
    pub g23: Vec<f64>,
    pub lcg23: Vec<i64>,
    pub aloc23: Vec<f64>,
    pub gloc23: Vec<f64>,
    pub meth23: Vec<i64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub lcef3: Vec<i64>,
    pub g23_3: Vec<f64>,
    pub g31_3: Vec<f64>,
    pub postv: Vec<i64>,
    pub ihis: Vec<f64>,
}
impl MAT_REINFORCED_THERMOPLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_REINFORCED_THERMOPLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcem: t.take("LCEM").and_then(|c| c.into_int()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcprm: t.take("LCPRM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_float()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef1: t.take("LCEF1").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_1: t.take("G23_1").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_1: t.take("G31_1").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg12: t.take("LCG12").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc12: t.take("ALOC12").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc12: t.take("GLOC12").and_then(|c| c.into_float()).unwrap_or_default(),
            meth12: t.take("METH12").and_then(|c| c.into_int()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef2: t.take("LCEF2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_2: t.take("G23_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_2: t.take("G31_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg23: t.take("LCG23").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc23: t.take("ALOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc23: t.take("GLOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            meth23: t.take("METH23").and_then(|c| c.into_int()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef3: t.take("LCEF3").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_3: t.take("G23_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_3: t.take("G31_3").and_then(|c| c.into_float()).unwrap_or_default(),
            postv: t.take("POSTV").and_then(|c| c.into_int()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_REINFORCED_THERMOPLASTIC_CRASH {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub prm: Vec<f64>,
    pub lcsigy: Vec<i64>,
    pub beta: Vec<f64>,
    pub pfl: Vec<f64>,
    pub visc: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub thick: Vec<f64>,
    pub vg1: Vec<f64>,
    pub vb1: Vec<f64>,
    pub vg2: Vec<f64>,
    pub vb2: Vec<f64>,
    pub vg3: Vec<f64>,
    pub vb3: Vec<f64>,
    pub vg4: Vec<f64>,
    pub vb4: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<i64>,
    pub ef1: Vec<i64>,
    pub lcef1: Vec<i64>,
    pub g23_1: Vec<i64>,
    pub g31_1: Vec<i64>,
    pub daf1: Vec<i64>,
    pub dam1: Vec<i64>,
    pub g12: Vec<i64>,
    pub lcg12: Vec<i64>,
    pub aloc12: Vec<i64>,
    pub gloc12: Vec<i64>,
    pub meth12: Vec<i64>,
    pub dam12: Vec<i64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub lcef2: Vec<i64>,
    pub g23_2: Vec<f64>,
    pub g31_2: Vec<f64>,
    pub daf2: Vec<i64>,
    pub dam2: Vec<i64>,
    pub g23: Vec<f64>,
    pub lcg23: Vec<i64>,
    pub aloc23: Vec<f64>,
    pub gloc23: Vec<f64>,
    pub meth23: Vec<i64>,
    pub dam23: Vec<i64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub lcef3: Vec<i64>,
    pub g23_3: Vec<f64>,
    pub g31_3: Vec<f64>,
    pub daf3: Vec<i64>,
    pub dam3: Vec<i64>,
    pub postv: Vec<f64>,
    pub viscs: Vec<f64>,
    pub ihis: Vec<f64>,
}
impl MAT_REINFORCED_THERMOPLASTIC_CRASH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_REINFORCED_THERMOPLASTIC_CRASH").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            pfl: t.take("PFL").and_then(|c| c.into_float()).unwrap_or_default(),
            visc: t.take("VISC").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            vg1: t.take("VG1").and_then(|c| c.into_float()).unwrap_or_default(),
            vb1: t.take("VB1").and_then(|c| c.into_float()).unwrap_or_default(),
            vg2: t.take("VG2").and_then(|c| c.into_float()).unwrap_or_default(),
            vb2: t.take("VB2").and_then(|c| c.into_float()).unwrap_or_default(),
            vg3: t.take("VG3").and_then(|c| c.into_float()).unwrap_or_default(),
            vb3: t.take("VB3").and_then(|c| c.into_float()).unwrap_or_default(),
            vg4: t.take("VG4").and_then(|c| c.into_float()).unwrap_or_default(),
            vb4: t.take("VB4").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_int()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcef1: t.take("LCEF1").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_1: t.take("G23_1").and_then(|c| c.into_int()).unwrap_or_default(),
            g31_1: t.take("G31_1").and_then(|c| c.into_int()).unwrap_or_default(),
            daf1: t.take("DAF1").and_then(|c| c.into_int()).unwrap_or_default(),
            dam1: t.take("DAM1").and_then(|c| c.into_int()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg12: t.take("LCG12").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc12: t.take("ALOC12").and_then(|c| c.into_int()).unwrap_or_default(),
            gloc12: t.take("GLOC12").and_then(|c| c.into_int()).unwrap_or_default(),
            meth12: t.take("METH12").and_then(|c| c.into_int()).unwrap_or_default(),
            dam12: t.take("DAM12").and_then(|c| c.into_int()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef2: t.take("LCEF2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_2: t.take("G23_2").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_2: t.take("G31_2").and_then(|c| c.into_float()).unwrap_or_default(),
            daf2: t.take("DAF2").and_then(|c| c.into_int()).unwrap_or_default(),
            dam2: t.take("DAM2").and_then(|c| c.into_int()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            lcg23: t.take("LCG23").and_then(|c| c.into_int()).unwrap_or_default(),
            aloc23: t.take("ALOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            gloc23: t.take("GLOC23").and_then(|c| c.into_float()).unwrap_or_default(),
            meth23: t.take("METH23").and_then(|c| c.into_int()).unwrap_or_default(),
            dam23: t.take("DAM23").and_then(|c| c.into_int()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            lcef3: t.take("LCEF3").and_then(|c| c.into_int()).unwrap_or_default(),
            g23_3: t.take("G23_3").and_then(|c| c.into_float()).unwrap_or_default(),
            g31_3: t.take("G31_3").and_then(|c| c.into_float()).unwrap_or_default(),
            daf3: t.take("DAF3").and_then(|c| c.into_int()).unwrap_or_default(),
            dam3: t.take("DAM3").and_then(|c| c.into_int()).unwrap_or_default(),
            postv: t.take("POSTV").and_then(|c| c.into_float()).unwrap_or_default(),
            viscs: t.take("VISCS").and_then(|c| c.into_float()).unwrap_or_default(),
            ihis: t.take("IHIS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_REINFORCED_THERMOPLASTIC_UDFIBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub em: Vec<f64>,
    pub prm: Vec<f64>,
    pub g: Vec<f64>,
    pub ezdef: Vec<f64>,
    pub nfib: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub mangl: Vec<f64>,
    pub idf1: Vec<i64>,
    pub alph1: Vec<f64>,
    pub ef1: Vec<f64>,
    pub kap1: Vec<f64>,
    pub idf2: Vec<i64>,
    pub alph2: Vec<f64>,
    pub ef2: Vec<f64>,
    pub kap2: Vec<f64>,
    pub idf3: Vec<i64>,
    pub alph3: Vec<f64>,
    pub ef3: Vec<f64>,
    pub kap3: Vec<f64>,
}
impl MAT_REINFORCED_THERMOPLASTIC_UDFIBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_REINFORCED_THERMOPLASTIC_UDFIBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            ezdef: t.take("EZDEF").and_then(|c| c.into_float()).unwrap_or_default(),
            nfib: t.take("NFIB").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            mangl: t.take("MANGL").and_then(|c| c.into_float()).unwrap_or_default(),
            idf1: t.take("IDF1").and_then(|c| c.into_int()).unwrap_or_default(),
            alph1: t.take("ALPH1").and_then(|c| c.into_float()).unwrap_or_default(),
            ef1: t.take("EF1").and_then(|c| c.into_float()).unwrap_or_default(),
            kap1: t.take("KAP1").and_then(|c| c.into_float()).unwrap_or_default(),
            idf2: t.take("IDF2").and_then(|c| c.into_int()).unwrap_or_default(),
            alph2: t.take("ALPH2").and_then(|c| c.into_float()).unwrap_or_default(),
            ef2: t.take("EF2").and_then(|c| c.into_float()).unwrap_or_default(),
            kap2: t.take("KAP2").and_then(|c| c.into_float()).unwrap_or_default(),
            idf3: t.take("IDF3").and_then(|c| c.into_int()).unwrap_or_default(),
            alph3: t.take("ALPH3").and_then(|c| c.into_float()).unwrap_or_default(),
            ef3: t.take("EF3").and_then(|c| c.into_float()).unwrap_or_default(),
            kap3: t.take("KAP3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RESULTANT_ANISOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11p: Vec<f64>,
    pub e22p: Vec<f64>,
    pub v12p: Vec<f64>,
    pub v21p: Vec<f64>,
    pub g12p: Vec<f64>,
    pub g23p: Vec<f64>,
    pub g31p: Vec<f64>,
    pub e11b: Vec<f64>,
    pub e22b: Vec<f64>,
    pub v12b: Vec<f64>,
    pub v21b: Vec<f64>,
    pub g12b: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ln11: Vec<f64>,
    pub ln22: Vec<f64>,
    pub ln12: Vec<f64>,
    pub lq1: Vec<f64>,
    pub lq2: Vec<f64>,
    pub lm11: Vec<f64>,
    pub lm22: Vec<f64>,
    pub lm12: Vec<f64>,
    pub null: Vec<f64>,
    pub null_2: Vec<f64>,
    pub null_3: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_RESULTANT_ANISOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RESULTANT_ANISOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11p: t.take("E11P").and_then(|c| c.into_float()).unwrap_or_default(),
            e22p: t.take("E22P").and_then(|c| c.into_float()).unwrap_or_default(),
            v12p: t.take("V12P").and_then(|c| c.into_float()).unwrap_or_default(),
            v21p: t.take("V21P").and_then(|c| c.into_float()).unwrap_or_default(),
            g12p: t.take("G12P").and_then(|c| c.into_float()).unwrap_or_default(),
            g23p: t.take("G23P").and_then(|c| c.into_float()).unwrap_or_default(),
            g31p: t.take("G31P").and_then(|c| c.into_float()).unwrap_or_default(),
            e11b: t.take("E11B").and_then(|c| c.into_float()).unwrap_or_default(),
            e22b: t.take("E22B").and_then(|c| c.into_float()).unwrap_or_default(),
            v12b: t.take("V12B").and_then(|c| c.into_float()).unwrap_or_default(),
            v21b: t.take("V21B").and_then(|c| c.into_float()).unwrap_or_default(),
            g12b: t.take("G12B").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ln11: t.take("LN11").and_then(|c| c.into_float()).unwrap_or_default(),
            ln22: t.take("LN22").and_then(|c| c.into_float()).unwrap_or_default(),
            ln12: t.take("LN12").and_then(|c| c.into_float()).unwrap_or_default(),
            lq1: t.take("LQ1").and_then(|c| c.into_float()).unwrap_or_default(),
            lq2: t.take("LQ2").and_then(|c| c.into_float()).unwrap_or_default(),
            lm11: t.take("LM11").and_then(|c| c.into_float()).unwrap_or_default(),
            lm22: t.take("LM22").and_then(|c| c.into_float()).unwrap_or_default(),
            lm12: t.take("LM12").and_then(|c| c.into_float()).unwrap_or_default(),
            null: t.take("NULL").and_then(|c| c.into_float()).unwrap_or_default(),
            null_2: t.take("NULL_2").and_then(|c| c.into_float()).unwrap_or_default(),
            null_3: t.take("NULL_3").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RESULTANT_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
}
impl MAT_RESULTANT_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RESULTANT_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RHT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub shear: Vec<f64>,
    pub onempa: Vec<f64>,
    pub epsf: Vec<f64>,
    pub b0: Vec<f64>,
    pub b1: Vec<f64>,
    pub t1: Vec<f64>,
    pub a: Vec<f64>,
    pub n: Vec<f64>,
    pub fc: Vec<f64>,
    pub fs: Vec<f64>,
    pub ft: Vec<f64>,
    pub q0: Vec<f64>,
    pub b: Vec<f64>,
    pub t2: Vec<f64>,
    pub e0c: Vec<f64>,
    pub e0t: Vec<f64>,
    pub ec: Vec<f64>,
    pub et: Vec<f64>,
    pub betac: Vec<f64>,
    pub betat: Vec<f64>,
    pub ptf: Vec<f64>,
    pub gc: Vec<f64>,
    pub gt: Vec<f64>,
    pub xi: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub epm: Vec<f64>,
    pub af: Vec<f64>,
    pub nf: Vec<f64>,
    pub gamma: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub pel: Vec<f64>,
    pub pco: Vec<f64>,
    pub np: Vec<f64>,
    pub alpha: Vec<f64>,
}
impl MAT_RHT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RHT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            onempa: t.take("ONEMPA").and_then(|c| c.into_float()).unwrap_or_default(),
            epsf: t.take("EPSF").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS*").and_then(|c| c.into_float()).unwrap_or_default(),
            ft: t.take("FT*").and_then(|c| c.into_float()).unwrap_or_default(),
            q0: t.take("Q0").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            e0c: t.take("E0C").and_then(|c| c.into_float()).unwrap_or_default(),
            e0t: t.take("E0T").and_then(|c| c.into_float()).unwrap_or_default(),
            ec: t.take("EC").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            betac: t.take("BETAC").and_then(|c| c.into_float()).unwrap_or_default(),
            betat: t.take("BETAT").and_then(|c| c.into_float()).unwrap_or_default(),
            ptf: t.take("PTF").and_then(|c| c.into_float()).unwrap_or_default(),
            gc: t.take("GC*").and_then(|c| c.into_float()).unwrap_or_default(),
            gt: t.take("GT*").and_then(|c| c.into_float()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            epm: t.take("EPM").and_then(|c| c.into_float()).unwrap_or_default(),
            af: t.take("AF").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            pel: t.take("PEL").and_then(|c| c.into_float()).unwrap_or_default(),
            pco: t.take("PCO").and_then(|c| c.into_float()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RIGID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<f64>,
    pub couple: Vec<f64>,
    pub m: Vec<f64>,
    pub alias: Vec<String>,
    pub cmo: Vec<f64>,
    pub con1: Vec<f64>,
    pub con2: Vec<f64>,
    pub lco_or_a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_RIGID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RIGID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            couple: t.take("COUPLE").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            alias: t.take("ALIAS").and_then(|c| c.into_str()).unwrap_or_default(),
            cmo: t.take("CMO").and_then(|c| c.into_float()).unwrap_or_default(),
            con1: t.take("CON1").and_then(|c| c.into_float()).unwrap_or_default(),
            con2: t.take("CON2").and_then(|c| c.into_float()).unwrap_or_default(),
            lco_or_a1: t.take("LCO_OR_A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_RIGID_DISCRETE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_RIGID_DISCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_RIGID_DISCRETE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S01 {
    pub mid: Vec<i64>,
    pub k: Vec<f64>,
}
impl MAT_S01 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S01").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S02 {
    pub mid: Vec<i64>,
    pub dc: Vec<f64>,
}
impl MAT_S02 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S02").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S03 {
    pub mid: Vec<i64>,
    pub k: Vec<f64>,
    pub kt: Vec<f64>,
    pub fy: Vec<f64>,
}
impl MAT_S03 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S03").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            kt: t.take("KT").and_then(|c| c.into_float()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S04 {
    pub mid: Vec<i64>,
    pub lcd: Vec<i64>,
    pub lcr: Vec<i64>,
}
impl MAT_S04 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S04").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd: t.take("LCD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S05 {
    pub mid: Vec<i64>,
    pub lcdr: Vec<i64>,
}
impl MAT_S05 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S05").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdr: t.take("LCDR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S06 {
    pub mid: Vec<i64>,
    pub lcdl: Vec<i64>,
    pub lcdu: Vec<i64>,
    pub beta: Vec<f64>,
    pub tyi: Vec<f64>,
    pub cyi: Vec<f64>,
}
impl MAT_S06 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S06").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdl: t.take("LCDL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdu: t.take("LCDU").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tyi: t.take("TYI").and_then(|c| c.into_float()).unwrap_or_default(),
            cyi: t.take("CYI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S07 {
    pub mid: Vec<i64>,
    pub k0: Vec<f64>,
    pub ki: Vec<f64>,
    pub beta: Vec<f64>,
    pub tc: Vec<f64>,
    pub fc: Vec<f64>,
    pub copt: Vec<f64>,
}
impl MAT_S07 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S07").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            copt: t.take("COPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S08 {
    pub mid: Vec<i64>,
    pub lcfd: Vec<i64>,
    pub ku: Vec<f64>,
    pub ctf: Vec<f64>,
}
impl MAT_S08 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S08").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfd: t.take("LCFD").and_then(|c| c.into_int()).unwrap_or_default(),
            ku: t.take("KU").and_then(|c| c.into_float()).unwrap_or_default(),
            ctf: t.take("CTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S13 {
    pub mid: Vec<i64>,
    pub defl1: Vec<f64>,
    pub f1: Vec<f64>,
    pub defl2: Vec<f64>,
    pub f2: Vec<f64>,
    pub defl3: Vec<f64>,
    pub f3: Vec<f64>,
    pub fflag: Vec<f64>,
}
impl MAT_S13 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S13").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            defl1: t.take("DEFL1").and_then(|c| c.into_float()).unwrap_or_default(),
            f1: t.take("F1").and_then(|c| c.into_float()).unwrap_or_default(),
            defl2: t.take("DEFL2").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            defl3: t.take("DEFL3").and_then(|c| c.into_float()).unwrap_or_default(),
            f3: t.take("F3").and_then(|c| c.into_float()).unwrap_or_default(),
            fflag: t.take("FFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S14 {
    pub mid: Vec<i64>,
    pub a14: Vec<f64>,
    pub b14: Vec<f64>,
    pub c14: Vec<f64>,
    pub d14: Vec<f64>,
    pub e14: Vec<f64>,
    pub lcid: Vec<i64>,
    pub psd: Vec<f64>,
}
impl MAT_S14 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S14").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            a14: t.take("A14").and_then(|c| c.into_float()).unwrap_or_default(),
            b14: t.take("B14").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            d14: t.take("D14").and_then(|c| c.into_float()).unwrap_or_default(),
            e14: t.take("E14").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            psd: t.take("PSD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_S15 {
    pub mid: Vec<i64>,
    pub lo: Vec<f64>,
    pub vmax: Vec<f64>,
    pub sv: Vec<f64>,
    pub a: Vec<f64>,
    pub fmax: Vec<f64>,
    pub tl: Vec<f64>,
    pub tv: Vec<f64>,
    pub fpe: Vec<f64>,
    pub lmax: Vec<f64>,
    pub ksh: Vec<f64>,
}
impl MAT_S15 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_S15").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lo: t.take("LO").and_then(|c| c.into_float()).unwrap_or_default(),
            vmax: t.take("VMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sv: t.take("SV").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            tl: t.take("TL").and_then(|c| c.into_float()).unwrap_or_default(),
            tv: t.take("TV").and_then(|c| c.into_float()).unwrap_or_default(),
            fpe: t.take("FPE").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ksh: t.take("KSH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SAMP_1 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub gmod: Vec<f64>,
    pub emod: Vec<f64>,
    pub nue: Vec<f64>,
    pub rbcfac: Vec<f64>,
    pub numint: Vec<i64>,
    pub lcid_t: Vec<i64>,
    pub lcid_c: Vec<i64>,
    pub lcid_s: Vec<i64>,
    pub lcid_b: Vec<i64>,
    pub nuep: Vec<f64>,
    pub lcid_p: Vec<i64>,
    pub unused: Vec<i64>,
    pub incdam: Vec<i64>,
    pub lcid_d: Vec<i64>,
    pub epfail: Vec<f64>,
    pub deprpt: Vec<f64>,
    pub lcid_tri: Vec<i64>,
    pub lcid_lc: Vec<i64>,
    pub miter: Vec<i64>,
    pub mipds: Vec<i64>,
    pub f20: Vec<i64>,
    pub incfail: Vec<i64>,
    pub iconv: Vec<i64>,
    pub asaf: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub filt: Vec<f64>,
}
impl MAT_SAMP_1 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SAMP-1").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            nue: t.take("NUE").and_then(|c| c.into_float()).unwrap_or_default(),
            rbcfac: t.take("RBCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_t: t.take("LCID-T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_c: t.take("LCID-C").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_s: t.take("LCID-S").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_b: t.take("LCID-B").and_then(|c| c.into_int()).unwrap_or_default(),
            nuep: t.take("NUEP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_p: t.take("LCID-P").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            incdam: t.take("INCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_d: t.take("LCID_D").and_then(|c| c.into_int()).unwrap_or_default(),
            epfail: t.take("EPFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            deprpt: t.take("DEPRPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_tri: t.take("LCID-TRI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_lc: t.take("LCID_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            miter: t.take("MITER").and_then(|c| c.into_int()).unwrap_or_default(),
            mipds: t.take("MIPDS").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            incfail: t.take("INCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            iconv: t.take("ICONV").and_then(|c| c.into_int()).unwrap_or_default(),
            asaf: t.take("ASAF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            filt: t.take("FILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SAMP_1_2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub gmod: Vec<f64>,
    pub emod: Vec<f64>,
    pub nue: Vec<f64>,
    pub rbcfac: Vec<f64>,
    pub numint: Vec<i64>,
    pub lcid_t: Vec<i64>,
    pub lcid_c: Vec<i64>,
    pub lcid_s: Vec<i64>,
    pub lcid_b: Vec<i64>,
    pub nuep: Vec<f64>,
    pub lcid_p: Vec<i64>,
    pub unused: Vec<i64>,
    pub incdam: Vec<i64>,
    pub lcid_d: Vec<i64>,
    pub epfail: Vec<f64>,
    pub deprpt: Vec<f64>,
    pub lcid_tri: Vec<i64>,
    pub lcid_lc: Vec<i64>,
    pub miter: Vec<i64>,
    pub mipds: Vec<i64>,
    pub f20: Vec<i64>,
    pub incfail: Vec<i64>,
    pub iconv: Vec<i64>,
    pub asaf: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub filt: Vec<f64>,
}
impl MAT_SAMP_1_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SAMP_1").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            gmod: t.take("GMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            nue: t.take("NUE").and_then(|c| c.into_float()).unwrap_or_default(),
            rbcfac: t.take("RBCFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_t: t.take("LCID-T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_c: t.take("LCID-C").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_s: t.take("LCID-S").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_b: t.take("LCID-B").and_then(|c| c.into_int()).unwrap_or_default(),
            nuep: t.take("NUEP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_p: t.take("LCID-P").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            incdam: t.take("INCDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_d: t.take("LCID_D").and_then(|c| c.into_int()).unwrap_or_default(),
            epfail: t.take("EPFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            deprpt: t.take("DEPRPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_tri: t.take("LCID-TRI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_lc: t.take("LCID_LC").and_then(|c| c.into_int()).unwrap_or_default(),
            miter: t.take("MITER").and_then(|c| c.into_int()).unwrap_or_default(),
            mipds: t.take("MIPDS").and_then(|c| c.into_int()).unwrap_or_default(),
            f20: t.take("f20").and_then(|c| c.into_int()).unwrap_or_default(),
            incfail: t.take("INCFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            iconv: t.take("ICONV").and_then(|c| c.into_int()).unwrap_or_default(),
            asaf: t.take("ASAF").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            filt: t.take("FILT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SAMP_LIGHT {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub emod: Vec<f64>,
    pub nue: Vec<f64>,
    pub lcemod: Vec<i64>,
    pub beta: Vec<f64>,
    pub lcid_t: Vec<i64>,
    pub lcid_c: Vec<i64>,
    pub ctflg: Vec<i64>,
    pub rateop: Vec<i64>,
    pub nuep: Vec<f64>,
    pub lcid_p: Vec<i64>,
    pub rfiltf: Vec<f64>,
}
impl MAT_SAMP_LIGHT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SAMP_LIGHT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            emod: t.take("EMOD").and_then(|c| c.into_float()).unwrap_or_default(),
            nue: t.take("NUE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcemod: t.take("LCEMOD").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_t: t.take("LCID_T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_c: t.take("LCID_C").and_then(|c| c.into_int()).unwrap_or_default(),
            ctflg: t.take("CTFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            rateop: t.take("RATEOP").and_then(|c| c.into_int()).unwrap_or_default(),
            nuep: t.take("NUEP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_p: t.take("LCID-P").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SCC_ON_RCC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e0: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub pr: Vec<f64>,
    pub g: Vec<f64>,
    pub g_scl: Vec<f64>,
    pub tsl: Vec<f64>,
    pub eps_tan: Vec<f64>,
}
impl MAT_SCC_ON_RCC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SCC_ON_RCC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            g_scl: t.take("G_SCL").and_then(|c| c.into_float()).unwrap_or_default(),
            tsl: t.take("TSL").and_then(|c| c.into_float()).unwrap_or_default(),
            eps_tan: t.take("EPS_TAN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SCHWER_MURRAY_CAP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub shear: Vec<f64>,
    pub bulk: Vec<f64>,
    pub grun: Vec<f64>,
    pub shock: Vec<f64>,
    pub pore: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub efit: Vec<f64>,
    pub ffit: Vec<f64>,
    pub alphan: Vec<f64>,
    pub calpha: Vec<f64>,
    pub ro_2: Vec<f64>,
    pub xo: Vec<f64>,
    pub irock: Vec<f64>,
    pub secp: Vec<f64>,
    pub afit: Vec<f64>,
    pub bfit: Vec<f64>,
    pub rdamo: Vec<f64>,
    pub w: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub nplot: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub cfit: Vec<f64>,
    pub dfit: Vec<f64>,
    pub tfail: Vec<f64>,
    pub failfg: Vec<f64>,
    pub dbeta: Vec<f64>,
    pub ddelta: Vec<f64>,
    pub vptau: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub theta1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub theta2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub beta2: Vec<f64>,
}
impl MAT_SCHWER_MURRAY_CAP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SCHWER_MURRAY_CAP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            grun: t.take("GRUN").and_then(|c| c.into_float()).unwrap_or_default(),
            shock: t.take("SHOCK").and_then(|c| c.into_float()).unwrap_or_default(),
            pore: t.take("PORE").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            efit: t.take("EFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ffit: t.take("FFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            alphan: t.take("ALPHAN").and_then(|c| c.into_float()).unwrap_or_default(),
            calpha: t.take("CALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ro_2: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            irock: t.take("IROCK").and_then(|c| c.into_float()).unwrap_or_default(),
            secp: t.take("SECP").and_then(|c| c.into_float()).unwrap_or_default(),
            afit: t.take("AFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            bfit: t.take("BFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdamo: t.take("RDAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            cfit: t.take("CFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfit: t.take("DFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            failfg: t.take("FAILFG").and_then(|c| c.into_float()).unwrap_or_default(),
            dbeta: t.take("DBETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ddelta: t.take("DDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            vptau: t.take("VPTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SCHWER_MURRAY_CAP_MODEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub shear: Vec<f64>,
    pub bulk: Vec<f64>,
    pub grun: Vec<f64>,
    pub shock: Vec<f64>,
    pub pore: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub beta: Vec<f64>,
    pub efit: Vec<f64>,
    pub ffit: Vec<f64>,
    pub alphan: Vec<f64>,
    pub calpha: Vec<f64>,
    pub ro_2: Vec<f64>,
    pub xo: Vec<f64>,
    pub irock: Vec<f64>,
    pub secp: Vec<f64>,
    pub afit: Vec<f64>,
    pub bfit: Vec<f64>,
    pub rdamo: Vec<f64>,
    pub w: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub nplot: Vec<f64>,
    pub epsmax: Vec<f64>,
    pub cfit: Vec<f64>,
    pub dfit: Vec<f64>,
    pub tfail: Vec<f64>,
    pub failfg: Vec<f64>,
    pub dbeta: Vec<f64>,
    pub ddelta: Vec<f64>,
    pub vptau: Vec<f64>,
    pub alpha1: Vec<f64>,
    pub theta1: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub beta1: Vec<f64>,
    pub alpha2: Vec<f64>,
    pub theta2: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub beta2: Vec<f64>,
}
impl MAT_SCHWER_MURRAY_CAP_MODEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SCHWER_MURRAY_CAP_MODEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            grun: t.take("GRUN").and_then(|c| c.into_float()).unwrap_or_default(),
            shock: t.take("SHOCK").and_then(|c| c.into_float()).unwrap_or_default(),
            pore: t.take("PORE").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            efit: t.take("EFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            ffit: t.take("FFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            alphan: t.take("ALPHAN").and_then(|c| c.into_float()).unwrap_or_default(),
            calpha: t.take("CALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ro_2: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            xo: t.take("XO").and_then(|c| c.into_float()).unwrap_or_default(),
            irock: t.take("IROCK").and_then(|c| c.into_float()).unwrap_or_default(),
            secp: t.take("SECP").and_then(|c| c.into_float()).unwrap_or_default(),
            afit: t.take("AFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            bfit: t.take("BFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            rdamo: t.take("RDAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_float()).unwrap_or_default(),
            epsmax: t.take("EPSMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            cfit: t.take("CFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfit: t.take("DFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            failfg: t.take("FAILFG").and_then(|c| c.into_float()).unwrap_or_default(),
            dbeta: t.take("DBETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ddelta: t.take("DDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            vptau: t.take("VPTAU").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha1: t.take("ALPHA1").and_then(|c| c.into_float()).unwrap_or_default(),
            theta1: t.take("THETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            beta1: t.take("BETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha2: t.take("ALPHA2").and_then(|c| c.into_float()).unwrap_or_default(),
            theta2: t.take("THETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            beta2: t.take("BETA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SEATBELT {
    pub mid: Vec<i64>,
    pub mpul: Vec<f64>,
    pub llcid: Vec<i64>,
    pub ulcid: Vec<i64>,
    pub lmin: Vec<f64>,
    pub cse: Vec<f64>,
    pub damp: Vec<f64>,
    pub e: Vec<f64>,
    pub a: Vec<f64>,
    pub i: Vec<f64>,
    pub j: Vec<f64>,
    pub as_: Vec<f64>,
    pub f: Vec<f64>,
    pub m: Vec<f64>,
    pub r: Vec<f64>,
}
impl MAT_SEATBELT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SEATBELT").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mpul: t.take("MPUL").and_then(|c| c.into_float()).unwrap_or_default(),
            llcid: t.take("LLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ulcid: t.take("ULCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            i: t.take("I").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SEATBELT_2D {
    pub mid: Vec<i64>,
    pub mpul: Vec<f64>,
    pub llcid: Vec<i64>,
    pub ulcid: Vec<i64>,
    pub lmin: Vec<f64>,
    pub cse: Vec<f64>,
    pub damp: Vec<f64>,
    pub e: Vec<f64>,
    pub a: Vec<f64>,
    pub i: Vec<f64>,
    pub j: Vec<f64>,
    pub as_: Vec<f64>,
    pub f: Vec<f64>,
    pub m: Vec<f64>,
    pub r: Vec<f64>,
    pub p1doff: Vec<i64>,
    pub form: Vec<i64>,
    pub ecoat: Vec<f64>,
    pub tcoat: Vec<f64>,
    pub scoat: Vec<f64>,
    pub eb: Vec<f64>,
    pub prba: Vec<f64>,
    pub prba_2: Vec<f64>,
    pub gab: Vec<f64>,
}
impl MAT_SEATBELT_2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SEATBELT_2D").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            mpul: t.take("MPUL").and_then(|c| c.into_float()).unwrap_or_default(),
            llcid: t.take("LLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            ulcid: t.take("ULCID").and_then(|c| c.into_int()).unwrap_or_default(),
            lmin: t.take("LMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            i: t.take("I").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("AS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            p1doff: t.take("P1DOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            ecoat: t.take("ECOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            tcoat: t.take("TCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            scoat: t.take("SCOAT").and_then(|c| c.into_float()).unwrap_or_default(),
            eb: t.take("EB").and_then(|c| c.into_float()).unwrap_or_default(),
            prba: t.take("PRBA").and_then(|c| c.into_float()).unwrap_or_default(),
            prba_2: t.take("PRBA_2").and_then(|c| c.into_float()).unwrap_or_default(),
            gab: t.take("GAB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SEISMIC_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub asflag: Vec<f64>,
    pub ftype: Vec<i64>,
    pub degrad: Vec<i64>,
    pub ifema: Vec<i64>,
    pub lcpms: Vec<i64>,
    pub sfs: Vec<f64>,
    pub lcpmt: Vec<i64>,
    pub sft: Vec<f64>,
    pub lcat: Vec<i64>,
    pub sfat: Vec<f64>,
    pub lcac: Vec<i64>,
    pub sfac: Vec<f64>,
    pub alpha: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub delta: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub foffs: Vec<f64>,
    pub f: Vec<f64>,
    pub sigy: Vec<f64>,
    pub d: Vec<f64>,
    pub w: Vec<f64>,
    pub tf: Vec<f64>,
    pub tw: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub phi_t: Vec<f64>,
    pub phi_c: Vec<f64>,
    pub phi_b: Vec<f64>,
    pub f_4: Vec<f64>,
    pub f_2_3: Vec<f64>,
    pub f_3_2: Vec<f64>,
    pub f_4_2: Vec<f64>,
    pub f_5: Vec<f64>,
}
impl MAT_SEISMIC_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SEISMIC_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            asflag: t.take("ASFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            degrad: t.take("DEGRAD").and_then(|c| c.into_int()).unwrap_or_default(),
            ifema: t.take("IFEMA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcpms: t.take("LCPMS").and_then(|c| c.into_int()).unwrap_or_default(),
            sfs: t.take("SFS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpmt: t.take("LCPMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcat: t.take("LCAT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfat: t.take("SFAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcac: t.take("LCAC").and_then(|c| c.into_int()).unwrap_or_default(),
            sfac: t.take("SFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            delta: t.take("DELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            foffs: t.take("FOFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            tf: t.take("TF").and_then(|c| c.into_float()).unwrap_or_default(),
            tw: t.take("TW").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_t: t.take("PHI_T").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_c: t.take("PHI_C").and_then(|c| c.into_float()).unwrap_or_default(),
            phi_b: t.take("PHI_B").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_3: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            f_4_2: t.take("-_4").and_then(|c| c.into_float()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SEISMIC_ISOLATOR {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub a: Vec<f64>,
    pub beta: Vec<f64>,
    pub gamma: Vec<f64>,
    pub dispy: Vec<f64>,
    pub stiffv: Vec<f64>,
    pub itype: Vec<i64>,
    pub preload: Vec<f64>,
    pub damp: Vec<f64>,
    pub mx1: Vec<f64>,
    pub mx2: Vec<f64>,
    pub my1: Vec<f64>,
    pub my2: Vec<f64>,
    pub cde: Vec<f64>,
    pub iextra: Vec<i64>,
    pub fmax: Vec<f64>,
    pub delf: Vec<f64>,
    pub afric: Vec<f64>,
    pub radx: Vec<f64>,
    pub rady: Vec<f64>,
    pub radb: Vec<f64>,
    pub stiffl: Vec<f64>,
    pub stiffts: Vec<f64>,
    pub forcey: Vec<f64>,
    pub alpha: Vec<f64>,
    pub stifft: Vec<f64>,
    pub dfail: Vec<f64>,
    pub fmaxyc: Vec<f64>,
    pub fmaxxt: Vec<f64>,
    pub fmaxyt: Vec<f64>,
    pub ylock: Vec<f64>,
    pub htcore: Vec<f64>,
    pub rcore: Vec<f64>,
    pub tshim: Vec<f64>,
    pub rolcl: Vec<f64>,
    pub roscs: Vec<f64>,
    pub thcst: Vec<f64>,
    pub yle2: Vec<f64>,
    pub pcrini: Vec<f64>,
    pub diamb: Vec<f64>,
    pub fcav0: Vec<f64>,
    pub cavk: Vec<f64>,
    pub cavtr: Vec<f64>,
    pub cava: Vec<f64>,
    pub phim: Vec<f64>,
    pub beta_2: Vec<f64>,
    pub kthx: Vec<f64>,
    pub kthy: Vec<f64>,
    pub kthz: Vec<f64>,
}
impl MAT_SEISMIC_ISOLATOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SEISMIC_ISOLATOR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            dispy: t.take("DISPY").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffv: t.take("STIFFV").and_then(|c| c.into_float()).unwrap_or_default(),
            itype: t.take("ITYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            preload: t.take("PRELOAD").and_then(|c| c.into_float()).unwrap_or_default(),
            damp: t.take("DAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            mx1: t.take("MX1").and_then(|c| c.into_float()).unwrap_or_default(),
            mx2: t.take("MX2").and_then(|c| c.into_float()).unwrap_or_default(),
            my1: t.take("MY1").and_then(|c| c.into_float()).unwrap_or_default(),
            my2: t.take("MY2").and_then(|c| c.into_float()).unwrap_or_default(),
            cde: t.take("CDE").and_then(|c| c.into_float()).unwrap_or_default(),
            iextra: t.take("IEXTRA").and_then(|c| c.into_int()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            delf: t.take("DELF").and_then(|c| c.into_float()).unwrap_or_default(),
            afric: t.take("AFRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radx: t.take("RADX").and_then(|c| c.into_float()).unwrap_or_default(),
            rady: t.take("RADY").and_then(|c| c.into_float()).unwrap_or_default(),
            radb: t.take("RADB").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffl: t.take("STIFFL").and_then(|c| c.into_float()).unwrap_or_default(),
            stiffts: t.take("STIFFTS").and_then(|c| c.into_float()).unwrap_or_default(),
            forcey: t.take("FORCEY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            stifft: t.take("STIFFT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfail: t.take("DFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxyc: t.take("FMAXYC").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxxt: t.take("FMAXXT").and_then(|c| c.into_float()).unwrap_or_default(),
            fmaxyt: t.take("FMAXYT").and_then(|c| c.into_float()).unwrap_or_default(),
            ylock: t.take("YLOCK").and_then(|c| c.into_float()).unwrap_or_default(),
            htcore: t.take("HTCORE").and_then(|c| c.into_float()).unwrap_or_default(),
            rcore: t.take("RCORE").and_then(|c| c.into_float()).unwrap_or_default(),
            tshim: t.take("TSHIM").and_then(|c| c.into_float()).unwrap_or_default(),
            rolcl: t.take("ROLCL").and_then(|c| c.into_float()).unwrap_or_default(),
            roscs: t.take("ROSCS").and_then(|c| c.into_float()).unwrap_or_default(),
            thcst: t.take("THCST").and_then(|c| c.into_float()).unwrap_or_default(),
            yle2: t.take("YLE2").and_then(|c| c.into_float()).unwrap_or_default(),
            pcrini: t.take("PCRINI").and_then(|c| c.into_float()).unwrap_or_default(),
            diamb: t.take("DIAMB").and_then(|c| c.into_float()).unwrap_or_default(),
            fcav0: t.take("FCAV0").and_then(|c| c.into_float()).unwrap_or_default(),
            cavk: t.take("CAVK").and_then(|c| c.into_float()).unwrap_or_default(),
            cavtr: t.take("CAVTR").and_then(|c| c.into_float()).unwrap_or_default(),
            cava: t.take("CAVA").and_then(|c| c.into_float()).unwrap_or_default(),
            phim: t.take("PHIM").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_2: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            kthx: t.take("KTHX").and_then(|c| c.into_float()).unwrap_or_default(),
            kthy: t.take("KTHY").and_then(|c| c.into_float()).unwrap_or_default(),
            kthz: t.take("KTHZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SHAPE_MEMORY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lcss: Vec<f64>,
    pub sig_ass: Vec<f64>,
    pub sig_asf: Vec<f64>,
    pub sig_sas: Vec<f64>,
    pub sig_saf: Vec<f64>,
    pub epsl: Vec<f64>,
    pub alpha: Vec<f64>,
    pub ymrt: Vec<f64>,
    pub lcid_as: Vec<i64>,
    pub lcid_sa: Vec<i64>,
}
impl MAT_SHAPE_MEMORY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SHAPE_MEMORY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_ass: t.take("SIG_ASS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_asf: t.take("SIG_ASF").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_sas: t.take("SIG_SAS").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_saf: t.take("SIG_SAF").and_then(|c| c.into_float()).unwrap_or_default(),
            epsl: t.take("EPSL").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            ymrt: t.take("YMRT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid_as: t.take("LCID_AS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid_sa: t.take("LCID_SA").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SHAPE_MEMORY_ALLOY {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub em: Vec<f64>,
    pub ea: Vec<f64>,
    pub prm: Vec<f64>,
    pub pra: Vec<f64>,
    pub aopt: Vec<i64>,
    pub stype: Vec<i64>,
    pub cpm: Vec<f64>,
    pub cpa: Vec<f64>,
    pub lh: Vec<f64>,
    pub tc: Vec<f64>,
    pub tmf: Vec<f64>,
    pub tms: Vec<f64>,
    pub tas: Vec<f64>,
    pub taf: Vec<f64>,
    pub a1i: Vec<f64>,
    pub a2i: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub ki: Vec<f64>,
    pub mi: Vec<f64>,
    pub kl: Vec<f64>,
    pub ml: Vec<f64>,
    pub a1s: Vec<f64>,
    pub a2s: Vec<f64>,
    pub bs: Vec<f64>,
    pub cs: Vec<f64>,
    pub ks: Vec<f64>,
    pub ms: Vec<f64>,
    pub d0l: Vec<f64>,
    pub d0m: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
    pub n11: Vec<f64>,
    pub n22: Vec<f64>,
    pub n33: Vec<f64>,
    pub n44: Vec<f64>,
    pub n55: Vec<f64>,
    pub n66: Vec<f64>,
    pub n12: Vec<f64>,
    pub n23: Vec<f64>,
    pub n34: Vec<f64>,
    pub n45: Vec<f64>,
    pub n56: Vec<f64>,
    pub n13: Vec<f64>,
    pub n24: Vec<f64>,
    pub n35: Vec<f64>,
    pub n46: Vec<f64>,
    pub n14: Vec<f64>,
    pub n25: Vec<f64>,
    pub n36: Vec<f64>,
    pub n15: Vec<f64>,
    pub n26: Vec<f64>,
    pub n16: Vec<f64>,
}
impl MAT_SHAPE_MEMORY_ALLOY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SHAPE_MEMORY_ALLOY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cpm: t.take("CPM").and_then(|c| c.into_float()).unwrap_or_default(),
            cpa: t.take("CPA").and_then(|c| c.into_float()).unwrap_or_default(),
            lh: t.take("LH").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            tmf: t.take("TMF").and_then(|c| c.into_float()).unwrap_or_default(),
            tms: t.take("TMS").and_then(|c| c.into_float()).unwrap_or_default(),
            tas: t.take("TAS").and_then(|c| c.into_float()).unwrap_or_default(),
            taf: t.take("TAF").and_then(|c| c.into_float()).unwrap_or_default(),
            a1i: t.take("A1I").and_then(|c| c.into_float()).unwrap_or_default(),
            a2i: t.take("A2I").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("BI").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("CI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            mi: t.take("MI").and_then(|c| c.into_float()).unwrap_or_default(),
            kl: t.take("KL").and_then(|c| c.into_float()).unwrap_or_default(),
            ml: t.take("ML").and_then(|c| c.into_float()).unwrap_or_default(),
            a1s: t.take("A1S").and_then(|c| c.into_float()).unwrap_or_default(),
            a2s: t.take("A2S").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            ks: t.take("KS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            d0l: t.take("D0L").and_then(|c| c.into_float()).unwrap_or_default(),
            d0m: t.take("D0M").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_float()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_float()).unwrap_or_default(),
            n33: t.take("N33").and_then(|c| c.into_float()).unwrap_or_default(),
            n44: t.take("N44").and_then(|c| c.into_float()).unwrap_or_default(),
            n55: t.take("N55").and_then(|c| c.into_float()).unwrap_or_default(),
            n66: t.take("N66").and_then(|c| c.into_float()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_float()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_float()).unwrap_or_default(),
            n34: t.take("N34").and_then(|c| c.into_float()).unwrap_or_default(),
            n45: t.take("N45").and_then(|c| c.into_float()).unwrap_or_default(),
            n56: t.take("N56").and_then(|c| c.into_float()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_float()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_float()).unwrap_or_default(),
            n35: t.take("N35").and_then(|c| c.into_float()).unwrap_or_default(),
            n46: t.take("N46").and_then(|c| c.into_float()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_float()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_float()).unwrap_or_default(),
            n36: t.take("N36").and_then(|c| c.into_float()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_float()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_float()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SHAPE_MEMORY_ALLOY_MEDTRONIC {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
    pub em: Vec<f64>,
    pub ea: Vec<f64>,
    pub prm: Vec<f64>,
    pub pra: Vec<f64>,
    pub aopt: Vec<i64>,
    pub stype: Vec<i64>,
    pub cpm: Vec<f64>,
    pub cpa: Vec<f64>,
    pub lh: Vec<f64>,
    pub tc: Vec<f64>,
    pub tmf: Vec<f64>,
    pub tms: Vec<f64>,
    pub tas: Vec<f64>,
    pub taf: Vec<f64>,
    pub a1i: Vec<f64>,
    pub a2i: Vec<f64>,
    pub bi: Vec<f64>,
    pub ci: Vec<f64>,
    pub ki: Vec<f64>,
    pub mi: Vec<f64>,
    pub kl: Vec<f64>,
    pub ml: Vec<f64>,
    pub a1s: Vec<f64>,
    pub a2s: Vec<f64>,
    pub bs: Vec<f64>,
    pub cs: Vec<f64>,
    pub ks: Vec<f64>,
    pub ms: Vec<f64>,
    pub d0l: Vec<f64>,
    pub d0m: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<i64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ref_: Vec<f64>,
    pub n11: Vec<f64>,
    pub n22: Vec<f64>,
    pub n33: Vec<f64>,
    pub n44: Vec<f64>,
    pub n55: Vec<f64>,
    pub n66: Vec<f64>,
    pub n12: Vec<f64>,
    pub n23: Vec<f64>,
    pub n34: Vec<f64>,
    pub n45: Vec<f64>,
    pub n56: Vec<f64>,
    pub n13: Vec<f64>,
    pub n24: Vec<f64>,
    pub n35: Vec<f64>,
    pub n46: Vec<f64>,
    pub n14: Vec<f64>,
    pub n25: Vec<f64>,
    pub n36: Vec<f64>,
    pub n15: Vec<f64>,
    pub n26: Vec<f64>,
    pub n16: Vec<f64>,
    pub kp: Vec<f64>,
    pub mp: Vec<f64>,
    pub kc: Vec<f64>,
    pub mc: Vec<f64>,
    pub d0p: Vec<f64>,
    pub qp: Vec<f64>,
    pub np: Vec<f64>,
    pub ql: Vec<f64>,
    pub nl: Vec<f64>,
    pub qm: Vec<f64>,
    pub nm: Vec<f64>,
}
impl MAT_SHAPE_MEMORY_ALLOY_MEDTRONIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SHAPE_MEMORY_ALLOY_MEDTRONIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
            em: t.take("EM").and_then(|c| c.into_float()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            prm: t.take("PRM").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            stype: t.take("STYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cpm: t.take("CPM").and_then(|c| c.into_float()).unwrap_or_default(),
            cpa: t.take("CPA").and_then(|c| c.into_float()).unwrap_or_default(),
            lh: t.take("LH").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            tmf: t.take("TMF").and_then(|c| c.into_float()).unwrap_or_default(),
            tms: t.take("TMS").and_then(|c| c.into_float()).unwrap_or_default(),
            tas: t.take("TAS").and_then(|c| c.into_float()).unwrap_or_default(),
            taf: t.take("TAF").and_then(|c| c.into_float()).unwrap_or_default(),
            a1i: t.take("A1I").and_then(|c| c.into_float()).unwrap_or_default(),
            a2i: t.take("A2I").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("BI").and_then(|c| c.into_float()).unwrap_or_default(),
            ci: t.take("CI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            mi: t.take("MI").and_then(|c| c.into_float()).unwrap_or_default(),
            kl: t.take("KL").and_then(|c| c.into_float()).unwrap_or_default(),
            ml: t.take("ML").and_then(|c| c.into_float()).unwrap_or_default(),
            a1s: t.take("A1S").and_then(|c| c.into_float()).unwrap_or_default(),
            a2s: t.take("A2S").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            cs: t.take("CS").and_then(|c| c.into_float()).unwrap_or_default(),
            ks: t.take("KS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            d0l: t.take("D0L").and_then(|c| c.into_float()).unwrap_or_default(),
            d0m: t.take("D0M").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            n11: t.take("N11").and_then(|c| c.into_float()).unwrap_or_default(),
            n22: t.take("N22").and_then(|c| c.into_float()).unwrap_or_default(),
            n33: t.take("N33").and_then(|c| c.into_float()).unwrap_or_default(),
            n44: t.take("N44").and_then(|c| c.into_float()).unwrap_or_default(),
            n55: t.take("N55").and_then(|c| c.into_float()).unwrap_or_default(),
            n66: t.take("N66").and_then(|c| c.into_float()).unwrap_or_default(),
            n12: t.take("N12").and_then(|c| c.into_float()).unwrap_or_default(),
            n23: t.take("N23").and_then(|c| c.into_float()).unwrap_or_default(),
            n34: t.take("N34").and_then(|c| c.into_float()).unwrap_or_default(),
            n45: t.take("N45").and_then(|c| c.into_float()).unwrap_or_default(),
            n56: t.take("N56").and_then(|c| c.into_float()).unwrap_or_default(),
            n13: t.take("N13").and_then(|c| c.into_float()).unwrap_or_default(),
            n24: t.take("N24").and_then(|c| c.into_float()).unwrap_or_default(),
            n35: t.take("N35").and_then(|c| c.into_float()).unwrap_or_default(),
            n46: t.take("N46").and_then(|c| c.into_float()).unwrap_or_default(),
            n14: t.take("N14").and_then(|c| c.into_float()).unwrap_or_default(),
            n25: t.take("N25").and_then(|c| c.into_float()).unwrap_or_default(),
            n36: t.take("N36").and_then(|c| c.into_float()).unwrap_or_default(),
            n15: t.take("N15").and_then(|c| c.into_float()).unwrap_or_default(),
            n26: t.take("N26").and_then(|c| c.into_float()).unwrap_or_default(),
            n16: t.take("N16").and_then(|c| c.into_float()).unwrap_or_default(),
            kp: t.take("KP").and_then(|c| c.into_float()).unwrap_or_default(),
            mp: t.take("MP").and_then(|c| c.into_float()).unwrap_or_default(),
            kc: t.take("KC").and_then(|c| c.into_float()).unwrap_or_default(),
            mc: t.take("MC").and_then(|c| c.into_float()).unwrap_or_default(),
            d0p: t.take("D0P").and_then(|c| c.into_float()).unwrap_or_default(),
            qp: t.take("QP").and_then(|c| c.into_float()).unwrap_or_default(),
            np: t.take("NP").and_then(|c| c.into_float()).unwrap_or_default(),
            ql: t.take("QL").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_float()).unwrap_or_default(),
            qm: t.take("QM").and_then(|c| c.into_float()).unwrap_or_default(),
            nm: t.take("NM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SID_DAMPER_DISCRETE_BEAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub st: Vec<f64>,
    pub d: Vec<f64>,
    pub r: Vec<f64>,
    pub h: Vec<f64>,
    pub k: Vec<f64>,
    pub c: Vec<f64>,
    pub c3: Vec<f64>,
    pub stf: Vec<f64>,
    pub rhof: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub lcidf: Vec<f64>,
    pub lcidd: Vec<f64>,
    pub s0: Vec<f64>,
    pub orfloc: Vec<f64>,
    pub orfrad: Vec<f64>,
    pub sf: Vec<f64>,
    pub dc: Vec<f64>,
}
impl MAT_SID_DAMPER_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SID_DAMPER_DISCRETE_BEAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            stf: t.take("STF").and_then(|c| c.into_float()).unwrap_or_default(),
            rhof: t.take("RHOF").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidf: t.take("LCIDF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidd: t.take("LCIDD").and_then(|c| c.into_float()).unwrap_or_default(),
            s0: t.take("S0").and_then(|c| c.into_float()).unwrap_or_default(),
            orfloc: t.take("ORFLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            orfrad: t.take("ORFRAD").and_then(|c| c.into_float()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_JOHNSON_COOK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub psfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub sigsat: Vec<f64>,
    pub epso: Vec<f64>,
}
impl MAT_SIMPLIFIED_JOHNSON_COOK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_JOHNSON_COOK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigsat: t.take("SIGSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_JOHNSON_COOK_ORTHOTROPIC_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub eppfr: Vec<f64>,
    pub lcdm: Vec<i64>,
    pub numint: Vec<i64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub psfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub sigsat: Vec<f64>,
    pub epso: Vec<f64>,
}
impl MAT_SIMPLIFIED_JOHNSON_COOK_ORTHOTROPIC_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_JOHNSON_COOK_ORTHOTROPIC_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            eppfr: t.take("EPPFR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcdm: t.take("LCDM").and_then(|c| c.into_int()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigsat: t.take("SIGSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_JOHNSON_COOK_STOCHASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub n: Vec<f64>,
    pub c: Vec<f64>,
    pub psfail: Vec<f64>,
    pub sigmax: Vec<f64>,
    pub sigsat: Vec<f64>,
    pub epso: Vec<f64>,
}
impl MAT_SIMPLIFIED_JOHNSON_COOK_STOCHASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_JOHNSON_COOK_STOCHASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            psfail: t.take("PSFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigmax: t.take("SIGMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigsat: t.take("SIGSAT").and_then(|c| c.into_float()).unwrap_or_default(),
            epso: t.take("EPSO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER/FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_FOAM_LOG_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_FOAM_LOG_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER/FOAM_LOG_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_FOAM_LOG_LOG_INTERPOLATION_WITH_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub k: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub eh: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_FOAM_LOG_LOG_INTERPOLATION_WITH_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER/FOAM_LOG_LOG_INTERPOLATION_WITH_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_FOAM_WITH_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub k: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub eh: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_FOAM_WITH_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER/FOAM_WITH_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_FOAM_WITH_FAILURE_LOG_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub k: Vec<f64>,
    pub gama1: Vec<f64>,
    pub gama2: Vec<f64>,
    pub eh: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_FOAM_WITH_FAILURE_LOG_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER/FOAM_WITH_FAILURE_LOG_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            gama1: t.take("GAMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gama2: t.take("GAMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBERFOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub km: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub ref_: Vec<f64>,
    pub prten: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<i64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub pra: Vec<f64>,
    pub lcunld: Vec<i64>,
    pub hu: Vec<f64>,
    pub shape: Vec<f64>,
    pub stol: Vec<f64>,
    pub visco: Vec<f64>,
    pub hisout: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub vflag: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBERFOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBERFOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            km: t.take("KM").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            prten: t.take("PRTEN").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_int()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            pra: t.take("PRA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
            hu: t.take("HU").and_then(|c| c.into_float()).unwrap_or_default(),
            shape: t.take("SHAPE").and_then(|c| c.into_float()).unwrap_or_default(),
            stol: t.take("STOL").and_then(|c| c.into_float()).unwrap_or_default(),
            visco: t.take("VISCO").and_then(|c| c.into_float()).unwrap_or_default(),
            hisout: t.take("HISOUT").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("Gi").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAi").and_then(|c| c.into_float()).unwrap_or_default(),
            vflag: t.take("VFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<f64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub lcunld: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_float()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE_LOG_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub mu: Vec<f64>,
    pub g: Vec<f64>,
    pub sigf: Vec<f64>,
    pub sgl: Vec<f64>,
    pub sw: Vec<f64>,
    pub st: Vec<f64>,
    pub lc_tbid: Vec<f64>,
    pub tension: Vec<f64>,
    pub rtype: Vec<f64>,
    pub avgopt: Vec<f64>,
    pub lcunld: Vec<i64>,
}
impl MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE_LOG_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SIMPLIFIED_RUBBER_WITH_DAMAGE_LOG_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            sigf: t.take("SIGF").and_then(|c| c.into_float()).unwrap_or_default(),
            sgl: t.take("SGL").and_then(|c| c.into_float()).unwrap_or_default(),
            sw: t.take("SW").and_then(|c| c.into_float()).unwrap_or_default(),
            st: t.take("ST").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_tbid: t.take("LC/TBID").and_then(|c| c.into_float()).unwrap_or_default(),
            tension: t.take("TENSION").and_then(|c| c.into_float()).unwrap_or_default(),
            rtype: t.take("RTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
            avgopt: t.take("AVGOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcunld: t.take("LCUNLD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SMOOTH_VISCOELASTIC_VISCOPLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub a0: Vec<f64>,
    pub b0: Vec<f64>,
    pub a1: Vec<f64>,
    pub b1: Vec<f64>,
    pub m: Vec<f64>,
    pub kapas: Vec<f64>,
    pub kapa0: Vec<f64>,
    pub shear: Vec<f64>,
    pub a0_2: Vec<f64>,
    pub b0_2: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub b1_2: Vec<f64>,
    pub m_2: Vec<f64>,
    pub kapas_2: Vec<f64>,
    pub kapa0_2: Vec<f64>,
    pub shear_2: Vec<f64>,
    pub a0_3: Vec<f64>,
    pub b0_3: Vec<f64>,
    pub a1_3: Vec<f64>,
    pub b1_3: Vec<f64>,
    pub m_3: Vec<f64>,
    pub kapas_3: Vec<f64>,
    pub kapa0_3: Vec<f64>,
    pub shear_3: Vec<f64>,
    pub a0_4: Vec<f64>,
    pub b0_4: Vec<f64>,
    pub a1_4: Vec<f64>,
    pub b1_4: Vec<f64>,
    pub m_4: Vec<f64>,
    pub kapas_4: Vec<f64>,
    pub kapa0_4: Vec<f64>,
    pub shear_4: Vec<f64>,
    pub a0_5: Vec<f64>,
    pub b0_5: Vec<f64>,
    pub a1_5: Vec<f64>,
    pub b1_5: Vec<f64>,
    pub m_5: Vec<f64>,
    pub kapas_5: Vec<f64>,
    pub kapa0_5: Vec<f64>,
    pub shear_5: Vec<f64>,
    pub a0_6: Vec<f64>,
    pub b0_6: Vec<f64>,
    pub a1_6: Vec<f64>,
    pub b1_6: Vec<f64>,
    pub m_6: Vec<f64>,
    pub kapas_6: Vec<f64>,
    pub kapa0_6: Vec<f64>,
    pub shear_6: Vec<f64>,
}
impl MAT_SMOOTH_VISCOELASTIC_VISCOPLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SMOOTH_VISCOELASTIC_VISCOPLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_2: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_2: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_2: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_2: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_2: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_2: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_2: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_3: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_3: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_3: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_3: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_3: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_3: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_3: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_3: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_4: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_4: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_4: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_4: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_4: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_4: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_4: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_4: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_5: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_5: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_5: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_5: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_5: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_5: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_5: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_5: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            a0_6: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            b0_6: t.take("B0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_6: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1_6: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            m_6: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            kapas_6: t.take("KAPAS").and_then(|c| c.into_float()).unwrap_or_default(),
            kapa0_6: t.take("KAPA0").and_then(|c| c.into_float()).unwrap_or_default(),
            shear_6: t.take("SHEAR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOFT_TISSUE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub xk: Vec<f64>,
    pub xlam: Vec<f64>,
    pub fang: Vec<f64>,
    pub xlam0: Vec<f64>,
    pub failsf: Vec<f64>,
    pub failsm: Vec<f64>,
    pub failshr: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub la1: Vec<f64>,
    pub la2: Vec<f64>,
    pub la3: Vec<f64>,
    pub macf: Vec<i64>,
}
impl MAT_SOFT_TISSUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOFT_TISSUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            xk: t.take("XK").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam: t.take("XLAM").and_then(|c| c.into_float()).unwrap_or_default(),
            fang: t.take("FANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam0: t.take("XLAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            failsf: t.take("FAILSF").and_then(|c| c.into_float()).unwrap_or_default(),
            failsm: t.take("FAILSM").and_then(|c| c.into_float()).unwrap_or_default(),
            failshr: t.take("FAILSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            la1: t.take("LA1").and_then(|c| c.into_float()).unwrap_or_default(),
            la2: t.take("LA2").and_then(|c| c.into_float()).unwrap_or_default(),
            la3: t.take("LA3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOFT_TISSUE_VISCO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub xk: Vec<f64>,
    pub xlam: Vec<f64>,
    pub fang: Vec<f64>,
    pub xlam0: Vec<f64>,
    pub failsf: Vec<f64>,
    pub failsm: Vec<f64>,
    pub failshr: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ax: Vec<f64>,
    pub ay: Vec<f64>,
    pub az: Vec<f64>,
    pub bx: Vec<f64>,
    pub by: Vec<f64>,
    pub bz: Vec<f64>,
    pub la1: Vec<f64>,
    pub la2: Vec<f64>,
    pub la3: Vec<f64>,
    pub macf: Vec<i64>,
    pub s1: Vec<f64>,
    pub s2: Vec<f64>,
    pub s3: Vec<f64>,
    pub s4: Vec<f64>,
    pub s5: Vec<f64>,
    pub s6: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
}
impl MAT_SOFT_TISSUE_VISCO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOFT_TISSUE_VISCO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            xk: t.take("XK").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam: t.take("XLAM").and_then(|c| c.into_float()).unwrap_or_default(),
            fang: t.take("FANG").and_then(|c| c.into_float()).unwrap_or_default(),
            xlam0: t.take("XLAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            failsf: t.take("FAILSF").and_then(|c| c.into_float()).unwrap_or_default(),
            failsm: t.take("FAILSM").and_then(|c| c.into_float()).unwrap_or_default(),
            failshr: t.take("FAILSHR").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ax: t.take("AX").and_then(|c| c.into_float()).unwrap_or_default(),
            ay: t.take("AY").and_then(|c| c.into_float()).unwrap_or_default(),
            az: t.take("AZ").and_then(|c| c.into_float()).unwrap_or_default(),
            bx: t.take("BX").and_then(|c| c.into_float()).unwrap_or_default(),
            by: t.take("BY").and_then(|c| c.into_float()).unwrap_or_default(),
            bz: t.take("BZ").and_then(|c| c.into_float()).unwrap_or_default(),
            la1: t.take("LA1").and_then(|c| c.into_float()).unwrap_or_default(),
            la2: t.take("LA2").and_then(|c| c.into_float()).unwrap_or_default(),
            la3: t.take("LA3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            s1: t.take("S1").and_then(|c| c.into_float()).unwrap_or_default(),
            s2: t.take("S2").and_then(|c| c.into_float()).unwrap_or_default(),
            s3: t.take("S3").and_then(|c| c.into_float()).unwrap_or_default(),
            s4: t.take("S4").and_then(|c| c.into_float()).unwrap_or_default(),
            s5: t.take("S5").and_then(|c| c.into_float()).unwrap_or_default(),
            s6: t.take("S6").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOIL_AND_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub bulk: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub pc: Vec<f64>,
    pub vcr: Vec<f64>,
    pub ref_: Vec<f64>,
    pub lcid: Vec<i64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p9: Vec<f64>,
    pub p10: Vec<f64>,
}
impl MAT_SOIL_AND_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOIL_AND_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vcr: t.take("VCR").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p9: t.take("P9").and_then(|c| c.into_float()).unwrap_or_default(),
            p10: t.take("P10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOIL_AND_FOAM_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub bulk: Vec<f64>,
    pub a0: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub pc: Vec<f64>,
    pub vcr: Vec<f64>,
    pub ref_: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub eps9: Vec<f64>,
    pub eps10: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p9: Vec<f64>,
    pub p10: Vec<f64>,
}
impl MAT_SOIL_AND_FOAM_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOIL_AND_FOAM_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            a0: t.take("A0").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            vcr: t.take("VCR").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            eps9: t.take("EPS9").and_then(|c| c.into_float()).unwrap_or_default(),
            eps10: t.take("EPS10").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p9: t.take("P9").and_then(|c| c.into_float()).unwrap_or_default(),
            p10: t.take("P10").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOIL_BRICK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub rlamda: Vec<f64>,
    pub rkappa: Vec<f64>,
    pub riota: Vec<f64>,
    pub rbeta1: Vec<f64>,
    pub rbeta2: Vec<f64>,
    pub rmu: Vec<f64>,
    pub rnu: Vec<f64>,
    pub rlcid: Vec<f64>,
    pub tol: Vec<f64>,
    pub pgcl: Vec<f64>,
    pub sub_inc: Vec<f64>,
    pub blk: Vec<f64>,
    pub grav: Vec<f64>,
    pub theory: Vec<i64>,
}
impl MAT_SOIL_BRICK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOIL_BRICK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            rlamda: t.take("RLAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            rkappa: t.take("RKAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            riota: t.take("RIOTA").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta1: t.take("RBETA1").and_then(|c| c.into_float()).unwrap_or_default(),
            rbeta2: t.take("RBETA2").and_then(|c| c.into_float()).unwrap_or_default(),
            rmu: t.take("RMU").and_then(|c| c.into_float()).unwrap_or_default(),
            rnu: t.take("RNU").and_then(|c| c.into_float()).unwrap_or_default(),
            rlcid: t.take("RLCID").and_then(|c| c.into_float()).unwrap_or_default(),
            tol: t.take("TOL").and_then(|c| c.into_float()).unwrap_or_default(),
            pgcl: t.take("PGCL").and_then(|c| c.into_float()).unwrap_or_default(),
            sub_inc: t.take("SUB-INC").and_then(|c| c.into_float()).unwrap_or_default(),
            blk: t.take("BLK").and_then(|c| c.into_float()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_float()).unwrap_or_default(),
            theory: t.take("THEORY").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SOIL_CONCRETE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub lcpv: Vec<i64>,
    pub lcyp: Vec<i64>,
    pub lcfp: Vec<i64>,
    pub lcrp: Vec<i64>,
    pub pc: Vec<f64>,
    pub out: Vec<f64>,
    pub b: Vec<f64>,
    pub fail: Vec<f64>,
}
impl MAT_SOIL_CONCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SOIL_CONCRETE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpv: t.take("LCPV").and_then(|c| c.into_int()).unwrap_or_default(),
            lcyp: t.take("LCYP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfp: t.take("LCFP").and_then(|c| c.into_int()).unwrap_or_default(),
            lcrp: t.take("LCRP").and_then(|c| c.into_int()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            out: t.take("OUT").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPECIAL_ORTHOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ys: Vec<f64>,
    pub ep: Vec<f64>,
    pub e11p: Vec<f64>,
    pub e22p: Vec<f64>,
    pub v12p: Vec<f64>,
    pub v21p: Vec<f64>,
    pub g12p: Vec<f64>,
    pub g23p: Vec<f64>,
    pub g31p: Vec<f64>,
    pub e11b: Vec<f64>,
    pub e22b: Vec<f64>,
    pub v12b: Vec<f64>,
    pub v21b: Vec<f64>,
    pub g12b: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_SPECIAL_ORTHOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPECIAL_ORTHOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            ep: t.take("EP").and_then(|c| c.into_float()).unwrap_or_default(),
            e11p: t.take("E11P").and_then(|c| c.into_float()).unwrap_or_default(),
            e22p: t.take("E22P").and_then(|c| c.into_float()).unwrap_or_default(),
            v12p: t.take("V12P").and_then(|c| c.into_float()).unwrap_or_default(),
            v21p: t.take("V21P").and_then(|c| c.into_float()).unwrap_or_default(),
            g12p: t.take("G12P").and_then(|c| c.into_float()).unwrap_or_default(),
            g23p: t.take("G23P").and_then(|c| c.into_float()).unwrap_or_default(),
            g31p: t.take("G31P").and_then(|c| c.into_float()).unwrap_or_default(),
            e11b: t.take("E11B").and_then(|c| c.into_float()).unwrap_or_default(),
            e22b: t.take("E22B").and_then(|c| c.into_float()).unwrap_or_default(),
            v12b: t.take("V12B").and_then(|c| c.into_float()).unwrap_or_default(),
            v21b: t.take("V21B").and_then(|c| c.into_float()).unwrap_or_default(),
            g12b: t.take("G12B").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_01 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub rc: Vec<f64>,
    pub rn: Vec<f64>,
}
impl MAT_SPH_01 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_01").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_02 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mu: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub stens: Vec<f64>,
}
impl MAT_SPH_02 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_02").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            stens: t.take("STENS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_03 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub beta: Vec<f64>,
    pub rough: Vec<f64>,
    pub adh: Vec<f64>,
}
impl MAT_SPH_03 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_03").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rough: t.take("ROUGH").and_then(|c| c.into_float()).unwrap_or_default(),
            adh: t.take("ADH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_IMPLICIT_FLUID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mu: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub stens: Vec<f64>,
}
impl MAT_SPH_IMPLICIT_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_IMPLICIT_FLUID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            stens: t.take("STENS").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_IMPLICIT_STRUCTURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub beta: Vec<f64>,
    pub rough: Vec<f64>,
    pub adh: Vec<f64>,
}
impl MAT_SPH_IMPLICIT_STRUCTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_IMPLICIT_STRUCTURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rough: t.take("ROUGH").and_then(|c| c.into_float()).unwrap_or_default(),
            adh: t.take("ADH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_INCOMPRESSIBLE_FLUID {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mu: Vec<f64>,
    pub gamma1: Vec<f64>,
    pub gamma2: Vec<f64>,
    pub stens: Vec<f64>,
    pub cp: Vec<f64>,
    pub lambda: Vec<f64>,
}
impl MAT_SPH_INCOMPRESSIBLE_FLUID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_INCOMPRESSIBLE_FLUID").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma1: t.take("GAMMA1").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma2: t.take("GAMMA2").and_then(|c| c.into_float()).unwrap_or_default(),
            stens: t.take("STENS").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            lambda: t.take("LAMBDA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_INCOMPRESSIBLE_STRUCTURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub beta: Vec<f64>,
    pub rough: Vec<f64>,
    pub adh: Vec<f64>,
}
impl MAT_SPH_INCOMPRESSIBLE_STRUCTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_INCOMPRESSIBLE_STRUCTURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rough: t.take("ROUGH").and_then(|c| c.into_float()).unwrap_or_default(),
            adh: t.take("ADH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPH_VISCOUS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub pc: Vec<f64>,
    pub mulo: Vec<f64>,
    pub muhi: Vec<f64>,
    pub rk: Vec<f64>,
    pub rc: Vec<f64>,
    pub rn: Vec<f64>,
}
impl MAT_SPH_VISCOUS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPH_VISCOUS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            mulo: t.take("MULO").and_then(|c| c.into_float()).unwrap_or_default(),
            muhi: t.take("MUHI").and_then(|c| c.into_float()).unwrap_or_default(),
            rk: t.take("RK").and_then(|c| c.into_float()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_float()).unwrap_or_default(),
            rn: t.take("RN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
}
impl MAT_SPOTWELD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_DAIMLERCHRYSLER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub unused_3_2: Vec<f64>,
    pub unused_4: Vec<f64>,
    pub unused_5: Vec<f64>,
    pub unused_6: Vec<f64>,
    pub nf: Vec<f64>,
    pub rs: Vec<f64>,
    pub asff: Vec<i64>,
    pub f: Vec<f64>,
    pub true_t: Vec<f64>,
    pub con_id: Vec<i64>,
    pub rfiltf: Vec<f64>,
    pub jtol: Vec<f64>,
}
impl MAT_SPOTWELD_DAIMLERCHRYSLER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_DAIMLERCHRYSLER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            asff: t.take("ASFF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("_").and_then(|c| c.into_float()).unwrap_or_default(),
            true_t: t.take("TRUE_T").and_then(|c| c.into_float()).unwrap_or_default(),
            con_id: t.take("CON_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
            jtol: t.take("JTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_DAIMLERCHRYSLER_UNIAXIAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub unused_3: Vec<f64>,
    pub unused_2_2: Vec<f64>,
    pub unused_3_2: Vec<f64>,
    pub unused_4: Vec<f64>,
    pub unused_5: Vec<f64>,
    pub unused_6: Vec<f64>,
    pub nf: Vec<f64>,
    pub rs: Vec<f64>,
    pub asff: Vec<i64>,
    pub f: Vec<f64>,
    pub true_t: Vec<f64>,
    pub con_id: Vec<i64>,
    pub rfiltf: Vec<f64>,
    pub jtol: Vec<f64>,
}
impl MAT_SPOTWELD_DAIMLERCHRYSLER_UNIAXIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_DAIMLERCHRYSLER_UNIAXIAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_3_2: t.take("UNUSED_3").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED_4").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_5: t.take("UNUSED_5").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_6: t.take("UNUSED_6").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            rs: t.take("RS").and_then(|c| c.into_float()).unwrap_or_default(),
            asff: t.take("ASFF").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("_").and_then(|c| c.into_float()).unwrap_or_default(),
            true_t: t.take("TRUE_T").and_then(|c| c.into_float()).unwrap_or_default(),
            con_id: t.take("CON_ID").and_then(|c| c.into_int()).unwrap_or_default(),
            rfiltf: t.take("RFILTF").and_then(|c| c.into_float()).unwrap_or_default(),
            jtol: t.take("JTOL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_DAMAGE_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
    pub efail_2: Vec<f64>,
    pub sigax: Vec<f64>,
    pub sigtu: Vec<f64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub f60: Vec<i64>,
    pub nf_2: Vec<f64>,
}
impl MAT_SPOTWELD_DAMAGE_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_DAMAGE-FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            efail_2: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigax: t.take("SIGAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigtu: t.take("SIGTU").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            nf_2: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_DAMAGE_FAILURE_UNIAXIAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
    pub efail_2: Vec<f64>,
    pub sigax: Vec<f64>,
    pub sigtu: Vec<f64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub f60: Vec<i64>,
    pub nf_2: Vec<f64>,
}
impl MAT_SPOTWELD_DAMAGE_FAILURE_UNIAXIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_DAMAGE-FAILURE_UNIAXIAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            efail_2: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigax: t.take("SIGAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigtu: t.take("SIGTU").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            nf_2: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_DAMAGE_FAILURE_2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
    pub efail_2: Vec<f64>,
    pub sigax: Vec<f64>,
    pub sigtu: Vec<f64>,
    pub f30: Vec<i64>,
    pub f40: Vec<i64>,
    pub f50: Vec<i64>,
    pub f60: Vec<i64>,
    pub nf_2: Vec<f64>,
}
impl MAT_SPOTWELD_DAMAGE_FAILURE_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_DAMAGE_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
            efail_2: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            sigax: t.take("SIGAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sigtu: t.take("SIGTU").and_then(|c| c.into_float()).unwrap_or_default(),
            f30: t.take("f30").and_then(|c| c.into_int()).unwrap_or_default(),
            f40: t.take("f40").and_then(|c| c.into_int()).unwrap_or_default(),
            f50: t.take("f50").and_then(|c| c.into_int()).unwrap_or_default(),
            f60: t.take("f60").and_then(|c| c.into_int()).unwrap_or_default(),
            nf_2: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPOTWELD_UNIAXIAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub eh: Vec<f64>,
    pub dt: Vec<f64>,
    pub tfail: Vec<f64>,
    pub efail: Vec<f64>,
    pub nrr: Vec<f64>,
    pub nrs: Vec<f64>,
    pub nrt: Vec<f64>,
    pub mrr: Vec<f64>,
    pub mss: Vec<f64>,
    pub mtt: Vec<f64>,
    pub nf: Vec<f64>,
}
impl MAT_SPOTWELD_UNIAXIAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPOTWELD_UNIAXIAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            tfail: t.take("TFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            efail: t.take("EFAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            nrr: t.take("NRR").and_then(|c| c.into_float()).unwrap_or_default(),
            nrs: t.take("NRS").and_then(|c| c.into_float()).unwrap_or_default(),
            nrt: t.take("NRT").and_then(|c| c.into_float()).unwrap_or_default(),
            mrr: t.take("MRR").and_then(|c| c.into_float()).unwrap_or_default(),
            mss: t.take("MSS").and_then(|c| c.into_float()).unwrap_or_default(),
            mtt: t.take("MTT").and_then(|c| c.into_float()).unwrap_or_default(),
            nf: t.take("NF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_ELASTIC {
    pub mid: Vec<i64>,
    pub k: Vec<f64>,
}
impl MAT_SPRING_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_ELASTOPLASTIC {
    pub mid: Vec<i64>,
    pub k: Vec<f64>,
    pub kt: Vec<f64>,
    pub fy: Vec<f64>,
}
impl MAT_SPRING_ELASTOPLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_ELASTOPLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            kt: t.take("KT").and_then(|c| c.into_float()).unwrap_or_default(),
            fy: t.take("FY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_GENERAL_NONLINEAR {
    pub mid: Vec<i64>,
    pub lcdl: Vec<i64>,
    pub lcdu: Vec<i64>,
    pub beta: Vec<f64>,
    pub tyi: Vec<f64>,
    pub cyi: Vec<f64>,
}
impl MAT_SPRING_GENERAL_NONLINEAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_GENERAL_NONLINEAR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdl: t.take("LCDL").and_then(|c| c.into_int()).unwrap_or_default(),
            lcdu: t.take("LCDU").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tyi: t.take("TYI").and_then(|c| c.into_float()).unwrap_or_default(),
            cyi: t.take("CYI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_INELASTIC {
    pub mid: Vec<i64>,
    pub lcfd: Vec<i64>,
    pub ku: Vec<f64>,
    pub ctf: Vec<f64>,
}
impl MAT_SPRING_INELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_INELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcfd: t.take("LCFD").and_then(|c| c.into_int()).unwrap_or_default(),
            ku: t.take("KU").and_then(|c| c.into_float()).unwrap_or_default(),
            ctf: t.take("CTF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_MAXWELL {
    pub mid: Vec<i64>,
    pub k0: Vec<f64>,
    pub ki: Vec<f64>,
    pub beta: Vec<f64>,
    pub tc: Vec<f64>,
    pub fc: Vec<f64>,
    pub copt: Vec<f64>,
}
impl MAT_SPRING_MAXWELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_MAXWELL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            k0: t.take("K0").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
            fc: t.take("FC").and_then(|c| c.into_float()).unwrap_or_default(),
            copt: t.take("COPT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_MUSCLE {
    pub mid: Vec<i64>,
    pub lo: Vec<f64>,
    pub vmax: Vec<f64>,
    pub sv: Vec<f64>,
    pub a: Vec<f64>,
    pub fmax: Vec<f64>,
    pub tl: Vec<f64>,
    pub tv: Vec<f64>,
    pub fpe: Vec<f64>,
    pub lmax: Vec<f64>,
    pub ksh: Vec<f64>,
}
impl MAT_SPRING_MUSCLE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_MUSCLE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lo: t.take("LO").and_then(|c| c.into_float()).unwrap_or_default(),
            vmax: t.take("VMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sv: t.take("SV").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            fmax: t.take("FMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            tl: t.take("TL").and_then(|c| c.into_float()).unwrap_or_default(),
            tv: t.take("TV").and_then(|c| c.into_float()).unwrap_or_default(),
            fpe: t.take("FPE").and_then(|c| c.into_float()).unwrap_or_default(),
            lmax: t.take("LMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ksh: t.take("KSH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_NONLINEAR_ELASTIC {
    pub mid: Vec<i64>,
    pub lcd: Vec<i64>,
    pub lcr: Vec<i64>,
}
impl MAT_SPRING_NONLINEAR_ELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_NONLINEAR_ELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcd: t.take("LCD").and_then(|c| c.into_int()).unwrap_or_default(),
            lcr: t.take("LCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_SQUAT_SHEARWALL {
    pub mid: Vec<i64>,
    pub a14: Vec<f64>,
    pub b14: Vec<f64>,
    pub c14: Vec<f64>,
    pub d14: Vec<f64>,
    pub e14: Vec<f64>,
    pub lcid: Vec<i64>,
    pub psd: Vec<f64>,
}
impl MAT_SPRING_SQUAT_SHEARWALL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_SQUAT_SHEARWALL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            a14: t.take("A14").and_then(|c| c.into_float()).unwrap_or_default(),
            b14: t.take("B14").and_then(|c| c.into_float()).unwrap_or_default(),
            c14: t.take("C14").and_then(|c| c.into_float()).unwrap_or_default(),
            d14: t.take("D14").and_then(|c| c.into_float()).unwrap_or_default(),
            e14: t.take("E14").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            psd: t.take("PSD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPRING_TRILINEAR_DEGRADING {
    pub mid: Vec<i64>,
    pub defl1: Vec<f64>,
    pub f1: Vec<f64>,
    pub defl2: Vec<f64>,
    pub f2: Vec<f64>,
    pub defl3: Vec<f64>,
    pub f3: Vec<f64>,
    pub fflag: Vec<f64>,
}
impl MAT_SPRING_TRILINEAR_DEGRADING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPRING_TRILINEAR_DEGRADING").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            defl1: t.take("DEFL1").and_then(|c| c.into_float()).unwrap_or_default(),
            f1: t.take("F1").and_then(|c| c.into_float()).unwrap_or_default(),
            defl2: t.take("DEFL2").and_then(|c| c.into_float()).unwrap_or_default(),
            f2: t.take("F2").and_then(|c| c.into_float()).unwrap_or_default(),
            defl3: t.take("DEFL3").and_then(|c| c.into_float()).unwrap_or_default(),
            f3: t.take("F3").and_then(|c| c.into_float()).unwrap_or_default(),
            fflag: t.take("FFLAG").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_SPR_JLR {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub helas: Vec<f64>,
    pub telas: Vec<f64>,
    pub lcaxh: Vec<i64>,
    pub lcshh: Vec<i64>,
    pub lcbmh: Vec<i64>,
    pub sfaxh: Vec<f64>,
    pub sfshh: Vec<f64>,
    pub sfbmh: Vec<f64>,
    pub dfakh: Vec<f64>,
    pub dfshh: Vec<f64>,
    pub rfbmh: Vec<f64>,
    pub dmfaxh: Vec<f64>,
    pub dmfshh: Vec<f64>,
    pub dmfbmh: Vec<f64>,
    pub lcaxt: Vec<i64>,
    pub lcsht: Vec<i64>,
    pub lcbmt: Vec<i64>,
    pub sfaxt: Vec<f64>,
    pub sfsht: Vec<f64>,
    pub sbfmt: Vec<f64>,
    pub dfaxt: Vec<f64>,
    pub dfsht: Vec<f64>,
    pub rfbmt: Vec<f64>,
    pub dfmaxt: Vec<f64>,
    pub dmfsht: Vec<f64>,
    pub dmfbmt: Vec<f64>,
}
impl MAT_SPR_JLR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_SPR_JLR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            helas: t.take("HELAS").and_then(|c| c.into_float()).unwrap_or_default(),
            telas: t.take("TELAS").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaxh: t.take("LCAXH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcshh: t.take("LCSHH").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbmh: t.take("LCBMH").and_then(|c| c.into_int()).unwrap_or_default(),
            sfaxh: t.take("SFAXH").and_then(|c| c.into_float()).unwrap_or_default(),
            sfshh: t.take("SFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            sfbmh: t.take("SFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfakh: t.take("DFAKH").and_then(|c| c.into_float()).unwrap_or_default(),
            dfshh: t.take("DFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            rfbmh: t.take("RFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfaxh: t.take("DMFAXH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfshh: t.take("DMFSHH").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfbmh: t.take("DMFBMH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcaxt: t.take("LCAXT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsht: t.take("LCSHT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcbmt: t.take("LCBMT").and_then(|c| c.into_int()).unwrap_or_default(),
            sfaxt: t.take("SFAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            sfsht: t.take("SFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            sbfmt: t.take("SBFMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfaxt: t.take("DFAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfsht: t.take("DFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            rfbmt: t.take("RFBMT").and_then(|c| c.into_float()).unwrap_or_default(),
            dfmaxt: t.take("DFMAXT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfsht: t.take("DMFSHT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmfbmt: t.take("DMFBMT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STEEL_CONCENTRIC_BRACE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub ym: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub lamda: Vec<f64>,
    pub fbuck: Vec<f64>,
    pub fbuck2: Vec<f64>,
    pub ccbrf: Vec<f64>,
    pub bcur: Vec<f64>,
    pub ts1: Vec<f64>,
    pub ts2: Vec<f64>,
    pub ts3: Vec<f64>,
    pub ts4: Vec<f64>,
    pub cs1: Vec<f64>,
    pub cs2: Vec<f64>,
    pub cs3: Vec<f64>,
    pub cs4: Vec<f64>,
}
impl MAT_STEEL_CONCENTRIC_BRACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STEEL_CONCENTRIC_BRACE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lamda: t.take("LAMDA").and_then(|c| c.into_float()).unwrap_or_default(),
            fbuck: t.take("FBUCK").and_then(|c| c.into_float()).unwrap_or_default(),
            fbuck2: t.take("FBUCK2").and_then(|c| c.into_float()).unwrap_or_default(),
            ccbrf: t.take("CCBRF").and_then(|c| c.into_float()).unwrap_or_default(),
            bcur: t.take("BCUR").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts3: t.take("TS3").and_then(|c| c.into_float()).unwrap_or_default(),
            ts4: t.take("TS4").and_then(|c| c.into_float()).unwrap_or_default(),
            cs1: t.take("CS1").and_then(|c| c.into_float()).unwrap_or_default(),
            cs2: t.take("CS2").and_then(|c| c.into_float()).unwrap_or_default(),
            cs3: t.take("CS3").and_then(|c| c.into_float()).unwrap_or_default(),
            cs4: t.take("CS4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STEEL_EC3 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sgy: Vec<f64>,
    pub lc_e: Vec<i64>,
    pub lc_pr: Vec<i64>,
    pub lc_al: Vec<i64>,
    pub tbl_ss: Vec<i64>,
    pub lc_fs: Vec<i64>,
}
impl MAT_STEEL_EC3 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STEEL_EC3").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sgy: t.take("SGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lc_e: t.take("LC_E").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_pr: t.take("LC_PR").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_al: t.take("LC_AL").and_then(|c| c.into_int()).unwrap_or_default(),
            tbl_ss: t.take("TBL_SS").and_then(|c| c.into_int()).unwrap_or_default(),
            lc_fs: t.take("LC_FS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STEINBERG {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g0: Vec<f64>,
    pub sigo: Vec<f64>,
    pub beta: Vec<f64>,
    pub n: Vec<f64>,
    pub gama: Vec<f64>,
    pub sigm: Vec<f64>,
    pub b: Vec<f64>,
    pub bp: Vec<f64>,
    pub h: Vec<f64>,
    pub f: Vec<f64>,
    pub a: Vec<f64>,
    pub tmo: Vec<f64>,
    pub gamo: Vec<f64>,
    pub sa: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub rp: Vec<f64>,
    pub flag: Vec<f64>,
    pub mmn: Vec<f64>,
    pub mmx: Vec<f64>,
    pub eco: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec2: Vec<f64>,
    pub ec3: Vec<f64>,
    pub ec4: Vec<f64>,
    pub ec5: Vec<f64>,
    pub ec6: Vec<f64>,
    pub ec7: Vec<f64>,
    pub ec8: Vec<f64>,
    pub ec9: Vec<f64>,
}
impl MAT_STEINBERG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STEINBERG").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigo: t.take("SIGO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigm: t.take("SIGM").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            bp: t.take("BP").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            tmo: t.take("TMO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamo: t.take("GAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            sa: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            mmn: t.take("MMN").and_then(|c| c.into_float()).unwrap_or_default(),
            mmx: t.take("MMX").and_then(|c| c.into_float()).unwrap_or_default(),
            eco: t.take("ECO").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec3: t.take("EC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ec4: t.take("EC4").and_then(|c| c.into_float()).unwrap_or_default(),
            ec5: t.take("EC5").and_then(|c| c.into_float()).unwrap_or_default(),
            ec6: t.take("EC6").and_then(|c| c.into_float()).unwrap_or_default(),
            ec7: t.take("EC7").and_then(|c| c.into_float()).unwrap_or_default(),
            ec8: t.take("EC8").and_then(|c| c.into_float()).unwrap_or_default(),
            ec9: t.take("EC9").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STEINBERG_LUND {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub g0: Vec<f64>,
    pub sigo: Vec<f64>,
    pub beta: Vec<f64>,
    pub n: Vec<f64>,
    pub gama: Vec<f64>,
    pub sigm: Vec<f64>,
    pub b: Vec<f64>,
    pub bp: Vec<f64>,
    pub h: Vec<f64>,
    pub f: Vec<f64>,
    pub a: Vec<f64>,
    pub tmo: Vec<f64>,
    pub gamo: Vec<f64>,
    pub sa: Vec<f64>,
    pub pc: Vec<f64>,
    pub spall: Vec<f64>,
    pub rp: Vec<f64>,
    pub flag: Vec<f64>,
    pub mmn: Vec<f64>,
    pub mmx: Vec<f64>,
    pub eco: Vec<f64>,
    pub ec1: Vec<f64>,
    pub ec2: Vec<f64>,
    pub ec3: Vec<f64>,
    pub ec4: Vec<f64>,
    pub ec5: Vec<f64>,
    pub ec6: Vec<f64>,
    pub ec7: Vec<f64>,
    pub ec8: Vec<f64>,
    pub ec9: Vec<f64>,
    pub uk: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub yp: Vec<f64>,
    pub ya: Vec<f64>,
    pub ym: Vec<f64>,
}
impl MAT_STEINBERG_LUND {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STEINBERG_LUND").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            sigo: t.take("SIGO").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            sigm: t.take("SIGM").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            bp: t.take("BP").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            tmo: t.take("TMO").and_then(|c| c.into_float()).unwrap_or_default(),
            gamo: t.take("GAMO").and_then(|c| c.into_float()).unwrap_or_default(),
            sa: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            pc: t.take("PC").and_then(|c| c.into_float()).unwrap_or_default(),
            spall: t.take("SPALL").and_then(|c| c.into_float()).unwrap_or_default(),
            rp: t.take("RP").and_then(|c| c.into_float()).unwrap_or_default(),
            flag: t.take("FLAG").and_then(|c| c.into_float()).unwrap_or_default(),
            mmn: t.take("MMN").and_then(|c| c.into_float()).unwrap_or_default(),
            mmx: t.take("MMX").and_then(|c| c.into_float()).unwrap_or_default(),
            eco: t.take("ECO").and_then(|c| c.into_float()).unwrap_or_default(),
            ec1: t.take("EC1").and_then(|c| c.into_float()).unwrap_or_default(),
            ec2: t.take("EC2").and_then(|c| c.into_float()).unwrap_or_default(),
            ec3: t.take("EC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ec4: t.take("EC4").and_then(|c| c.into_float()).unwrap_or_default(),
            ec5: t.take("EC5").and_then(|c| c.into_float()).unwrap_or_default(),
            ec6: t.take("EC6").and_then(|c| c.into_float()).unwrap_or_default(),
            ec7: t.take("EC7").and_then(|c| c.into_float()).unwrap_or_default(),
            ec8: t.take("EC8").and_then(|c| c.into_float()).unwrap_or_default(),
            ec9: t.take("EC9").and_then(|c| c.into_float()).unwrap_or_default(),
            uk: t.take("UK").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            ya: t.take("YA").and_then(|c| c.into_float()).unwrap_or_default(),
            ym: t.take("YM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STOUGHTON_NON_ASSOCIATED_FLOW {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub sig00: Vec<f64>,
    pub sig45: Vec<f64>,
    pub sig90: Vec<f64>,
    pub sig_b: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub scale: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_STOUGHTON_NON_ASSOCIATED_FLOW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STOUGHTON_NON_ASSOCIATED_FLOW").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig00: t.take("SIG00").and_then(|c| c.into_float()).unwrap_or_default(),
            sig45: t.take("SIG45").and_then(|c| c.into_float()).unwrap_or_default(),
            sig90: t.take("SIG90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_b: t.take("SIG_B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STOUGHTON_NON_ASSOCIATED_FLOW_XUE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub sig00: Vec<f64>,
    pub sig45: Vec<f64>,
    pub sig90: Vec<f64>,
    pub sig_b: Vec<f64>,
    pub lcids: Vec<i64>,
    pub lcidv: Vec<i64>,
    pub scale: Vec<f64>,
    pub ef0: Vec<f64>,
    pub plim: Vec<f64>,
    pub q: Vec<f64>,
    pub gama: Vec<f64>,
    pub m: Vec<f64>,
    pub beta: Vec<f64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_STOUGHTON_NON_ASSOCIATED_FLOW_XUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STOUGHTON_NON_ASSOCIATED_FLOW_XUE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig00: t.take("SIG00").and_then(|c| c.into_float()).unwrap_or_default(),
            sig45: t.take("SIG45").and_then(|c| c.into_float()).unwrap_or_default(),
            sig90: t.take("SIG90").and_then(|c| c.into_float()).unwrap_or_default(),
            sig_b: t.take("SIG_B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcids: t.take("LCIDS").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidv: t.take("LCIDV").and_then(|c| c.into_int()).unwrap_or_default(),
            scale: t.take("SCALE").and_then(|c| c.into_float()).unwrap_or_default(),
            ef0: t.take("EF0").and_then(|c| c.into_float()).unwrap_or_default(),
            plim: t.take("PLIM").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            gama: t.take("GAMA").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_STRAIN_RATE_DEPENDENT_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub vp: Vec<f64>,
    pub lc1: Vec<i64>,
    pub etan: Vec<f64>,
    pub lc2: Vec<i64>,
    pub lc3: Vec<i64>,
    pub lc4: Vec<i64>,
    pub tdel: Vec<f64>,
    pub rdef: Vec<f64>,
}
impl MAT_STRAIN_RATE_DEPENDENT_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_STRAIN_RATE_DEPENDENT_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            lc1: t.take("LC1").and_then(|c| c.into_int()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            lc2: t.take("LC2").and_then(|c| c.into_int()).unwrap_or_default(),
            lc3: t.take("LC3").and_then(|c| c.into_int()).unwrap_or_default(),
            lc4: t.take("LC4").and_then(|c| c.into_int()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            rdef: t.take("RDEF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T01 {
    pub tmid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hc: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_T01 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T01").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T02 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hc: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_T02 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T02").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T03 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
}
impl MAT_T03 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T03").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T04 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1_1: Vec<f64>,
    pub k1_2: Vec<f64>,
    pub k1_3: Vec<f64>,
    pub k1_4: Vec<f64>,
    pub k1_5: Vec<f64>,
    pub k1_6: Vec<f64>,
    pub k1_7: Vec<f64>,
    pub k1_8: Vec<f64>,
    pub k2_1: Vec<f64>,
    pub k2_2: Vec<f64>,
    pub k2_3: Vec<f64>,
    pub k2_4: Vec<f64>,
    pub k2_5: Vec<f64>,
    pub k2_6: Vec<f64>,
    pub k2_7: Vec<f64>,
    pub k2_8: Vec<f64>,
    pub k3_1: Vec<f64>,
    pub k_2: Vec<f64>,
    pub k3_3: Vec<f64>,
    pub k3_4: Vec<f64>,
    pub k3_5: Vec<f64>,
    pub k3_6: Vec<f64>,
    pub k3_7: Vec<f64>,
    pub k3_8: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_T04 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T04").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_1: t.take("(K1-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_2: t.take("(K1-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_3: t.take("(K1-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_4: t.take("(K1-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_5: t.take("(K1-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_6: t.take("(K1-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_7: t.take("(K1-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_8: t.take("(K1-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_1: t.take("(K2-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_2: t.take("(K2-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_3: t.take("(K2-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_4: t.take("(K2-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_5: t.take("(K2-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_6: t.take("(K2-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_7: t.take("(K2-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_8: t.take("(K2-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_1: t.take("(K3-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k_2: t.take("(K-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_3: t.take("(K3-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_4: t.take("(K3-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_5: t.take("(K3-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_6: t.take("(K3-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_7: t.take("(K3-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_8: t.take("(K3-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T05 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub hc: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_T05 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T05").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T06 {
    pub tmid: Vec<i64>,
    pub nchsp: Vec<i64>,
    pub nchrx: Vec<i64>,
    pub icend: Vec<i64>,
    pub cend: Vec<f64>,
    pub gasc: Vec<f64>,
    pub fid: Vec<i64>,
    pub mf: Vec<i64>,
    pub rhof: Vec<f64>,
    pub lccf: Vec<i64>,
    pub lckf: Vec<i64>,
    pub vff: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub lcci: Vec<i64>,
    pub lcki: Vec<i64>,
    pub vfi: Vec<f64>,
    pub mwi: Vec<f64>,
    pub rci1: Vec<f64>,
    pub rci2: Vec<f64>,
    pub rci3: Vec<f64>,
    pub rci4: Vec<f64>,
    pub rci5: Vec<f64>,
    pub rci6: Vec<f64>,
    pub rci7: Vec<f64>,
    pub rci8: Vec<f64>,
    pub rxi1: Vec<f64>,
    pub rxi2: Vec<f64>,
    pub rxi3: Vec<f64>,
    pub rxi4: Vec<f64>,
    pub rxi5: Vec<f64>,
    pub rxi6: Vec<f64>,
    pub rxi7: Vec<f64>,
    pub rxi8: Vec<f64>,
    pub lczi1: Vec<f64>,
    pub lczi2: Vec<f64>,
    pub lczi3: Vec<f64>,
    pub lczi4: Vec<f64>,
    pub lczi5: Vec<f64>,
    pub lczi6: Vec<f64>,
    pub lczi7: Vec<f64>,
    pub lczi8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub q4: Vec<f64>,
    pub q5: Vec<f64>,
    pub q6: Vec<f64>,
    pub q7: Vec<f64>,
    pub q8: Vec<f64>,
}
impl MAT_T06 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T06").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchsp: t.take("NCHSP").and_then(|c| c.into_int()).unwrap_or_default(),
            nchrx: t.take("NCHRX").and_then(|c| c.into_int()).unwrap_or_default(),
            icend: t.take("ICEND").and_then(|c| c.into_int()).unwrap_or_default(),
            cend: t.take("CEND").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            mf: t.take("MF").and_then(|c| c.into_int()).unwrap_or_default(),
            rhof: t.take("RHOf").and_then(|c| c.into_float()).unwrap_or_default(),
            lccf: t.take("LCCf").and_then(|c| c.into_int()).unwrap_or_default(),
            lckf: t.take("LCKf").and_then(|c| c.into_int()).unwrap_or_default(),
            vff: t.take("VFf").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOi").and_then(|c| c.into_float()).unwrap_or_default(),
            lcci: t.take("LCCi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcki: t.take("LCKi").and_then(|c| c.into_int()).unwrap_or_default(),
            vfi: t.take("VFi").and_then(|c| c.into_float()).unwrap_or_default(),
            mwi: t.take("MWi").and_then(|c| c.into_float()).unwrap_or_default(),
            rci1: t.take("RCi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rci2: t.take("RCi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rci3: t.take("RCi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rci4: t.take("RCi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rci5: t.take("RCi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rci6: t.take("RCi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rci7: t.take("RCi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rci8: t.take("RCi8").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi1: t.take("RXi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi2: t.take("RXi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi3: t.take("RXi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi4: t.take("RXi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi5: t.take("RXi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi6: t.take("RXi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi7: t.take("RXi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi8: t.take("RXi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            q4: t.take("Q4").and_then(|c| c.into_float()).unwrap_or_default(),
            q5: t.take("Q5").and_then(|c| c.into_float()).unwrap_or_default(),
            q6: t.take("Q6").and_then(|c| c.into_float()).unwrap_or_default(),
            q7: t.take("Q7").and_then(|c| c.into_float()).unwrap_or_default(),
            q8: t.take("Q8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T07 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub hdead: Vec<f64>,
    pub tdead: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub lchc: Vec<i64>,
    pub lctc: Vec<i64>,
    pub tlstart: Vec<f64>,
    pub tlend: Vec<f64>,
    pub tistart: Vec<f64>,
    pub tiend: Vec<f64>,
    pub hghost: Vec<f64>,
    pub tghost: Vec<f64>,
}
impl MAT_T07 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T07").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            hdead: t.take("HDEAD").and_then(|c| c.into_float()).unwrap_or_default(),
            tdead: t.take("TDEAD").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchc: t.take("LCHC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctc: t.take("LCTC").and_then(|c| c.into_int()).unwrap_or_default(),
            tlstart: t.take("TLSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tlend: t.take("TLEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tistart: t.take("TISTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tiend: t.take("TIEND").and_then(|c| c.into_float()).unwrap_or_default(),
            hghost: t.take("HGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            tghost: t.take("TGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T08 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lck1: Vec<i64>,
    pub lck2: Vec<i64>,
    pub lck3: Vec<i64>,
    pub ilcchsv: Vec<i64>,
    pub ilckhsv: Vec<f64>,
    pub itghsv: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_T08 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T08").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1: t.take("LCK1").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2: t.take("LCK2").and_then(|c| c.into_int()).unwrap_or_default(),
            lck3: t.take("LCK3").and_then(|c| c.into_int()).unwrap_or_default(),
            ilcchsv: t.take("ILCCHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            ilckhsv: t.take("ILCKHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            itghsv: t.take("ITGHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T09 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
    pub solt: Vec<f64>,
    pub liqt: Vec<f64>,
    pub lh: Vec<f64>,
}
impl MAT_T09 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T09").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
            solt: t.take("SOLT").and_then(|c| c.into_float()).unwrap_or_default(),
            liqt: t.take("LIQT").and_then(|c| c.into_float()).unwrap_or_default(),
            lh: t.take("LH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T10 {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hclc: Vec<i64>,
    pub tclc: Vec<i64>,
    pub hchsv: Vec<f64>,
    pub tchsv: Vec<f64>,
    pub tghsv: Vec<f64>,
}
impl MAT_T10 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T10").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hclc: t.take("HCLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tclc: t.take("TCLC").and_then(|c| c.into_int()).unwrap_or_default(),
            hchsv: t.take("HCHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            tchsv: t.take("TCHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            tghsv: t.take("TGHSV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T11 {
    pub tmid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mt: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iortho: Vec<i64>,
    pub ihve: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_T11 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T11").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mt: t.take("MT").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            ihve: t.take("IHVE").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_T17 {
    pub tmid: Vec<i64>,
    pub nchsp: Vec<i64>,
    pub nchrx: Vec<i64>,
    pub icend: Vec<i64>,
    pub cend: Vec<f64>,
    pub gasc: Vec<f64>,
    pub fid: Vec<i64>,
    pub mf: Vec<i64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub rhof: Vec<f64>,
    pub lccf: Vec<i64>,
    pub lck1f: Vec<i64>,
    pub lck2f: Vec<f64>,
    pub lck3f: Vec<f64>,
    pub vff: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub lcci: Vec<i64>,
    pub lck1i: Vec<i64>,
    pub lck2i: Vec<i64>,
    pub lck3i: Vec<i64>,
    pub vfi: Vec<f64>,
    pub mwi: Vec<f64>,
    pub rci1: Vec<f64>,
    pub rci2: Vec<f64>,
    pub rci3: Vec<f64>,
    pub rci4: Vec<f64>,
    pub rci5: Vec<f64>,
    pub rci6: Vec<f64>,
    pub rci7: Vec<f64>,
    pub rci8: Vec<f64>,
    pub rxi1: Vec<f64>,
    pub rxi2: Vec<f64>,
    pub rxi3: Vec<f64>,
    pub rxi4: Vec<f64>,
    pub rxi5: Vec<f64>,
    pub rxi6: Vec<f64>,
    pub rxi7: Vec<f64>,
    pub rxi8: Vec<f64>,
    pub lczi1: Vec<f64>,
    pub lczi2: Vec<f64>,
    pub lczi3: Vec<f64>,
    pub lczi4: Vec<f64>,
    pub lczi5: Vec<f64>,
    pub lczi6: Vec<f64>,
    pub lczi7: Vec<f64>,
    pub lczi8: Vec<f64>,
    pub lczi1_2: Vec<f64>,
    pub lczi2_2: Vec<f64>,
    pub lczi3_2: Vec<f64>,
    pub lczi4_2: Vec<f64>,
    pub lczi5_2: Vec<f64>,
    pub lczi6_2: Vec<f64>,
    pub lczi7_2: Vec<f64>,
    pub lczi8_2: Vec<f64>,
    pub lczi1_3: Vec<f64>,
    pub lczi2_3: Vec<f64>,
    pub lczi3_3: Vec<f64>,
    pub lczi4_3: Vec<f64>,
    pub lczi5_3: Vec<f64>,
    pub lczi6_3: Vec<f64>,
    pub lczi7_3: Vec<f64>,
    pub lczi8_3: Vec<f64>,
}
impl MAT_T17 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_T17").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchsp: t.take("NCHSP").and_then(|c| c.into_int()).unwrap_or_default(),
            nchrx: t.take("NCHRX").and_then(|c| c.into_int()).unwrap_or_default(),
            icend: t.take("ICEND").and_then(|c| c.into_int()).unwrap_or_default(),
            cend: t.take("CEND").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            mf: t.take("MF").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            rhof: t.take("RHOf").and_then(|c| c.into_float()).unwrap_or_default(),
            lccf: t.take("LCCf").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1f: t.take("LCK1f").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2f: t.take("LCK2F").and_then(|c| c.into_float()).unwrap_or_default(),
            lck3f: t.take("LCK3F").and_then(|c| c.into_float()).unwrap_or_default(),
            vff: t.take("VFf").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOi").and_then(|c| c.into_float()).unwrap_or_default(),
            lcci: t.take("LCCi").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1i: t.take("LCK1i").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2i: t.take("LCK2i").and_then(|c| c.into_int()).unwrap_or_default(),
            lck3i: t.take("LCK3i").and_then(|c| c.into_int()).unwrap_or_default(),
            vfi: t.take("VFi").and_then(|c| c.into_float()).unwrap_or_default(),
            mwi: t.take("MWi").and_then(|c| c.into_float()).unwrap_or_default(),
            rci1: t.take("RCi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rci2: t.take("RCi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rci3: t.take("RCi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rci4: t.take("RCi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rci5: t.take("RCi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rci6: t.take("RCi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rci7: t.take("RCi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rci8: t.take("RCi8").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi1: t.take("RXi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi2: t.take("RXi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi3: t.take("RXi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi4: t.take("RXi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi5: t.take("RXi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi6: t.take("RXi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi7: t.take("RXi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi8: t.take("RXi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1_2: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2_2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3_2: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4_2: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5_2: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6_2: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7_2: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8_2: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1_3: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2_3: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3_3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4_3: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5_3: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6_3: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7_3: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8_3: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TABULATED_JOHNSON_COOK {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub tabk1: Vec<i64>,
    pub tabkt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub failopt: Vec<i64>,
    pub numavg: Vec<i64>,
    pub ncyfail: Vec<i64>,
    pub erode: Vec<i64>,
    pub lcps: Vec<i64>,
}
impl MAT_TABULATED_JOHNSON_COOK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TABULATED_JOHNSON_COOK").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            tabk1: t.take("TABK1").and_then(|c| c.into_int()).unwrap_or_default(),
            tabkt: t.take("TABKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            failopt: t.take("FAILOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            numavg: t.take("NUMAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyfail: t.take("NCYFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcps: t.take("LCPS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TABULATED_JOHNSON_COOK_GYS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub lck1: Vec<i64>,
    pub lckt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub lccr: Vec<i64>,
    pub lcct: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub lcst: Vec<i64>,
    pub iflag: Vec<i64>,
    pub sfiepm: Vec<f64>,
    pub niter: Vec<i64>,
}
impl MAT_TABULATED_JOHNSON_COOK_GYS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TABULATED_JOHNSON_COOK_GYS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lck1: t.take("LCK1").and_then(|c| c.into_int()).unwrap_or_default(),
            lckt: t.take("LCKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            lccr: t.take("LCCR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcct: t.take("LCCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcst: t.take("LCST").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfiepm: t.take("SFIEPM").and_then(|c| c.into_float()).unwrap_or_default(),
            niter: t.take("NITER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TABULATED_JOHNSON_COOK_GYS_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub lck1: Vec<i64>,
    pub lckt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub lccr: Vec<i64>,
    pub lcct: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub lcst: Vec<i64>,
    pub iflag: Vec<i64>,
    pub sfiepm: Vec<f64>,
    pub niter: Vec<i64>,
}
impl MAT_TABULATED_JOHNSON_COOK_GYS_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TABULATED_JOHNSON_COOK_GYS_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lck1: t.take("LCK1").and_then(|c| c.into_int()).unwrap_or_default(),
            lckt: t.take("LCKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            lccr: t.take("LCCR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcct: t.take("LCCT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcst: t.take("LCST").and_then(|c| c.into_int()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfiepm: t.take("SFIEPM").and_then(|c| c.into_float()).unwrap_or_default(),
            niter: t.take("NITER").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TABULATED_JOHNSON_COOK_LOG_INTERPOLATION {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub tabk1: Vec<i64>,
    pub tabkt: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub failopt: Vec<i64>,
    pub numavg: Vec<i64>,
    pub ncyfail: Vec<i64>,
    pub erode: Vec<i64>,
    pub lcps: Vec<i64>,
}
impl MAT_TABULATED_JOHNSON_COOK_LOG_INTERPOLATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TABULATED_JOHNSON_COOK_LOG_INTERPOLATION").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            tabk1: t.take("TABK1").and_then(|c| c.into_int()).unwrap_or_default(),
            tabkt: t.take("TABKT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            failopt: t.take("FAILOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            numavg: t.take("NUMAVG").and_then(|c| c.into_int()).unwrap_or_default(),
            ncyfail: t.take("NCYFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            erode: t.take("ERODE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcps: t.take("LCPS").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TABULATED_JOHNSON_COOK_ORTHO_PLASTICITY {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub tr: Vec<f64>,
    pub beta: Vec<f64>,
    pub numint: Vec<f64>,
    pub lct00r: Vec<i64>,
    pub lct00t: Vec<i64>,
    pub lcf: Vec<i64>,
    pub lcg: Vec<i64>,
    pub lch: Vec<i64>,
    pub lci: Vec<i64>,
    pub lcc00r: Vec<f64>,
    pub lcc00t: Vec<f64>,
    pub lcs45r: Vec<f64>,
    pub lcs45t: Vec<f64>,
    pub iflag: Vec<i64>,
    pub sfiepm: Vec<i64>,
    pub niter: Vec<i64>,
    pub aopt: Vec<f64>,
    pub lct90r: Vec<i64>,
    pub lct45r: Vec<i64>,
    pub lctthr: Vec<i64>,
    pub lcc90r: Vec<i64>,
    pub lcc45r: Vec<i64>,
    pub lccth: Vec<i64>,
    pub lct90t: Vec<i64>,
    pub lct45t: Vec<i64>,
    pub lcttht: Vec<i64>,
    pub lcc90t: Vec<i64>,
    pub lcc45t: Vec<i64>,
    pub lcctht: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub macf: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta_2: Vec<f64>,
}
impl MAT_TABULATED_JOHNSON_COOK_ORTHO_PLASTICITY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TABULATED_JOHNSON_COOK_ORTHO_PLASTICITY").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            tr: t.take("TR").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            numint: t.take("NUMINT").and_then(|c| c.into_float()).unwrap_or_default(),
            lct00r: t.take("LCT00R").and_then(|c| c.into_int()).unwrap_or_default(),
            lct00t: t.take("LCT00T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcf: t.take("LCF").and_then(|c| c.into_int()).unwrap_or_default(),
            lcg: t.take("LCG").and_then(|c| c.into_int()).unwrap_or_default(),
            lch: t.take("LCH").and_then(|c| c.into_int()).unwrap_or_default(),
            lci: t.take("LCI").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc00r: t.take("LCC00R").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc00t: t.take("LCC00T").and_then(|c| c.into_float()).unwrap_or_default(),
            lcs45r: t.take("LCS45R").and_then(|c| c.into_float()).unwrap_or_default(),
            lcs45t: t.take("LCS45T").and_then(|c| c.into_float()).unwrap_or_default(),
            iflag: t.take("IFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            sfiepm: t.take("SFIEPM").and_then(|c| c.into_int()).unwrap_or_default(),
            niter: t.take("NITER").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            lct90r: t.take("LCT90R").and_then(|c| c.into_int()).unwrap_or_default(),
            lct45r: t.take("LCT45R").and_then(|c| c.into_int()).unwrap_or_default(),
            lctthr: t.take("LCTTHR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc90r: t.take("LCC90R").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc45r: t.take("LCC45R").and_then(|c| c.into_int()).unwrap_or_default(),
            lccth: t.take("LCCTH").and_then(|c| c.into_int()).unwrap_or_default(),
            lct90t: t.take("LCT90T").and_then(|c| c.into_int()).unwrap_or_default(),
            lct45t: t.take("LCT45T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcttht: t.take("LCTTHT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc90t: t.take("LCC90T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcc45t: t.take("LCC45T").and_then(|c| c.into_int()).unwrap_or_default(),
            lcctht: t.take("LCCTHT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta_2: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TAILORED_PROPERTIES {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub fail: Vec<f64>,
    pub tdel: Vec<f64>,
    pub f_3: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub lcss: Vec<i64>,
    pub f_3_2: Vec<i64>,
    pub vp: Vec<f64>,
    pub hisvn: Vec<i64>,
    pub phase: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub es1: Vec<f64>,
    pub es2: Vec<f64>,
    pub es3: Vec<f64>,
    pub es4: Vec<f64>,
    pub es5: Vec<f64>,
    pub es6: Vec<f64>,
    pub es7: Vec<f64>,
    pub es8: Vec<f64>,
}
impl MAT_TAILORED_PROPERTIES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TAILORED_PROPERTIES").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdel: t.take("TDEL").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3_2: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            vp: t.take("VP").and_then(|c| c.into_float()).unwrap_or_default(),
            hisvn: t.take("HISVN").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            es1: t.take("ES1").and_then(|c| c.into_float()).unwrap_or_default(),
            es2: t.take("ES2").and_then(|c| c.into_float()).unwrap_or_default(),
            es3: t.take("ES3").and_then(|c| c.into_float()).unwrap_or_default(),
            es4: t.take("ES4").and_then(|c| c.into_float()).unwrap_or_default(),
            es5: t.take("ES5").and_then(|c| c.into_float()).unwrap_or_default(),
            es6: t.take("ES6").and_then(|c| c.into_float()).unwrap_or_default(),
            es7: t.take("ES7").and_then(|c| c.into_float()).unwrap_or_default(),
            es8: t.take("ES8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TEMPERATURE_DEPENDENT_ORTHOTROPIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub aopt: Vec<f64>,
    pub ref_: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub eai: Vec<f64>,
    pub ebi: Vec<f64>,
    pub eci: Vec<f64>,
    pub prabi: Vec<f64>,
    pub prcai: Vec<f64>,
    pub prcbi: Vec<f64>,
    pub aai: Vec<f64>,
    pub abi: Vec<f64>,
    pub aci: Vec<f64>,
    pub gabi: Vec<f64>,
    pub gbci: Vec<f64>,
    pub gcai: Vec<f64>,
    pub ti: Vec<f64>,
}
impl MAT_TEMPERATURE_DEPENDENT_ORTHOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TEMPERATURE_DEPENDENT_ORTHOTROPIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            ref_: t.take("REF").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            eai: t.take("EAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ebi: t.take("EBI").and_then(|c| c.into_float()).unwrap_or_default(),
            eci: t.take("ECI").and_then(|c| c.into_float()).unwrap_or_default(),
            prabi: t.take("PRABI").and_then(|c| c.into_float()).unwrap_or_default(),
            prcai: t.take("PRCAI").and_then(|c| c.into_float()).unwrap_or_default(),
            prcbi: t.take("PRCBI").and_then(|c| c.into_float()).unwrap_or_default(),
            aai: t.take("AAi").and_then(|c| c.into_float()).unwrap_or_default(),
            abi: t.take("ABi").and_then(|c| c.into_float()).unwrap_or_default(),
            aci: t.take("ACi").and_then(|c| c.into_float()).unwrap_or_default(),
            gabi: t.take("GABi").and_then(|c| c.into_float()).unwrap_or_default(),
            gbci: t.take("GBCi").and_then(|c| c.into_float()).unwrap_or_default(),
            gcai: t.take("GCAi").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_CHEMICAL_REACTION {
    pub tmid: Vec<i64>,
    pub nchsp: Vec<i64>,
    pub nchrx: Vec<i64>,
    pub icend: Vec<i64>,
    pub cend: Vec<f64>,
    pub gasc: Vec<f64>,
    pub fid: Vec<i64>,
    pub mf: Vec<i64>,
    pub rhof: Vec<f64>,
    pub lccf: Vec<i64>,
    pub lckf: Vec<i64>,
    pub vff: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub lcci: Vec<i64>,
    pub lcki: Vec<i64>,
    pub vfi: Vec<f64>,
    pub mwi: Vec<f64>,
    pub rci1: Vec<f64>,
    pub rci2: Vec<f64>,
    pub rci3: Vec<f64>,
    pub rci4: Vec<f64>,
    pub rci5: Vec<f64>,
    pub rci6: Vec<f64>,
    pub rci7: Vec<f64>,
    pub rci8: Vec<f64>,
    pub rxi1: Vec<f64>,
    pub rxi2: Vec<f64>,
    pub rxi3: Vec<f64>,
    pub rxi4: Vec<f64>,
    pub rxi5: Vec<f64>,
    pub rxi6: Vec<f64>,
    pub rxi7: Vec<f64>,
    pub rxi8: Vec<f64>,
    pub lczi1: Vec<f64>,
    pub lczi2: Vec<f64>,
    pub lczi3: Vec<f64>,
    pub lczi4: Vec<f64>,
    pub lczi5: Vec<f64>,
    pub lczi6: Vec<f64>,
    pub lczi7: Vec<f64>,
    pub lczi8: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub e4: Vec<f64>,
    pub e5: Vec<f64>,
    pub e6: Vec<f64>,
    pub e7: Vec<f64>,
    pub e8: Vec<f64>,
    pub q1: Vec<f64>,
    pub q2: Vec<f64>,
    pub q3: Vec<f64>,
    pub q4: Vec<f64>,
    pub q5: Vec<f64>,
    pub q6: Vec<f64>,
    pub q7: Vec<f64>,
    pub q8: Vec<f64>,
}
impl MAT_THERMAL_CHEMICAL_REACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_CHEMICAL_REACTION").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchsp: t.take("NCHSP").and_then(|c| c.into_int()).unwrap_or_default(),
            nchrx: t.take("NCHRX").and_then(|c| c.into_int()).unwrap_or_default(),
            icend: t.take("ICEND").and_then(|c| c.into_int()).unwrap_or_default(),
            cend: t.take("CEND").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            mf: t.take("MF").and_then(|c| c.into_int()).unwrap_or_default(),
            rhof: t.take("RHOf").and_then(|c| c.into_float()).unwrap_or_default(),
            lccf: t.take("LCCf").and_then(|c| c.into_int()).unwrap_or_default(),
            lckf: t.take("LCKf").and_then(|c| c.into_int()).unwrap_or_default(),
            vff: t.take("VFf").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOi").and_then(|c| c.into_float()).unwrap_or_default(),
            lcci: t.take("LCCi").and_then(|c| c.into_int()).unwrap_or_default(),
            lcki: t.take("LCKi").and_then(|c| c.into_int()).unwrap_or_default(),
            vfi: t.take("VFi").and_then(|c| c.into_float()).unwrap_or_default(),
            mwi: t.take("MWi").and_then(|c| c.into_float()).unwrap_or_default(),
            rci1: t.take("RCi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rci2: t.take("RCi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rci3: t.take("RCi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rci4: t.take("RCi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rci5: t.take("RCi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rci6: t.take("RCi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rci7: t.take("RCi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rci8: t.take("RCi8").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi1: t.take("RXi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi2: t.take("RXi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi3: t.take("RXi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi4: t.take("RXi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi5: t.take("RXi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi6: t.take("RXi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi7: t.take("RXi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi8: t.take("RXi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_float()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_float()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_float()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_float()).unwrap_or_default(),
            e8: t.take("E8").and_then(|c| c.into_float()).unwrap_or_default(),
            q1: t.take("Q1").and_then(|c| c.into_float()).unwrap_or_default(),
            q2: t.take("Q2").and_then(|c| c.into_float()).unwrap_or_default(),
            q3: t.take("Q3").and_then(|c| c.into_float()).unwrap_or_default(),
            q4: t.take("Q4").and_then(|c| c.into_float()).unwrap_or_default(),
            q5: t.take("Q5").and_then(|c| c.into_float()).unwrap_or_default(),
            q6: t.take("Q6").and_then(|c| c.into_float()).unwrap_or_default(),
            q7: t.take("Q7").and_then(|c| c.into_float()).unwrap_or_default(),
            q8: t.take("Q8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_CHEMICAL_REACTION_ORTHOTROPIC {
    pub tmid: Vec<i64>,
    pub nchsp: Vec<i64>,
    pub nchrx: Vec<i64>,
    pub icend: Vec<i64>,
    pub cend: Vec<f64>,
    pub gasc: Vec<f64>,
    pub fid: Vec<i64>,
    pub mf: Vec<i64>,
    pub aopt: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub rhof: Vec<f64>,
    pub lccf: Vec<i64>,
    pub lck1f: Vec<i64>,
    pub lck2f: Vec<f64>,
    pub lck3f: Vec<f64>,
    pub vff: Vec<f64>,
    pub rhoi: Vec<f64>,
    pub lcci: Vec<i64>,
    pub lck1i: Vec<i64>,
    pub lck2i: Vec<i64>,
    pub lck3i: Vec<i64>,
    pub vfi: Vec<f64>,
    pub mwi: Vec<f64>,
    pub rci1: Vec<f64>,
    pub rci2: Vec<f64>,
    pub rci3: Vec<f64>,
    pub rci4: Vec<f64>,
    pub rci5: Vec<f64>,
    pub rci6: Vec<f64>,
    pub rci7: Vec<f64>,
    pub rci8: Vec<f64>,
    pub rxi1: Vec<f64>,
    pub rxi2: Vec<f64>,
    pub rxi3: Vec<f64>,
    pub rxi4: Vec<f64>,
    pub rxi5: Vec<f64>,
    pub rxi6: Vec<f64>,
    pub rxi7: Vec<f64>,
    pub rxi8: Vec<f64>,
    pub lczi1: Vec<f64>,
    pub lczi2: Vec<f64>,
    pub lczi3: Vec<f64>,
    pub lczi4: Vec<f64>,
    pub lczi5: Vec<f64>,
    pub lczi6: Vec<f64>,
    pub lczi7: Vec<f64>,
    pub lczi8: Vec<f64>,
    pub lczi1_2: Vec<f64>,
    pub lczi2_2: Vec<f64>,
    pub lczi3_2: Vec<f64>,
    pub lczi4_2: Vec<f64>,
    pub lczi5_2: Vec<f64>,
    pub lczi6_2: Vec<f64>,
    pub lczi7_2: Vec<f64>,
    pub lczi8_2: Vec<f64>,
    pub lczi1_3: Vec<f64>,
    pub lczi2_3: Vec<f64>,
    pub lczi3_3: Vec<f64>,
    pub lczi4_3: Vec<f64>,
    pub lczi5_3: Vec<f64>,
    pub lczi6_3: Vec<f64>,
    pub lczi7_3: Vec<f64>,
    pub lczi8_3: Vec<f64>,
}
impl MAT_THERMAL_CHEMICAL_REACTION_ORTHOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_CHEMICAL_REACTION_ORTHOTROPIC").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            nchsp: t.take("NCHSP").and_then(|c| c.into_int()).unwrap_or_default(),
            nchrx: t.take("NCHRX").and_then(|c| c.into_int()).unwrap_or_default(),
            icend: t.take("ICEND").and_then(|c| c.into_int()).unwrap_or_default(),
            cend: t.take("CEND").and_then(|c| c.into_float()).unwrap_or_default(),
            gasc: t.take("GASC").and_then(|c| c.into_float()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            mf: t.take("MF").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            rhof: t.take("RHOf").and_then(|c| c.into_float()).unwrap_or_default(),
            lccf: t.take("LCCf").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1f: t.take("LCK1f").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2f: t.take("LCK2F").and_then(|c| c.into_float()).unwrap_or_default(),
            lck3f: t.take("LCK3F").and_then(|c| c.into_float()).unwrap_or_default(),
            vff: t.take("VFf").and_then(|c| c.into_float()).unwrap_or_default(),
            rhoi: t.take("RHOi").and_then(|c| c.into_float()).unwrap_or_default(),
            lcci: t.take("LCCi").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1i: t.take("LCK1i").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2i: t.take("LCK2i").and_then(|c| c.into_int()).unwrap_or_default(),
            lck3i: t.take("LCK3i").and_then(|c| c.into_int()).unwrap_or_default(),
            vfi: t.take("VFi").and_then(|c| c.into_float()).unwrap_or_default(),
            mwi: t.take("MWi").and_then(|c| c.into_float()).unwrap_or_default(),
            rci1: t.take("RCi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rci2: t.take("RCi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rci3: t.take("RCi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rci4: t.take("RCi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rci5: t.take("RCi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rci6: t.take("RCi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rci7: t.take("RCi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rci8: t.take("RCi8").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi1: t.take("RXi1").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi2: t.take("RXi2").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi3: t.take("RXi3").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi4: t.take("RXi4").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi5: t.take("RXi5").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi6: t.take("RXi6").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi7: t.take("RXi7").and_then(|c| c.into_float()).unwrap_or_default(),
            rxi8: t.take("RXi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1_2: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2_2: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3_2: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4_2: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5_2: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6_2: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7_2: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8_2: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi1_3: t.take("LCZi1").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi2_3: t.take("LCZi2").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi3_3: t.take("LCZi3").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi4_3: t.take("LCZi4").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi5_3: t.take("LCZi5").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi6_3: t.take("LCZi6").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi7_3: t.take("LCZi7").and_then(|c| c.into_float()).unwrap_or_default(),
            lczi8_3: t.take("LCZi8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_CWM {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub hdead: Vec<f64>,
    pub tdead: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub lchc: Vec<i64>,
    pub lctc: Vec<i64>,
    pub tlstart: Vec<f64>,
    pub tlend: Vec<f64>,
    pub tistart: Vec<f64>,
    pub tiend: Vec<f64>,
    pub hghost: Vec<f64>,
    pub tghost: Vec<f64>,
}
impl MAT_THERMAL_CWM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_CWM").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            hdead: t.take("HDEAD").and_then(|c| c.into_float()).unwrap_or_default(),
            tdead: t.take("TDEAD").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lchc: t.take("LCHC").and_then(|c| c.into_int()).unwrap_or_default(),
            lctc: t.take("LCTC").and_then(|c| c.into_int()).unwrap_or_default(),
            tlstart: t.take("TLSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tlend: t.take("TLEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tistart: t.take("TISTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tiend: t.take("TIEND").and_then(|c| c.into_float()).unwrap_or_default(),
            hghost: t.take("HGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            tghost: t.take("TGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_DISCRETE_BEAM {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub hc: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_THERMAL_DISCRETE_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_DISCRETE_BEAM").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ISOTROPIC {
    pub tmid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hc: Vec<f64>,
    pub tc: Vec<f64>,
}
impl MAT_THERMAL_ISOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ISOTROPIC").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ISOTROPIC_PHASE_CHANGE {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
    pub solt: Vec<f64>,
    pub liqt: Vec<f64>,
    pub lh: Vec<f64>,
}
impl MAT_THERMAL_ISOTROPIC_PHASE_CHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ISOTROPIC_PHASE_CHANGE").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
            solt: t.take("SOLT").and_then(|c| c.into_float()).unwrap_or_default(),
            liqt: t.take("LIQT").and_then(|c| c.into_float()).unwrap_or_default(),
            lh: t.take("LH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ISOTROPIC_TD {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<f64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub k4: Vec<f64>,
    pub k5: Vec<f64>,
    pub k6: Vec<f64>,
    pub k7: Vec<f64>,
    pub k8: Vec<f64>,
}
impl MAT_THERMAL_ISOTROPIC_TD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ISOTROPIC_TD").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_float()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_float()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_float()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_float()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_float()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ISOTROPIC_TD_LC {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hclc: Vec<i64>,
    pub tclc: Vec<i64>,
    pub hchsv: Vec<f64>,
    pub tchsv: Vec<f64>,
    pub tghsv: Vec<f64>,
}
impl MAT_THERMAL_ISOTROPIC_TD_LC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ISOTROPIC_TD_LC").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hclc: t.take("HCLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tclc: t.take("TCLC").and_then(|c| c.into_int()).unwrap_or_default(),
            hchsv: t.take("HCHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            tchsv: t.take("TCHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            tghsv: t.take("TGHSV").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ORTHOTROPIC {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub hc: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
    pub k3: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_THERMAL_ORTHOTROPIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ORTHOTROPIC").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hc: t.take("HC").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ORTHOTROPIC_TD {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub t5: Vec<f64>,
    pub t6: Vec<f64>,
    pub t7: Vec<f64>,
    pub t8: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub k1_1: Vec<f64>,
    pub k1_2: Vec<f64>,
    pub k1_3: Vec<f64>,
    pub k1_4: Vec<f64>,
    pub k1_5: Vec<f64>,
    pub k1_6: Vec<f64>,
    pub k1_7: Vec<f64>,
    pub k1_8: Vec<f64>,
    pub k2_1: Vec<f64>,
    pub k2_2: Vec<f64>,
    pub k2_3: Vec<f64>,
    pub k2_4: Vec<f64>,
    pub k2_5: Vec<f64>,
    pub k2_6: Vec<f64>,
    pub k2_7: Vec<f64>,
    pub k2_8: Vec<f64>,
    pub k3_1: Vec<f64>,
    pub k_2: Vec<f64>,
    pub k3_3: Vec<f64>,
    pub k3_4: Vec<f64>,
    pub k3_5: Vec<f64>,
    pub k3_6: Vec<f64>,
    pub k3_7: Vec<f64>,
    pub k3_8: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_THERMAL_ORTHOTROPIC_TD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ORTHOTROPIC_TD").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            t5: t.take("T5").and_then(|c| c.into_float()).unwrap_or_default(),
            t6: t.take("T6").and_then(|c| c.into_float()).unwrap_or_default(),
            t7: t.take("T7").and_then(|c| c.into_float()).unwrap_or_default(),
            t8: t.take("T8").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_1: t.take("(K1-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_2: t.take("(K1-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_3: t.take("(K1-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_4: t.take("(K1-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_5: t.take("(K1-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_6: t.take("(K1-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_7: t.take("(K1-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k1_8: t.take("(K1-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_1: t.take("(K2-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_2: t.take("(K2-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_3: t.take("(K2-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_4: t.take("(K2-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_5: t.take("(K2-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_6: t.take("(K2-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_7: t.take("(K2-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k2_8: t.take("(K2-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_1: t.take("(K3-1)").and_then(|c| c.into_float()).unwrap_or_default(),
            k_2: t.take("(K-2)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_3: t.take("(K3-3)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_4: t.take("(K3-4)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_5: t.take("(K3-5)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_6: t.take("(K3-6)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_7: t.take("(K3-7)").and_then(|c| c.into_float()).unwrap_or_default(),
            k3_8: t.take("(K3-8)").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_ORTHOTROPIC_TD_LC {
    pub tmid: Vec<i64>,
    pub tro: Vec<f64>,
    pub tgrlc: Vec<i64>,
    pub tgmult: Vec<f64>,
    pub aopt: Vec<f64>,
    pub tlat: Vec<f64>,
    pub hlat: Vec<f64>,
    pub lcc: Vec<i64>,
    pub lck1: Vec<i64>,
    pub lck2: Vec<i64>,
    pub lck3: Vec<i64>,
    pub ilcchsv: Vec<i64>,
    pub ilckhsv: Vec<f64>,
    pub itghsv: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_THERMAL_ORTHOTROPIC_TD_LC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_ORTHOTROPIC_TD_LC").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            tro: t.take("TRO").and_then(|c| c.into_float()).unwrap_or_default(),
            tgrlc: t.take("TGRLC").and_then(|c| c.into_int()).unwrap_or_default(),
            tgmult: t.take("TGMULT").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            tlat: t.take("TLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            hlat: t.take("HLAT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_int()).unwrap_or_default(),
            lck1: t.take("LCK1").and_then(|c| c.into_int()).unwrap_or_default(),
            lck2: t.take("LCK2").and_then(|c| c.into_int()).unwrap_or_default(),
            lck3: t.take("LCK3").and_then(|c| c.into_int()).unwrap_or_default(),
            ilcchsv: t.take("ILCCHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            ilckhsv: t.take("ILCKHSV").and_then(|c| c.into_float()).unwrap_or_default(),
            itghsv: t.take("ITGHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMAL_USER_DEFINED {
    pub tmid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mt: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub aopt: Vec<f64>,
    pub iortho: Vec<i64>,
    pub ihve: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_THERMAL_USER_DEFINED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMAL_USER_DEFINED").unwrap());
        Self {
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mt: t.take("MT").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            ihve: t.take("IHVE").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_THERMO_ELASTO_VISCOPLASTIC_CREEP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcss: Vec<i64>,
    pub reftem: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub c: Vec<f64>,
    pub p: Vec<f64>,
    pub lce: Vec<f64>,
    pub lcpr: Vec<f64>,
    pub lcsigy: Vec<f64>,
    pub lcqr: Vec<f64>,
    pub lcqx: Vec<f64>,
    pub lcalph: Vec<f64>,
    pub lcc: Vec<f64>,
    pub lcp: Vec<f64>,
    pub lccr: Vec<f64>,
    pub lccx: Vec<f64>,
    pub crpa: Vec<f64>,
    pub crpb: Vec<f64>,
    pub crpq: Vec<f64>,
    pub crpm: Vec<f64>,
    pub crplaw: Vec<f64>,
}
impl MAT_THERMO_ELASTO_VISCOPLASTIC_CREEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_THERMO_ELASTO_VISCOPLASTIC_CREEP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            reftem: t.take("REFTEM").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            lce: t.take("LCE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcpr: t.take("LCPR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcsigy: t.take("LCSIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            lcqr: t.take("LCQR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcqx: t.take("LCQX").and_then(|c| c.into_float()).unwrap_or_default(),
            lcalph: t.take("LCALPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcc: t.take("LCC").and_then(|c| c.into_float()).unwrap_or_default(),
            lcp: t.take("LCP").and_then(|c| c.into_float()).unwrap_or_default(),
            lccr: t.take("LCCR").and_then(|c| c.into_float()).unwrap_or_default(),
            lccx: t.take("LCCX").and_then(|c| c.into_float()).unwrap_or_default(),
            crpa: t.take("CRPA").and_then(|c| c.into_float()).unwrap_or_default(),
            crpb: t.take("CRPB").and_then(|c| c.into_float()).unwrap_or_default(),
            crpq: t.take("CRPQ").and_then(|c| c.into_float()).unwrap_or_default(),
            crpm: t.take("CRPM").and_then(|c| c.into_float()).unwrap_or_default(),
            crplaw: t.take("CRPLAW").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TISSUE_DISPERSED {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub f: Vec<f64>,
    pub sigma: Vec<f64>,
    pub mu: Vec<f64>,
    pub kappa: Vec<f64>,
    pub act: Vec<i64>,
    pub init: Vec<i64>,
    pub fid: Vec<i64>,
    pub orth: Vec<i64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub theta: Vec<f64>,
    pub act1: Vec<f64>,
    pub act2: Vec<f64>,
    pub act3: Vec<f64>,
    pub act4: Vec<f64>,
    pub act5: Vec<f64>,
    pub act6: Vec<f64>,
    pub act7: Vec<f64>,
    pub act8: Vec<f64>,
    pub act9: Vec<f64>,
    pub act10: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_TISSUE_DISPERSED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TISSUE_DISPERSED").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("F").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma: t.take("SIGMA").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            act: t.take("ACT").and_then(|c| c.into_int()).unwrap_or_default(),
            init: t.take("INIT").and_then(|c| c.into_int()).unwrap_or_default(),
            fid: t.take("FID").and_then(|c| c.into_int()).unwrap_or_default(),
            orth: t.take("ORTH").and_then(|c| c.into_int()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            theta: t.take("THETA").and_then(|c| c.into_float()).unwrap_or_default(),
            act1: t.take("ACT1").and_then(|c| c.into_float()).unwrap_or_default(),
            act2: t.take("ACT2").and_then(|c| c.into_float()).unwrap_or_default(),
            act3: t.take("ACT3").and_then(|c| c.into_float()).unwrap_or_default(),
            act4: t.take("ACT4").and_then(|c| c.into_float()).unwrap_or_default(),
            act5: t.take("ACT5").and_then(|c| c.into_float()).unwrap_or_default(),
            act6: t.take("ACT6").and_then(|c| c.into_float()).unwrap_or_default(),
            act7: t.take("ACT7").and_then(|c| c.into_float()).unwrap_or_default(),
            act8: t.take("ACT8").and_then(|c| c.into_float()).unwrap_or_default(),
            act9: t.take("ACT9").and_then(|c| c.into_float()).unwrap_or_default(),
            act10: t.take("ACT10").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TNM_POLYMER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub roflg: Vec<i64>,
    pub mua: Vec<f64>,
    pub thetah: Vec<f64>,
    pub lambl: Vec<f64>,
    pub kappa: Vec<f64>,
    pub tauha: Vec<f64>,
    pub a: Vec<f64>,
    pub ma: Vec<f64>,
    pub n: Vec<f64>,
    pub mubi: Vec<f64>,
    pub mubf: Vec<f64>,
    pub beta: Vec<f64>,
    pub tauhb: Vec<f64>,
    pub mb: Vec<f64>,
    pub muc: Vec<f64>,
    pub q: Vec<f64>,
    pub alpha: Vec<f64>,
    pub theata0: Vec<f64>,
    pub ibulk: Vec<f64>,
    pub ig: Vec<f64>,
    pub tsstif: Vec<f64>,
    pub gamma0: Vec<f64>,
}
impl MAT_TNM_POLYMER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TNM_POLYMER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            roflg: t.take("ROFLG").and_then(|c| c.into_int()).unwrap_or_default(),
            mua: t.take("MUA").and_then(|c| c.into_float()).unwrap_or_default(),
            thetah: t.take("THETAH").and_then(|c| c.into_float()).unwrap_or_default(),
            lambl: t.take("LAMBL").and_then(|c| c.into_float()).unwrap_or_default(),
            kappa: t.take("KAPPA").and_then(|c| c.into_float()).unwrap_or_default(),
            tauha: t.take("TAUHA").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            ma: t.take("MA").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            mubi: t.take("MUBI").and_then(|c| c.into_float()).unwrap_or_default(),
            mubf: t.take("MUBF").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            tauhb: t.take("TAUHB").and_then(|c| c.into_float()).unwrap_or_default(),
            mb: t.take("MB").and_then(|c| c.into_float()).unwrap_or_default(),
            muc: t.take("MUC").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            theata0: t.take("THEATA0").and_then(|c| c.into_float()).unwrap_or_default(),
            ibulk: t.take("IBULK").and_then(|c| c.into_float()).unwrap_or_default(),
            ig: t.take("IG").and_then(|c| c.into_float()).unwrap_or_default(),
            tsstif: t.take("TSSTIF").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma0: t.take("GAMMA0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TOUGHENED_ADHESIVE_POLYMER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub flg: Vec<i64>,
    pub jcfl: Vec<i64>,
    pub dopt: Vec<i64>,
    pub lcss: Vec<i64>,
    pub tau0: Vec<f64>,
    pub q: Vec<f64>,
    pub b: Vec<f64>,
    pub h: Vec<f64>,
    pub c: Vec<f64>,
    pub gam0: Vec<f64>,
    pub gamm: Vec<f64>,
    pub a10: Vec<f64>,
    pub a20: Vec<f64>,
    pub a1h: Vec<f64>,
    pub a2h: Vec<f64>,
    pub a2s: Vec<f64>,
    pub pow: Vec<f64>,
    pub f: Vec<i64>,
    pub srfilt: Vec<f64>,
    pub f_2: Vec<i64>,
    pub f_2_2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub d4: Vec<f64>,
    pub d1c: Vec<f64>,
    pub d2c: Vec<f64>,
}
impl MAT_TOUGHENED_ADHESIVE_POLYMER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TOUGHENED_ADHESIVE_POLYMER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            flg: t.take("FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            jcfl: t.take("JCFL").and_then(|c| c.into_int()).unwrap_or_default(),
            dopt: t.take("DOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            tau0: t.take("TAU0").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            gam0: t.take("GAM0").and_then(|c| c.into_float()).unwrap_or_default(),
            gamm: t.take("GAMM").and_then(|c| c.into_float()).unwrap_or_default(),
            a10: t.take("A10").and_then(|c| c.into_float()).unwrap_or_default(),
            a20: t.take("A20").and_then(|c| c.into_float()).unwrap_or_default(),
            a1h: t.take("A1H").and_then(|c| c.into_float()).unwrap_or_default(),
            a2h: t.take("A2H").and_then(|c| c.into_float()).unwrap_or_default(),
            a2s: t.take("A2S").and_then(|c| c.into_float()).unwrap_or_default(),
            pow: t.take("POW").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            srfilt: t.take("SRFILT").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            d4: t.take("D4").and_then(|c| c.into_float()).unwrap_or_default(),
            d1c: t.take("D1C").and_then(|c| c.into_float()).unwrap_or_default(),
            d2c: t.take("D2C").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ANISOTROPIC_CRUSHABLE_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11: Vec<f64>,
    pub e22: Vec<f64>,
    pub e12: Vec<f64>,
    pub e23: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub i11: Vec<i64>,
    pub i22: Vec<i64>,
    pub i12: Vec<i64>,
    pub i23: Vec<i64>,
    pub iaa: Vec<i64>,
    pub nsym: Vec<i64>,
    pub ang: Vec<f64>,
    pub mu: Vec<f64>,
    pub aopt: Vec<f64>,
    pub iscl: Vec<i64>,
    pub beta: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_TRANSVERSELY_ANISOTROPIC_CRUSHABLE_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ANISOTROPIC_CRUSHABLE_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11: t.take("E11").and_then(|c| c.into_float()).unwrap_or_default(),
            e22: t.take("E22").and_then(|c| c.into_float()).unwrap_or_default(),
            e12: t.take("E12").and_then(|c| c.into_float()).unwrap_or_default(),
            e23: t.take("E23").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            i11: t.take("I11").and_then(|c| c.into_int()).unwrap_or_default(),
            i22: t.take("I22").and_then(|c| c.into_int()).unwrap_or_default(),
            i12: t.take("I12").and_then(|c| c.into_int()).unwrap_or_default(),
            i23: t.take("I23").and_then(|c| c.into_int()).unwrap_or_default(),
            iaa: t.take("IAA").and_then(|c| c.into_int()).unwrap_or_default(),
            nsym: t.take("NSYM").and_then(|c| c.into_int()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iscl: t.take("ISCL").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
}
impl MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_ECHANGE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub idscale: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
}
impl MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_ECHANGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_ECHANGE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            idscale: t.take("IDSCALE").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP2 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
}
impl MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP2").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP_FAILURE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub sigy: Vec<f64>,
    pub etan: Vec<f64>,
    pub r: Vec<f64>,
    pub hlcid: Vec<i64>,
    pub idscale: Vec<i64>,
    pub ea: Vec<f64>,
    pub coe: Vec<f64>,
    pub icfld: Vec<i64>,
    pub unused: Vec<i64>,
    pub strainlt: Vec<f64>,
}
impl MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ANISOTROPIC_ELASTIC_PLASTIC_NLP_FAILURE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            sigy: t.take("SIGY").and_then(|c| c.into_float()).unwrap_or_default(),
            etan: t.take("ETAN").and_then(|c| c.into_float()).unwrap_or_default(),
            r: t.take("R").and_then(|c| c.into_float()).unwrap_or_default(),
            hlcid: t.take("HLCID").and_then(|c| c.into_int()).unwrap_or_default(),
            idscale: t.take("IDSCALE").and_then(|c| c.into_int()).unwrap_or_default(),
            ea: t.take("EA").and_then(|c| c.into_float()).unwrap_or_default(),
            coe: t.take("COE").and_then(|c| c.into_float()).unwrap_or_default(),
            icfld: t.take("ICFLD").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            strainlt: t.take("STRAINLT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRANSVERSELY_ISOTROPIC_CRUSHABLE_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e11: Vec<f64>,
    pub e22: Vec<f64>,
    pub e12: Vec<f64>,
    pub e23: Vec<f64>,
    pub g: Vec<f64>,
    pub k: Vec<f64>,
    pub i11: Vec<i64>,
    pub i22: Vec<i64>,
    pub i12: Vec<i64>,
    pub i23: Vec<i64>,
    pub iaa: Vec<i64>,
    pub nsym: Vec<i64>,
    pub ang: Vec<f64>,
    pub mu: Vec<f64>,
    pub aopt: Vec<f64>,
    pub iscl: Vec<i64>,
    pub beta: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_TRANSVERSELY_ISOTROPIC_CRUSHABLE_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRANSVERSELY_ISOTROPIC_CRUSHABLE_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e11: t.take("E11").and_then(|c| c.into_float()).unwrap_or_default(),
            e22: t.take("E22").and_then(|c| c.into_float()).unwrap_or_default(),
            e12: t.take("E12").and_then(|c| c.into_float()).unwrap_or_default(),
            e23: t.take("E23").and_then(|c| c.into_float()).unwrap_or_default(),
            g: t.take("G").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            i11: t.take("I11").and_then(|c| c.into_int()).unwrap_or_default(),
            i22: t.take("I22").and_then(|c| c.into_int()).unwrap_or_default(),
            i12: t.take("I12").and_then(|c| c.into_int()).unwrap_or_default(),
            i23: t.take("I23").and_then(|c| c.into_int()).unwrap_or_default(),
            iaa: t.take("IAA").and_then(|c| c.into_int()).unwrap_or_default(),
            nsym: t.take("NSYM").and_then(|c| c.into_int()).unwrap_or_default(),
            ang: t.take("ANG").and_then(|c| c.into_float()).unwrap_or_default(),
            mu: t.take("MU").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            iscl: t.take("ISCL").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_TRIP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub cp: Vec<f64>,
    pub t0: Vec<f64>,
    pub tref: Vec<f64>,
    pub ta0: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub d: Vec<f64>,
    pub p: Vec<f64>,
    pub q: Vec<f64>,
    pub e0mart: Vec<f64>,
    pub vm0: Vec<f64>,
    pub ahs: Vec<f64>,
    pub bhs: Vec<f64>,
    pub m: Vec<f64>,
    pub n: Vec<f64>,
    pub eps0: Vec<f64>,
    pub hmart: Vec<f64>,
    pub k1: Vec<f64>,
    pub k2: Vec<f64>,
}
impl MAT_TRIP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_TRIP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            cp: t.take("CP").and_then(|c| c.into_float()).unwrap_or_default(),
            t0: t.take("T0").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            ta0: t.take("TA0").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            d: t.take("D").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            q: t.take("Q").and_then(|c| c.into_float()).unwrap_or_default(),
            e0mart: t.take("E0MART").and_then(|c| c.into_float()).unwrap_or_default(),
            vm0: t.take("VM0").and_then(|c| c.into_float()).unwrap_or_default(),
            ahs: t.take("AHS").and_then(|c| c.into_float()).unwrap_or_default(),
            bhs: t.take("BHS").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            hmart: t.take("HMART").and_then(|c| c.into_float()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_float()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_UHS_STEEL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub tunit: Vec<f64>,
    pub crsh: Vec<i64>,
    pub phase: Vec<i64>,
    pub heat: Vec<i64>,
    pub lcy1: Vec<i64>,
    pub lcy2: Vec<i64>,
    pub lcy3: Vec<i64>,
    pub lcy4: Vec<i64>,
    pub lcy5: Vec<i64>,
    pub kfer: Vec<f64>,
    pub kper: Vec<f64>,
    pub b: Vec<f64>,
    pub c: Vec<f64>,
    pub co: Vec<f64>,
    pub mo: Vec<f64>,
    pub cr: Vec<f64>,
    pub ni: Vec<f64>,
    pub mn: Vec<f64>,
    pub si: Vec<f64>,
    pub v: Vec<f64>,
    pub w: Vec<f64>,
    pub cu: Vec<f64>,
    pub p: Vec<f64>,
    pub al: Vec<f64>,
    pub as_: Vec<f64>,
    pub ti: Vec<f64>,
    pub cwm: Vec<i64>,
    pub lctre: Vec<i64>,
    pub thexp1: Vec<f64>,
    pub thexp5: Vec<f64>,
    pub lcth1: Vec<i64>,
    pub lcth5: Vec<i64>,
    pub tref: Vec<f64>,
    pub lat1: Vec<f64>,
    pub lat5: Vec<f64>,
    pub tabth: Vec<i64>,
    pub qr2: Vec<f64>,
    pub qr3: Vec<f64>,
    pub qr4: Vec<f64>,
    pub alpha: Vec<f64>,
    pub grain: Vec<f64>,
    pub toffe: Vec<f64>,
    pub tofpe: Vec<f64>,
    pub tofba: Vec<f64>,
    pub plmem2: Vec<f64>,
    pub plmem3: Vec<f64>,
    pub plmem4: Vec<f64>,
    pub plmem5: Vec<f64>,
    pub strc: Vec<f64>,
    pub strp: Vec<f64>,
    pub react: Vec<i64>,
    pub temper: Vec<i64>,
    pub aust: Vec<f64>,
    pub ferr: Vec<f64>,
    pub pear: Vec<f64>,
    pub bain: Vec<f64>,
    pub mart: Vec<f64>,
    pub grk: Vec<f64>,
    pub grqr: Vec<f64>,
    pub tau1: Vec<f64>,
    pub gra: Vec<f64>,
    pub grb: Vec<f64>,
    pub expa: Vec<f64>,
    pub expb: Vec<f64>,
    pub grcc: Vec<f64>,
    pub grcm: Vec<f64>,
    pub heatn: Vec<f64>,
    pub tau2: Vec<f64>,
    pub fs: Vec<f64>,
    pub ps: Vec<f64>,
    pub bs: Vec<f64>,
    pub ms: Vec<f64>,
    pub msig: Vec<f64>,
    pub lceps23: Vec<i64>,
    pub lceps4: Vec<i64>,
    pub lceps5: Vec<i64>,
    pub lch4: Vec<i64>,
    pub lch5: Vec<i64>,
    pub dtcrit: Vec<f64>,
    pub tsamp: Vec<f64>,
    pub tastart: Vec<f64>,
    pub taend: Vec<f64>,
    pub tlstart: Vec<f64>,
    pub tlend: Vec<f64>,
    pub eghost: Vec<f64>,
    pub pghost: Vec<f64>,
    pub aghost: Vec<f64>,
}
impl MAT_UHS_STEEL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_UHS_STEEL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            tunit: t.take("TUNIT").and_then(|c| c.into_float()).unwrap_or_default(),
            crsh: t.take("CRSH").and_then(|c| c.into_int()).unwrap_or_default(),
            phase: t.take("PHASE").and_then(|c| c.into_int()).unwrap_or_default(),
            heat: t.take("HEAT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy1: t.take("LCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy2: t.take("LCY2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy3: t.take("LCY3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy4: t.take("LCY4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcy5: t.take("LCY5").and_then(|c| c.into_int()).unwrap_or_default(),
            kfer: t.take("KFER").and_then(|c| c.into_float()).unwrap_or_default(),
            kper: t.take("KPER").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            co: t.take("Co").and_then(|c| c.into_float()).unwrap_or_default(),
            mo: t.take("Mo").and_then(|c| c.into_float()).unwrap_or_default(),
            cr: t.take("Cr").and_then(|c| c.into_float()).unwrap_or_default(),
            ni: t.take("Ni").and_then(|c| c.into_float()).unwrap_or_default(),
            mn: t.take("Mn").and_then(|c| c.into_float()).unwrap_or_default(),
            si: t.take("Si").and_then(|c| c.into_float()).unwrap_or_default(),
            v: t.take("V").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            cu: t.take("Cu").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            al: t.take("Al").and_then(|c| c.into_float()).unwrap_or_default(),
            as_: t.take("As").and_then(|c| c.into_float()).unwrap_or_default(),
            ti: t.take("Ti").and_then(|c| c.into_float()).unwrap_or_default(),
            cwm: t.take("CWM").and_then(|c| c.into_int()).unwrap_or_default(),
            lctre: t.take("LCTRE").and_then(|c| c.into_int()).unwrap_or_default(),
            thexp1: t.take("THEXP1").and_then(|c| c.into_float()).unwrap_or_default(),
            thexp5: t.take("THEXP5").and_then(|c| c.into_float()).unwrap_or_default(),
            lcth1: t.take("LCTH1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcth5: t.take("LCTH5").and_then(|c| c.into_int()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            lat1: t.take("LAT1").and_then(|c| c.into_float()).unwrap_or_default(),
            lat5: t.take("LAT5").and_then(|c| c.into_float()).unwrap_or_default(),
            tabth: t.take("TABTH").and_then(|c| c.into_int()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            qr3: t.take("QR3").and_then(|c| c.into_float()).unwrap_or_default(),
            qr4: t.take("QR4").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            grain: t.take("GRAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            toffe: t.take("TOFFE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofpe: t.take("TOFPE").and_then(|c| c.into_float()).unwrap_or_default(),
            tofba: t.take("TOFBA").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem2: t.take("PLMEM2").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem3: t.take("PLMEM3").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem4: t.take("PLMEM4").and_then(|c| c.into_float()).unwrap_or_default(),
            plmem5: t.take("PLMEM5").and_then(|c| c.into_float()).unwrap_or_default(),
            strc: t.take("STRC").and_then(|c| c.into_float()).unwrap_or_default(),
            strp: t.take("STRP").and_then(|c| c.into_float()).unwrap_or_default(),
            react: t.take("REACT").and_then(|c| c.into_int()).unwrap_or_default(),
            temper: t.take("TEMPER").and_then(|c| c.into_int()).unwrap_or_default(),
            aust: t.take("AUST").and_then(|c| c.into_float()).unwrap_or_default(),
            ferr: t.take("FERR").and_then(|c| c.into_float()).unwrap_or_default(),
            pear: t.take("PEAR").and_then(|c| c.into_float()).unwrap_or_default(),
            bain: t.take("BAIN").and_then(|c| c.into_float()).unwrap_or_default(),
            mart: t.take("MART").and_then(|c| c.into_float()).unwrap_or_default(),
            grk: t.take("GRK").and_then(|c| c.into_float()).unwrap_or_default(),
            grqr: t.take("GRQR").and_then(|c| c.into_float()).unwrap_or_default(),
            tau1: t.take("TAU1").and_then(|c| c.into_float()).unwrap_or_default(),
            gra: t.take("GRA").and_then(|c| c.into_float()).unwrap_or_default(),
            grb: t.take("GRB").and_then(|c| c.into_float()).unwrap_or_default(),
            expa: t.take("EXPA").and_then(|c| c.into_float()).unwrap_or_default(),
            expb: t.take("EXPB").and_then(|c| c.into_float()).unwrap_or_default(),
            grcc: t.take("GRCC").and_then(|c| c.into_float()).unwrap_or_default(),
            grcm: t.take("GRCM").and_then(|c| c.into_float()).unwrap_or_default(),
            heatn: t.take("HEATN").and_then(|c| c.into_float()).unwrap_or_default(),
            tau2: t.take("TAU2").and_then(|c| c.into_float()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            ps: t.take("PS").and_then(|c| c.into_float()).unwrap_or_default(),
            bs: t.take("BS").and_then(|c| c.into_float()).unwrap_or_default(),
            ms: t.take("MS").and_then(|c| c.into_float()).unwrap_or_default(),
            msig: t.take("MSIG").and_then(|c| c.into_float()).unwrap_or_default(),
            lceps23: t.take("LCEPS23").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps4: t.take("LCEPS4").and_then(|c| c.into_int()).unwrap_or_default(),
            lceps5: t.take("LCEPS5").and_then(|c| c.into_int()).unwrap_or_default(),
            lch4: t.take("LCH4").and_then(|c| c.into_int()).unwrap_or_default(),
            lch5: t.take("LCH5").and_then(|c| c.into_int()).unwrap_or_default(),
            dtcrit: t.take("DTCRIT").and_then(|c| c.into_float()).unwrap_or_default(),
            tsamp: t.take("TSAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            tastart: t.take("TASTART").and_then(|c| c.into_float()).unwrap_or_default(),
            taend: t.take("TAEND").and_then(|c| c.into_float()).unwrap_or_default(),
            tlstart: t.take("TLSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tlend: t.take("TLEND").and_then(|c| c.into_float()).unwrap_or_default(),
            eghost: t.take("EGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            pghost: t.take("PGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
            aghost: t.take("AGHOST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_UNIFIED_CREEP {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub a: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
}
impl MAT_UNIFIED_CREEP {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_UNIFIED_CREEP").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_UNIFIED_CREEP_ORTHO {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub e3: Vec<f64>,
    pub pr21: Vec<f64>,
    pub pr31: Vec<f64>,
    pub pr32: Vec<f64>,
    pub g12: Vec<f64>,
    pub g23: Vec<f64>,
    pub g13: Vec<f64>,
    pub a: Vec<f64>,
    pub n: Vec<f64>,
    pub m: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_UNIFIED_CREEP_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_UNIFIED_CREEP_ORTHO").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_float()).unwrap_or_default(),
            pr21: t.take("PR21").and_then(|c| c.into_float()).unwrap_or_default(),
            pr31: t.take("PR31").and_then(|c| c.into_float()).unwrap_or_default(),
            pr32: t.take("PR32").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            g13: t.take("G13").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_USER_DEFINED_MATERIAL_MODELS {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub mt: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhv: Vec<i64>,
    pub iortho: Vec<i64>,
    pub ibulk: Vec<i64>,
    pub ig: Vec<i64>,
    pub ivect: Vec<i64>,
    pub ifail: Vec<i64>,
    pub itherm: Vec<i64>,
    pub ihyper: Vec<i64>,
    pub ieos: Vec<i64>,
    pub lmca: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub ievts: Vec<i64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
    pub p1_2: Vec<f64>,
    pub p2_2: Vec<f64>,
    pub p3_2: Vec<f64>,
    pub p4_2: Vec<f64>,
    pub p5_2: Vec<f64>,
    pub p6_2: Vec<f64>,
    pub p7_2: Vec<f64>,
    pub p8_2: Vec<f64>,
}
impl MAT_USER_DEFINED_MATERIAL_MODELS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_USER_DEFINED_MATERIAL_MODELS").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            mt: t.take("MT").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhv: t.take("NHV").and_then(|c| c.into_int()).unwrap_or_default(),
            iortho: t.take("IORTHO").and_then(|c| c.into_int()).unwrap_or_default(),
            ibulk: t.take("IBULK").and_then(|c| c.into_int()).unwrap_or_default(),
            ig: t.take("IG").and_then(|c| c.into_int()).unwrap_or_default(),
            ivect: t.take("IVECT").and_then(|c| c.into_int()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            itherm: t.take("ITHERM").and_then(|c| c.into_int()).unwrap_or_default(),
            ihyper: t.take("IHYPER").and_then(|c| c.into_int()).unwrap_or_default(),
            ieos: t.take("IEOS").and_then(|c| c.into_int()).unwrap_or_default(),
            lmca: t.take("LMCA").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            ievts: t.take("IEVTS").and_then(|c| c.into_int()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
            p1_2: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2_2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3_2: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4_2: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5_2: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6_2: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7_2: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8_2: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VACUUM {
    pub mid: Vec<i64>,
    pub rho: Vec<f64>,
}
impl MAT_VACUUM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VACUUM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            rho: t.take("RHO").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VEGTER {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fun_i: Vec<f64>,
    pub run_i: Vec<f64>,
    pub fps1_i: Vec<f64>,
    pub fps2_i: Vec<f64>,
    pub fsh_i: Vec<f64>,
}
impl MAT_VEGTER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VEGTER").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fun_i: t.take("FUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            run_i: t.take("RUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps1_i: t.take("FPS1-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps2_i: t.take("FPS2-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fsh_i: t.take("FSH-I").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VEGTER_2017 {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub unused: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub unused_2: Vec<i64>,
    pub dys: Vec<f64>,
    pub raten: Vec<f64>,
    pub srn0: Vec<f64>,
    pub exsr: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub rm_0: Vec<f64>,
    pub rm_45: Vec<f64>,
    pub rm_90: Vec<f64>,
    pub ag_0: Vec<f64>,
    pub ag_45: Vec<f64>,
    pub ag_90: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
}
impl MAT_VEGTER_2017 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VEGTER_2017").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            dys: t.take("DYS").and_then(|c| c.into_float()).unwrap_or_default(),
            raten: t.take("RATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            srn0: t.take("SRN0").and_then(|c| c.into_float()).unwrap_or_default(),
            exsr: t.take("EXSR").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_0: t.take("RM-0").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_45: t.take("RM-45").and_then(|c| c.into_float()).unwrap_or_default(),
            rm_90: t.take("RM-90").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_0: t.take("AG-0").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_45: t.take("AG-45").and_then(|c| c.into_float()).unwrap_or_default(),
            ag_90: t.take("AG-90").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VEGTER_STANDARD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub n: Vec<i64>,
    pub fbi: Vec<f64>,
    pub rbi0: Vec<f64>,
    pub lcid: Vec<f64>,
    pub sys: Vec<f64>,
    pub sip: Vec<f64>,
    pub shs: Vec<f64>,
    pub shl: Vec<f64>,
    pub esh: Vec<f64>,
    pub e0: Vec<f64>,
    pub alpha: Vec<f64>,
    pub lcid2: Vec<f64>,
    pub aopt: Vec<f64>,
    pub unused: Vec<i64>,
    pub dys: Vec<f64>,
    pub raten: Vec<f64>,
    pub srn0: Vec<f64>,
    pub exsr: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub beta: Vec<f64>,
    pub fun_i: Vec<f64>,
    pub run_i: Vec<f64>,
    pub fps1_i: Vec<f64>,
    pub alps_i: Vec<f64>,
    pub fsh_i: Vec<f64>,
}
impl MAT_VEGTER_STANDARD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VEGTER_STANDARD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_int()).unwrap_or_default(),
            fbi: t.take("FBI").and_then(|c| c.into_float()).unwrap_or_default(),
            rbi0: t.take("RBI0").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_float()).unwrap_or_default(),
            sys: t.take("SYS").and_then(|c| c.into_float()).unwrap_or_default(),
            sip: t.take("SIP").and_then(|c| c.into_float()).unwrap_or_default(),
            shs: t.take("SHS").and_then(|c| c.into_float()).unwrap_or_default(),
            shl: t.take("SHL").and_then(|c| c.into_float()).unwrap_or_default(),
            esh: t.take("ESH").and_then(|c| c.into_float()).unwrap_or_default(),
            e0: t.take("E0").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid2: t.take("LCID2").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            dys: t.take("DYS").and_then(|c| c.into_float()).unwrap_or_default(),
            raten: t.take("RATEN").and_then(|c| c.into_float()).unwrap_or_default(),
            srn0: t.take("SRN0").and_then(|c| c.into_float()).unwrap_or_default(),
            exsr: t.take("EXSR").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fun_i: t.take("FUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            run_i: t.take("RUN-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fps1_i: t.take("FPS1-I").and_then(|c| c.into_float()).unwrap_or_default(),
            alps_i: t.take("ALPS-I").and_then(|c| c.into_float()).unwrap_or_default(),
            fsh_i: t.take("FSH-I").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOELASTIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub g0: Vec<f64>,
    pub gi: Vec<f64>,
    pub beta: Vec<f64>,
}
impl MAT_VISCOELASTIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOELASTIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            g0: t.take("G0").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOELASTIC_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub f: Vec<f64>,
    pub f_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub cse: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_VISCOELASTIC_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOELASTIC_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            cse: t.take("CSE").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOELASTIC_HILL_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub k: Vec<f64>,
    pub n: Vec<f64>,
    pub nu: Vec<f64>,
    pub lcid: Vec<i64>,
    pub fittype: Vec<i64>,
    pub lcsr: Vec<i64>,
    pub lcve: Vec<i64>,
    pub nt: Vec<f64>,
    pub gstart: Vec<f64>,
    pub c1: Vec<f64>,
    pub c2: Vec<f64>,
    pub c3: Vec<f64>,
    pub c4: Vec<f64>,
    pub c5: Vec<f64>,
    pub c6: Vec<f64>,
    pub c7: Vec<f64>,
    pub c8: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
}
impl MAT_VISCOELASTIC_HILL_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOELASTIC_HILL_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            n: t.take("N").and_then(|c| c.into_float()).unwrap_or_default(),
            nu: t.take("NU").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            fittype: t.take("FITTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            lcsr: t.take("LCSR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcve: t.take("LCVE").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_float()).unwrap_or_default(),
            gstart: t.take("GSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            c1: t.take("C1").and_then(|c| c.into_float()).unwrap_or_default(),
            c2: t.take("C2").and_then(|c| c.into_float()).unwrap_or_default(),
            c3: t.take("C3").and_then(|c| c.into_float()).unwrap_or_default(),
            c4: t.take("C4").and_then(|c| c.into_float()).unwrap_or_default(),
            c5: t.take("C5").and_then(|c| c.into_float()).unwrap_or_default(),
            c6: t.take("C6").and_then(|c| c.into_float()).unwrap_or_default(),
            c7: t.take("C7").and_then(|c| c.into_float()).unwrap_or_default(),
            c8: t.take("C8").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOELASTIC_LOOSE_FABRIC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub e2: Vec<f64>,
    pub g12: Vec<f64>,
    pub eu: Vec<f64>,
    pub thl: Vec<f64>,
    pub thi: Vec<f64>,
    pub ta: Vec<f64>,
    pub w: Vec<f64>,
    pub s: Vec<f64>,
    pub t: Vec<f64>,
    pub h: Vec<f64>,
    pub s_2: Vec<f64>,
    pub eka: Vec<f64>,
    pub eua: Vec<f64>,
    pub vmb: Vec<f64>,
    pub c: Vec<f64>,
    pub g23: Vec<f64>,
    pub ekb: Vec<f64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_VISCOELASTIC_LOOSE_FABRIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOELASTIC_LOOSE_FABRIC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            g12: t.take("G12").and_then(|c| c.into_float()).unwrap_or_default(),
            eu: t.take("EU").and_then(|c| c.into_float()).unwrap_or_default(),
            thl: t.take("THL").and_then(|c| c.into_float()).unwrap_or_default(),
            thi: t.take("THI").and_then(|c| c.into_float()).unwrap_or_default(),
            ta: t.take("TA").and_then(|c| c.into_float()).unwrap_or_default(),
            w: t.take("W").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("s").and_then(|c| c.into_float()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            s_2: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            eka: t.take("EKA").and_then(|c| c.into_float()).unwrap_or_default(),
            eua: t.take("EUA").and_then(|c| c.into_float()).unwrap_or_default(),
            vmb: t.take("VMB").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            g23: t.take("G23").and_then(|c| c.into_float()).unwrap_or_default(),
            ekb: t.take("EKB").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("Xp").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("Yp").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("Zp").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOELASTIC_THERMAL {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub bulk: Vec<f64>,
    pub pcf: Vec<f64>,
    pub ef: Vec<f64>,
    pub tref: Vec<f64>,
    pub a: Vec<f64>,
    pub b: Vec<f64>,
    pub lcid: Vec<i64>,
    pub nt: Vec<i64>,
    pub bstart: Vec<f64>,
    pub tramp: Vec<f64>,
    pub lcidk: Vec<i64>,
    pub ntk: Vec<i64>,
    pub bstartk: Vec<f64>,
    pub trampk: Vec<f64>,
    pub gi: Vec<f64>,
    pub betai: Vec<f64>,
    pub ki: Vec<f64>,
    pub betaki: Vec<f64>,
}
impl MAT_VISCOELASTIC_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOELASTIC_THERMAL").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            bulk: t.take("BULK").and_then(|c| c.into_float()).unwrap_or_default(),
            pcf: t.take("PCF").and_then(|c| c.into_float()).unwrap_or_default(),
            ef: t.take("EF").and_then(|c| c.into_float()).unwrap_or_default(),
            tref: t.take("TREF").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            b: t.take("B").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nt: t.take("NT").and_then(|c| c.into_int()).unwrap_or_default(),
            bstart: t.take("BSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            tramp: t.take("TRAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcidk: t.take("LCIDK").and_then(|c| c.into_int()).unwrap_or_default(),
            ntk: t.take("NTK").and_then(|c| c.into_int()).unwrap_or_default(),
            bstartk: t.take("BSTARTK").and_then(|c| c.into_float()).unwrap_or_default(),
            trampk: t.take("TRAMPK").and_then(|c| c.into_float()).unwrap_or_default(),
            gi: t.take("GI").and_then(|c| c.into_float()).unwrap_or_default(),
            betai: t.take("BETAI").and_then(|c| c.into_float()).unwrap_or_default(),
            ki: t.take("KI").and_then(|c| c.into_float()).unwrap_or_default(),
            betaki: t.take("BETAKI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOPLASTIC_MIXED_HARDENING {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub lcss: Vec<i64>,
    pub beta: Vec<f64>,
    pub fail: Vec<f64>,
}
impl MAT_VISCOPLASTIC_MIXED_HARDENING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOPLASTIC_MIXED_HARDENING").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            lcss: t.take("LCSS").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            fail: t.take("FAIL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_VISCOUS_FOAM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e1: Vec<f64>,
    pub n1: Vec<f64>,
    pub v2: Vec<f64>,
    pub e2: Vec<f64>,
    pub n2: Vec<f64>,
    pub pr: Vec<f64>,
}
impl MAT_VISCOUS_FOAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_VISCOUS_FOAM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_float()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_float()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WINFRITH_CONCRETE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub tm: Vec<f64>,
    pub pr: Vec<f64>,
    pub ucs: Vec<f64>,
    pub uts: Vec<f64>,
    pub fe: Vec<f64>,
    pub asize: Vec<f64>,
    pub e: Vec<f64>,
    pub ys: Vec<f64>,
    pub eh: Vec<f64>,
    pub uelong: Vec<f64>,
    pub rate: Vec<f64>,
    pub conm: Vec<f64>,
    pub conl: Vec<f64>,
    pub cont: Vec<f64>,
    pub eps1: Vec<f64>,
    pub eps2: Vec<f64>,
    pub eps3: Vec<f64>,
    pub eps4: Vec<f64>,
    pub eps5: Vec<f64>,
    pub eps6: Vec<f64>,
    pub eps7: Vec<f64>,
    pub eps8: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl MAT_WINFRITH_CONCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WINFRITH_CONCRETE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            ucs: t.take("UCS").and_then(|c| c.into_float()).unwrap_or_default(),
            uts: t.take("UTS").and_then(|c| c.into_float()).unwrap_or_default(),
            fe: t.take("FE").and_then(|c| c.into_float()).unwrap_or_default(),
            asize: t.take("ASIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            ys: t.take("YS").and_then(|c| c.into_float()).unwrap_or_default(),
            eh: t.take("EH").and_then(|c| c.into_float()).unwrap_or_default(),
            uelong: t.take("UELONG").and_then(|c| c.into_float()).unwrap_or_default(),
            rate: t.take("RATE").and_then(|c| c.into_float()).unwrap_or_default(),
            conm: t.take("CONM").and_then(|c| c.into_float()).unwrap_or_default(),
            conl: t.take("CONL").and_then(|c| c.into_float()).unwrap_or_default(),
            cont: t.take("CONT").and_then(|c| c.into_float()).unwrap_or_default(),
            eps1: t.take("EPS1").and_then(|c| c.into_float()).unwrap_or_default(),
            eps2: t.take("EPS2").and_then(|c| c.into_float()).unwrap_or_default(),
            eps3: t.take("EPS3").and_then(|c| c.into_float()).unwrap_or_default(),
            eps4: t.take("EPS4").and_then(|c| c.into_float()).unwrap_or_default(),
            eps5: t.take("EPS5").and_then(|c| c.into_float()).unwrap_or_default(),
            eps6: t.take("EPS6").and_then(|c| c.into_float()).unwrap_or_default(),
            eps7: t.take("EPS7").and_then(|c| c.into_float()).unwrap_or_default(),
            eps8: t.take("EPS8").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WINFRITH_CONCRETE_REINFORCEMENT {
    pub eid1_unused: Vec<i64>,
    pub eid2_pid: Vec<i64>,
    pub inc_axis: Vec<i64>,
    pub xr_coor: Vec<f64>,
    pub yr_rqa: Vec<f64>,
    pub zr_rqb: Vec<f64>,
}
impl MAT_WINFRITH_CONCRETE_REINFORCEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WINFRITH_CONCRETE_REINFORCEMENT").unwrap());
        Self {
            eid1_unused: t.take("EID1/UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2_pid: t.take("EID2/PID").and_then(|c| c.into_int()).unwrap_or_default(),
            inc_axis: t.take("INC/AXIS").and_then(|c| c.into_int()).unwrap_or_default(),
            xr_coor: t.take("XR/COOR").and_then(|c| c.into_float()).unwrap_or_default(),
            yr_rqa: t.take("YR/RQA").and_then(|c| c.into_float()).unwrap_or_default(),
            zr_rqb: t.take("ZR/RQB").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WOOD {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub el: Vec<f64>,
    pub et: Vec<f64>,
    pub glt: Vec<f64>,
    pub gtr: Vec<f64>,
    pub pr: Vec<f64>,
    pub xt: Vec<f64>,
    pub xc: Vec<f64>,
    pub yt: Vec<f64>,
    pub yc: Vec<f64>,
    pub sxy: Vec<f64>,
    pub syz: Vec<f64>,
    pub gf1: Vec<f64>,
    pub gf2: Vec<f64>,
    pub bfit: Vec<f64>,
    pub dmax: Vec<f64>,
    pub gf1p: Vec<f64>,
    pub gf2p: Vec<f64>,
    pub dfit: Vec<f64>,
    pub dmaxp: Vec<f64>,
    pub flpar: Vec<f64>,
    pub flparc: Vec<f64>,
    pub powpar: Vec<f64>,
    pub flper: Vec<f64>,
    pub flperc: Vec<f64>,
    pub powper: Vec<f64>,
    pub npar: Vec<f64>,
    pub cpar: Vec<f64>,
    pub nper: Vec<f64>,
    pub cper: Vec<f64>,
    pub aopt: Vec<f64>,
    pub macf: Vec<i64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_WOOD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WOOD").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            el: t.take("EL").and_then(|c| c.into_float()).unwrap_or_default(),
            et: t.take("ET").and_then(|c| c.into_float()).unwrap_or_default(),
            glt: t.take("GLT").and_then(|c| c.into_float()).unwrap_or_default(),
            gtr: t.take("GTR").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            sxy: t.take("SXY").and_then(|c| c.into_float()).unwrap_or_default(),
            syz: t.take("SYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            gf1: t.take("GF1||").and_then(|c| c.into_float()).unwrap_or_default(),
            gf2: t.take("GF2||").and_then(|c| c.into_float()).unwrap_or_default(),
            bfit: t.take("BFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmax: t.take("DMAX||").and_then(|c| c.into_float()).unwrap_or_default(),
            gf1p: t.take("GF1P").and_then(|c| c.into_float()).unwrap_or_default(),
            gf2p: t.take("GF2P").and_then(|c| c.into_float()).unwrap_or_default(),
            dfit: t.take("DFIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dmaxp: t.take("DMAXP").and_then(|c| c.into_float()).unwrap_or_default(),
            flpar: t.take("FLPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            flparc: t.take("FLPARC").and_then(|c| c.into_float()).unwrap_or_default(),
            powpar: t.take("POWPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            flper: t.take("FLPER").and_then(|c| c.into_float()).unwrap_or_default(),
            flperc: t.take("FLPERC").and_then(|c| c.into_float()).unwrap_or_default(),
            powper: t.take("POWPER").and_then(|c| c.into_float()).unwrap_or_default(),
            npar: t.take("NPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            cpar: t.take("CPAR").and_then(|c| c.into_float()).unwrap_or_default(),
            nper: t.take("NPER").and_then(|c| c.into_float()).unwrap_or_default(),
            cper: t.take("CPER").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            macf: t.take("MACF").and_then(|c| c.into_int()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WOOD_FIR {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub mois: Vec<f64>,
    pub temp: Vec<f64>,
    pub qual_t: Vec<f64>,
    pub qual_c: Vec<f64>,
    pub units: Vec<i64>,
    pub iqual: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_WOOD_FIR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WOOD_FIR").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mois: t.take("MOIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_t: t.take("QUAL_T").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_c: t.take("QUAL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            iqual: t.take("IQUAL").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WOOD_PINE {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub nplot: Vec<i64>,
    pub iters: Vec<i64>,
    pub irate: Vec<i64>,
    pub ghard: Vec<f64>,
    pub ifail: Vec<i64>,
    pub ivol: Vec<i64>,
    pub mois: Vec<f64>,
    pub temp: Vec<f64>,
    pub qual_t: Vec<f64>,
    pub qual_c: Vec<f64>,
    pub units: Vec<i64>,
    pub iqual: Vec<i64>,
    pub aopt: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
}
impl MAT_WOOD_PINE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WOOD_PINE").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            nplot: t.take("NPLOT").and_then(|c| c.into_int()).unwrap_or_default(),
            iters: t.take("ITERS").and_then(|c| c.into_int()).unwrap_or_default(),
            irate: t.take("IRATE").and_then(|c| c.into_int()).unwrap_or_default(),
            ghard: t.take("GHARD").and_then(|c| c.into_float()).unwrap_or_default(),
            ifail: t.take("IFAIL").and_then(|c| c.into_int()).unwrap_or_default(),
            ivol: t.take("IVOL").and_then(|c| c.into_int()).unwrap_or_default(),
            mois: t.take("MOIS").and_then(|c| c.into_float()).unwrap_or_default(),
            temp: t.take("TEMP").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_t: t.take("QUAL_T").and_then(|c| c.into_float()).unwrap_or_default(),
            qual_c: t.take("QUAL_C").and_then(|c| c.into_float()).unwrap_or_default(),
            units: t.take("UNITS").and_then(|c| c.into_int()).unwrap_or_default(),
            iqual: t.take("IQUAL").and_then(|c| c.into_int()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WTM_STM {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub numfi: Vec<f64>,
    pub npsc: Vec<f64>,
    pub wc: Vec<f64>,
    pub tauc: Vec<f64>,
    pub sigma0: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub k: Vec<f64>,
    pub lc: Vec<f64>,
    pub flg: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub a7: Vec<f64>,
    pub a8: Vec<f64>,
    pub s00: Vec<f64>,
    pub s45: Vec<f64>,
    pub s90: Vec<f64>,
    pub sbb: Vec<f64>,
    pub r00: Vec<f64>,
    pub r45: Vec<f64>,
    pub r90: Vec<f64>,
    pub rbb: Vec<f64>,
    pub a: Vec<f64>,
    pub c: Vec<f64>,
    pub h: Vec<f64>,
    pub p: Vec<f64>,
    pub qx1: Vec<f64>,
    pub cx1: Vec<f64>,
    pub qx2: Vec<f64>,
    pub cx2: Vec<f64>,
    pub edot: Vec<f64>,
    pub m: Vec<f64>,
    pub emin: Vec<f64>,
    pub s100: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub a2_2: Vec<f64>,
    pub a3_2: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_WTM_STM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WTM_STM").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            numfi: t.take("NUMFI").and_then(|c| c.into_float()).unwrap_or_default(),
            npsc: t.take("NPSC").and_then(|c| c.into_float()).unwrap_or_default(),
            wc: t.take("WC").and_then(|c| c.into_float()).unwrap_or_default(),
            tauc: t.take("TAUC").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma0: t.take("SIGMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            lc: t.take("LC").and_then(|c| c.into_float()).unwrap_or_default(),
            flg: t.take("FLG").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            a7: t.take("A7").and_then(|c| c.into_float()).unwrap_or_default(),
            a8: t.take("A8").and_then(|c| c.into_float()).unwrap_or_default(),
            s00: t.take("S00").and_then(|c| c.into_float()).unwrap_or_default(),
            s45: t.take("S45").and_then(|c| c.into_float()).unwrap_or_default(),
            s90: t.take("S90").and_then(|c| c.into_float()).unwrap_or_default(),
            sbb: t.take("SBB").and_then(|c| c.into_float()).unwrap_or_default(),
            r00: t.take("R00").and_then(|c| c.into_float()).unwrap_or_default(),
            r45: t.take("R45").and_then(|c| c.into_float()).unwrap_or_default(),
            r90: t.take("R90").and_then(|c| c.into_float()).unwrap_or_default(),
            rbb: t.take("RBB").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            c: t.take("C").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            p: t.take("P").and_then(|c| c.into_float()).unwrap_or_default(),
            qx1: t.take("QX1").and_then(|c| c.into_float()).unwrap_or_default(),
            cx1: t.take("CX1").and_then(|c| c.into_float()).unwrap_or_default(),
            qx2: t.take("QX2").and_then(|c| c.into_float()).unwrap_or_default(),
            cx2: t.take("CX2").and_then(|c| c.into_float()).unwrap_or_default(),
            edot: t.take("EDOT").and_then(|c| c.into_float()).unwrap_or_default(),
            m: t.take("M").and_then(|c| c.into_float()).unwrap_or_default(),
            emin: t.take("EMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            s100: t.take("S100").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2_2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3_2: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MAT_WTM_STM_PLC {
    pub mid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
    pub numfi: Vec<f64>,
    pub npsc: Vec<f64>,
    pub wc: Vec<f64>,
    pub tauc: Vec<f64>,
    pub sigma0: Vec<f64>,
    pub qr1: Vec<f64>,
    pub cr1: Vec<f64>,
    pub qr2: Vec<f64>,
    pub cr2: Vec<f64>,
    pub k: Vec<f64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
    pub a5: Vec<f64>,
    pub a6: Vec<f64>,
    pub a7: Vec<f64>,
    pub a8: Vec<f64>,
    pub s: Vec<f64>,
    pub h: Vec<f64>,
    pub omega: Vec<f64>,
    pub td: Vec<f64>,
    pub alpha: Vec<f64>,
    pub eps0: Vec<f64>,
    pub aopt: Vec<f64>,
    pub beta: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub a1_2: Vec<f64>,
    pub a2_2: Vec<f64>,
    pub a3_2: Vec<f64>,
    pub v1: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl MAT_WTM_STM_PLC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MAT_WTM_STM_PLC").unwrap());
        Self {
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
            numfi: t.take("NUMFI").and_then(|c| c.into_float()).unwrap_or_default(),
            npsc: t.take("NPSC").and_then(|c| c.into_float()).unwrap_or_default(),
            wc: t.take("WC").and_then(|c| c.into_float()).unwrap_or_default(),
            tauc: t.take("TAUC").and_then(|c| c.into_float()).unwrap_or_default(),
            sigma0: t.take("SIGMA0").and_then(|c| c.into_float()).unwrap_or_default(),
            qr1: t.take("QR1").and_then(|c| c.into_float()).unwrap_or_default(),
            cr1: t.take("CR1").and_then(|c| c.into_float()).unwrap_or_default(),
            qr2: t.take("QR2").and_then(|c| c.into_float()).unwrap_or_default(),
            cr2: t.take("CR2").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_float()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
            a5: t.take("A5").and_then(|c| c.into_float()).unwrap_or_default(),
            a6: t.take("A6").and_then(|c| c.into_float()).unwrap_or_default(),
            a7: t.take("A7").and_then(|c| c.into_float()).unwrap_or_default(),
            a8: t.take("A8").and_then(|c| c.into_float()).unwrap_or_default(),
            s: t.take("S").and_then(|c| c.into_float()).unwrap_or_default(),
            h: t.take("H").and_then(|c| c.into_float()).unwrap_or_default(),
            omega: t.take("OMEGA").and_then(|c| c.into_float()).unwrap_or_default(),
            td: t.take("TD").and_then(|c| c.into_float()).unwrap_or_default(),
            alpha: t.take("ALPHA").and_then(|c| c.into_float()).unwrap_or_default(),
            eps0: t.take("EPS0").and_then(|c| c.into_float()).unwrap_or_default(),
            aopt: t.take("AOPT").and_then(|c| c.into_float()).unwrap_or_default(),
            beta: t.take("BETA").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            a1_2: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2_2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3_2: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            v1: t.take("V1").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MESH_BL {
    pub pid: Vec<i64>,
    pub nelth: Vec<i64>,
    pub blth: Vec<f64>,
    pub blfe: Vec<f64>,
    pub blst: Vec<i64>,
}
impl MESH_BL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_BL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nelth: t.take("NELTH").and_then(|c| c.into_int()).unwrap_or_default(),
            blth: t.take("BLTH").and_then(|c| c.into_float()).unwrap_or_default(),
            blfe: t.take("BLFE").and_then(|c| c.into_float()).unwrap_or_default(),
            blst: t.take("BLST").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_BL_SYM {
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl MESH_BL_SYM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_BL_SYM").unwrap());
        Self {
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_EMBEDSHELL {
    pub volid: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl MESH_EMBEDSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_EMBEDSHELL").unwrap());
        Self {
            volid: t.take("VOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_INTERF {
    pub volid: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl MESH_INTERF {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_INTERF").unwrap());
        Self {
            volid: t.take("VOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_NODE {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl MESH_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MESH_SIZE {
    pub volid: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl MESH_SIZE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_SIZE").unwrap());
        Self {
            volid: t.take("VOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_SIZE_SHAPE {
    pub sname: Vec<String>,
    pub force: Vec<i64>,
    pub method: Vec<i64>,
    pub bt: Vec<f64>,
    pub dt: Vec<f64>,
    pub msize: Vec<f64>,
    pub pminx: Vec<f64>,
    pub pminy: Vec<f64>,
    pub pminz: Vec<f64>,
    pub pmaxx: Vec<f64>,
    pub pmaxy: Vec<f64>,
    pub pmaxz: Vec<f64>,
    pub msize_2: Vec<f64>,
    pub radius: Vec<f64>,
    pub centerx: Vec<f64>,
    pub centery: Vec<f64>,
    pub centerz: Vec<f64>,
    pub msize_3: Vec<f64>,
    pub radius_2: Vec<f64>,
    pub pminx_2: Vec<f64>,
    pub pminy_2: Vec<f64>,
    pub pminz_2: Vec<f64>,
    pub pmaxx_2: Vec<f64>,
    pub pmaxy_2: Vec<f64>,
    pub pmaxz_2: Vec<f64>,
}
impl MESH_SIZE_SHAPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_SIZE_SHAPE").unwrap());
        Self {
            sname: t.take("Sname").and_then(|c| c.into_str()).unwrap_or_default(),
            force: t.take("Force").and_then(|c| c.into_int()).unwrap_or_default(),
            method: t.take("METHOD").and_then(|c| c.into_int()).unwrap_or_default(),
            bt: t.take("BT").and_then(|c| c.into_float()).unwrap_or_default(),
            dt: t.take("DT").and_then(|c| c.into_float()).unwrap_or_default(),
            msize: t.take("MSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            pminx: t.take("PminX").and_then(|c| c.into_float()).unwrap_or_default(),
            pminy: t.take("PminY").and_then(|c| c.into_float()).unwrap_or_default(),
            pminz: t.take("PminZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxx: t.take("PmaxX").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxy: t.take("PmaxY").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxz: t.take("PmaxZ").and_then(|c| c.into_float()).unwrap_or_default(),
            msize_2: t.take("MSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            radius: t.take("Radius").and_then(|c| c.into_float()).unwrap_or_default(),
            centerx: t.take("CenterX").and_then(|c| c.into_float()).unwrap_or_default(),
            centery: t.take("CenterY").and_then(|c| c.into_float()).unwrap_or_default(),
            centerz: t.take("CenterZ").and_then(|c| c.into_float()).unwrap_or_default(),
            msize_3: t.take("MSIZE").and_then(|c| c.into_float()).unwrap_or_default(),
            radius_2: t.take("Radius").and_then(|c| c.into_float()).unwrap_or_default(),
            pminx_2: t.take("PminX").and_then(|c| c.into_float()).unwrap_or_default(),
            pminy_2: t.take("PminY").and_then(|c| c.into_float()).unwrap_or_default(),
            pminz_2: t.take("PminZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxx_2: t.take("PmaxX").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxy_2: t.take("PmaxY").and_then(|c| c.into_float()).unwrap_or_default(),
            pmaxz_2: t.take("PmaxZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MESH_SURFACE_ELEMENT {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl MESH_SURFACE_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_SURFACE_ELEMENT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_SURFACE_NODE {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl MESH_SURFACE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_SURFACE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MESH_VOLUME {
    pub volid: Vec<i64>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl MESH_VOLUME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_VOLUME").unwrap());
        Self {
            volid: t.take("VOLID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_VOLUME_ELEMENT {
    pub eid: Vec<i64>,
    pub pid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl MESH_VOLUME_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_VOLUME_ELEMENT").unwrap());
        Self {
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct MESH_VOLUME_NODE {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl MESH_VOLUME_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_VOLUME_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct MESH_VOLUME_PART {
    pub volprt: Vec<i64>,
    pub solprt: Vec<i64>,
    pub solver: Vec<String>,
}
impl MESH_VOLUME_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MESH_VOLUME_PART").unwrap());
        Self {
            volprt: t.take("VOLPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            solprt: t.take("SOLPRT").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct MODULE_LOAD {
    pub mdlid: Vec<i64>,
    pub title: Vec<String>,
    pub filename: Vec<String>,
}
impl MODULE_LOAD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MODULE_LOAD").unwrap());
        Self {
            mdlid: t.take("MDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct MODULE_PATH {
    pub path: Vec<String>,
}
impl MODULE_PATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MODULE_PATH").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct MODULE_PATH_RELATIVE {
    pub path: Vec<String>,
}
impl MODULE_PATH_RELATIVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MODULE_PATH_RELATIVE").unwrap());
        Self {
            path: t.take("PATH").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct MODULE_USE {
    pub mdlid: Vec<i64>,
    pub type_: Vec<String>,
    pub param1: Vec<String>,
    pub param2: Vec<String>,
}
impl MODULE_USE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("MODULE_USE").unwrap());
        Self {
            mdlid: t.take("MDLID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            param1: t.take("PARAM1").and_then(|c| c.into_str()).unwrap_or_default(),
            param2: t.take("PARAM2").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct NODE_MERGE {
    pub nid: Vec<i64>,
}
impl NODE_MERGE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_MERGE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_MERGE_SET {
    pub nsid: Vec<i64>,
}
impl NODE_MERGE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_MERGE_SET").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_MERGE_TOLERANCE {
    pub tolr: Vec<f64>,
}
impl NODE_MERGE_TOLERANCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_MERGE_TOLERANCE").unwrap());
        Self {
            tolr: t.take("TOLR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct NODE_NODE {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl NODE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_NODE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_NODES {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
    pub tc: Vec<i64>,
    pub rc: Vec<i64>,
}
impl NODE_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_NODES").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
            tc: t.take("TC").and_then(|c| c.into_int()).unwrap_or_default(),
            rc: t.take("RC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_RIGID_SURFACE {
    pub nid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl NODE_RIGID_SURFACE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_RIGID_SURFACE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct NODE_SCALAR {
    pub nid: Vec<i64>,
    pub ndof: Vec<i64>,
}
impl NODE_SCALAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_SCALAR").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            ndof: t.take("NDOF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_SCALAR_VALUE {
    pub nid: Vec<i64>,
    pub x1: Vec<f64>,
    pub x2: Vec<f64>,
    pub x3: Vec<f64>,
    pub ndof: Vec<i64>,
}
impl NODE_SCALAR_VALUE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_SCALAR_VALUE").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            x1: t.take("X1").and_then(|c| c.into_float()).unwrap_or_default(),
            x2: t.take("X2").and_then(|c| c.into_float()).unwrap_or_default(),
            x3: t.take("X3").and_then(|c| c.into_float()).unwrap_or_default(),
            ndof: t.take("NDOF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_THICKNESS {
    pub id1: Vec<i64>,
    pub thk: Vec<f64>,
    pub id2: Vec<i64>,
    pub inc: Vec<i64>,
}
impl NODE_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_THICKNESS").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thk: t.take("THK").and_then(|c| c.into_float()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            inc: t.take("INC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_THICKNESS_GENERATE {
    pub id1: Vec<i64>,
    pub thk: Vec<f64>,
    pub id2: Vec<i64>,
    pub inc: Vec<i64>,
}
impl NODE_THICKNESS_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_THICKNESS_GENERATE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thk: t.take("THK").and_then(|c| c.into_float()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            inc: t.take("INC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_THICKNESS_SET {
    pub id1: Vec<i64>,
    pub thk: Vec<f64>,
    pub id2: Vec<i64>,
    pub inc: Vec<i64>,
}
impl NODE_THICKNESS_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_THICKNESS_SET").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thk: t.take("THK").and_then(|c| c.into_float()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            inc: t.take("INC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_THICKNESS_SET_GENERATE {
    pub id1: Vec<i64>,
    pub thk: Vec<f64>,
    pub id2: Vec<i64>,
    pub inc: Vec<i64>,
}
impl NODE_THICKNESS_SET_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_THICKNESS_SET_GENERATE").unwrap());
        Self {
            id1: t.take("ID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thk: t.take("THK").and_then(|c| c.into_float()).unwrap_or_default(),
            id2: t.take("ID2").and_then(|c| c.into_int()).unwrap_or_default(),
            inc: t.take("INC").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct NODE_TO_TARGET_VECTOR {
    pub nid: Vec<i64>,
    pub xdelta: Vec<f64>,
    pub ydelta: Vec<f64>,
    pub zdelta: Vec<f64>,
}
impl NODE_TO_TARGET_VECTOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_TO_TARGET_VECTOR").unwrap());
        Self {
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            xdelta: t.take("XDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            ydelta: t.take("YDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
            zdelta: t.take("ZDELTA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct NODE_TRANSFORM {
    pub trsid: Vec<i64>,
    pub nsid: Vec<i64>,
    pub immed: Vec<i64>,
}
impl NODE_TRANSFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("NODE_TRANSFORM").unwrap());
        Self {
            trsid: t.take("TRSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            immed: t.take("IMMED").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_DUPLICATION {
    pub dflag: Vec<i64>,
}
impl PARAMETER_DUPLICATION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_DUPLICATION").unwrap());
        Self {
            dflag: t.take("DFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_EXPRESSION {
    pub prmr: Vec<String>,
    pub expression: Vec<String>,
}
impl PARAMETER_EXPRESSION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_EXPRESSION").unwrap());
        Self {
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
            expression: t.take("EXPRESSION").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_EXPRESSION_LOCAL {
    pub prmr: Vec<String>,
    pub expression: Vec<String>,
}
impl PARAMETER_EXPRESSION_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_EXPRESSION_LOCAL").unwrap());
        Self {
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
            expression: t.take("EXPRESSION").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_EXPRESSION_NOECHO {
    pub prmr: Vec<String>,
    pub expression: Vec<String>,
}
impl PARAMETER_EXPRESSION_NOECHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_EXPRESSION_NOECHO").unwrap());
        Self {
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
            expression: t.take("EXPRESSION").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_LOCAL {
    pub prmr1: Vec<String>,
    pub val1: Vec<String>,
    pub prmr2: Vec<String>,
    pub val2: Vec<String>,
    pub prmr3: Vec<String>,
    pub val3: Vec<String>,
    pub prmr4: Vec<String>,
    pub val4: Vec<String>,
}
impl PARAMETER_LOCAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_LOCAL").unwrap());
        Self {
            prmr1: t.take("PRMR1").and_then(|c| c.into_str()).unwrap_or_default(),
            val1: t.take("VAL1").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr2: t.take("PRMR2").and_then(|c| c.into_str()).unwrap_or_default(),
            val2: t.take("VAL2").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr3: t.take("PRMR3").and_then(|c| c.into_str()).unwrap_or_default(),
            val3: t.take("VAL3").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr4: t.take("PRMR4").and_then(|c| c.into_str()).unwrap_or_default(),
            val4: t.take("VAL4").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_NOECHO {
    pub prmr1: Vec<String>,
    pub val1: Vec<String>,
    pub prmr2: Vec<String>,
    pub val2: Vec<String>,
    pub prmr3: Vec<String>,
    pub val3: Vec<String>,
    pub prmr4: Vec<String>,
    pub val4: Vec<String>,
}
impl PARAMETER_NOECHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_NOECHO").unwrap());
        Self {
            prmr1: t.take("PRMR1").and_then(|c| c.into_str()).unwrap_or_default(),
            val1: t.take("VAL1").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr2: t.take("PRMR2").and_then(|c| c.into_str()).unwrap_or_default(),
            val2: t.take("VAL2").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr3: t.take("PRMR3").and_then(|c| c.into_str()).unwrap_or_default(),
            val3: t.take("VAL3").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr4: t.take("PRMR4").and_then(|c| c.into_str()).unwrap_or_default(),
            val4: t.take("VAL4").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_PARAMETER {
    pub prmr1: Vec<String>,
    pub val1: Vec<String>,
    pub prmr2: Vec<String>,
    pub val2: Vec<String>,
    pub prmr3: Vec<String>,
    pub val3: Vec<String>,
    pub prmr4: Vec<String>,
    pub val4: Vec<String>,
}
impl PARAMETER_PARAMETER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_PARAMETER").unwrap());
        Self {
            prmr1: t.take("PRMR1").and_then(|c| c.into_str()).unwrap_or_default(),
            val1: t.take("VAL1").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr2: t.take("PRMR2").and_then(|c| c.into_str()).unwrap_or_default(),
            val2: t.take("VAL2").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr3: t.take("PRMR3").and_then(|c| c.into_str()).unwrap_or_default(),
            val3: t.take("VAL3").and_then(|c| c.into_str()).unwrap_or_default(),
            prmr4: t.take("PRMR4").and_then(|c| c.into_str()).unwrap_or_default(),
            val4: t.take("VAL4").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARAMETER_TYPE {
    pub prmr: Vec<String>,
    pub val: Vec<i64>,
    pub prtyp: Vec<String>,
}
impl PARAMETER_TYPE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARAMETER_TYPE").unwrap());
        Self {
            prmr: t.take("PRMR").and_then(|c| c.into_str()).unwrap_or_default(),
            val: t.take("VAL").and_then(|c| c.into_int()).unwrap_or_default(),
            prtyp: t.take("PRTYP").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct PARTICLE_BLAST {
    pub lagsid: Vec<i64>,
    pub lagstype: Vec<i64>,
    pub dodid: Vec<i64>,
    pub dodtype: Vec<i64>,
    pub hecid: Vec<i64>,
    pub hectype: Vec<i64>,
    pub aircid: Vec<i64>,
    pub nphe: Vec<i64>,
    pub npair: Vec<i64>,
    pub iunit: Vec<i64>,
    pub ihetype: Vec<i64>,
    pub densit: Vec<f64>,
    pub energy: Vec<f64>,
    pub gamma: Vec<f64>,
    pub covol: Vec<f64>,
    pub deto_v: Vec<f64>,
    pub detx: Vec<f64>,
    pub dety: Vec<f64>,
    pub detz: Vec<f64>,
    pub tdet: Vec<f64>,
    pub btend: Vec<f64>,
    pub nid: Vec<i64>,
    pub bcxo: Vec<f64>,
    pub bcx1: Vec<f64>,
    pub bcy0: Vec<f64>,
    pub bcy1: Vec<f64>,
    pub bcz0: Vec<f64>,
    pub bcz1: Vec<f64>,
    pub ibcx0: Vec<i64>,
    pub ibcx1: Vec<i64>,
    pub ibcy0: Vec<i64>,
    pub ibcy1: Vec<i64>,
    pub ibcz0: Vec<i64>,
    pub ibcz1: Vec<i64>,
    pub bc_p: Vec<i64>,
}
impl PARTICLE_BLAST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PARTICLE_BLAST").unwrap());
        Self {
            lagsid: t.take("LAGSID").and_then(|c| c.into_int()).unwrap_or_default(),
            lagstype: t.take("LAGSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            dodid: t.take("DODID").and_then(|c| c.into_int()).unwrap_or_default(),
            dodtype: t.take("DODTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            hecid: t.take("HECID").and_then(|c| c.into_int()).unwrap_or_default(),
            hectype: t.take("HECTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            aircid: t.take("AIRCID").and_then(|c| c.into_int()).unwrap_or_default(),
            nphe: t.take("NPHE").and_then(|c| c.into_int()).unwrap_or_default(),
            npair: t.take("NPAIR").and_then(|c| c.into_int()).unwrap_or_default(),
            iunit: t.take("IUNIT").and_then(|c| c.into_int()).unwrap_or_default(),
            ihetype: t.take("IHETYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            densit: t.take("DENSIT").and_then(|c| c.into_float()).unwrap_or_default(),
            energy: t.take("ENERGY").and_then(|c| c.into_float()).unwrap_or_default(),
            gamma: t.take("GAMMA").and_then(|c| c.into_float()).unwrap_or_default(),
            covol: t.take("COVOL").and_then(|c| c.into_float()).unwrap_or_default(),
            deto_v: t.take("DETO_V").and_then(|c| c.into_float()).unwrap_or_default(),
            detx: t.take("DETX").and_then(|c| c.into_float()).unwrap_or_default(),
            dety: t.take("DETY").and_then(|c| c.into_float()).unwrap_or_default(),
            detz: t.take("DETZ").and_then(|c| c.into_float()).unwrap_or_default(),
            tdet: t.take("TDET").and_then(|c| c.into_float()).unwrap_or_default(),
            btend: t.take("BTEND").and_then(|c| c.into_float()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            bcxo: t.take("BCXO").and_then(|c| c.into_float()).unwrap_or_default(),
            bcx1: t.take("BCX1").and_then(|c| c.into_float()).unwrap_or_default(),
            bcy0: t.take("BCY0").and_then(|c| c.into_float()).unwrap_or_default(),
            bcy1: t.take("BCY1").and_then(|c| c.into_float()).unwrap_or_default(),
            bcz0: t.take("BCZ0").and_then(|c| c.into_float()).unwrap_or_default(),
            bcz1: t.take("BCZ1").and_then(|c| c.into_float()).unwrap_or_default(),
            ibcx0: t.take("IBCX0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcx1: t.take("IBCX1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcy0: t.take("IBCY0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcy1: t.take("IBCY1").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcz0: t.take("IBCZ0").and_then(|c| c.into_int()).unwrap_or_default(),
            ibcz1: t.take("IBCZ1").and_then(|c| c.into_int()).unwrap_or_default(),
            bc_p: t.take("BC_P").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_ADAPTIVE_FAILURE {
    pub pid: Vec<i64>,
    pub t: Vec<f64>,
    pub term: Vec<i64>,
}
impl PART_ADAPTIVE_FAILURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_ADAPTIVE_FAILURE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            term: t.take("TERM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_ANNEAL {
    pub pid: Vec<i64>,
    pub time: Vec<f64>,
}
impl PART_ANNEAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_ANNEAL").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_ANNEAL_SET {
    pub psid: Vec<i64>,
    pub time: Vec<f64>,
}
impl PART_ANNEAL_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_ANNEAL_SET").unwrap());
        Self {
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
            time: t.take("TIME").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_AVERAGED {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
}
impl PART_AVERAGED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_AVERAGED").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub hgid: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub thshel: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
    pub tmid2: Vec<i64>,
}
impl PART_COMPOSITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid2: t.take("TMID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_CONTACT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub hgid: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub thshel: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
    pub tmid2: Vec<i64>,
}
impl PART_COMPOSITE_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_CONTACT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid2: t.take("TMID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_IGA_SHELL {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nloc: Vec<f64>,
    pub f: Vec<i64>,
    pub irl: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
    pub tmid2: Vec<i64>,
}
impl PART_COMPOSITE_IGA_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_IGA_SHELL").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            irl: t.take("IRL").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid2: t.take("TMID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_LONG {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub hgid: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub thshel: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub plyid: Vec<i64>,
    pub shrfac: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
}
impl PART_COMPOSITE_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_LONG").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid: t.take("PLYID").and_then(|c| c.into_int()).unwrap_or_default(),
            shrfac: t.take("SHRFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_LONG_CONTACT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub hgid: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub thshel: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub plyid: Vec<i64>,
    pub shrfac: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
}
impl PART_COMPOSITE_LONG_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_LONG_CONTACT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            thshel: t.take("THSHEL").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid: t.take("PLYID").and_then(|c| c.into_int()).unwrap_or_default(),
            shrfac: t.take("SHRFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_TSHELL {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub hgid: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub tshear: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub thick2: Vec<f64>,
    pub b2: Vec<f64>,
    pub tmid2: Vec<i64>,
}
impl PART_COMPOSITE_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_TSHELL").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            tshear: t.take("TSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            thick2: t.take("THICK2").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid2: t.take("TMID2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_COMPOSITE_TSHELL_LONG {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub unused: Vec<f64>,
    pub unused_2: Vec<f64>,
    pub hgid: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub tshear: Vec<i64>,
    pub mid1: Vec<i64>,
    pub thick1: Vec<f64>,
    pub b1: Vec<f64>,
    pub tmid1: Vec<i64>,
    pub plyid: Vec<i64>,
    pub shrfac: Vec<f64>,
    pub unused_4: Vec<i64>,
    pub unused_2_2: Vec<i64>,
}
impl PART_COMPOSITE_TSHELL_LONG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_COMPOSITE_TSHELL_LONG").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_float()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            tshear: t.take("TSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            thick1: t.take("THICK1").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            tmid1: t.take("TMID1").and_then(|c| c.into_int()).unwrap_or_default(),
            plyid: t.take("PLYID").and_then(|c| c.into_int()).unwrap_or_default(),
            shrfac: t.take("SHRFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            unused_4: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_CONTACT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
}
impl PART_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_CONTACT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_CONTACT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub ansid: Vec<i64>,
}
impl PART_CONTACT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_CONTACT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_CONTACT_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
}
impl PART_CONTACT_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_CONTACT_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_CONTACT_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_CONTACT_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_CONTACT_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_DUPLICATE {
    pub ptype: Vec<String>,
    pub typeid: Vec<i64>,
    pub idpoff: Vec<i64>,
    pub ideoff: Vec<i64>,
    pub idnoff: Vec<i64>,
    pub tranid: Vec<i64>,
    pub boxid: Vec<i64>,
    pub zmin: Vec<f64>,
}
impl PART_DUPLICATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_DUPLICATE").unwrap());
        Self {
            ptype: t.take("PTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            idpoff: t.take("IDPOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ideoff: t.take("IDEOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            idnoff: t.take("IDNOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            tranid: t.take("TRANID").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            zmin: t.take("ZMIN").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_DUPLICATE_NULL_OVERLAY {
    pub ptype: Vec<String>,
    pub typeid: Vec<i64>,
    pub idpoff: Vec<i64>,
    pub ideoff: Vec<i64>,
    pub density: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl PART_DUPLICATE_NULL_OVERLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_DUPLICATE_NULL_OVERLAY").unwrap());
        Self {
            ptype: t.take("PTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            idpoff: t.take("IDPOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            ideoff: t.take("IDEOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            density: t.take("DENSITY").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
}
impl PART_INERTIA {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_INERTIA_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_CONTACT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
}
impl PART_INERTIA_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_CONTACT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_CONTACT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub ansid: Vec<i64>,
}
impl PART_INERTIA_CONTACT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_CONTACT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_CONTACT_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
}
impl PART_INERTIA_CONTACT_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_CONTACT_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_CONTACT_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_INERTIA_CONTACT_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_CONTACT_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub prbf: Vec<i64>,
}
impl PART_INERTIA_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_INERTIA_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub xc: Vec<f64>,
    pub yc: Vec<f64>,
    pub zc: Vec<f64>,
    pub tm: Vec<f64>,
    pub ircs: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub ixx: Vec<f64>,
    pub ixy: Vec<f64>,
    pub ixz: Vec<f64>,
    pub iyy: Vec<f64>,
    pub iyz: Vec<f64>,
    pub izz: Vec<f64>,
    pub vtx: Vec<f64>,
    pub vty: Vec<f64>,
    pub vtz: Vec<f64>,
    pub vrx: Vec<f64>,
    pub vry: Vec<f64>,
    pub vrz: Vec<f64>,
    pub xl: Vec<f64>,
    pub yl: Vec<f64>,
    pub zl: Vec<f64>,
    pub xlip: Vec<f64>,
    pub ylip: Vec<f64>,
    pub zlip: Vec<f64>,
    pub cid: Vec<i64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_INERTIA_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_INERTIA_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            xc: t.take("XC").and_then(|c| c.into_float()).unwrap_or_default(),
            yc: t.take("YC").and_then(|c| c.into_float()).unwrap_or_default(),
            zc: t.take("ZC").and_then(|c| c.into_float()).unwrap_or_default(),
            tm: t.take("TM").and_then(|c| c.into_float()).unwrap_or_default(),
            ircs: t.take("IRCS").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            ixx: t.take("IXX").and_then(|c| c.into_float()).unwrap_or_default(),
            ixy: t.take("IXY").and_then(|c| c.into_float()).unwrap_or_default(),
            ixz: t.take("IXZ").and_then(|c| c.into_float()).unwrap_or_default(),
            iyy: t.take("IYY").and_then(|c| c.into_float()).unwrap_or_default(),
            iyz: t.take("IYZ").and_then(|c| c.into_float()).unwrap_or_default(),
            izz: t.take("IZZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vtx: t.take("VTX").and_then(|c| c.into_float()).unwrap_or_default(),
            vty: t.take("VTY").and_then(|c| c.into_float()).unwrap_or_default(),
            vtz: t.take("VTZ").and_then(|c| c.into_float()).unwrap_or_default(),
            vrx: t.take("VRX").and_then(|c| c.into_float()).unwrap_or_default(),
            vry: t.take("VRY").and_then(|c| c.into_float()).unwrap_or_default(),
            vrz: t.take("VRZ").and_then(|c| c.into_float()).unwrap_or_default(),
            xl: t.take("XL").and_then(|c| c.into_float()).unwrap_or_default(),
            yl: t.take("YL").and_then(|c| c.into_float()).unwrap_or_default(),
            zl: t.take("ZL").and_then(|c| c.into_float()).unwrap_or_default(),
            xlip: t.take("XLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            ylip: t.take("YLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            zlip: t.take("ZLIP").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_MODES {
    pub pid: Vec<i64>,
    pub nmfb: Vec<i64>,
    pub form: Vec<i64>,
    pub ansid: Vec<i64>,
    pub format: Vec<i64>,
    pub kmflag: Vec<i64>,
    pub nupdf: Vec<i64>,
    pub sigrec: Vec<i64>,
    pub filename: Vec<String>,
    pub mode1: Vec<i64>,
    pub mode2: Vec<i64>,
    pub mode3: Vec<i64>,
    pub mode4: Vec<i64>,
    pub mode5: Vec<i64>,
    pub mode6: Vec<i64>,
    pub mode7: Vec<i64>,
    pub mode8: Vec<i64>,
    pub mstart: Vec<i64>,
    pub mstop: Vec<i64>,
    pub dampf: Vec<f64>,
}
impl PART_MODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_MODES").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            nmfb: t.take("NMFB").and_then(|c| c.into_int()).unwrap_or_default(),
            form: t.take("FORM").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
            format: t.take("FORMAT").and_then(|c| c.into_int()).unwrap_or_default(),
            kmflag: t.take("KMFLAG").and_then(|c| c.into_int()).unwrap_or_default(),
            nupdf: t.take("NUPDF").and_then(|c| c.into_int()).unwrap_or_default(),
            sigrec: t.take("SIGREC").and_then(|c| c.into_int()).unwrap_or_default(),
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            mode1: t.take("MODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            mode2: t.take("MODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            mode3: t.take("MODE3").and_then(|c| c.into_int()).unwrap_or_default(),
            mode4: t.take("MODE4").and_then(|c| c.into_int()).unwrap_or_default(),
            mode5: t.take("MODE5").and_then(|c| c.into_int()).unwrap_or_default(),
            mode6: t.take("MODE6").and_then(|c| c.into_int()).unwrap_or_default(),
            mode7: t.take("MODE7").and_then(|c| c.into_int()).unwrap_or_default(),
            mode8: t.take("MODE8").and_then(|c| c.into_int()).unwrap_or_default(),
            mstart: t.take("MSTART").and_then(|c| c.into_int()).unwrap_or_default(),
            mstop: t.take("MSTOP").and_then(|c| c.into_int()).unwrap_or_default(),
            dampf: t.take("DAMPF").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_MOVE {
    pub pid: Vec<i64>,
    pub xmov: Vec<f64>,
    pub ymov: Vec<f64>,
    pub zmov: Vec<f64>,
    pub cid: Vec<i64>,
    pub ifset: Vec<i64>,
}
impl PART_MOVE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_MOVE").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            xmov: t.take("XMOV").and_then(|c| c.into_float()).unwrap_or_default(),
            ymov: t.take("YMOV").and_then(|c| c.into_float()).unwrap_or_default(),
            zmov: t.take("ZMOV").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ifset: t.take("IFSET").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_PART {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
}
impl PART_PART {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_PART").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub prbf: Vec<i64>,
}
impl PART_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
}
impl PART_REPOSITION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_REPOSITION_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_CONTACT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
}
impl PART_REPOSITION_CONTACT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_CONTACT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_CONTACT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub ansid: Vec<i64>,
}
impl PART_REPOSITION_CONTACT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_CONTACT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_CONTACT_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
}
impl PART_REPOSITION_CONTACT_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_CONTACT_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_CONTACT_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub fs: Vec<f64>,
    pub fd: Vec<f64>,
    pub dc: Vec<f64>,
    pub vc: Vec<f64>,
    pub optt: Vec<f64>,
    pub sft: Vec<f64>,
    pub ssf: Vec<f64>,
    pub cparm8: Vec<f64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_REPOSITION_CONTACT_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_CONTACT_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            dc: t.take("DC").and_then(|c| c.into_float()).unwrap_or_default(),
            vc: t.take("VC").and_then(|c| c.into_float()).unwrap_or_default(),
            optt: t.take("OPTT").and_then(|c| c.into_float()).unwrap_or_default(),
            sft: t.take("SFT").and_then(|c| c.into_float()).unwrap_or_default(),
            ssf: t.take("SSF").and_then(|c| c.into_float()).unwrap_or_default(),
            cparm8: t.take("CPARM8").and_then(|c| c.into_float()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_PRINT {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub prbf: Vec<i64>,
}
impl PART_REPOSITION_PRINT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_PRINT").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_REPOSITION_PRINT_ATTACHMENT_NODES {
    pub title: Vec<String>,
    pub pid: Vec<i64>,
    pub secid: Vec<i64>,
    pub mid: Vec<i64>,
    pub eosid: Vec<i64>,
    pub hgid: Vec<i64>,
    pub grav: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub tmid: Vec<i64>,
    pub cmsn: Vec<i64>,
    pub mdep: Vec<i64>,
    pub movopt: Vec<i64>,
    pub prbf: Vec<i64>,
    pub ansid: Vec<i64>,
}
impl PART_REPOSITION_PRINT_ATTACHMENT_NODES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_REPOSITION_PRINT_ATTACHMENT_NODES").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid: t.take("MID").and_then(|c| c.into_int()).unwrap_or_default(),
            eosid: t.take("EOSID").and_then(|c| c.into_int()).unwrap_or_default(),
            hgid: t.take("HGID").and_then(|c| c.into_int()).unwrap_or_default(),
            grav: t.take("GRAV").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            tmid: t.take("TMID").and_then(|c| c.into_int()).unwrap_or_default(),
            cmsn: t.take("CMSN").and_then(|c| c.into_int()).unwrap_or_default(),
            mdep: t.take("MDEP").and_then(|c| c.into_int()).unwrap_or_default(),
            movopt: t.take("MOVOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            prbf: t.take("PRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ansid: t.take("ANSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_SENSOR {
    pub pid: Vec<i64>,
    pub sida: Vec<i64>,
    pub active: Vec<i64>,
}
impl PART_SENSOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_SENSOR").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            sida: t.take("SIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            active: t.take("ACTIVE").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PART_STACKED_ELEMENTS {
    pub title: Vec<String>,
    pub pidref: Vec<i64>,
    pub numlay: Vec<i64>,
    pub adpopt: Vec<i64>,
    pub inplcmp: Vec<i64>,
    pub pidi: Vec<i64>,
    pub sidi: Vec<i64>,
    pub midi: Vec<i64>,
    pub hgidi: Vec<i64>,
    pub tmidi: Vec<i64>,
    pub thki: Vec<f64>,
    pub nsldi: Vec<i64>,
}
impl PART_STACKED_ELEMENTS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PART_STACKED_ELEMENTS").unwrap());
        Self {
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            pidref: t.take("PIDREF").and_then(|c| c.into_int()).unwrap_or_default(),
            numlay: t.take("NUMLAY").and_then(|c| c.into_int()).unwrap_or_default(),
            adpopt: t.take("ADPOPT").and_then(|c| c.into_int()).unwrap_or_default(),
            inplcmp: t.take("INPLCMP").and_then(|c| c.into_int()).unwrap_or_default(),
            pidi: t.take("PIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            sidi: t.take("SIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            midi: t.take("MIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            hgidi: t.take("HGIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            tmidi: t.take("TMIDi").and_then(|c| c.into_int()).unwrap_or_default(),
            thki: t.take("THKi").and_then(|c| c.into_float()).unwrap_or_default(),
            nsldi: t.take("NSLDi").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct PERTURBATION_MAT {
    pub type_: Vec<i64>,
    pub pid: Vec<i64>,
    pub scl: Vec<f64>,
    pub cmp: Vec<i64>,
    pub icoord: Vec<i64>,
    pub cid: Vec<i64>,
    pub ampl: Vec<f64>,
    pub xwl: Vec<f64>,
    pub xoff: Vec<f64>,
    pub ywl: Vec<f64>,
    pub yoff: Vec<f64>,
    pub zwl: Vec<f64>,
    pub zoff: Vec<f64>,
    pub fade: Vec<f64>,
    pub fname: Vec<String>,
    pub cstype: Vec<i64>,
    pub ellip1: Vec<f64>,
    pub ellip2: Vec<f64>,
    pub rnd: Vec<i64>,
    pub cftype: Vec<i64>,
    pub cfc1: Vec<f64>,
    pub cfc2: Vec<f64>,
    pub cfc3: Vec<f64>,
    pub ampl_2: Vec<i64>,
    pub dtype: Vec<f64>,
}
impl PERTURBATION_MAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PERTURBATION_MAT").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            scl: t.take("SCL").and_then(|c| c.into_float()).unwrap_or_default(),
            cmp: t.take("CMP").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            xwl: t.take("XWL").and_then(|c| c.into_float()).unwrap_or_default(),
            xoff: t.take("XOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            ywl: t.take("YWL").and_then(|c| c.into_float()).unwrap_or_default(),
            yoff: t.take("YOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            zwl: t.take("ZWL").and_then(|c| c.into_float()).unwrap_or_default(),
            zoff: t.take("ZOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            fade: t.take("FADE").and_then(|c| c.into_float()).unwrap_or_default(),
            fname: t.take("FNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            cstype: t.take("CSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ellip1: t.take("ELLIP1").and_then(|c| c.into_float()).unwrap_or_default(),
            ellip2: t.take("ELLIP2").and_then(|c| c.into_float()).unwrap_or_default(),
            rnd: t.take("RND").and_then(|c| c.into_int()).unwrap_or_default(),
            cftype: t.take("CFTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cfc1: t.take("CFC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc2: t.take("CFC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc3: t.take("CFC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ampl_2: t.take("AMPL").and_then(|c| c.into_int()).unwrap_or_default(),
            dtype: t.take("DTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PERTURBATION_NODE {
    pub type_: Vec<i64>,
    pub nsid: Vec<i64>,
    pub scl: Vec<f64>,
    pub cmp: Vec<i64>,
    pub icoord: Vec<i64>,
    pub cid: Vec<i64>,
    pub ampl: Vec<f64>,
    pub xwl: Vec<f64>,
    pub xoff: Vec<f64>,
    pub ywl: Vec<f64>,
    pub yoff: Vec<f64>,
    pub zwl: Vec<f64>,
    pub zoff: Vec<f64>,
    pub fade: Vec<f64>,
    pub fname: Vec<String>,
    pub cstype: Vec<i64>,
    pub ellip1: Vec<f64>,
    pub ellip2: Vec<f64>,
    pub rnd: Vec<i64>,
    pub cftype: Vec<i64>,
    pub cfc1: Vec<f64>,
    pub cfc2: Vec<f64>,
    pub cfc3: Vec<f64>,
    pub ampl_2: Vec<i64>,
    pub dtype: Vec<f64>,
}
impl PERTURBATION_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PERTURBATION_NODE").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            scl: t.take("SCL").and_then(|c| c.into_float()).unwrap_or_default(),
            cmp: t.take("CMP").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            xwl: t.take("XWL").and_then(|c| c.into_float()).unwrap_or_default(),
            xoff: t.take("XOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            ywl: t.take("YWL").and_then(|c| c.into_float()).unwrap_or_default(),
            yoff: t.take("YOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            zwl: t.take("ZWL").and_then(|c| c.into_float()).unwrap_or_default(),
            zoff: t.take("ZOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            fade: t.take("FADE").and_then(|c| c.into_float()).unwrap_or_default(),
            fname: t.take("FNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            cstype: t.take("CSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ellip1: t.take("ELLIP1").and_then(|c| c.into_float()).unwrap_or_default(),
            ellip2: t.take("ELLIP2").and_then(|c| c.into_float()).unwrap_or_default(),
            rnd: t.take("RND").and_then(|c| c.into_int()).unwrap_or_default(),
            cftype: t.take("CFTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cfc1: t.take("CFC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc2: t.take("CFC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc3: t.take("CFC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ampl_2: t.take("AMPL").and_then(|c| c.into_int()).unwrap_or_default(),
            dtype: t.take("DTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct PERTURBATION_SHELL_THICKNESS {
    pub type_: Vec<i64>,
    pub eid: Vec<i64>,
    pub scl: Vec<f64>,
    pub cmp: Vec<i64>,
    pub icoord: Vec<i64>,
    pub cid: Vec<i64>,
    pub ampl: Vec<f64>,
    pub xwl: Vec<f64>,
    pub xoff: Vec<f64>,
    pub ywl: Vec<f64>,
    pub yoff: Vec<f64>,
    pub zwl: Vec<f64>,
    pub zoff: Vec<f64>,
    pub fade: Vec<f64>,
    pub fname: Vec<String>,
    pub cstype: Vec<i64>,
    pub ellip1: Vec<f64>,
    pub ellip2: Vec<f64>,
    pub rnd: Vec<i64>,
    pub cftype: Vec<i64>,
    pub cfc1: Vec<f64>,
    pub cfc2: Vec<f64>,
    pub cfc3: Vec<f64>,
    pub ampl_2: Vec<i64>,
    pub dtype: Vec<f64>,
}
impl PERTURBATION_SHELL_THICKNESS {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("PERTURBATION_SHELL_THICKNESS").unwrap());
        Self {
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            scl: t.take("SCL").and_then(|c| c.into_float()).unwrap_or_default(),
            cmp: t.take("CMP").and_then(|c| c.into_int()).unwrap_or_default(),
            icoord: t.take("ICOORD").and_then(|c| c.into_int()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ampl: t.take("AMPL").and_then(|c| c.into_float()).unwrap_or_default(),
            xwl: t.take("XWL").and_then(|c| c.into_float()).unwrap_or_default(),
            xoff: t.take("XOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            ywl: t.take("YWL").and_then(|c| c.into_float()).unwrap_or_default(),
            yoff: t.take("YOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            zwl: t.take("ZWL").and_then(|c| c.into_float()).unwrap_or_default(),
            zoff: t.take("ZOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            fade: t.take("FADE").and_then(|c| c.into_float()).unwrap_or_default(),
            fname: t.take("FNAME").and_then(|c| c.into_str()).unwrap_or_default(),
            cstype: t.take("CSTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            ellip1: t.take("ELLIP1").and_then(|c| c.into_float()).unwrap_or_default(),
            ellip2: t.take("ELLIP2").and_then(|c| c.into_float()).unwrap_or_default(),
            rnd: t.take("RND").and_then(|c| c.into_int()).unwrap_or_default(),
            cftype: t.take("CFTYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            cfc1: t.take("CFC1").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc2: t.take("CFC2").and_then(|c| c.into_float()).unwrap_or_default(),
            cfc3: t.take("CFC3").and_then(|c| c.into_float()).unwrap_or_default(),
            ampl_2: t.take("AMPL").and_then(|c| c.into_int()).unwrap_or_default(),
            dtype: t.take("DTYPE").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RAIL_TRACK {
    pub id: Vec<i64>,
    pub bsetid1: Vec<i64>,
    pub norgn1: Vec<i64>,
    pub lcur1: Vec<i64>,
    pub oset1: Vec<f64>,
    pub sf1: Vec<f64>,
    pub ga1: Vec<f64>,
    pub idir: Vec<i64>,
    pub unused: Vec<i64>,
    pub bsetid2: Vec<i64>,
    pub norgn2: Vec<i64>,
    pub lcur2: Vec<i64>,
    pub oset2: Vec<f64>,
    pub sf2: Vec<f64>,
    pub ga2: Vec<f64>,
}
impl RAIL_TRACK {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RAIL_TRACK").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsetid1: t.take("BSETID1").and_then(|c| c.into_int()).unwrap_or_default(),
            norgn1: t.take("NORGN1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur1: t.take("LCUR1").and_then(|c| c.into_int()).unwrap_or_default(),
            oset1: t.take("OSET1").and_then(|c| c.into_float()).unwrap_or_default(),
            sf1: t.take("SF1").and_then(|c| c.into_float()).unwrap_or_default(),
            ga1: t.take("GA1").and_then(|c| c.into_float()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            bsetid2: t.take("BSETID2").and_then(|c| c.into_int()).unwrap_or_default(),
            norgn2: t.take("NORGN2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur2: t.take("LCUR2").and_then(|c| c.into_int()).unwrap_or_default(),
            oset2: t.take("OSET2").and_then(|c| c.into_float()).unwrap_or_default(),
            sf2: t.take("SF2").and_then(|c| c.into_float()).unwrap_or_default(),
            ga2: t.take("GA2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RAIL_TRAIN {
    pub id: Vec<i64>,
    pub nsetid: Vec<i64>,
    pub omit: Vec<f64>,
    pub finit: Vec<f64>,
    pub omit__2: Vec<f64>,
    pub trid: Vec<i64>,
    pub lcur: Vec<i64>,
    pub offs: Vec<f64>,
    pub vertstf: Vec<f64>,
    pub latstf: Vec<f64>,
    pub v2: Vec<f64>,
    pub v3: Vec<f64>,
    pub l2: Vec<f64>,
    pub l3: Vec<f64>,
    pub latdir: Vec<f64>,
    pub fric: Vec<f64>,
}
impl RAIL_TRAIN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RAIL_TRAIN").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsetid: t.take("NSETID").and_then(|c| c.into_int()).unwrap_or_default(),
            omit: t.take("(omit)").and_then(|c| c.into_float()).unwrap_or_default(),
            finit: t.take("FINIT").and_then(|c| c.into_float()).unwrap_or_default(),
            omit__2: t.take("(omit)_2").and_then(|c| c.into_float()).unwrap_or_default(),
            trid: t.take("TRID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcur: t.take("LCUR").and_then(|c| c.into_int()).unwrap_or_default(),
            offs: t.take("OFFS").and_then(|c| c.into_float()).unwrap_or_default(),
            vertstf: t.take("VERTSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            latstf: t.take("LATSTF").and_then(|c| c.into_float()).unwrap_or_default(),
            v2: t.take("V2").and_then(|c| c.into_float()).unwrap_or_default(),
            v3: t.take("V3").and_then(|c| c.into_float()).unwrap_or_default(),
            l2: t.take("L2").and_then(|c| c.into_float()).unwrap_or_default(),
            l3: t.take("L3").and_then(|c| c.into_float()).unwrap_or_default(),
            latdir: t.take("LATDIR").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_FORCE_TRANSDUCER {
    pub tid: Vec<i64>,
    pub rwid: Vec<i64>,
    pub heading: Vec<String>,
    pub nsid: Vec<i64>,
}
impl RIGIDWALL_FORCE_TRANSDUCER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_FORCE_TRANSDUCER").unwrap());
        Self {
            tid: t.take("TID").and_then(|c| c.into_int()).unwrap_or_default(),
            rwid: t.take("RWID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_DISPLAY_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_INTERIOR_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DEFORM_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DEFORM_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub xp: Vec<f64>,
    pub yp: Vec<f64>,
    pub zp: Vec<f64>,
    pub nl: Vec<i64>,
    pub narc: Vec<i64>,
    pub nr: Vec<i64>,
    pub lcidr: Vec<i64>,
    pub lcida: Vec<i64>,
    pub lcidb: Vec<i64>,
    pub lcidg: Vec<i64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DEFORM_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DEFORM_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            xp: t.take("XP").and_then(|c| c.into_float()).unwrap_or_default(),
            yp: t.take("YP").and_then(|c| c.into_float()).unwrap_or_default(),
            zp: t.take("ZP").and_then(|c| c.into_float()).unwrap_or_default(),
            nl: t.take("NL").and_then(|c| c.into_int()).unwrap_or_default(),
            narc: t.take("NARC").and_then(|c| c.into_int()).unwrap_or_default(),
            nr: t.take("NR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidr: t.take("LCIDR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcida: t.take("LCIDA").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidb: t.take("LCIDB").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidg: t.take("LCIDG").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_INTERIOR_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_CYLINDER_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radcyl: Vec<f64>,
    pub lencyl: Vec<f64>,
    pub nsegs: Vec<i64>,
    pub vl: Vec<f64>,
    pub height: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_CYLINDER_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_CYLINDER_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radcyl: t.take("RADCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            lencyl: t.take("LENCYL").and_then(|c| c.into_float()).unwrap_or_default(),
            nsegs: t.take("NSEGS").and_then(|c| c.into_int()).unwrap_or_default(),
            vl: t.take("VL").and_then(|c| c.into_float()).unwrap_or_default(),
            height: t.take("HEIGHT").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_FLAT {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_FLAT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_FLAT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_FLAT_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_FLAT_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_FLAT_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_FLAT_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_FLAT_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_FLAT_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_FLAT_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_FLAT_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_FLAT_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_PRISM {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lenp: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_PRISM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_PRISM").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lenp: t.take("LENP").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_PRISM_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lenp: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_PRISM_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_PRISM_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lenp: t.take("LENP").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_PRISM_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lenp: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_PRISM_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_PRISM_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lenp: t.take("LENP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_PRISM_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub lenp: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_PRISM_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_PRISM_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            lenp: t.take("LENP").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_SPHERE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radsph: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_SPHERE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_SPHERE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radsph: t.take("RADSPH").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_SPHERE_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radsph: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_SPHERE_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_SPHERE_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radsph: t.take("RADSPH").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_SPHERE_MOTION {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radsph: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_SPHERE_MOTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_SPHERE_MOTION").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radsph: t.take("RADSPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_GEOMETRIC_SPHERE_MOTION_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub radsph: Vec<f64>,
    pub lcid: Vec<i64>,
    pub opt: Vec<i64>,
    pub vx: Vec<f64>,
    pub vy: Vec<f64>,
    pub vz: Vec<f64>,
    pub pid: Vec<i64>,
    pub ro: Vec<f64>,
    pub e: Vec<f64>,
    pub pr: Vec<f64>,
}
impl RIGIDWALL_GEOMETRIC_SPHERE_MOTION_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_GEOMETRIC_SPHERE_MOTION_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            radsph: t.take("RADSPH").and_then(|c| c.into_float()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            opt: t.take("OPT").and_then(|c| c.into_int()).unwrap_or_default(),
            vx: t.take("VX").and_then(|c| c.into_float()).unwrap_or_default(),
            vy: t.take("VY").and_then(|c| c.into_float()).unwrap_or_default(),
            vz: t.take("VZ").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            ro: t.take("RO").and_then(|c| c.into_float()).unwrap_or_default(),
            e: t.take("E").and_then(|c| c.into_float()).unwrap_or_default(),
            pr: t.take("PR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
}
impl RIGIDWALL_PLANAR {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
}
impl RIGIDWALL_PLANAR_FINITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
}
impl RIGIDWALL_PLANAR_FINITE_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_FINITE_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_MOVING {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
}
impl RIGIDWALL_PLANAR_FINITE_MOVING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_MOVING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_MOVING_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
}
impl RIGIDWALL_PLANAR_FINITE_MOVING_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_MOVING_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_MOVING_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_FINITE_MOVING_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_MOVING_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FINITE_MOVING_FORCES_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_FINITE_MOVING_FORCES_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FINITE_MOVING_FORCES_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_MOVING {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
}
impl RIGIDWALL_PLANAR_MOVING {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_MOVING").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_MOVING_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
}
impl RIGIDWALL_PLANAR_MOVING_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_MOVING_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_MOVING_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_MOVING_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_MOVING_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_MOVING_FORCES_DISPLAY {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub mass: Vec<f64>,
    pub v0: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_MOVING_FORCES_DISPLAY {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_MOVING_FORCES_DISPLAY").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            mass: t.take("MASS").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_ORTHO {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub sfrica: Vec<f64>,
    pub sfricb: Vec<f64>,
    pub dfrica: Vec<f64>,
    pub dfricb: Vec<f64>,
    pub decaya: Vec<f64>,
    pub decayb: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
}
impl RIGIDWALL_PLANAR_ORTHO {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_ORTHO").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfrica: t.take("SFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfricb: t.take("SFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            dfrica: t.take("DFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            dfricb: t.take("DFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            decaya: t.take("DECAYA").and_then(|c| c.into_float()).unwrap_or_default(),
            decayb: t.take("DECAYB").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_ORTHO_FINITE {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub sfrica: Vec<f64>,
    pub sfricb: Vec<f64>,
    pub dfrica: Vec<f64>,
    pub dfricb: Vec<f64>,
    pub decaya: Vec<f64>,
    pub decayb: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
}
impl RIGIDWALL_PLANAR_ORTHO_FINITE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_ORTHO_FINITE").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfrica: t.take("SFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfricb: t.take("SFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            dfrica: t.take("DFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            dfricb: t.take("DFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            decaya: t.take("DECAYA").and_then(|c| c.into_float()).unwrap_or_default(),
            decayb: t.take("DECAYB").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_ORTHO_FINITE_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub sfrica: Vec<f64>,
    pub sfricb: Vec<f64>,
    pub dfrica: Vec<f64>,
    pub dfricb: Vec<f64>,
    pub decaya: Vec<f64>,
    pub decayb: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub xhev: Vec<f64>,
    pub yhev: Vec<f64>,
    pub zhev: Vec<f64>,
    pub lenl: Vec<f64>,
    pub lenm: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_ORTHO_FINITE_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_ORTHO_FINITE_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfrica: t.take("SFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfricb: t.take("SFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            dfrica: t.take("DFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            dfricb: t.take("DFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            decaya: t.take("DECAYA").and_then(|c| c.into_float()).unwrap_or_default(),
            decayb: t.take("DECAYB").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            xhev: t.take("XHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            yhev: t.take("YHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            zhev: t.take("ZHEV").and_then(|c| c.into_float()).unwrap_or_default(),
            lenl: t.take("LENL").and_then(|c| c.into_float()).unwrap_or_default(),
            lenm: t.take("LENM").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGIDWALL_PLANAR_ORTHO_FORCES {
    pub id: Vec<i64>,
    pub title: Vec<String>,
    pub nsid: Vec<i64>,
    pub nsidex: Vec<i64>,
    pub boxid: Vec<i64>,
    pub offset: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub rwksf: Vec<f64>,
    pub xt: Vec<f64>,
    pub yt: Vec<f64>,
    pub zt: Vec<f64>,
    pub xh: Vec<f64>,
    pub yh: Vec<f64>,
    pub zh: Vec<f64>,
    pub fric: Vec<f64>,
    pub wvel: Vec<f64>,
    pub sfrica: Vec<f64>,
    pub sfricb: Vec<f64>,
    pub dfrica: Vec<f64>,
    pub dfricb: Vec<f64>,
    pub decaya: Vec<f64>,
    pub decayb: Vec<f64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub d1: Vec<f64>,
    pub d2: Vec<f64>,
    pub d3: Vec<f64>,
    pub soft: Vec<i64>,
    pub ssid: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
}
impl RIGIDWALL_PLANAR_ORTHO_FORCES {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGIDWALL_PLANAR_ORTHO_FORCES").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            title: t.take("TITLE").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            nsidex: t.take("NSIDEX").and_then(|c| c.into_int()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            rwksf: t.take("RWKSF").and_then(|c| c.into_float()).unwrap_or_default(),
            xt: t.take("XT").and_then(|c| c.into_float()).unwrap_or_default(),
            yt: t.take("YT").and_then(|c| c.into_float()).unwrap_or_default(),
            zt: t.take("ZT").and_then(|c| c.into_float()).unwrap_or_default(),
            xh: t.take("XH").and_then(|c| c.into_float()).unwrap_or_default(),
            yh: t.take("YH").and_then(|c| c.into_float()).unwrap_or_default(),
            zh: t.take("ZH").and_then(|c| c.into_float()).unwrap_or_default(),
            fric: t.take("FRIC").and_then(|c| c.into_float()).unwrap_or_default(),
            wvel: t.take("WVEL").and_then(|c| c.into_float()).unwrap_or_default(),
            sfrica: t.take("SFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            sfricb: t.take("SFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            dfrica: t.take("DFRICA").and_then(|c| c.into_float()).unwrap_or_default(),
            dfricb: t.take("DFRICB").and_then(|c| c.into_float()).unwrap_or_default(),
            decaya: t.take("DECAYA").and_then(|c| c.into_float()).unwrap_or_default(),
            decayb: t.take("DECAYB").and_then(|c| c.into_float()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            d1: t.take("D1").and_then(|c| c.into_float()).unwrap_or_default(),
            d2: t.take("D2").and_then(|c| c.into_float()).unwrap_or_default(),
            d3: t.take("D3").and_then(|c| c.into_float()).unwrap_or_default(),
            soft: t.take("SOFT").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGID_DEFORMABLE_CONTROL {
    pub nrbf: Vec<i64>,
    pub ncsf: Vec<i64>,
    pub rwf: Vec<i64>,
    pub dtmax: Vec<f64>,
}
impl RIGID_DEFORMABLE_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGID_DEFORMABLE_CONTROL").unwrap());
        Self {
            nrbf: t.take("NRBF").and_then(|c| c.into_int()).unwrap_or_default(),
            ncsf: t.take("NCSF").and_then(|c| c.into_int()).unwrap_or_default(),
            rwf: t.take("RWF").and_then(|c| c.into_int()).unwrap_or_default(),
            dtmax: t.take("DTMAX").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct RIGID_DEFORMABLE_D2R {
    pub pid: Vec<i64>,
    pub lrb: Vec<i64>,
}
impl RIGID_DEFORMABLE_D2R {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGID_DEFORMABLE_D2R").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            lrb: t.take("LRB").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RIGID_DEFORMABLE_R2D {
    pub pid: Vec<i64>,
}
impl RIGID_DEFORMABLE_R2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RIGID_DEFORMABLE_R2D").unwrap());
        Self {
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct RVE_ANALYSIS_FEM {
    pub filename: Vec<String>,
    pub inpt: Vec<i64>,
    pub oupt: Vec<i64>,
    pub lcid: Vec<i64>,
    pub idof: Vec<i64>,
    pub bc: Vec<i64>,
    pub imatch: Vec<i64>,
    pub h11: Vec<f64>,
    pub h22: Vec<f64>,
    pub h33: Vec<f64>,
    pub h12: Vec<f64>,
    pub h23: Vec<f64>,
    pub h13: Vec<f64>,
}
impl RVE_ANALYSIS_FEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("RVE_ANALYSIS_FEM").unwrap());
        Self {
            filename: t.take("FILENAME").and_then(|c| c.into_str()).unwrap_or_default(),
            inpt: t.take("INPT").and_then(|c| c.into_int()).unwrap_or_default(),
            oupt: t.take("OUPT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcid: t.take("LCID").and_then(|c| c.into_int()).unwrap_or_default(),
            idof: t.take("IDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            bc: t.take("BC").and_then(|c| c.into_int()).unwrap_or_default(),
            imatch: t.take("IMATCH").and_then(|c| c.into_int()).unwrap_or_default(),
            h11: t.take("H11").and_then(|c| c.into_float()).unwrap_or_default(),
            h22: t.take("H22").and_then(|c| c.into_float()).unwrap_or_default(),
            h33: t.take("H33").and_then(|c| c.into_float()).unwrap_or_default(),
            h12: t.take("H12").and_then(|c| c.into_float()).unwrap_or_default(),
            h23: t.take("H23").and_then(|c| c.into_float()).unwrap_or_default(),
            h13: t.take("H13").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_ALE1D {
    pub secid: Vec<i64>,
    pub aleform: Vec<i64>,
    pub aet: Vec<i64>,
    pub elform: Vec<i64>,
    pub thick: Vec<f64>,
    pub thick_2: Vec<f64>,
}
impl SECTION_ALE1D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_ALE1D").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            aleform: t.take("ALEFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
            thick_2: t.take("THICK_2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_ALE2D {
    pub secid: Vec<i64>,
    pub aleform: Vec<i64>,
    pub aet: Vec<i64>,
    pub elform: Vec<i64>,
}
impl SECTION_ALE2D {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_ALE2D").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            aleform: t.take("ALEFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_BEAM {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub cst: Vec<i64>,
    pub scoor: Vec<f64>,
    pub nsm: Vec<f64>,
    pub naupd: Vec<i64>,
    pub ts1: Vec<f64>,
    pub ts2: Vec<f64>,
    pub tt1: Vec<f64>,
    pub tt2: Vec<f64>,
    pub nsloc: Vec<f64>,
    pub ntloc: Vec<f64>,
    pub a: Vec<f64>,
    pub iss: Vec<f64>,
    pub itt: Vec<f64>,
    pub j: Vec<f64>,
    pub sa: Vec<f64>,
    pub ist: Vec<f64>,
    pub a_2: Vec<f64>,
    pub rampt: Vec<f64>,
    pub stress: Vec<f64>,
    pub ts1_2: Vec<f64>,
    pub ts2_2: Vec<f64>,
    pub tt1_2: Vec<f64>,
    pub tt2_2: Vec<f64>,
    pub vol: Vec<f64>,
    pub iner: Vec<f64>,
    pub cid: Vec<i64>,
    pub ca: Vec<f64>,
    pub offset: Vec<f64>,
    pub rrcon: Vec<f64>,
    pub srcon: Vec<f64>,
    pub trcon: Vec<f64>,
    pub vol_2: Vec<f64>,
    pub iner_2: Vec<f64>,
    pub cid_2: Vec<i64>,
    pub dofn1: Vec<f64>,
    pub dofn2: Vec<f64>,
    pub ts1_3: Vec<f64>,
    pub ts2_3: Vec<f64>,
    pub tt1_3: Vec<f64>,
    pub tt2_3: Vec<f64>,
    pub ts1_4: Vec<f64>,
    pub ts2_4: Vec<f64>,
    pub tt1_4: Vec<f64>,
    pub tt2_4: Vec<f64>,
    pub print: Vec<f64>,
    pub f: Vec<i64>,
    pub itoff: Vec<f64>,
    pub a_3: Vec<f64>,
    pub iss_2: Vec<f64>,
    pub itt_2: Vec<f64>,
    pub j_2: Vec<f64>,
    pub sa_2: Vec<f64>,
    pub ist_2: Vec<f64>,
}
impl SECTION_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_BEAM").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            cst: t.take("CST").and_then(|c| c.into_int()).unwrap_or_default(),
            scoor: t.take("SCOOR").and_then(|c| c.into_float()).unwrap_or_default(),
            nsm: t.take("NSM").and_then(|c| c.into_float()).unwrap_or_default(),
            naupd: t.take("NAUPD").and_then(|c| c.into_int()).unwrap_or_default(),
            ts1: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            tt1: t.take("TT1").and_then(|c| c.into_float()).unwrap_or_default(),
            tt2: t.take("TT2").and_then(|c| c.into_float()).unwrap_or_default(),
            nsloc: t.take("NSLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ntloc: t.take("NTLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            a: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            iss: t.take("ISS").and_then(|c| c.into_float()).unwrap_or_default(),
            itt: t.take("ITT").and_then(|c| c.into_float()).unwrap_or_default(),
            j: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            sa: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            ist: t.take("IST").and_then(|c| c.into_float()).unwrap_or_default(),
            a_2: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            rampt: t.take("RAMPT").and_then(|c| c.into_float()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1_2: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2_2: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            tt1_2: t.take("TT1").and_then(|c| c.into_float()).unwrap_or_default(),
            tt2_2: t.take("TT2").and_then(|c| c.into_float()).unwrap_or_default(),
            vol: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            ca: t.take("CA").and_then(|c| c.into_float()).unwrap_or_default(),
            offset: t.take("OFFSET").and_then(|c| c.into_float()).unwrap_or_default(),
            rrcon: t.take("RRCON").and_then(|c| c.into_float()).unwrap_or_default(),
            srcon: t.take("SRCON").and_then(|c| c.into_float()).unwrap_or_default(),
            trcon: t.take("TRCON").and_then(|c| c.into_float()).unwrap_or_default(),
            vol_2: t.take("VOL").and_then(|c| c.into_float()).unwrap_or_default(),
            iner_2: t.take("INER").and_then(|c| c.into_float()).unwrap_or_default(),
            cid_2: t.take("CID").and_then(|c| c.into_int()).unwrap_or_default(),
            dofn1: t.take("DOFN1").and_then(|c| c.into_float()).unwrap_or_default(),
            dofn2: t.take("DOFN2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1_3: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2_3: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            tt1_3: t.take("TT1").and_then(|c| c.into_float()).unwrap_or_default(),
            tt2_3: t.take("TT2").and_then(|c| c.into_float()).unwrap_or_default(),
            ts1_4: t.take("TS1").and_then(|c| c.into_float()).unwrap_or_default(),
            ts2_4: t.take("TS2").and_then(|c| c.into_float()).unwrap_or_default(),
            tt1_4: t.take("TT1").and_then(|c| c.into_float()).unwrap_or_default(),
            tt2_4: t.take("TT2").and_then(|c| c.into_float()).unwrap_or_default(),
            print: t.take("PRINT").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            itoff: t.take("ITOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            a_3: t.take("A").and_then(|c| c.into_float()).unwrap_or_default(),
            iss_2: t.take("ISS").and_then(|c| c.into_float()).unwrap_or_default(),
            itt_2: t.take("ITT").and_then(|c| c.into_float()).unwrap_or_default(),
            j_2: t.take("J").and_then(|c| c.into_float()).unwrap_or_default(),
            sa_2: t.take("SA").and_then(|c| c.into_float()).unwrap_or_default(),
            ist_2: t.take("IST").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_BEAM_AISC {
    pub secid: Vec<i64>,
    pub label: Vec<String>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nsm: Vec<f64>,
    pub lfac: Vec<f64>,
    pub nsloc: Vec<f64>,
    pub ntloc: Vec<f64>,
    pub k: Vec<i64>,
    pub elform_2: Vec<i64>,
    pub shrf_2: Vec<f64>,
    pub nsm_2: Vec<f64>,
    pub lfac_2: Vec<f64>,
    pub elform_3: Vec<i64>,
    pub lfac_3: Vec<f64>,
    pub rampt: Vec<f64>,
    pub stress: Vec<f64>,
    pub elform_4: Vec<i64>,
    pub shrf_3: Vec<f64>,
    pub nsm_3: Vec<f64>,
    pub lfac_4: Vec<f64>,
    pub k_2: Vec<i64>,
}
impl SECTION_BEAM_AISC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_BEAM_AISC").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            label: t.take("LABEL").and_then(|c| c.into_str()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsm: t.take("NSM").and_then(|c| c.into_float()).unwrap_or_default(),
            lfac: t.take("LFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            nsloc: t.take("NSLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            ntloc: t.take("NTLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            k: t.take("K").and_then(|c| c.into_int()).unwrap_or_default(),
            elform_2: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf_2: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsm_2: t.take("NSM").and_then(|c| c.into_float()).unwrap_or_default(),
            lfac_2: t.take("LFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            elform_3: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            lfac_3: t.take("LFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            rampt: t.take("RAMPT").and_then(|c| c.into_float()).unwrap_or_default(),
            stress: t.take("STRESS").and_then(|c| c.into_float()).unwrap_or_default(),
            elform_4: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf_3: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nsm_3: t.take("NSM").and_then(|c| c.into_float()).unwrap_or_default(),
            lfac_4: t.take("LFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            k_2: t.take("K").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_DISCRETE {
    pub secid: Vec<i64>,
    pub dro: Vec<i64>,
    pub kd: Vec<f64>,
    pub v0: Vec<f64>,
    pub cl: Vec<f64>,
    pub fd: Vec<f64>,
    pub cdl: Vec<f64>,
    pub tdl: Vec<f64>,
}
impl SECTION_DISCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_DISCRETE").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            dro: t.take("DRO").and_then(|c| c.into_int()).unwrap_or_default(),
            kd: t.take("KD").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            cdl: t.take("CDL").and_then(|c| c.into_float()).unwrap_or_default(),
            tdl: t.take("TDL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_FPD {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub f: Vec<f64>,
    pub kernel: Vec<i64>,
    pub f_2: Vec<f64>,
    pub f_2_2: Vec<f64>,
    pub f_3: Vec<f64>,
    pub tstart: Vec<f64>,
    pub dt_imp: Vec<f64>,
    pub dtscl: Vec<f64>,
}
impl SECTION_FPD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_FPD").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            kernel: t.take("KERNEL").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            f_2_2: t.take("-_2").and_then(|c| c.into_float()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_float()).unwrap_or_default(),
            tstart: t.take("TSTART").and_then(|c| c.into_float()).unwrap_or_default(),
            dt_imp: t.take("DT_IMP").and_then(|c| c.into_float()).unwrap_or_default(),
            dtscl: t.take("DTSCL").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_IGA_SHELL {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub irl: Vec<i64>,
    pub qr_irid: Vec<f64>,
    pub icomp: Vec<i64>,
    pub t: Vec<f64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub nloc: Vec<f64>,
    pub b1: Vec<f64>,
    pub b2: Vec<f64>,
    pub b3: Vec<f64>,
    pub b4: Vec<f64>,
    pub b5: Vec<f64>,
    pub b6: Vec<f64>,
    pub b7: Vec<f64>,
    pub b8: Vec<f64>,
}
impl SECTION_IGA_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_IGA_SHELL").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            irl: t.take("IRL").and_then(|c| c.into_int()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_float()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            t: t.take("T").and_then(|c| c.into_float()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            b1: t.take("B1").and_then(|c| c.into_float()).unwrap_or_default(),
            b2: t.take("B2").and_then(|c| c.into_float()).unwrap_or_default(),
            b3: t.take("B3").and_then(|c| c.into_float()).unwrap_or_default(),
            b4: t.take("B4").and_then(|c| c.into_float()).unwrap_or_default(),
            b5: t.take("B5").and_then(|c| c.into_float()).unwrap_or_default(),
            b6: t.take("B6").and_then(|c| c.into_float()).unwrap_or_default(),
            b7: t.take("B7").and_then(|c| c.into_float()).unwrap_or_default(),
            b8: t.take("B8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_IGA_SOLID {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub ir: Vec<i64>,
}
impl SECTION_IGA_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_IGA_SOLID").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            ir: t.take("IR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_POINT_SOURCE {
    pub secid: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub lcidvolr: Vec<i64>,
    pub lcidvel: Vec<i64>,
    pub nlc001: Vec<i64>,
    pub nlc002: Vec<i64>,
    pub nlc003: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub vecid: Vec<i64>,
    pub orifarea: Vec<f64>,
}
impl SECTION_POINT_SOURCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_POINT_SOURCE").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidvolr: t.take("LCIDVOLR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidvel: t.take("LCIDVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nlc001: t.take("NLC001").and_then(|c| c.into_int()).unwrap_or_default(),
            nlc002: t.take("NLC002").and_then(|c| c.into_int()).unwrap_or_default(),
            nlc003: t.take("NLC003").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            orifarea: t.take("ORIFAREA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_POINT_SOURCE_MIXTURE {
    pub secid: Vec<i64>,
    pub lcidt: Vec<i64>,
    pub notused: Vec<i64>,
    pub lcidvel: Vec<i64>,
    pub nidlc001: Vec<i64>,
    pub nidlc002: Vec<i64>,
    pub nidlc003: Vec<i64>,
    pub idir: Vec<i64>,
    pub lcmdot1: Vec<i64>,
    pub lcmdot2: Vec<i64>,
    pub lcmdot3: Vec<i64>,
    pub lcmdot4: Vec<i64>,
    pub lcmdot5: Vec<i64>,
    pub lcmdot6: Vec<i64>,
    pub lcmdot7: Vec<i64>,
    pub lcmdot8: Vec<i64>,
    pub nodeid: Vec<i64>,
    pub vecid: Vec<i64>,
    pub orifarea: Vec<f64>,
}
impl SECTION_POINT_SOURCE_MIXTURE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_POINT_SOURCE_MIXTURE").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidt: t.take("LCIDT").and_then(|c| c.into_int()).unwrap_or_default(),
            notused: t.take("NOTUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            lcidvel: t.take("LCIDVEL").and_then(|c| c.into_int()).unwrap_or_default(),
            nidlc001: t.take("NIDLC001").and_then(|c| c.into_int()).unwrap_or_default(),
            nidlc002: t.take("NIDLC002").and_then(|c| c.into_int()).unwrap_or_default(),
            nidlc003: t.take("NIDLC003").and_then(|c| c.into_int()).unwrap_or_default(),
            idir: t.take("IDIR").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot1: t.take("LCMDOT1").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot2: t.take("LCMDOT2").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot3: t.take("LCMDOT3").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot4: t.take("LCMDOT4").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot5: t.take("LCMDOT5").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot6: t.take("LCMDOT6").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot7: t.take("LCMDOT7").and_then(|c| c.into_int()).unwrap_or_default(),
            lcmdot8: t.take("LCMDOT8").and_then(|c| c.into_int()).unwrap_or_default(),
            nodeid: t.take("NODEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vecid: t.take("VECID").and_then(|c| c.into_int()).unwrap_or_default(),
            orifarea: t.take("ORIFAREA").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SEATBELT {
    pub secid: Vec<i64>,
    pub area: Vec<f64>,
    pub thick: Vec<f64>,
}
impl SECTION_SEATBELT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SEATBELT").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            area: t.take("AREA").and_then(|c| c.into_float()).unwrap_or_default(),
            thick: t.take("THICK").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SHELL {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub icomp: Vec<i64>,
    pub setyp: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub idof: Vec<f64>,
    pub edgset: Vec<i64>,
    pub nipp: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub iunf: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub iloc: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
    pub pi: Vec<f64>,
    pub pi_2: Vec<f64>,
    pub pi_3: Vec<f64>,
    pub pi_4: Vec<f64>,
    pub pi_5: Vec<f64>,
    pub pi_6: Vec<f64>,
    pub pi_7: Vec<f64>,
    pub pi_8: Vec<f64>,
}
impl SECTION_SHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SHELL").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            idof: t.take("IDOF").and_then(|c| c.into_float()).unwrap_or_default(),
            edgset: t.take("EDGSET").and_then(|c| c.into_int()).unwrap_or_default(),
            nipp: t.take("NIPP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            iunf: t.take("IUNF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            iloc: t.take("ILOC").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
            pi: t.take("Pi").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_2: t.take("Pi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_3: t.take("Pi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_4: t.take("Pi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_5: t.take("Pi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_6: t.take("Pi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_7: t.take("Pi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_8: t.take("Pi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SHELL_EFG {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub icomp: Vec<i64>,
    pub setyp: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub ispline: Vec<i64>,
    pub idila: Vec<i64>,
    pub iebt: Vec<i64>,
    pub idim: Vec<i64>,
    pub nipp: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub iunf: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub iloc: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
    pub pi: Vec<f64>,
    pub pi_2: Vec<f64>,
    pub pi_3: Vec<f64>,
    pub pi_4: Vec<f64>,
    pub pi_5: Vec<f64>,
    pub pi_6: Vec<f64>,
    pub pi_7: Vec<f64>,
    pub pi_8: Vec<f64>,
}
impl SECTION_SHELL_EFG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SHELL_EFG").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            ispline: t.take("ISPLINE").and_then(|c| c.into_int()).unwrap_or_default(),
            idila: t.take("IDILA").and_then(|c| c.into_int()).unwrap_or_default(),
            iebt: t.take("IEBT").and_then(|c| c.into_int()).unwrap_or_default(),
            idim: t.take("IDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            nipp: t.take("NIPP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            iunf: t.take("IUNF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            iloc: t.take("ILOC").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
            pi: t.take("Pi").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_2: t.take("Pi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_3: t.take("Pi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_4: t.take("Pi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_5: t.take("Pi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_6: t.take("Pi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_7: t.take("Pi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_8: t.take("Pi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SHELL_MISC {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub icomp: Vec<i64>,
    pub setyp: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub thkscl: Vec<f64>,
    pub nipp: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub iunf: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub iloc: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
    pub pi: Vec<f64>,
    pub pi_2: Vec<f64>,
    pub pi_3: Vec<f64>,
    pub pi_4: Vec<f64>,
    pub pi_5: Vec<f64>,
    pub pi_6: Vec<f64>,
    pub pi_7: Vec<f64>,
    pub pi_8: Vec<f64>,
}
impl SECTION_SHELL_MISC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SHELL_MISC").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            thkscl: t.take("THKSCL").and_then(|c| c.into_float()).unwrap_or_default(),
            nipp: t.take("NIPP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            iunf: t.take("IUNF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            iloc: t.take("ILOC").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
            pi: t.take("Pi").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_2: t.take("Pi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_3: t.take("Pi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_4: t.take("Pi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_5: t.take("Pi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_6: t.take("Pi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_7: t.take("Pi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_8: t.take("Pi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SHELL_THERMAL {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub icomp: Vec<i64>,
    pub setyp: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub ithelfm: Vec<i64>,
    pub nipp: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub iunf: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub iloc: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
    pub pi: Vec<f64>,
    pub pi_2: Vec<f64>,
    pub pi_3: Vec<f64>,
    pub pi_4: Vec<f64>,
    pub pi_5: Vec<f64>,
    pub pi_6: Vec<f64>,
    pub pi_7: Vec<f64>,
    pub pi_8: Vec<f64>,
}
impl SECTION_SHELL_THERMAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SHELL_THERMAL").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            ithelfm: t.take("ITHELFM").and_then(|c| c.into_int()).unwrap_or_default(),
            nipp: t.take("NIPP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            iunf: t.take("IUNF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            iloc: t.take("ILOC").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
            pi: t.take("Pi").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_2: t.take("Pi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_3: t.take("Pi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_4: t.take("Pi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_5: t.take("Pi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_6: t.take("Pi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_7: t.take("Pi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_8: t.take("Pi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SHELL_XFEM {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr_irid: Vec<i64>,
    pub icomp: Vec<i64>,
    pub setyp: Vec<i64>,
    pub t1: Vec<f64>,
    pub t2: Vec<f64>,
    pub t3: Vec<f64>,
    pub t4: Vec<f64>,
    pub nloc: Vec<f64>,
    pub marea: Vec<f64>,
    pub cmid: Vec<i64>,
    pub baselm: Vec<i64>,
    pub domint: Vec<i64>,
    pub failcr: Vec<i64>,
    pub propcr: Vec<i64>,
    pub fs: Vec<f64>,
    pub ls_fs1: Vec<f64>,
    pub nc_cl: Vec<f64>,
    pub nipp: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub iunf: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub iloc: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
    pub pi: Vec<f64>,
    pub pi_2: Vec<f64>,
    pub pi_3: Vec<f64>,
    pub pi_4: Vec<f64>,
    pub pi_5: Vec<f64>,
    pub pi_6: Vec<f64>,
    pub pi_7: Vec<f64>,
    pub pi_8: Vec<f64>,
}
impl SECTION_SHELL_XFEM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SHELL_XFEM").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr_irid: t.take("QR/IRID").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            setyp: t.take("SETYP").and_then(|c| c.into_int()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
            t3: t.take("T3").and_then(|c| c.into_float()).unwrap_or_default(),
            t4: t.take("T4").and_then(|c| c.into_float()).unwrap_or_default(),
            nloc: t.take("NLOC").and_then(|c| c.into_float()).unwrap_or_default(),
            marea: t.take("MAREA").and_then(|c| c.into_float()).unwrap_or_default(),
            cmid: t.take("CMID").and_then(|c| c.into_int()).unwrap_or_default(),
            baselm: t.take("BASELM").and_then(|c| c.into_int()).unwrap_or_default(),
            domint: t.take("DOMINT").and_then(|c| c.into_int()).unwrap_or_default(),
            failcr: t.take("FAILCR").and_then(|c| c.into_int()).unwrap_or_default(),
            propcr: t.take("PROPCR").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            ls_fs1: t.take("LS/FS1").and_then(|c| c.into_float()).unwrap_or_default(),
            nc_cl: t.take("NC/CL").and_then(|c| c.into_float()).unwrap_or_default(),
            nipp: t.take("NIPP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            iunf: t.take("IUNF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            iloc: t.take("ILOC").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
            pi: t.take("Pi").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_2: t.take("Pi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_3: t.take("Pi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_4: t.take("Pi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_5: t.take("Pi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_6: t.take("Pi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_7: t.take("Pi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            pi_8: t.take("Pi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SOLID {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub aet: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub cohoff: Vec<f64>,
    pub gaskeit: Vec<f64>,
    pub nip: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub zeta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl SECTION_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SOLID").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            cohoff: t.take("COHOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            gaskeit: t.take("GASKEIT").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta: t.take("ZETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SOLID_EFG {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub aet: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub cohoff: Vec<f64>,
    pub gaskeit: Vec<f64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub ispline: Vec<i64>,
    pub idila: Vec<i64>,
    pub iebt: Vec<i64>,
    pub idim: Vec<i64>,
    pub toldef: Vec<f64>,
    pub ips: Vec<i64>,
    pub stime: Vec<f64>,
    pub iken: Vec<i64>,
    pub sf: Vec<f64>,
    pub cmid: Vec<i64>,
    pub ibr: Vec<i64>,
    pub ds: Vec<f64>,
    pub ecut: Vec<f64>,
    pub nip: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub zeta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl SECTION_SOLID_EFG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SOLID_EFG").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            cohoff: t.take("COHOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            gaskeit: t.take("GASKEIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ispline: t.take("ISPLINE").and_then(|c| c.into_int()).unwrap_or_default(),
            idila: t.take("IDILA").and_then(|c| c.into_int()).unwrap_or_default(),
            iebt: t.take("IEBT").and_then(|c| c.into_int()).unwrap_or_default(),
            idim: t.take("IDIM").and_then(|c| c.into_int()).unwrap_or_default(),
            toldef: t.take("TOLDEF").and_then(|c| c.into_float()).unwrap_or_default(),
            ips: t.take("IPS").and_then(|c| c.into_int()).unwrap_or_default(),
            stime: t.take("STIME").and_then(|c| c.into_float()).unwrap_or_default(),
            iken: t.take("IKEN").and_then(|c| c.into_int()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            cmid: t.take("CMID").and_then(|c| c.into_int()).unwrap_or_default(),
            ibr: t.take("IBR").and_then(|c| c.into_int()).unwrap_or_default(),
            ds: t.take("DS").and_then(|c| c.into_float()).unwrap_or_default(),
            ecut: t.take("ECUT").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta: t.take("ZETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SOLID_MISC {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub aet: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub cohoff: Vec<f64>,
    pub gaskeit: Vec<f64>,
    pub cohthk: Vec<f64>,
    pub f: Vec<i64>,
    pub f_2: Vec<i64>,
    pub f_3: Vec<i64>,
    pub f_4: Vec<i64>,
    pub f_5: Vec<i64>,
    pub f_6: Vec<i64>,
    pub f_7: Vec<i64>,
    pub nip: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub zeta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl SECTION_SOLID_MISC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SOLID_MISC").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            cohoff: t.take("COHOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            gaskeit: t.take("GASKEIT").and_then(|c| c.into_float()).unwrap_or_default(),
            cohthk: t.take("COHTHK").and_then(|c| c.into_float()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            f_2: t.take("-_2").and_then(|c| c.into_int()).unwrap_or_default(),
            f_3: t.take("-_3").and_then(|c| c.into_int()).unwrap_or_default(),
            f_4: t.take("-_4").and_then(|c| c.into_int()).unwrap_or_default(),
            f_5: t.take("-_5").and_then(|c| c.into_int()).unwrap_or_default(),
            f_6: t.take("-_6").and_then(|c| c.into_int()).unwrap_or_default(),
            f_7: t.take("-_7").and_then(|c| c.into_int()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta: t.take("ZETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SOLID_PERI {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub dr: Vec<f64>,
    pub ptype: Vec<i64>,
}
impl SECTION_SOLID_PERI {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SOLID_PERI").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            dr: t.take("Dr").and_then(|c| c.into_float()).unwrap_or_default(),
            ptype: t.take("ptype").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SOLID_SPG {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub aet: Vec<i64>,
    pub unused: Vec<i64>,
    pub unused_2: Vec<i64>,
    pub unused_3: Vec<i64>,
    pub cohoff: Vec<f64>,
    pub gaskeit: Vec<f64>,
    pub dx: Vec<f64>,
    pub dy: Vec<f64>,
    pub dz: Vec<f64>,
    pub ispline: Vec<i64>,
    pub kernel: Vec<i64>,
    pub f: Vec<f64>,
    pub smstep: Vec<i64>,
    pub msc: Vec<f64>,
    pub idam: Vec<i64>,
    pub fs: Vec<f64>,
    pub stretch: Vec<f64>,
    pub itb: Vec<i64>,
    pub msfac: Vec<f64>,
    pub isc: Vec<f64>,
    pub boxid: Vec<i64>,
    pub pdamp: Vec<f64>,
    pub nip: Vec<i64>,
    pub nxdof: Vec<i64>,
    pub ihgf: Vec<i64>,
    pub itaj: Vec<i64>,
    pub lmc: Vec<i64>,
    pub nhsv: Vec<i64>,
    pub xi: Vec<f64>,
    pub eta: Vec<f64>,
    pub zeta: Vec<f64>,
    pub wgt: Vec<f64>,
    pub p1: Vec<f64>,
    pub p2: Vec<f64>,
    pub p3: Vec<f64>,
    pub p4: Vec<f64>,
    pub p5: Vec<f64>,
    pub p6: Vec<f64>,
    pub p7: Vec<f64>,
    pub p8: Vec<f64>,
}
impl SECTION_SOLID_SPG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SOLID_SPG").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            aet: t.take("AET").and_then(|c| c.into_int()).unwrap_or_default(),
            unused: t.take("UNUSED").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_2: t.take("UNUSED_2").and_then(|c| c.into_int()).unwrap_or_default(),
            unused_3: t.take("UNUSED_3").and_then(|c| c.into_int()).unwrap_or_default(),
            cohoff: t.take("COHOFF").and_then(|c| c.into_float()).unwrap_or_default(),
            gaskeit: t.take("GASKEIT").and_then(|c| c.into_float()).unwrap_or_default(),
            dx: t.take("DX").and_then(|c| c.into_float()).unwrap_or_default(),
            dy: t.take("DY").and_then(|c| c.into_float()).unwrap_or_default(),
            dz: t.take("DZ").and_then(|c| c.into_float()).unwrap_or_default(),
            ispline: t.take("ISPLINE").and_then(|c| c.into_int()).unwrap_or_default(),
            kernel: t.take("KERNEL").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_float()).unwrap_or_default(),
            smstep: t.take("SMSTEP").and_then(|c| c.into_int()).unwrap_or_default(),
            msc: t.take("MSC").and_then(|c| c.into_float()).unwrap_or_default(),
            idam: t.take("IDAM").and_then(|c| c.into_int()).unwrap_or_default(),
            fs: t.take("FS").and_then(|c| c.into_float()).unwrap_or_default(),
            stretch: t.take("STRETCH").and_then(|c| c.into_float()).unwrap_or_default(),
            itb: t.take("ITB").and_then(|c| c.into_int()).unwrap_or_default(),
            msfac: t.take("MSFAC").and_then(|c| c.into_float()).unwrap_or_default(),
            isc: t.take("ISC").and_then(|c| c.into_float()).unwrap_or_default(),
            boxid: t.take("BOXID").and_then(|c| c.into_int()).unwrap_or_default(),
            pdamp: t.take("PDAMP").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            nxdof: t.take("NXDOF").and_then(|c| c.into_int()).unwrap_or_default(),
            ihgf: t.take("IHGF").and_then(|c| c.into_int()).unwrap_or_default(),
            itaj: t.take("ITAJ").and_then(|c| c.into_int()).unwrap_or_default(),
            lmc: t.take("LMC").and_then(|c| c.into_int()).unwrap_or_default(),
            nhsv: t.take("NHSV").and_then(|c| c.into_int()).unwrap_or_default(),
            xi: t.take("XI").and_then(|c| c.into_float()).unwrap_or_default(),
            eta: t.take("ETA").and_then(|c| c.into_float()).unwrap_or_default(),
            zeta: t.take("ZETA").and_then(|c| c.into_float()).unwrap_or_default(),
            wgt: t.take("WGT").and_then(|c| c.into_float()).unwrap_or_default(),
            p1: t.take("P1").and_then(|c| c.into_float()).unwrap_or_default(),
            p2: t.take("P2").and_then(|c| c.into_float()).unwrap_or_default(),
            p3: t.take("P3").and_then(|c| c.into_float()).unwrap_or_default(),
            p4: t.take("P4").and_then(|c| c.into_float()).unwrap_or_default(),
            p5: t.take("P5").and_then(|c| c.into_float()).unwrap_or_default(),
            p6: t.take("P6").and_then(|c| c.into_float()).unwrap_or_default(),
            p7: t.take("P7").and_then(|c| c.into_float()).unwrap_or_default(),
            p8: t.take("P8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPH {
    pub secid: Vec<i64>,
    pub cslh: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub sphini: Vec<f64>,
    pub death: Vec<f64>,
    pub start: Vec<f64>,
    pub sphkern: Vec<i64>,
}
impl SECTION_SPH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPH").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            cslh: t.take("CSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sphini: t.take("SPHINI").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            sphkern: t.take("SPHKERN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPH_ELLIPSE {
    pub secid: Vec<i64>,
    pub cslh: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub sphini: Vec<f64>,
    pub death: Vec<f64>,
    pub start: Vec<f64>,
    pub sphkern: Vec<i64>,
    pub hxcslh: Vec<f64>,
    pub hycslh: Vec<f64>,
    pub hzcslh: Vec<f64>,
    pub hxini: Vec<f64>,
    pub hyini: Vec<f64>,
    pub hzini: Vec<f64>,
}
impl SECTION_SPH_ELLIPSE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPH_ELLIPSE").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            cslh: t.take("CSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sphini: t.take("SPHINI").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            sphkern: t.take("SPHKERN").and_then(|c| c.into_int()).unwrap_or_default(),
            hxcslh: t.take("HXCSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hycslh: t.take("HYCSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hzcslh: t.take("HZCSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hxini: t.take("HXINI").and_then(|c| c.into_float()).unwrap_or_default(),
            hyini: t.take("HYINI").and_then(|c| c.into_float()).unwrap_or_default(),
            hzini: t.take("HZINI").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPH_INTERACTION {
    pub secid: Vec<i64>,
    pub cslh: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub sphini: Vec<f64>,
    pub death: Vec<f64>,
    pub start: Vec<f64>,
    pub sphkern: Vec<i64>,
}
impl SECTION_SPH_INTERACTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPH_INTERACTION").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            cslh: t.take("CSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sphini: t.take("SPHINI").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            sphkern: t.take("SPHKERN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPH_USER {
    pub secid: Vec<i64>,
    pub cslh: Vec<f64>,
    pub hmin: Vec<f64>,
    pub hmax: Vec<f64>,
    pub sphini: Vec<f64>,
    pub death: Vec<f64>,
    pub start: Vec<f64>,
    pub sphkern: Vec<i64>,
}
impl SECTION_SPH_USER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPH_USER").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            cslh: t.take("CSLH").and_then(|c| c.into_float()).unwrap_or_default(),
            hmin: t.take("HMIN").and_then(|c| c.into_float()).unwrap_or_default(),
            hmax: t.take("HMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            sphini: t.take("SPHINI").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            start: t.take("START").and_then(|c| c.into_float()).unwrap_or_default(),
            sphkern: t.take("SPHKERN").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPRING_DAMPER {
    pub secid: Vec<i64>,
    pub dro: Vec<i64>,
    pub kd: Vec<f64>,
    pub v0: Vec<f64>,
    pub cl: Vec<f64>,
    pub fd: Vec<f64>,
}
impl SECTION_SPRING_DAMPER {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPRING-DAMPER").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            dro: t.take("DRO").and_then(|c| c.into_int()).unwrap_or_default(),
            kd: t.take("KD").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_SPRING_DAMPER_2 {
    pub secid: Vec<i64>,
    pub dro: Vec<i64>,
    pub kd: Vec<f64>,
    pub v0: Vec<f64>,
    pub cl: Vec<f64>,
    pub fd: Vec<f64>,
    pub cdl: Vec<i64>,
    pub tdl: Vec<i64>,
}
impl SECTION_SPRING_DAMPER_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_SPRING_DAMPER").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            dro: t.take("DRO").and_then(|c| c.into_int()).unwrap_or_default(),
            kd: t.take("KD").and_then(|c| c.into_float()).unwrap_or_default(),
            v0: t.take("V0").and_then(|c| c.into_float()).unwrap_or_default(),
            cl: t.take("CL").and_then(|c| c.into_float()).unwrap_or_default(),
            fd: t.take("FD").and_then(|c| c.into_float()).unwrap_or_default(),
            cdl: t.take("CDL").and_then(|c| c.into_int()).unwrap_or_default(),
            tdl: t.take("TDL").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SECTION_TSHELL {
    pub secid: Vec<i64>,
    pub elform: Vec<i64>,
    pub shrf: Vec<f64>,
    pub nip: Vec<i64>,
    pub propt: Vec<f64>,
    pub qr: Vec<i64>,
    pub icomp: Vec<i64>,
    pub tshear: Vec<i64>,
    pub bi: Vec<f64>,
    pub bi_2: Vec<f64>,
    pub bi_3: Vec<f64>,
    pub bi_4: Vec<f64>,
    pub bi_5: Vec<f64>,
    pub bi_6: Vec<f64>,
    pub bi_7: Vec<f64>,
    pub bi_8: Vec<f64>,
}
impl SECTION_TSHELL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SECTION_TSHELL").unwrap());
        Self {
            secid: t.take("SECID").and_then(|c| c.into_int()).unwrap_or_default(),
            elform: t.take("ELFORM").and_then(|c| c.into_int()).unwrap_or_default(),
            shrf: t.take("SHRF").and_then(|c| c.into_float()).unwrap_or_default(),
            nip: t.take("NIP").and_then(|c| c.into_int()).unwrap_or_default(),
            propt: t.take("PROPT").and_then(|c| c.into_float()).unwrap_or_default(),
            qr: t.take("QR").and_then(|c| c.into_int()).unwrap_or_default(),
            icomp: t.take("ICOMP").and_then(|c| c.into_int()).unwrap_or_default(),
            tshear: t.take("TSHEAR").and_then(|c| c.into_int()).unwrap_or_default(),
            bi: t.take("Bi").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_2: t.take("Bi_2").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_3: t.take("Bi_3").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_4: t.take("Bi_4").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_5: t.take("Bi_5").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_6: t.take("Bi_6").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_7: t.take("Bi_7").and_then(|c| c.into_float()).unwrap_or_default(),
            bi_8: t.take("Bi_8").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_CONTROL {
    pub cntlid: Vec<i64>,
    pub type_: Vec<String>,
    pub typeid: Vec<i64>,
    pub timeoff: Vec<i64>,
    pub nrep: Vec<i64>,
    pub estyp: Vec<String>,
    pub initstt: Vec<String>,
    pub swit1: Vec<i64>,
    pub swit2: Vec<i64>,
    pub swit3: Vec<i64>,
    pub swit4: Vec<i64>,
    pub swit5: Vec<i64>,
    pub swit6: Vec<i64>,
    pub swit7: Vec<i64>,
}
impl SENSOR_CONTROL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_CONTROL").unwrap());
        Self {
            cntlid: t.take("CNTLID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            timeoff: t.take("TIMEOFF").and_then(|c| c.into_int()).unwrap_or_default(),
            nrep: t.take("NREP").and_then(|c| c.into_int()).unwrap_or_default(),
            estyp: t.take("ESTYP").and_then(|c| c.into_str()).unwrap_or_default(),
            initstt: t.take("INITSTT").and_then(|c| c.into_str()).unwrap_or_default(),
            swit1: t.take("SWIT1").and_then(|c| c.into_int()).unwrap_or_default(),
            swit2: t.take("SWIT2").and_then(|c| c.into_int()).unwrap_or_default(),
            swit3: t.take("SWIT3").and_then(|c| c.into_int()).unwrap_or_default(),
            swit4: t.take("SWIT4").and_then(|c| c.into_int()).unwrap_or_default(),
            swit5: t.take("SWIT5").and_then(|c| c.into_int()).unwrap_or_default(),
            swit6: t.take("SWIT6").and_then(|c| c.into_int()).unwrap_or_default(),
            swit7: t.take("SWIT7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_CPM_AIRBAG {
    pub cpmid: Vec<i64>,
    pub switid: Vec<i64>,
    pub tbirth: Vec<f64>,
    pub tdeath: Vec<f64>,
    pub tdr: Vec<f64>,
    pub defps: Vec<i64>,
    pub rbpid: Vec<i64>,
}
impl SENSOR_CPM_AIRBAG {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_CPM_AIRBAG").unwrap());
        Self {
            cpmid: t.take("CPMID").and_then(|c| c.into_int()).unwrap_or_default(),
            switid: t.take("SWITID").and_then(|c| c.into_int()).unwrap_or_default(),
            tbirth: t.take("TBIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdeath: t.take("TDEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            tdr: t.take("TDR").and_then(|c| c.into_float()).unwrap_or_default(),
            defps: t.take("DEFPS").and_then(|c| c.into_int()).unwrap_or_default(),
            rbpid: t.take("RBPID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_CALC_MATH {
    pub sensid: Vec<i64>,
    pub calc: Vec<String>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
}
impl SENSOR_DEFINE_CALC_MATH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_CALC-MATH").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            calc: t.take("CALC").and_then(|c| c.into_str()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_CALC_MATH_UPDATE {
    pub sensid: Vec<i64>,
    pub calc: Vec<String>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_CALC_MATH_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_CALC-MATH_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            calc: t.take("CALC").and_then(|c| c.into_str()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_CALC_MATH_2 {
    pub sensid: Vec<i64>,
    pub calc: Vec<String>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
}
impl SENSOR_DEFINE_CALC_MATH_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_CALC_MATH").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            calc: t.take("CALC").and_then(|c| c.into_str()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_CALC_MATH_UPDATE_2 {
    pub sensid: Vec<i64>,
    pub calc: Vec<String>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_CALC_MATH_UPDATE_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_CALC_MATH_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            calc: t.take("CALC").and_then(|c| c.into_str()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_ELEMENT {
    pub sensid: Vec<i64>,
    pub etype: Vec<String>,
    pub elemid: Vec<i64>,
    pub comp: Vec<String>,
    pub ctype: Vec<String>,
    pub layer: Vec<String>,
    pub sf: Vec<f64>,
    pub pwr: Vec<f64>,
}
impl SENSOR_DEFINE_ELEMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_ELEMENT").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            elemid: t.take("ELEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_str()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            layer: t.take("LAYER").and_then(|c| c.into_str()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pwr: t.take("PWR").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_ELEMENT_SET {
    pub sensid: Vec<i64>,
    pub etype: Vec<String>,
    pub elemid: Vec<i64>,
    pub comp: Vec<String>,
    pub ctype: Vec<String>,
    pub layer: Vec<String>,
    pub sf: Vec<f64>,
    pub pwr: Vec<f64>,
    pub setopt: Vec<String>,
}
impl SENSOR_DEFINE_ELEMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_ELEMENT_SET").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            elemid: t.take("ELEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_str()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            layer: t.take("LAYER").and_then(|c| c.into_str()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pwr: t.take("PWR").and_then(|c| c.into_float()).unwrap_or_default(),
            setopt: t.take("SETOPT").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_ELEMENT_SET_UPDATE {
    pub sensid: Vec<i64>,
    pub etype: Vec<String>,
    pub elemid: Vec<i64>,
    pub comp: Vec<String>,
    pub ctype: Vec<String>,
    pub layer: Vec<String>,
    pub sf: Vec<f64>,
    pub pwr: Vec<f64>,
    pub setopt: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_ELEMENT_SET_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_ELEMENT_SET_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            elemid: t.take("ELEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_str()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            layer: t.take("LAYER").and_then(|c| c.into_str()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pwr: t.take("PWR").and_then(|c| c.into_float()).unwrap_or_default(),
            setopt: t.take("SETOPT").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_ELEMENT_UPDATE {
    pub sensid: Vec<i64>,
    pub etype: Vec<String>,
    pub elemid: Vec<i64>,
    pub comp: Vec<String>,
    pub ctype: Vec<String>,
    pub layer: Vec<String>,
    pub sf: Vec<f64>,
    pub pwr: Vec<f64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_ELEMENT_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_ELEMENT_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            etype: t.take("ETYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            elemid: t.take("ELEMID").and_then(|c| c.into_int()).unwrap_or_default(),
            comp: t.take("COMP").and_then(|c| c.into_str()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            layer: t.take("LAYER").and_then(|c| c.into_str()).unwrap_or_default(),
            sf: t.take("SF").and_then(|c| c.into_float()).unwrap_or_default(),
            pwr: t.take("PWR").and_then(|c| c.into_float()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_FORCE {
    pub sensid: Vec<i64>,
    pub ftype: Vec<String>,
    pub typeid: Vec<i64>,
    pub vid: Vec<String>,
    pub crd: Vec<i64>,
}
impl SENSOR_DEFINE_FORCE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_FORCE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            crd: t.take("CRD").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_FORCE_UPDATE {
    pub sensid: Vec<i64>,
    pub ftype: Vec<String>,
    pub typeid: Vec<i64>,
    pub vid: Vec<String>,
    pub crd: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_FORCE_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_FORCE_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ftype: t.take("FTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            typeid: t.take("TYPEID").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            crd: t.take("CRD").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_FUNCTION {
    pub sensid: Vec<i64>,
    pub func: Vec<i64>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
    pub sensi: Vec<i64>,
    pub sensi_1: Vec<i64>,
    pub sensi_2: Vec<i64>,
    pub sensi_3: Vec<i64>,
    pub sensi_4: Vec<i64>,
    pub sensi_5: Vec<i64>,
    pub sensi_6: Vec<i64>,
    pub sensi_7: Vec<i64>,
}
impl SENSOR_DEFINE_FUNCTION {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_FUNCTION").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            func: t.take("FUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi: t.take("SENSi").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_1: t.take("SENSi+1").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_2: t.take("SENSi+2").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_3: t.take("SENSi+3").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_4: t.take("SENSi+4").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_5: t.take("SENSi+5").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_6: t.take("SENSi+6").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_7: t.take("SENSi+7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_FUNCTION_UPDATE {
    pub sensid: Vec<i64>,
    pub func: Vec<i64>,
    pub sens1: Vec<i64>,
    pub sens2: Vec<i64>,
    pub sens3: Vec<i64>,
    pub sens4: Vec<i64>,
    pub sens5: Vec<i64>,
    pub sens6: Vec<i64>,
    pub sensi: Vec<i64>,
    pub sensi_1: Vec<i64>,
    pub sensi_2: Vec<i64>,
    pub sensi_3: Vec<i64>,
    pub sensi_4: Vec<i64>,
    pub sensi_5: Vec<i64>,
    pub sensi_6: Vec<i64>,
    pub sensi_7: Vec<i64>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_FUNCTION_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_FUNCTION_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            func: t.take("FUNC").and_then(|c| c.into_int()).unwrap_or_default(),
            sens1: t.take("SENS1").and_then(|c| c.into_int()).unwrap_or_default(),
            sens2: t.take("SENS2").and_then(|c| c.into_int()).unwrap_or_default(),
            sens3: t.take("SENS3").and_then(|c| c.into_int()).unwrap_or_default(),
            sens4: t.take("SENS4").and_then(|c| c.into_int()).unwrap_or_default(),
            sens5: t.take("SENS5").and_then(|c| c.into_int()).unwrap_or_default(),
            sens6: t.take("SENS6").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi: t.take("SENSi").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_1: t.take("SENSi+1").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_2: t.take("SENSi+2").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_3: t.take("SENSi+3").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_4: t.take("SENSi+4").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_5: t.take("SENSi+5").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_6: t.take("SENSi+6").and_then(|c| c.into_int()).unwrap_or_default(),
            sensi_7: t.take("SENSi+7").and_then(|c| c.into_int()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_MISC {
    pub sensid: Vec<i64>,
    pub mtype: Vec<String>,
    pub i0: Vec<String>,
    pub i1: Vec<String>,
    pub i2: Vec<String>,
    pub i3: Vec<String>,
    pub i4: Vec<String>,
    pub i5: Vec<String>,
}
impl SENSOR_DEFINE_MISC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_MISC").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            i0: t.take("I0").and_then(|c| c.into_str()).unwrap_or_default(),
            i1: t.take("I1").and_then(|c| c.into_str()).unwrap_or_default(),
            i2: t.take("I2").and_then(|c| c.into_str()).unwrap_or_default(),
            i3: t.take("I3").and_then(|c| c.into_str()).unwrap_or_default(),
            i4: t.take("I4").and_then(|c| c.into_str()).unwrap_or_default(),
            i5: t.take("I5").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_MISC_UPDATE {
    pub sensid: Vec<i64>,
    pub mtype: Vec<String>,
    pub i0: Vec<String>,
    pub i1: Vec<String>,
    pub i2: Vec<String>,
    pub i3: Vec<String>,
    pub i4: Vec<String>,
    pub i5: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_MISC_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_MISC_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            mtype: t.take("MTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            i0: t.take("I0").and_then(|c| c.into_str()).unwrap_or_default(),
            i1: t.take("I1").and_then(|c| c.into_str()).unwrap_or_default(),
            i2: t.take("I2").and_then(|c| c.into_str()).unwrap_or_default(),
            i3: t.take("I3").and_then(|c| c.into_str()).unwrap_or_default(),
            i4: t.take("I4").and_then(|c| c.into_str()).unwrap_or_default(),
            i5: t.take("I5").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_NODE {
    pub sensid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub vid: Vec<String>,
    pub f: Vec<i64>,
    pub ctype: Vec<String>,
}
impl SENSOR_DEFINE_NODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_NODE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_NODE_SET {
    pub sensid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub vid: Vec<String>,
    pub f: Vec<i64>,
    pub ctype: Vec<String>,
    pub setopt: Vec<String>,
}
impl SENSOR_DEFINE_NODE_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_NODE_SET").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            setopt: t.take("SETOPT").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_NODE_SET_UPDATE {
    pub sensid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub vid: Vec<String>,
    pub f: Vec<i64>,
    pub ctype: Vec<String>,
    pub setopt: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_NODE_SET_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_NODE_SET_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            setopt: t.take("SETOPT").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_DEFINE_NODE_UPDATE {
    pub sensid: Vec<i64>,
    pub node1: Vec<i64>,
    pub node2: Vec<i64>,
    pub vid: Vec<String>,
    pub f: Vec<i64>,
    pub ctype: Vec<String>,
    pub birth: Vec<f64>,
    pub death: Vec<f64>,
    pub dtupd: Vec<f64>,
}
impl SENSOR_DEFINE_NODE_UPDATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_DEFINE_NODE_UPDATE").unwrap());
        Self {
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            node1: t.take("NODE1").and_then(|c| c.into_int()).unwrap_or_default(),
            node2: t.take("NODE2").and_then(|c| c.into_int()).unwrap_or_default(),
            vid: t.take("VID").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            ctype: t.take("CTYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            birth: t.take("BIRTH").and_then(|c| c.into_float()).unwrap_or_default(),
            death: t.take("DEATH").and_then(|c| c.into_float()).unwrap_or_default(),
            dtupd: t.take("DTUPD").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_SWITCH {
    pub switid: Vec<i64>,
    pub type_: Vec<String>,
    pub sensid: Vec<i64>,
    pub logic: Vec<String>,
    pub value: Vec<f64>,
    pub filtrid: Vec<i64>,
    pub timwin: Vec<f64>,
    pub tdelay: Vec<f64>,
}
impl SENSOR_SWITCH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_SWITCH").unwrap());
        Self {
            switid: t.take("SWITID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_str()).unwrap_or_default(),
            sensid: t.take("SENSID").and_then(|c| c.into_int()).unwrap_or_default(),
            logic: t.take("LOGIC").and_then(|c| c.into_str()).unwrap_or_default(),
            value: t.take("VALUE").and_then(|c| c.into_float()).unwrap_or_default(),
            filtrid: t.take("FILTRID").and_then(|c| c.into_int()).unwrap_or_default(),
            timwin: t.take("TIMWIN").and_then(|c| c.into_float()).unwrap_or_default(),
            tdelay: t.take("TDELAY").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_SWITCH_CALC_LOGIC {
    pub switid: Vec<i64>,
    pub swit1: Vec<i64>,
    pub swit2: Vec<i64>,
    pub swit3: Vec<i64>,
    pub swit4: Vec<i64>,
    pub swit5: Vec<i64>,
    pub swit6: Vec<i64>,
    pub swit7: Vec<i64>,
}
impl SENSOR_SWITCH_CALC_LOGIC {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_SWITCH_CALC-LOGIC").unwrap());
        Self {
            switid: t.take("SWITID").and_then(|c| c.into_int()).unwrap_or_default(),
            swit1: t.take("SWIT1").and_then(|c| c.into_int()).unwrap_or_default(),
            swit2: t.take("SWIT2").and_then(|c| c.into_int()).unwrap_or_default(),
            swit3: t.take("SWIT3").and_then(|c| c.into_int()).unwrap_or_default(),
            swit4: t.take("SWIT4").and_then(|c| c.into_int()).unwrap_or_default(),
            swit5: t.take("SWIT5").and_then(|c| c.into_int()).unwrap_or_default(),
            swit6: t.take("SWIT6").and_then(|c| c.into_int()).unwrap_or_default(),
            swit7: t.take("SWIT7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_SWITCH_CALC_LOGIC_2 {
    pub switid: Vec<i64>,
    pub swit1: Vec<i64>,
    pub swit2: Vec<i64>,
    pub swit3: Vec<i64>,
    pub swit4: Vec<i64>,
    pub swit5: Vec<i64>,
    pub swit6: Vec<i64>,
    pub swit7: Vec<i64>,
}
impl SENSOR_SWITCH_CALC_LOGIC_2 {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_SWITCH_CALC_LOGIC").unwrap());
        Self {
            switid: t.take("SWITID").and_then(|c| c.into_int()).unwrap_or_default(),
            swit1: t.take("SWIT1").and_then(|c| c.into_int()).unwrap_or_default(),
            swit2: t.take("SWIT2").and_then(|c| c.into_int()).unwrap_or_default(),
            swit3: t.take("SWIT3").and_then(|c| c.into_int()).unwrap_or_default(),
            swit4: t.take("SWIT4").and_then(|c| c.into_int()).unwrap_or_default(),
            swit5: t.take("SWIT5").and_then(|c| c.into_int()).unwrap_or_default(),
            swit6: t.take("SWIT6").and_then(|c| c.into_int()).unwrap_or_default(),
            swit7: t.take("SWIT7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SENSOR_SWITCH_SHELL_TO_VENT {
    pub id: Vec<i64>,
    pub type_: Vec<i64>,
    pub c23: Vec<f64>,
    pub amax: Vec<f64>,
    pub ssid: Vec<i64>,
    pub ftime: Vec<f64>,
    pub c23v: Vec<f64>,
}
impl SENSOR_SWITCH_SHELL_TO_VENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SENSOR_SWITCH_SHELL_TO_VENT").unwrap());
        Self {
            id: t.take("ID").and_then(|c| c.into_int()).unwrap_or_default(),
            type_: t.take("TYPE").and_then(|c| c.into_int()).unwrap_or_default(),
            c23: t.take("C23").and_then(|c| c.into_float()).unwrap_or_default(),
            amax: t.take("AMAX").and_then(|c| c.into_float()).unwrap_or_default(),
            ssid: t.take("SSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ftime: t.take("FTIME").and_then(|c| c.into_float()).unwrap_or_default(),
            c23v: t.take("C23V").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_2D_SEGMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub pid: Vec<i64>,
}
impl SET_2D_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_2D_SEGMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_2D_SEGMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub pid: Vec<i64>,
}
impl SET_2D_SEGMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_2D_SEGMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_2D_SEGMENT_SET {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub psid: Vec<i64>,
}
impl SET_2D_SEGMENT_SET {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_2D_SEGMENT_SET").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_2D_SEGMENT_SET_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub psid: Vec<i64>,
}
impl SET_2D_SEGMENT_SET_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_2D_SEGMENT_SET_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            psid: t.take("PSID").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM {
    pub sid: Vec<i64>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_BEAM {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_ADD {
    pub sid: Vec<i64>,
    pub bsid1: Vec<i64>,
    pub bsid2: Vec<i64>,
    pub bsid3: Vec<i64>,
    pub bsid4: Vec<i64>,
    pub bsid5: Vec<i64>,
    pub bsid6: Vec<i64>,
    pub bsid7: Vec<i64>,
    pub bsid8: Vec<i64>,
}
impl SET_BEAM_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid1: t.take("BSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid2: t.take("BSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid3: t.take("BSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid4: t.take("BSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid5: t.take("BSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid6: t.take("BSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid7: t.take("BSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid8: t.take("BSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_COLLECT {
    pub sid: Vec<i64>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_BEAM_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERAL {
    pub sid: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_BEAM_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_BEAM_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERATE {
    pub sid: Vec<i64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_BEAM_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_BEAM_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_BEAM_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_BEAM_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BEAM_INTERSECT {
    pub sid: Vec<i64>,
    pub bsid1: Vec<i64>,
    pub bsid2: Vec<i64>,
    pub bsid3: Vec<i64>,
    pub bsid4: Vec<i64>,
    pub bsid5: Vec<i64>,
    pub bsid6: Vec<i64>,
    pub bsid7: Vec<i64>,
    pub bsid8: Vec<i64>,
}
impl SET_BEAM_INTERSECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BEAM_INTERSECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid1: t.take("BSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid2: t.take("BSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid3: t.take("BSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid4: t.take("BSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid5: t.take("BSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid6: t.take("BSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid7: t.take("BSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            bsid8: t.take("BSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_BOX {
    pub sid: Vec<i64>,
    pub bid1: Vec<i64>,
    pub bid2: Vec<i64>,
    pub bid3: Vec<i64>,
    pub bid4: Vec<i64>,
    pub bid5: Vec<i64>,
    pub bid6: Vec<i64>,
    pub bid7: Vec<i64>,
    pub bid8: Vec<i64>,
}
impl SET_BOX {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_BOX").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            bid1: t.take("BID1").and_then(|c| c.into_int()).unwrap_or_default(),
            bid2: t.take("BID2").and_then(|c| c.into_int()).unwrap_or_default(),
            bid3: t.take("BID3").and_then(|c| c.into_int()).unwrap_or_default(),
            bid4: t.take("BID4").and_then(|c| c.into_int()).unwrap_or_default(),
            bid5: t.take("BID5").and_then(|c| c.into_int()).unwrap_or_default(),
            bid6: t.take("BID6").and_then(|c| c.into_int()).unwrap_or_default(),
            bid7: t.take("BID7").and_then(|c| c.into_int()).unwrap_or_default(),
            bid8: t.take("BID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE {
    pub sid: Vec<i64>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_DISCRETE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_ADD {
    pub sid: Vec<i64>,
    pub dsid1: Vec<i64>,
    pub dsid2: Vec<i64>,
    pub dsid3: Vec<i64>,
    pub dsid4: Vec<i64>,
    pub dsid5: Vec<i64>,
    pub dsid6: Vec<i64>,
    pub dsid7: Vec<i64>,
    pub dsid8: Vec<i64>,
}
impl SET_DISCRETE_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid1: t.take("DSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid2: t.take("DSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid3: t.take("DSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid4: t.take("DSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid5: t.take("DSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid6: t.take("DSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid7: t.take("DSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid8: t.take("DSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_COLLECT {
    pub sid: Vec<i64>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_DISCRETE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_GENERAL {
    pub sid: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_DISCRETE_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_DISCRETE_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_GENERATE {
    pub sid: Vec<i64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_DISCRETE_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_DISCRETE_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_DISCRETE_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_DISCRETE_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_UVW_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_EDGE_XYZ_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub fid1: Vec<i64>,
    pub fid2: Vec<i64>,
    pub fid3: Vec<i64>,
    pub fid4: Vec<i64>,
    pub fid5: Vec<i64>,
    pub fid6: Vec<i64>,
    pub fid7: Vec<i64>,
    pub fid8: Vec<i64>,
}
impl SET_IGA_FACE_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            fid1: t.take("FID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fid2: t.take("FID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fid3: t.take("FID3").and_then(|c| c.into_int()).unwrap_or_default(),
            fid4: t.take("FID4").and_then(|c| c.into_int()).unwrap_or_default(),
            fid5: t.take("FID5").and_then(|c| c.into_int()).unwrap_or_default(),
            fid6: t.take("FID6").and_then(|c| c.into_int()).unwrap_or_default(),
            fid7: t.take("FID7").and_then(|c| c.into_int()).unwrap_or_default(),
            fid8: t.take("FID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_FACE_UVW_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub fid1: Vec<i64>,
    pub fid2: Vec<i64>,
    pub fid3: Vec<i64>,
    pub fid4: Vec<i64>,
    pub fid5: Vec<i64>,
    pub fid6: Vec<i64>,
    pub fid7: Vec<i64>,
    pub fid8: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            fid1: t.take("FID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fid2: t.take("FID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fid3: t.take("FID3").and_then(|c| c.into_int()).unwrap_or_default(),
            fid4: t.take("FID4").and_then(|c| c.into_int()).unwrap_or_default(),
            fid5: t.take("FID5").and_then(|c| c.into_int()).unwrap_or_default(),
            fid6: t.take("FID6").and_then(|c| c.into_int()).unwrap_or_default(),
            fid7: t.take("FID7").and_then(|c| c.into_int()).unwrap_or_default(),
            fid8: t.take("FID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub fid1: Vec<i64>,
    pub fid2: Vec<i64>,
    pub fid3: Vec<i64>,
    pub fid4: Vec<i64>,
    pub fid5: Vec<i64>,
    pub fid6: Vec<i64>,
    pub fid7: Vec<i64>,
    pub fid8: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            fid1: t.take("FID1").and_then(|c| c.into_int()).unwrap_or_default(),
            fid2: t.take("FID2").and_then(|c| c.into_int()).unwrap_or_default(),
            fid3: t.take("FID3").and_then(|c| c.into_int()).unwrap_or_default(),
            fid4: t.take("FID4").and_then(|c| c.into_int()).unwrap_or_default(),
            fid5: t.take("FID5").and_then(|c| c.into_int()).unwrap_or_default(),
            fid6: t.take("FID6").and_then(|c| c.into_int()).unwrap_or_default(),
            fid7: t.take("FID7").and_then(|c| c.into_int()).unwrap_or_default(),
            fid8: t.take("FID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_UVW_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_FACE_XYZ {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_FACE_XYZ_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_IGA_POINT_UVW {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_IGA_POINT_UVW_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_IGA_POINT_UVW_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_MODE {
    pub sid: Vec<i64>,
    pub mid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub mid3: Vec<i64>,
    pub mid4: Vec<i64>,
    pub mid5: Vec<i64>,
    pub mid6: Vec<i64>,
    pub mid7: Vec<i64>,
    pub mid8: Vec<i64>,
}
impl SET_MODE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_MODE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            mid5: t.take("MID5").and_then(|c| c.into_int()).unwrap_or_default(),
            mid6: t.take("MID6").and_then(|c| c.into_int()).unwrap_or_default(),
            mid7: t.take("MID7").and_then(|c| c.into_int()).unwrap_or_default(),
            mid8: t.take("MID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_MODE_LIST {
    pub sid: Vec<i64>,
    pub mid1: Vec<i64>,
    pub mid2: Vec<i64>,
    pub mid3: Vec<i64>,
    pub mid4: Vec<i64>,
    pub mid5: Vec<i64>,
    pub mid6: Vec<i64>,
    pub mid7: Vec<i64>,
    pub mid8: Vec<i64>,
}
impl SET_MODE_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_MODE_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            mid1: t.take("MID1").and_then(|c| c.into_int()).unwrap_or_default(),
            mid2: t.take("MID2").and_then(|c| c.into_int()).unwrap_or_default(),
            mid3: t.take("MID3").and_then(|c| c.into_int()).unwrap_or_default(),
            mid4: t.take("MID4").and_then(|c| c.into_int()).unwrap_or_default(),
            mid5: t.take("MID5").and_then(|c| c.into_int()).unwrap_or_default(),
            mid6: t.take("MID6").and_then(|c| c.into_int()).unwrap_or_default(),
            mid7: t.take("MID7").and_then(|c| c.into_int()).unwrap_or_default(),
            mid8: t.take("MID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_MODE_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub m1beg: Vec<i64>,
    pub m1end: Vec<i64>,
    pub m2beg: Vec<i64>,
    pub m2end: Vec<i64>,
    pub m3beg: Vec<i64>,
    pub m3end: Vec<i64>,
    pub m4beg: Vec<i64>,
    pub m4end: Vec<i64>,
}
impl SET_MODE_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_MODE_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            m1beg: t.take("M1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m1end: t.take("M1END").and_then(|c| c.into_int()).unwrap_or_default(),
            m2beg: t.take("M2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m2end: t.take("M2END").and_then(|c| c.into_int()).unwrap_or_default(),
            m3beg: t.take("M3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m3end: t.take("M3END").and_then(|c| c.into_int()).unwrap_or_default(),
            m4beg: t.take("M4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            m4end: t.take("M4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_MULTI_MATERIAL_GROUP_LIST {
    pub ammsid: Vec<i64>,
    pub ammgid1: Vec<i64>,
    pub ammgid2: Vec<i64>,
    pub ammgid3: Vec<i64>,
    pub ammgid4: Vec<i64>,
    pub ammgid5: Vec<i64>,
    pub ammgid6: Vec<i64>,
    pub ammgid7: Vec<i64>,
    pub ammgid8: Vec<i64>,
}
impl SET_MULTI_MATERIAL_GROUP_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_MULTI-MATERIAL_GROUP_LIST").unwrap());
        Self {
            ammsid: t.take("AMMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid1: t.take("AMMGID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid2: t.take("AMMGID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid3: t.take("AMMGID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid4: t.take("AMMGID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid5: t.take("AMMGID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid6: t.take("AMMGID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid7: t.take("AMMGID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid8: t.take("AMMGID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_MULTI_MATERIAL_GROUP_LIST_GPNAME {
    pub ammsid: Vec<i64>,
    pub ammgid1: Vec<String>,
    pub ammgid2: Vec<String>,
    pub ammgid3: Vec<String>,
    pub ammgid4: Vec<String>,
    pub ammgid5: Vec<String>,
    pub ammgid6: Vec<String>,
    pub ammgid7: Vec<String>,
    pub ammgid8: Vec<String>,
}
impl SET_MULTI_MATERIAL_GROUP_LIST_GPNAME {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_MULTI_MATERIAL_GROUP_LIST_GPNAME").unwrap());
        Self {
            ammsid: t.take("AMMSID").and_then(|c| c.into_int()).unwrap_or_default(),
            ammgid1: t.take("AMMGID1").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid2: t.take("AMMGID2").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid3: t.take("AMMGID3").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid4: t.take("AMMGID4").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid5: t.take("AMMGID5").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid6: t.take("AMMGID6").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid7: t.take("AMMGID7").and_then(|c| c.into_str()).unwrap_or_default(),
            ammgid8: t.take("AMMGID8").and_then(|c| c.into_str()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_ADD {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub nsid1: Vec<i64>,
    pub nsid2: Vec<i64>,
    pub nsid3: Vec<i64>,
    pub nsid4: Vec<i64>,
    pub nsid5: Vec<i64>,
    pub nsid6: Vec<i64>,
    pub nsid7: Vec<i64>,
    pub nsid8: Vec<i64>,
}
impl SET_NODE_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid3: t.take("NSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid4: t.take("NSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid5: t.take("NSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid6: t.take("NSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid7: t.take("NSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid8: t.take("NSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_ADD_ADVANCED {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub sid1: Vec<i64>,
    pub type1: Vec<i64>,
    pub sid2: Vec<i64>,
    pub type2: Vec<i64>,
    pub sid3: Vec<i64>,
    pub type3: Vec<i64>,
    pub sid4: Vec<i64>,
    pub type4: Vec<i64>,
}
impl SET_NODE_ADD_ADVANCED {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_ADD_ADVANCED").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            sid1: t.take("SID1").and_then(|c| c.into_int()).unwrap_or_default(),
            type1: t.take("TYPE1").and_then(|c| c.into_int()).unwrap_or_default(),
            sid2: t.take("SID2").and_then(|c| c.into_int()).unwrap_or_default(),
            type2: t.take("TYPE2").and_then(|c| c.into_int()).unwrap_or_default(),
            sid3: t.take("SID3").and_then(|c| c.into_int()).unwrap_or_default(),
            type3: t.take("TYPE3").and_then(|c| c.into_int()).unwrap_or_default(),
            sid4: t.take("SID4").and_then(|c| c.into_int()).unwrap_or_default(),
            type4: t.take("TYPE4").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_COLUMN {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_NODE_COLUMN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_COLUMN").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_COLUMN_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_NODE_COLUMN_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_COLUMN_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid: t.take("NID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_GENERAL {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_NODE_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_NODE_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_INTERSECT {
    pub nsid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub nsid1: Vec<i64>,
    pub nsid2: Vec<i64>,
    pub nsid3: Vec<i64>,
    pub nsid4: Vec<i64>,
    pub nsid5: Vec<i64>,
    pub nsid6: Vec<i64>,
    pub nsid7: Vec<i64>,
    pub nsid8: Vec<i64>,
}
impl SET_NODE_INTERSECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_INTERSECT").unwrap());
        Self {
            nsid: t.take("NSID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            nsid1: t.take("NSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid2: t.take("NSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid3: t.take("NSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid4: t.take("NSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid5: t.take("NSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid6: t.take("NSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid7: t.take("NSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nsid8: t.take("NSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl SET_NODE_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl SET_NODE_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_NODE_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_NODE_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_NODE_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_NODE_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_SMOOTH {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl SET_NODE_LIST_SMOOTH {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_SMOOTH").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_NODE_LIST_SMOOTH_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<String>,
    pub f: Vec<String>,
    pub nid1: Vec<i64>,
    pub nid2: Vec<i64>,
    pub nid3: Vec<i64>,
    pub nid4: Vec<i64>,
    pub nid5: Vec<i64>,
    pub nid6: Vec<i64>,
    pub nid7: Vec<i64>,
    pub nid8: Vec<i64>,
}
impl SET_NODE_LIST_SMOOTH_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_NODE_LIST_SMOOTH_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_str()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_str()).unwrap_or_default(),
            nid1: t.take("NID1").and_then(|c| c.into_int()).unwrap_or_default(),
            nid2: t.take("NID2").and_then(|c| c.into_int()).unwrap_or_default(),
            nid3: t.take("NID3").and_then(|c| c.into_int()).unwrap_or_default(),
            nid4: t.take("NID4").and_then(|c| c.into_int()).unwrap_or_default(),
            nid5: t.take("NID5").and_then(|c| c.into_int()).unwrap_or_default(),
            nid6: t.take("NID6").and_then(|c| c.into_int()).unwrap_or_default(),
            nid7: t.take("NID7").and_then(|c| c.into_int()).unwrap_or_default(),
            nid8: t.take("NID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_ADD {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub psid1: Vec<i64>,
    pub psid2: Vec<i64>,
    pub psid3: Vec<i64>,
    pub psid4: Vec<i64>,
    pub psid5: Vec<i64>,
    pub psid6: Vec<i64>,
    pub psid7: Vec<i64>,
    pub psid8: Vec<i64>,
}
impl SET_PART_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            psid1: t.take("PSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            psid2: t.take("PSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            psid3: t.take("PSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            psid4: t.take("PSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            psid5: t.take("PSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            psid6: t.take("PSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            psid7: t.take("PSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            psid8: t.take("PSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_PART_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_COLUMN {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_PART_COLUMN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_COLUMN").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_COLUMN_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_PART_COLUMN_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_COLUMN_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_PART_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub pid1: Vec<i64>,
    pub pid2: Vec<i64>,
    pub pid3: Vec<i64>,
    pub pid4: Vec<i64>,
    pub pid5: Vec<i64>,
    pub pid6: Vec<i64>,
    pub pid7: Vec<i64>,
    pub pid8: Vec<i64>,
}
impl SET_PART_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            pid1: t.take("PID1").and_then(|c| c.into_int()).unwrap_or_default(),
            pid2: t.take("PID2").and_then(|c| c.into_int()).unwrap_or_default(),
            pid3: t.take("PID3").and_then(|c| c.into_int()).unwrap_or_default(),
            pid4: t.take("PID4").and_then(|c| c.into_int()).unwrap_or_default(),
            pid5: t.take("PID5").and_then(|c| c.into_int()).unwrap_or_default(),
            pid6: t.take("PID6").and_then(|c| c.into_int()).unwrap_or_default(),
            pid7: t.take("PID7").and_then(|c| c.into_int()).unwrap_or_default(),
            pid8: t.take("PID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_PART_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_PART_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_PART_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_PART_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PART_TREE {
    pub brid: Vec<i64>,
    pub heading: Vec<String>,
    pub compi: Vec<i64>,
    pub compi_2: Vec<i64>,
    pub compi_3: Vec<i64>,
    pub compi_4: Vec<i64>,
    pub compi_5: Vec<i64>,
    pub compi_6: Vec<i64>,
    pub compi_7: Vec<i64>,
    pub compi_8: Vec<i64>,
}
impl SET_PART_TREE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PART_TREE").unwrap());
        Self {
            brid: t.take("BRID").and_then(|c| c.into_int()).unwrap_or_default(),
            heading: t.take("HEADING").and_then(|c| c.into_str()).unwrap_or_default(),
            compi: t.take("COMPi").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_2: t.take("COMPi_2").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_3: t.take("COMPi_3").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_4: t.take("COMPi_4").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_5: t.take("COMPi_5").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_6: t.take("COMPi_6").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_7: t.take("COMPi_7").and_then(|c| c.into_int()).unwrap_or_default(),
            compi_8: t.take("COMPi_8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_PERI_LAMINATE {
    pub sid: Vec<i64>,
    pub pid: Vec<i64>,
    pub a1: Vec<f64>,
    pub t1: Vec<f64>,
    pub pid_2: Vec<i64>,
    pub a2: Vec<f64>,
    pub t2: Vec<f64>,
}
impl SET_PERI_LAMINATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_PERI_LAMINATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            pid: t.take("PID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            t1: t.take("T1").and_then(|c| c.into_float()).unwrap_or_default(),
            pid_2: t.take("PID_2").and_then(|c| c.into_int()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            t2: t.take("T2").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_POINT_LIST {
    pub sid: Vec<i64>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
    pub z: Vec<f64>,
}
impl SET_POINT_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_POINT_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            x: t.take("X").and_then(|c| c.into_float()).unwrap_or_default(),
            y: t.take("Y").and_then(|c| c.into_float()).unwrap_or_default(),
            z: t.take("Z").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_POROUS_ALE {
    pub eidbeg: Vec<i64>,
    pub eidend: Vec<i64>,
    pub local: Vec<i64>,
    pub veccid1: Vec<i64>,
    pub veccid2: Vec<i64>,
    pub userdef: Vec<i64>,
    pub axx: Vec<f64>,
    pub axy: Vec<f64>,
    pub axz: Vec<f64>,
    pub bxx: Vec<f64>,
    pub bxy: Vec<f64>,
    pub bxz: Vec<f64>,
    pub ayx: Vec<f64>,
    pub ayy: Vec<f64>,
    pub ayz: Vec<f64>,
    pub byx: Vec<f64>,
    pub byy: Vec<f64>,
    pub byz: Vec<f64>,
    pub azx: Vec<f64>,
    pub azy: Vec<f64>,
    pub azz: Vec<f64>,
    pub bzx: Vec<f64>,
    pub bzy: Vec<f64>,
    pub bzz: Vec<f64>,
}
impl SET_POROUS_ALE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_POROUS_ALE").unwrap());
        Self {
            eidbeg: t.take("EIDBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            eidend: t.take("EIDEND").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            veccid1: t.take("VECCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            veccid2: t.take("VECCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            userdef: t.take("USERDEF").and_then(|c| c.into_int()).unwrap_or_default(),
            axx: t.take("Axx").and_then(|c| c.into_float()).unwrap_or_default(),
            axy: t.take("Axy").and_then(|c| c.into_float()).unwrap_or_default(),
            axz: t.take("Axz").and_then(|c| c.into_float()).unwrap_or_default(),
            bxx: t.take("Bxx").and_then(|c| c.into_float()).unwrap_or_default(),
            bxy: t.take("Bxy").and_then(|c| c.into_float()).unwrap_or_default(),
            bxz: t.take("Bxz").and_then(|c| c.into_float()).unwrap_or_default(),
            ayx: t.take("Ayx").and_then(|c| c.into_float()).unwrap_or_default(),
            ayy: t.take("Ayy").and_then(|c| c.into_float()).unwrap_or_default(),
            ayz: t.take("Ayz").and_then(|c| c.into_float()).unwrap_or_default(),
            byx: t.take("Byx").and_then(|c| c.into_float()).unwrap_or_default(),
            byy: t.take("Byy").and_then(|c| c.into_float()).unwrap_or_default(),
            byz: t.take("Byz").and_then(|c| c.into_float()).unwrap_or_default(),
            azx: t.take("Azx").and_then(|c| c.into_float()).unwrap_or_default(),
            azy: t.take("Azy").and_then(|c| c.into_float()).unwrap_or_default(),
            azz: t.take("Azz").and_then(|c| c.into_float()).unwrap_or_default(),
            bzx: t.take("Bzx").and_then(|c| c.into_float()).unwrap_or_default(),
            bzy: t.take("Bzy").and_then(|c| c.into_float()).unwrap_or_default(),
            bzz: t.take("Bzz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_POROUS_LAGRANGIAN {
    pub eidbeg: Vec<i64>,
    pub eidend: Vec<i64>,
    pub local: Vec<i64>,
    pub veccid1: Vec<i64>,
    pub veccid2: Vec<i64>,
    pub userdef: Vec<i64>,
    pub axx: Vec<f64>,
    pub axy: Vec<f64>,
    pub axz: Vec<f64>,
    pub bxx: Vec<f64>,
    pub bxy: Vec<f64>,
    pub bxz: Vec<f64>,
    pub ayx: Vec<f64>,
    pub ayy: Vec<f64>,
    pub ayz: Vec<f64>,
    pub byx: Vec<f64>,
    pub byy: Vec<f64>,
    pub byz: Vec<f64>,
    pub azx: Vec<f64>,
    pub azy: Vec<f64>,
    pub azz: Vec<f64>,
    pub bzx: Vec<f64>,
    pub bzy: Vec<f64>,
    pub bzz: Vec<f64>,
}
impl SET_POROUS_LAGRANGIAN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_POROUS_LAGRANGIAN").unwrap());
        Self {
            eidbeg: t.take("EIDBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            eidend: t.take("EIDEND").and_then(|c| c.into_int()).unwrap_or_default(),
            local: t.take("LOCAL").and_then(|c| c.into_int()).unwrap_or_default(),
            veccid1: t.take("VECCID1").and_then(|c| c.into_int()).unwrap_or_default(),
            veccid2: t.take("VECCID2").and_then(|c| c.into_int()).unwrap_or_default(),
            userdef: t.take("USERDEF").and_then(|c| c.into_int()).unwrap_or_default(),
            axx: t.take("Axx").and_then(|c| c.into_float()).unwrap_or_default(),
            axy: t.take("Axy").and_then(|c| c.into_float()).unwrap_or_default(),
            axz: t.take("Axz").and_then(|c| c.into_float()).unwrap_or_default(),
            bxx: t.take("Bxx").and_then(|c| c.into_float()).unwrap_or_default(),
            bxy: t.take("Bxy").and_then(|c| c.into_float()).unwrap_or_default(),
            bxz: t.take("Bxz").and_then(|c| c.into_float()).unwrap_or_default(),
            ayx: t.take("Ayx").and_then(|c| c.into_float()).unwrap_or_default(),
            ayy: t.take("Ayy").and_then(|c| c.into_float()).unwrap_or_default(),
            ayz: t.take("Ayz").and_then(|c| c.into_float()).unwrap_or_default(),
            byx: t.take("Byx").and_then(|c| c.into_float()).unwrap_or_default(),
            byy: t.take("Byy").and_then(|c| c.into_float()).unwrap_or_default(),
            byz: t.take("Byz").and_then(|c| c.into_float()).unwrap_or_default(),
            azx: t.take("Azx").and_then(|c| c.into_float()).unwrap_or_default(),
            azy: t.take("Azy").and_then(|c| c.into_float()).unwrap_or_default(),
            azz: t.take("Azz").and_then(|c| c.into_float()).unwrap_or_default(),
            bzx: t.take("Bzx").and_then(|c| c.into_float()).unwrap_or_default(),
            bzy: t.take("Bzy").and_then(|c| c.into_float()).unwrap_or_default(),
            bzz: t.take("Bzz").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<i64>,
    pub f: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_SEGMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT_ADD {
    pub sid: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub ssid3: Vec<i64>,
    pub ssid4: Vec<i64>,
    pub ssid5: Vec<i64>,
    pub ssid6: Vec<i64>,
    pub ssid7: Vec<i64>,
    pub ssid8: Vec<i64>,
}
impl SET_SEGMENT_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid3: t.take("SSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid4: t.take("SSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid5: t.take("SSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid6: t.take("SSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid7: t.take("SSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid8: t.take("SSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<i64>,
    pub f: Vec<i64>,
    pub n1: Vec<i64>,
    pub n2: Vec<i64>,
    pub n3: Vec<i64>,
    pub n4: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_SEGMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            n1: t.take("N1").and_then(|c| c.into_int()).unwrap_or_default(),
            n2: t.take("N2").and_then(|c| c.into_int()).unwrap_or_default(),
            n3: t.take("N3").and_then(|c| c.into_int()).unwrap_or_default(),
            n4: t.take("N4").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT_GENERAL {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<i64>,
    pub f: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SEGMENT_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub solver: Vec<String>,
    pub its: Vec<i64>,
    pub f: Vec<i64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SEGMENT_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            its: t.take("ITS").and_then(|c| c.into_int()).unwrap_or_default(),
            f: t.take("-").and_then(|c| c.into_int()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SEGMENT_INTERSECT {
    pub sid: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub ssid3: Vec<i64>,
    pub ssid4: Vec<i64>,
    pub ssid5: Vec<i64>,
    pub ssid6: Vec<i64>,
    pub ssid7: Vec<i64>,
    pub ssid8: Vec<i64>,
}
impl SET_SEGMENT_INTERSECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SEGMENT_INTERSECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid3: t.take("SSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid4: t.take("SSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid5: t.take("SSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid6: t.take("SSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid7: t.take("SSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid8: t.take("SSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_ADD {
    pub sid: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub ssid3: Vec<i64>,
    pub ssid4: Vec<i64>,
    pub ssid5: Vec<i64>,
    pub ssid6: Vec<i64>,
    pub ssid7: Vec<i64>,
    pub ssid8: Vec<i64>,
}
impl SET_SHELL_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid3: t.take("SSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid4: t.take("SSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid5: t.take("SSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid6: t.take("SSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid7: t.take("SSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid8: t.take("SSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_COLUMN {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub eid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_SHELL_COLUMN {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_COLUMN").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_COLUMN_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub eid: Vec<i64>,
    pub a1: Vec<f64>,
    pub a2: Vec<f64>,
    pub a3: Vec<f64>,
    pub a4: Vec<f64>,
}
impl SET_SHELL_COLUMN_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_COLUMN_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            eid: t.take("EID").and_then(|c| c.into_int()).unwrap_or_default(),
            a1: t.take("A1").and_then(|c| c.into_float()).unwrap_or_default(),
            a2: t.take("A2").and_then(|c| c.into_float()).unwrap_or_default(),
            a3: t.take("A3").and_then(|c| c.into_float()).unwrap_or_default(),
            a4: t.take("A4").and_then(|c| c.into_float()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_GENERAL {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SHELL_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SHELL_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_INTERSECT {
    pub sid: Vec<i64>,
    pub ssid1: Vec<i64>,
    pub ssid2: Vec<i64>,
    pub ssid3: Vec<i64>,
    pub ssid4: Vec<i64>,
    pub ssid5: Vec<i64>,
    pub ssid6: Vec<i64>,
    pub ssid7: Vec<i64>,
    pub ssid8: Vec<i64>,
}
impl SET_SHELL_INTERSECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_INTERSECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid1: t.take("SSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid2: t.take("SSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid3: t.take("SSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid4: t.take("SSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid5: t.take("SSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid6: t.take("SSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid7: t.take("SSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            ssid8: t.take("SSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_SHELL_LIST {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub eid1: Vec<i64>,
    pub eid2: Vec<i64>,
    pub eid3: Vec<i64>,
    pub eid4: Vec<i64>,
    pub eid5: Vec<i64>,
    pub eid6: Vec<i64>,
    pub eid7: Vec<i64>,
    pub eid8: Vec<i64>,
}
impl SET_SHELL_LIST_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            eid1: t.take("EID1").and_then(|c| c.into_int()).unwrap_or_default(),
            eid2: t.take("EID2").and_then(|c| c.into_int()).unwrap_or_default(),
            eid3: t.take("EID3").and_then(|c| c.into_int()).unwrap_or_default(),
            eid4: t.take("EID4").and_then(|c| c.into_int()).unwrap_or_default(),
            eid5: t.take("EID5").and_then(|c| c.into_int()).unwrap_or_default(),
            eid6: t.take("EID6").and_then(|c| c.into_int()).unwrap_or_default(),
            eid7: t.take("EID7").and_then(|c| c.into_int()).unwrap_or_default(),
            eid8: t.take("EID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST_GENERATE {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_SHELL_LIST_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_SHELL_LIST_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_SHELL_LIST_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SHELL_LIST_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub da1: Vec<f64>,
    pub da2: Vec<f64>,
    pub da3: Vec<f64>,
    pub da4: Vec<f64>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_SHELL_LIST_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SHELL_LIST_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            da1: t.take("DA1").and_then(|c| c.into_float()).unwrap_or_default(),
            da2: t.take("DA2").and_then(|c| c.into_float()).unwrap_or_default(),
            da3: t.take("DA3").and_then(|c| c.into_float()).unwrap_or_default(),
            da4: t.take("DA4").and_then(|c| c.into_float()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_SOLID {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_ADD {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub dsid1: Vec<i64>,
    pub dsid2: Vec<i64>,
    pub dsid3: Vec<i64>,
    pub dsid4: Vec<i64>,
    pub dsid5: Vec<i64>,
    pub dsid6: Vec<i64>,
    pub dsid7: Vec<i64>,
    pub dsid8: Vec<i64>,
}
impl SET_SOLID_ADD {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_ADD").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            dsid1: t.take("DSID1").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid2: t.take("DSID2").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid3: t.take("DSID3").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid4: t.take("DSID4").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid5: t.take("DSID5").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid6: t.take("DSID6").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid7: t.take("DSID7").and_then(|c| c.into_int()).unwrap_or_default(),
            dsid8: t.take("DSID8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_COLLECT {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub k1: Vec<i64>,
    pub k2: Vec<i64>,
    pub k3: Vec<i64>,
    pub k4: Vec<i64>,
    pub k5: Vec<i64>,
    pub k6: Vec<i64>,
    pub k7: Vec<i64>,
    pub k8: Vec<i64>,
}
impl SET_SOLID_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            k1: t.take("K1").and_then(|c| c.into_int()).unwrap_or_default(),
            k2: t.take("K2").and_then(|c| c.into_int()).unwrap_or_default(),
            k3: t.take("K3").and_then(|c| c.into_int()).unwrap_or_default(),
            k4: t.take("K4").and_then(|c| c.into_int()).unwrap_or_default(),
            k5: t.take("K5").and_then(|c| c.into_int()).unwrap_or_default(),
            k6: t.take("K6").and_then(|c| c.into_int()).unwrap_or_default(),
            k7: t.take("K7").and_then(|c| c.into_int()).unwrap_or_default(),
            k8: t.take("K8").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERAL {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SOLID_GENERAL {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERAL").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERAL_COLLECT {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub option: Vec<String>,
    pub e1: Vec<i64>,
    pub e2: Vec<i64>,
    pub e3: Vec<i64>,
    pub e4: Vec<i64>,
    pub e5: Vec<i64>,
    pub e6: Vec<i64>,
    pub e7: Vec<i64>,
}
impl SET_SOLID_GENERAL_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERAL_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            option: t.take("OPTION").and_then(|c| c.into_str()).unwrap_or_default(),
            e1: t.take("E1").and_then(|c| c.into_int()).unwrap_or_default(),
            e2: t.take("E2").and_then(|c| c.into_int()).unwrap_or_default(),
            e3: t.take("E3").and_then(|c| c.into_int()).unwrap_or_default(),
            e4: t.take("E4").and_then(|c| c.into_int()).unwrap_or_default(),
            e5: t.take("E5").and_then(|c| c.into_int()).unwrap_or_default(),
            e6: t.take("E6").and_then(|c| c.into_int()).unwrap_or_default(),
            e7: t.take("E7").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERATE {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_SOLID_GENERATE {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERATE").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERATE_COLLECT {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub b1beg: Vec<i64>,
    pub b1end: Vec<i64>,
    pub b2beg: Vec<i64>,
    pub b2end: Vec<i64>,
    pub b3beg: Vec<i64>,
    pub b3end: Vec<i64>,
    pub b4beg: Vec<i64>,
    pub b4end: Vec<i64>,
}
impl SET_SOLID_GENERATE_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERATE_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            b1beg: t.take("B1BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b1end: t.take("B1END").and_then(|c| c.into_int()).unwrap_or_default(),
            b2beg: t.take("B2BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b2end: t.take("B2END").and_then(|c| c.into_int()).unwrap_or_default(),
            b3beg: t.take("B3BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b3end: t.take("B3END").and_then(|c| c.into_int()).unwrap_or_default(),
            b4beg: t.take("B4BEG").and_then(|c| c.into_int()).unwrap_or_default(),
            b4end: t.take("B4END").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERATE_INCREMENT {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_SOLID_GENERATE_INCREMENT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERATE_INCREMENT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}

pub struct SET_SOLID_GENERATE_INCREMENT_COLLECT {
    pub sid: Vec<i64>,
    pub solver: Vec<String>,
    pub bbeg: Vec<i64>,
    pub bend: Vec<i64>,
    pub incr: Vec<i64>,
}
impl SET_SOLID_GENERATE_INCREMENT_COLLECT {
    pub fn parse(p: &ParsedFile) -> Self {
        let mut t = parse_schema(p, &super::schema("SET_SOLID_GENERATE_INCREMENT_COLLECT").unwrap());
        Self {
            sid: t.take("SID").and_then(|c| c.into_int()).unwrap_or_default(),
            solver: t.take("SOLVER").and_then(|c| c.into_str()).unwrap_or_default(),
            bbeg: t.take("BBEG").and_then(|c| c.into_int()).unwrap_or_default(),
            bend: t.take("BEND").and_then(|c| c.into_int()).unwrap_or_default(),
            incr: t.take("INCR").and_then(|c| c.into_int()).unwrap_or_default(),
        }
    }
}
