[package]
name = "dynars-derive"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true
description = "Derive macro for dynars keyword schemas — #[derive(Keyword)] / #[derive(Card)]."

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
